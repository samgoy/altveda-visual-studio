import { type ApplyVisualControlCodemod } from '@remotion/studio-shared';
export declare const applyVisualControl: ({ input, transformation, }: {
    input: string;
    transformation: ApplyVisualControlCodemod;
}) => {
    newContents: string;
    changesMade: {
        description: string;
    }[];
};
