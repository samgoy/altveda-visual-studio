import type { RecastCodemod } from '@remotion/studio-shared';
import { type Change } from './recast-mods';
type FolderCodemod = Extract<RecastCodemod, {
    type: 'delete-folder' | 'move-composition-or-folder' | 'move-composition-to-folder' | 'new-folder' | 'rename-folder';
}>;
export declare const editFolderInSource: ({ codeMod, input, }: {
    codeMod: FolderCodemod;
    input: string;
}) => {
    changesMade: Change[];
    newContents: string;
};
export {};
