export declare const formatSerializedValue: ({ input, linePrefix, previousValue, serialized, }: {
    input: string;
    linePrefix: string;
    previousValue: string | null;
    serialized: string;
}) => string;
