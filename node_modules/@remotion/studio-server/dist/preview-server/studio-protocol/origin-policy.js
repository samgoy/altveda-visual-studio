"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleStudioProtocolOptions = exports.setStudioProtocolCorsHeaders = exports.getAllowedStudioProtocolOrigin = void 0;
const isLoopbackHttp = (url) => url.protocol === 'http:' &&
    (url.hostname === 'localhost' || url.hostname === '127.0.0.1');
const getAllowedStudioProtocolOrigin = (request) => {
    try {
        const { origin, referer, host } = request.headers;
        if (origin === undefined) {
            // Browsers omit Origin on same-origin GET requests. Only trust a
            // Referer matching this local HTTP Studio, never an arbitrary site.
            const refererUrl = new URL(referer !== null && referer !== void 0 ? referer : '');
            return isLoopbackHttp(refererUrl) &&
                refererUrl.origin === `http://${host}`
                ? refererUrl.origin
                : null;
        }
        const url = new URL(origin);
        if (url.protocol !== 'https:' && !isLoopbackHttp(url)) {
            return null;
        }
        return url.origin;
    }
    catch (_a) {
        return null;
    }
};
exports.getAllowedStudioProtocolOrigin = getAllowedStudioProtocolOrigin;
const setStudioProtocolCorsHeaders = ({ request, response, }) => {
    const origin = (0, exports.getAllowedStudioProtocolOrigin)(request);
    if (origin === null) {
        return;
    }
    response.setHeader('Access-Control-Allow-Origin', origin);
    response.setHeader('Vary', 'Origin, Access-Control-Request-Headers, Access-Control-Request-Private-Network');
    response.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS');
    response.setHeader('Access-Control-Allow-Headers', 'Content-Type');
    response.setHeader('Access-Control-Max-Age', '600');
    response.setHeader('Access-Control-Allow-Private-Network', 'true');
};
exports.setStudioProtocolCorsHeaders = setStudioProtocolCorsHeaders;
const handleStudioProtocolOptions = ({ request, response, }) => {
    (0, exports.setStudioProtocolCorsHeaders)({ request, response });
    const origin = (0, exports.getAllowedStudioProtocolOrigin)(request);
    response.writeHead(origin === null ? 403 : 204);
    response.end();
    return Promise.resolve();
};
exports.handleStudioProtocolOptions = handleStudioProtocolOptions;
