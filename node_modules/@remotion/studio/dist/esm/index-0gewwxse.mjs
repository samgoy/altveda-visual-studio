import {
  GearIcon,
  NumberSetting,
  OptionExplainerBubble,
  WebRendererCustomOptionExplainerBubble,
  WebRendererOptionExplainerBubble,
  getDefaultCaptionOutputName,
  validateCaptionOutputName
} from "./index-f97r9e2g.mjs";
import {
  RenderModalOutputName
} from "./index-x40wwfmt.mjs";
import {
  ActionTooltip,
  AssetFileIcon,
  AudioFileIcon,
  AudioIcon,
  Button,
  CaretDown,
  Checkbox,
  Checkmark,
  CircularProgress,
  CollapsedFolderIcon,
  Combobox,
  CompositionListContext,
  CompositionListProvider,
  CompositionOrStillIcon,
  ConfirmationDialog,
  ContextMenu,
  ContextMenuForTarget,
  CopyIcon,
  DataEditor,
  DismissableModal,
  EDGE_SCROLL_VERTICAL_INCREMENT,
  EditorShowGuidesContext,
  EllipsisIcon,
  ExpandedFolderIcon,
  ExpandedFoldersContext,
  ExpandedTracksGetterContext,
  ExpandedTracksProvider,
  ExpandedTracksSetterContext,
  FastRefreshContext,
  FileIcon,
  FilmIcon,
  FolderContext,
  FolderContextProvider,
  ForceSpecificCursor,
  FullscreenIcon,
  InfoBubble,
  InitialCompositionLoader,
  InlineDropdown,
  InlineRemoveButton,
  InputDragger,
  InspectorOpenInEditor,
  InspectorQuickAction,
  LicenseExplanation,
  MenuContent,
  ModalButton,
  ModalFooterContainer,
  ModalHeader,
  ModelsIcon,
  NotificationCenter,
  ObserveDefaultProps,
  OptionsPanel,
  PicIcon,
  PreviewServerConnection,
  RemTextarea,
  RemotionInput,
  RenderModalHr,
  RenderQueueOpenInFinderItem,
  RightAlignInput,
  ScaleLockContext,
  ScaleLockProvider,
  SegmentedButton,
  SegmentedControl,
  SelectedModalContext,
  SetSelectedModalContext,
  SettingsProvider,
  ShortcutHint,
  SidebarContext,
  SidebarContextProvider,
  SnowflakeIcon,
  StaticFilesProvider,
  StillIcon,
  StudioProtocolInternals,
  StudioServerConnectionCtx,
  SuccessIcon,
  TIMELINE_BACKGROUND,
  TIMELINE_BORDER,
  TIMELINE_EXPANDED_SELECTED_BACKGROUND,
  TIMELINE_FRAME_WIDTH_AT_MAX_ZOOM,
  TIMELINE_ITEM_BORDER_BOTTOM,
  TIMELINE_LAYER_FILMSTRIP_HEIGHT,
  TIMELINE_LAYER_HEIGHT_AUDIO,
  TIMELINE_LIST_ITEM_ROW_HEIGHT,
  TIMELINE_MARQUEE_ITEM_ATTR,
  TIMELINE_PADDING,
  TIMELINE_SCRUBBER_ATTR,
  TIMELINE_SELECTED_BACKGROUND,
  TIMELINE_SELECTED_LABEL_BACKGROUND,
  TIMELINE_TICKS_BACKGROUND,
  TIMELINE_VIDEO_INFO_WAVEFORM_HEIGHT,
  TIMELINE_ZOOM_SLIDER_PROPS,
  TRANSFORMERS_PACKAGE,
  Tab,
  Tabs,
  TimelineExpandArrowButton,
  TimelineExpandArrowSpacer,
  TimelineExpandedRow,
  TimelineKeyframeDiamondIcon,
  TimelineKeyframeDragStateProvider,
  TimelineKeyframeEasingLine,
  TimelineKeyframeTracksProvider,
  TimelineLayerEye,
  TimelineLayerEyeSpacer,
  TimelineRowChrome,
  TimelineRowSelectedBackgroundContext,
  TimelineSelectAllKeybindings,
  TimelineSelectableItemsProvider,
  TimelineSelectionProvider,
  TimelineSlider,
  TimelineWidthContext,
  TimelineWidthProvider,
  TimelineZoomContext,
  TimelineZoomCtx,
  Transform3DModeStateContext,
  Transform3DModeStateProvider,
  UploadIcon,
  VerticalTab,
  ZoomPersistor,
  addAssetCacheBust,
  addEffect,
  applySelectedOutlineDragAxisLock,
  applySelectedOutlineTransformOriginAxisLock,
  buildAssetFolderStructure,
  buildTimelineTree,
  calculateTimeline,
  callAddEffectKeyframe,
  callAddKeyframes,
  canEditSelectedOutlineCrop,
  canShowUpdates,
  clampTimelineZoom,
  clearSelectedOutlineDragOverrides,
  clearSelectedOutlineRotationDragOverrides,
  clearSelectedOutlineScaleDragOverrides,
  clearUrl,
  commitPendingInspectorFields,
  compensateTranslateForTransformOrigin,
  compositionListRenderedRef,
  constrainUv,
  copyText,
  createDragAwareDoubleClickTracker,
  createFolderTree,
  cropFieldKeys,
  deriveCanvasContentFromUrl,
  dot,
  downloadBlob,
  duplicateSequencesFromSource,
  ensureFrameIsInViewport,
  explorerSidebarTabs,
  filterTimelineExpandedTree,
  findOriginalPositionInFileAtProperty,
  findTrackForNodePathInfo,
  flattenVisibleTreeNodes,
  forceSpecificCursor,
  formatContextForAgents,
  formatFileLocation,
  getAngleDegrees,
  getAssetActionAvailability,
  getAssetElementFromPath,
  getAssetMetadata,
  getAssetPreviewMetadata,
  getCachedImageMetadata,
  getCachedMediaMetadata,
  getClientRenderProgressMessage,
  getClipboardFigmaHtml,
  getClipboardImageFiles,
  getClipboardSvgMarkup,
  getCompositionContextMenuItems,
  getCompositionMenuItems,
  getCompositionPositionForDrop,
  getConfigureDefaultAppsMenuItems,
  getConnectedCompositions,
  getCopyContextForAgentsMenuItem,
  getCurrentDuration,
  getCurrentFps,
  getCurrentFrame,
  getCurrentZoom,
  getDefaultOpenInTarget,
  getDefaultOutputBaseName,
  getElementPositionForDrop,
  getFileManagerName,
  getFrameFromTimelineDrop,
  getFrameFromX,
  getFrameIncrementFromWidth,
  getFrameWhileScrollingLeft,
  getFrameWhileScrollingRight,
  getFromForDrop,
  getGitMenuItem,
  getGitSourceName,
  getKeyframeDisplayOffset,
  getKeysToExpand,
  getMissingPackages,
  getNavigationWindow,
  getNodeHasKeyframes,
  getNodeKeyframes,
  getOpenInMenuItems,
  getOriginalLocationFromStack,
  getOriginalPosition,
  getOutlineSelectionInteraction,
  getPortal,
  getPreferredEditorId,
  getPreviewFileType,
  getRelativeFileLocation,
  getRenamedStaticFilePath,
  getRotationCursor,
  getRoute,
  getScrollPositionForCursorOnLeftEdge,
  getScrollPositionForCursorOnRightEdge,
  getSelectedCropInfo,
  getSelectedEffectFieldsBySequenceKey,
  getSelectedOutline3DRotationDragValues,
  getSelectedOutlineActiveSchema,
  getSelectedOutlineCropDragChanges,
  getSelectedOutlineCropDragValues,
  getSelectedOutlineCropFollowingTransformOrigin,
  getSelectedOutlineDragChanges,
  getSelectedOutlineDragStates,
  getSelectedOutlineDragValues,
  getSelectedOutlineKeyboardNudgeDeltas,
  getSelectedOutlineKeyboardNudgeDirection,
  getSelectedOutlineRotationCornerInfo,
  getSelectedOutlineRotationDeltaDegrees,
  getSelectedOutlineRotationDragChanges,
  getSelectedOutlineRotationDragStates,
  getSelectedOutlineRotationDragValues,
  getSelectedOutlineRotationPivot,
  getSelectedOutlineScaleDragChanges,
  getSelectedOutlineScaleDragStates,
  getSelectedOutlineScaleDragValues,
  getSelectedOutlineScaleEdgeInfo,
  getSelectedOutlineTransformOriginDragChanges,
  getSelectedOutlineTransformOriginLockedAxis,
  getSelectedRotationInfo,
  getSelectedSequenceKeys,
  getSelectedTimelineExpandedRowKeys,
  getSelectedTransformOriginInfo,
  getSelectedUvHandles,
  getSequenceFreezeFrameMenuItem,
  getSequenceKeysContainingSelection,
  getSequencesWithSelectableOutlines,
  getSourceMap,
  getStaticFileBaseName,
  getStaticFileRenameSelection,
  getTimelineAssetLinkInfo,
  getTimelineColor,
  getTimelineContentWidth,
  getTimelineEasingSegments,
  getTimelineLayerHeight,
  getTimelineMinZoom,
  getTimelineSelectedLabelStyle,
  getTimelineSelectedTrackHighlightStyle,
  getTimelineSelectionFromNodePathInfo,
  getTimelineSequenceSelectionKey,
  getTimelineSequenceSplitEligibility,
  getTimelineWidth,
  getTimelineZoom,
  getTreeRowHeight,
  getUvCoordinateForPoint,
  getUvEllipseInteractiveControls,
  getUvHandleConnectionEllipses,
  getUvHandleConnectionLines,
  getUvHandlePosition,
  getXPositionOfItemInTimelineImperatively,
  hasResolvedStack,
  hasSvgFile,
  importAssets,
  importFigmaClipboard,
  importRemoteAsset,
  inlineCodeSnippet,
  input,
  insertComponent,
  insertComposition,
  insertElement,
  insertExistingAssets,
  insertRemoteAudio,
  insertSvgMarkup,
  installPackages,
  installRequiredPackages,
  isCompositionStill,
  isCursorInViewport,
  isRestoredClientJob,
  isSelectedOutlineDragPastThreshold,
  isTimelineExpandedNodeSelected,
  isTimelineSelectionModifierEvent,
  label,
  loadEditorShowGuidesOption,
  loadGuidesList,
  makeClientRetryPayload,
  makeRetryPayload,
  measureOutlineTargets,
  measureOutlines,
  menuContainerTowardsBottom,
  midpoint,
  noop,
  normalizedToTimelineZoom,
  openFolderKey,
  openGitSource,
  openInCodingAgent,
  openInEditor,
  openOriginalPositionInEditor,
  openTimelineAssetLink,
  optionRow,
  optionsSidebarTabs,
  outerPortal,
  outlinesAreEqual,
  parseCssRotation,
  parseError,
  parseKeyframeFieldFromNodePath,
  parseTransformOrigin,
  parseTranslate,
  parsedTransformOriginToUv,
  persistEditorShowGuidesOption,
  persistExpandedFolders,
  persistGuidesList,
  persistSelectedOptionsSidebarPanel,
  pickFilesToImport,
  pointToString,
  preloadCompositionComponentInfo,
  prepareElementInstall,
  propStatusHas3DTransformValue,
  pushUrl,
  redrawTimelineSliderFast,
  registerClientRender,
  reloadUrl,
  remotion_outputsBase,
  renderFrame,
  resolveFileSource,
  resolvedStackToSymbolicated,
  rightRow,
  rotateFieldKey,
  roundNumericUvEllipseValue,
  roundUvCoordinate,
  saveEffectProp,
  saveOutputFile,
  saveSequenceProps,
  scaleFieldKey,
  scrollToTimelineXOffset,
  scrollableRef,
  sectionHeader,
  selectedOutlineRotationSnapStepDegrees,
  serializeTransformOrigin,
  serializeTranslate,
  setCurrentDuration,
  setCurrentFps,
  setCurrentFrame,
  setCurrentZoom,
  shouldSelectTimelineRowOnPointerDown,
  showNotification,
  sliderAreaRef,
  sliderValueToTimelineZoom,
  slugifyName,
  snapSelectedOutlineRotationDeltaDegrees,
  snapSelectedOutlineTransformOriginUv,
  snapSelectedOutlineUv,
  sortFolderTreeAlphabetically,
  splitParentIntoNameAndParent,
  splitTimelineSequenceFromSource,
  startTimelineEdgeAutoScroll,
  stopForcingSpecificCursor,
  subscribeToSequenceProps,
  timelineNodePathInfoToKey,
  timelineSequenceNodePathToKey,
  timelineVerticalScroll,
  timelineZoomToNormalized,
  timelineZoomToSliderValue,
  toggleBooleanMapKey,
  transformOriginFieldKey,
  translateFieldKey,
  tuplesEqual,
  unsubscribeFromSequenceProps,
  useAssetContextMenuItems,
  useBreakpoint,
  useCachedCompositionComponentInfo,
  useConfigureDefaultApps,
  useConfirmationDialog,
  useCopyFeedback,
  useCurrentTimelineSelectionStateAsRef,
  useDefaultCodingAgentInfo,
  useDeleteTimelineItems,
  useEditorOpening,
  useMediaMetadata,
  useMobileLayout,
  useOpenInMenuApps,
  useOpenSequenceInApps,
  useRenameComposition,
  useRenameSequence,
  useRenameStaticFile,
  useResolveStackAndReactToChange,
  useResolvedStack,
  useRuntimeValue,
  useRuntimeValueSelector,
  useRuntimeValueSnapshots,
  useSelectAsset,
  useSelectComposition,
  useSettings,
  useStaticFiles,
  useStudioConfigRevision,
  useTimelineExpandedTree,
  useTimelineGuideSelection,
  useTimelineKeyframeDrag,
  useTimelineKeyframeDragState,
  useTimelineKeyframeSelection,
  useTimelineMarqueeSelectableItem,
  useTimelineMarqueeSelection,
  useTimelineRowContainsSelection,
  useTimelineRowHighlightBackground,
  useTimelineRowSelection,
  useTimelineSelection,
  useTimelineSequenceHasExpandableContent,
  useUpdateStatus,
  uvsEqual,
  validateCompositionDimension,
  validateCompositionDuration,
  validateCompositionName,
  validatePublicOutputName,
  validateStaticFileRename,
  vectorBetween,
  vectorLength,
  viewportClientXToScrollContentX,
  withRequiredAuxiliaryPackages
} from "./index-bs39wngg.mjs";
import {
  RenderQueueContext,
  RenderQueueContextProvider,
  isClientRenderJob
} from "./index-9x6e1dq0.mjs";
import {
  pause,
  play,
  seek,
  shutDownStudio
} from "./index-4dsz9035.mjs";
import {
  AppsIcon,
  CodingAgentIcon,
  DEFAULT_PROPS_PATH_ACTIVE_CLASSNAME,
  EditorIcon,
  SetVisualControlsContext,
  VisualControlsProvider,
  ZodProvider,
  addSequenceRenderJob,
  addStillRenderJob,
  addVideoRenderJob,
  applyCodemod,
  callApi,
  cancelRenderJob,
  clearInsertedElementSelection,
  getInsertedElementSelection,
  getReleaseNotes,
  getStaticFiles,
  isStudioInteractivityEnabled,
  isStudioSelectionEnabled,
  openInFileExplorer,
  removeRenderJob,
  restartStudio,
  subscribeToFileExistenceWatcher,
  subscribeToInsertedElementSelection,
  takePendingSequenceNodePathMutations,
  unsubscribeFromFileExistenceWatcher,
  useSyncExternalStore
} from "./index-eb5txqp6.mjs";
import {
  canInstallPackages,
  canUseEffectOperations,
  getBrowserStudioEffectOperations,
  getBrowserStudioOperations,
  writeStaticFile
} from "./index-cw0pnpg5.mjs";
import {
  COMPACT_CONTROL_ROW_HEIGHT,
  CheckCircleFilled,
  CloudDownloadIcon,
  FOCUS_VISIBLE_ONLY_CLASS_NAME,
  Flex,
  HORIZONTAL_SCROLLBAR_CLASSNAME,
  HOVERABLE_CLASS_NAME,
  HOVER_GROUP_CLASS_NAME,
  HOVER_GROUP_REVEAL_CLASS_NAME,
  HigherZIndex,
  HighestZIndexProvider,
  InlineAction,
  KeybindingContextProvider,
  MENU_INITIATOR_CLASSNAME,
  ModalContainer,
  NO_HOVER_BACKGROUND_STYLE,
  Row,
  SPACING_UNIT,
  Spacing,
  Spinner,
  TrashIcon,
  VERTICAL_SCROLLBAR_CLASSNAME,
  ValidationMessage,
  WarningTriangle,
  areKeyboardShortcutsDisabled,
  askAIKeyboardShortcutGroup,
  buttonStyle,
  container,
  defaultKeyboardShortcuts,
  flexer,
  formatKeyboardShortcut,
  getKeyboardShortcutAriaKeyShortcuts,
  getKeyboardShortcutLabel,
  getKeyboardShortcutsForAction,
  getMaxModalHeight,
  getMaxModalWidth,
  horizontalLayout,
  horizontalTab,
  hoverableStyle,
  icon,
  iconContainer,
  isMac,
  isMenuItem,
  isPointerSessionRelease,
  keyboardShortcutGroups,
  keyboardShortcutsOverlap,
  leftSidebar,
  makeHoverableCSS,
  optionsPanel,
  outerModalStyle,
  shortcutFromKeyboardEvent,
  startCapturedPointerSession,
  startDeferredCapturedPointerSession,
  useKeybinding,
  useZIndex
} from "./index-cdy8v54v.mjs";
import {
  getStudioAskAIEnabled,
  getStudioBufferStateDelayInMilliseconds,
  getStudioKeyboardShortcutsEnabled,
  getStudioMaxTimelineTracks
} from "./index-5zrb1ft4.mjs";
import {
  BACKGROUND,
  BACKGROUND_HEX,
  BACKGROUND__TRANSPARENT,
  BLACK,
  BLACK_ALPHA_30,
  BLACK_ALPHA_50,
  BLACK_ALPHA_60,
  BLACK_HEX,
  BLUE,
  BLUE_DISABLED,
  BLUE_HOVERED,
  BORDER_BLACK,
  BORDER_BLACK_ALPHA_50,
  BORDER_CURRENT_COLOR,
  BORDER_INFO_BLUE,
  BORDER_TIMELINE_DROP_BLUE,
  BORDER_TIMELINE_MARQUEE_BLUE,
  BORDER_WHITE_ALPHA_12,
  BORDER_WHITE_ALPHA_20,
  CHECKERBOARD_BACKGROUND_IMAGE,
  CURRENT_COLOR,
  CURRENT_COLOR_LOWERCASE,
  ERROR_CODE_FRAME_BACKGROUND,
  ERROR_CODE_FRAME_LINE_BACKGROUND,
  ERROR_LINK_COLOR,
  ERROR_MESSAGE_MASK_IMAGE,
  FAIL_COLOR,
  FOCUS_BOX_SHADOW,
  INFO_BLUE,
  INFO_BLUE_BACKGROUND,
  INPUT_BACKGROUND,
  LIGHT_COLOR,
  LIGHT_TEXT,
  LOOPED_INDICATOR_DROP_SHADOW,
  RED,
  RENDER_STATUS_BACKGROUND,
  RULER_COLOR,
  SELECTED_BACKGROUND,
  SELECTED_GUIDE,
  SELECTED_OUTLINE_DROP_SHADOW,
  SELECTED_OUTLINE_UV_DROP_SHADOW,
  SERVER_DISCONNECTED_BACKGROUND,
  SERVER_DISCONNECTED_SHADOW,
  SHADOW_BLACK,
  TIMELINE_AUDIO_GRADIENT,
  TIMELINE_BACKGROUND_COLOR,
  TIMELINE_BLUE,
  TIMELINE_DROP_BLUE_ALPHA_12,
  TIMELINE_DROP_BLUE_ALPHA_16,
  TIMELINE_MARQUEE_BLUE_ALPHA_16,
  TIMELINE_NEGATIVE_START_BACKGROUND_COLOR,
  TIMELINE_NEGATIVE_START_BORDER_COLOR,
  TIMELINE_TRACK_SEPARATOR,
  TIMELINE_VIDEO_GRADIENT,
  TRANSPARENT,
  UNSELECTED_GUIDE,
  WARNING_COLOR,
  WHITE,
  WHITE_ALPHA_05,
  WHITE_ALPHA_06,
  WHITE_ALPHA_10,
  WHITE_ALPHA_12,
  WHITE_ALPHA_15,
  WHITE_ALPHA_20,
  WHITE_ALPHA_30,
  WHITE_ALPHA_40,
  WHITE_ALPHA_50,
  WHITE_ALPHA_60,
  WHITE_ALPHA_70,
  WHITE_ALPHA_72,
  WHITE_ALPHA_80,
  getBackgroundFromHoverState
} from "./index-k426ye99.mjs";
import {
  __require,
  __toESM
} from "./index-rcv7qkt5.mjs";

// src/Studio.tsx
import { useLayoutEffect as useLayoutEffect30 } from "react";
import { createPortal as createPortal3 } from "react-dom";
import { Internals as Internals116 } from "remotion";

// src/components/Editor.tsx
import { PlayerInternals as PlayerInternals17 } from "@remotion/player";
import React160, { useCallback as useCallback125, useLayoutEffect as useLayoutEffect29, useMemo as useMemo131, useState as useState90 } from "react";
import { Internals as Internals109 } from "remotion";

// src/helpers/studio-fit-padding.ts
import { PlayerInternals } from "@remotion/player";
import { Internals } from "remotion";
var STUDIO_FIT_PADDING = 8;
var getPadding = (length) => {
  return Math.min(STUDIO_FIT_PADDING, Math.max(0, (length - 1) / 2));
};
var getStudioPaddedCanvasSize = (canvasSize) => {
  const horizontalPadding = getPadding(canvasSize.width);
  const verticalPadding = getPadding(canvasSize.height);
  return {
    canvasSize: {
      ...canvasSize,
      width: canvasSize.width - horizontalPadding * 2,
      height: canvasSize.height - verticalPadding * 2
    },
    horizontalPadding,
    verticalPadding
  };
};
var getStudioCurrentScaleContext = (canvasSize) => {
  return {
    type: "canvas-size",
    canvasSize,
    canvasSizeForAuto: getStudioPaddedCanvasSize(canvasSize).canvasSize
  };
};
var calculateStudioScale = ({
  canvasSize,
  compositionHeight,
  compositionWidth,
  previewSize
}) => {
  const paddedCanvasSize = getStudioPaddedCanvasSize(canvasSize).canvasSize;
  return Internals.calculateScale({
    canvasSize: paddedCanvasSize,
    compositionHeight,
    compositionWidth,
    previewSize
  });
};
var calculateStudioCanvasTransformation = ({
  canvasSize,
  compositionHeight,
  compositionWidth,
  previewSize
}) => {
  if (previewSize !== "auto") {
    return PlayerInternals.calculateCanvasTransformation({
      canvasSize,
      compositionHeight,
      compositionWidth,
      previewSize
    });
  }
  const {
    canvasSize: paddedCanvasSize,
    horizontalPadding,
    verticalPadding
  } = getStudioPaddedCanvasSize(canvasSize);
  const transformation = PlayerInternals.calculateCanvasTransformation({
    canvasSize: paddedCanvasSize,
    compositionHeight,
    compositionWidth,
    previewSize
  });
  return {
    ...transformation,
    centerX: transformation.centerX + horizontalPadding,
    centerY: transformation.centerY + verticalPadding
  };
};

// src/state/canvas-ref.ts
import { createContext, createRef } from "react";
var canvasRef = createRef();
var drawRef = createRef();
var RefreshCanvasSizeContext = createContext(null);

// src/components/CanvasCaptureDropHandler.tsx
import { useCallback, useContext, useEffect } from "react";

// src/helpers/get-canvas-capture-import.ts
import { parseCanvasCaptureData } from "@remotion/studio-shared";
import { ALL_FORMATS, BlobSource, Input } from "mediabunny";
var getCanvasCaptureImport = async (file) => {
  const input2 = new Input({
    formats: ALL_FORMATS,
    source: new BlobSource(file)
  });
  try {
    const [metadata, videoTrack] = await Promise.all([
      input2.getMetadataTags(),
      input2.getPrimaryVideoTrack()
    ]);
    const data = parseCanvasCaptureData(metadata);
    if (data === null || videoTrack === null) {
      return null;
    }
    const [durationFromMetadata, width, height] = await Promise.all([
      input2.getDurationFromMetadata(),
      videoTrack.getDisplayWidth(),
      videoTrack.getDisplayHeight()
    ]);
    const durationInSeconds = durationFromMetadata ?? await input2.computeDuration([videoTrack]);
    if (!Number.isFinite(durationInSeconds) || durationInSeconds <= 0 || !Number.isFinite(width) || width <= 0 || !Number.isFinite(height) || height <= 0) {
      return null;
    }
    return { data, durationInSeconds, file, height, width };
  } catch {
    return null;
  } finally {
    input2.dispose();
  }
};

// src/components/canvas-capture-drop.ts
var handleCanvasCaptureDrop = async ({
  files,
  setSelectedModal
}) => {
  if (files.length !== 1) {
    return false;
  }
  const canvasCapture = await getCanvasCaptureImport(files[0]);
  if (canvasCapture === null) {
    return false;
  }
  setSelectedModal({
    type: "new-comp",
    canvasCapture,
    folderName: null,
    parentName: null,
    stack: null
  });
  return true;
};

// src/helpers/remote-asset-drag.ts
var remoteAssetDragTypes = ["text/uri-list", "text/html", "text/plain"];
var isHttpUrl = (value) => {
  try {
    const url = new URL(value);
    return url.protocol === "http:" || url.protocol === "https:";
  } catch {
    return false;
  }
};
var decodeHtmlAttribute = (value) => {
  return value.replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&lt;/g, "<").replace(/&gt;/g, ">");
};
var hasRemoteAssetDragData = (dataTransfer) => {
  return remoteAssetDragTypes.some((type) => Array.from(dataTransfer?.types ?? []).includes(type));
};
var getRemoteAssetUrlFromUriList = (uriList) => {
  const lines = uriList.split(/\r?\n/g);
  for (const line of lines) {
    const trimmed = line.trim();
    if (trimmed === "" || trimmed.startsWith("#")) {
      continue;
    }
    if (isHttpUrl(trimmed)) {
      return trimmed;
    }
  }
  return null;
};
var getRemoteAssetUrlFromHtml = (html) => {
  const imgSrc = html.match(/<img\b[^>]*\bsrc\s*=\s*(?:"([^"]+)"|'([^']+)'|([^\s>]+))/i);
  const match = imgSrc?.[1] ?? imgSrc?.[2] ?? imgSrc?.[3] ?? null;
  if (match) {
    const decoded = decodeHtmlAttribute(match.trim());
    if (isHttpUrl(decoded)) {
      return decoded;
    }
  }
  const fallback = html.match(/https?:\/\/[^\s"'<>]+/i)?.[0] ?? null;
  return fallback && isHttpUrl(fallback) ? decodeHtmlAttribute(fallback) : null;
};
var getRemoteAssetUrlFromDataTransfer = (dataTransfer) => {
  if (!dataTransfer) {
    return null;
  }
  const uriListUrl = getRemoteAssetUrlFromUriList(dataTransfer.getData("text/uri-list"));
  if (uriListUrl) {
    return uriListUrl;
  }
  const htmlUrl = getRemoteAssetUrlFromHtml(dataTransfer.getData("text/html"));
  if (htmlUrl) {
    return htmlUrl;
  }
  const plainText = dataTransfer.getData("text/plain").trim();
  return isHttpUrl(plainText) ? plainText : null;
};

// src/components/composition-drag-data.ts
import * as z from "zod/mini";
var REMOTION_DRAG_MIME_TYPE = "application/vnd.remotion.drag+json";
var DRAG_MIME_VERSION = 1;
var MAX_MIME_TYPE_LENGTH = 512;
var MAX_DIMENSION = 1e5;
var MAX_DURATION_IN_FRAMES = 1e8;
var compositionIdSchema = z.string().check(z.refine((value) => value.length > 0 && value.length < 500 && /^([a-zA-Z0-9-\u4E00-\u9FFF])+$/.test(value)));
var compositionFileSchema = z.nullable(z.string().check(z.refine((value) => value.length > 0 && value.length < 2000 && !value.includes("\x00") && !value.includes("\\") && !value.startsWith("/") && !value.split("/").includes(".."))));
var compositionDragDataSchema = z.object({
  type: z.literal("remotion-composition"),
  version: z.literal(1),
  compositionId: compositionIdSchema,
  compositionFile: compositionFileSchema
});
var parsePositiveNumber = (value, max) => {
  if (!/^(?:[1-9]\d*|0\.\d+|[1-9]\d*\.\d+)$/.test(value)) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) && parsed > 0 && parsed <= max ? parsed : null;
};
var parsePositiveInteger = (value, max) => {
  if (!/^[1-9]\d*$/.test(value)) {
    return null;
  }
  const parsed = Number(value);
  return Number.isInteger(parsed) && parsed <= max ? parsed : null;
};
var parseCompositionDragMimeType = (mimeType) => {
  if (mimeType.length > MAX_MIME_TYPE_LENGTH || !mimeType.startsWith(`${REMOTION_DRAG_MIME_TYPE};`)) {
    return null;
  }
  const segments = mimeType.split(";");
  if (segments.shift() !== REMOTION_DRAG_MIME_TYPE) {
    return null;
  }
  const values = new Map;
  for (const segment of segments) {
    const separator = segment.indexOf("=");
    if (separator === -1) {
      return null;
    }
    const key = segment.slice(0, separator);
    const value = segment.slice(separator + 1);
    if (!["v", "type", "width", "height", "duration"].includes(key) || value === "" || values.has(key)) {
      return null;
    }
    values.set(key, value);
  }
  if (values.get("v") !== String(DRAG_MIME_VERSION) || values.get("type") !== "composition") {
    return null;
  }
  const widthValue = values.get("width");
  const heightValue = values.get("height");
  if (widthValue === undefined !== (heightValue === undefined)) {
    return null;
  }
  const width = widthValue === undefined ? null : parsePositiveNumber(widthValue, MAX_DIMENSION);
  const height = heightValue === undefined ? null : parsePositiveNumber(heightValue, MAX_DIMENSION);
  const durationValue = values.get("duration");
  const durationInFrames = durationValue === undefined ? null : parsePositiveInteger(durationValue, MAX_DURATION_IN_FRAMES);
  if (widthValue !== undefined && width === null) {
    return null;
  }
  if (heightValue !== undefined && height === null) {
    return null;
  }
  if (durationValue !== undefined && durationInFrames === null) {
    return null;
  }
  return {
    type: "composition",
    width,
    height,
    durationInFrames,
    mimeType
  };
};
var makeCompositionDragData = ({
  compositionFile,
  compositionId,
  width,
  height,
  durationInFrames
}) => {
  if (width === null !== (height === null)) {
    throw new TypeError("width and height must either both be numbers or both be null");
  }
  if (width !== null && (!Number.isFinite(width) || width <= 0 || width > MAX_DIMENSION)) {
    throw new TypeError(`width and height must be numbers between 0 and ${MAX_DIMENSION}`);
  }
  if (height !== null && (!Number.isFinite(height) || height <= 0 || height > MAX_DIMENSION)) {
    throw new TypeError(`width and height must be numbers between 0 and ${MAX_DIMENSION}`);
  }
  if (durationInFrames !== null && (!Number.isInteger(durationInFrames) || durationInFrames <= 0 || durationInFrames > MAX_DURATION_IN_FRAMES)) {
    throw new TypeError(`durationInFrames must be an integer between 1 and ${MAX_DURATION_IN_FRAMES}`);
  }
  const data = {
    type: "remotion-composition",
    version: 1,
    compositionFile,
    compositionId
  };
  const segments = [
    REMOTION_DRAG_MIME_TYPE,
    `v=${DRAG_MIME_VERSION}`,
    "type=composition"
  ];
  if (width !== null && height !== null) {
    segments.push(`width=${width}`, `height=${height}`);
  }
  if (durationInFrames !== null) {
    segments.push(`duration=${durationInFrames}`);
  }
  return {
    mimeType: segments.join(";"),
    payload: JSON.stringify(data),
    data
  };
};
var getCompositionDragPreviewMetadata = (mimeTypes) => {
  for (let index = 0;index < mimeTypes.length; index++) {
    const parsed = parseCompositionDragMimeType(mimeTypes[index]);
    if (parsed !== null) {
      return parsed;
    }
  }
  return null;
};
var parseCompositionDragData = (source) => {
  const serialized = "types" in source ? (() => {
    const preview = getCompositionDragPreviewMetadata(source.types);
    return preview === null ? null : {
      mimeType: preview.mimeType,
      payload: source.getData(preview.mimeType)
    };
  })() : source;
  if (serialized === null || parseCompositionDragMimeType(serialized.mimeType) === null) {
    return null;
  }
  try {
    const parsed = z.safeParse(compositionDragDataSchema, JSON.parse(serialized.payload));
    return parsed.success ? {
      type: "remotion-composition",
      version: 1,
      compositionFile: parsed.data.compositionFile,
      compositionId: parsed.data.compositionId
    } : null;
  } catch {
    return null;
  }
};

// src/components/drop-handler-data.ts
var isFileDragEvent = (event) => {
  return Array.from(event.dataTransfer?.types ?? []).includes("Files");
};
var isRemotionDragEvent = (event, type) => {
  return StudioProtocolInternals.getDragPreviewMetadata(event.dataTransfer?.types ?? [])?.type === type;
};
var isAssetDragEvent = (event) => {
  return isRemotionDragEvent(event, "asset");
};
var isComponentDragEvent = (event) => {
  return isRemotionDragEvent(event, "component");
};
var isCompositionDragEvent = (event) => {
  return getCompositionDragPreviewMetadata(event.dataTransfer?.types ?? []) !== null;
};
var isElementDragEvent = (event) => {
  return isRemotionDragEvent(event, "element");
};
var isSfxDragEvent = (event) => {
  return isRemotionDragEvent(event, "sfx");
};
var isRemoteAssetDragEvent = (event) => {
  return !isFileDragEvent(event) && !isAssetDragEvent(event) && !isCompositionDragEvent(event) && !isComponentDragEvent(event) && !isElementDragEvent(event) && !isSfxDragEvent(event) && hasRemoteAssetDragData(event.dataTransfer);
};
var isSupportedDropEvent = (event) => {
  return isFileDragEvent(event) || isAssetDragEvent(event) || isCompositionDragEvent(event) || isComponentDragEvent(event) || isElementDragEvent(event) || isSfxDragEvent(event) || isRemoteAssetDragEvent(event);
};
var getAssetDragPath = (event) => {
  const parsed = event.dataTransfer ? StudioProtocolInternals.parseDragData(event.dataTransfer) : null;
  return parsed?.type === "asset" ? parsed.data.assetPath : null;
};
var getCompositionDragData = (event) => {
  return event.dataTransfer ? parseCompositionDragData(event.dataTransfer) : null;
};
var getComponentDragData = (event) => {
  const parsed = event.dataTransfer ? StudioProtocolInternals.parseDragData(event.dataTransfer) : null;
  return parsed?.type === "component" ? parsed.data : null;
};
var getSfxDragUrl = (event) => {
  const parsed = event.dataTransfer ? StudioProtocolInternals.parseDragData(event.dataTransfer) : null;
  return parsed?.type === "sfx" ? parsed.data.sfx.url : null;
};

// src/components/CanvasCaptureDropHandler.tsx
var CanvasCaptureDropHandler = ({ readOnlyStudio }) => {
  const { setSelectedModal } = useContext(SetSelectedModalContext);
  const onDragOver = useCallback((event) => {
    if (readOnlyStudio || !isFileDragEvent(event)) {
      return;
    }
    event.preventDefault();
    if (event.dataTransfer) {
      event.dataTransfer.dropEffect = "copy";
    }
  }, [readOnlyStudio]);
  const onDrop = useCallback(async (event) => {
    if (readOnlyStudio || !isFileDragEvent(event)) {
      return;
    }
    const files = Array.from(event.dataTransfer?.files ?? []);
    if (files.length === 0) {
      return;
    }
    event.preventDefault();
    await handleCanvasCaptureDrop({ files, setSelectedModal });
  }, [readOnlyStudio, setSelectedModal]);
  useEffect(() => {
    document.addEventListener("dragover", onDragOver);
    document.addEventListener("drop", onDrop);
    return () => {
      document.removeEventListener("dragover", onDragOver);
      document.removeEventListener("drop", onDrop);
    };
  }, [onDragOver, onDrop]);
  return null;
};

// src/components/EditorContent.tsx
import { useCallback as useCallback74, useContext as useContext95 } from "react";
import { Internals as Internals93 } from "remotion";

// src/components/GlobalKeybindings.tsx
import { useCallback as useCallback8, useContext as useContext7, useEffect as useEffect5, useMemo as useMemo6 } from "react";
import { Internals as Internals5 } from "remotion";

// src/components/Notifications/ColorDot.tsx
import { useMemo } from "react";
import { jsx } from "react/jsx-runtime";
var container2 = {
  height: 16,
  width: 16,
  backgroundColor: RED,
  border: BORDER_WHITE_ALPHA_20,
  borderRadius: 8
};
var ColorDot = ({ color }) => {
  const style = useMemo(() => {
    return {
      ...container2,
      backgroundColor: color
    };
  }, [color]);
  return /* @__PURE__ */ jsx("div", {
    style
  });
};

// src/helpers/pick-color.tsx
import { jsx as jsx2, jsxs, Fragment } from "react/jsx-runtime";
var pickColor = () => {
  const open = new EyeDropper().open();
  open.then((color) => {
    copyText(color.sRGBHex).then(() => {
      showNotification(/* @__PURE__ */ jsxs(Fragment, {
        children: [
          /* @__PURE__ */ jsx2(ColorDot, {
            color: color.sRGBHex
          }),
          " ",
          /* @__PURE__ */ jsx2(Spacing, {
            x: 1
          }),
          " Copied",
          " ",
          color.sRGBHex
        ]
      }), 2000);
    }).catch((err) => {
      showNotification(`Could not copy: ${err.message}`, 2000);
    });
  }).catch((err) => {
    if (err.message.includes("canceled")) {
      return;
    }
    showNotification(`Could not pick color.`, 2000);
  });
};

// src/state/checkerboard.ts
import { createContext as createContext2 } from "react";
var persistCheckerboardOption = (option) => {
  localStorage.setItem("option", String(option));
};
var loadCheckerboardOption = () => {
  const item = localStorage.getItem("option");
  return item !== "false";
};
var CheckerboardContext = createContext2({
  checkerboard: loadCheckerboardOption(),
  setCheckerboard: () => {
    return;
  }
});

// src/state/editor-outlines.ts
import { createContext as createContext3 } from "react";
var key = "remotion.editorShowOutlines";
var persistEditorShowOutlinesOption = (option) => {
  localStorage.setItem(key, String(option));
};
var loadEditorShowOutlinesOption = () => {
  const item = localStorage.getItem(key);
  return item !== "false";
};
var EditorShowOutlinesContext = createContext3({
  editorShowOutlines: true,
  setEditorShowOutlines: () => {
    return;
  }
});

// src/state/editor-rulers.ts
import { createContext as createContext4 } from "react";
var persistEditorShowRulersOption = (option) => {
  localStorage.setItem("remotion.editorShowRulers", String(option));
};
var loadEditorShowRulersOption = () => {
  const item = localStorage.getItem("remotion.editorShowRulers");
  return item === "true";
};
var EditorShowRulersContext = createContext4({
  editorShowRulers: loadEditorShowRulersOption(),
  setEditorShowRulers: () => {
    return;
  }
});
var RULER_WIDTH = 20;
var MINIMUM_VISIBLE_CANVAS_SIZE = 50;
var PREDEFINED_RULER_SCALE_GAPS = [
  1,
  2,
  5,
  10,
  20,
  50,
  100,
  250,
  500,
  1000,
  2000,
  5000
];
var MAXIMUM_PREDEFINED_RULER_SCALE_GAP = 5000;
var MINIMUM_RULER_MARKING_GAP_PX = 50;

// src/state/editor-snapping.ts
import { createContext as createContext5 } from "react";
var key2 = "remotion.editorSnapping";
var persistEditorSnappingOption = (option) => {
  localStorage.setItem(key2, String(option));
};
var loadEditorSnappingOption = () => {
  const item = localStorage.getItem(key2);
  return item !== "false";
};
var EditorSnappingContext = createContext5({
  editorSnapping: true,
  setEditorSnapping: () => {
    return;
  }
});

// src/components/AskAiModal.tsx
import {
  createRef as createRef2,
  useCallback as useCallback2,
  useEffect as useEffect2,
  useImperativeHandle,
  useRef,
  useState
} from "react";
import { Internals as Internals2 } from "remotion";
import { jsx as jsx3, jsxs as jsxs2 } from "react/jsx-runtime";
var container3 = {
  height: "calc(100vh - 100px)",
  width: "calc(100vw - 160px)",
  maxWidth: 800,
  maxHeight: 900,
  display: "block"
};
var askAiModalRef = createRef2();
var AskAiModal = () => {
  const [state, setState] = useState("never-opened");
  const iframe = useRef(null);
  useImperativeHandle(askAiModalRef, () => ({
    toggle: () => {
      setState((s) => {
        if (s === "visible") {
          iframe.current?.blur();
          iframe.current?.contentWindow?.blur();
        }
        return s === "visible" ? "hidden" : "visible";
      });
    }
  }), []);
  useEffect2(() => {
    const onMessage = (event) => {
      try {
        const json = typeof event.data === "string" ? JSON.parse(event.data) : event.data;
        if (json.type === "cmd-i") {
          askAiModalRef.current?.toggle();
        }
      } catch {}
    };
    window.addEventListener("message", onMessage);
    return () => {
      window.removeEventListener("message", onMessage);
    };
  }, []);
  const onQuit = useCallback2(() => {
    setState("hidden");
  }, [setState]);
  useEffect2(() => {
    if (!iframe.current) {
      return;
    }
    if (state === "visible") {
      iframe.current.contentWindow?.postMessage({
        type: "focus"
      }, "*");
    }
  }, [state]);
  if (state === "never-opened") {
    return null;
  }
  return /* @__PURE__ */ jsx3(Internals2.AbsoluteFillElement, {
    style: { display: state === "visible" ? "block" : "none" },
    children: /* @__PURE__ */ jsxs2(ModalContainer, {
      noZIndex: state === "hidden",
      onOutsideClick: onQuit,
      onEscape: onQuit,
      children: [
        /* @__PURE__ */ jsx3(ModalHeader, {
          title: "Ask AI",
          onClose: onQuit
        }),
        /* @__PURE__ */ jsx3("iframe", {
          ref: iframe,
          frameBorder: 0,
          style: container3,
          src: "https://www.remotion.dev/ai-embed",
          allow: "clipboard-read; clipboard-write"
        })
      ]
    })
  });
};

// src/components/CompositionSelector.tsx
import {
  useCallback as useCallback7,
  useContext as useContext6,
  useEffect as useEffect4,
  useMemo as useMemo5,
  useRef as useRef3,
  useState as useState3
} from "react";
import { Internals as Internals4 } from "remotion";

// src/components/composition-selector-drag-data.ts
var MIME_TYPE = "application/remotion-composition-selector-reorder";
var isSafeString = (value, maxLength) => {
  return typeof value === "string" && value.length > 0 && value.length <= maxLength && !value.includes("\x00");
};
var isCompositionOrFolder = (value) => {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const item = value;
  if (item.type === "composition") {
    return isSafeString(item.compositionId, 500);
  }
  return item.type === "folder" && isSafeString(item.folderName, 500) && (item.parentName === null || isSafeString(item.parentName, 2000));
};
var isSafeSourceFile = (value) => {
  return value === null || isSafeString(value, 2000) && !value.includes("\\") && !value.startsWith("/") && !value.split("/").includes("..");
};
var makeCompositionSelectorDragData = ({
  item,
  sourceFile
}) => {
  const data = {
    type: "remotion-composition-selector",
    version: 1,
    item,
    sourceFile
  };
  return { mimeType: MIME_TYPE, payload: JSON.stringify(data), data };
};
var hasCompositionSelectorDragData = (mimeTypes) => {
  return Array.from(mimeTypes).includes(MIME_TYPE);
};
var parseCompositionSelectorDragData = (dataTransfer) => {
  if (!hasCompositionSelectorDragData(dataTransfer.types)) {
    return null;
  }
  try {
    const parsed = JSON.parse(dataTransfer.getData(MIME_TYPE));
    if (parsed.type !== "remotion-composition-selector" || parsed.version !== 1 || !isCompositionOrFolder(parsed.item) || !isSafeSourceFile(parsed.sourceFile)) {
      return null;
    }
    return {
      type: "remotion-composition-selector",
      version: 1,
      item: parsed.item,
      sourceFile: parsed.sourceFile
    };
  } catch {
    return null;
  }
};
var compositionSelectorDragDataToSymbolicatedStack = (dragData) => {
  if (dragData.sourceFile === null) {
    return null;
  }
  return {
    originalColumnNumber: null,
    originalFileName: dragData.sourceFile,
    originalFunctionName: null,
    originalLineNumber: null,
    originalScriptCode: null
  };
};

// src/components/CompositionSelectorItem.tsx
import {
  useCallback as useCallback5,
  useContext as useContext4,
  useEffect as useEffect3,
  useLayoutEffect,
  useMemo as useMemo4,
  useRef as useRef2,
  useState as useState2
} from "react";

// src/helpers/sidebar-scroll-into-view.ts
var getComputedOverflowY = (el) => {
  return getComputedStyle(el).overflowY;
};
var findVerticalScrollParent = (element) => {
  let cur = element?.parentElement ?? null;
  while (cur) {
    const overflowY = getComputedOverflowY(cur);
    if (overflowY === "auto" || overflowY === "scroll" || overflowY === "overlay") {
      return cur;
    }
    cur = cur.parentElement;
  }
  return null;
};
var isVerticallyFullyVisibleInSidebarList = (element) => {
  const scrollParent = findVerticalScrollParent(element);
  const rect = element.getBoundingClientRect();
  if (!scrollParent) {
    return rect.top >= 0 && rect.bottom <= window.innerHeight;
  }
  const parentRect = scrollParent.getBoundingClientRect();
  return rect.top >= parentRect.top && rect.bottom <= parentRect.bottom;
};
var scrollSidebarRowIntoViewIfNeeded = (element) => {
  if (!element) {
    return;
  }
  if (isVerticallyFullyVisibleInSidebarList(element)) {
    return;
  }
  element.scrollIntoView({ block: "nearest", behavior: "auto" });
};
var skipCompositionScrollIntoViewForId = null;
var markCompositionSidebarScrollFromRowClick = (compositionId) => {
  skipCompositionScrollIntoViewForId = compositionId;
};
var shouldSkipCompositionScrollIntoView = (compositionId) => {
  if (skipCompositionScrollIntoViewForId === compositionId) {
    skipCompositionScrollIntoViewForId = null;
    return true;
  }
  return false;
};
var maybeScrollCompositionSidebarRowIntoView = ({
  element,
  compositionId,
  selected
}) => {
  if (!selected) {
    return;
  }
  if (shouldSkipCompositionScrollIntoView(compositionId)) {
    return;
  }
  scrollSidebarRowIntoViewIfNeeded(element);
};
var skipAssetScrollIntoViewForPath = null;
var markAssetSidebarScrollFromRowClick = (assetPath) => {
  skipAssetScrollIntoViewForPath = assetPath;
};
var shouldSkipAssetScrollIntoView = (assetPath) => {
  if (skipAssetScrollIntoViewForPath === assetPath) {
    skipAssetScrollIntoViewForPath = null;
    return true;
  }
  return false;
};
var maybeScrollAssetSidebarRowIntoView = ({
  element,
  assetPath,
  selected
}) => {
  if (!selected) {
    return;
  }
  if (shouldSkipAssetScrollIntoView(assetPath)) {
    return;
  }
  scrollSidebarRowIntoViewIfNeeded(element);
};

// src/components/CompositionContextButton.tsx
import { useCallback as useCallback3, useContext as useContext2, useMemo as useMemo2 } from "react";
import { jsx as jsx4, jsxs as jsxs3 } from "react/jsx-runtime";
var revealStyle = {
  display: "flex"
};
var CompositionContextButton = ({ visible, getItems, readOnlyStudio }) => {
  const iconStyle = useMemo2(() => {
    return {
      style: {
        height: 12
      }
    };
  }, []);
  const connectionStatus = useContext2(StudioServerConnectionCtx).previewServerState.type;
  const renderAction = useCallback3((color) => {
    return /* @__PURE__ */ jsx4(EllipsisIcon, {
      fill: color,
      svgProps: iconStyle
    });
  }, [iconStyle]);
  if (!visible || connectionStatus !== "connected" && !readOnlyStudio) {
    return null;
  }
  return /* @__PURE__ */ jsxs3("div", {
    className: HOVER_GROUP_REVEAL_CLASS_NAME,
    style: revealStyle,
    children: [
      /* @__PURE__ */ jsx4(Spacing, {
        x: 0.5
      }),
      /* @__PURE__ */ jsx4(InlineDropdown, {
        renderAction,
        getItems,
        variant: null,
        style: NO_HOVER_BACKGROUND_STYLE,
        className: FOCUS_VISIBLE_ONLY_CLASS_NAME
      })
    ]
  });
};

// src/components/folder-menu-items.ts
import { NoReactInternals } from "remotion/no-react";

// src/helpers/get-folder-id.ts
var getFolderId = ({
  folderName,
  parentName
}) => {
  return [parentName, folderName].filter(Boolean).join("/");
};

// src/components/folder-menu-items.ts
var getFolderMenuItems = ({
  closeMenu,
  connectionStatus,
  folder,
  readOnlyStudio,
  resolvedLocation,
  setSelectedModal,
  editorId,
  editorName
}) => {
  const folderId = getFolderId({
    folderName: folder.name,
    parentName: folder.parent
  });
  const fileLocation = formatFileLocation({
    location: resolvedLocation,
    root: window.remotion_cwd
  });
  const contextForAgents = formatContextForAgents({
    location: resolvedLocation,
    name: folderId,
    root: window.remotion_cwd
  });
  const showInEditorDisabled = !editorId || connectionStatus !== "connected" || !resolvedLocation;
  const copyFileLocationDisabled = !fileLocation;
  const codemodDisabled = readOnlyStudio || !folder.stack;
  return [
    editorName ? {
      id: "show-folder-in-editor",
      keyHint: null,
      label: `Show folder in ${editorName}`,
      leftItem: null,
      onClick: async () => {
        closeMenu();
        if (!resolvedLocation || !editorId) {
          return;
        }
        try {
          await openOriginalPositionInEditor(resolvedLocation, editorId);
        } catch (err) {
          showNotification(err.message, 2000);
        }
      },
      quickSwitcherLabel: `Show folder in ${editorName}`,
      subMenu: null,
      type: "item",
      value: "show-folder-in-editor",
      disabled: showInEditorDisabled
    } : null,
    {
      id: "copy-context-for-agents",
      keyHint: null,
      label: `Copy context for agents`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        if (!contextForAgents) {
          return;
        }
        navigator.clipboard.writeText(contextForAgents).catch((err) => {
          showNotification(`Could not copy to clipboard: ${err.message}`, 1000);
        });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: "copy-context-for-agents",
      disabled: !contextForAgents
    },
    {
      id: "copy-folder-file-location",
      keyHint: null,
      label: `Copy file location`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        if (!fileLocation) {
          return;
        }
        navigator.clipboard.writeText(fileLocation).then(() => {
          showNotification("Copied file location to clipboard", 1000);
        }).catch((err) => {
          showNotification(`Could not copy to clipboard: ${err.message}`, 1000);
        });
      },
      quickSwitcherLabel: "Copy folder file location",
      subMenu: null,
      type: "item",
      value: "copy-folder-file-location",
      disabled: copyFileLocationDisabled
    },
    editorName || fileLocation ? {
      type: "divider",
      id: "show-folder-in-editor-divider"
    } : null,
    {
      id: "new-composition-in-folder",
      keyHint: null,
      label: `New composition...`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "new-comp",
          folderName: folder.name,
          parentName: folder.parent,
          stack: folder.stack,
          canvasCapture: null
        });
      },
      quickSwitcherLabel: "New composition in folder...",
      subMenu: null,
      type: "item",
      value: "new-composition-in-folder",
      disabled: codemodDisabled
    },
    {
      id: "new-folder-in-folder",
      keyHint: null,
      label: `New folder...`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "new-folder",
          parentName: folderId,
          stack: folder.stack
        });
      },
      quickSwitcherLabel: "New folder in folder...",
      subMenu: null,
      type: "item",
      value: "new-folder-in-folder",
      disabled: codemodDisabled
    },
    {
      id: "rename-folder",
      keyHint: null,
      label: `Rename...`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "rename-folder",
          folderName: folder.name,
          parentName: folder.parent,
          stack: folder.stack
        });
      },
      quickSwitcherLabel: "Rename folder...",
      subMenu: null,
      type: "item",
      value: "rename-folder",
      disabled: codemodDisabled
    },
    {
      id: "delete-folder",
      keyHint: null,
      label: `Delete...`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "delete-folder",
          folderName: folder.name,
          parentName: folder.parent,
          stack: folder.stack
        });
      },
      quickSwitcherLabel: "Delete folder...",
      subMenu: null,
      type: "item",
      value: "delete-folder",
      disabled: codemodDisabled
    },
    {
      type: "divider",
      id: "copy-folder-id-divider"
    },
    {
      id: "copy-folder-id",
      keyHint: null,
      label: `Copy ID`,
      leftItem: null,
      onClick: () => {
        closeMenu();
        navigator.clipboard.writeText(folderId).then(() => {
          showNotification("Copied to clipboard", 1000);
        }).catch((err) => {
          showNotification(`Could not copy to clipboard: ${err.message}`, 1000);
        });
      },
      quickSwitcherLabel: "Copy folder ID",
      subMenu: null,
      type: "item",
      value: "copy-folder-id",
      disabled: false
    }
  ].filter(NoReactInternals.truthy);
};

// src/components/SidebarRenderButton.tsx
import { useCallback as useCallback4, useContext as useContext3, useMemo as useMemo3 } from "react";
import { Internals as Internals3 } from "remotion";

// src/icons/render.tsx
import { jsx as jsx5 } from "react/jsx-runtime";
var ThinRenderIcon = (props) => {
  return /* @__PURE__ */ jsx5("svg", {
    ...props.svgProps,
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512",
    children: /* @__PURE__ */ jsx5("path", {
      fill: props.fill,
      d: "M188.9 372l-50.4-50.4c18.6-42.6 61.7-137.7 95.1-187C304.6 30.1 409 24.6 475.7 36.3c11.7 66.7 6.2 171.1-98.4 242c-49.4 33.5-145.5 75.6-188.4 93.7zm-79.9-62.8c-5.2 11.9-2.5 25.7 6.7 34.9l50.7 50.7c9.1 9.1 22.7 11.9 34.5 6.9c6.5-2.7 14.3-6 23-9.8L224 496c0 5.5 2.9 10.7 7.6 13.6s10.6 3.2 15.6 .7l101.5-50.7c21.7-10.8 35.4-33 35.4-57.2V312.1c4-2.5 7.7-4.9 11.3-7.3C516.1 222.9 520.1 100.9 506.7 28.1c-2.1-11.6-11.2-20.6-22.8-22.8C411.1-8.1 289.1-4.1 207.2 116.7c-2.4 3.6-4.9 7.3-7.3 11.3l-90.2 0c-24.2 0-46.4 13.7-57.2 35.4L1.7 264.8c-2.5 5-2.2 10.8 .7 15.6s8.1 7.6 13.6 7.6H118.5c-3.6 8-6.8 15.2-9.4 21.2zM256 470.1l0-92.5c30.3-13.7 65.4-30.3 96-47v71.7c0 12.1-6.8 23.2-17.7 28.6L256 470.1zM109.7 160h71.5c-16.9 30.7-34 65.8-48.1 96H41.9L81 177.7c5.4-10.8 16.5-17.7 28.6-17.7zM392 144a24 24 0 1 1 -48 0 24 24 0 1 1 48 0zM368 88a56 56 0 1 0 0 112 56 56 0 1 0 0-112z"
    })
  });
};

// src/components/SidebarRenderButton.tsx
import { jsx as jsx6 } from "react/jsx-runtime";
var revealStyle2 = {
  display: "flex"
};
var SidebarRenderButton = ({ composition, visible, readOnlyStudio }) => {
  const { setSelectedModal } = useContext3(SetSelectedModalContext);
  const iconStyle = useMemo3(() => {
    return {
      style: {
        height: 12
      }
    };
  }, []);
  const connectionStatus = useContext3(StudioServerConnectionCtx).previewServerState.type;
  const { props } = useContext3(Internals3.EditorPropsContext);
  const onClick = useCallback4((e) => {
    const defaults = window.remotion_renderDefaults;
    if (!defaults) {
      throw new Error("expected defaults");
    }
    e.stopPropagation();
    setSelectedModal({
      type: "server-render",
      compositionId: composition.id,
      initialFrame: 0,
      initialVideoImageFormat: defaults.videoImageFormat,
      initialStillImageFormat: defaults.stillImageFormat,
      initialJpegQuality: defaults.jpegQuality,
      initialScale: defaults.scale,
      initialLogLevel: defaults.logLevel,
      initialConcurrency: defaults.concurrency,
      maxConcurrency: defaults.maxConcurrency,
      minConcurrency: defaults.minConcurrency,
      initialMuted: defaults.muted,
      initialEnforceAudioTrack: defaults.enforceAudioTrack,
      initialProResProfile: defaults.proResProfile,
      initialx264Preset: defaults.x264Preset,
      initialGopSize: defaults.gopSize,
      initialPixelFormat: null,
      initialAudioBitrate: defaults.audioBitrate,
      initialVideoBitrate: defaults.videoBitrate,
      initialEveryNthFrame: defaults.everyNthFrame,
      initialNumberOfGifLoops: defaults.numberOfGifLoops,
      initialDelayRenderTimeout: defaults.delayRenderTimeout,
      defaultConfigurationAudioCodec: defaults.audioCodec,
      initialEnvVariables: window.process.env,
      initialDisableWebSecurity: defaults.disableWebSecurity,
      initialOpenGlRenderer: defaults.openGlRenderer,
      initialHeadless: defaults.headless,
      initialOffthreadVideoCacheSizeInBytes: defaults.offthreadVideoCacheSizeInBytes,
      initialOffthreadVideoThreads: defaults.offthreadVideoThreads,
      initialIgnoreCertificateErrors: defaults.ignoreCertificateErrors,
      defaultProps: props[composition.id] ?? composition.defaultProps,
      inFrameMark: null,
      outFrameMark: null,
      initialColorSpace: defaults.colorSpace,
      initialMultiProcessOnLinux: defaults.multiProcessOnLinux,
      defaultConfigurationVideoCodec: defaults.codec,
      initialEncodingBufferSize: defaults.encodingBufferSize,
      initialEncodingMaxRate: defaults.encodingMaxRate,
      initialUserAgent: defaults.userAgent,
      initialBeep: defaults.beepOnFinish,
      initialRepro: defaults.repro,
      initialForSeamlessAacConcatenation: defaults.forSeamlessAacConcatenation,
      renderTypeOfLastRender: null,
      defaulMetadata: defaults.metadata,
      initialHardwareAcceleration: defaults.hardwareAcceleration,
      initialSampleRate: defaults.sampleRate,
      initialChromeMode: defaults.chromeMode,
      initialMediaCacheSizeInBytes: defaults.mediaCacheSizeInBytes,
      renderDefaults: defaults,
      initialDarkMode: defaults.darkMode,
      readOnlyStudio
    });
  }, [
    composition.defaultProps,
    composition.id,
    props,
    readOnlyStudio,
    setSelectedModal
  ]);
  const renderAction = useCallback4((color) => {
    return /* @__PURE__ */ jsx6(ThinRenderIcon, {
      fill: color,
      svgProps: iconStyle
    });
  }, [iconStyle]);
  if (!visible || connectionStatus !== "connected" && !readOnlyStudio) {
    return null;
  }
  return /* @__PURE__ */ jsx6("div", {
    className: HOVER_GROUP_REVEAL_CLASS_NAME,
    style: revealStyle2,
    children: /* @__PURE__ */ jsx6(InlineAction, {
      renderAction,
      onClick,
      variant: null,
      style: NO_HOVER_BACKGROUND_STYLE,
      className: FOCUS_VISIBLE_ONLY_CLASS_NAME
    })
  });
};

// src/components/CompositionSelectorItem.tsx
import { jsx as jsx7, jsxs as jsxs4, Fragment as Fragment2 } from "react/jsx-runtime";
var itemStyle = {
  paddingRight: 2,
  paddingTop: 5,
  paddingBottom: 5,
  fontSize: 13,
  display: "flex",
  textDecoration: "none",
  cursor: "default",
  alignItems: "center",
  marginBottom: 1,
  marginLeft: 8,
  marginRight: 4,
  appearance: "none",
  border: "none",
  borderRadius: 4,
  width: "calc(100% - 12px)",
  textAlign: "left",
  height: COMPACT_CONTROL_ROW_HEIGHT,
  userSelect: "none"
};
var labelStyle = {
  textAlign: "left",
  textDecoration: "none",
  fontSize: 13,
  flex: 1,
  whiteSpace: "nowrap",
  overflow: "hidden",
  textOverflow: "ellipsis"
};
var iconStyle = {
  width: 18,
  height: 18,
  flexShrink: 0
};
var reorderWrapper = {
  position: "relative"
};
var reorderLineBase = {
  backgroundColor: TIMELINE_BLUE,
  height: 2,
  left: 0,
  pointerEvents: "none",
  position: "absolute",
  right: 0,
  zIndex: 1
};
var folderAutoExpansionDelay = 700;
var itemToDescriptor = (item) => {
  return item.type === "composition" ? { type: "composition", compositionId: item.composition.id } : {
    type: "folder",
    folderName: item.folderName,
    parentName: item.parentName
  };
};
var itemsAreEqual = (left, right) => {
  if (left.type === "composition") {
    return right.type === "composition" && left.compositionId === right.compositionId;
  }
  return right.type === "folder" && left.folderName === right.folderName && left.parentName === right.parentName;
};
var getFolderPath = ({
  folderName,
  parentName
}) => {
  return [parentName, folderName].filter(Boolean).join("/");
};
var wouldMoveFolderIntoItself = ({
  activeDrag,
  destinationParentFolderPath
}) => {
  if (activeDrag.item.type !== "folder" || destinationParentFolderPath === null) {
    return false;
  }
  const sourceFolderPath = getFolderPath(activeDrag.item);
  return destinationParentFolderPath === sourceFolderPath || destinationParentFolderPath.startsWith(`${sourceFolderPath}/`);
};
var canMoveIntoFolder = ({
  activeDrag,
  destinationFolderPath
}) => {
  return activeDrag.parentFolderPath !== destinationFolderPath && !wouldMoveFolderIntoItself({
    activeDrag,
    destinationParentFolderPath: destinationFolderPath
  });
};
var CompositionSelectorItem = ({
  item,
  level,
  currentComposition,
  tabIndex,
  selectComposition,
  toggleFolder,
  clearRootDragHover,
  canReorder,
  activeDragRef,
  parentFolderPath,
  previousSibling,
  nextSibling
}) => {
  const selected = useMemo4(() => {
    if (item.type === "composition") {
      return currentComposition === item.composition.id;
    }
    return false;
  }, [item, currentComposition]);
  const [isDragging, setIsDragging] = useState2(false);
  const [dropPosition, setDropPosition] = useState2(null);
  const dropPositionRef = useRef2(null);
  const folderExpansionTimer = useRef2(null);
  const folderExpansionRequested = useRef2(false);
  const cancelFolderExpansion = useCallback5(() => {
    if (folderExpansionTimer.current !== null) {
      window.clearTimeout(folderExpansionTimer.current);
      folderExpansionTimer.current = null;
    }
  }, []);
  const updateDropPosition = useCallback5((position) => {
    dropPositionRef.current = position;
    setDropPosition(position);
  }, []);
  useEffect3(() => {
    return cancelFolderExpansion;
  }, [cancelFolderExpansion]);
  const folderIsExpanded = item.type === "folder" && item.expanded;
  useEffect3(() => {
    if (folderIsExpanded) {
      folderExpansionRequested.current = false;
      cancelFolderExpansion();
    }
  }, [cancelFolderExpansion, folderIsExpanded]);
  const compositionRowRef = useRef2(null);
  const compositionId = item.type === "composition" ? item.composition.id : null;
  useLayoutEffect(() => {
    if (compositionId === null) {
      return;
    }
    maybeScrollCompositionSidebarRowIntoView({
      element: compositionRowRef.current,
      compositionId,
      selected
    });
  }, [compositionId, selected]);
  const style = useMemo4(() => {
    const idleBackground = dropPosition === "inside" ? WHITE_ALPHA_12 : selected ? WHITE_ALPHA_06 : TRANSPARENT;
    return {
      ...itemStyle,
      ...hoverableStyle({
        idleBackground,
        hoverBackground: dropPosition === "inside" ? WHITE_ALPHA_12 : WHITE_ALPHA_06,
        idleColor: selected ? WHITE : LIGHT_TEXT,
        hoverColor: WHITE
      }),
      paddingLeft: 12 + level * 8
    };
  }, [dropPosition, level, selected]);
  const reorderLineStyle = useMemo4(() => {
    if (dropPosition !== "before" && dropPosition !== "after") {
      return null;
    }
    return {
      ...reorderLineBase,
      ...dropPosition === "before" ? { top: -1 } : { bottom: -1 }
    };
  }, [dropPosition]);
  const onClick = useCallback5((evt) => {
    evt.preventDefault();
    if (item.type === "composition") {
      markCompositionSidebarScrollFromRowClick(item.composition.id);
      selectComposition(item.composition, true);
    } else {
      toggleFolder(item.folderName, item.parentName);
    }
  }, [item, selectComposition, toggleFolder]);
  const onKeyDown = useCallback5((evt) => {
    if (evt.key === "Enter") {
      onClick(evt);
    }
  }, [onClick]);
  const { setSelectedModal } = useContext4(SetSelectedModalContext);
  const { connectionStatus, openInApps } = useOpenInMenuApps();
  const resolvedLocation = useResolvedStack(item.type === "composition" ? item.composition.stack : item.folder.stack);
  const getContextMenuItems = useCallback5(() => {
    if (item.type === "composition") {
      return getCompositionContextMenuItems({
        closeMenu: noop,
        composition: item.composition,
        connectionStatus,
        includeCompositionManagementItems: true,
        openInApps,
        resolvedLocation,
        setSelectedModal,
        readOnlyStudio: window.remotion_isReadOnlyStudio
      });
    }
    return getFolderMenuItems({
      closeMenu: noop,
      connectionStatus,
      editorId: openInApps.defaultEditorId,
      editorName: openInApps.defaultEditorName,
      folder: item.folder,
      resolvedLocation,
      setSelectedModal,
      readOnlyStudio: window.remotion_isReadOnlyStudio
    });
  }, [connectionStatus, item, openInApps, resolvedLocation, setSelectedModal]);
  const onItemDragStart = useCallback5((event) => {
    if (window.remotion_isReadOnlyStudio) {
      event.preventDefault();
      return;
    }
    const itemDescriptor = itemToDescriptor(item);
    activeDragRef.current = {
      item: itemDescriptor,
      parentFolderPath
    };
    setIsDragging(true);
    event.dataTransfer.effectAllowed = "copyMove";
    const selectorDragData = makeCompositionSelectorDragData({
      item: itemDescriptor,
      sourceFile: resolvedLocation?.source ?? null
    });
    event.dataTransfer.setData(selectorDragData.mimeType, selectorDragData.payload);
    if (item.type === "composition") {
      const browserStudioOperations = getBrowserStudioOperations();
      const compositionDragData = makeCompositionDragData({
        compositionFile: browserStudioOperations === null ? resolvedLocation?.source ?? null : browserStudioOperations.getCompositionFile(item.composition.id),
        compositionId: item.composition.id,
        width: item.composition.width ?? null,
        height: item.composition.height ?? null,
        durationInFrames: item.composition.durationInFrames ?? null
      });
      event.dataTransfer.setData(compositionDragData.mimeType, compositionDragData.payload);
    }
  }, [activeDragRef, item, parentFolderPath, resolvedLocation?.source]);
  const onItemDragEnd = useCallback5(() => {
    activeDragRef.current = null;
    cancelFolderExpansion();
    setIsDragging(false);
    updateDropPosition(null);
  }, [activeDragRef, cancelFolderExpansion, updateDropPosition]);
  const getDropPosition = useCallback5((event) => {
    const activeDrag = activeDragRef.current;
    if (activeDrag === null) {
      return null;
    }
    if (!canReorder) {
      if (item.type !== "folder") {
        return null;
      }
      return canMoveIntoFolder({
        activeDrag,
        destinationFolderPath: getFolderPath(item)
      }) ? "inside" : null;
    }
    const { top, height } = event.currentTarget.getBoundingClientRect();
    const progress = height === 0 ? 0.5 : (event.clientY - top) / height;
    const position = item.type === "composition" ? progress < 0.5 ? "before" : "after" : progress < 0.25 ? "before" : progress > 0.75 ? "after" : "inside";
    const target = itemToDescriptor(item);
    if (itemsAreEqual(activeDrag.item, target)) {
      return null;
    }
    if (position === "before" && previousSibling !== null && itemsAreEqual(activeDrag.item, itemToDescriptor(previousSibling))) {
      return null;
    }
    if (position === "after" && nextSibling !== null && itemsAreEqual(activeDrag.item, itemToDescriptor(nextSibling))) {
      return null;
    }
    if (position === "inside" && item.type === "folder") {
      return canMoveIntoFolder({
        activeDrag,
        destinationFolderPath: getFolderPath(item)
      }) ? position : null;
    }
    return wouldMoveFolderIntoItself({
      activeDrag,
      destinationParentFolderPath: parentFolderPath
    }) ? null : position;
  }, [
    activeDragRef,
    canReorder,
    item,
    nextSibling,
    parentFolderPath,
    previousSibling
  ]);
  const scheduleFolderExpansion = useCallback5(() => {
    if (item.type !== "folder" || item.expanded || folderExpansionTimer.current !== null || folderExpansionRequested.current) {
      return;
    }
    const activeDrag = activeDragRef.current;
    if (activeDrag === null || !canMoveIntoFolder({
      activeDrag,
      destinationFolderPath: getFolderPath(item)
    })) {
      return;
    }
    folderExpansionTimer.current = window.setTimeout(() => {
      folderExpansionTimer.current = null;
      const currentActiveDrag = activeDragRef.current;
      if (currentActiveDrag === null || !canMoveIntoFolder({
        activeDrag: currentActiveDrag,
        destinationFolderPath: getFolderPath(item)
      })) {
        return;
      }
      folderExpansionRequested.current = true;
      toggleFolder(item.folderName, item.parentName);
    }, folderAutoExpansionDelay);
  }, [activeDragRef, item, toggleFolder]);
  const onRowDragOver = useCallback5((event) => {
    if (window.remotion_isReadOnlyStudio || !hasCompositionSelectorDragData(event.dataTransfer.types)) {
      return;
    }
    const position = getDropPosition(event);
    if (position === null) {
      cancelFolderExpansion();
      updateDropPosition(null);
      event.stopPropagation();
      return;
    }
    if (position === "inside") {
      scheduleFolderExpansion();
    } else {
      cancelFolderExpansion();
    }
    event.preventDefault();
    event.stopPropagation();
    event.dataTransfer.dropEffect = "move";
    clearRootDragHover();
    updateDropPosition(position);
  }, [
    cancelFolderExpansion,
    clearRootDragHover,
    getDropPosition,
    scheduleFolderExpansion,
    updateDropPosition
  ]);
  const onRowDragLeave = useCallback5(() => {
    cancelFolderExpansion();
    updateDropPosition(null);
  }, [cancelFolderExpansion, updateDropPosition]);
  const moveItem = useCallback5(async ({
    destination,
    dragData
  }) => {
    const label2 = dragData.item.type === "composition" ? dragData.item.compositionId : dragData.item.folderName;
    const notification = showNotification(`Moving ${label2}...`, null);
    try {
      const result = await applyCodemod({
        codemod: {
          type: "move-composition-or-folder",
          source: dragData.item,
          destination
        },
        dryRun: false,
        signal: new AbortController().signal,
        symbolicatedStack: compositionSelectorDragDataToSymbolicatedStack(dragData),
        undoRedoNavigation: null
      });
      if (result.success) {
        notification.dismiss();
      } else {
        notification.replaceContent(result.reason, 4000);
      }
      if (result.success && destination.type === "folder" && item.type === "folder" && !item.expanded) {
        toggleFolder(item.folderName, item.parentName);
      }
    } catch (err) {
      notification.replaceContent(err instanceof Error ? err.message : String(err), 4000);
    }
  }, [item, toggleFolder]);
  const onRowDrop = useCallback5(async (event) => {
    cancelFolderExpansion();
    const dragData = parseCompositionSelectorDragData(event.dataTransfer);
    const position = dropPositionRef.current;
    if (dragData === null) {
      return;
    }
    if (position === null) {
      event.stopPropagation();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    clearRootDragHover();
    updateDropPosition(null);
    await moveItem({
      dragData,
      destination: position === "inside" && item.type === "folder" ? {
        type: "folder",
        folderName: item.folderName,
        parentName: item.parentName
      } : {
        type: position === "before" ? "before" : "after",
        target: itemToDescriptor(item)
      }
    });
  }, [
    cancelFolderExpansion,
    clearRootDragHover,
    item,
    moveItem,
    updateDropPosition
  ]);
  const onFolderChildListDragOver = useCallback5((event) => {
    if (item.type !== "folder" || window.remotion_isReadOnlyStudio || !hasCompositionSelectorDragData(event.dataTransfer.types)) {
      return;
    }
    const activeDrag = activeDragRef.current;
    if (activeDrag === null || !canMoveIntoFolder({
      activeDrag,
      destinationFolderPath: getFolderPath(item)
    })) {
      event.stopPropagation();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.dataTransfer.dropEffect = "move";
    clearRootDragHover();
  }, [activeDragRef, clearRootDragHover, item]);
  const onFolderChildListDrop = useCallback5(async (event) => {
    if (item.type !== "folder" || window.remotion_isReadOnlyStudio) {
      return;
    }
    const dragData = parseCompositionSelectorDragData(event.dataTransfer);
    if (dragData === null) {
      return;
    }
    const activeDrag = activeDragRef.current;
    if (activeDrag === null || !canMoveIntoFolder({
      activeDrag,
      destinationFolderPath: getFolderPath(item)
    })) {
      event.stopPropagation();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    clearRootDragHover();
    await moveItem({
      dragData,
      destination: {
        type: "folder",
        folderName: item.folderName,
        parentName: item.parentName
      }
    });
  }, [activeDragRef, clearRootDragHover, item, moveItem]);
  if (item.type === "folder") {
    const folderPath = getFolderPath(item);
    return /* @__PURE__ */ jsxs4(Fragment2, {
      children: [
        /* @__PURE__ */ jsx7(ContextMenu, {
          getItems: getContextMenuItems,
          children: /* @__PURE__ */ jsxs4("div", {
            style: reorderWrapper,
            children: [
              reorderLineStyle ? /* @__PURE__ */ jsx7("div", {
                "data-composition-reorder-line": true,
                style: reorderLineStyle
              }) : null,
              /* @__PURE__ */ jsx7(Row, {
                align: "center",
                children: /* @__PURE__ */ jsxs4("div", {
                  style,
                  className: `__remotion-composition-selector-item ${HOVERABLE_CLASS_NAME} ${HOVER_GROUP_CLASS_NAME}`,
                  tabIndex,
                  onClick,
                  onKeyDown,
                  draggable: !window.remotion_isReadOnlyStudio,
                  onDragStart: onItemDragStart,
                  onDragEnd: onItemDragEnd,
                  onDragOver: onRowDragOver,
                  onDragLeave: onRowDragLeave,
                  onDrop: onRowDrop,
                  title: item.folderName,
                  role: "button",
                  "aria-expanded": item.expanded,
                  children: [
                    item.expanded ? /* @__PURE__ */ jsx7(ExpandedFolderIcon, {
                      style: iconStyle,
                      color: CURRENT_COLOR
                    }) : /* @__PURE__ */ jsx7(CollapsedFolderIcon, {
                      color: CURRENT_COLOR,
                      style: iconStyle
                    }),
                    /* @__PURE__ */ jsx7(Spacing, {
                      x: 1
                    }),
                    /* @__PURE__ */ jsx7("div", {
                      style: labelStyle,
                      children: item.folderName
                    }),
                    /* @__PURE__ */ jsx7(CompositionContextButton, {
                      getItems: getContextMenuItems,
                      visible: true,
                      readOnlyStudio: window.remotion_isReadOnlyStudio
                    })
                  ]
                })
              })
            ]
          })
        }),
        item.expanded ? /* @__PURE__ */ jsx7("div", {
          onDragOver: onFolderChildListDragOver,
          onDrop: onFolderChildListDrop,
          children: item.items.map((childItem, index) => {
            return /* @__PURE__ */ jsx7(CompositionSelectorItem, {
              currentComposition,
              selectComposition,
              item: childItem,
              tabIndex,
              level: level + 1,
              toggleFolder,
              clearRootDragHover,
              canReorder,
              activeDragRef,
              parentFolderPath: folderPath,
              previousSibling: index === 0 ? null : item.items[index - 1],
              nextSibling: index === item.items.length - 1 ? null : item.items[index + 1]
            }, childItem.key + childItem.type);
          })
        }) : null
      ]
    });
  }
  return /* @__PURE__ */ jsx7(ContextMenu, {
    getItems: getContextMenuItems,
    children: /* @__PURE__ */ jsxs4("div", {
      style: reorderWrapper,
      children: [
        reorderLineStyle ? /* @__PURE__ */ jsx7("div", {
          "data-composition-reorder-line": true,
          style: reorderLineStyle
        }) : null,
        /* @__PURE__ */ jsx7(Row, {
          align: "center",
          children: /* @__PURE__ */ jsxs4("a", {
            ref: compositionRowRef,
            style,
            tabIndex,
            onClick,
            onKeyDown,
            draggable: !window.remotion_isReadOnlyStudio,
            onDragStart: onItemDragStart,
            onDragEnd: onItemDragEnd,
            onDragOver: onRowDragOver,
            onDragLeave: onRowDragLeave,
            onDrop: onRowDrop,
            type: "button",
            title: item.composition.id,
            className: `__remotion-composition __remotion-composition-selector-item ${HOVERABLE_CLASS_NAME} ${HOVER_GROUP_CLASS_NAME}`,
            "data-compname": item.composition.id,
            children: [
              /* @__PURE__ */ jsx7(CompositionOrStillIcon, {
                composition: item.composition,
                color: CURRENT_COLOR,
                style: iconStyle
              }),
              /* @__PURE__ */ jsx7(Spacing, {
                x: 1
              }),
              /* @__PURE__ */ jsx7("div", {
                style: labelStyle,
                children: item.composition.id
              }),
              /* @__PURE__ */ jsx7(CompositionContextButton, {
                getItems: getContextMenuItems,
                visible: !isDragging,
                readOnlyStudio: window.remotion_isReadOnlyStudio
              }),
              /* @__PURE__ */ jsx7(SidebarRenderButton, {
                visible: !isDragging,
                composition: item.composition,
                readOnlyStudio: window.remotion_isReadOnlyStudio
              })
            ]
          })
        })
      ]
    })
  });
};

// src/components/QuickSwitcher/ExplorerQuickSwitcherTrigger.tsx
import { useCallback as useCallback6, useContext as useContext5 } from "react";

// src/helpers/use-keyboard-shortcut-label.ts
var useKeyboardShortcutLabel = (action) => {
  useStudioConfigRevision();
  return getKeyboardShortcutLabel(action);
};
var useKeyboardShortcutAriaKeyShortcuts = (action) => {
  useStudioConfigRevision();
  return getKeyboardShortcutAriaKeyShortcuts(action);
};

// src/components/QuickSwitcher/ExplorerQuickSwitcherTrigger.tsx
import { jsx as jsx8, jsxs as jsxs5 } from "react/jsx-runtime";
var quickSwitcherArea = {
  padding: "4px 4px 4px 8px",
  borderBottom: `1px solid ${BLACK_HEX}`,
  display: "flex",
  alignItems: "center",
  gap: 4
};
var quickSwitcherTrigger = {
  borderRadius: 4,
  padding: "4px 10px 4px 12px",
  fontSize: 12,
  cursor: "default",
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  border: "none",
  flex: 1,
  appearance: "none",
  userSelect: "none",
  ...hoverableStyle({
    idleBackground: WHITE_ALPHA_06,
    hoverBackground: WHITE_ALPHA_06,
    idleColor: LIGHT_TEXT,
    hoverColor: WHITE
  })
};
var ellipsisSvgProps = {
  style: {
    height: 12
  }
};
var shortcutLabel = {
  fontSize: 11,
  opacity: 0.6
};
var ExplorerQuickSwitcherTrigger = ({ mode, showShortcut, tabIndex, getActions }) => {
  const { setSelectedModal } = useContext5(SetSelectedModalContext);
  const openQuickSwitcher = useCallback6(() => {
    setSelectedModal({
      type: "quick-switcher",
      mode,
      invocationTimestamp: Date.now(),
      assetSelection: null,
      compositionSelection: null
    });
  }, [mode, setSelectedModal]);
  const renderMoreActions = useCallback6((color) => {
    return /* @__PURE__ */ jsx8(EllipsisIcon, {
      svgProps: ellipsisSvgProps,
      fill: color
    });
  }, []);
  const moreActionsTitle = mode === "assets" ? "More asset actions" : "More composition actions";
  const quickSwitcherShortcut = useKeyboardShortcutLabel("quickSwitcher");
  return /* @__PURE__ */ jsxs5("div", {
    style: quickSwitcherArea,
    children: [
      /* @__PURE__ */ jsxs5("button", {
        type: "button",
        style: quickSwitcherTrigger,
        onClick: openQuickSwitcher,
        tabIndex,
        className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
        children: [
          "Search...",
          showShortcut && !areKeyboardShortcutsDisabled() && quickSwitcherShortcut !== "" ? /* @__PURE__ */ jsx8("span", {
            style: shortcutLabel,
            children: quickSwitcherShortcut
          }) : null
        ]
      }),
      /* @__PURE__ */ jsx8(InlineDropdown, {
        variant: null,
        title: moreActionsTitle,
        renderAction: renderMoreActions,
        getItems: getActions,
        className: FOCUS_VISIBLE_ONLY_CLASS_NAME
      })
    ]
  });
};

// src/components/root-composition-menu-items.tsx
import { jsx as jsx9 } from "react/jsx-runtime";
var getRootCompositionMenuItems = ({
  connectionStatus,
  readOnlyStudio,
  setSelectedModal,
  compositionSortOrder,
  setCompositionSortOrder
}) => {
  return [
    {
      id: "new-root-composition",
      keyHint: null,
      label: "New composition...",
      leftItem: null,
      onClick: () => {
        setSelectedModal({
          type: "new-comp",
          folderName: null,
          parentName: null,
          stack: null,
          canvasCapture: null
        });
      },
      quickSwitcherLabel: "New composition...",
      subMenu: null,
      type: "item",
      value: "new-root-composition",
      disabled: readOnlyStudio || connectionStatus !== "connected"
    },
    {
      id: "new-root-folder",
      keyHint: null,
      label: "New folder...",
      leftItem: null,
      onClick: () => {
        setSelectedModal({
          type: "new-folder",
          parentName: null,
          stack: null
        });
      },
      quickSwitcherLabel: "New folder...",
      subMenu: null,
      type: "item",
      value: "new-root-folder",
      disabled: readOnlyStudio || connectionStatus !== "connected"
    },
    {
      id: "sort-compositions-divider",
      type: "divider"
    },
    {
      id: "sort-compositions-header",
      label: "Sort",
      type: "section-header"
    },
    {
      id: "sort-compositions-registration",
      keyHint: null,
      label: "As registered",
      leftItem: compositionSortOrder === "registration" ? /* @__PURE__ */ jsx9(Checkmark, {}) : null,
      onClick: () => {
        setCompositionSortOrder("registration");
      },
      quickSwitcherLabel: "Sort compositions as registered",
      subMenu: null,
      type: "item",
      value: "registration"
    },
    {
      id: "sort-compositions-alphabetical",
      keyHint: null,
      label: "Alphabetically",
      leftItem: compositionSortOrder === "alphabetical" ? /* @__PURE__ */ jsx9(Checkmark, {}) : null,
      onClick: () => {
        setCompositionSortOrder("alphabetical");
      },
      quickSwitcherLabel: "Sort compositions alphabetically",
      subMenu: null,
      type: "item",
      value: "alphabetical"
    }
  ];
};

// src/components/CompositionSelector.tsx
import { jsx as jsx10, jsxs as jsxs6 } from "react/jsx-runtime";
var useCompositionNavigation = () => {
  const { compositions, canvasContent } = useContext6(Internals4.CompositionManager);
  const selectComposition = useSelectComposition();
  const navigateToNextComposition = useCallback7(() => {
    if (!canvasContent || canvasContent.type !== "composition" || compositions.length <= 1) {
      return;
    }
    const currentIndex = compositions.findIndex((c) => c.id === canvasContent.compositionId);
    if (currentIndex === -1) {
      return;
    }
    const nextIndex = (currentIndex + 1) % compositions.length;
    const nextComposition = compositions[nextIndex];
    selectComposition(nextComposition, true);
  }, [canvasContent, compositions, selectComposition]);
  const navigateToPreviousComposition = useCallback7(() => {
    if (!canvasContent || canvasContent.type !== "composition" || compositions.length <= 1) {
      return;
    }
    const currentIndex = compositions.findIndex((c) => c.id === canvasContent.compositionId);
    if (currentIndex === -1) {
      return;
    }
    const previousIndex = (currentIndex - 1 + compositions.length) % compositions.length;
    const previousComposition = compositions[previousIndex];
    selectComposition(previousComposition, true);
  }, [canvasContent, compositions, selectComposition]);
  return useMemo5(() => ({
    navigateToNextComposition,
    navigateToPreviousComposition
  }), [navigateToNextComposition, navigateToPreviousComposition]);
};
var container4 = {
  display: "flex",
  flexDirection: "column",
  flex: 1,
  overflow: "hidden",
  backgroundColor: BACKGROUND
};
var list = {
  flex: 1,
  overflowY: "auto",
  paddingTop: 4,
  paddingBottom: 4,
  backgroundColor: BACKGROUND
};
var rootReorderLine = {
  backgroundColor: TIMELINE_BLUE,
  height: 2,
  pointerEvents: "none",
  width: "100%"
};
var autoScrollThreshold = 70;
var maxAutoScrollSpeed = 18;
var getAutoScrollSpeed = ({
  clientY,
  element
}) => {
  const { top, bottom } = element.getBoundingClientRect();
  const threshold = Math.min(autoScrollThreshold, element.clientHeight / 2);
  if (threshold <= 0) {
    return 0;
  }
  const distanceToTop = clientY - top;
  const distanceToBottom = bottom - clientY;
  if (distanceToTop < threshold) {
    const progress = Math.min(1, (threshold - distanceToTop) / threshold);
    return -Math.ceil(progress * maxAutoScrollSpeed);
  }
  if (distanceToBottom < threshold) {
    const progress = Math.min(1, (threshold - distanceToBottom) / threshold);
    return Math.ceil(progress * maxAutoScrollSpeed);
  }
  return 0;
};
var CompositionSelector = () => {
  const { compositions, canvasContent, folders } = useContext6(Internals4.CompositionManager);
  const { foldersExpanded } = useContext6(ExpandedFoldersContext);
  const { compositionSortOrder, setCompositionSortOrder } = useContext6(FolderContext);
  const { setSelectedModal } = useContext6(SetSelectedModalContext);
  const connectionStatus = useContext6(StudioServerConnectionCtx).previewServerState.type;
  const getRootContextMenuItems = useCallback7(() => {
    return getRootCompositionMenuItems({
      connectionStatus,
      readOnlyStudio: window.remotion_isReadOnlyStudio,
      setSelectedModal,
      compositionSortOrder,
      setCompositionSortOrder
    });
  }, [
    compositionSortOrder,
    connectionStatus,
    setCompositionSortOrder,
    setSelectedModal
  ]);
  const [rootDragHovered, setRootDragHovered] = useState3(false);
  const activeDragRef = useRef3(null);
  const listRef = useRef3(null);
  const autoScrollAnimation = useRef3(null);
  const autoScrollSpeed = useRef3(0);
  const { tabIndex } = useZIndex();
  const selectComposition = useSelectComposition();
  const items = useMemo5(() => {
    const tree = createFolderTree(compositions, folders, foldersExpanded);
    return compositionSortOrder === "alphabetical" ? sortFolderTreeAlphabetically(tree) : tree;
  }, [compositionSortOrder, compositions, folders, foldersExpanded]);
  const toggleFolder = useCallback7((folderName, parentName) => {
    Internals4.compositionSelectorRef.current?.toggleFolder(folderName, parentName);
  }, []);
  const clearRootDragHover = useCallback7(() => {
    setRootDragHovered(false);
  }, []);
  const stopCompositionListAutoScroll = useCallback7(() => {
    autoScrollSpeed.current = 0;
    if (autoScrollAnimation.current !== null) {
      cancelAnimationFrame(autoScrollAnimation.current);
      autoScrollAnimation.current = null;
    }
  }, []);
  const runCompositionListAutoScroll = useCallback7(() => {
    const listElement = listRef.current;
    const speed = autoScrollSpeed.current;
    if (listElement === null || speed === 0) {
      autoScrollAnimation.current = null;
      return;
    }
    const scrollTopBefore = listElement.scrollTop;
    listElement.scrollTop += speed;
    if (listElement.scrollTop === scrollTopBefore) {
      autoScrollAnimation.current = null;
      return;
    }
    autoScrollAnimation.current = requestAnimationFrame(runCompositionListAutoScroll);
  }, []);
  const setCompositionListAutoScrollSpeed = useCallback7((speed) => {
    if (speed === 0) {
      stopCompositionListAutoScroll();
      return;
    }
    autoScrollSpeed.current = speed;
    if (autoScrollAnimation.current === null) {
      autoScrollAnimation.current = requestAnimationFrame(runCompositionListAutoScroll);
    }
  }, [runCompositionListAutoScroll, stopCompositionListAutoScroll]);
  const onCompositionListDragOverCapture = useCallback7((event) => {
    if (window.remotion_isReadOnlyStudio || !hasCompositionSelectorDragData(event.dataTransfer.types)) {
      stopCompositionListAutoScroll();
      return;
    }
    const listElement = listRef.current;
    if (listElement === null) {
      stopCompositionListAutoScroll();
      return;
    }
    setCompositionListAutoScrollSpeed(getAutoScrollSpeed({ clientY: event.clientY, element: listElement }));
  }, [setCompositionListAutoScrollSpeed, stopCompositionListAutoScroll]);
  useEffect4(() => {
    return stopCompositionListAutoScroll;
  }, [stopCompositionListAutoScroll]);
  const onRootDragOver = useCallback7((event) => {
    if (window.remotion_isReadOnlyStudio || !hasCompositionSelectorDragData(event.dataTransfer.types)) {
      return;
    }
    if (activeDragRef.current === null || activeDragRef.current.parentFolderPath === null) {
      setRootDragHovered(false);
      return;
    }
    event.preventDefault();
    event.dataTransfer.dropEffect = "move";
    setRootDragHovered(true);
  }, []);
  const onRootDragLeave = useCallback7((event) => {
    const { relatedTarget } = event;
    if (relatedTarget instanceof Node && event.currentTarget.contains(relatedTarget)) {
      return;
    }
    stopCompositionListAutoScroll();
    setRootDragHovered(false);
  }, [stopCompositionListAutoScroll]);
  const onRootDrop = useCallback7(async (event) => {
    if (window.remotion_isReadOnlyStudio) {
      return;
    }
    const dragData = parseCompositionSelectorDragData(event.dataTransfer);
    if (dragData === null) {
      return;
    }
    if (activeDragRef.current === null || activeDragRef.current.parentFolderPath === null) {
      event.stopPropagation();
      setRootDragHovered(false);
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    stopCompositionListAutoScroll();
    setRootDragHovered(false);
    const notification = showNotification(`Moving ${dragData.item.type === "composition" ? dragData.item.compositionId : dragData.item.folderName}...`, null);
    const controller = new AbortController;
    try {
      const result = await applyCodemod({
        codemod: {
          type: "move-composition-or-folder",
          source: dragData.item,
          destination: { type: "root" }
        },
        dryRun: false,
        signal: controller.signal,
        symbolicatedStack: compositionSelectorDragDataToSymbolicatedStack(dragData),
        undoRedoNavigation: null
      });
      if (result.success) {
        notification.dismiss();
      } else {
        notification.replaceContent(result.reason, 4000);
      }
    } catch (err) {
      notification.replaceContent(err instanceof Error ? err.message : String(err), 4000);
    }
  }, [stopCompositionListAutoScroll]);
  return /* @__PURE__ */ jsxs6("div", {
    style: container4,
    children: [
      /* @__PURE__ */ jsx10(ContextMenuForTarget, {
        triggerRef: listRef,
        getItems: getRootContextMenuItems
      }),
      /* @__PURE__ */ jsx10(ExplorerQuickSwitcherTrigger, {
        mode: "compositions",
        showShortcut: true,
        tabIndex,
        getActions: getRootContextMenuItems
      }),
      /* @__PURE__ */ jsxs6("div", {
        ref: listRef,
        className: "__remotion-vertical-scrollbar",
        style: list,
        onDragOverCapture: onCompositionListDragOverCapture,
        onDragEndCapture: stopCompositionListAutoScroll,
        onDragOver: onRootDragOver,
        onDragLeave: onRootDragLeave,
        onDropCapture: stopCompositionListAutoScroll,
        onDrop: onRootDrop,
        children: [
          items.map((c, index) => {
            return /* @__PURE__ */ jsx10(CompositionSelectorItem, {
              level: 0,
              currentComposition: canvasContent && canvasContent.type === "composition" ? canvasContent.compositionId : null,
              selectComposition,
              toggleFolder,
              clearRootDragHover,
              canReorder: compositionSortOrder === "registration",
              tabIndex,
              item: c,
              activeDragRef,
              parentFolderPath: null,
              previousSibling: index === 0 ? null : items[index - 1],
              nextSibling: index === items.length - 1 ? null : items[index + 1]
            }, c.key + c.type);
          }),
          rootDragHovered ? /* @__PURE__ */ jsx10("div", {
            "data-composition-root-reorder-line": true,
            style: rootReorderLine
          }) : null
        ]
      })
    ]
  });
};

// src/components/GlobalKeybindings.tsx
var sequencePropShortcuts = [
  { action: "selectTranslateProp", fieldKey: "style.translate" },
  { action: "selectRotateProp", fieldKey: "style.rotate" },
  { action: "selectScaleProp", fieldKey: "style.scale" },
  { action: "selectOpacityProp", fieldKey: "style.opacity" }
];
var hasOwnProperty = (obj, key3) => Object.prototype.hasOwnProperty.call(obj, key3);
var GlobalKeybindings = () => {
  const keybindings = useKeybinding();
  const { setSelectedModal } = useContext7(SetSelectedModalContext);
  const { setCheckerboard } = useContext7(CheckerboardContext);
  const { setEditorSnapping } = useContext7(EditorSnappingContext);
  const { canvasContent } = useContext7(Internals5.CompositionManager);
  const { setEditorShowOutlines } = useContext7(EditorShowOutlinesContext);
  const { editorShowRulers, setEditorShowRulers } = useContext7(EditorShowRulersContext);
  const { editorShowGuides, setEditorShowGuides } = useContext7(EditorShowGuidesContext);
  const showGuides = canvasContent?.type === "composition";
  const showCanvasViewControls = showGuides || canvasContent?.type === "asset" && getPreviewFileType(canvasContent.asset) === "video";
  useEffect5(() => {
    if (!showCanvasViewControls)
      return;
    const outlines = showGuides ? keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleOutlines",
      callback: (event) => {
        if (!event.repeat)
          setEditorShowOutlines((current) => !current);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    }) : null;
    const rulers = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleRulersAndGuides",
      callback: (event) => {
        if (event.repeat)
          return;
        const visible = editorShowRulers || showGuides && editorShowGuides;
        setEditorShowRulers(() => !visible);
        if (showGuides)
          setEditorShowGuides(() => !visible);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      outlines?.unregister();
      rulers.unregister();
    };
  }, [
    keybindings,
    showCanvasViewControls,
    showGuides,
    editorShowRulers,
    editorShowGuides,
    setEditorShowOutlines,
    setEditorShowRulers,
    setEditorShowGuides
  ]);
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  const { sequences } = useContext7(Internals5.SequenceManager);
  const videoConfig = Internals5.useUnsafeVideoConfig();
  const { overrideIdToNodePathMappings } = useContext7(Internals5.OverrideIdsToNodePathsGettersContext);
  const { navigateToNextComposition, navigateToPreviousComposition } = useCompositionNavigation();
  const video = Internals5.useVideo();
  const timeline = useMemo6(() => {
    if (videoConfig === null) {
      return [];
    }
    return calculateTimeline({
      sequences,
      overrideIdsToNodePaths: overrideIdToNodePathMappings
    });
  }, [overrideIdToNodePathMappings, sequences, videoConfig]);
  const selectSequenceProp = useCallback8((fieldKey) => {
    const { selectedItems, selectItems } = currentSelection.current;
    if (selectedItems.length !== 1) {
      return false;
    }
    const [selection] = selectedItems;
    if (selection.type !== "sequence" && selection.type !== "sequence-prop") {
      return false;
    }
    const selectedTrackKey = getTimelineSequenceSelectionKey(selection.nodePathInfo);
    const track = timeline.find((candidate) => candidate.nodePathInfo !== null && timelineNodePathInfoToKey(candidate.nodePathInfo) === selectedTrackKey);
    if (!track?.sequence.controls) {
      return false;
    }
    const { schema, runtimeValues } = track.sequence.controls;
    const currentRuntimeValueDotNotation = runtimeValues.getSnapshot();
    if (!hasOwnProperty(schema, fieldKey) && !hasOwnProperty(currentRuntimeValueDotNotation, fieldKey)) {
      return false;
    }
    selectItems([
      {
        type: "sequence-prop",
        nodePathInfo: {
          ...selection.nodePathInfo,
          auxiliaryKeys: ["controls", fieldKey]
        },
        key: fieldKey
      }
    ], { reveal: true });
    return true;
  }, [currentSelection, timeline]);
  const openRenderModal = useCallback8(() => {
    if (!video) {
      return;
    }
    const renderButton = document.getElementById("render-modal-button");
    renderButton.click();
  }, [video]);
  useEffect5(() => {
    const cmdKKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "quickSwitcher",
      callback: () => {
        setSelectedModal({
          type: "quick-switcher",
          mode: explorerSidebarTabs.current?.getSelectedPanel() === "assets" ? "assets" : "compositions",
          invocationTimestamp: Date.now(),
          assetSelection: null,
          compositionSelection: null
        });
      },
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false,
      preventDefault: true
    });
    const cmdSKey = keybindings.registerKeybinding({
      event: "keydown",
      key: "s",
      callback: () => {
        showNotification("Remotion saves automatically", 2000);
      },
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false,
      commandCtrlKey: true,
      preventDefault: true
    });
    const cmdIKey = getStudioAskAIEnabled() ? keybindings.registerKeybinding({
      event: "keydown",
      action: "askAI",
      callback: () => {
        askAiModalRef.current?.toggle();
      },
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: true,
      preventDefault: true
    }) : null;
    const colorPicker = "EyeDropper" in window ? keybindings.registerKeybinding({
      event: "keydown",
      action: "pickColor",
      callback: () => {
        pickColor();
      },
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false,
      preventDefault: true
    }) : null;
    const sequencePropKeys = sequencePropShortcuts.map(({ action, fieldKey }) => keybindings.registerKeybinding({
      event: "keydown",
      action,
      callback: (event) => {
        if (selectSequenceProp(fieldKey)) {
          event.preventDefault();
        }
      },
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    }));
    const render = keybindings.registerKeybinding({
      event: "keydown",
      action: "render",
      callback: (event) => {
        if (event.defaultPrevented)
          return;
        openRenderModal();
        event.preventDefault();
      },
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const checkerboard = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleCheckerboard",
      callback: (event) => {
        if (event.defaultPrevented)
          return;
        setCheckerboard((current) => !current);
        event.preventDefault();
      },
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const questionMark = keybindings.registerKeybinding({
      event: "keydown",
      action: "showKeyboardShortcuts",
      callback: () => {
        setSelectedModal({
          type: "settings",
          initialTab: "shortcuts",
          initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
        });
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const pageDown = keybindings.registerKeybinding({
      event: "keydown",
      action: "nextComposition",
      callback: navigateToNextComposition,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const pageUp = keybindings.registerKeybinding({
      event: "keydown",
      action: "previousComposition",
      callback: navigateToPreviousComposition,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const shiftMKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleSnapping",
      callback: () => {
        setEditorSnapping((current) => !current);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      for (const sequencePropKey of sequencePropKeys) {
        sequencePropKey.unregister();
      }
      render.unregister();
      checkerboard.unregister();
      questionMark.unregister();
      cmdKKey.unregister();
      cmdSKey.unregister();
      cmdIKey?.unregister();
      colorPicker?.unregister();
      pageDown.unregister();
      pageUp.unregister();
      shiftMKey.unregister();
    };
  }, [
    keybindings,
    openRenderModal,
    selectSequenceProp,
    setCheckerboard,
    setEditorSnapping,
    setSelectedModal,
    navigateToNextComposition,
    navigateToPreviousComposition
  ]);
  return null;
};

// src/components/MenuToolbar.tsx
import { useCallback as useCallback56, useContext as useContext67, useMemo as useMemo58, useState as useState37 } from "react";

// src/helpers/use-menu-structure.tsx
import { useContext as useContext10, useEffect as useEffect7, useMemo as useMemo9 } from "react";
import { Internals as Internals9 } from "remotion";
import { NoReactInternals as NoReactInternals2 } from "remotion/no-react";

// src/components/SizeSelector.tsx
import { useContext as useContext8, useMemo as useMemo7 } from "react";
import { Internals as Internals6 } from "remotion";

// src/icons/canvas-zoom.tsx
import { jsx as jsx11 } from "react/jsx-runtime";
var iconStyle2 = {
  width: 20,
  height: 20,
  flexShrink: 0
};
var CanvasZoomIcon = ({ color }) => {
  return /* @__PURE__ */ jsx11("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 640",
    style: iconStyle2,
    children: /* @__PURE__ */ jsx11("path", {
      fill: color,
      d: "M272 96C369.2 96 448 174.8 448 272C448 369.2 369.2 448 272 448C174.8 448 96 369.2 96 272C96 174.8 174.8 96 272 96zM272 480C323.7 480 371 461.2 407.3 430L548.7 571.3C554.9 577.5 565.1 577.5 571.3 571.3C577.5 565.1 577.5 554.9 571.3 548.7L430 407.3C461.2 371 480 323.7 480 272C480 157.1 386.9 64 272 64C157.1 64 64 157.1 64 272C64 386.9 157.1 480 272 480zM256 344C256 352.8 263.2 360 272 360C280.8 360 288 352.8 288 344L288 288L344 288C352.8 288 360 280.8 360 272C360 263.2 352.8 256 344 256L288 256L288 200C288 191.2 280.8 184 272 184C263.2 184 256 191.2 256 200L256 256L200 256C191.2 256 184 263.2 184 272C184 280.8 191.2 288 200 288L256 288L256 344z"
    })
  });
};
var CanvasFitIcon = ({ color }) => {
  return /* @__PURE__ */ jsx11("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 640",
    style: iconStyle2,
    children: /* @__PURE__ */ jsx11("path", {
      fill: color,
      d: "M448 272C448 174.8 369.2 96 272 96C174.8 96 96 174.8 96 272C96 369.2 174.8 448 272 448C369.2 448 448 369.2 448 272zM407.3 430C371 461.2 323.7 480 272 480C157.1 480 64 386.9 64 272C64 157.1 157.1 64 272 64C386.9 64 480 157.1 480 272C480 323.7 461.2 371 430 407.3L571.3 548.7C577.5 554.9 577.5 565.1 571.3 571.3C565.1 577.5 554.9 577.5 548.7 571.3L407.3 430z"
    })
  });
};
var CanvasZoomOutIcon = ({ color }) => {
  return /* @__PURE__ */ jsx11("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 640",
    style: iconStyle2,
    children: /* @__PURE__ */ jsx11("path", {
      fill: color,
      d: "M272 96C369.2 96 448 174.8 448 272C448 369.2 369.2 448 272 448C174.8 448 96 369.2 96 272C96 174.8 174.8 96 272 96zM272 480C323.7 480 371 461.2 407.3 430L548.7 571.3C554.9 577.5 565.1 577.5 571.3 571.3C577.5 565.1 577.5 554.9 571.3 548.7L430 407.3C461.2 371 480 323.7 480 272C480 157.1 386.9 64 272 64C157.1 64 64 157.1 64 272C64 386.9 157.1 480 272 480zM192 256C183.2 256 176 263.2 176 272C176 280.8 183.2 288 192 288L352 288C360.8 288 368 280.8 368 272C368 263.2 360.8 256 352 256L192 256z"
    })
  });
};

// src/components/TimelineCombobox.tsx
import { jsx as jsx12, jsxs as jsxs7, Fragment as Fragment3 } from "react/jsx-runtime";
var segmentedButtonStyle = {
  height: 28
};
var label2 = {
  flex: "none",
  fontFamily: "inherit",
  fontSize: 12,
  lineHeight: "16px",
  minWidth: 0,
  overflow: "hidden",
  textAlign: "left",
  textOverflow: "ellipsis",
  userSelect: "none",
  whiteSpace: "nowrap"
};
var TimelineCombobox = ({
  values,
  selectedId,
  title,
  tooltipDelay,
  labelWidth = 32,
  renderLeftItem,
  unhoveredIconColor = LIGHT_TEXT
}) => {
  const selected = values.find((value) => value.id === selectedId);
  const segments = [
    {
      ariaLabel: title,
      buttonId: null,
      disabled: false,
      idleColor: unhoveredIconColor,
      leaveLeftSpace: true,
      onOpenChange: null,
      renderContent: (color) => /* @__PURE__ */ jsxs7(Fragment3, {
        children: [
          renderLeftItem ? /* @__PURE__ */ jsxs7(Fragment3, {
            children: [
              renderLeftItem(color),
              /* @__PURE__ */ jsx12(Spacing, {
                x: 0.5
              })
            ]
          }) : null,
          selected ? /* @__PURE__ */ jsx12("div", {
            title: tooltipDelay === null && typeof selected.label === "string" ? selected.label : undefined,
            style: { ...label2, width: labelWidth },
            children: selected.label
          }) : null,
          /* @__PURE__ */ jsx12(Spacing, {
            x: 0.5
          }),
          /* @__PURE__ */ jsx12(CaretDown, {
            color
          })
        ]
      }),
      segmentId: "selector",
      selectedId,
      style: { fontFamily: "inherit", padding: "0 4px" },
      title: tooltipDelay === null ? title : null,
      tooltipLabel: null,
      type: "menu",
      values
    }
  ];
  const button = /* @__PURE__ */ jsx12(SegmentedButton, {
    segments,
    style: segmentedButtonStyle,
    title: null
  });
  return tooltipDelay === null ? button : /* @__PURE__ */ jsx12(ActionTooltip, {
    label: title,
    shortcut: null,
    delay: tooltipDelay,
    dismissOnClick: true,
    children: button
  });
};

// src/components/SizeSelector.tsx
import { jsx as jsx13 } from "react/jsx-runtime";
var commonPreviewSizes = [
  {
    size: "auto",
    translation: {
      x: 0,
      y: 0
    }
  },
  {
    size: 0.25,
    translation: {
      x: 0,
      y: 0
    }
  },
  {
    size: 0.5,
    translation: {
      x: 0,
      y: 0
    }
  },
  {
    size: 1,
    translation: {
      x: 0,
      y: 0
    }
  }
];
var getPreviewSizeLabel = (previewSize) => {
  if (previewSize.size === "auto") {
    return "Fit";
  }
  return `${(previewSize.size * 100).toFixed(0)}%`;
};
var accessibilityLabel = "Zoom";
var getUniqueSizes = (size) => {
  const customPreviewSizes = [size, ...commonPreviewSizes];
  const uniqueSizes = [];
  customPreviewSizes.forEach((p) => {
    if (!uniqueSizes.find((s) => s.size === p.size)) {
      uniqueSizes.push(p);
    }
  });
  return uniqueSizes.sort((a, b) => {
    if (a.size === "auto") {
      return -1;
    }
    if (b.size === "auto") {
      return 1;
    }
    return a.size - b.size;
  });
};
var zoomableFileTypes = ["video", "image"];
var usePreviewSizeMenuItems = () => {
  const { size, setSize } = useContext8(Internals6.PreviewSizeContext);
  const { canvasContent } = useContext8(Internals6.CompositionManager);
  const zoomable = useMemo7(() => {
    if (!canvasContent) {
      return null;
    }
    if (canvasContent.type === "composition") {
      return true;
    }
    if (canvasContent.type === "asset" && zoomableFileTypes.includes(getPreviewFileType(canvasContent.asset))) {
      return true;
    }
    if (canvasContent.type === "output" && zoomableFileTypes.includes(getPreviewFileType(canvasContent.path))) {
      return true;
    }
    return false;
  }, [canvasContent]);
  const configuredResetZoomShortcut = useKeyboardShortcutLabel("resetZoom");
  const resetZoomShortcut = areKeyboardShortcutsDisabled() ? "" : configuredResetZoomShortcut;
  const items = useMemo7(() => {
    return getUniqueSizes(size).map((newSize) => {
      return {
        id: String(newSize.size),
        label: getPreviewSizeLabel(newSize),
        onClick: () => {
          return setSize(() => {
            return newSize;
          });
        },
        type: "item",
        value: newSize.size,
        keyHint: newSize.size === "auto" && resetZoomShortcut !== "" ? resetZoomShortcut : null,
        leftItem: String(size.size) === String(newSize.size) ? /* @__PURE__ */ jsx13(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      };
    });
  }, [resetZoomShortcut, setSize, size]);
  return {
    items,
    selectedId: String(size.size),
    zoomable
  };
};
var SizeSelector = () => {
  const { items, selectedId, zoomable } = usePreviewSizeMenuItems();
  if (!zoomable) {
    return null;
  }
  return /* @__PURE__ */ jsx13(TimelineCombobox, {
    title: accessibilityLabel,
    tooltipDelay: 800,
    selectedId,
    values: items,
    renderLeftItem: (color) => selectedId === "auto" ? /* @__PURE__ */ jsx13(CanvasFitIcon, {
      color
    }) : Number(selectedId) < 1 ? /* @__PURE__ */ jsx13(CanvasZoomOutIcon, {
      color
    }) : /* @__PURE__ */ jsx13(CanvasZoomIcon, {
      color
    }),
    unhoveredIconColor: WHITE_ALPHA_80
  });
};

// src/components/TimelineInOutToggle.tsx
import { PlayerInternals as PlayerInternals2 } from "@remotion/player";
import {
  createRef as createRef3,
  useCallback as useCallback9,
  useEffect as useEffect6,
  useImperativeHandle as useImperativeHandle2
} from "react";
import { Internals as Internals8 } from "remotion";

// src/icons/timelineInOutPointer.tsx
import { jsx as jsx14 } from "react/jsx-runtime";
var TimelineInPointer = (props) => {
  return /* @__PURE__ */ jsx14("svg", {
    viewBox: "0 0 192 512",
    ...props,
    children: /* @__PURE__ */ jsx14("path", {
      d: "M0 88C0 57.1 25.1 32 56 32h64c13.3 0 24 10.7 24 24s-10.7 24-24 24H56c-4.4 0-8 3.6-8 8v336c0 4.4 3.6 8 8 8h64c13.3 0 24 10.7 24 24s-10.7 24-24 24H56c-30.9 0-56-25.1-56-56V88z",
      fill: props.color
    })
  });
};
var TimelineOutPointer = (props) => {
  return /* @__PURE__ */ jsx14("svg", {
    viewBox: "0 0 192 512",
    ...props,
    children: /* @__PURE__ */ jsx14("path", {
      d: "M192 88c0-30.9-25.1-56-56-56H72C58.7 32 48 42.7 48 56s10.7 24 24 24h64c4.4 0 8 3.6 8 8v336c0 4.4-3.6 8-8 8H72c-13.3 0-24 10.7-24 24s10.7 24 24 24h64c30.9 0 56-25.1 56-56V88z",
      fill: props.color
    })
  });
};

// src/state/in-out.ts
import { createContext as createContext6, useContext as useContext9, useMemo as useMemo8 } from "react";
import { Internals as Internals7 } from "remotion";
var TimelineInOutContext = createContext6({});
var SetTimelineInOutContext = createContext6({
  setInAndOutFrames: () => {
    throw new Error("default");
  }
});
var useTimelineInOutFramePosition = () => {
  const videoConfig = Internals7.useUnsafeVideoConfig();
  const state = useContext9(TimelineInOutContext);
  const id = videoConfig?.id;
  const durationInFrames = videoConfig?.durationInFrames;
  return useMemo8(() => {
    if (!id || !durationInFrames) {
      return { inFrame: null, outFrame: null };
    }
    const maxFrame = durationInFrames - 1;
    const actualInFrame = state[id]?.inFrame ?? null;
    const actualOutFrame = state[id]?.outFrame ?? null;
    return {
      inFrame: actualInFrame === null ? null : actualInFrame >= maxFrame ? null : actualInFrame,
      outFrame: actualOutFrame === null ? null : actualOutFrame >= maxFrame ? null : actualOutFrame
    };
  }, [durationInFrames, id, state]);
};
var useTimelineSetInOutFramePosition = () => {
  const { setInAndOutFrames } = useContext9(SetTimelineInOutContext);
  return { setInAndOutFrames };
};

// src/components/ControlButton.tsx
import { jsx as jsx15 } from "react/jsx-runtime";
var ControlButton = (props) => {
  const { children, onClick, title, ...buttonProps } = props;
  const renderAction = typeof children === "function" ? children : () => children;
  return /* @__PURE__ */ jsx15(InlineAction, {
    ...buttonProps,
    title,
    onClick,
    renderAction,
    variant: null,
    unhoveredColor: WHITE_ALPHA_80
  });
};

// src/components/TimelineInOutToggle.tsx
import { jsx as jsx16, jsxs as jsxs8, Fragment as Fragment4 } from "react/jsx-runtime";
var style = {
  width: 17,
  height: 17
};
var buttonStyle2 = {
  width: 18
};
var inOutHandles = createRef3();
var defaultInOutValue = { inFrame: null, outFrame: null };
var TimelineInOutPointToggle = () => {
  const inShortcut = useKeyboardShortcutLabel("setInPoint");
  const outShortcut = useKeyboardShortcutLabel("setOutPoint");
  const inAriaShortcut = useKeyboardShortcutAriaKeyShortcuts("setInPoint");
  const outAriaShortcut = useKeyboardShortcutAriaKeyShortcuts("setOutPoint");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const { inFrame, outFrame } = useTimelineInOutFramePosition();
  const { setInAndOutFrames } = useTimelineSetInOutFramePosition();
  const videoConfig = Internals8.useUnsafeVideoConfig();
  const keybindings = useKeybinding();
  const { getCurrentFrame: getCurrentFrame2 } = PlayerInternals2.usePlayerMethods();
  const timelinePosition = Internals8.Timeline.useTimelinePosition();
  const isFirstFrame = timelinePosition === 0;
  const isLastFrame = timelinePosition === (videoConfig?.durationInFrames ?? 1) - 1;
  const onInOutClear = useCallback9((composition) => {
    setInAndOutFrames((prev) => {
      return {
        ...prev,
        [composition]: {
          inFrame: null,
          outFrame: null
        }
      };
    });
  }, [setInAndOutFrames]);
  const onInMark = useCallback9((e) => {
    if (!videoConfig) {
      return null;
    }
    if (e?.shiftKey) {
      setInAndOutFrames((prev) => {
        return {
          ...prev,
          [videoConfig.id]: {
            ...prev[videoConfig.id] ?? defaultInOutValue,
            inFrame: null
          }
        };
      });
      return null;
    }
    setInAndOutFrames((prev) => {
      const prevOut = prev[videoConfig.id]?.outFrame;
      const biggestPossible = prevOut === undefined || prevOut === null ? Infinity : prevOut - 1;
      const selected = Math.min(getCurrentFrame2(), biggestPossible);
      if (selected === 0) {
        return {
          ...prev,
          [videoConfig.id]: {
            ...prev[videoConfig.id] ?? defaultInOutValue,
            inFrame: null
          }
        };
      }
      const prevIn = prev[videoConfig.id]?.inFrame;
      if (prevIn !== null && prevIn !== undefined) {
        if (prevIn === selected) {
          return {
            ...prev,
            [videoConfig.id]: {
              ...prev[videoConfig.id] ?? defaultInOutValue,
              inFrame: null
            }
          };
        }
      }
      return {
        ...prev,
        [videoConfig.id]: {
          ...prev[videoConfig.id] ?? defaultInOutValue,
          inFrame: selected
        }
      };
    });
  }, [getCurrentFrame2, setInAndOutFrames, videoConfig]);
  const clearInMark = useCallback9((e) => {
    if (!videoConfig) {
      return null;
    }
    e.preventDefault();
    setInAndOutFrames((f) => {
      return {
        ...f,
        [videoConfig.id]: {
          ...f[videoConfig.id] ?? defaultInOutValue,
          inFrame: null
        }
      };
    });
  }, [setInAndOutFrames, videoConfig]);
  const clearOutMark = useCallback9((e) => {
    if (!videoConfig) {
      return null;
    }
    e?.preventDefault();
    setInAndOutFrames((f) => {
      return {
        ...f,
        [videoConfig.id]: {
          ...f[videoConfig.id] ?? defaultInOutValue,
          outFrame: null
        }
      };
    });
  }, [setInAndOutFrames, videoConfig]);
  const onOutMark = useCallback9((e) => {
    if (!videoConfig) {
      return null;
    }
    if (e?.shiftKey) {
      setInAndOutFrames((f) => {
        return {
          ...f,
          [videoConfig.id]: {
            ...f[videoConfig.id] ?? defaultInOutValue,
            outFrame: null
          }
        };
      });
      return;
    }
    setInAndOutFrames((prev) => {
      const prevInFrame = prev[videoConfig.id]?.inFrame;
      const smallestPossible = prevInFrame === null || prevInFrame === undefined ? -Infinity : prevInFrame + 1;
      const selected = Math.max(getCurrentFrame2(), smallestPossible);
      if (selected === videoConfig.durationInFrames - 1) {
        return {
          ...prev,
          [videoConfig.id]: {
            ...prev[videoConfig.id] ?? defaultInOutValue,
            outFrame: null
          }
        };
      }
      const prevOut = prev[videoConfig.id]?.outFrame;
      if (prevOut !== null && prevOut !== undefined) {
        if (prevOut === selected) {
          return {
            ...prev,
            [videoConfig.id]: {
              ...prev[videoConfig.id] ?? defaultInOutValue,
              outFrame: null
            }
          };
        }
      }
      return {
        ...prev,
        [videoConfig.id]: {
          ...prev[videoConfig.id] ?? defaultInOutValue,
          outFrame: selected
        }
      };
    });
  }, [getCurrentFrame2, setInAndOutFrames, videoConfig]);
  const confId = videoConfig?.id;
  useEffect6(() => {
    if (!confId) {
      return;
    }
    const iKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "setInPoint",
      callback: (e) => {
        onInMark(e);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const oKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "setOutPoint",
      callback: (e) => {
        onOutMark(e);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const xKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "clearInOutPoints",
      callback: () => {
        onInOutClear(confId);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      oKey.unregister();
      iKey.unregister();
      xKey.unregister();
    };
  }, [confId, keybindings, onInMark, onInOutClear, onOutMark]);
  useImperativeHandle2(inOutHandles, () => {
    return {
      clearMarks: () => {
        if (!confId) {
          return;
        }
        onInOutClear(confId);
      },
      inMarkClick: onInMark,
      outMarkClick: onOutMark
    };
  }, [confId, onInMark, onInOutClear, onOutMark]);
  return /* @__PURE__ */ jsxs8(Fragment4, {
    children: [
      /* @__PURE__ */ jsx16(ActionTooltip, {
        label: "In point",
        shortcut: shortcutsDisabled ? null : inShortcut,
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx16(ControlButton, {
          title: "",
          "aria-label": "In point",
          "aria-description": "Right click to clear",
          "aria-keyshortcuts": shortcutsDisabled ? undefined : inAriaShortcut || undefined,
          style: buttonStyle2,
          onClick: onInMark,
          onContextMenu: clearInMark,
          disabled: !videoConfig || isFirstFrame,
          children: (color) => /* @__PURE__ */ jsx16(TimelineInPointer, {
            color: inFrame === null ? color : BLUE,
            style
          })
        })
      }),
      /* @__PURE__ */ jsx16(ActionTooltip, {
        label: "Out point",
        shortcut: shortcutsDisabled ? null : outShortcut,
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx16(ControlButton, {
          title: "",
          "aria-label": "Out point",
          "aria-description": "Right click to clear",
          "aria-keyshortcuts": shortcutsDisabled ? undefined : outAriaShortcut || undefined,
          style: buttonStyle2,
          onClick: onOutMark,
          onContextMenu: clearOutMark,
          disabled: !videoConfig || isLastFrame,
          children: (color) => /* @__PURE__ */ jsx16(TimelineOutPointer, {
            color: outFrame === null ? color : BLUE,
            style
          })
        })
      })
    ]
  });
};

// src/state/editor-pixel-grid.ts
import { createContext as createContext7 } from "react";
var key3 = "remotion.editorShowPixelGrid";
var persistEditorShowPixelGridOption = (option) => {
  localStorage.setItem(key3, String(option));
};
var loadEditorShowPixelGridOption = () => {
  const item = localStorage.getItem(key3);
  return item !== "false";
};
var EditorShowPixelGridContext = createContext7({
  editorShowPixelGrid: true,
  setEditorShowPixelGrid: () => {
    return;
  }
});

// src/state/editor-zoom-gestures.ts
import { createContext as createContext8 } from "react";
var persistEditorZoomGesturesOption = (option) => {
  localStorage.setItem("remotion.editorZoomGestures", String(option));
};
var loadEditorZoomGesturesOption = () => {
  const item = localStorage.getItem("remotion.editorZoomGestures");
  return item !== "false";
};
var EditorZoomGesturesContext = createContext8({
  editorZoomGestures: loadEditorZoomGesturesOption(),
  setEditorZoomGestures: () => {
    return;
  }
});

// src/helpers/check-fullscreen-support.ts
var checkFullscreenSupport = () => {
  return document.fullscreenEnabled || document.webkitFullscreenEnabled;
};

// src/helpers/settings-tab-availability.ts
var canEditStudioConfig = ({
  isBrowserStudio,
  previewServerConnected,
  readOnlyStudio
}) => {
  return previewServerConnected && !readOnlyStudio && !isBrowserStudio;
};
var canInstallPackagesInStudio = (studioMode) => {
  return studioMode.isBrowserStudio || canEditStudioConfig(studioMode);
};
var getAvailableSettingsTabs = ({
  isBrowserStudio,
  packageManagerAvailable,
  previewServerConnected,
  readOnlyStudio,
  showUpdates
}) => {
  const studioMode = {
    isBrowserStudio,
    previewServerConnected,
    readOnlyStudio
  };
  const canEditConfig = canEditStudioConfig(studioMode);
  const canInstallPackages2 = canInstallPackagesInStudio(studioMode) && (isBrowserStudio || packageManagerAvailable);
  return [
    ...canEditConfig ? ["studio", "rendering"] : [],
    "shortcuts",
    ...canInstallPackages2 ? ["packages"] : [],
    ...canEditConfig ? ["skills"] : [],
    ...isBrowserStudio || !readOnlyStudio ? ["models"] : [],
    ...canEditConfig ? ["apps", "license"] : [],
    ...showUpdates ? ["updates"] : []
  ];
};
var getSafeSettingsTab = (requestedTab, availableTabs) => {
  return availableTabs.includes(requestedTab) ? requestedTab : "shortcuts";
};

// src/helpers/use-menu-structure.tsx
import { jsx as jsx17 } from "react/jsx-runtime";
var openExternal = (link) => {
  window.open(link, "_blank");
};
var inheritColor = {
  color: "inherit"
};
var ICON_SIZE = 14;
var getFileMenu = ({
  readOnlyStudio,
  closeMenu,
  editorName,
  editorId,
  previewServerState,
  setSelectedModal
}) => {
  const fileManagerName = getFileManagerName(window.remotion_fileSystemPlatform);
  const browserStudioOperations = getBrowserStudioOperations();
  const newProjectItems = [
    readOnlyStudio ? null : {
      id: "new-composition",
      value: "new-composition",
      label: "New composition...",
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "new-comp",
          folderName: null,
          parentName: null,
          stack: null,
          canvasCapture: null
        });
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "New composition...",
      disabled: previewServerState !== "connected"
    },
    readOnlyStudio ? null : {
      id: "new-folder",
      value: "new-folder",
      label: "New folder...",
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "new-folder",
          parentName: null,
          stack: null
        });
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "New folder...",
      disabled: previewServerState !== "connected"
    }
  ].filter(NoReactInternals2.truthy);
  const projectItems = [
    window.remotion_isReadOnlyStudio ? {
      id: "input-props-override",
      value: "input-props-override",
      label: "Set input props...",
      onClick: () => {
        closeMenu();
        setSelectedModal({
          type: "input-props-override"
        });
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "Override input props"
    } : null,
    editorName && editorId && !readOnlyStudio ? {
      id: "open-in-editor",
      value: "open-in-editor",
      label: `Open in ${editorName}`,
      onClick: async () => {
        await openInEditor({
          originalFileName: `${window.remotion_cwd}`,
          originalLineNumber: 1,
          originalColumnNumber: 1,
          originalFunctionName: null,
          originalScriptCode: null
        }, editorId).then(({ success }) => {
          if (!success) {
            showNotification(`Could not open ${editorName}`, 2000);
          }
        }).catch((err) => {
          console.error(err);
          showNotification(`Could not open ${editorName}`, 2000);
        });
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "Open in editor...",
      disabled: previewServerState !== "connected"
    } : null,
    !readOnlyStudio && browserStudioOperations === null ? {
      id: "open-project-in-explorer",
      value: "open-project-in-explorer",
      label: `Open in ${fileManagerName}`,
      onClick: () => {
        closeMenu();
        openInFileExplorer({ directory: window.remotion_cwd }).catch((err) => {
          showNotification(`Could not open project: ${err.message}`, 2000);
        });
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: `Open project in ${fileManagerName}`,
      disabled: previewServerState !== "connected"
    } : null,
    getGitMenuItem()
  ].filter(NoReactInternals2.truthy);
  const items = [
    ...newProjectItems,
    newProjectItems.length > 0 && projectItems.length > 0 ? {
      type: "divider",
      id: "new-project-item-divider"
    } : null,
    ...projectItems
  ].filter(NoReactInternals2.truthy);
  if (items.length === 0) {
    return null;
  }
  return {
    id: "file",
    label: "File",
    leaveLeftPadding: false,
    items,
    quickSwitcherLabel: null
  };
};
var getRenderMenuItems = ({
  closeMenu,
  previewServerState,
  readOnlyStudio,
  renderShortcut
}) => {
  return [
    readOnlyStudio ? null : {
      id: "render",
      value: "render",
      label: "Render...",
      onClick: () => {
        closeMenu();
        if (previewServerState !== "connected") {
          showNotification("Restart the studio to render", 2000);
          return;
        }
        const renderButton = document.getElementById("render-modal-button-server");
        renderButton.click();
      },
      type: "item",
      keyHint: areKeyboardShortcutsDisabled() ? null : renderShortcut || null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "Render..."
    },
    {
      id: "render-on-web",
      value: "render-on-web",
      label: "Render in browser...",
      onClick: () => {
        closeMenu();
        const renderButton = document.getElementById("render-modal-button-client");
        renderButton.click();
      },
      type: "item",
      keyHint: null,
      leftItem: null,
      subMenu: null,
      quickSwitcherLabel: "Render in browser..."
    },
    {
      type: "divider",
      id: "render-divider"
    }
  ].filter(NoReactInternals2.truthy);
};
var useMenuStructure = (closeMenu, readOnlyStudio) => {
  const { setSelectedModal } = useContext10(SetSelectedModalContext);
  const { checkerboard, setCheckerboard } = useContext10(CheckerboardContext);
  const { editorZoomGestures, setEditorZoomGestures } = useContext10(EditorZoomGesturesContext);
  const { editorShowPixelGrid, setEditorShowPixelGrid } = useContext10(EditorShowPixelGridContext);
  const { editorShowRulers, setEditorShowRulers } = useContext10(EditorShowRulersContext);
  const { editorShowGuides, setEditorShowGuides } = useContext10(EditorShowGuidesContext);
  const { editorSnapping, setEditorSnapping } = useContext10(EditorSnappingContext);
  const { size, setSize } = useContext10(Internals9.PreviewSizeContext);
  const { canvasContent, compositions } = useContext10(Internals9.CompositionManager);
  const { connectionStatus: type, openInApps } = useOpenInMenuApps();
  const { defaultEditorId, defaultEditorName } = openInApps;
  const keyboardShortcutsDisabled = areKeyboardShortcutsDisabled();
  const resetZoomShortcut = useKeyboardShortcutLabel("resetZoom");
  const toggleSnappingShortcut = useKeyboardShortcutLabel("toggleSnapping");
  const checkerboardShortcut = useKeyboardShortcutLabel("toggleCheckerboard");
  const quickSwitcherShortcut = useKeyboardShortcutLabel("quickSwitcher");
  const setInPointShortcut = useKeyboardShortcutLabel("setInPoint");
  const setOutPointShortcut = useKeyboardShortcutLabel("setOutPoint");
  const clearInOutPointsShortcut = useKeyboardShortcutLabel("clearInOutPoints");
  const goToFrameShortcut = useKeyboardShortcutLabel("goToFrame");
  const renderShortcut = useKeyboardShortcutLabel("render");
  const askAIShortcut = useKeyboardShortcutLabel("askAI");
  const colorPickerShortcut = useKeyboardShortcutLabel("pickColor");
  const showKeyboardShortcutsShortcut = useKeyboardShortcutLabel("showKeyboardShortcuts");
  const studioAskAIEnabled = getStudioAskAIEnabled();
  const {
    setSidebarCollapsedState,
    sidebarCollapsedStateLeft,
    sidebarCollapsedStateRight
  } = useContext10(SidebarContext);
  const sizes = useMemo9(() => getUniqueSizes(size), [size]);
  const isFullscreenSupported = checkFullscreenSupport();
  const { remotion_packageManager } = window;
  const browserStudioOperations = getBrowserStudioOperations();
  const studioConfigEditable = canEditStudioConfig({
    isBrowserStudio: browserStudioOperations !== null,
    previewServerConnected: type === "connected",
    readOnlyStudio
  });
  const sizePreselectIndex = sizes.findIndex((s) => String(size.size) === String(s.size));
  const mobileLayout = useMobileLayout();
  const currentComposition = useMemo9(() => {
    if (canvasContent === null || canvasContent.type !== "composition") {
      return null;
    }
    return compositions.find((c) => c.id === canvasContent.compositionId) ?? null;
  }, [canvasContent, compositions]);
  const resolvedCompositionLocation = useResolvedStack(currentComposition?.stack ?? null);
  useEffect7(() => {
    if (type !== "connected" || !currentComposition || !resolvedCompositionLocation?.source) {
      return;
    }
    preloadCompositionComponentInfo({
      compositionFile: resolvedCompositionLocation.source,
      compositionId: currentComposition.id
    });
  }, [currentComposition, resolvedCompositionLocation?.source, type]);
  const structure = useMemo9(() => {
    let struct = [
      {
        id: "remotion",
        label: /* @__PURE__ */ jsx17(Row, {
          align: "center",
          justify: "center",
          style: inheritColor,
          children: /* @__PURE__ */ jsx17("svg", {
            width: ICON_SIZE,
            height: ICON_SIZE,
            viewBox: "0 0 415 426",
            fill: "none",
            xmlns: "http://www.w3.org/2000/svg",
            style: inheritColor,
            children: /* @__PURE__ */ jsx17("path", {
              d: "M93.4691 0.0367432C84.4873 0.520935 77.2494 1.93634 69.9553 4.69266C66.3176 6.05219 60.3548 9.0134 57.0734 11.062C43.3476 19.6103 32.8846 32.4606 27.428 47.4154C26.3405 50.3766 23.3966 59.8188 21.5027 66.3185C8.88329 109.768 1.7204 157.277 0.182822 207.561C-0.0609408 215.569 -0.0609408 234.639 0.182822 242.517C1.21413 275.854 4.42055 305.949 10.2334 336.641C12.596 349.063 16.3837 365.75 18.5776 373.33C23.059 388.732 32.2095 401.843 45.2227 411.453C53.9419 417.896 63.8425 422.217 74.8118 424.34C80.0996 425.365 87.075 425.83 92.2127 425.495C99.3194 425.029 113.42 423.148 124.877 421.118C176.517 411.974 224.22 395.604 267.478 372.175C294.874 357.332 318.294 341.26 340.888 321.761C363.408 302.355 382.609 281.478 399.504 258.049C403.423 252.63 405.392 249.464 407.361 245.478C412.424 235.198 414.805 224.974 414.786 213.539C414.786 202.886 412.761 193.425 408.392 183.741C406.292 179.066 404.286 175.714 399.785 169.345C383.21 145.898 364.815 125.467 342.389 105.614C307.624 74.8481 266.335 49.613 220.226 30.9334C210.232 26.8921 200.387 23.335 188.537 19.4799C163.448 11.3413 132.396 4.28293 106.126 0.763062C102.001 0.204346 96.3942 -0.112244 93.4691 0.0367432Z",
              fill: CURRENT_COLOR,
              style: inheritColor
            })
          })
        }),
        leaveLeftPadding: false,
        items: [
          {
            id: "about",
            value: "about",
            label: "About Remotion",
            onClick: () => {
              closeMenu();
              openExternal("https://remotion.dev");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Help: About Remotion"
          },
          {
            id: "changelog",
            value: "changelog",
            label: "Changelog",
            onClick: () => {
              closeMenu();
              openExternal("https://github.com/remotion-dev/remotion/releases");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Help: Changelog"
          },
          {
            id: "settings",
            value: "settings",
            label: "Settings...",
            onClick: () => {
              closeMenu();
              setSelectedModal({
                type: "settings",
                initialTab: studioConfigEditable ? "studio" : "shortcuts",
                initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
              });
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Settings..."
          },
          {
            id: "acknowledgements",
            value: "acknowledgements",
            label: "Acknowledgements",
            onClick: () => {
              closeMenu();
              openExternal("https://remotion.dev/acknowledgements");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Help: Acknowledgements"
          },
          studioConfigEditable ? {
            type: "divider",
            id: "timeline-divider-1"
          } : null,
          studioConfigEditable ? {
            id: "restart-studio",
            value: "restart-studio",
            label: "Restart Studio Server",
            onClick: () => {
              closeMenu();
              restartStudio();
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Restart Studio Server"
          } : null
        ].filter(NoReactInternals2.truthy),
        quickSwitcherLabel: null
      },
      getFileMenu({
        readOnlyStudio,
        closeMenu,
        editorId: defaultEditorId,
        editorName: defaultEditorName,
        previewServerState: type,
        setSelectedModal
      }),
      {
        id: "view",
        label: "View",
        leaveLeftPadding: true,
        items: [
          {
            id: "preview-size",
            keyHint: null,
            label: "Preview size",
            onClick: () => {
              return;
            },
            type: "item",
            value: "preview-size",
            leftItem: null,
            subMenu: {
              leaveLeftSpace: true,
              preselectIndex: sizePreselectIndex,
              items: sizes.map((newSize) => ({
                id: String(newSize.size),
                keyHint: newSize.size === 1 && !keyboardShortcutsDisabled ? resetZoomShortcut || null : null,
                label: getPreviewSizeLabel(newSize),
                leftItem: String(newSize.size) === String(size.size) ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                onClick: () => {
                  closeMenu();
                  setSize(() => newSize);
                },
                subMenu: null,
                type: "item",
                value: newSize.size,
                quickSwitcherLabel: null
              })),
              quickSwitcherLabel: null
            },
            quickSwitcherLabel: null
          },
          {
            id: "editor-zoom-gestures",
            keyHint: null,
            label: "Zoom and Pan Gestures",
            onClick: () => {
              closeMenu();
              setEditorZoomGestures((c) => !c);
            },
            type: "item",
            value: "editor-zoom-gestures",
            leftItem: editorZoomGestures ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: editorZoomGestures ? "Disable Zoom and Pan Gestures" : "Enable Zoom and Pan Gestures"
          },
          {
            id: "pixel-grid",
            keyHint: null,
            label: "Pixel Grid",
            onClick: () => {
              closeMenu();
              setEditorShowPixelGrid((c) => !c);
            },
            type: "item",
            value: "pixel-grid",
            leftItem: editorShowPixelGrid ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: editorShowPixelGrid ? "Hide Pixel Grid" : "Show Pixel Grid"
          },
          {
            id: "show-rulers",
            keyHint: null,
            label: "Show Rulers",
            onClick: () => {
              closeMenu();
              setEditorShowRulers((c) => !c);
            },
            type: "item",
            value: "show-ruler",
            leftItem: editorShowRulers ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: editorShowRulers ? "Hide Rulers" : "Show Rulers"
          },
          {
            id: "show-guides",
            keyHint: null,
            label: "Show Guides",
            onClick: () => {
              closeMenu();
              setEditorShowGuides((c) => !c);
            },
            type: "item",
            value: "show-guides",
            leftItem: editorShowGuides ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: editorShowGuides ? "Hide Guides" : "Show Guides"
          },
          {
            id: "enable-snapping",
            keyHint: keyboardShortcutsDisabled ? null : toggleSnappingShortcut || null,
            label: "Enable Snapping",
            onClick: () => {
              closeMenu();
              setEditorSnapping((c) => !c);
            },
            type: "item",
            value: "enable-snapping",
            leftItem: editorSnapping ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: editorSnapping ? "Disable Snapping" : "Enable Snapping"
          },
          {
            id: "timeline-divider-1",
            type: "divider"
          },
          {
            id: "left-sidebar",
            label: "Left Sidebar",
            keyHint: null,
            type: "item",
            value: "preview-size",
            leftItem: null,
            quickSwitcherLabel: null,
            subMenu: {
              leaveLeftSpace: true,
              preselectIndex: 0,
              items: [
                {
                  id: "left-sidebar-responsive",
                  keyHint: null,
                  label: "Responsive",
                  leftItem: sidebarCollapsedStateLeft === "responsive" ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                  onClick: () => {
                    closeMenu();
                    setSidebarCollapsedState({
                      left: "responsive",
                      right: null
                    });
                  },
                  subMenu: null,
                  type: "item",
                  value: "responsive",
                  quickSwitcherLabel: null
                },
                {
                  id: "left-sidebar-expanded",
                  keyHint: null,
                  label: "Expanded",
                  leftItem: sidebarCollapsedStateLeft === "expanded" ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                  onClick: () => {
                    closeMenu();
                    setSidebarCollapsedState({ left: "expanded", right: null });
                  },
                  subMenu: null,
                  type: "item",
                  value: "expanded",
                  quickSwitcherLabel: "Expand"
                },
                {
                  id: "left-sidebar-collapsed",
                  keyHint: null,
                  label: "Collapsed",
                  leftItem: sidebarCollapsedStateLeft === "collapsed" ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                  onClick: () => {
                    closeMenu();
                    setSidebarCollapsedState({
                      left: "collapsed",
                      right: null
                    });
                  },
                  subMenu: null,
                  type: "item",
                  value: "collapsed",
                  quickSwitcherLabel: "Collapse"
                }
              ]
            },
            onClick: () => {
              return;
            }
          },
          {
            id: "right-sidebar",
            label: "Right Sidebar",
            keyHint: null,
            type: "item",
            value: "preview-size",
            leftItem: null,
            quickSwitcherLabel: null,
            subMenu: {
              leaveLeftSpace: true,
              preselectIndex: 0,
              items: [
                {
                  id: "sidebar-expanded",
                  keyHint: null,
                  label: "Expanded",
                  leftItem: sidebarCollapsedStateRight === "expanded" ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                  onClick: () => {
                    closeMenu();
                    setSidebarCollapsedState({ left: null, right: "expanded" });
                  },
                  subMenu: null,
                  type: "item",
                  value: "expanded",
                  quickSwitcherLabel: "Expand"
                },
                {
                  id: "right-sidebar-collapsed",
                  keyHint: null,
                  label: "Collapsed",
                  leftItem: sidebarCollapsedStateRight === "collapsed" ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
                  onClick: () => {
                    closeMenu();
                    setSidebarCollapsedState({
                      left: null,
                      right: "collapsed"
                    });
                  },
                  subMenu: null,
                  type: "item",
                  value: "collapsed",
                  quickSwitcherLabel: "Collapse"
                }
              ]
            },
            onClick: () => {
              return;
            }
          },
          {
            id: "timeline-divider-2",
            type: "divider"
          },
          {
            id: "checkerboard",
            keyHint: keyboardShortcutsDisabled ? null : checkerboardShortcut || null,
            label: "Transparency as checkerboard",
            onClick: () => {
              closeMenu();
              setCheckerboard((c) => !c);
            },
            type: "item",
            value: "checkerboard",
            leftItem: checkerboard ? /* @__PURE__ */ jsx17(Checkmark, {}) : null,
            subMenu: null,
            quickSwitcherLabel: checkerboard ? "Disable Checkerboard Transparency" : "Enable Checkerboard Transparency"
          },
          {
            id: "timeline-divider-3",
            type: "divider"
          },
          {
            id: "quick-switcher",
            keyHint: keyboardShortcutsDisabled ? null : quickSwitcherShortcut || null,
            label: "Quick Switcher",
            onClick: () => {
              closeMenu();
              setSelectedModal({
                type: "quick-switcher",
                mode: "compositions",
                invocationTimestamp: Date.now(),
                assetSelection: null,
                compositionSelection: null
              });
            },
            type: "item",
            value: "quick-switcher",
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Switch composition"
          },
          {
            id: "in-out-divider-5",
            type: "divider"
          },
          {
            id: "in-mark",
            keyHint: keyboardShortcutsDisabled ? null : setInPointShortcut || null,
            label: "In Mark",
            leftItem: null,
            onClick: () => {
              closeMenu();
              inOutHandles.current?.inMarkClick(null);
            },
            subMenu: null,
            type: "item",
            value: "in-mark",
            quickSwitcherLabel: "Timeline: Set In Mark"
          },
          {
            id: "out-mark",
            keyHint: keyboardShortcutsDisabled ? null : setOutPointShortcut || null,
            label: "Out Mark",
            leftItem: null,
            onClick: () => {
              closeMenu();
              inOutHandles.current?.outMarkClick(null);
            },
            subMenu: null,
            type: "item",
            value: "out-mark",
            quickSwitcherLabel: "Timeline: Set Out Mark"
          },
          {
            id: "x-mark",
            keyHint: keyboardShortcutsDisabled ? null : clearInOutPointsShortcut || null,
            label: "Clear In/Out Marks",
            leftItem: null,
            onClick: () => {
              closeMenu();
              inOutHandles.current?.clearMarks();
            },
            subMenu: null,
            type: "item",
            value: "clear-marks",
            quickSwitcherLabel: "Timeline: Clear In and Out Mark"
          },
          {
            id: "goto-time",
            keyHint: keyboardShortcutsDisabled ? null : goToFrameShortcut || null,
            label: "Go to frame",
            leftItem: null,
            onClick: () => {
              closeMenu();
              Internals9.timeValueRef.current?.goToFrame();
            },
            subMenu: null,
            type: "item",
            value: "clear-marks",
            quickSwitcherLabel: "Timeline: Go to frame"
          },
          {
            id: "fullscreen-divider",
            type: "divider"
          },
          isFullscreenSupported ? {
            id: "fullscreen",
            keyHint: null,
            label: "Fullscreen",
            leftItem: null,
            onClick: () => {
              closeMenu();
              drawRef.current?.requestFullscreen();
            },
            subMenu: null,
            type: "item",
            value: "fullscreen",
            quickSwitcherLabel: "Go Fullscreen"
          } : null
        ].filter(Internals9.truthy)
      },
      {
        id: "composition",
        label: "Composition",
        leaveLeftPadding: false,
        items: [
          ...getRenderMenuItems({
            closeMenu,
            previewServerState: type,
            readOnlyStudio,
            renderShortcut
          }),
          ...getCompositionMenuItems({
            closeMenu,
            composition: currentComposition,
            connectionStatus: type,
            includeCompositionManagementItems: true,
            openInApps,
            resolvedLocation: resolvedCompositionLocation,
            setSelectedModal,
            readOnlyStudio
          })
        ],
        quickSwitcherLabel: null
      },
      {
        id: "tools",
        label: "Tools",
        leaveLeftPadding: false,
        items: [
          studioAskAIEnabled ? {
            id: "ask-ai",
            value: "ask-ai",
            label: "Ask AI",
            onClick: () => {
              closeMenu();
              askAiModalRef.current?.toggle();
            },
            leftItem: null,
            keyHint: keyboardShortcutsDisabled ? null : askAIShortcut || null,
            subMenu: null,
            type: "item",
            quickSwitcherLabel: "Ask AI"
          } : null,
          "EyeDropper" in window ? {
            id: "color-picker",
            value: "color-picker",
            label: "Color Picker",
            onClick: () => {
              closeMenu();
              pickColor();
            },
            leftItem: null,
            keyHint: keyboardShortcutsDisabled ? null : colorPickerShortcut || null,
            subMenu: null,
            type: "item",
            quickSwitcherLabel: "Show Color Picker"
          } : null,
          !canInstallPackages() || browserStudioOperations === null && (remotion_packageManager === "unknown" || type !== "connected") ? null : {
            id: "install-packages",
            value: "install-packages",
            label: "Install package...",
            onClick: () => {
              closeMenu();
              setSelectedModal({
                type: "settings",
                initialTab: "packages",
                initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
              });
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: `Install package...`
          }
        ].filter(Internals9.truthy),
        quickSwitcherLabel: null
      },
      {
        id: "help",
        label: "Help",
        leaveLeftPadding: false,
        items: [
          {
            id: "shortcuts",
            value: "shortcuts",
            label: keyboardShortcutsDisabled ? "Shortcuts (disabled)" : "Shortcuts",
            onClick: () => {
              closeMenu();
              setSelectedModal({
                type: "settings",
                initialTab: "shortcuts",
                initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
              });
            },
            keyHint: keyboardShortcutsDisabled ? null : showKeyboardShortcutsShortcut || null,
            leftItem: null,
            subMenu: null,
            type: "item",
            quickSwitcherLabel: keyboardShortcutsDisabled ? "Show all Keyboard Shortcuts (disabled)" : "Show all Keyboard Shortcuts"
          },
          {
            id: "docs",
            value: "docs",
            label: "Docs",
            onClick: () => {
              closeMenu();
              openExternal("https://remotion.dev/docs");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Visit Documentation"
          },
          {
            id: "file-issue",
            value: "file-issue",
            label: "File an issue",
            onClick: () => {
              closeMenu();
              openExternal("https://github.com/remotion-dev/remotion/issues/new/choose");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "File GitHub issue"
          },
          {
            id: "discord",
            value: "discord",
            label: "Join Discord community",
            onClick: () => {
              closeMenu();
              openExternal("https://discord.com/invite/6VzzNDwUwV");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: null
          },
          {
            id: "help-divider-6",
            type: "divider"
          },
          {
            id: "insta",
            value: "insta",
            label: "Instagram",
            onClick: () => {
              closeMenu();
              openExternal("https://instagram.com/remotion");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Follow Remotion on Instagram"
          },
          {
            id: "x",
            value: "x",
            label: "X",
            onClick: () => {
              closeMenu();
              openExternal("https://x.com/remotion");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Follow Remotion on X"
          },
          {
            id: "youtube",
            value: "youtube",
            label: "YouTube",
            onClick: () => {
              closeMenu();
              openExternal("https://www.youtube.com/@remotion_dev");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Watch Remotion on YouTube"
          },
          {
            id: "linkedin",
            value: "linkedin",
            label: "LinkedIn",
            onClick: () => {
              closeMenu();
              openExternal("https://www.linkedin.com/company/remotion-dev/");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Follow Remotion on LinkedIn"
          },
          {
            id: "tiktok",
            value: "tiktok",
            label: "TikTok",
            onClick: () => {
              closeMenu();
              openExternal("https://www.tiktok.com/@remotion");
            },
            type: "item",
            keyHint: null,
            leftItem: null,
            subMenu: null,
            quickSwitcherLabel: "Follow Remotion on TikTok"
          }
        ]
      }
    ].filter(Internals9.truthy);
    if (mobileLayout) {
      struct = [
        {
          ...struct[0],
          items: [
            ...struct.slice(1).map((s) => {
              return {
                ...s,
                keyHint: null,
                onClick: () => {
                  return;
                },
                type: "item",
                value: s.id,
                leftItem: null,
                subMenu: {
                  items: s.items,
                  leaveLeftSpace: true,
                  preselectIndex: 0
                },
                quickSwitcherLabel: null
              };
            }),
            ...struct[0].items
          ]
        }
      ];
    }
    return struct;
  }, [
    readOnlyStudio,
    closeMenu,
    type,
    sizePreselectIndex,
    sizes,
    currentComposition,
    resolvedCompositionLocation,
    editorZoomGestures,
    editorShowPixelGrid,
    editorShowRulers,
    editorShowGuides,
    editorSnapping,
    sidebarCollapsedStateLeft,
    sidebarCollapsedStateRight,
    checkerboard,
    isFullscreenSupported,
    remotion_packageManager,
    mobileLayout,
    defaultEditorId,
    defaultEditorName,
    openInApps,
    keyboardShortcutsDisabled,
    studioAskAIEnabled,
    askAIShortcut,
    colorPickerShortcut,
    checkerboardShortcut,
    clearInOutPointsShortcut,
    goToFrameShortcut,
    quickSwitcherShortcut,
    renderShortcut,
    resetZoomShortcut,
    setInPointShortcut,
    setOutPointShortcut,
    showKeyboardShortcutsShortcut,
    toggleSnappingShortcut,
    browserStudioOperations,
    studioConfigEditable,
    size.size,
    setSize,
    setEditorZoomGestures,
    setEditorShowPixelGrid,
    setEditorShowRulers,
    setEditorShowGuides,
    setEditorSnapping,
    setSidebarCollapsedState,
    setCheckerboard,
    setSelectedModal
  ]);
  return structure;
};
var getItemLabel = (item) => {
  if (item.quickSwitcherLabel !== null) {
    return item.quickSwitcherLabel;
  }
  if (typeof item.label === "string") {
    return item.label;
  }
  return item.label?.toString();
};
var itemToSearchResult = (item, setSelectedModal, prefixes) => {
  if (item.disabled) {
    return [];
  }
  if (item.subMenu) {
    return item.subMenu.items.map((subItem) => {
      if (subItem.type !== "item") {
        return null;
      }
      return itemToSearchResult(subItem, setSelectedModal, [
        ...prefixes,
        getItemLabel(item)
      ]);
    }).flat(1).filter(NoReactInternals2.truthy);
  }
  return [
    {
      type: "menu-item",
      id: item.id,
      onSelected: () => {
        setSelectedModal(null);
        item.onClick(item.id, null);
      },
      title: [...prefixes, getItemLabel(item)].join(": ")
    }
  ];
};
var makeSearchResults = (actions, setSelectedModal) => {
  const items = actions.map((menu) => {
    return menu.items.map((item) => {
      if (item.type !== "item") {
        return null;
      }
      return itemToSearchResult(item, setSelectedModal, []);
    });
  }).flat(Infinity).filter(NoReactInternals2.truthy);
  return items;
};

// src/icons/search.tsx
import { jsx as jsx18 } from "react/jsx-runtime";
var SearchIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx18("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 640",
    ...props,
    children: /* @__PURE__ */ jsx18("path", {
      fill: color,
      d: "M432 272C432 183.6 360.4 112 272 112C183.6 112 112 183.6 112 272C112 360.4 183.6 432 272 432C360.4 432 432 360.4 432 272zM401.1 435.1C365.7 463.2 320.8 480 272 480C157.1 480 64 386.9 64 272C64 157.1 157.1 64 272 64C386.9 64 480 157.1 480 272C480 320.8 463.2 365.7 435.1 401.1L569 535C578.4 544.4 578.4 559.6 569 568.9C559.6 578.2 544.4 578.3 535.1 568.9L401.1 435.1z"
    })
  });
};

// src/components/menu-toolbar-height.ts
var MENU_TOOLBAR_HEIGHT = 30;

// src/components/Menu/MenuItem.tsx
import { PlayerInternals as PlayerInternals3 } from "@remotion/player";
import { useCallback as useCallback10, useMemo as useMemo10, useRef as useRef4 } from "react";
import ReactDOM from "react-dom";
import { jsx as jsx19, jsxs as jsxs9, Fragment as Fragment5 } from "react/jsx-runtime";
var MENU_ITEM_HEIGHT = 24;
var MENU_ITEM_VERTICAL_MARGIN = (MENU_TOOLBAR_HEIGHT - MENU_ITEM_HEIGHT) / 2;
var container5 = {
  alignItems: "center",
  boxSizing: "border-box",
  display: "inline-flex",
  fontSize: 13,
  height: MENU_ITEM_HEIGHT,
  justifyContent: "center",
  paddingLeft: 8,
  paddingRight: 8,
  cursor: "default",
  paddingTop: 0,
  paddingBottom: 0,
  userSelect: "none",
  WebkitUserSelect: "none",
  border: "none",
  borderRadius: 3
};
var MenuItem = ({
  label: itemName,
  selected,
  id,
  onItemSelected,
  onItemHovered,
  onItemQuit,
  onPreviousMenu,
  onNextMenu,
  menu
}) => {
  const ref = useRef4(null);
  const size = PlayerInternals3.useElementSize(ref, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const { tabIndex, currentZIndex } = useZIndex();
  const containerStyle = useMemo10(() => {
    return {
      ...container5,
      ...hoverableStyle({
        idleBackground: getBackgroundFromHoverState({
          hovered: false,
          selected
        }),
        hoverBackground: getBackgroundFromHoverState({
          hovered: true,
          selected
        }),
        idleColor: selected ? WHITE : WHITE_ALPHA_80,
        hoverColor: WHITE
      })
    };
  }, [selected]);
  const portalStyle = useMemo10(() => {
    if (!selected || !size) {
      return null;
    }
    return {
      ...menuContainerTowardsBottom,
      left: size.left,
      top: size.top + size.height + MENU_ITEM_VERTICAL_MARGIN
    };
  }, [selected, size]);
  const onPointerEnter = useCallback10(() => {
    onItemHovered(id);
  }, [id, onItemHovered]);
  const onPointerDown = useCallback10((e) => {
    e.stopPropagation();
    if (e.button !== 0) {
      return;
    }
    onItemSelected(id);
    startCapturedPointerSession({
      event: e.nativeEvent,
      captureTarget: e.currentTarget,
      onEnd: (reason, evt) => {
        if (isPointerSessionRelease(reason, evt)) {
          const target = document.elementFromPoint(evt.clientX, evt.clientY);
          if (!target || !isMenuItem(target)) {
            onItemQuit();
          }
        }
      }
    });
  }, [id, onItemQuit, onItemSelected]);
  const onMenuPointerDown = useCallback10((e) => {
    e.stopPropagation();
  }, []);
  const onClick = useCallback10((e) => {
    e.stopPropagation();
    const isKeyboardInitiated = e.detail === 0;
    if (!isKeyboardInitiated) {
      return;
    }
    onItemSelected((p) => {
      return p === null ? id : null;
    });
  }, [id, onItemSelected]);
  const outerStyle = useMemo10(() => {
    return {
      ...outerPortal,
      top: (size?.top ?? 0) + (size?.height ?? 0) + MENU_ITEM_VERTICAL_MARGIN
    };
  }, [size]);
  return /* @__PURE__ */ jsxs9(Fragment5, {
    children: [
      /* @__PURE__ */ jsx19("button", {
        ref,
        role: "button",
        tabIndex,
        onPointerEnter,
        onPointerDown,
        onClick,
        style: containerStyle,
        type: "button",
        className: `${MENU_INITIATOR_CLASSNAME} ${HOVERABLE_CLASS_NAME}`,
        children: itemName
      }),
      portalStyle ? ReactDOM.createPortal(/* @__PURE__ */ jsx19("div", {
        className: "css-reset",
        style: outerStyle,
        children: /* @__PURE__ */ jsx19(HigherZIndex, {
          onEscape: onItemQuit,
          onOutsideClick: onItemQuit,
          children: /* @__PURE__ */ jsx19("div", {
            style: portalStyle,
            onPointerDown: onMenuPointerDown,
            children: /* @__PURE__ */ jsx19(MenuContent, {
              onNextMenu: onPreviousMenu,
              onPreviousMenu: onNextMenu,
              values: menu.items,
              onHide: onItemQuit,
              leaveLeftSpace: menu.leaveLeftPadding,
              preselectIndex: false,
              topItemCanBeUnselected: true,
              fixedHeight: null
            })
          })
        })
      }), getPortal(currentZIndex)) : null
    ]
  });
};

// src/components/MenuBuildIndicator.tsx
import { useEffect as useEffect8, useMemo as useMemo12, useState as useState4 } from "react";

// src/components/MenuCompositionName.tsx
import { useCallback as useCallback11, useContext as useContext11, useMemo as useMemo11 } from "react";
import { Internals as Internals10 } from "remotion";
import { jsx as jsx20, jsxs as jsxs10, Fragment as Fragment6 } from "react/jsx-runtime";
var baseStyle = {
  cursor: "default",
  textDecoration: "none",
  fontSize: "inherit",
  textUnderlineOffset: 2,
  whiteSpace: "nowrap"
};
var assetNameStyle = {
  ...baseStyle,
  color: WHITE_ALPHA_80
};
var contextMenuStyle = {
  alignItems: "center",
  color: "inherit",
  display: "flex",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "inherit"
};
var compositionNameStyle = {
  ...baseStyle,
  appearance: "none",
  border: "none",
  backgroundColor: TRANSPARENT,
  fontFamily: "inherit",
  fontSize: 13,
  height: "auto",
  lineHeight: 1.5,
  margin: 0,
  padding: 0,
  width: "auto",
  ...NO_HOVER_BACKGROUND_STYLE
};
var slashStyle = {
  color: LIGHT_TEXT,
  marginInline: 4,
  opacity: 0.8,
  position: "relative",
  top: 1
};
var MenuCompositionName = () => {
  const { canvasContent, compositions } = useContext11(Internals10.CompositionManager);
  const composition = useMemo11(() => {
    if (canvasContent === null || canvasContent.type !== "composition") {
      return null;
    }
    return compositions.find((c) => c.id === canvasContent.compositionId) ?? null;
  }, [canvasContent, compositions]);
  const asset = canvasContent?.type === "asset" ? canvasContent.asset : null;
  const { setSelectedModal } = useContext11(SetSelectedModalContext);
  const { connectionStatus, openInApps } = useOpenInMenuApps();
  const resolvedLocation = useResolvedStack(composition?.stack ?? null);
  const getContextMenuItems = useCallback11(() => {
    return getCompositionContextMenuItems({
      closeMenu: noop,
      composition,
      connectionStatus,
      includeCompositionManagementItems: true,
      openInApps,
      resolvedLocation,
      setSelectedModal,
      readOnlyStudio: window.remotion_isReadOnlyStudio
    });
  }, [
    composition,
    connectionStatus,
    openInApps,
    resolvedLocation,
    setSelectedModal
  ]);
  if (!composition && asset === null) {
    return null;
  }
  const name = composition?.id ?? asset;
  return /* @__PURE__ */ jsxs10(Fragment6, {
    children: [
      /* @__PURE__ */ jsx20("span", {
        style: slashStyle,
        children: "/"
      }),
      composition ? /* @__PURE__ */ jsx20(ContextMenu, {
        getItems: getContextMenuItems,
        style: contextMenuStyle,
        children: /* @__PURE__ */ jsx20(InlineDropdown, {
          "aria-label": "Open composition menu",
          className: FOCUS_VISIBLE_ONLY_CLASS_NAME,
          getItems: getContextMenuItems,
          hoveredColor: WHITE,
          renderAction: () => composition.id,
          style: compositionNameStyle,
          title: "Open composition menu",
          unhoveredColor: WHITE_ALPHA_80,
          variant: null
        })
      }) : /* @__PURE__ */ jsx20("span", {
        style: assetNameStyle,
        children: name
      })
    ]
  });
};

// src/components/MenuBuildIndicator.tsx
import { jsx as jsx21, jsxs as jsxs11, Fragment as Fragment7 } from "react/jsx-runtime";
var cwd = {
  fontSize: 13,
  color: WHITE_ALPHA_80,
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  userSelect: "none",
  whiteSpace: "nowrap"
};
var spinnerSize = 14;
var spinner = {
  opacity: 0.8,
  position: "relative",
  width: spinnerSize
};
var openInEditor2 = {
  display: "flex",
  opacity: 0.8
};
var noSpinner = {
  position: "relative",
  width: spinnerSize
};
var MenuBuildIndicator = ({ mobileLayout }) => {
  const [isBuilding, setIsBuilding] = useState4(false);
  const folderLocation = useMemo12(() => {
    return {
      source: window.remotion_cwd,
      line: 1,
      column: 1
    };
  }, []);
  useEffect8(() => {
    window.remotion_isBuilding = () => {
      setIsBuilding(true);
    };
    window.remotion_finishedBuilding = () => {
      setIsBuilding(false);
    };
    return () => {
      window.remotion_isBuilding = undefined;
      window.remotion_finishedBuilding = undefined;
    };
  }, []);
  return /* @__PURE__ */ jsxs11("div", {
    style: cwd,
    title: window.remotion_cwd,
    children: [
      /* @__PURE__ */ jsx21(Spacing, {
        x: mobileLayout ? 0.5 : 2
      }),
      window.remotion_projectName,
      /* @__PURE__ */ jsx21(MenuCompositionName, {}),
      /* @__PURE__ */ jsx21(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx21("div", {
        style: openInEditor2,
        children: /* @__PURE__ */ jsx21(InspectorOpenInEditor, {
          location: folderLocation,
          locationType: "folder",
          showTooltips: true
        })
      }),
      mobileLayout ? null : /* @__PURE__ */ jsx21(Spacing, {
        x: 0.5
      }),
      mobileLayout ? isBuilding ? /* @__PURE__ */ jsxs11(Fragment7, {
        children: [
          /* @__PURE__ */ jsx21(Spacing, {
            x: 0.5
          }),
          /* @__PURE__ */ jsx21("div", {
            style: spinner,
            children: /* @__PURE__ */ jsx21(Spinner, {
              duration: 0.5,
              size: spinnerSize,
              color: WHITE_ALPHA_80
            })
          })
        ]
      }) : null : isBuilding ? /* @__PURE__ */ jsx21("div", {
        style: spinner,
        children: /* @__PURE__ */ jsx21(Spinner, {
          duration: 0.5,
          size: spinnerSize,
          color: WHITE_ALPHA_80
        })
      }) : /* @__PURE__ */ jsx21("div", {
        style: noSpinner
      })
    ]
  });
};

// src/components/SettingsButton.tsx
import { useCallback as useCallback12, useContext as useContext12 } from "react";
import { jsx as jsx22 } from "react/jsx-runtime";
var SettingsButton = ({ showUpdates }) => {
  const { setSelectedModal } = useContext12(SetSelectedModalContext);
  const { previewServerState } = useContext12(StudioServerConnectionCtx);
  const { info, knownBugs } = useUpdateStatus();
  const updateAvailable = showUpdates && Boolean(info?.updateAvailable || info?.skillsUpdateAvailable);
  const hasBugfixesAvailable = showUpdates && Boolean(info?.updateAvailable && knownBugs && knownBugs.length > 0);
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const canEditConfig = canEditStudioConfig({
    isBrowserStudio,
    previewServerConnected: previewServerState.type === "connected",
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  const openModal = useCallback12(() => {
    setSelectedModal({
      type: "settings",
      initialTab: updateAvailable ? "updates" : canEditConfig ? "studio" : "shortcuts",
      initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
    });
  }, [canEditConfig, setSelectedModal, updateAvailable]);
  const renderGearIcon = useCallback12((color) => {
    return /* @__PURE__ */ jsx22(GearIcon, {
      color,
      width: 16,
      height: 16
    });
  }, []);
  const renderUpdateIcon = useCallback12((color) => {
    return /* @__PURE__ */ jsx22("svg", {
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        height: 16,
        width: 16,
        color,
        flexShrink: 0
      },
      viewBox: "0 0 512 512",
      children: /* @__PURE__ */ jsx22("path", {
        fill: color,
        d: "M256 32a224 224 0 1 1 0 448 224 224 0 1 1 0-448zm0 480a256 256 0 1 0 0-512 256 256 0 1 0 0 512zM377 231L273 127c-9.4-9.4-24.6-9.4-33.9 0L135 231c-6.9 6.9-8.9 17.2-5.2 26.2S142.3 272 152 272l40 0 0 72c0 22.1 17.9 40 40 40l48 0c22.1 0 40-17.9 40-40l0-72 40 0c9.7 0 18.5-5.8 22.2-14.8s1.7-19.3-5.2-26.2z"
      })
    });
  }, []);
  const label3 = hasBugfixesAvailable ? "Bugfixes available" : updateAvailable ? "Update available" : "Settings";
  return /* @__PURE__ */ jsx22(ActionTooltip, {
    label: label3,
    shortcut: null,
    delay: 800,
    dismissOnClick: true,
    children: /* @__PURE__ */ jsx22(InlineAction, {
      variant: null,
      onClick: openModal,
      renderAction: updateAvailable ? renderUpdateIcon : renderGearIcon,
      unhoveredColor: hasBugfixesAvailable ? WARNING_COLOR : WHITE_ALPHA_80,
      title: "",
      "aria-label": label3
    })
  });
};

// src/components/SidebarCollapserControls.tsx
import { useCallback as useCallback54, useContext as useContext65, useEffect as useEffect41 } from "react";

// src/components/TopPanel.tsx
import React72, { useCallback as useCallback53, useContext as useContext64, useEffect as useEffect40, useMemo as useMemo57 } from "react";
import { Internals as Internals59 } from "remotion";

// src/components/CanvasIfSizeIsAvailable.tsx
import { useContext as useContext43, useLayoutEffect as useLayoutEffect13, useMemo as useMemo43 } from "react";
import { Internals as Internals45 } from "remotion";

// src/helpers/ruler-canvas-size.ts
var applyRulerInsetsToCanvasSize = ({
  rulersAreVisible,
  size
}) => {
  if (!rulersAreVisible) {
    return size;
  }
  return {
    ...size,
    left: size.left + RULER_WIDTH,
    top: size.top + RULER_WIDTH,
    width: size.width - RULER_WIDTH,
    height: size.height - RULER_WIDTH
  };
};
var getRulerCanvasSize = ({
  orientation,
  size
}) => {
  if (orientation === "vertical") {
    return {
      height: size.height,
      width: RULER_WIDTH
    };
  }
  return {
    height: RULER_WIDTH,
    width: size.width
  };
};

// src/components/CanvasOrLoading.tsx
import { useContext as useContext41, useEffect as useEffect31 } from "react";
import { Internals as Internals43 } from "remotion";

// src/error-overlay/remotion-overlay/ErrorLoader.tsx
import { getLocationFromBuildError as getLocationFromBuildError2 } from "@remotion/studio-shared";
import { useEffect as useEffect14, useState as useState10 } from "react";

// src/error-overlay/current-error.ts
var registerCurrentError = (error) => {
  window.remotion_currentErrors = [
    ...window.remotion_currentErrors ?? [],
    error
  ];
  return () => {
    window.remotion_currentErrors = window.remotion_currentErrors?.filter((candidate) => candidate !== error);
  };
};
var getCurrentError = () => {
  return window.remotion_currentErrors?.[0] ?? null;
};

// src/error-overlay/error-origin.ts
var errorsLoggedByServer = new WeakSet;
var errorMessagesLoggedByServer = new Set;
var maxLoggedErrorMessages = 100;
var rememberServerLoggedMessage = (message) => {
  if (errorMessagesLoggedByServer.size >= maxLoggedErrorMessages) {
    errorMessagesLoggedByServer.clear();
  }
  errorMessagesLoggedByServer.add(message);
};
var markErrorAsLoggedByServer = (error) => {
  errorsLoggedByServer.add(error);
};
var markErrorMessageAsLoggedByServer = (message) => {
  if (message.trim() === "") {
    return;
  }
  rememberServerLoggedMessage(message);
};
var wasErrorLoggedByServer = (error) => {
  if (errorsLoggedByServer.has(error)) {
    return true;
  }
  const message = typeof error.message === "string" ? error.message : null;
  const stack = typeof error.stack === "string" ? error.stack : null;
  if (message && errorMessagesLoggedByServer.has(message)) {
    return true;
  }
  if (stack) {
    for (const serverLoggedMessage of errorMessagesLoggedByServer) {
      if (stack.includes(serverLoggedMessage)) {
        return true;
      }
    }
  }
  return false;
};

// src/error-overlay/remotion-overlay/Overlay.tsx
import {
  createRef as createRef4,
  useCallback as useCallback13,
  useImperativeHandle as useImperativeHandle3,
  useState as useState5
} from "react";
import { Internals as Internals11 } from "remotion";
import { jsx as jsx23 } from "react/jsx-runtime";
var setErrorsRef = createRef4();
var errorsAreTheSame = (first, second) => {
  return first.stack === second.stack && first.message === second.message;
};
var BACKGROUND_COLOR = BACKGROUND_HEX;
var Overlay = () => {
  const [errors, setErrors] = useState5({ type: "clear" });
  const addError = useCallback13((err) => {
    setErrors((state) => {
      if (state.type === "errors") {
        if (state.errors.some((e) => errorsAreTheSame(e, err))) {
          return state;
        }
        return {
          ...state,
          errors: [...state.errors, err]
        };
      }
      return {
        type: "errors",
        errors: [err]
      };
    });
  }, []);
  useImperativeHandle3(setErrorsRef, () => {
    return { setErrors, addError };
  }, [addError]);
  if (errors.type === "clear") {
    return null;
  }
  if (errors.errors.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx23(PreviewServerConnection, {
    children: /* @__PURE__ */ jsx23(SettingsProvider, {
      children: /* @__PURE__ */ jsx23(KeybindingContextProvider, {
        children: /* @__PURE__ */ jsx23(Internals11.AbsoluteFillElement, {
          style: {
            backgroundColor: BACKGROUND_COLOR,
            overflow: "auto",
            color: WHITE,
            top: MENU_TOOLBAR_HEIGHT,
            height: `calc(100% - ${MENU_TOOLBAR_HEIGHT}px)`
          },
          children: errors.errors.map((err, i) => {
            return /* @__PURE__ */ jsx23(ErrorLoader, {
              keyboardShortcuts: i === 0,
              error: err,
              onRetry: null,
              canHaveDismissButton: true,
              calculateMetadata: false
            }, (err.stack ?? "") + i);
          })
        })
      })
    })
  });
};

// src/error-overlay/react-overlay/effects/format-warning.ts
function stripInlineStacktrace(message) {
  return message.split(`
`).filter((line) => !line.match(/^\s*in/)).join(`
`);
}
function massageWarning(warning, frames) {
  const message = stripInlineStacktrace(warning);
  let stack = "";
  let lastFilename;
  let lastLineNumber;
  for (let index = 0;index < frames.length; ++index) {
    const { fileName, lineNumber } = frames[index];
    if (fileName === null || lineNumber === null || lineNumber === undefined || fileName === undefined) {
      continue;
    }
    if (fileName === lastFilename && typeof lineNumber === "number" && typeof lastLineNumber === "number" && Math.abs(lineNumber - lastLineNumber) < 3) {
      continue;
    }
    lastFilename = fileName;
    lastLineNumber = lineNumber;
    let { name } = frames[index];
    name = name || "(anonymous function)";
    stack += `in ${name} (at ${fileName}:${lineNumber})
`;
  }
  return { message, stack };
}

// src/error-overlay/react-overlay/effects/proxy-console.ts
var reactFrameStack = [];
var registerReactStack = () => {
  if (typeof console !== "undefined") {
    console.reactStack = (frames) => reactFrameStack.push(frames);
    console.reactStackEnd = () => reactFrameStack.pop();
  }
};
var unregisterReactStack = () => {
  if (typeof console !== "undefined") {
    console.reactStack = undefined;
    console.reactStackEnd = undefined;
  }
};
var permanentRegister = function(type, callback) {
  if (typeof console !== "undefined") {
    const orig = console[type];
    if (typeof orig === "function") {
      console[type] = function(...args) {
        try {
          const message = args[0];
          if (typeof message === "string" && reactFrameStack.length > 0) {
            callback({
              type: "webpack-error",
              message,
              frames: reactFrameStack[reactFrameStack.length - 1]
            });
          }
          if (message instanceof Error) {
            callback({
              type: "build-error",
              error: message
            });
          }
        } catch (err) {
          setTimeout(() => {
            throw err;
          });
        }
        return orig.apply(this, args);
      };
    }
  }
};

// src/error-overlay/react-overlay/effects/stack-trace-limit.ts
var stackTraceRegistered = false;
var restoreStackTraceValue = 10;
var MAX_STACK_LENGTH = 50;
function registerStackTraceLimit(limit = MAX_STACK_LENGTH) {
  if (stackTraceRegistered) {
    return;
  }
  try {
    restoreStackTraceValue = Error.stackTraceLimit;
    Error.stackTraceLimit = limit;
    stackTraceRegistered = true;
  } catch {}
}
function unregisterStackTraceLimit() {
  if (!stackTraceRegistered) {
    return;
  }
  try {
    Error.stackTraceLimit = restoreStackTraceValue;
    stackTraceRegistered = false;
  } catch {}
}

// src/error-overlay/react-overlay/effects/unhandled-error.ts
var boundErrorHandler = null;
function errorHandler(callback, e) {
  if (!e.error) {
    return;
  }
  const { error } = e;
  if (error instanceof Error) {
    callback(error);
  } else {
    callback(new Error(error));
  }
}
function registerUnhandledError(target, callback) {
  if (boundErrorHandler !== null) {
    return;
  }
  boundErrorHandler = errorHandler.bind(undefined, callback);
  target.addEventListener("error", boundErrorHandler);
}
function unregisterUnhandledError(target) {
  if (boundErrorHandler === null) {
    return;
  }
  target.removeEventListener("error", boundErrorHandler);
  boundErrorHandler = null;
}

// src/error-overlay/react-overlay/effects/unhandled-rejection.ts
var boundRejectionHandler = null;
function rejectionHandler(callback, e) {
  if (!e?.reason) {
    return callback(new Error("Unknown"));
  }
  const { reason } = e;
  if (reason instanceof Error) {
    return callback(reason);
  }
  return callback(new Error(reason));
}
function registerUnhandledRejection(target, callback) {
  if (boundRejectionHandler !== null) {
    return;
  }
  boundRejectionHandler = rejectionHandler.bind(undefined, callback);
  target.addEventListener("unhandledrejection", boundRejectionHandler);
}
function unregisterUnhandledRejection(target) {
  if (boundRejectionHandler === null) {
    return;
  }
  target.removeEventListener("unhandledrejection", boundRejectionHandler);
  boundRejectionHandler = null;
}

// src/error-overlay/react-overlay/utils/unmapper.ts
import { sourceContentFor } from "@jridgewell/trace-mapping";
import { Internals as Internals12 } from "remotion";

// src/error-overlay/react-overlay/utils/get-lines-around.ts
function getLinesAround(line, count, lines) {
  const result = [];
  for (let index = Math.max(0, line - 1 - count);index <= Math.min(lines.length - 1, line - 1 + count); ++index) {
    result.push({
      lineNumber: index + 1,
      content: lines[index],
      highlight: index === line - 1
    });
  }
  return result;
}

// src/error-overlay/react-overlay/utils/unmapper.ts
var getFileContents = async (fileName) => {
  const res = await fetch(fileName);
  const fileContents = await res.text();
  return fileContents;
};
var unmap = async (frames, contextLines) => {
  const transpiled = frames.filter((s) => s.type === "transpiled").map((s) => s.frame);
  const uniqueFileNames = [
    ...new Set(transpiled.map((f) => f.fileName).filter(Internals12.truthy))
  ];
  const maps = await Promise.all(uniqueFileNames.map(async (fileName) => {
    const fileContents = await getFileContents(fileName);
    return getSourceMap(fileName, fileContents);
  }));
  const mapValues = {};
  for (let i = 0;i < uniqueFileNames.length; i++) {
    mapValues[uniqueFileNames[i]] = maps[i];
  }
  return frames.map((frame) => {
    if (frame.type === "symbolicated") {
      return frame.frame;
    }
    const map = mapValues[frame.frame.fileName];
    if (!map) {
      return null;
    }
    const pos = getOriginalPosition(map, frame.frame.lineNumber, frame.frame.columnNumber);
    const { functionName } = frame.frame;
    let hasSource = null;
    hasSource = pos.source ? sourceContentFor(map, pos.source) : null;
    const scriptCode = hasSource && pos.line ? getLinesAround(pos.line, contextLines, hasSource.split(`
`)) : null;
    return {
      originalColumnNumber: pos.column,
      originalFileName: pos.source,
      originalFunctionName: functionName,
      originalLineNumber: pos.line,
      originalScriptCode: scriptCode
    };
  }).filter(Internals12.truthy);
};

// src/error-overlay/react-overlay/utils/get-stack-frames.ts
var getStackFrames = async (error, contextSize) => {
  const parsedFrames = await parseError(error, contextSize);
  const enhancedFrames = await unmap(parsedFrames, contextSize);
  if (enhancedFrames.map((f) => f.originalFileName).filter((f_1) => f_1 !== null && f_1 !== undefined).length === 0) {
    return null;
  }
  return enhancedFrames;
};

// src/error-overlay/react-overlay/listen-to-runtime-errors.ts
var CONTEXT_SIZE = 3;
var getErrorRecord = async (error) => {
  const stackFrames = await getStackFrames(error, CONTEXT_SIZE);
  if (stackFrames === null || stackFrames === undefined) {
    return null;
  }
  return {
    error,
    contextSize: CONTEXT_SIZE,
    stackFrames
  };
};
var crashWithFrames = (crash) => (error) => {
  const didHookOrderChange = error.message.startsWith("Rendered fewer hooks") || error.message.startsWith("Rendered more hooks") || error.message.startsWith("Should have a queue");
  const key4 = "remotion.lastCrashBecauseOfHooks";
  const previousCrashWasBecauseOfHooks = window.localStorage.getItem(key4);
  const justRefreshedBecauseOfHooks = previousCrashWasBecauseOfHooks ? Date.now() - Number(previousCrashWasBecauseOfHooks) < 5000 : false;
  window.localStorage.setItem("remotion.lastCrashBecauseOfHooks", String(Date.now()));
  if (didHookOrderChange && !justRefreshedBecauseOfHooks) {
    console.log("Hook order changed. Reloading app...");
    reloadUrl();
  } else {
    setErrorsRef.current?.addError(error);
    crash();
  }
};
function listenToRuntimeErrors(crash) {
  const crashWithFramesRunTime = crashWithFrames(crash);
  registerUnhandledError(window, (error) => {
    return crashWithFramesRunTime(error);
  });
  registerUnhandledRejection(window, (error) => {
    return crashWithFramesRunTime(error);
  });
  registerStackTraceLimit();
  registerReactStack();
  permanentRegister("error", (d) => {
    if (d.type === "webpack-error") {
      const { message, frames } = d;
      const data = massageWarning(message, frames);
      const error = {
        message: data.message,
        stack: data.stack,
        name: ""
      };
      markErrorAsLoggedByServer(error);
      crashWithFramesRunTime(error);
    }
    if (d.type === "build-error") {
      markErrorAsLoggedByServer(d.error);
      crashWithFramesRunTime(d.error);
    }
  });
  return function() {
    unregisterStackTraceLimit();
    unregisterUnhandledRejection(window);
    unregisterUnhandledError(window);
    unregisterReactStack();
  };
}

// src/error-overlay/remotion-overlay/ErrorDisplay.tsx
import { getLocationFromBuildError } from "@remotion/studio-shared";
import { useContext as useContext13, useMemo as useMemo18 } from "react";
import { MediaPlaybackError } from "remotion";

// src/components/CodingAgentButton.tsx
import { useCallback as useCallback14, useMemo as useMemo14 } from "react";

// src/components/AppLaunchButton.tsx
import { useMemo as useMemo13 } from "react";
import { jsx as jsx24 } from "react/jsx-runtime";
var compactMainSegmentStyle = {
  fontFamily: "inherit",
  gap: 6,
  padding: "0 9px"
};
var defaultMainSegmentStyle = {
  ...compactMainSegmentStyle,
  fontSize: 14,
  lineHeight: "21px"
};
var dropdownSegmentStyle = {
  fontFamily: "inherit",
  padding: 0,
  width: 20
};
var dropdownIconStyle = {
  display: "flex",
  transform: "translateY(-1px)"
};
var AppLaunchButton = ({
  actionButtonId,
  ariaLabel,
  children,
  disabled,
  menuAriaLabel,
  menuButtonId,
  menuItems,
  onConfigureApps,
  onClick,
  size,
  style: style2,
  title
}) => {
  const items = useMemo13(() => {
    return [
      ...menuItems,
      ...getConfigureDefaultAppsMenuItems({
        hasPreviousItems: menuItems.length > 0,
        onConfigureApps
      })
    ];
  }, [menuItems, onConfigureApps]);
  const segments = useMemo13(() => {
    return [
      {
        ariaLabel,
        buttonId: actionButtonId,
        disabled,
        idleColor: LIGHT_TEXT,
        onClick,
        onPointerDown: null,
        renderContent: () => children,
        segmentId: "preferred-app",
        style: size === "default" ? defaultMainSegmentStyle : compactMainSegmentStyle,
        title,
        tooltipLabel: null,
        type: "action"
      },
      ...items.length > 0 ? [
        {
          ariaLabel: menuAriaLabel,
          buttonId: menuButtonId,
          disabled: false,
          idleColor: LIGHT_TEXT,
          leaveLeftSpace: true,
          onOpenChange: null,
          renderContent: (color) => /* @__PURE__ */ jsx24("span", {
            style: dropdownIconStyle,
            children: /* @__PURE__ */ jsx24(CaretDown, {
              color
            })
          }),
          segmentId: "another-app",
          selectedId: null,
          style: dropdownSegmentStyle,
          title: menuAriaLabel,
          tooltipLabel: null,
          type: "menu",
          values: items
        }
      ] : []
    ];
  }, [
    actionButtonId,
    ariaLabel,
    children,
    disabled,
    items,
    menuAriaLabel,
    menuButtonId,
    onClick,
    size,
    title
  ]);
  return /* @__PURE__ */ jsx24(SegmentedButton, {
    segments,
    style: { ...size === "default" ? { height: 41 } : null, ...style2 },
    title: null
  });
};

// src/components/CodingAgentButton.tsx
import { jsx as jsx25, jsxs as jsxs12 } from "react/jsx-runtime";
var menuLabel = {
  color: "inherit",
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "16px"
};
var CodingAgentButton = ({ label: label3, prompt, size, style: style2 }) => {
  const { codingAgentInfo } = useSettings();
  const configureDefaultApps = useConfigureDefaultApps();
  const installedCodingAgents = codingAgentInfo?.installedCodingAgents ?? [];
  const defaultCodingAgent = installedCodingAgents.find((agent) => agent.id === codingAgentInfo?.defaultCodingAgent) ?? installedCodingAgents[0];
  const alternativeCodingAgents = installedCodingAgents.filter((agent) => agent.id !== defaultCodingAgent?.id);
  const openWithCodingAgent = useCallback14(async (codingAgentId, codingAgentName) => {
    try {
      const response = await openInCodingAgent(codingAgentId, codingAgentId === "copilot" ? null : prompt);
      if (!response.success) {
        showNotification(`Could not open ${codingAgentName}`, 2000);
      }
    } catch (err) {
      showNotification(err.message, 2000);
    }
  }, [prompt]);
  const agentMenuItems = useMemo14(() => {
    return alternativeCodingAgents.map((codingAgent) => ({
      id: `open-in-${codingAgent.id}`,
      keyHint: null,
      label: /* @__PURE__ */ jsx25("span", {
        style: menuLabel,
        children: codingAgent.name
      }),
      leftItem: /* @__PURE__ */ jsx25(CodingAgentIcon, {
        codingAgentId: codingAgent.id,
        size: 18
      }),
      onClick: () => {
        openWithCodingAgent(codingAgent.id, codingAgent.nameWithType).catch(() => {
          return;
        });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: `coding-agent-${codingAgent.id}`
    }));
  }, [alternativeCodingAgents, openWithCodingAgent]);
  if (!defaultCodingAgent) {
    return null;
  }
  return /* @__PURE__ */ jsxs12(AppLaunchButton, {
    actionButtonId: null,
    ariaLabel: `${label3} ${defaultCodingAgent.nameWithType}`,
    disabled: false,
    menuAriaLabel: `${label3} another coding agent`,
    menuButtonId: null,
    menuItems: agentMenuItems,
    onConfigureApps: configureDefaultApps,
    onClick: () => {
      openWithCodingAgent(defaultCodingAgent.id, defaultCodingAgent.nameWithType).catch(() => {
        return;
      });
    },
    size,
    style: style2,
    title: `${label3} ${defaultCodingAgent.nameWithType}`,
    children: [
      /* @__PURE__ */ jsx25(CodingAgentIcon, {
        codingAgentId: defaultCodingAgent.id,
        size: size === "default" ? 14 : 18
      }),
      label3,
      " ",
      defaultCodingAgent.name
    ]
  });
};

// src/error-overlay/remotion-overlay/AskOnDiscord.tsx
import { useCallback as useCallback15, useEffect as useEffect9 } from "react";
import { jsx as jsx26, jsxs as jsxs13 } from "react/jsx-runtime";
var DISCORD_LINK = "https://remotion.dev/discord";
var AskOnDiscord = ({ canHaveKeyboardShortcuts }) => {
  const openInBrowser = useCallback15(() => {
    window.open(DISCORD_LINK, "_blank");
  }, []);
  const { registerKeybinding } = useKeybinding();
  useEffect9(() => {
    if (!canHaveKeyboardShortcuts) {
      return;
    }
    const onEditor = () => {
      openInBrowser();
    };
    const { unregister } = registerKeybinding({
      event: "keydown",
      key: "d",
      callback: onEditor,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => unregister();
  }, [canHaveKeyboardShortcuts, openInBrowser, registerKeybinding]);
  return /* @__PURE__ */ jsxs13(Button, {
    onClick: openInBrowser,
    children: [
      "Ask on Discord",
      " ",
      canHaveKeyboardShortcuts ? /* @__PURE__ */ jsx26(ShortcutHint, {
        keyToPress: "d",
        cmdOrCtrl: true
      }) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/CalculateMetadataErrorExplainer.tsx
import { jsx as jsx27, jsxs as jsxs14 } from "react/jsx-runtime";
var CalculateMetadataErrorExplainer = () => {
  return /* @__PURE__ */ jsxs14("div", {
    style: style2,
    children: [
      "This error occurred while calling",
      " ",
      /* @__PURE__ */ jsx27("code", {
        style: inlineCodeSnippet,
        children: "calculateMetadata()"
      }),
      "."
    ]
  });
};
var style2 = {
  borderRadius: 3,
  color: WHITE,
  padding: 12,
  backgroundColor: BLACK_HEX,
  fontSize: 14,
  fontFamily: "sans-serif"
};

// src/error-overlay/remotion-overlay/CopyStackTrace.tsx
import { useCallback as useCallback16, useMemo as useMemo15, useState as useState6 } from "react";
import { jsx as jsx28 } from "react/jsx-runtime";
var CopyStackTrace = ({ errorText }) => {
  const [copyState, setCopyState] = useState6("idle");
  const handleCopyToClipboard = useCallback16(() => {
    navigator.clipboard.writeText(errorText).then(() => {
      setCopyState("copied");
      setTimeout(() => setCopyState("idle"), 2000);
    }).catch(() => {
      setCopyState("failed");
      setTimeout(() => setCopyState("idle"), 2000);
    });
  }, [errorText]);
  const label3 = useMemo15(() => {
    if (copyState === "copied") {
      return "Copied!";
    }
    if (copyState === "failed") {
      return "Failed!";
    }
    return "Copy stack";
  }, [copyState]);
  return /* @__PURE__ */ jsx28(Button, {
    onClick: handleCopyToClipboard,
    children: label3
  });
};

// src/error-overlay/react-overlay/index.ts
import { Internals as Internals13 } from "remotion";
var stopListeningToRuntimeErrors = null;
var didUnmountReactApp = () => {
  return !Internals13.getPreviewDomElement()?.hasChildNodes();
};
function startReportingRuntimeErrors(onError) {
  if (stopListeningToRuntimeErrors !== null) {
    throw new Error("Already listening");
  }
  const handleRuntimeError = () => {
    onError();
  };
  stopListeningToRuntimeErrors = listenToRuntimeErrors(handleRuntimeError);
}

// src/error-overlay/remotion-overlay/DismissButton.tsx
import { useCallback as useCallback17 } from "react";
import { jsx as jsx29 } from "react/jsx-runtime";
var size = {
  height: 20,
  width: 20
};
var style3 = {
  appearance: "none",
  WebkitAppearance: "none",
  backgroundColor: TRANSPARENT,
  border: "none",
  cursor: "pointer"
};
var DismissButton = () => {
  const dismiss = useCallback17(() => {
    clearUrl();
  }, []);
  return /* @__PURE__ */ jsx29("button", {
    type: "button",
    style: style3,
    onClick: dismiss,
    children: /* @__PURE__ */ jsx29("svg", {
      focusable: "false",
      role: "img",
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 352 512",
      style: size,
      children: /* @__PURE__ */ jsx29("path", {
        fill: WHITE,
        d: "M242.72 256l100.07-100.07c12.28-12.28 12.28-32.19 0-44.48l-22.24-22.24c-12.28-12.28-32.19-12.28-44.48 0L176 189.28 75.93 89.21c-12.28-12.28-32.19-12.28-44.48 0L9.21 111.45c-12.28 12.28-12.28 32.19 0 44.48L109.28 256 9.21 356.07c-12.28 12.28-12.28 32.19 0 44.48l22.24 22.24c12.28 12.28 32.2 12.28 44.48 0L176 322.72l100.07 100.07c12.28 12.28 32.2 12.28 44.48 0l22.24-22.24c12.28-12.28 12.28-32.19 0-44.48L242.72 256z"
      })
    })
  });
};

// src/error-overlay/remotion-overlay/ErrorMessage.tsx
import { PlayerInternals as PlayerInternals4 } from "@remotion/player";
import { useCallback as useCallback18, useMemo as useMemo16, useRef as useRef5, useState as useState7 } from "react";

// src/error-overlay/remotion-overlay/carets.tsx
import { jsx as jsx30 } from "react/jsx-runtime";
var CaretDown2 = ({ invert, size: size2 }) => {
  return /* @__PURE__ */ jsx30("svg", {
    "aria-hidden": "true",
    focusable: "false",
    role: "img",
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 320 512",
    style: { height: size2 ?? 20, transform: invert ? `rotate(180deg)` : "" },
    children: /* @__PURE__ */ jsx30("path", {
      fill: CURRENT_COLOR,
      d: "M31.3 192h257.3c17.8 0 26.7 21.5 14.1 34.1L174.1 354.8c-7.8 7.8-20.5 7.8-28.3 0L17.2 226.1C4.6 213.5 13.5 192 31.3 192z"
    })
  });
};

// src/error-overlay/remotion-overlay/ErrorMessage.tsx
import { jsx as jsx31, jsxs as jsxs15 } from "react/jsx-runtime";
var fontSize = 24;
var lineHeight = 1.5;
var maxLines = 2;
var buttonSize = 32;
var maskImage = ERROR_MESSAGE_MASK_IMAGE;
var container6 = {
  position: "relative",
  marginBottom: 15
};
var messageContainer = {
  overflow: "hidden"
};
var textContainer = {
  fontSize,
  lineHeight
};
var moreLine = {
  width: "100%",
  display: "flex",
  justifyContent: "center",
  position: "absolute",
  border: `1px solid ${WHITE_ALPHA_05}`,
  height: 0,
  marginTop: 4
};
var moreButton = {
  height: buttonSize,
  width: buttonSize,
  borderRadius: buttonSize / 2,
  backgroundColor: INPUT_BACKGROUND,
  border: `1px solid ${BLACK_ALPHA_60}`,
  marginTop: -buttonSize / 2,
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  cursor: "pointer",
  color: WHITE
};
var ErrorMessage = ({ message }) => {
  const [expanded, setExpanded] = useState7(false);
  const ref = useRef5(null);
  const size2 = PlayerInternals4.useElementSize(ref, {
    shouldApplyCssTransforms: false,
    triggerOnWindowResize: true
  });
  const errorLines = size2 ? size2.height / (lineHeight * fontSize) : null;
  const style4 = useMemo16(() => {
    const isExpanded = expanded || errorLines !== null && errorLines <= maxLines;
    return {
      ...messageContainer,
      maxHeight: isExpanded ? undefined : fontSize * lineHeight * maxLines,
      maskImage: isExpanded ? undefined : maskImage,
      WebkitMaskImage: isExpanded ? undefined : maskImage
    };
  }, [errorLines, expanded]);
  const toggle = useCallback18(() => {
    setExpanded((e) => !e);
  }, []);
  return /* @__PURE__ */ jsxs15("div", {
    style: container6,
    children: [
      /* @__PURE__ */ jsx31("div", {
        style: style4,
        children: /* @__PURE__ */ jsx31("div", {
          ref,
          style: textContainer,
          children: message
        })
      }),
      errorLines !== null && errorLines > maxLines ? /* @__PURE__ */ jsx31("div", {
        style: moreLine,
        children: /* @__PURE__ */ jsx31("button", {
          type: "button",
          onClick: toggle,
          style: moreButton,
          children: /* @__PURE__ */ jsx31(CaretDown2, {
            invert: expanded
          })
        })
      }) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/Symbolicating.tsx
import { jsx as jsx32, jsxs as jsxs16 } from "react/jsx-runtime";
var Symbolicating = (props) => {
  return /* @__PURE__ */ jsxs16("svg", {
    id: "loading",
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 32 32",
    width: "14",
    height: "14",
    fill: WHITE,
    ...props,
    children: [
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(0 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(45 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.125s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(90 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.25s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(135 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.375s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(180 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.5s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(225 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.675s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(270 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.75s"
        })
      }),
      /* @__PURE__ */ jsx32("path", {
        opacity: ".1",
        d: "M14 0 H18 V8 H14 z",
        transform: "rotate(315 16 16)",
        children: /* @__PURE__ */ jsx32("animate", {
          attributeName: "opacity",
          from: "1",
          to: ".1",
          dur: "1s",
          repeatCount: "indefinite",
          begin: "0.875s"
        })
      })
    ]
  });
};

// src/error-overlay/remotion-overlay/ErrorTitle.tsx
import { jsx as jsx33, jsxs as jsxs17, Fragment as Fragment8 } from "react/jsx-runtime";
var title = {
  marginBottom: 8,
  display: "flex",
  flexDirection: "row",
  justifyContent: "center"
};
var left = {
  flex: 1,
  paddingLeft: 14,
  paddingRight: 14,
  fontWeight: "bold",
  maxWidth: "100%"
};
var errName = {
  fontSize: 18,
  color: BLUE,
  display: "inline-block"
};
var row = {
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var spacer = {
  width: 5
};
var ErrorTitle = ({ name, message, symbolicating, canHaveDismissButton }) => {
  return /* @__PURE__ */ jsxs17("div", {
    style: title,
    className: "css-reset",
    children: [
      /* @__PURE__ */ jsxs17("div", {
        style: left,
        children: [
          /* @__PURE__ */ jsxs17("div", {
            style: row,
            children: [
              /* @__PURE__ */ jsx33("span", {
                style: errName,
                children: name
              }),
              symbolicating ? /* @__PURE__ */ jsxs17(Fragment8, {
                children: [
                  /* @__PURE__ */ jsx33("div", {
                    style: spacer
                  }),
                  /* @__PURE__ */ jsx33(Symbolicating, {})
                ]
              }) : null
            ]
          }),
          /* @__PURE__ */ jsx33("div", {
            style: row,
            children: /* @__PURE__ */ jsx33(ErrorMessage, {
              message
            })
          })
        ]
      }),
      didUnmountReactApp() ? null : canHaveDismissButton ? /* @__PURE__ */ jsx33(DismissButton, {}) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/get-help-link.ts
var getHelpLink = (message) => {
  if (message.includes("See https://www.remotion.dev/docs/the-fundamentals#defining-compositions")) {
    return {
      title: "Defining compositions",
      url: "See https://www.remotion.dev/docs/the-fundamentals#defining-compositions"
    };
  }
  if (message.includes("https://remotion.dev/docs/wrong-composition-mount")) {
    return {
      title: "Wrongly mounted <Composition>",
      url: "https://remotion.dev/docs/wrong-composition-mount"
    };
  }
  if (message.includes("https://remotion.dev/docs/staticfile-relative-paths")) {
    return {
      title: "staticFile() relative paths",
      url: "https://remotion.dev/docs/staticfile-relative-paths"
    };
  }
  if (message.includes("https://remotion.dev/docs/staticfile-remote-urls")) {
    return {
      title: "staticFile() remote URLs",
      url: "https://remotion.dev/docs/staticfile-remote-urls"
    };
  }
  if (message.includes("https://remotion.dev/docs/non-seekable-media")) {
    return {
      title: "Non-seekable media",
      url: "https://remotion.dev/docs/non-seekable-media"
    };
  }
  if (message.includes("https://remotion.dev/docs/media-playback-error")) {
    return {
      title: "Media playback error",
      url: "https://remotion.dev/docs/media-playback-error"
    };
  }
  if (message.includes("Div is not part of the THREE")) {
    return {
      title: "<Sequence> inside <ThreeCanvas>",
      url: "https://remotion.dev/docs/sequence#note-for-remotionthree"
    };
  }
  return null;
};

// src/error-overlay/remotion-overlay/HelpLink.tsx
import { useCallback as useCallback19, useEffect as useEffect10 } from "react";
import { jsx as jsx34, jsxs as jsxs18 } from "react/jsx-runtime";
var buttonStyle3 = {
  backgroundColor: BLUE,
  color: WHITE
};
var HelpLink = ({ canHaveKeyboardShortcuts, link }) => {
  const openLink = useCallback19(() => {
    window.open(link.url, "_blank");
  }, [link]);
  const { registerKeybinding } = useKeybinding();
  useEffect10(() => {
    if (!canHaveKeyboardShortcuts) {
      return;
    }
    const onEditor = () => {
      openLink();
    };
    const { unregister } = registerKeybinding({
      event: "keydown",
      key: "h",
      callback: onEditor,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => unregister();
  }, [canHaveKeyboardShortcuts, openLink, registerKeybinding]);
  return /* @__PURE__ */ jsxs18(Button, {
    style: buttonStyle3,
    onClick: openLink,
    children: [
      "Help: ",
      '"',
      link.title,
      '"',
      canHaveKeyboardShortcuts ? /* @__PURE__ */ jsx34(ShortcutHint, {
        keyToPress: "h",
        cmdOrCtrl: true
      }) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/MediaPlaybackErrorExplainer.tsx
import { useEffect as useEffect11, useState as useState8 } from "react";
import { jsx as jsx35, jsxs as jsxs19 } from "react/jsx-runtime";
var container7 = {
  borderRadius: 3,
  color: WHITE,
  padding: 12,
  backgroundColor: BLACK_HEX,
  fontSize: 14,
  fontFamily: "sans-serif",
  lineHeight: 1.5
};
var codeStyle = {
  backgroundColor: WHITE_ALPHA_10,
  padding: "2px 5px",
  borderRadius: 3,
  fontFamily: "monospace",
  fontSize: 13
};
var linkStyle = {
  color: ERROR_LINK_COLOR
};
var MediaPlaybackErrorExplainer = ({ src }) => {
  const [result, setResult] = useState8({ type: "loading" });
  useEffect11(() => {
    fetch(src, { method: "HEAD" }).then((res) => {
      if (res.status === 404) {
        setResult({ type: "not-found" });
      } else if (!res.ok) {
        setResult({ type: "other-error", statusCode: res.status });
      } else {
        const contentType = res.headers.get("content-type") ?? "";
        if (contentType.includes("text/html") || contentType.includes("application/json")) {
          setResult({ type: "wrong-content-type", contentType });
        } else {
          setResult({ type: "ok" });
        }
      }
    }).catch(() => {
      setResult({ type: "fetch-error" });
    });
  }, [src]);
  if (result.type === "loading") {
    return null;
  }
  if (result.type === "not-found") {
    return /* @__PURE__ */ jsxs19("div", {
      style: container7,
      children: [
        "The file ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: src
        }),
        " was not found (404).",
        /* @__PURE__ */ jsx35("br", {}),
        "If the file is in your ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: "public/"
        }),
        " folder, make sure to use",
        " ",
        /* @__PURE__ */ jsx35("a", {
          style: linkStyle,
          href: "https://remotion.dev/docs/staticfile",
          target: "_blank",
          children: /* @__PURE__ */ jsx35("code", {
            style: codeStyle,
            children: "staticFile()"
          })
        }),
        " ",
        "to reference it.",
        /* @__PURE__ */ jsx35("br", {}),
        /* @__PURE__ */ jsx35("a", {
          style: linkStyle,
          href: "https://remotion.dev/docs/media-playback-error",
          target: "_blank",
          children: "Learn more"
        })
      ]
    });
  }
  if (result.type === "other-error") {
    return /* @__PURE__ */ jsxs19("div", {
      style: container7,
      children: [
        "⚠️ Fetching ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: src
        }),
        " returned status code",
        " ",
        result.statusCode,
        ".",
        /* @__PURE__ */ jsx35("br", {}),
        /* @__PURE__ */ jsx35("a", {
          style: linkStyle,
          href: "https://remotion.dev/docs/media-playback-error",
          target: "_blank",
          children: "Learn more"
        })
      ]
    });
  }
  if (result.type === "wrong-content-type") {
    return /* @__PURE__ */ jsxs19("div", {
      style: container7,
      children: [
        "⚠️ Fetching ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: src
        }),
        " returned a",
        " ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: result.contentType
        }),
        " content type.",
        /* @__PURE__ */ jsx35("br", {}),
        "\uD83D\uDC49 If the file is in your ",
        /* @__PURE__ */ jsx35("code", {
          style: codeStyle,
          children: "public/"
        }),
        " ",
        "folder, make sure to use",
        " ",
        /* @__PURE__ */ jsx35("a", {
          style: linkStyle,
          href: "https://remotion.dev/docs/staticfile",
          target: "_blank",
          children: /* @__PURE__ */ jsx35("code", {
            style: codeStyle,
            children: "staticFile()"
          })
        }),
        " ",
        "to reference it.",
        /* @__PURE__ */ jsx35("br", {}),
        /* @__PURE__ */ jsx35("a", {
          style: linkStyle,
          href: "https://remotion.dev/docs/media-playback-error",
          target: "_blank",
          children: "Learn more"
        })
      ]
    });
  }
  return null;
};

// src/error-overlay/remotion-overlay/OpenInEditor.tsx
import { useCallback as useCallback20, useEffect as useEffect12, useMemo as useMemo17 } from "react";
import { jsx as jsx36, jsxs as jsxs20 } from "react/jsx-runtime";
var menuLabel2 = {
  color: "inherit",
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "16px"
};
var OpenInEditor = ({ stack, canHaveKeyboardShortcuts, editorId, editorName, size: size2 }) => {
  const { editorInfo } = useSettings();
  const configureDefaultApps = useConfigureDefaultApps();
  const { registerKeybinding } = useKeybinding();
  const openWithEditor = useCallback20(async (selectedEditorId, selectedEditorName) => {
    try {
      const response = await openInEditor(stack, selectedEditorId);
      if (!response.success) {
        showNotification(`Could not open ${selectedEditorName}`, 2000);
      }
    } catch (err) {
      showNotification(err.message, 2000);
    }
  }, [stack]);
  const openPreferredEditor = useCallback20(() => {
    openWithEditor(editorId, editorName).catch(() => {
      return;
    });
  }, [editorId, editorName, openWithEditor]);
  useEffect12(() => {
    if (!canHaveKeyboardShortcuts) {
      return;
    }
    const { unregister } = registerKeybinding({
      event: "keydown",
      key: "o",
      callback: openPreferredEditor,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => unregister();
  }, [canHaveKeyboardShortcuts, openPreferredEditor, registerKeybinding]);
  const menuItems = useMemo17(() => {
    const alternativeEditors = (editorInfo?.installedEditors ?? []).filter((editor) => editor.id !== editorId);
    const editorItems = alternativeEditors.map((editor) => ({
      id: `open-in-${editor.id}`,
      keyHint: null,
      label: /* @__PURE__ */ jsx36("span", {
        style: menuLabel2,
        children: editor.nameWithType
      }),
      leftItem: /* @__PURE__ */ jsx36(EditorIcon, {
        editorId: editor.id,
        size: 18
      }),
      onClick: () => {
        openWithEditor(editor.id, editor.nameWithType).catch(() => {
          return;
        });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: editor.id
    }));
    return editorItems;
  }, [editorId, editorInfo?.installedEditors, openWithEditor]);
  return /* @__PURE__ */ jsxs20(AppLaunchButton, {
    actionButtonId: "error-overlay-open-in-editor",
    ariaLabel: `Open in ${editorName}`,
    disabled: false,
    menuAriaLabel: "Open in another app",
    menuButtonId: "error-overlay-open-in-another-app",
    menuItems,
    onConfigureApps: configureDefaultApps,
    onClick: openPreferredEditor,
    size: size2,
    style: null,
    title: `Open in ${editorName}`,
    children: [
      /* @__PURE__ */ jsx36(EditorIcon, {
        editorId,
        size: size2 === "default" ? 14 : 18
      }),
      "Open in ",
      editorName,
      canHaveKeyboardShortcuts ? /* @__PURE__ */ jsx36(ShortcutHint, {
        keyToPress: "o",
        cmdOrCtrl: true
      }) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/Retry.tsx
import { jsx as jsx37 } from "react/jsx-runtime";
var RetryButton = ({ onClick, label: label3 = "Retry" }) => {
  return /* @__PURE__ */ jsx37(Button, {
    onClick,
    children: label3
  });
};

// src/error-overlay/remotion-overlay/SearchGitHubIssues.tsx
import { useCallback as useCallback21, useEffect as useEffect13 } from "react";
import { jsx as jsx38, jsxs as jsxs21 } from "react/jsx-runtime";
var SearchGithubIssues = ({ message, canHaveKeyboardShortcuts }) => {
  const openInBrowser = useCallback21(() => {
    window.open(`https://github.com/remotion-dev/remotion/issues?q=${encodeURIComponent(message)}`, "_blank");
  }, [message]);
  const { registerKeybinding } = useKeybinding();
  useEffect13(() => {
    if (!canHaveKeyboardShortcuts) {
      return;
    }
    const onEditor = () => {
      openInBrowser();
    };
    const { unregister } = registerKeybinding({
      event: "keydown",
      key: "g",
      callback: onEditor,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => unregister();
  }, [canHaveKeyboardShortcuts, openInBrowser, registerKeybinding]);
  return /* @__PURE__ */ jsxs21(Button, {
    onClick: openInBrowser,
    children: [
      "Search Issues",
      " ",
      canHaveKeyboardShortcuts ? /* @__PURE__ */ jsx38(ShortcutHint, {
        keyToPress: "g",
        cmdOrCtrl: true
      }) : null
    ]
  });
};

// src/error-overlay/remotion-overlay/StackFrame.tsx
import { useCallback as useCallback22, useState as useState9 } from "react";

// src/components/LazySyntaxHighlightedSource.tsx
import { lazy, Suspense } from "react";
import { jsx as jsx39 } from "react/jsx-runtime";
var SyntaxHighlightedSource = lazy(() => import("./SyntaxHighlightedSource-3xks91rz.mjs"));
var LazySyntaxHighlightedSource = ({ source }) => {
  return /* @__PURE__ */ jsx39(Suspense, {
    fallback: source,
    children: /* @__PURE__ */ jsx39(SyntaxHighlightedSource, {
      source
    })
  });
};

// src/components/CodeFrame.tsx
import { jsx as jsx40, jsxs as jsxs22 } from "react/jsx-runtime";
var frame = {
  backgroundColor: ERROR_CODE_FRAME_BACKGROUND,
  borderRadius: 6,
  display: "flex",
  marginBottom: 20,
  overflow: "hidden"
};
var sourceContainer = {
  flex: 1,
  minWidth: 0,
  overflowX: "auto",
  overscrollBehaviorX: "none"
};
var lineNumberColumn = {
  flexShrink: 0,
  width: 60
};
var lineNumber = {
  whiteSpace: "pre",
  paddingRight: 12,
  color: "inherit",
  lineHeight: 1.7,
  width: 60,
  flexShrink: 0,
  display: "flex",
  alignItems: "center",
  justifyContent: "flex-end",
  fontFamily: "monospace"
};
var CodeFrame = ({ source, lineNumberWidth, fontSize: fontSize2, horizontalMargin }) => {
  return /* @__PURE__ */ jsxs22("div", {
    style: {
      ...frame,
      marginLeft: horizontalMargin,
      marginRight: horizontalMargin
    },
    children: [
      /* @__PURE__ */ jsx40("div", {
        style: lineNumberColumn,
        children: source.map((s, j) => {
          return /* @__PURE__ */ jsx40("div", {
            style: {
              ...lineNumber,
              fontSize: fontSize2,
              backgroundColor: s.highlight ? ERROR_CODE_FRAME_BACKGROUND : ERROR_CODE_FRAME_LINE_BACKGROUND,
              backgroundImage: s.highlight ? `linear-gradient(${SELECTED_BACKGROUND}, ${SELECTED_BACKGROUND})` : undefined,
              color: s.highlight ? WHITE : WHITE_ALPHA_40
            },
            children: String(s.lineNumber).padStart(lineNumberWidth, " ")
          }, j);
        })
      }),
      /* @__PURE__ */ jsx40("div", {
        style: sourceContainer,
        className: HORIZONTAL_SCROLLBAR_CLASSNAME,
        children: /* @__PURE__ */ jsx40("div", {
          style: { minWidth: "100%", width: "max-content" },
          children: source.map((s, j) => {
            return /* @__PURE__ */ jsx40("div", {
              style: {
                backgroundColor: s.highlight ? SELECTED_BACKGROUND : TRANSPARENT
              },
              children: /* @__PURE__ */ jsx40("code", {
                className: "language-tsx",
                style: {
                  display: "block",
                  fontFamily: "monospace",
                  fontSize: fontSize2,
                  color: "#9cdcfe",
                  whiteSpace: "pre",
                  tabSize: 2,
                  backgroundColor: TRANSPARENT,
                  lineHeight: 1.7,
                  paddingRight: 12,
                  paddingLeft: 12,
                  paddingTop: 0,
                  paddingBottom: 0
                },
                children: /* @__PURE__ */ jsx40(LazySyntaxHighlightedSource, {
                  source: s.content
                })
              })
            }, j);
          })
        })
      })
    ]
  });
};

// src/error-overlay/remotion-overlay/format-location.ts
var formatLocation = (location) => {
  if (location.startsWith("webpack://")) {
    return location.replace("webpack://", "");
  }
  return location;
};

// src/error-overlay/remotion-overlay/StackFrame.tsx
import { jsx as jsx41, jsxs as jsxs23, Fragment as Fragment9 } from "react/jsx-runtime";
var location = {
  color: LIGHT_TEXT,
  fontFamily: "monospace",
  overflowWrap: "anywhere"
};
var header = {
  paddingTop: 10,
  paddingBottom: 10,
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var left2 = {
  paddingRight: 14,
  flex: 1,
  minWidth: 0
};
var fnName = {
  lineHeight: 1.5,
  marginBottom: 3
};
var StackElement = ({
  s,
  lineNumberWidth,
  isFirst,
  defaultFunctionName,
  editorId,
  collapsible,
  fontSize: fontSize2,
  horizontalSpacing,
  headerAction
}) => {
  const [showCodeFrame, setShowCodeFrame] = useState9(() => !s.originalFileName?.includes("node_modules") && !s.originalFileName?.startsWith("webpack/") || isFirst);
  const canOpenFileLocation = Boolean(editorId && s.originalFileName);
  const onOpenFileLocation = useCallback22(() => {
    if (!canOpenFileLocation) {
      return;
    }
    if (!editorId) {
      return;
    }
    openInEditor(s, editorId).catch((err) => {
      console.log("Could not open in editor", err);
    });
  }, [canOpenFileLocation, editorId, s]);
  const toggleCodeFrame = useCallback22(() => {
    setShowCodeFrame((f) => !f);
  }, []);
  const functionName = s.originalFunctionName ?? defaultFunctionName;
  const shouldShowCodeFrame = collapsible ? showCodeFrame : true;
  return /* @__PURE__ */ jsxs23("div", {
    className: "css-reset",
    children: [
      /* @__PURE__ */ jsxs23("div", {
        style: {
          ...header,
          borderBottom: shouldShowCodeFrame ? "none" : BORDER_WHITE_ALPHA_12,
          paddingLeft: horizontalSpacing,
          paddingRight: horizontalSpacing
        },
        children: [
          /* @__PURE__ */ jsxs23("div", {
            style: left2,
            children: [
              functionName === null ? null : /* @__PURE__ */ jsx41("div", {
                style: { ...fnName, fontSize: fontSize2 },
                children: functionName
              }),
              s.originalFileName ? /* @__PURE__ */ jsx41("div", {
                style: { ...location, fontSize: fontSize2 },
                children: canOpenFileLocation ? /* @__PURE__ */ jsxs23("button", {
                  type: "button",
                  className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
                  onClick: onOpenFileLocation,
                  style: {
                    ...location,
                    ...hoverableStyle({
                      idleBackground: TRANSPARENT,
                      hoverBackground: TRANSPARENT,
                      idleColor: LIGHT_TEXT,
                      hoverColor: WHITE
                    }),
                    appearance: "none",
                    border: "none",
                    color: undefined,
                    cursor: "default",
                    fontSize: fontSize2,
                    padding: 0
                  },
                  children: [
                    formatLocation(s.originalFileName),
                    ":",
                    s.originalLineNumber
                  ]
                }) : /* @__PURE__ */ jsxs23(Fragment9, {
                  children: [
                    formatLocation(s.originalFileName),
                    ":",
                    s.originalLineNumber
                  ]
                })
              }) : null
            ]
          }),
          headerAction,
          collapsible && s.originalScriptCode && s.originalScriptCode.length > 0 ? /* @__PURE__ */ jsx41(Button, {
            onClick: toggleCodeFrame,
            children: /* @__PURE__ */ jsx41("div", {
              style: {
                display: "flex",
                transform: shouldShowCodeFrame ? undefined : "rotate(-90deg)"
              },
              children: /* @__PURE__ */ jsx41(CaretDown, {})
            })
          }) : null
        ]
      }),
      /* @__PURE__ */ jsx41("div", {
        children: s.originalScriptCode && s.originalScriptCode.length > 0 && shouldShowCodeFrame ? /* @__PURE__ */ jsx41(CodeFrame, {
          fontSize: fontSize2,
          horizontalMargin: horizontalSpacing,
          lineNumberWidth,
          source: s.originalScriptCode
        }) : null
      })
    ]
  });
};

// src/error-overlay/remotion-overlay/ErrorDisplay.tsx
import { jsx as jsx42, jsxs as jsxs24, Fragment as Fragment10 } from "react/jsx-runtime";
var stack = {
  marginTop: 17,
  overflowX: "hidden",
  marginBottom: 14
};
var rawStack = {
  backgroundColor: ERROR_CODE_FRAME_BACKGROUND,
  borderRadius: 6,
  boxSizing: "border-box",
  color: WHITE,
  fontFamily: "monospace",
  fontSize: 14,
  lineHeight: 1.7,
  marginBottom: 0,
  marginTop: 17,
  maxWidth: "100%",
  overflowX: "auto",
  padding: 12,
  whiteSpace: "pre"
};
var symbolicationFailureStyle = {
  color: WHITE_ALPHA_60,
  fontFamily: "SF Pro Text, sans-serif",
  fontSize: 14,
  lineHeight: 1.5,
  marginBottom: 14,
  marginTop: 24
};
var spacer2 = {
  width: 5,
  display: "inline-block"
};
var actionRow = {
  alignItems: "center",
  display: "flex",
  flexWrap: "wrap",
  marginLeft: 4,
  marginTop: -10
};
var codingAgentButton = {
  marginLeft: 1,
  marginRight: 5
};
var ErrorDisplay = ({
  display,
  keyboardShortcuts,
  onRetry,
  canHaveDismissButton,
  calculateMetadata,
  symbolicationFailure
}) => {
  const { previewServerState } = useContext13(StudioServerConnectionCtx);
  const { canOpenInEditor, defaultEditorId, defaultEditorName } = useEditorOpening(previewServerState.type === "connected");
  const stackFrames = useMemo18(() => {
    const withoutReactInternals = display.stackFrames.filter((frame2) => {
      const fileName = `/${frame2.originalFileName?.replaceAll("\\", "/") ?? ""}`;
      return ![
        "/node_modules/react/",
        "/node_modules/react-dom/",
        "/node_modules/scheduler/"
      ].some((packagePath) => fileName.includes(packagePath));
    });
    return withoutReactInternals.length > 0 ? withoutReactInternals : display.stackFrames.slice(0, 1);
  }, [display.stackFrames]);
  const highestLineNumber = Math.max(0, ...stackFrames.map((s) => s.originalScriptCode).flat(1).map((s) => s?.lineNumber ?? 0));
  const message = useMemo18(() => {
    const location2 = getLocationFromBuildError(display.error);
    if (!location2) {
      return display.error.message;
    }
    return location2.message.replace(/\\n/g, `
`).replace(/\\t/g, "  ").replace(/^error:/, "").trim();
  }, [display.error]);
  const lineNumberWidth = String(highestLineNumber).length;
  const helpLink = getHelpLink(message);
  const errorTextForCopy = useMemo18(() => {
    const header2 = `${display.error.name}: ${message}`;
    if (stackFrames.length > 0) {
      const stackLines = stackFrames.map((frame2) => `at ${frame2.originalFunctionName || "<anonymous>"} (${frame2.originalFileName || "unknown"}:${frame2.originalLineNumber || "?"}:${frame2.originalColumnNumber || "?"})`).join(`
`);
      return `${header2}
${stackLines}`;
    }
    return display.error.stack || header2;
  }, [display.error, message, stackFrames]);
  const fixWithAgentPrompt = `Fix this error in my Remotion project:

${errorTextForCopy}`;
  return /* @__PURE__ */ jsxs24("div", {
    children: [
      /* @__PURE__ */ jsx42(ErrorTitle, {
        symbolicating: false,
        name: display.error.name,
        message,
        canHaveDismissButton
      }),
      /* @__PURE__ */ jsxs24("div", {
        style: actionRow,
        children: [
          helpLink ? /* @__PURE__ */ jsxs24(Fragment10, {
            children: [
              /* @__PURE__ */ jsx42(HelpLink, {
                link: helpLink,
                canHaveKeyboardShortcuts: keyboardShortcuts
              }),
              /* @__PURE__ */ jsx42("div", {
                style: spacer2
              })
            ]
          }) : null,
          stackFrames.length > 0 && canOpenInEditor && defaultEditorId && defaultEditorName ? /* @__PURE__ */ jsxs24(Fragment10, {
            children: [
              /* @__PURE__ */ jsx42(OpenInEditor, {
                canHaveKeyboardShortcuts: keyboardShortcuts,
                editorId: defaultEditorId,
                editorName: defaultEditorName,
                size: "default",
                stack: stackFrames[0]
              }),
              /* @__PURE__ */ jsx42("div", {
                style: spacer2
              })
            ]
          }) : null,
          /* @__PURE__ */ jsx42(CodingAgentButton, {
            label: "Fix with",
            prompt: fixWithAgentPrompt,
            size: "default",
            style: codingAgentButton
          }),
          /* @__PURE__ */ jsx42(CopyStackTrace, {
            errorText: errorTextForCopy
          }),
          /* @__PURE__ */ jsx42("div", {
            style: spacer2
          }),
          /* @__PURE__ */ jsx42(SearchGithubIssues, {
            canHaveKeyboardShortcuts: keyboardShortcuts,
            message: display.error.message
          }),
          /* @__PURE__ */ jsx42("div", {
            style: spacer2
          }),
          /* @__PURE__ */ jsx42(AskOnDiscord, {
            canHaveKeyboardShortcuts: keyboardShortcuts
          }),
          onRetry ? /* @__PURE__ */ jsxs24(Fragment10, {
            children: [
              /* @__PURE__ */ jsx42("div", {
                style: spacer2
              }),
              /* @__PURE__ */ jsx42(RetryButton, {
                onClick: onRetry,
                label: calculateMetadata ? "Retry calculateMetadata()" : "Retry"
              })
            ]
          }) : null
        ]
      }),
      calculateMetadata ? /* @__PURE__ */ jsxs24(Fragment10, {
        children: [
          /* @__PURE__ */ jsx42("br", {}),
          /* @__PURE__ */ jsx42(Spacing, {
            y: 0.5
          }),
          /* @__PURE__ */ jsx42(CalculateMetadataErrorExplainer, {})
        ]
      }) : null,
      display.error instanceof MediaPlaybackError ? /* @__PURE__ */ jsxs24(Fragment10, {
        children: [
          /* @__PURE__ */ jsx42("br", {}),
          /* @__PURE__ */ jsx42(Spacing, {
            y: 0.5
          }),
          /* @__PURE__ */ jsx42(MediaPlaybackErrorExplainer, {
            src: display.error.src
          })
        ]
      }) : null,
      stackFrames.length > 0 ? /* @__PURE__ */ jsx42("div", {
        style: stack,
        className: HORIZONTAL_SCROLLBAR_CLASSNAME,
        children: stackFrames.map((s, i) => {
          return /* @__PURE__ */ jsx42(StackElement, {
            collapsible: true,
            isFirst: i === 0,
            s,
            fontSize: 14,
            headerAction: null,
            horizontalSpacing: 14,
            lineNumberWidth,
            defaultFunctionName: "(anonymous function)",
            editorId: canOpenInEditor ? defaultEditorId : null
          }, i);
        })
      }) : /* @__PURE__ */ jsxs24(Fragment10, {
        children: [
          /* @__PURE__ */ jsx42("pre", {
            "aria-label": "Unsymbolicated stack trace",
            className: HORIZONTAL_SCROLLBAR_CLASSNAME,
            style: rawStack,
            children: display.error.stack ?? "Check the Terminal and browser console for error messages."
          }),
          symbolicationFailure ? /* @__PURE__ */ jsx42("div", {
            style: symbolicationFailureStyle,
            children: symbolicationFailure
          }) : null
        ]
      })
    ]
  });
};

// src/error-overlay/remotion-overlay/log-studio-error.ts
var loggedErrors = new Set;
var maxLoggedErrors = 100;
var logStudioErrorData = (data) => {
  if (getBrowserStudioOperations() !== null) {
    return;
  }
  const key4 = JSON.stringify([data.name, data.message, data.stack]);
  if (loggedErrors.has(key4)) {
    return;
  }
  if (loggedErrors.size >= maxLoggedErrors) {
    loggedErrors.clear();
  }
  loggedErrors.add(key4);
  callApi("/api/log-studio-error", data).catch(() => {
    return;
  });
};
var logStudioError = (error) => {
  logStudioErrorData({
    name: typeof error.name === "string" ? error.name : null,
    message: typeof error.message === "string" ? error.message : "",
    stack: typeof error.stack === "string" ? error.stack : null,
    symbolicatedStackFrames: null
  });
};

// src/error-overlay/remotion-overlay/ErrorLoader.tsx
import { jsx as jsx43 } from "react/jsx-runtime";
var container8 = {
  width: "100%",
  maxWidth: 1000,
  paddingLeft: 14,
  paddingRight: 14,
  marginLeft: "auto",
  marginRight: "auto",
  fontFamily: "SF Pro Text, sans-serif",
  paddingTop: 14,
  boxSizing: "border-box"
};
var shouldLogError = (error) => {
  return !wasErrorLoggedByServer(error) && getLocationFromBuildError2(error) === null;
};
var shouldIncludeFrameInServerLog = (frame2) => {
  return !(frame2.originalFileName?.includes("node_modules") || frame2.originalFileName?.startsWith("webpack/") || frame2.originalFileName?.includes("/bundler/dist/fast-refresh/") || frame2.originalFileName?.includes("bundler/dist/fast-refresh/"));
};
var logSymbolicatedStudioError = (record) => {
  const name = typeof record.error.name === "string" ? record.error.name : null;
  const message = typeof record.error.message === "string" ? record.error.message : "";
  const filteredStackFrames = record.stackFrames.filter(shouldIncludeFrameInServerLog);
  const stackFrames = filteredStackFrames.length > 0 ? filteredStackFrames : record.stackFrames[0] ? [record.stackFrames[0]] : [];
  logStudioErrorData({
    name,
    message,
    stack: typeof record.error.stack === "string" ? record.error.stack : null,
    symbolicatedStackFrames: stackFrames.length > 0 ? stackFrames : null
  });
};
var ErrorLoader = ({
  error,
  keyboardShortcuts,
  onRetry,
  canHaveDismissButton,
  calculateMetadata
}) => {
  const [state, setState] = useState10({
    type: "loading"
  });
  useEffect14(() => {
    const symbolication = getErrorRecord(error);
    const unregister = registerCurrentError({ error, symbolication });
    symbolication.then((record) => {
      if (record) {
        if (shouldLogError(error)) {
          logSymbolicatedStudioError(record);
        }
        setState({
          type: "symbolicated",
          record
        });
      } else {
        if (shouldLogError(error)) {
          logStudioError(error);
        }
        setState({
          type: "no-record"
        });
      }
    }).catch((err) => {
      if (shouldLogError(error)) {
        logStudioError(error);
      }
      setState({
        err,
        type: "error"
      });
    });
    return unregister;
  }, [error]);
  if (state.type === "loading") {
    return /* @__PURE__ */ jsx43("div", {
      style: container8,
      children: /* @__PURE__ */ jsx43(ErrorTitle, {
        symbolicating: true,
        name: error.name,
        message: error.message,
        canHaveDismissButton
      })
    });
  }
  if (state.type === "error") {
    return /* @__PURE__ */ jsx43("div", {
      style: container8,
      children: /* @__PURE__ */ jsx43(ErrorDisplay, {
        keyboardShortcuts,
        display: { error, contextSize: 0, stackFrames: [] },
        onRetry,
        canHaveDismissButton,
        calculateMetadata,
        symbolicationFailure: `Could not symbolicate the stack trace: ${state.err.message}`
      })
    });
  }
  if (state.type === "no-record") {
    return /* @__PURE__ */ jsx43("div", {
      style: container8,
      children: /* @__PURE__ */ jsx43(ErrorDisplay, {
        keyboardShortcuts,
        display: { error, contextSize: 0, stackFrames: [] },
        onRetry,
        canHaveDismissButton,
        calculateMetadata,
        symbolicationFailure: "Could not symbolicate the stack trace."
      })
    });
  }
  return /* @__PURE__ */ jsx43("div", {
    style: container8,
    children: /* @__PURE__ */ jsx43(ErrorDisplay, {
      keyboardShortcuts,
      display: state.record,
      onRetry,
      canHaveDismissButton,
      calculateMetadata,
      symbolicationFailure: null
    })
  });
};

// src/components/Canvas.tsx
import {
  useCallback as useCallback35,
  useContext as useContext40,
  useEffect as useEffect29,
  useMemo as useMemo41,
  useRef as useRef20,
  useState as useState25
} from "react";
import { Internals as Internals39, watchStaticFile } from "remotion";

// src/helpers/get-effective-translation.ts
import { Internals as Internals14 } from "remotion";

// src/helpers/smooth-zoom.ts
var BASE = Math.E / 4;
var MIN_ZOOM = 0.05;
var MAX_ZOOM = 10;
function logN(val) {
  return Math.log(val) / Math.log(BASE);
}
var smoothenZoom = (input2) => {
  return BASE ** (input2 - 1);
};
var unsmoothenZoom = (input2) => {
  if (input2 < 0) {
    return MAX_ZOOM;
  }
  return Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, logN(input2) + 1));
};

// src/helpers/get-effective-translation.ts
var getEffectiveXTranslation = ({
  canvasSize,
  scale,
  compositionWidth,
  translation
}) => {
  const maxTranslation = Math.abs(canvasSize.width / 2 + scale * compositionWidth / 2 - MUST_BE_INSIDE_CANVAS);
  return Math.max(-maxTranslation, Math.min(translation.x, maxTranslation));
};
var MUST_BE_INSIDE_CANVAS = 50;
var getEffectiveYTranslation = ({
  canvasSize,
  scale,
  compositionHeight,
  translation
}) => {
  const maxTranslation = Math.abs(canvasSize.height / 2 + scale * compositionHeight / 2) - MUST_BE_INSIDE_CANVAS;
  return Math.max(-maxTranslation, Math.min(translation.y, maxTranslation));
};
var getEffectiveTranslation = ({
  canvasSize,
  scale,
  compositionHeight,
  compositionWidth,
  translation
}) => {
  return {
    x: getEffectiveXTranslation({
      canvasSize,
      compositionWidth,
      scale,
      translation
    }),
    y: getEffectiveYTranslation({
      canvasSize,
      compositionHeight,
      scale,
      translation
    })
  };
};
var getUnboundedCenterPointWhileScrolling = ({
  size: size2,
  clientX,
  clientY,
  compositionWidth,
  compositionHeight,
  scale,
  translation
}) => {
  const mouseLeft = clientX - size2.left;
  const mouseTop = clientY - size2.top;
  const contentLeftPoint = size2.width / 2 - compositionWidth * scale / 2 - translation.x;
  const contentTopPoint = size2.height / 2 - compositionHeight * scale / 2 - translation.y;
  return {
    centerX: (mouseLeft - contentLeftPoint) / scale,
    centerY: (mouseTop - contentTopPoint) / scale
  };
};
var getCenterPointWhileScrolling = (options) => {
  const { centerX, centerY } = getUnboundedCenterPointWhileScrolling(options);
  return {
    centerX: Math.min(options.compositionWidth, Math.max(0, centerX)),
    centerY: Math.min(options.compositionHeight, Math.max(0, centerY))
  };
};
var applyZoomAroundFocalPoint = ({
  addFitPadding,
  canvasSize,
  contentDimensions,
  previewSizeBefore,
  oldNumericSize,
  newNumericSize,
  clientX,
  clientY
}) => {
  const scale = addFitPadding ? calculateStudioScale({
    canvasSize,
    compositionHeight: contentDimensions.height,
    compositionWidth: contentDimensions.width,
    previewSize: previewSizeBefore.size
  }) : Internals14.calculateScale({
    canvasSize,
    compositionHeight: contentDimensions.height,
    compositionWidth: contentDimensions.width,
    previewSize: previewSizeBefore.size
  });
  const clampedNew = Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, newNumericSize));
  if (clampedNew === oldNumericSize) {
    return previewSizeBefore;
  }
  const { centerX, centerY } = getCenterPointWhileScrolling({
    size: canvasSize,
    clientX,
    clientY,
    compositionWidth: contentDimensions.width,
    compositionHeight: contentDimensions.height,
    scale,
    translation: previewSizeBefore.translation
  });
  const zoomDifference = clampedNew - oldNumericSize;
  const uvCoordinatesX = centerX / contentDimensions.width;
  const uvCoordinatesY = centerY / contentDimensions.height;
  const correctionLeft = -uvCoordinatesX * (zoomDifference * contentDimensions.width) + (1 - uvCoordinatesX) * zoomDifference * contentDimensions.width;
  const correctionTop = -uvCoordinatesY * (zoomDifference * contentDimensions.height) + (1 - uvCoordinatesY) * zoomDifference * contentDimensions.height;
  return {
    size: clampedNew,
    translation: getEffectiveTranslation({
      translation: {
        x: previewSizeBefore.translation.x - correctionLeft / 2,
        y: previewSizeBefore.translation.y - correctionTop / 2
      },
      canvasSize,
      compositionHeight: contentDimensions.height,
      compositionWidth: contentDimensions.width,
      scale
    })
  };
};

// src/components/selected-outline-snap.ts
var selectedOutlineSnapThresholdPx = 10;
var selectedOutlineSnapIndicatorColor = "#ff00ff";
var EPSILON = 0.000001;
var getSelectedOutlineSnapTargets = ({
  compositionHeight,
  compositionWidth,
  guides
}) => {
  return [
    { axis: "x", position: 0, type: "canvas-left" },
    {
      axis: "x",
      position: compositionWidth / 2,
      type: "canvas-horizontal-center"
    },
    { axis: "x", position: compositionWidth, type: "canvas-right" },
    { axis: "y", position: 0, type: "canvas-top" },
    {
      axis: "y",
      position: compositionHeight / 2,
      type: "canvas-vertical-center"
    },
    { axis: "y", position: compositionHeight, type: "canvas-bottom" },
    ...guides.flatMap((guide) => {
      if (!guide.show) {
        return [];
      }
      return [
        guide.orientation === "vertical" ? {
          axis: "x",
          position: guide.position,
          type: "guide-vertical"
        } : {
          axis: "y",
          position: guide.position,
          type: "guide-horizontal"
        }
      ];
    })
  ];
};
var getSelectedOutlineBounds = ({
  deltaX,
  deltaY,
  outlines,
  scale
}) => {
  if (outlines.length === 0) {
    return null;
  }
  const points = outlines.flatMap((outline) => outline.points);
  const left3 = Math.min(...points.map((point) => point.x)) / scale + deltaX;
  const right = Math.max(...points.map((point) => point.x)) / scale + deltaX;
  const top = Math.min(...points.map((point) => point.y)) / scale + deltaY;
  const bottom = Math.max(...points.map((point) => point.y)) / scale + deltaY;
  return {
    left: left3,
    right,
    top,
    bottom,
    centerX: left3 + (right - left3) / 2,
    centerY: top + (bottom - top) / 2
  };
};
var canSnapEdgeToTarget = (edge, target) => {
  if (target.type === "canvas-horizontal-center") {
    return edge === "center-x";
  }
  if (target.type === "canvas-vertical-center") {
    return edge === "center-y";
  }
  if (target.type === "canvas-left" || target.type === "canvas-right") {
    return edge === "left" || edge === "right";
  }
  if (target.type === "guide-vertical") {
    return edge === "left" || edge === "right" || edge === "center-x";
  }
  if (target.type === "canvas-top" || target.type === "canvas-bottom") {
    return edge === "top" || edge === "bottom";
  }
  return edge === "top" || edge === "bottom" || edge === "center-y";
};
var findBestSnapForAxis = ({
  edges,
  targets,
  threshold
}) => {
  let bestDistance = Infinity;
  let bestSnaps = [];
  for (const target of targets) {
    for (const edge of edges) {
      if (!canSnapEdgeToTarget(edge.edge, target)) {
        continue;
      }
      const distance = Math.abs(edge.position - target.position);
      if (distance > threshold || distance > bestDistance + EPSILON) {
        continue;
      }
      if (distance < bestDistance - EPSILON) {
        bestSnaps = [];
        bestDistance = distance;
      }
      bestSnaps.push({
        offset: target.position - edge.position,
        snapPoint: {
          distance,
          edge: edge.edge,
          target
        }
      });
    }
  }
  if (bestSnaps.length > 1) {
    const centerSnap = bestSnaps.find((snap) => snap.snapPoint.target.type === "canvas-horizontal-center" || snap.snapPoint.target.type === "canvas-vertical-center");
    if (centerSnap) {
      return centerSnap;
    }
  }
  return bestSnaps[0] ?? null;
};
var findSelectedOutlineSnap = ({
  allowX,
  allowY,
  deltaX,
  deltaY,
  outlines,
  scale,
  targets
}) => {
  const bounds = getSelectedOutlineBounds({ deltaX, deltaY, outlines, scale });
  if (bounds === null) {
    return {
      activeSnapPoints: [],
      snapOffsetX: null,
      snapOffsetY: null
    };
  }
  const threshold = selectedOutlineSnapThresholdPx / scale;
  const snapX = allowX ? findBestSnapForAxis({
    edges: [
      { edge: "left", position: bounds.left },
      { edge: "center-x", position: bounds.centerX },
      { edge: "right", position: bounds.right }
    ],
    targets: targets.filter((target) => target.axis === "x"),
    threshold
  }) : null;
  const snapY = allowY ? findBestSnapForAxis({
    edges: [
      { edge: "top", position: bounds.top },
      { edge: "center-y", position: bounds.centerY },
      { edge: "bottom", position: bounds.bottom }
    ],
    targets: targets.filter((target) => target.axis === "y"),
    threshold
  }) : null;
  return {
    activeSnapPoints: [
      snapX?.snapPoint ?? null,
      snapY?.snapPoint ?? null
    ].filter((snapPoint) => snapPoint !== null),
    snapOffsetX: snapX?.offset ?? null,
    snapOffsetY: snapY?.offset ?? null
  };
};

// src/components/composition-drop-preview.ts
var snapCompositionDropPosition = ({
  compositionDimensions,
  destinationDimensions,
  dropPosition,
  scale
}) => {
  const left3 = dropPosition.centerX - compositionDimensions.width / 2;
  const top = dropPosition.centerY - compositionDimensions.height / 2;
  const right = left3 + compositionDimensions.width;
  const bottom = top + compositionDimensions.height;
  const outline = {
    key: "composition-drop-preview",
    dimensions: compositionDimensions,
    uncroppedPoints: null,
    points: [
      { x: left3 * scale, y: top * scale },
      { x: right * scale, y: top * scale },
      { x: right * scale, y: bottom * scale },
      { x: left3 * scale, y: bottom * scale }
    ]
  };
  const snap = findSelectedOutlineSnap({
    allowX: true,
    allowY: true,
    deltaX: 0,
    deltaY: 0,
    outlines: [outline],
    scale,
    targets: getSelectedOutlineSnapTargets({
      compositionHeight: destinationDimensions.height,
      compositionWidth: destinationDimensions.width,
      guides: []
    })
  });
  return {
    centerX: dropPosition.centerX + (snap.snapOffsetX ?? 0),
    centerY: dropPosition.centerY + (snap.snapOffsetY ?? 0)
  };
};
var getCompositionDropPreviewBox = ({
  canvasSize,
  destinationDimensions,
  preview,
  previewSize
}) => {
  const scale = calculateStudioScale({
    canvasSize,
    compositionHeight: destinationDimensions.height,
    compositionWidth: destinationDimensions.width,
    previewSize: previewSize.size
  });
  const position = getCompositionPositionForDrop({
    compositionDimensions: preview.compositionDimensions,
    dropPosition: preview.dropPosition
  });
  const contentLeft = canvasSize.width / 2 - destinationDimensions.width * scale / 2 - previewSize.translation.x;
  const contentTop = canvasSize.height / 2 - destinationDimensions.height * scale / 2 - previewSize.translation.y;
  return {
    left: contentLeft + (position?.x ?? 0) * scale,
    top: contentTop + (position?.y ?? 0) * scale,
    width: preview.compositionDimensions.width * scale,
    height: preview.compositionDimensions.height * scale
  };
};

// src/components/EditorGuides/index.tsx
import { useContext as useContext16, useMemo as useMemo21 } from "react";
import { Internals as Internals16 } from "remotion";

// src/helpers/use-studio-canvas-dimensions.ts
import { useContext as useContext14, useMemo as useMemo19 } from "react";
import { Internals as Internals15 } from "remotion";
var useStudioCanvasDimensions = ({
  canvasSize,
  contentDimensions,
  assetMetadata
}) => {
  const { size: previewSize } = useContext14(Internals15.PreviewSizeContext);
  const { centerX, centerY, scale } = useMemo19(() => {
    if (contentDimensions === "none" || contentDimensions === null || assetMetadata && (assetMetadata.type === "not-found" || assetMetadata.type === "metadata-error") || !canvasSize) {
      return {
        centerX: previewSize.translation.x,
        centerY: previewSize.translation.y,
        scale: 1
      };
    }
    return calculateStudioCanvasTransformation({
      canvasSize,
      compositionHeight: contentDimensions.height,
      compositionWidth: contentDimensions.width,
      previewSize: previewSize.size
    });
  }, [
    canvasSize,
    contentDimensions,
    previewSize.size,
    previewSize.translation.y,
    previewSize.translation.x,
    assetMetadata
  ]);
  const canvasPosition = useMemo19(() => {
    return {
      left: centerX - previewSize.translation.x,
      top: centerY - previewSize.translation.y,
      width: contentDimensions === "none" || !contentDimensions ? canvasSize?.width || 0 : contentDimensions.width * scale,
      height: contentDimensions === "none" || !contentDimensions ? canvasSize?.height || 0 : contentDimensions.height * scale
    };
  }, [
    scale,
    centerX,
    previewSize.translation.x,
    previewSize.translation.y,
    centerY,
    canvasSize,
    contentDimensions
  ]);
  return {
    canvasPosition,
    scale
  };
};

// src/components/EditorGuides/Guide.tsx
import { memo, useCallback as useCallback23, useContext as useContext15, useMemo as useMemo20, useRef as useRef6 } from "react";
import { NoReactInternals as NoReactInternals3 } from "remotion/no-react";

// src/helpers/editor-guide-selection.ts
var GUIDE_CLICK_THRESHOLD_PX = 3;
var isGuidePointerUpAClick = ({
  pointerDownPosition,
  guideId,
  clientX,
  clientY
}) => {
  if (pointerDownPosition === null || pointerDownPosition.guideId !== guideId) {
    return false;
  }
  return Math.abs(clientX - pointerDownPosition.clientX) <= GUIDE_CLICK_THRESHOLD_PX && Math.abs(clientY - pointerDownPosition.clientY) <= GUIDE_CLICK_THRESHOLD_PX;
};
var getEditorGuideColor = ({
  selected,
  active
}) => {
  if (selected) {
    return BLUE;
  }
  return active ? SELECTED_GUIDE : UNSELECTED_GUIDE;
};
var findGuide = (guidesList, guideId) => {
  if (guideId === null) {
    return null;
  }
  return guidesList.find((guide) => guide.id === guideId && guide.show) ?? null;
};
var getRulerGuideHighlight = ({
  guidesList,
  selectedItems,
  hoveredGuideId,
  draggingGuideId
}) => {
  const selectedGuideId = selectedItems.length === 1 && selectedItems[0]?.type === "guide" ? selectedItems[0].guideId ?? null : null;
  const selectedGuide = findGuide(guidesList, selectedGuideId);
  if (selectedGuide) {
    return {
      guide: selectedGuide,
      color: BLUE
    };
  }
  const draggingGuide = findGuide(guidesList, draggingGuideId);
  if (draggingGuide) {
    return {
      guide: draggingGuide,
      color: SELECTED_GUIDE
    };
  }
  const hoveredGuide = findGuide(guidesList, hoveredGuideId);
  if (hoveredGuide) {
    return {
      guide: hoveredGuide,
      color: SELECTED_GUIDE
    };
  }
  return null;
};

// src/components/Timeline/should-clear-selection-on-pointer-down.ts
var PREVENT_CLEAR_SELECTION_ON_POINTER_DOWN_ATTR = "data-remotion-prevent-selection-clear";
var isSelectionClearBlocked = (target) => {
  if (target === null || target === undefined || typeof target !== "object" || !("closest" in target) || typeof target.closest !== "function") {
    return false;
  }
  return target.closest(`[${PREVENT_CLEAR_SELECTION_ON_POINTER_DOWN_ATTR}]`) !== null;
};
var shouldClearSelectionOnPointerDown = (event) => {
  if (event.button !== 0 || event.metaKey === true || event.ctrlKey === true) {
    return false;
  }
  return !isSelectionClearBlocked(event.target);
};

// src/components/EditorGuides/Guide.tsx
import { jsx as jsx44 } from "react/jsx-runtime";
var PADDING_FOR_EASY_DRAG = 4;
var GuideComp = ({ guide, canvasDimensions, scale }) => {
  const {
    shouldCreateGuideRef,
    shouldDeleteGuideRef,
    setGuidesList,
    setDraggingGuideId,
    setHoveredGuideId,
    hoveredGuideId,
    draggingGuideId,
    moveGuidePointerRef
  } = useContext15(EditorShowGuidesContext);
  const { clearSelection, onSelect, selected } = useTimelineGuideSelection(guide.id);
  const pointerDownPositionRef = useRef6(null);
  const hasMovedGuideRef = useRef6(false);
  const onPointerEnter = useCallback23(() => {
    setHoveredGuideId(() => guide.id);
  }, [guide.id, setHoveredGuideId]);
  const onPointerLeave = useCallback23(() => {
    setHoveredGuideId(() => null);
  }, [setHoveredGuideId]);
  const isVerticalGuide = guide.orientation === "vertical";
  const guideStyle = useMemo20(() => {
    const canvasPosition = isVerticalGuide ? canvasDimensions.left : canvasDimensions.top;
    const guidePosition = guide.position * scale + canvasPosition;
    return {
      position: "absolute",
      width: `${isVerticalGuide ? "1px" : "100%"}`,
      height: `${isVerticalGuide ? "100%" : "1px"}`,
      left: `${isVerticalGuide ? guidePosition - PADDING_FOR_EASY_DRAG : 0}px`,
      top: `${isVerticalGuide ? 0 : guidePosition - PADDING_FOR_EASY_DRAG}px`,
      cursor: `${isVerticalGuide ? "ew-resize" : "ns-resize"}`,
      padding: isVerticalGuide ? `0 ${PADDING_FOR_EASY_DRAG}px` : `${PADDING_FOR_EASY_DRAG}px 0`
    };
  }, [guide, scale, canvasDimensions, isVerticalGuide]);
  const guideContentStyle = useMemo20(() => {
    return {
      position: "relative",
      minWidth: `${isVerticalGuide ? "1px" : `calc(100% + ${RULER_WIDTH}px`}`,
      minHeight: `${isVerticalGuide ? `calc(100% + ${RULER_WIDTH}px` : "1px"}`,
      top: `${isVerticalGuide ? `-${RULER_WIDTH}px` : "0px"}`,
      left: `${isVerticalGuide ? "0px" : `-${RULER_WIDTH}px`}`,
      display: guide.show ? "block" : "none",
      backgroundColor: getEditorGuideColor({
        selected,
        active: hoveredGuideId === guide.id || draggingGuideId === guide.id
      })
    };
  }, [
    isVerticalGuide,
    guide.show,
    hoveredGuideId,
    guide.id,
    selected,
    draggingGuideId
  ]);
  const updateHasMovedGuide = useCallback23((clientX, clientY) => {
    const pointerDownPosition = pointerDownPositionRef.current;
    if (pointerDownPosition === null) {
      return;
    }
    if (!isGuidePointerUpAClick({
      pointerDownPosition,
      guideId: guide.id,
      clientX,
      clientY
    })) {
      hasMovedGuideRef.current = true;
    }
  }, [guide.id]);
  const finishGuideInteraction = useCallback23(() => {
    const shouldDeleteGuide = shouldDeleteGuideRef.current;
    setGuidesList((prevState) => {
      const newGuides = shouldDeleteGuide ? prevState.filter((candidate) => candidate.id !== guide.id) : prevState;
      persistGuidesList(newGuides);
      return newGuides;
    });
    if (shouldDeleteGuide && selected) {
      clearSelection();
    }
    shouldDeleteGuideRef.current = false;
    shouldCreateGuideRef.current = false;
    stopForcingSpecificCursor();
    setDraggingGuideId(() => null);
  }, [
    clearSelection,
    guide.id,
    selected,
    setDraggingGuideId,
    setGuidesList,
    shouldCreateGuideRef,
    shouldDeleteGuideRef
  ]);
  const onPointerDown = useCallback23((e) => {
    if (e.button !== 0) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    hasMovedGuideRef.current = false;
    pointerDownPositionRef.current = {
      guideId: guide.id,
      clientX: e.clientX,
      clientY: e.clientY
    };
    shouldCreateGuideRef.current = false;
    setDraggingGuideId(() => guide.id);
    startCapturedPointerSession({
      event: e.nativeEvent,
      captureTarget: e.currentTarget,
      onMove: (moveEvent) => {
        updateHasMovedGuide(moveEvent.clientX, moveEvent.clientY);
        moveGuidePointerRef.current?.(moveEvent);
      },
      onEnd: (reason, endEvent) => {
        const pointerDownPosition = pointerDownPositionRef.current;
        pointerDownPositionRef.current = null;
        const shouldDeleteGuide = shouldDeleteGuideRef.current;
        finishGuideInteraction();
        if (shouldDeleteGuide || !isPointerSessionRelease(reason, endEvent)) {
          return;
        }
        if (isGuidePointerUpAClick({
          pointerDownPosition,
          guideId: guide.id,
          clientX: endEvent.clientX,
          clientY: endEvent.clientY
        })) {
          onSelect();
        }
      }
    });
  }, [
    finishGuideInteraction,
    guide.id,
    moveGuidePointerRef,
    onSelect,
    setDraggingGuideId,
    shouldCreateGuideRef,
    shouldDeleteGuideRef,
    updateHasMovedGuide
  ]);
  const onClick = useCallback23((e) => {
    if (e.button !== 0) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
  }, []);
  const isActive = selected || hoveredGuideId === guide.id;
  const activeClassName = isActive ? "__remotion_editor_guide_selected" : null;
  const guideClassName = useMemo20(() => {
    return ["__remotion_editor_guide_content", activeClassName].filter(NoReactInternals3.truthy).join(" ");
  }, [activeClassName]);
  const getContextMenuItems = useCallback23(() => {
    return [
      {
        id: "1",
        keyHint: null,
        label: "Remove guide",
        leftItem: null,
        onClick: () => {
          setGuidesList((prevState) => {
            const newGuides = prevState.filter((candidate) => {
              return candidate.id !== guide.id;
            });
            persistGuidesList(newGuides);
            return newGuides;
          });
          if (selected) {
            clearSelection();
          }
        },
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "remove"
      }
    ];
  }, [clearSelection, guide.id, selected, setGuidesList]);
  return /* @__PURE__ */ jsx44(ContextMenu, {
    getItems: getContextMenuItems,
    children: /* @__PURE__ */ jsx44("div", {
      style: guideStyle,
      onPointerDown,
      onClick,
      className: "__remotion_editor_guide",
      ...{ [PREVENT_CLEAR_SELECTION_ON_POINTER_DOWN_ATTR]: "true" },
      onPointerEnter,
      onPointerLeave,
      children: /* @__PURE__ */ jsx44("div", {
        style: guideContentStyle,
        className: guideClassName
      })
    })
  });
};
var Guide_default = memo(GuideComp);

// src/components/EditorGuides/index.tsx
import { jsx as jsx45, Fragment as Fragment11 } from "react/jsx-runtime";
var EditorGuides = ({ canvasSize, contentDimensions, assetMetadata }) => {
  const { canvasPosition: canvasDimensions, scale } = useStudioCanvasDimensions({
    canvasSize,
    contentDimensions,
    assetMetadata
  });
  const { canvasContent } = useContext16(Internals16.CompositionManager);
  if (canvasContent === null || canvasContent.type !== "composition") {
    throw new Error("Expected to be in a composition");
  }
  const { guidesList } = useContext16(EditorShowGuidesContext);
  const guidesForThisComposition = useMemo21(() => {
    return guidesList.filter((guide) => {
      return guide.compositionId === canvasContent.compositionId;
    });
  }, [canvasContent.compositionId, guidesList]);
  return /* @__PURE__ */ jsx45(Fragment11, {
    children: guidesForThisComposition.map((guide) => {
      return /* @__PURE__ */ jsx45(Guide_default, {
        guide,
        canvasDimensions,
        scale
      }, guide.id);
    })
  });
};
var EditorGuides_default = EditorGuides;

// src/components/EditorRuler/index.tsx
import {
  useCallback as useCallback25,
  useContext as useContext18,
  useLayoutEffect as useLayoutEffect2,
  useMemo as useMemo23,
  useRef as useRef8
} from "react";

// src/helpers/editor-ruler.ts
var drawLabel = ({
  orientation,
  context,
  label: label3,
  originDistance,
  color
}) => {
  context.fillStyle = color;
  if (orientation === "horizontal") {
    context.fillText(label3, originDistance + 4, 16);
  } else {
    context.rotate(-Math.PI / 2);
    context.fillText(label3, -originDistance + 4, 16);
    context.rotate(Math.PI / 2);
  }
};
var drawGradient = ({
  orientation,
  context,
  originDistance,
  canvasHeight,
  canvasWidth
}) => {
  const size2 = 250;
  const startX = orientation === "horizontal" ? originDistance - size2 / 2 : 0;
  const startY = orientation === "horizontal" ? 0 : originDistance - size2 / 2;
  const endX = orientation === "horizontal" ? originDistance + size2 / 2 : canvasWidth;
  const endY = orientation === "horizontal" ? canvasHeight : originDistance + size2 / 2;
  const grd = context.createLinearGradient(startX, startY, endX, endY);
  grd.addColorStop(0, BACKGROUND__TRANSPARENT);
  grd.addColorStop(0.25, BACKGROUND);
  grd.addColorStop(0.75, BACKGROUND);
  grd.addColorStop(1, BACKGROUND__TRANSPARENT);
  context.fillStyle = grd;
  context.fillRect(startX, startY, endX - startX, endY - startY);
};
var drawGuide = ({
  guideHighlight,
  scale,
  startMarking,
  context,
  canvasHeight,
  canvasWidth,
  orientation,
  originOffset
}) => {
  const { guide, color } = guideHighlight;
  const originDistance = rulerValueToPosition({
    value: guide.position,
    startMarking,
    scale
  }) + originOffset - startMarking * scale + 0.5;
  drawGradient({
    canvasHeight,
    context,
    orientation,
    originDistance,
    canvasWidth
  });
  context.strokeStyle = color;
  context.lineWidth = 1;
  context.beginPath();
  if (orientation === "horizontal" && guide.orientation === "horizontal") {
    return;
  }
  if (orientation === "vertical" && guide.orientation === "vertical") {
    return;
  }
  if (orientation === "vertical" && guide.orientation === "horizontal") {
    context.moveTo(0, originDistance);
    context.lineTo(canvasWidth, originDistance);
    drawLabel({
      context,
      label: guide.position.toString(),
      originDistance,
      orientation,
      color
    });
  } else if (orientation === "horizontal" && guide.orientation === "vertical") {
    context.moveTo(originDistance, 0);
    context.lineTo(originDistance, canvasHeight);
    drawLabel({
      context,
      label: guide.position.toString(),
      originDistance,
      orientation,
      color
    });
  }
  context.stroke();
};
var drawMarkingOnRulerCanvas = ({
  scale,
  points,
  startMarking,
  originOffset,
  markingGaps,
  orientation,
  rulerCanvasRef,
  guideHighlight,
  canvasHeight,
  canvasWidth
}) => {
  const canvas = rulerCanvasRef.current;
  if (!canvas)
    return;
  const context = canvas.getContext("2d");
  if (!context)
    return;
  canvas.width = canvasWidth;
  canvas.height = canvasHeight;
  context.scale(window.devicePixelRatio, window.devicePixelRatio);
  context.clearRect(0, 0, canvasWidth, canvasHeight);
  context.strokeStyle = WHITE_ALPHA_15;
  context.lineWidth = 1;
  context.beginPath();
  points.forEach((point) => {
    context.strokeStyle = WHITE_ALPHA_15;
    context.lineWidth = 1;
    const originDistance = point.position + originOffset - startMarking * scale;
    context.beginPath();
    if (orientation === "horizontal") {
      context.moveTo(originDistance, 0);
      context.lineTo(originDistance, canvasHeight);
    } else {
      context.moveTo(0, originDistance);
      context.lineTo(canvasWidth, originDistance);
    }
    for (let i = 1;i < 5; i++) {
      const markingOffsetXY = i * markingGaps * scale;
      if (orientation === "horizontal") {
        context.moveTo(originDistance + markingOffsetXY / 5, 0);
        context.lineTo(originDistance + markingOffsetXY / 5, 4);
      } else {
        context.moveTo(0, originDistance + markingOffsetXY / 5);
        context.lineTo(4, originDistance + markingOffsetXY / 5);
      }
    }
    context.stroke();
    context.font = "10px Arial, Helvetica, sans-serif";
    context.textAlign = "left";
    context.fillStyle = LIGHT_TEXT;
    drawLabel({
      orientation,
      context,
      label: point.value.toString(),
      originDistance,
      color: LIGHT_TEXT
    });
  });
  if (guideHighlight && orientation !== guideHighlight.guide.orientation) {
    drawGuide({
      canvasHeight,
      canvasWidth,
      context,
      guideHighlight,
      originOffset,
      scale,
      orientation,
      startMarking
    });
  }
};
var getRulerPoints = ({
  rulerScaleRange,
  rulerMarkingGaps,
  scale
}) => {
  const points = [];
  const startPoint = Math.ceil(rulerScaleRange.start / rulerMarkingGaps);
  const endPoint = Math.floor(rulerScaleRange.end / rulerMarkingGaps);
  const startMarking = startPoint * rulerMarkingGaps;
  for (let i = startPoint;i <= endPoint; i++) {
    points.push({
      value: i * rulerMarkingGaps,
      position: rulerValueToPosition({
        scale,
        startMarking,
        value: i * rulerMarkingGaps
      })
    });
  }
  return {
    points,
    startMarking
  };
};
var rulerValueToPosition = ({
  value,
  startMarking,
  scale
}) => {
  return (value + startMarking) * scale;
};
var getRulerScaleRange = ({
  canvasLength,
  scale,
  canvasSize
}) => {
  const scaleRangeBeyondCanvas = (canvasSize.width || MINIMUM_VISIBLE_CANVAS_SIZE - MINIMUM_VISIBLE_CANVAS_SIZE) / scale;
  return {
    start: -scaleRangeBeyondCanvas,
    end: scaleRangeBeyondCanvas + canvasLength
  };
};

// src/components/EditorRuler/Ruler.tsx
import {
  useCallback as useCallback24,
  useContext as useContext17,
  useEffect as useEffect15,
  useMemo as useMemo22,
  useRef as useRef7
} from "react";
import { Internals as Internals17 } from "remotion";
import { jsx as jsx46 } from "react/jsx-runtime";
var makeGuideId = () => {
  return Math.random().toString(36).substring(7);
};
var Ruler = ({
  scale,
  points,
  originOffset,
  startMarking,
  size: size2,
  markingGaps,
  orientation,
  canCreateGuides,
  onPointerSessionStart
}) => {
  const rulerCanvasRef = useRef7(null);
  const isVerticalRuler = orientation === "vertical";
  const {
    shouldCreateGuideRef,
    setGuidesList,
    draggingGuideId,
    hoveredGuideId,
    setDraggingGuideId,
    guidesList,
    setEditorShowGuides
  } = useContext17(EditorShowGuidesContext);
  const { selectedItems } = useTimelineSelection();
  const unsafeVideoConfig = Internals17.useUnsafeVideoConfig();
  if (!unsafeVideoConfig) {
    throw new Error("Video config not set");
  }
  const guideCursor = isVerticalRuler ? "ew-resize" : "ns-resize";
  const cursor = canCreateGuides ? guideCursor : "default";
  const guideHighlight = useMemo22(() => getRulerGuideHighlight({
    guidesList,
    selectedItems,
    hoveredGuideId,
    draggingGuideId
  }), [draggingGuideId, guidesList, hoveredGuideId, selectedItems]);
  const { width: rulerWidth, height: rulerHeight } = getRulerCanvasSize({
    orientation,
    size: size2
  });
  useEffect15(() => {
    drawMarkingOnRulerCanvas({
      scale,
      points,
      startMarking,
      originOffset,
      markingGaps,
      orientation,
      rulerCanvasRef,
      guideHighlight,
      canvasHeight: rulerHeight * window.devicePixelRatio,
      canvasWidth: rulerWidth * window.devicePixelRatio
    });
  }, [
    scale,
    points,
    startMarking,
    originOffset,
    markingGaps,
    orientation,
    guideHighlight,
    size2,
    rulerHeight,
    rulerWidth
  ]);
  const rulerStyle = useMemo22(() => ({
    position: "absolute",
    background: BACKGROUND,
    width: rulerWidth,
    height: rulerHeight,
    left: isVerticalRuler ? 0 : "unset",
    top: isVerticalRuler ? "unset" : 0,
    boxShadow: SHADOW_BLACK,
    cursor
  }), [rulerWidth, rulerHeight, cursor, isVerticalRuler]);
  const onPointerDown = useCallback24((e) => {
    if (!canCreateGuides || e.button !== 0) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    shouldCreateGuideRef.current = true;
    forceSpecificCursor(guideCursor);
    const guideId = makeGuideId();
    setEditorShowGuides(() => true);
    setDraggingGuideId(() => guideId);
    setGuidesList((prevState) => {
      return [
        ...prevState,
        {
          orientation,
          position: -originOffset,
          show: false,
          id: guideId,
          compositionId: unsafeVideoConfig.id
        }
      ];
    });
    onPointerSessionStart(e, guideId);
  }, [
    shouldCreateGuideRef,
    setEditorShowGuides,
    setDraggingGuideId,
    setGuidesList,
    orientation,
    originOffset,
    unsafeVideoConfig.id,
    guideCursor,
    onPointerSessionStart,
    canCreateGuides
  ]);
  return /* @__PURE__ */ jsx46("canvas", {
    ref: rulerCanvasRef,
    width: rulerWidth * window.devicePixelRatio,
    height: rulerHeight * window.devicePixelRatio,
    style: rulerStyle,
    "aria-label": `${isVerticalRuler ? "Vertical" : "Horizontal"} ruler`,
    "aria-readonly": !canCreateGuides,
    ...{ [PREVENT_CLEAR_SELECTION_ON_POINTER_DOWN_ATTR]: "true" },
    onPointerDown
  });
};
var Ruler_default = Ruler;

// src/components/EditorRuler/index.tsx
import { jsx as jsx47, jsxs as jsxs25, Fragment as Fragment12 } from "react/jsx-runtime";
var originBlockStyles = {
  position: "absolute",
  top: 0,
  left: 0,
  boxShadow: `inset -1px -1px 0 ${WHITE_ALPHA_15}`,
  width: `${RULER_WIDTH}px`,
  height: `${RULER_WIDTH}px`,
  background: BACKGROUND
};
var EditorRulers = ({
  contentDimensions,
  canvasSize,
  assetMetadata,
  containerRef,
  canCreateGuides
}) => {
  const { scale, canvasPosition } = useStudioCanvasDimensions({
    canvasSize,
    contentDimensions,
    assetMetadata
  });
  const {
    shouldCreateGuideRef,
    shouldDeleteGuideRef,
    setGuidesList,
    draggingGuideId,
    moveGuidePointerRef,
    setDraggingGuideId
  } = useContext18(EditorShowGuidesContext);
  const { clearSelection, selectedItems } = useTimelineSelection();
  const rulerMarkingGaps = useMemo23(() => {
    const minimumGap = MINIMUM_RULER_MARKING_GAP_PX;
    const predefinedGap = PREDEFINED_RULER_SCALE_GAPS.find((gap) => gap * scale > minimumGap);
    return predefinedGap || MAXIMUM_PREDEFINED_RULER_SCALE_GAP;
  }, [scale]);
  const horizontalRulerScaleRange = useMemo23(() => getRulerScaleRange({
    canvasLength: canvasPosition.width,
    scale,
    canvasSize
  }), [canvasPosition.width, canvasSize, scale]);
  const verticalRulerScaleRange = useMemo23(() => getRulerScaleRange({
    canvasLength: canvasPosition.height,
    scale,
    canvasSize
  }), [canvasPosition.height, canvasSize, scale]);
  const {
    points: horizontalRulerPoints,
    startMarking: horizontalRulerStartMarking
  } = useMemo23(() => getRulerPoints({
    rulerScaleRange: horizontalRulerScaleRange,
    rulerMarkingGaps,
    scale
  }), [horizontalRulerScaleRange, rulerMarkingGaps, scale]);
  const { points: verticalRulerPoints, startMarking: verticalRulerStartMarking } = useMemo23(() => getRulerPoints({
    rulerScaleRange: verticalRulerScaleRange,
    rulerMarkingGaps,
    scale
  }), [verticalRulerScaleRange, rulerMarkingGaps, scale]);
  const requestAnimationFrameRef = useRef8(null);
  const onMouseMove = useCallback25((e, guideId) => {
    if (requestAnimationFrameRef.current) {
      cancelAnimationFrame(requestAnimationFrameRef.current);
    }
    requestAnimationFrameRef.current = requestAnimationFrame(() => {
      const { clientX: mouseX, clientY: mouseY } = e;
      const {
        left: containerLeft = 0,
        top: containerTop = 0,
        right: containerRight = 0,
        bottom: containerBottom = 0
      } = containerRef.current?.getBoundingClientRect() || {};
      if (mouseX < containerLeft || mouseX > containerRight || mouseY < containerTop || mouseY > containerBottom) {
        if (!shouldDeleteGuideRef.current) {
          shouldDeleteGuideRef.current = true;
        }
        setGuidesList((prevState) => {
          const newGuides = prevState.map((guide) => {
            if (guide.id !== guideId) {
              return guide;
            }
            const desiredCursor = guide.orientation === "vertical" ? "ew-resize" : "ns-resize";
            forceSpecificCursor(desiredCursor);
            return {
              ...guide,
              show: false
            };
          });
          persistGuidesList(newGuides);
          return newGuides;
        });
      } else {
        if (shouldDeleteGuideRef.current) {
          shouldDeleteGuideRef.current = false;
        }
        setGuidesList((prevState) => {
          return prevState.map((guide) => {
            if (guide.id !== guideId) {
              return guide;
            }
            const position = guide.orientation === "vertical" ? (mouseX - containerLeft) / scale - canvasPosition.left / scale : (mouseY - containerTop) / scale - canvasPosition.top / scale;
            const desiredCursor = guide.orientation === "vertical" ? "ew-resize" : "ns-resize";
            forceSpecificCursor(desiredCursor);
            return {
              ...guide,
              position: Math.floor(position / 1),
              show: true
            };
          });
        });
      }
    });
  }, [
    containerRef,
    shouldDeleteGuideRef,
    setGuidesList,
    scale,
    canvasPosition.left,
    canvasPosition.top
  ]);
  const finishGuideInteraction = useCallback25((guideId) => {
    if (requestAnimationFrameRef.current) {
      cancelAnimationFrame(requestAnimationFrameRef.current);
      requestAnimationFrameRef.current = null;
    }
    const shouldDeleteGuide = shouldDeleteGuideRef.current;
    setGuidesList((prevState) => {
      const newGuides = prevState.filter((selected) => {
        if (!shouldDeleteGuide) {
          return true;
        }
        return selected.id !== guideId;
      });
      persistGuidesList(newGuides);
      return newGuides;
    });
    const deletedGuideWasSelected = selectedItems.some((item) => item.type === "guide" && item.guideId === guideId);
    if (shouldDeleteGuide && deletedGuideWasSelected) {
      clearSelection();
    }
    shouldDeleteGuideRef.current = false;
    stopForcingSpecificCursor();
    shouldCreateGuideRef.current = false;
    setDraggingGuideId(() => null);
  }, [
    clearSelection,
    shouldCreateGuideRef,
    shouldDeleteGuideRef,
    setDraggingGuideId,
    setGuidesList,
    selectedItems
  ]);
  useLayoutEffect2(() => {
    moveGuidePointerRef.current = (event) => {
      onMouseMove(event, draggingGuideId);
    };
    return () => {
      moveGuidePointerRef.current = null;
      if (requestAnimationFrameRef.current) {
        cancelAnimationFrame(requestAnimationFrameRef.current);
      }
    };
  }, [draggingGuideId, moveGuidePointerRef, onMouseMove]);
  const onPointerSessionStart = useCallback25((event, guideId) => {
    startCapturedPointerSession({
      event: event.nativeEvent,
      captureTarget: event.currentTarget,
      onMove: (moveEvent) => onMouseMove(moveEvent, guideId),
      onEnd: () => finishGuideInteraction(guideId)
    });
  }, [finishGuideInteraction, onMouseMove]);
  return /* @__PURE__ */ jsxs25(Fragment12, {
    children: [
      /* @__PURE__ */ jsx47(Ruler_default, {
        canCreateGuides,
        orientation: "horizontal",
        scale,
        points: horizontalRulerPoints,
        startMarking: horizontalRulerStartMarking,
        markingGaps: rulerMarkingGaps,
        originOffset: canvasPosition.left,
        size: canvasSize,
        onPointerSessionStart
      }),
      /* @__PURE__ */ jsx47(Ruler_default, {
        canCreateGuides,
        orientation: "vertical",
        scale,
        points: verticalRulerPoints,
        startMarking: verticalRulerStartMarking,
        markingGaps: rulerMarkingGaps,
        originOffset: canvasPosition.top,
        size: canvasSize,
        onPointerSessionStart
      }),
      /* @__PURE__ */ jsx47("div", {
        style: originBlockStyles
      })
    ]
  });
};

// src/components/EditorRuler/use-is-ruler-visible.ts
import { useContext as useContext19 } from "react";
import { Internals as Internals18 } from "remotion";
var useIsRulerVisible = () => {
  const { canvasContent, currentAssetMetadata } = useContext19(Internals18.CompositionManager);
  const { editorShowRulers } = useContext19(EditorShowRulersContext);
  return editorShowRulers && canvasContent !== null && (canvasContent.type === "composition" || canvasContent.type === "asset" && getPreviewFileType(canvasContent.asset) === "video" && currentAssetMetadata?.asset === canvasContent.asset);
};

// src/components/effect-drag-and-drop.ts
import {
  getRequiredPackageForEffectImportPath
} from "@remotion/studio-shared";
var hasEffectDragType = (dataTransfer) => {
  return StudioProtocolInternals.getDragPreviewMetadata(dataTransfer.types)?.type === "effect";
};
var hasExplicitEffectDragType = (dataTransfer) => {
  return hasEffectDragType(dataTransfer);
};
var getEffectDragData = (dataTransfer) => {
  const parsed = StudioProtocolInternals.parseDragData(dataTransfer);
  return parsed?.type === "effect" ? parsed.data : null;
};
var addEffectFromDragData = ({
  clientId,
  dragData,
  fileName,
  nodePath
}) => {
  return addEffectToSequence({
    clientId,
    effect: dragData.effect,
    fileName,
    nodePath
  });
};
var addEffectToSequence = async ({
  clientId,
  effect,
  fileName,
  nodePath
}) => {
  try {
    const requiredPackage = getRequiredPackageForEffectImportPath(effect.importPath);
    if (getBrowserStudioEffectOperations() === null) {
      await installRequiredPackages(requiredPackage ? [{ name: requiredPackage, version: null }] : []);
    }
    const result = await addEffect({
      fileName,
      sequenceNodePath: nodePath,
      effectName: effect.name,
      effectImportPath: effect.importPath,
      effectConfig: effect.config,
      clientId
    });
    if (!result.success) {
      showNotification(result.reason, 4000);
    }
  } catch (err) {
    showNotification(err.message, 4000);
  }
};

// src/components/element-drag-and-drop.ts
var hasElementDragType = (dataTransfer) => {
  return StudioProtocolInternals.getDragPreviewMetadata(dataTransfer?.types ?? [])?.type === "element";
};
var getElementDragData = (dataTransfer) => {
  if (!dataTransfer) {
    return null;
  }
  const parsed = StudioProtocolInternals.parseDragData(dataTransfer);
  return parsed?.type === "element" ? parsed.data : null;
};

// src/components/element-install-request.ts
var listeners = new Set;
var enqueueElementInstallRequest = (request) => {
  for (const listener of listeners) {
    listener(request);
  }
};
var subscribeToElementInstallRequests = (listener) => {
  listeners.add(listener);
  return () => {
    listeners.delete(listener);
  };
};

// src/components/handle-drop.ts
var handleDrop = async ({
  chooseSvgImportMode,
  compositionFile,
  compositionId,
  destinationDimensions,
  dropPosition,
  event,
  fps,
  from,
  localFiles,
  preferCompositionStart
}) => {
  if (localFiles !== null || isFileDragEvent(event)) {
    const files = localFiles ?? Array.from(event.dataTransfer?.files ?? []);
    if (files.length === 0) {
      return;
    }
    const svgImportMode = hasSvgFile(files) ? await chooseSvgImportMode() : "image";
    if (svgImportMode === null) {
      return;
    }
    await importAssets({
      compositionFile,
      compositionId,
      destinationDimensions,
      dropPosition,
      files,
      fps,
      from,
      preferCompositionStart,
      svgImportMode
    });
    return;
  }
  if (isAssetDragEvent(event)) {
    const assetPath = getAssetDragPath(event);
    if (assetPath === null) {
      return;
    }
    await insertExistingAssets({
      assetPaths: [assetPath],
      compositionFile,
      compositionId,
      destinationDimensions,
      dropPosition,
      fps,
      from,
      preferCompositionStart
    });
    return;
  }
  if (isSfxDragEvent(event)) {
    const sfxUrl = getSfxDragUrl(event);
    if (sfxUrl === null) {
      return;
    }
    await insertRemoteAudio({
      compositionFile,
      compositionId,
      fps,
      from,
      preferCompositionStart,
      url: sfxUrl
    });
    return;
  }
  if (isCompositionDragEvent(event)) {
    const composition = getCompositionDragData(event);
    if (composition === null) {
      return;
    }
    await insertComposition({
      composition,
      compositionFile,
      compositionId,
      dropPosition,
      from,
      preferCompositionStart
    });
    return;
  }
  const element = getElementDragData(event.dataTransfer);
  if (element !== null) {
    enqueueElementInstallRequest({
      id: crypto.randomUUID(),
      clientId: "drag-and-drop",
      createdAt: Date.now(),
      compositionFile,
      compositionId,
      element: {
        ...element.element,
        durationInFrames: element.element.durationInFrames ?? null,
        initialProps: element.element.initialProps ?? null,
        installationMode: element.element.installationMode ?? null
      },
      from: getFromForDrop({
        durationInFrames: element.element.durationInFrames,
        from,
        preferCompositionStart
      }),
      position: getElementPositionForDrop({
        dimensions: element.element.dimensions,
        dropPosition
      }),
      source: { type: "drag-and-drop" }
    });
    return;
  }
  const component = getComponentDragData(event);
  if (component !== null) {
    await insertComponent({
      component: component.component,
      compositionFile,
      compositionId,
      dropPosition,
      from,
      preferCompositionStart
    });
    return;
  }
  const remoteAssetUrl = getRemoteAssetUrlFromDataTransfer(event.dataTransfer);
  if (remoteAssetUrl === null) {
    return;
  }
  await importRemoteAsset({
    compositionFile,
    compositionId,
    destinationDimensions,
    dropPosition,
    fps,
    from,
    preferCompositionStart,
    url: remoteAssetUrl
  });
};

// ../media/dist/esm/index.mjs
import { useMemo as useMemo32, useState as useState32 } from "react";
import {
  Freeze,
  Internals as Internals19,
  Interactive,
  Sequence,
  useRemotionEnvironment as useRemotionEnvironment2,
  useVideoConfig as useVideoConfig2
} from "remotion";
import { Internals as Internals20 } from "remotion";
import { useContext as useContext22, useEffect as useEffect16, useMemo as useMemo24, useRef as useRef9, useState as useState11 } from "react";
import {
  Internals as Internals92,
  Audio as RemotionAudio,
  useBufferState,
  useCurrentFrame,
  useVideoConfig
} from "remotion";
import { Internals as Internals22 } from "remotion";
import { Internals as Internals72 } from "remotion";
import {
  AudioBufferSink,
  InputDisposedError
} from "mediabunny";
import { Internals as Internals32 } from "remotion";
import { ALL_FORMATS as ALL_FORMATS2, Input as Input2, UrlSource } from "mediabunny";
import { Internals as Internals52 } from "remotion";
import { cancelRender, Internals as Internals42 } from "remotion";
import { CanvasSink } from "mediabunny";
import { Internals as Internals62 } from "remotion";
import { useContext as useContext20, useLayoutEffect as useLayoutEffect3 } from "react";
import { Internals as Internals82 } from "remotion";
import { jsx as jsx48 } from "react/jsx-runtime";
import { useContext as useContext32, useLayoutEffect as useLayoutEffect22, useMemo as useMemo25, useState as useState22 } from "react";
import {
  cancelRender as cancelRender2,
  Html5Audio,
  Internals as Internals182,
  random,
  useCurrentFrame as useCurrentFrame2,
  useDelayRender,
  useRemotionEnvironment
} from "remotion";
import React210 from "react";
import { Internals as Internals162 } from "remotion";
import { Internals as Internals112 } from "remotion";
import { Internals as Internals102 } from "remotion";
import { Internals as Internals132 } from "remotion";
import {
  ALL_FORMATS as ALL_FORMATS22,
  AudioSampleSink,
  EncodedPacketSink,
  Input as Input22,
  MATROSKA,
  UrlSource as UrlSource2,
  VideoSampleSink,
  WEBM
} from "mediabunny";
import { Internals as Internals122 } from "remotion";
import { Internals as Internals152 } from "remotion";
import { Internals as Internals142 } from "remotion";
import { Internals as Internals172 } from "remotion";
import { jsx as jsx210 } from "react/jsx-runtime";
import { jsx as jsx310 } from "react/jsx-runtime";
import React62, { useMemo as useMemo62, useState as useState62 } from "react";
import {
  Freeze as Freeze2,
  Internals as Internals24,
  Interactive as Interactive2,
  Sequence as Sequence2,
  useRemotionEnvironment as useRemotionEnvironment4,
  useVideoConfig as useVideoConfig5
} from "remotion";
import { Internals as Internals202 } from "remotion";
import {
  useCallback as useCallback26,
  useContext as useContext42,
  useEffect as useEffect22,
  useLayoutEffect as useLayoutEffect32,
  useMemo as useMemo42,
  useRef as useRef22,
  useState as useState42
} from "react";
import {
  Html5Video,
  Internals as Internals222,
  useBufferState as useBufferState2,
  useCurrentFrame as useCurrentFrame3,
  useVideoConfig as useVideoConfig3
} from "remotion";
import { Internals as Internals21 } from "remotion";
import { jsx as jsx49 } from "react/jsx-runtime";
import {
  useContext as useContext52,
  useLayoutEffect as useLayoutEffect4,
  useMemo as useMemo52,
  useRef as useRef32,
  useState as useState52
} from "react";
import {
  Internals as Internals23,
  Loop,
  random as random2,
  useCurrentFrame as useCurrentFrame4,
  useDelayRender as useDelayRender2,
  useRemotionEnvironment as useRemotionEnvironment3,
  useVideoConfig as useVideoConfig4
} from "remotion";
import { jsx as jsx52 } from "react/jsx-runtime";
import { jsx as jsx62 } from "react/jsx-runtime";
var __dispose = Symbol.dispose || /* @__PURE__ */ Symbol.for("Symbol.dispose");
var __asyncDispose = Symbol.asyncDispose || /* @__PURE__ */ Symbol.for("Symbol.asyncDispose");
var __using = (stack2, value, async) => {
  if (value != null) {
    if (typeof value !== "object" && typeof value !== "function")
      throw TypeError('Object expected to be assigned to "using" declaration');
    var dispose;
    if (async)
      dispose = value[__asyncDispose];
    if (dispose === undefined)
      dispose = value[__dispose];
    if (typeof dispose !== "function")
      throw TypeError("Object not disposable");
    stack2.push([async, dispose, value]);
  } else if (async) {
    stack2.push([async]);
  }
  return value;
};
var __callDispose = (stack2, error, hasError) => {
  var E = typeof SuppressedError === "function" ? SuppressedError : function(e, s, m, _) {
    return _ = Error(m), _.name = "SuppressedError", _.error = e, _.suppressed = s, _;
  }, fail = (e) => error = hasError ? new E(e, error, "An error was suppressed during disposal") : (hasError = true, e), next = (it) => {
    while (it = stack2.pop()) {
      try {
        var result = it[1] && it[1].call(it[2]);
        if (it[0])
          return Promise.resolve(result).then(next, (e) => (fail(e), next()));
      } catch (e) {
        fail(e);
      }
    }
    if (hasError)
      throw error;
  };
  return next();
};
var getLoopDisplay = ({
  loop,
  mediaDurationInSeconds,
  playbackRate,
  trimAfter,
  trimBefore,
  sequenceDurationInFrames,
  compFps
}) => {
  if (!loop || !mediaDurationInSeconds) {
    return;
  }
  const durationInFrames = Internals20.calculateMediaDuration({
    mediaDurationInFrames: mediaDurationInSeconds * compFps,
    playbackRate,
    trimAfter,
    trimBefore
  });
  const maxTimes = sequenceDurationInFrames / durationInFrames;
  return {
    numberOfTimes: maxTimes,
    startOffset: 0,
    durationInFrames
  };
};
var validateToneFrequency = ({
  toneFrequency,
  component
}) => {
  if (toneFrequency === undefined) {
    return;
  }
  if (typeof toneFrequency !== "number" || !Number.isFinite(toneFrequency) || toneFrequency < 0.01 || toneFrequency > 2) {
    throw new TypeError(`The \`toneFrequency\` prop of <${component}> must be a finite number between 0.01 and 2, but got ${String(toneFrequency)}.`);
  }
};
var getTimeInSeconds = ({
  loop,
  mediaDurationInSeconds,
  unloopedTimeInSeconds,
  src,
  trimAfter,
  trimBefore,
  fps,
  playbackRate,
  ifNoMediaDuration
}) => {
  if (mediaDurationInSeconds === null && loop && ifNoMediaDuration === "fail") {
    throw new Error(`Could not determine duration of ${src}, but "loop" was set.`);
  }
  const loopDurationInFrames = loop ? Internals22.calculateMediaDuration({
    trimAfter,
    mediaDurationInFrames: mediaDurationInSeconds ? mediaDurationInSeconds * fps : Infinity,
    playbackRate: 1,
    trimBefore
  }) : Infinity;
  const timeInSeconds = loop ? unloopedTimeInSeconds * playbackRate * fps % loopDurationInFrames / fps : unloopedTimeInSeconds * playbackRate;
  if ((trimAfter ?? null) !== null && !loop) {
    const time = (trimAfter - (trimBefore ?? 0)) / fps;
    if (timeInSeconds >= time) {
      return null;
    }
  }
  return timeInSeconds + (trimBefore ?? 0) / fps;
};
var calculateEndTime = ({
  mediaDurationInSeconds,
  ifNoMediaDuration,
  src,
  trimAfter,
  trimBefore,
  fps
}) => {
  if (mediaDurationInSeconds === null && ifNoMediaDuration === "fail") {
    throw new Error(`Could not determine duration of ${src}, but "loop" was set.`);
  }
  const mediaDuration = Internals22.calculateMediaDuration({
    trimAfter,
    mediaDurationInFrames: mediaDurationInSeconds ? mediaDurationInSeconds * fps : Infinity,
    playbackRate: 1,
    trimBefore
  }) / fps;
  return mediaDuration + (trimBefore ?? 0) / fps;
};
var ALLOWED_GLOBAL_TIME_ANCHOR_SHIFT = 0.1;
var makeAudioIterator = ({
  startFromSecond,
  iterator,
  unscheduleAudioNode
}) => {
  let destroyed = false;
  const queuedAudioNodes = [];
  let mostRecentTimestamp = -Infinity;
  const cleanupAudioQueue = () => {
    for (const { node } of queuedAudioNodes) {
      unscheduleAudioNode(node);
      try {
        node.stop();
      } catch {}
    }
    queuedAudioNodes.length = 0;
  };
  const getNextFn = async () => {
    const next = await iterator.next();
    if (next.value) {
      mostRecentTimestamp = Math.max(mostRecentTimestamp, next.value.timelineTimestamp + next.value.sourceDurationInSeconds);
    }
    return next;
  };
  return {
    destroy: () => {
      cleanupAudioQueue();
      destroyed = true;
      iterator.return().catch(() => {
        return;
      });
    },
    getNextFn,
    isDestroyed: () => {
      return destroyed;
    },
    addQueuedAudioNode: (queuedNode) => {
      queuedAudioNodes.push(queuedNode);
    },
    guessNextTimestamp: () => {
      return !Number.isFinite(mostRecentTimestamp) ? startFromSecond : mostRecentTimestamp;
    },
    getQueuedPeriod: () => {
      let until = -Infinity;
      let from = Infinity;
      for (const node of queuedAudioNodes) {
        until = Math.max(until, node.timestamp + node.sourceDurationInSeconds);
        from = Math.min(from, node.timestamp);
      }
      if (!Number.isFinite(from) || !Number.isFinite(until)) {
        return null;
      }
      return {
        from,
        until
      };
    }
  };
};
var isAlreadyQueued = (time, queuedPeriod) => {
  if (!queuedPeriod) {
    return false;
  }
  return time >= queuedPeriod.from && time < queuedPeriod.until;
};
var getScheduledTime = ({
  mediaTimestamp,
  targetTime,
  currentTime,
  sequenceStartTime
}) => {
  const needsTrimStart = mediaTimestamp < sequenceStartTime;
  const offsetBecauseOfTrim = needsTrimStart ? sequenceStartTime - mediaTimestamp : 0;
  const offsetBecauseOfTooLate = targetTime < 0 ? -targetTime : 0;
  const offset = offsetBecauseOfTrim + offsetBecauseOfTooLate;
  const scheduledTime = targetTime + currentTime + offset;
  return scheduledTime;
};
var getDurationOfNode = ({
  sourceDurationInSeconds,
  sourceOffsetInSeconds,
  offset
}) => {
  return Math.max(0, sourceDurationInSeconds - (offset - sourceOffsetInSeconds));
};
var getTrimStartForAudioNode = ({
  mediaTimestamp,
  targetTime,
  sequenceStartTime,
  combinedPlaybackRate,
  sourceStartOffsetInSeconds
}) => {
  const needsTrimStart = mediaTimestamp < sequenceStartTime;
  const offsetBecauseOfTrim = needsTrimStart ? sequenceStartTime - mediaTimestamp : 0;
  const offsetBecauseOfTooLate = targetTime < 0 ? -targetTime * combinedPlaybackRate : 0;
  return sourceStartOffsetInSeconds + offsetBecauseOfTrim + offsetBecauseOfTooLate;
};
var REFERENCE_SAMPLE_RATE = 48000;
var REFERENCE_HOP_SIZE = 512;
var makePlanarAudio = (numberOfChannels, length) => {
  return new Array(numberOfChannels).fill(null).map(() => new Float32Array(length));
};
var ensurePlanarCapacity = ({
  buffers,
  requiredLength
}) => {
  if (buffers[0].length >= requiredLength) {
    return buffers;
  }
  let newLength = buffers[0].length;
  while (newLength < requiredLength) {
    newLength *= 2;
  }
  return buffers.map((buffer) => {
    const expanded = new Float32Array(newLength);
    expanded.set(buffer);
    return expanded;
  });
};

class PlanarAudioQueue {
  chunks = [];
  length = 0;
  push(audio) {
    if (audio[0].length === 0) {
      return;
    }
    this.chunks.push(audio);
    this.length += audio[0].length;
  }
  take(numberOfFrames, numberOfChannels) {
    const framesToTake = Math.min(numberOfFrames, this.length);
    const result = makePlanarAudio(numberOfChannels, framesToTake);
    let written = 0;
    while (written < framesToTake) {
      const first = this.chunks[0];
      const available = first[0].length;
      const count = Math.min(available, framesToTake - written);
      for (let channel = 0;channel < numberOfChannels; channel++) {
        result[channel].set(first[channel].subarray(0, count), written);
      }
      if (count === available) {
        this.chunks.shift();
      } else {
        this.chunks[0] = first.map((channel) => channel.subarray(count));
      }
      written += count;
      this.length -= count;
    }
    return result;
  }
  getLength() {
    return this.length;
  }
}

class StreamingTimeStretcher {
  numberOfChannels;
  factor;
  hopSize;
  windowSize;
  searchRadius;
  analysisHop;
  input;
  inputLength = 0;
  output;
  outputLength = 0;
  analysisPosition = 0;
  synthesisPosition = 0;
  initialized = false;
  finalized = false;
  totalInputFrames = 0;
  totalOutputFrames = 0;
  constructor({
    numberOfChannels,
    sampleRate,
    factor
  }) {
    this.numberOfChannels = numberOfChannels;
    this.factor = factor;
    this.hopSize = Math.max(32, Math.round(REFERENCE_HOP_SIZE * sampleRate / REFERENCE_SAMPLE_RATE));
    this.windowSize = this.hopSize * 2;
    this.searchRadius = this.hopSize;
    this.analysisHop = this.hopSize / factor;
    this.input = makePlanarAudio(numberOfChannels, 65536);
    this.output = makePlanarAudio(numberOfChannels, 65536);
  }
  append(audio) {
    if (this.finalized) {
      throw new Error("Cannot append audio after the time stretcher was finalized.");
    }
    const { length } = audio[0];
    this.input = ensurePlanarCapacity({
      buffers: this.input,
      requiredLength: this.inputLength + length
    });
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.input[channel].set(audio[channel], this.inputLength);
    }
    this.inputLength += length;
    this.totalInputFrames += length;
    this.process();
    return this.drainFinalizedOutput();
  }
  findBestAnalysisPosition({
    expectedPosition,
    nextSynthesisPosition
  }) {
    const minimum = Math.max(0, Math.floor(expectedPosition - this.searchRadius));
    const maximum = Math.min(this.inputLength - this.windowSize, Math.ceil(expectedPosition + this.searchRadius));
    let bestPosition = minimum;
    let bestCorrelation = -Infinity;
    for (let candidate = minimum;candidate <= maximum; candidate += 4) {
      let dotProduct = 0;
      let previousEnergy = 0;
      let candidateEnergy = 0;
      for (let channel = 0;channel < this.numberOfChannels; channel++) {
        const previous = this.output[channel];
        const incoming = this.input[channel];
        for (let frame2 = 0;frame2 < this.hopSize; frame2 += 2) {
          const previousValue = previous[nextSynthesisPosition + frame2];
          const candidateValue = incoming[candidate + frame2];
          dotProduct += previousValue * candidateValue;
          previousEnergy += previousValue * previousValue;
          candidateEnergy += candidateValue * candidateValue;
        }
      }
      const correlation = dotProduct / (Math.sqrt(previousEnergy * candidateEnergy) || Number.EPSILON);
      if (correlation > bestCorrelation) {
        bestCorrelation = correlation;
        bestPosition = candidate;
      }
    }
    const fineMinimum = Math.max(minimum, bestPosition - 4);
    const fineMaximum = Math.min(maximum, bestPosition + 4);
    for (let candidate = fineMinimum;candidate <= fineMaximum; candidate++) {
      let dotProduct = 0;
      let previousEnergy = 0;
      let candidateEnergy = 0;
      for (let channel = 0;channel < this.numberOfChannels; channel++) {
        const previous = this.output[channel];
        const incoming = this.input[channel];
        for (let frame2 = 0;frame2 < this.hopSize; frame2++) {
          const previousValue = previous[nextSynthesisPosition + frame2];
          const candidateValue = incoming[candidate + frame2];
          dotProduct += previousValue * candidateValue;
          previousEnergy += previousValue * previousValue;
          candidateEnergy += candidateValue * candidateValue;
        }
      }
      const correlation = dotProduct / (Math.sqrt(previousEnergy * candidateEnergy) || Number.EPSILON);
      if (correlation > bestCorrelation) {
        bestCorrelation = correlation;
        bestPosition = candidate;
      }
    }
    return bestPosition;
  }
  process() {
    if (!this.initialized) {
      if (this.inputLength < this.windowSize + this.searchRadius) {
        return;
      }
      for (let channel = 0;channel < this.numberOfChannels; channel++) {
        this.output[channel].set(this.input[channel].subarray(0, this.windowSize));
      }
      this.outputLength = this.windowSize;
      this.initialized = true;
    }
    while (true) {
      const expectedPosition = this.analysisPosition + this.analysisHop;
      if (expectedPosition + this.searchRadius + this.windowSize > this.inputLength) {
        break;
      }
      const nextSynthesisPosition = this.synthesisPosition + this.hopSize;
      this.output = ensurePlanarCapacity({
        buffers: this.output,
        requiredLength: nextSynthesisPosition + this.windowSize
      });
      const bestPosition = this.findBestAnalysisPosition({
        expectedPosition,
        nextSynthesisPosition
      });
      for (let channel = 0;channel < this.numberOfChannels; channel++) {
        for (let frame2 = 0;frame2 < this.hopSize; frame2++) {
          const fadeIn = 0.5 - 0.5 * Math.cos(Math.PI * (frame2 + 1) / (this.hopSize + 1));
          const outputIndex = nextSynthesisPosition + frame2;
          this.output[channel][outputIndex] = this.output[channel][outputIndex] * (1 - fadeIn) + this.input[channel][bestPosition + frame2] * fadeIn;
        }
        this.output[channel].set(this.input[channel].subarray(bestPosition + this.hopSize, bestPosition + this.windowSize), nextSynthesisPosition + this.hopSize);
      }
      this.analysisPosition = expectedPosition;
      this.synthesisPosition = nextSynthesisPosition;
      this.outputLength = nextSynthesisPosition + this.windowSize;
    }
  }
  drainFinalizedOutput() {
    if (!this.initialized) {
      return makePlanarAudio(this.numberOfChannels, 0);
    }
    const finalizedLength = Math.max(0, this.synthesisPosition + this.hopSize);
    const result = this.output.map((channel) => channel.slice(0, finalizedLength));
    this.totalOutputFrames += finalizedLength;
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.output[channel].copyWithin(0, finalizedLength, this.outputLength);
    }
    this.outputLength -= finalizedLength;
    this.synthesisPosition -= finalizedLength;
    const inputFramesToDiscard = Math.max(0, Math.floor(this.analysisPosition) - this.searchRadius);
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.input[channel].copyWithin(0, inputFramesToDiscard, this.inputLength);
    }
    this.inputLength -= inputFramesToDiscard;
    this.analysisPosition -= inputFramesToDiscard;
    return result;
  }
  finalize() {
    if (this.finalized) {
      throw new Error("The time stretcher has already been finalized.");
    }
    this.finalized = true;
    const targetLength = Math.round(this.totalInputFrames * this.factor);
    const padding = makePlanarAudio(this.numberOfChannels, this.windowSize + this.searchRadius * 2);
    this.input = ensurePlanarCapacity({
      buffers: this.input,
      requiredLength: this.inputLength + padding[0].length
    });
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.input[channel].set(padding[channel], this.inputLength);
    }
    this.inputLength += padding[0].length;
    this.process();
    const finalized = this.drainFinalizedOutput();
    const remaining = Math.max(0, targetLength - this.totalOutputFrames + finalized[0].length);
    if (finalized[0].length >= remaining) {
      return finalized.map((channel) => channel.slice(0, remaining));
    }
    const result = makePlanarAudio(this.numberOfChannels, remaining);
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      result[channel].set(finalized[channel]);
    }
    return result;
  }
}

class StreamingLinearResampler {
  numberOfChannels;
  step;
  input;
  inputLength = 0;
  position = 0;
  constructor({
    numberOfChannels,
    step
  }) {
    this.numberOfChannels = numberOfChannels;
    this.step = step;
    this.input = makePlanarAudio(numberOfChannels, 65536);
  }
  append(audio) {
    this.input = ensurePlanarCapacity({
      buffers: this.input,
      requiredLength: this.inputLength + audio[0].length
    });
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.input[channel].set(audio[channel], this.inputLength);
    }
    this.inputLength += audio[0].length;
    return this.process(false);
  }
  process(finalizing) {
    const outputLength = Math.max(0, Math.floor((this.inputLength - (finalizing ? 0 : 1) - this.position) / this.step) + 1);
    const result = makePlanarAudio(this.numberOfChannels, outputLength);
    for (let outputFrame = 0;outputFrame < outputLength; outputFrame++) {
      const leftIndex = Math.floor(this.position);
      const rightIndex = Math.min(leftIndex + 1, this.inputLength - 1);
      const fraction = this.position - leftIndex;
      for (let channel = 0;channel < this.numberOfChannels; channel++) {
        const left3 = this.input[channel][leftIndex];
        const right = this.input[channel][rightIndex];
        result[channel][outputFrame] = left3 + (right - left3) * fraction;
      }
      this.position += this.step;
    }
    const discard = Math.min(Math.floor(this.position), this.inputLength);
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      this.input[channel].copyWithin(0, discard, this.inputLength);
    }
    this.inputLength -= discard;
    this.position -= discard;
    return result;
  }
  finalize() {
    return this.process(true);
  }
}

class StreamingPitchShifter {
  numberOfChannels;
  stretcher;
  resampler;
  outputQueue = new PlanarAudioQueue;
  totalInputFrames = 0;
  totalOutputFrames = 0;
  constructor({
    numberOfChannels,
    sampleRate,
    toneFrequency
  }) {
    this.numberOfChannels = numberOfChannels;
    this.stretcher = new StreamingTimeStretcher({
      numberOfChannels,
      sampleRate,
      factor: toneFrequency
    });
    this.resampler = new StreamingLinearResampler({
      numberOfChannels,
      step: toneFrequency
    });
  }
  append(audio) {
    this.totalInputFrames += audio[0].length;
    const stretched = this.stretcher.append(audio);
    this.outputQueue.push(this.resampler.append(stretched));
    return this.takeAvailableOutput();
  }
  takeAvailableOutput() {
    const availableInputFrames = this.totalInputFrames - this.totalOutputFrames;
    const framesToTake = Math.min(availableInputFrames, this.outputQueue.getLength());
    const result = this.outputQueue.take(framesToTake, this.numberOfChannels);
    this.totalOutputFrames += framesToTake;
    return result;
  }
  finalize() {
    this.outputQueue.push(this.resampler.append(this.stretcher.finalize()));
    this.outputQueue.push(this.resampler.finalize());
    const remaining = this.totalInputFrames - this.totalOutputFrames;
    const available = this.outputQueue.take(Math.min(remaining, this.outputQueue.getLength()), this.numberOfChannels);
    const result = makePlanarAudio(this.numberOfChannels, remaining);
    for (let channel = 0;channel < this.numberOfChannels; channel++) {
      result[channel].set(available[channel]);
    }
    this.totalOutputFrames += remaining;
    return result;
  }
}
var getPlanarSlice = (slice) => {
  const { buffer } = slice.buffer;
  const startFrame = Math.max(0, Math.round(slice.sourceOffsetInSeconds * buffer.sampleRate));
  const numberOfFrames = Math.min(buffer.length - startFrame, Math.round(slice.sourceDurationInSeconds * buffer.sampleRate));
  return new Array(buffer.numberOfChannels).fill(null).map((_, channel) => buffer.getChannelData(channel).slice(startFrame, startFrame + numberOfFrames));
};
var makeAudioBufferSlice = ({
  audio,
  timelineTimestamp,
  sampleRate
}) => {
  const buffer = new AudioBuffer({
    length: audio[0].length,
    numberOfChannels: audio.length,
    sampleRate
  });
  for (let channel = 0;channel < audio.length; channel++) {
    buffer.copyToChannel(new Float32Array(audio[channel]), channel);
  }
  const duration = audio[0].length / sampleRate;
  return {
    buffer: { buffer, timestamp: timelineTimestamp, duration },
    timelineTimestamp,
    sourceOffsetInSeconds: 0,
    sourceDurationInSeconds: duration
  };
};
async function* pitchShiftAudioIterator({
  iterator,
  toneFrequency
}) {
  if (toneFrequency === 1) {
    yield* iterator;
    return;
  }
  let shifter = null;
  let sampleRate = 0;
  let numberOfChannels = 0;
  let segmentStart = 0;
  let segmentInputFrames = 0;
  let segmentOutputFrames = 0;
  const flush = () => {
    if (!shifter) {
      return null;
    }
    const audio = shifter.finalize();
    const slice = audio[0].length === 0 ? null : makeAudioBufferSlice({
      audio,
      timelineTimestamp: segmentStart + segmentOutputFrames / sampleRate,
      sampleRate
    });
    shifter = null;
    return slice;
  };
  for await (const slice of iterator) {
    const planar = getPlanarSlice(slice);
    if (planar[0].length === 0) {
      continue;
    }
    const nextSampleRate = slice.buffer.buffer.sampleRate;
    const nextNumberOfChannels = slice.buffer.buffer.numberOfChannels;
    const expectedTimestamp = segmentStart + segmentInputFrames / sampleRate;
    const startsNewSegment = !shifter || nextSampleRate !== sampleRate || nextNumberOfChannels !== numberOfChannels || Math.abs(slice.timelineTimestamp - expectedTimestamp) > 1.5 / nextSampleRate;
    if (startsNewSegment) {
      const previousSegmentFinalSlice = flush();
      if (previousSegmentFinalSlice) {
        yield previousSegmentFinalSlice;
      }
      sampleRate = nextSampleRate;
      numberOfChannels = nextNumberOfChannels;
      segmentStart = slice.timelineTimestamp;
      segmentInputFrames = 0;
      segmentOutputFrames = 0;
      shifter = new StreamingPitchShifter({
        numberOfChannels,
        sampleRate,
        toneFrequency
      });
    }
    if (!shifter) {
      throw new Error("Pitch shifter was not initialized.");
    }
    segmentInputFrames += planar[0].length;
    const output = shifter.append(planar);
    if (output[0].length > 0) {
      yield makeAudioBufferSlice({
        audio: output,
        timelineTimestamp: segmentStart + segmentOutputFrames / sampleRate,
        sampleRate
      });
      segmentOutputFrames += output[0].length;
    }
  }
  const finalSlice = flush();
  if (finalSlice) {
    yield finalSlice;
  }
}

class StaleWaiterError extends Error {
  constructor() {
    super("Waiter became stale before it got its turn");
    this.name = "StaleWaiterError";
  }
}
var CONCURRENCY = 1;
var waiters = [];
var running = 0;
var runningEntry = null;
var processNext = () => {
  if (running >= CONCURRENCY) {
    if (runningEntry?.waiter.getPriority() === null) {
      runningEntry.cancel();
    } else {
      return;
    }
  }
  const staleWaiters = [];
  for (let i = waiters.length - 1;i >= 0; i--) {
    if (waiters[i].getPriority() === null) {
      const [stale] = waiters.splice(i, 1);
      staleWaiters.push(stale);
    }
  }
  for (const stale of staleWaiters) {
    stale.onError(new StaleWaiterError);
  }
  if (waiters.length === 0) {
    return;
  }
  let bestIndex = 0;
  let bestPriority = waiters[0].getPriority();
  if (bestPriority === null) {
    throw new Error("Stale waiter should have been removed");
  }
  for (let i = 1;i < waiters.length; i++) {
    const priority = waiters[i].getPriority();
    if (priority === null) {
      throw new Error("Stale waiter should have been removed");
    }
    if (priority < bestPriority) {
      bestPriority = priority;
      bestIndex = i;
    }
  }
  if (bestPriority > 2) {
    return;
  }
  const [next] = waiters.splice(bestIndex, 1);
  running++;
  let settled = false;
  let cancelled = false;
  const entry = {
    waiter: next,
    cancel: () => {
      cancelled = true;
      entry.settle();
    },
    settle: () => {
      if (settled) {
        return;
      }
      settled = true;
      running--;
      if (runningEntry === entry) {
        runningEntry = null;
      }
    }
  };
  runningEntry = entry;
  next.fn().then((value) => {
    entry.settle();
    if (cancelled) {
      return;
    }
    next.onDone(value, processNext);
  }, (err) => {
    entry.settle();
    if (cancelled) {
      return;
    }
    next.onError(err);
  });
};
var waitForTurn = ({
  getPriority,
  fn,
  onDone,
  onError
}) => {
  waiters.push({
    getPriority,
    fn,
    onDone,
    onError
  });
  processNext();
};
var AUDIO_PRIMING_SECONDS = 0.5;
async function* makeIteratorWithPriming({
  audioSink,
  timeToSeek,
  maximumTimestamp
}) {
  const primingStart = Math.max(0, timeToSeek - AUDIO_PRIMING_SECONDS);
  const iterator = audioSink.buffers(primingStart, maximumTimestamp);
  for await (const buffer of iterator) {
    const sourceStart = Math.max(buffer.timestamp, timeToSeek);
    const sourceEnd = Math.min(buffer.timestamp + buffer.duration, maximumTimestamp);
    if (sourceEnd <= sourceStart) {
      continue;
    }
    yield {
      buffer,
      timelineTimestamp: sourceStart,
      sourceOffsetInSeconds: sourceStart - buffer.timestamp,
      sourceDurationInSeconds: sourceEnd - sourceStart
    };
  }
}
async function* makeLoopingIterator({
  audioSink,
  seekTimeInSeconds,
  loopStartInSeconds,
  segmentEndInSeconds,
  maximumContinuousTimestamp
}) {
  if (segmentEndInSeconds <= loopStartInSeconds) {
    throw new Error(`Cannot loop audio over an empty range (${loopStartInSeconds} to ${segmentEndInSeconds})`);
  }
  let passStartInSeconds = seekTimeInSeconds;
  let passBaseTimestamp = seekTimeInSeconds;
  while (true) {
    let yieldedInPass = false;
    for await (const item of makeIteratorWithPriming({
      audioSink,
      timeToSeek: passStartInSeconds,
      maximumTimestamp: segmentEndInSeconds
    })) {
      yieldedInPass = true;
      const timelineTimestamp = passBaseTimestamp + (item.timelineTimestamp - passStartInSeconds);
      const sourceDurationInSeconds = Math.min(item.sourceDurationInSeconds, maximumContinuousTimestamp - timelineTimestamp);
      if (sourceDurationInSeconds <= 0) {
        return;
      }
      yield {
        ...item,
        timelineTimestamp,
        sourceDurationInSeconds
      };
      if (timelineTimestamp + sourceDurationInSeconds >= maximumContinuousTimestamp) {
        return;
      }
    }
    if (!yieldedInPass && passStartInSeconds === loopStartInSeconds) {
      return;
    }
    passBaseTimestamp += segmentEndInSeconds - passStartInSeconds;
    passStartInSeconds = loopStartInSeconds;
  }
}
var MINIMUM_AUDIO_BUFFERING_TIME_SECONDS = 0.1;
var hasEnoughAudioToStartPlayback = (bufferedDuration) => {
  return bufferedDuration >= MINIMUM_AUDIO_BUFFERING_TIME_SECONDS;
};
var anchorToContinuousTime = ({
  anchor,
  unloopedTimeInSeconds,
  playbackRate
}) => {
  return anchor.mediaStartInSeconds + (unloopedTimeInSeconds - anchor.unloopedStartInSeconds) * playbackRate;
};
var audioIteratorManager = ({
  audioTrack,
  delayPlaybackHandleIfNotPremounting,
  sharedAudioContext,
  getSequenceEndTimestamp,
  getSequenceDurationInSeconds,
  getMediaEndTimestamp,
  getStartTime,
  initialMuted,
  initialVolume,
  toneFrequency,
  drawDebugOverlay
}) => {
  let muted = initialMuted;
  let currentVolume = Math.max(0, initialVolume);
  let currentToneFrequency = toneFrequency;
  let currentSeek = null;
  const gainNode = sharedAudioContext.audioContext.createGain();
  gainNode.gain.value = muted ? 0 : currentVolume;
  gainNode.connect(sharedAudioContext.gainNode);
  const audioSink = new AudioBufferSink(audioTrack);
  let audioBufferIterator = null;
  let currentAnchor = null;
  let audioIteratorsCreated = 0;
  let totalAudioScheduledInSeconds = 0;
  let currentDelayHandle = null;
  const unblockCurrentDelayHandle = () => {
    if (currentDelayHandle) {
      currentDelayHandle.unblock();
      currentDelayHandle = null;
    }
  };
  const pendingScheduleWaiters = [];
  const notifyNodeScheduled = () => {
    for (let i = pendingScheduleWaiters.length - 1;i >= 0; i--) {
      const waiter = pendingScheduleWaiters[i];
      waiter.remaining--;
      if (waiter.remaining <= 0) {
        waiter.resolve();
        pendingScheduleWaiters.splice(i, 1);
      }
    }
  };
  const waitForNScheduledNodes = (n) => {
    if (n <= 0) {
      return Promise.resolve();
    }
    return new Promise((resolve) => {
      pendingScheduleWaiters.push({ remaining: n, resolve });
    });
  };
  const scheduleAudioChunk = ({
    buffer,
    mediaTimestamp,
    originalUnloopedMediaTimestamp,
    sourceOffsetInSeconds,
    sourceDurationInSeconds,
    playbackRate,
    scheduleAudioNode,
    logLevel
  }) => {
    if (!audioBufferIterator) {
      throw new Error("Audio buffer iterator not found");
    }
    if (muted) {
      return;
    }
    const node = sharedAudioContext.audioContext.createBufferSource();
    node.buffer = buffer;
    node.playbackRate.value = playbackRate;
    node.connect(gainNode);
    const started = scheduleAudioNode(node, mediaTimestamp, originalUnloopedMediaTimestamp, sourceOffsetInSeconds, sourceDurationInSeconds);
    if (started.type === "not-started") {
      Internals32.Log.verbose({ logLevel, tag: "audio-scheduling" }, "not started, disconnected: %s %s", mediaTimestamp.toFixed(3), buffer.duration.toFixed(3));
      node.disconnect();
      return;
    }
    audioBufferIterator.addQueuedAudioNode({
      node,
      timestamp: mediaTimestamp,
      buffer,
      sourceDurationInSeconds,
      scheduledTime: started.scheduledTime,
      playbackRate,
      scheduledAtAnchor: sharedAudioContext.audioSyncAnchor.value
    });
  };
  const onAudioChunk = ({
    buffer,
    playbackRate,
    scheduleAudioNode,
    logLevel
  }) => {
    if (muted) {
      return;
    }
    const startTime = getStartTime();
    const sequenceEndTime = getSequenceEndTimestamp();
    if (buffer.timelineTimestamp + buffer.sourceDurationInSeconds <= startTime) {
      return;
    }
    if (buffer.timelineTimestamp >= sequenceEndTime) {
      return;
    }
    const scheduledStart = Math.max(buffer.timelineTimestamp, startTime);
    const scheduledEnd = Math.min(buffer.timelineTimestamp + buffer.sourceDurationInSeconds, sequenceEndTime);
    totalAudioScheduledInSeconds += Math.max(0, scheduledEnd - scheduledStart);
    scheduleAudioChunk({
      buffer: buffer.buffer.buffer,
      mediaTimestamp: buffer.timelineTimestamp,
      playbackRate,
      scheduleAudioNode,
      logLevel,
      originalUnloopedMediaTimestamp: buffer.buffer.timestamp,
      sourceOffsetInSeconds: buffer.sourceOffsetInSeconds,
      sourceDurationInSeconds: buffer.sourceDurationInSeconds
    });
    drawDebugOverlay();
  };
  const proceedScheduling = ({
    iterator,
    nonce,
    getTargetTime,
    playbackRate,
    scheduleAudioNode,
    onScheduled,
    onDestroyed,
    onDone,
    logLevel,
    currentTime,
    getAudioContextCurrentTimeMockedInTest
  }) => {
    waitForTurn({
      getPriority: () => {
        if (iterator.isDestroyed()) {
          onDestroyed();
          return null;
        }
        const guessedNextTimestamp = iterator.guessNextTimestamp();
        const targetTime = getTargetTime(guessedNextTimestamp, currentTime);
        if (targetTime === null) {
          return null;
        }
        const scheduledTime = getScheduledTime({
          mediaTimestamp: guessedNextTimestamp,
          targetTime,
          currentTime,
          sequenceStartTime: getStartTime()
        });
        return scheduledTime - getAudioContextCurrentTimeMockedInTest();
      },
      fn: () => iterator.getNextFn(),
      onDone: (result, next) => {
        if (iterator.isDestroyed()) {
          next();
          onDestroyed();
          return;
        }
        if (!result.value) {
          next();
          onDone();
          return;
        }
        onScheduled(result.value.sourceDurationInSeconds, result.value.timelineTimestamp);
        notifyNodeScheduled();
        onAudioChunk({
          buffer: result.value,
          playbackRate,
          scheduleAudioNode,
          logLevel
        });
        proceedScheduling({
          iterator,
          nonce,
          getTargetTime,
          playbackRate,
          scheduleAudioNode,
          onScheduled,
          onDestroyed,
          onDone,
          logLevel,
          currentTime,
          getAudioContextCurrentTimeMockedInTest
        });
        next();
      },
      onError: (e) => {
        if (e instanceof InputDisposedError) {
          onDestroyed();
          return;
        }
        if (e instanceof StaleWaiterError) {
          onDestroyed();
          return;
        }
        throw e;
      }
    });
  };
  const startAudioIterator = ({
    nonce,
    playbackRate,
    startFromSecond,
    unloopedStartFromSecond,
    scheduleAudioNode,
    getTargetTime,
    logLevel,
    loop,
    unscheduleAudioNode,
    getAudioContextCurrentTimeMockedInTest
  }) => {
    if (muted) {
      return;
    }
    const maximumTimestamp = getMediaEndTimestamp();
    if (startFromSecond >= maximumTimestamp) {
      return;
    }
    audioBufferIterator?.destroy();
    unblockCurrentDelayHandle();
    const delayHandle = delayPlaybackHandleIfNotPremounting();
    currentDelayHandle = delayHandle;
    currentAnchor = {
      unloopedStartInSeconds: unloopedStartFromSecond,
      mediaStartInSeconds: startFromSecond
    };
    const maximumContinuousTimestamp = startFromSecond + getSequenceDurationInSeconds() * playbackRate;
    const unshiftedSource = loop ? makeLoopingIterator({
      audioSink,
      seekTimeInSeconds: startFromSecond,
      loopStartInSeconds: getStartTime(),
      segmentEndInSeconds: maximumTimestamp,
      maximumContinuousTimestamp
    }) : makeIteratorWithPriming({
      audioSink,
      timeToSeek: startFromSecond,
      maximumTimestamp
    });
    const source = pitchShiftAudioIterator({
      iterator: unshiftedSource,
      toneFrequency: currentToneFrequency
    });
    const iterator = makeAudioIterator({
      startFromSecond,
      iterator: source,
      unscheduleAudioNode
    });
    audioIteratorsCreated++;
    audioBufferIterator = iterator;
    let bufferedUntil = startFromSecond;
    let hasUnblockedPlayback = false;
    const unblockPlayback = () => {
      if (hasUnblockedPlayback) {
        return;
      }
      hasUnblockedPlayback = true;
      delayHandle.unblock();
    };
    proceedScheduling({
      iterator,
      nonce,
      getTargetTime,
      playbackRate,
      scheduleAudioNode,
      onScheduled: (sourceDurationInSeconds, timelineTimestamp) => {
        bufferedUntil = Math.max(bufferedUntil, timelineTimestamp + sourceDurationInSeconds);
        const bufferedDuration = bufferedUntil - startFromSecond;
        if (hasEnoughAudioToStartPlayback(bufferedDuration)) {
          unblockPlayback();
        }
      },
      onDestroyed: () => {
        unblockPlayback();
      },
      onDone: () => {
        unblockPlayback();
      },
      logLevel,
      currentTime: sharedAudioContext.audioContext.currentTime,
      getAudioContextCurrentTimeMockedInTest
    });
  };
  const seek2 = ({
    newTime,
    unloopedNewTime,
    nonce,
    playbackRate,
    localPlaybackRate,
    scheduleAudioNode,
    getTargetTime,
    logLevel,
    loop,
    trimBefore,
    trimAfter,
    sequenceOffset,
    sequenceDurationInFrames,
    fps,
    getAudioContextCurrentTimeMockedInTest
  }) => {
    if (nonce.isStale()) {
      return;
    }
    if (currentSeek !== null && currentSeek.time === newTime && currentSeek.playbackRate === playbackRate && currentSeek.trimBefore === trimBefore && currentSeek.trimAfter === trimAfter && currentSeek.sequenceOffset === sequenceOffset && currentSeek.sequenceDurationInFrames === sequenceDurationInFrames && currentSeek.loop === loop && currentSeek.fps === fps) {
      return;
    }
    currentSeek = {
      time: newTime,
      playbackRate,
      trimBefore,
      trimAfter,
      sequenceOffset,
      sequenceDurationInFrames,
      loop,
      fps
    };
    if (muted) {
      return;
    }
    if (audioBufferIterator && !audioBufferIterator.isDestroyed()) {
      const timeToCheck = loop && currentAnchor ? anchorToContinuousTime({
        anchor: currentAnchor,
        unloopedTimeInSeconds: unloopedNewTime,
        playbackRate: localPlaybackRate
      }) : newTime;
      const queuedPeriod = audioBufferIterator.getQueuedPeriod();
      const queuedPeriodMinusLatency = queuedPeriod ? {
        from: queuedPeriod.from - ALLOWED_GLOBAL_TIME_ANCHOR_SHIFT - sharedAudioContext.audioContext.baseLatency - sharedAudioContext.audioContext.outputLatency,
        until: queuedPeriod.until
      } : null;
      const currentTimeIsAlreadyQueued = isAlreadyQueued(timeToCheck, queuedPeriodMinusLatency);
      if (currentTimeIsAlreadyQueued) {
        processNext();
        return;
      }
      const currentIteratorTimestamp = audioBufferIterator.guessNextTimestamp();
      const iteratorHasAdvancedThroughSilence = loop && currentAnchor !== null && unloopedNewTime >= currentAnchor.unloopedStartInSeconds && currentIteratorTimestamp >= timeToCheck;
      if (iteratorHasAdvancedThroughSilence || currentIteratorTimestamp < timeToCheck && Math.abs(currentIteratorTimestamp - timeToCheck) < 1) {
        processNext();
        return;
      }
    }
    startAudioIterator({
      nonce,
      playbackRate,
      startFromSecond: newTime,
      unloopedStartFromSecond: unloopedNewTime,
      scheduleAudioNode,
      getTargetTime,
      logLevel,
      loop,
      unscheduleAudioNode: sharedAudioContext.unscheduleAudioNode,
      getAudioContextCurrentTimeMockedInTest
    });
  };
  return {
    startAudioIterator,
    getAudioBufferIterator: () => audioBufferIterator,
    getCurrentAnchor: () => currentAnchor,
    destroyIterator: () => {
      audioBufferIterator?.destroy();
      audioBufferIterator = null;
      currentAnchor = null;
      currentSeek = null;
      unblockCurrentDelayHandle();
    },
    seek: seek2,
    getAudioIteratorsCreated: () => audioIteratorsCreated,
    getTotalAudioScheduledInSeconds: () => totalAudioScheduledInSeconds,
    setMuted: (newMuted) => {
      muted = newMuted;
      gainNode.gain.value = muted ? 0 : currentVolume;
    },
    setVolume: (volume) => {
      currentVolume = Math.max(0, volume);
      gainNode.gain.value = muted ? 0 : currentVolume;
    },
    setToneFrequency: (newToneFrequency) => {
      currentToneFrequency = newToneFrequency;
    },
    scheduleAudioChunk,
    waitForNScheduledNodes
  };
};
var drawPreviewOverlay = ({
  context,
  audioTime,
  audioContextState,
  audioSyncAnchor,
  playing,
  audioIteratorManager: audioIteratorManager2,
  videoIteratorManager,
  playbackRate
}) => {
  const anchorValue = audioSyncAnchor?.value ?? 0;
  const lines = [
    "Debug overlay",
    `Video iterators created: ${videoIteratorManager?.getVideoIteratorsCreated()}`,
    `Audio iterators created: ${audioIteratorManager2?.getAudioIteratorsCreated()}`,
    `Audio scheduled: ${(audioIteratorManager2?.getTotalAudioScheduledInSeconds() ?? 0).toFixed(3)}s`,
    `Frames rendered: ${videoIteratorManager?.getFramesRendered()}`,
    `Audio context state: ${audioContextState}`,
    audioTime ? `Audio time: ${((audioTime - anchorValue) * playbackRate).toFixed(3)}s` : null
  ].filter(Boolean);
  if (audioIteratorManager2) {
    const queuedPeriod = audioIteratorManager2.getAudioBufferIterator()?.getQueuedPeriod();
    if (queuedPeriod) {
      const aheadText = audioTime ? ` (${(queuedPeriod.until - (audioTime - anchorValue) * playbackRate).toFixed(3)}s ahead)` : "";
      lines.push(`Audio queued until ${queuedPeriod.until.toFixed(3)}s${aheadText}`);
    }
    lines.push(`Playing: ${playing}`);
  }
  const lineHeight2 = 30;
  const boxPaddingX = 10;
  const boxPaddingY = 10;
  const boxLeft = 20;
  const boxTop = 20;
  const boxWidth = 600;
  const boxHeight = lines.length * lineHeight2 + 2 * boxPaddingY;
  context.fillStyle = "rgba(0, 0, 0, 1)";
  context.fillRect(boxLeft, boxTop, boxWidth, boxHeight);
  context.fillStyle = "white";
  context.font = "24px sans-serif";
  context.textBaseline = "top";
  for (let i = 0;i < lines.length; i++) {
    context.fillText(lines[i], boxLeft + boxPaddingX, boxTop + boxPaddingY + i * lineHeight2);
  }
};
var getDurationOrCompute = async (input2) => {
  return await input2.getDurationFromMetadata(undefined, {
    skipLiveWait: true
  }) ?? input2.computeDuration(undefined, { skipLiveWait: true });
};
var getUncachedMaxCacheSize = (logLevel) => {
  if (typeof window !== "undefined" && window.remotion_mediaCacheSizeInBytes !== undefined && window.remotion_mediaCacheSizeInBytes !== null) {
    if (window.remotion_mediaCacheSizeInBytes < 240 * 1024 * 1024) {
      cancelRender(new Error(`The minimum value for the "mediaCacheSizeInBytes" prop is 240MB (${240 * 1024 * 1024}), got: ${window.remotion_mediaCacheSizeInBytes}`));
    }
    if (window.remotion_mediaCacheSizeInBytes > 20000 * 1024 * 1024) {
      cancelRender(new Error(`The maximum value for the "mediaCacheSizeInBytes" prop is 20GB (${20000 * 1024 * 1024}), got: ${window.remotion_mediaCacheSizeInBytes}`));
    }
    Internals42.Log.verbose({ logLevel, tag: "@remotion/media" }, `Using cache size set using "mediaCacheSizeInBytes": ${(window.remotion_mediaCacheSizeInBytes / 1024 / 1024).toFixed(1)} MB`);
    return window.remotion_mediaCacheSizeInBytes;
  }
  if (typeof window !== "undefined" && window.remotion_initialMemoryAvailable !== undefined && window.remotion_initialMemoryAvailable !== null) {
    const value = window.remotion_initialMemoryAvailable / 2;
    if (value < 500 * 1024 * 1024) {
      Internals42.Log.verbose({ logLevel, tag: "@remotion/media" }, `Using cache size set based on minimum value of 500MB (which is more than half of the available system memory!)`);
      return 500 * 1024 * 1024;
    }
    if (value > 20000 * 1024 * 1024) {
      Internals42.Log.verbose({ logLevel, tag: "@remotion/media" }, `Using cache size set based on maximum value of 20GB (which is less than half of the available system memory)`);
      return 20000 * 1024 * 1024;
    }
    Internals42.Log.verbose({ logLevel, tag: "@remotion/media" }, `Using cache size set based on available memory (50% of available memory): ${(value / 1024 / 1024).toFixed(1)} MB`);
    return value;
  }
  return 1000 * 1000 * 1000;
};
var cachedMaxCacheSize = null;
var getMaxVideoCacheSize = (logLevel) => {
  if (cachedMaxCacheSize !== null) {
    return cachedMaxCacheSize;
  }
  cachedMaxCacheSize = getUncachedMaxCacheSize(logLevel);
  return cachedMaxCacheSize;
};
var MIN_SOURCE_CACHE_SIZE = 8 * 1024 * 1024;
var MAX_SOURCE_CACHE_SIZE = 64 * 1024 * 1024;
var getMaxSourceCacheSizeFromBudget = (budget) => {
  return Math.min(MAX_SOURCE_CACHE_SIZE, Math.max(MIN_SOURCE_CACHE_SIZE, Math.floor(budget / 16)));
};
var getMaxSourceCacheSize = (logLevel) => {
  return getMaxSourceCacheSizeFromBudget(getMaxVideoCacheSize(logLevel));
};
var normalizeMediaHeaders = (headers) => {
  if (!headers) {
    return;
  }
  const entries = [];
  if (headers instanceof Headers) {
    headers.forEach((value, key4) => {
      entries.push([key4.toLowerCase(), value]);
    });
  } else if (Array.isArray(headers)) {
    for (const [key4, value] of headers) {
      entries.push([key4.toLowerCase(), value]);
    }
  } else {
    for (const [key4, value] of Object.entries(headers)) {
      entries.push([key4.toLowerCase(), value]);
    }
  }
  entries.sort(([a], [b]) => a < b ? -1 : a > b ? 1 : 0);
  return entries;
};
var normalizeMediaRequestInit = (requestInit) => {
  if (!requestInit) {
    return;
  }
  const headers = normalizeMediaHeaders(requestInit.headers);
  const normalized = {
    ...requestInit.cache === undefined ? null : { cache: requestInit.cache },
    ...requestInit.credentials === undefined ? null : { credentials: requestInit.credentials },
    ...headers === undefined ? null : { headers },
    ...requestInit.integrity === undefined ? null : { integrity: requestInit.integrity },
    ...requestInit.mode === undefined ? null : { mode: requestInit.mode },
    ...requestInit.redirect === undefined ? null : { redirect: requestInit.redirect },
    ...requestInit.referrer === undefined ? null : { referrer: requestInit.referrer },
    ...requestInit.referrerPolicy === undefined ? null : { referrerPolicy: requestInit.referrerPolicy }
  };
  return Object.keys(normalized).length === 0 ? undefined : normalized;
};
var getMediaRequestInitFingerprint = (requestInit) => {
  const normalized = normalizeMediaRequestInit(requestInit);
  if (!normalized) {
    return null;
  }
  return [
    normalized.cache ?? null,
    normalized.credentials ?? null,
    normalized.integrity ?? null,
    normalized.mode ?? null,
    normalized.redirect ?? null,
    normalized.referrer ?? null,
    normalized.referrerPolicy ?? null,
    normalized.headers ?? null
  ];
};
var resolveRequestInit = ({
  credentials,
  requestInit
}) => {
  if (credentials === undefined) {
    return normalizeMediaRequestInit(requestInit);
  }
  return normalizeMediaRequestInit({
    credentials,
    ...requestInit
  });
};
var getSharedInputCacheKey = ({
  src,
  credentials,
  requestInit
}) => Internals52.getMediabunnyInputResourceKey({
  src,
  credentials: credentials ?? null,
  requestInitFingerprint: getMediaRequestInitFingerprint(requestInit),
  revision: null
});
var acquireSharedInput = ({
  src,
  credentials,
  requestInit,
  logLevel
}) => {
  const normalizedRequestInit = normalizeMediaRequestInit(requestInit);
  const cacheKey = getSharedInputCacheKey({
    src,
    credentials,
    requestInit: normalizedRequestInit
  });
  const resolvedRequestInit = resolveRequestInit({
    credentials,
    requestInit: normalizedRequestInit
  });
  const lease = Internals52.globalMediaResourceManager.acquire({
    key: cacheKey,
    create: () => {
      const input2 = new Input2({
        source: new UrlSource(src, {
          handleUnhandledError: (error) => {
            Internals52.Log.warn({ logLevel, tag: "@remotion/media" }, `A speculative fetch for "${src}" failed:`, error);
          },
          maxCacheSize: getMaxSourceCacheSize(logLevel),
          ...resolvedRequestInit ? { requestInit: resolvedRequestInit } : undefined
        }),
        formats: ALL_FORMATS2
      });
      return { resource: input2, dispose: () => input2.dispose() };
    }
  });
  return {
    input: lease.resource,
    getDuration: () => lease.getOrCreateValue(Internals52.MEDIABUNNY_DURATION_VALUE_KEY, () => getDurationOrCompute(lease.resource)),
    release: lease.release
  };
};
var resolveAudioTrack = async ({
  videoTrack,
  audioTracks,
  audioStreamIndex
}) => {
  if (audioStreamIndex !== null) {
    return audioTracks[audioStreamIndex] ?? null;
  }
  if (videoTrack) {
    return await videoTrack.getPrimaryPairableAudioTrack() ?? null;
  }
  return audioTracks[0] ?? null;
};
function isNetworkError(error) {
  if (error.message.includes("Failed to fetch") || error.message.includes("Load failed") || error.message.includes("NetworkError when attempting to fetch resource")) {
    return true;
  }
  return false;
}
function isUnsupportedConfigurationError(error) {
  return error.message.includes("Unsupported configuration");
}
var makeNonceManager = () => {
  let nonce = 0;
  const createAsyncOperation = () => {
    nonce++;
    const currentNonce = nonce;
    return {
      isStale: () => nonce !== currentNonce
    };
  };
  return {
    createAsyncOperation
  };
};

class PremountAwareDelayPlayback {
  isPremounting;
  isPostmounting;
  activeHandles = new Set;
  delayPlayback;
  constructor({
    bufferState,
    isPremounting,
    isPostmounting
  }) {
    this.delayPlayback = bufferState.delayPlayback;
    this.isPremounting = isPremounting;
    this.isPostmounting = isPostmounting;
  }
  shouldDelayPlayback() {
    return !this.isPremounting && !this.isPostmounting;
  }
  syncHandles() {
    for (const handle of this.activeHandles) {
      if (this.shouldDelayPlayback()) {
        handle.arm();
      } else {
        handle.disarm();
      }
    }
  }
  setIsPremounting(isPremounting) {
    this.isPremounting = isPremounting;
    this.syncHandles();
  }
  setIsPostmounting(isPostmounting) {
    this.isPostmounting = isPostmounting;
    this.syncHandles();
  }
  createHandle() {
    let armed = false;
    let unblock = null;
    let disposed = false;
    const arm = () => {
      if (armed || disposed) {
        return;
      }
      unblock = this.delayPlayback().unblock;
      armed = true;
    };
    const disarm = () => {
      if (!armed) {
        return;
      }
      unblock?.();
      unblock = null;
      armed = false;
    };
    const entry = {
      arm,
      disarm,
      dispose: () => {}
    };
    entry.dispose = () => {
      if (disposed) {
        return;
      }
      disposed = true;
      disarm();
      this.activeHandles.delete(entry);
    };
    this.activeHandles.add(entry);
    if (this.shouldDelayPlayback()) {
      arm();
    }
    return {
      unblock: entry.dispose,
      [Symbol.dispose]: entry.dispose
    };
  }
}
var roundTo4Digits = (timestamp) => {
  return Math.round(timestamp * 1000) / 1000;
};
var BUFFER_SIZE = 3;
var releaseCanvas = Symbol("releaseCanvas");
var releaseStableFrame = (frame2) => {
  frame2?.[releaseCanvas]?.();
};
var makeStableFramePool = () => {
  const availableCanvases = [];
  const makeStableFrame = (frame2) => {
    const { canvas } = frame2;
    const stableCanvas = availableCanvases.pop() ?? new OffscreenCanvas(canvas.width, canvas.height);
    if (stableCanvas.width !== canvas.width) {
      stableCanvas.width = canvas.width;
    }
    if (stableCanvas.height !== canvas.height) {
      stableCanvas.height = canvas.height;
    }
    const context = stableCanvas.getContext("2d");
    if (!context) {
      throw new Error("Could not create canvas context");
    }
    context.clearRect(0, 0, stableCanvas.width, stableCanvas.height);
    context.drawImage(canvas, 0, 0);
    let released = false;
    const stableFrame = {
      canvas: stableCanvas,
      duration: frame2.duration,
      timestamp: frame2.timestamp,
      [releaseCanvas]: () => {
        if (released) {
          return;
        }
        released = true;
        availableCanvases.push(stableCanvas);
      }
    };
    return stableFrame;
  };
  return { makeStableFrame };
};
var canvasesAheadOfTime = (videoSink, startTimestamp) => {
  const iterator = videoSink.canvases(startTimestamp);
  const buffer = [];
  let chaining = false;
  let reachedEnd = false;
  let closed = false;
  let inFlight = null;
  const stableFramePool = makeStableFramePool();
  const takeFrame = (frame2) => {
    return stableFramePool.makeStableFrame(frame2);
  };
  const fillNext = () => {
    if (chaining || reachedEnd || closed)
      return;
    if (buffer.length >= BUFFER_SIZE)
      return;
    chaining = true;
    const slot = { promise: iterator.next(), resolved: null };
    buffer.push(slot);
    inFlight = slot.promise.then((result) => {
      chaining = false;
      inFlight = null;
      if (result.done) {
        slot.resolved = result;
        reachedEnd = true;
        return;
      }
      if (closed) {
        slot.resolved = { done: true, value: undefined };
        return;
      }
      slot.resolved = { ...result, value: takeFrame(result.value) };
      fillNext();
    }, () => {
      chaining = false;
      inFlight = null;
    });
  };
  fillNext();
  const next = () => {
    const slot = buffer.shift();
    fillNext();
    if (!slot) {
      if (reachedEnd || closed) {
        return { type: "ready", frame: null };
      }
      const chain = inFlight;
      return {
        type: "pending",
        wait: async () => {
          await chain;
          const next2 = buffer.shift();
          fillNext();
          if (!next2)
            return null;
          if (next2.resolved) {
            return next2.resolved.done ? null : next2.resolved.value;
          }
          const result = await next2.promise;
          return result.done ? null : takeFrame(result.value);
        }
      };
    }
    if (slot.resolved) {
      if (slot.resolved.done) {
        return { type: "ready", frame: null };
      }
      return { type: "ready", frame: slot.resolved.value };
    }
    return {
      type: "pending",
      wait: async () => {
        const result = await slot.promise;
        if (slot.resolved) {
          return slot.resolved.done ? null : slot.resolved.value;
        }
        return result.done ? null : takeFrame(result.value);
      }
    };
  };
  const closeIterator = async () => {
    closed = true;
    for (const slot of buffer) {
      if (slot.resolved && !slot.resolved.done) {
        releaseStableFrame(slot.resolved.value);
      }
    }
    buffer.length = 0;
    await iterator.return();
  };
  return { next, closeIterator };
};
var makePrewarmedVideoIteratorCache = (videoSink) => {
  const prewarmedVideoIterators = new Map;
  const prewarmIteratorForLooping = ({ timeToSeek }) => {
    if (!prewarmedVideoIterators.has(timeToSeek)) {
      prewarmedVideoIterators.set(timeToSeek, canvasesAheadOfTime(videoSink, timeToSeek));
    }
  };
  const makeIteratorOrUsePrewarmed = (timeToSeek) => {
    const prewarmedIterator = prewarmedVideoIterators.get(timeToSeek);
    if (prewarmedIterator) {
      prewarmedVideoIterators.delete(timeToSeek);
      return prewarmedIterator;
    }
    return canvasesAheadOfTime(videoSink, timeToSeek);
  };
  const destroy = () => {
    for (const iterator of prewarmedVideoIterators.values()) {
      iterator.closeIterator();
    }
    prewarmedVideoIterators.clear();
  };
  return {
    prewarmIteratorForLooping,
    makeIteratorOrUsePrewarmed,
    destroy
  };
};
var createVideoIterator = async (timeToSeek, cache) => {
  let destroyed = false;
  const iterator = cache.makeIteratorOrUsePrewarmed(timeToSeek);
  let iteratorEnded = false;
  const firstAwait = iterator.next();
  const initialFrame = firstAwait && firstAwait.type === "ready" ? firstAwait.frame : await firstAwait.wait();
  let lastReturnedFrame = initialFrame;
  let peekedFrame = null;
  const setLastReturnedFrame = (frame2) => {
    if (lastReturnedFrame !== frame2) {
      releaseStableFrame(lastReturnedFrame);
    }
    lastReturnedFrame = frame2;
  };
  const setPeekedFrame = (frame2) => {
    peekedFrame = frame2;
    if (peekedFrame === null) {
      iteratorEnded = true;
    }
    return peekedFrame;
  };
  const peekIfReady = () => {
    if (peekedFrame) {
      return { type: "ready", frame: peekedFrame };
    }
    if (iteratorEnded) {
      return { type: "ready", frame: null };
    }
    const next = iterator.next();
    if (next.type === "ready") {
      return { type: "ready", frame: setPeekedFrame(next.frame) };
    }
    return {
      type: "pending",
      wait: next.wait
    };
  };
  const getFrameEndTimestampFromPeek = (frame2) => {
    return frame2 ? roundTo4Digits(frame2.timestamp) : Infinity;
  };
  const getFrameEndTimestamp = async ({
    pendingFrameBehavior,
    shouldContinue
  }) => {
    const peeked = peekIfReady();
    if (peeked.type === "ready") {
      return {
        type: "ready",
        timestamp: getFrameEndTimestampFromPeek(peeked.frame)
      };
    }
    if (pendingFrameBehavior === "restart-iterator") {
      return { type: "pending" };
    }
    if (!shouldContinue()) {
      return { type: "cancelled" };
    }
    const awaitedPeeked = setPeekedFrame(await peeked.wait());
    if (!shouldContinue()) {
      return { type: "cancelled" };
    }
    return {
      type: "ready",
      timestamp: getFrameEndTimestampFromPeek(awaitedPeeked)
    };
  };
  const getNextOrNullIfNotAvailable = () => {
    if (peekedFrame) {
      const frame2 = peekedFrame;
      setLastReturnedFrame(frame2);
      const retValue = {
        type: "got-frame-or-end",
        frame: frame2
      };
      peekedFrame = null;
      return retValue;
    }
    const next = iterator.next();
    if (next.type === "pending") {
      return {
        type: "need-to-wait-for-it",
        waitPromise: async () => {
          const res = await next.wait();
          if (res) {
            setLastReturnedFrame(res);
          } else {
            iteratorEnded = true;
          }
          return res;
        }
      };
    }
    if (next.frame) {
      setLastReturnedFrame(next.frame);
    } else {
      iteratorEnded = true;
    }
    return {
      type: "got-frame-or-end",
      frame: next.frame ?? null
    };
  };
  const destroy = () => {
    destroyed = true;
    releaseStableFrame(lastReturnedFrame);
    if (peekedFrame !== lastReturnedFrame) {
      releaseStableFrame(peekedFrame);
    }
    lastReturnedFrame = null;
    peekedFrame = null;
    iterator.closeIterator().catch(() => {
      return;
    });
  };
  const tryToSatisfySeek = async (time, options) => {
    if (!options.shouldContinue()) {
      return {
        type: "not-satisfied",
        reason: "seek was superseded"
      };
    }
    const timestamp = roundTo4Digits(time);
    if (lastReturnedFrame) {
      const frameTimestamp = roundTo4Digits(lastReturnedFrame.timestamp);
      if (timestamp < frameTimestamp) {
        const lastFrameWasInitialFrame = lastReturnedFrame === initialFrame;
        const firstFrameDoesSatisfy = lastFrameWasInitialFrame && roundTo4Digits(time) >= roundTo4Digits(timeToSeek);
        if (firstFrameDoesSatisfy) {
          return {
            type: "satisfied",
            frame: lastReturnedFrame
          };
        }
        return {
          type: "not-satisfied",
          reason: `iterator is too far, most recently returned ${frameTimestamp}`
        };
      }
      const frameEndTimestamp = await getFrameEndTimestamp({
        pendingFrameBehavior: options.pendingFrameBehavior,
        shouldContinue: options.shouldContinue
      });
      if (frameEndTimestamp.type === "cancelled") {
        return {
          type: "not-satisfied",
          reason: "seek was superseded"
        };
      }
      if (frameEndTimestamp.type === "pending") {
        return {
          type: "not-satisfied",
          reason: "iterator did not have next frame ready"
        };
      }
      if (frameTimestamp <= timestamp && frameEndTimestamp.timestamp > timestamp) {
        return {
          type: "satisfied",
          frame: lastReturnedFrame
        };
      }
    }
    if (iteratorEnded) {
      if (lastReturnedFrame) {
        return {
          type: "satisfied",
          frame: lastReturnedFrame
        };
      }
      return {
        type: "not-satisfied",
        reason: "iterator ended"
      };
    }
    while (true) {
      if (!options.shouldContinue()) {
        return {
          type: "not-satisfied",
          reason: "seek was superseded"
        };
      }
      const frame2 = getNextOrNullIfNotAvailable();
      if (frame2.type === "need-to-wait-for-it") {
        return {
          type: "not-satisfied",
          reason: "iterator did not have frame ready"
        };
      }
      if (frame2.type === "got-frame-or-end") {
        if (frame2.frame === null) {
          iteratorEnded = true;
          if (lastReturnedFrame) {
            return {
              type: "satisfied",
              frame: lastReturnedFrame
            };
          }
          return {
            type: "not-satisfied",
            reason: "iterator ended and did not have frame ready"
          };
        }
        const frameTimestamp = roundTo4Digits(frame2.frame.timestamp);
        const frameEndTimestamp = await getFrameEndTimestamp({
          pendingFrameBehavior: options.pendingFrameBehavior,
          shouldContinue: options.shouldContinue
        });
        if (frameEndTimestamp.type === "cancelled") {
          return {
            type: "not-satisfied",
            reason: "seek was superseded"
          };
        }
        if (frameEndTimestamp.type === "pending") {
          return {
            type: "not-satisfied",
            reason: "iterator did not have next frame ready"
          };
        }
        if (frameTimestamp <= timestamp && frameEndTimestamp.timestamp > timestamp) {
          return {
            type: "satisfied",
            frame: frame2.frame
          };
        }
        continue;
      }
      throw new Error("Unreachable");
    }
  };
  return {
    destroy,
    initialFrame,
    isDestroyed: () => {
      return destroyed;
    },
    tryToSatisfySeek
  };
};
var { runEffectChain } = Internals62;
var isSequentialMediaTimeAdvance = ({
  previousTime,
  newTime,
  fps,
  playbackRate,
  isPlaying
}) => {
  if (!isPlaying || newTime < previousTime) {
    return false;
  }
  const maximumSequentialAdvance = Math.abs(playbackRate) / fps;
  return roundTo4Digits(newTime - previousTime) <= roundTo4Digits(maximumSequentialAdvance);
};
var videoIteratorManager = async ({
  delayPlaybackHandleIfNotPremounting,
  canvas,
  context,
  drawDebugOverlay,
  logLevel,
  getOnVideoFrameCallback,
  videoTrack,
  getLoopSegmentMediaEndTimestamp,
  getStartTime,
  getIsLooping,
  getEffects,
  getEffectChainState
}) => {
  let videoIteratorsCreated = 0;
  let videoFrameIterator = null;
  let framesRendered = 0;
  let currentDelayHandle = null;
  let lastDrawnFrame = null;
  let currentSeek = null;
  const clearLastDrawnFrame = () => {
    lastDrawnFrame = null;
  };
  if (canvas) {
    const displayWidth = await videoTrack.getDisplayWidth();
    const displayHeight = await videoTrack.getDisplayHeight();
    if (canvas.width !== displayWidth || canvas.height !== displayHeight) {
      canvas.width = displayWidth;
      canvas.height = displayHeight;
    }
  }
  const canvasSink = new CanvasSink(videoTrack, {
    poolSize: 3,
    fit: "contain",
    alpha: true
  });
  const prewarmedVideoIteratorCache = makePrewarmedVideoIteratorCache(canvasSink);
  const paintFrame = async (frame2) => {
    if (context && canvas) {
      const effects = getEffects();
      const chainState = getEffectChainState(canvas.width, canvas.height);
      if (effects.length > 0 && chainState && canvas instanceof HTMLCanvasElement) {
        await runEffectChain({
          state: chainState,
          source: frame2.canvas,
          effects,
          output: canvas,
          width: canvas.width,
          height: canvas.height
        });
      } else {
        context.clearRect(0, 0, canvas.width, canvas.height);
        context.drawImage(frame2.canvas, 0, 0);
      }
    }
  };
  const drawFrame = async (frame2) => {
    await paintFrame(frame2);
    lastDrawnFrame = frame2;
    framesRendered++;
    drawDebugOverlay();
    const callback = getOnVideoFrameCallback();
    if (callback) {
      callback(frame2.canvas);
    }
    Internals62.Log.trace({ logLevel, tag: "@remotion/media" }, `[MediaPlayer] Drew frame ${frame2.timestamp.toFixed(3)}s`);
  };
  const redrawCurrentFrame = async () => {
    if (!lastDrawnFrame) {
      return;
    }
    await paintFrame(lastDrawnFrame);
    drawDebugOverlay();
    const callback = getOnVideoFrameCallback();
    if (callback) {
      callback(lastDrawnFrame.canvas);
    }
    Internals62.Log.trace({ logLevel, tag: "@remotion/media" }, `[MediaPlayer] Redrew frame ${lastDrawnFrame.timestamp.toFixed(3)}s with updated effects`);
  };
  const startVideoIterator = async (timeToSeek, nonce) => {
    let __stack = [];
    try {
      clearLastDrawnFrame();
      videoFrameIterator?.destroy();
      const delayHandle = __using(__stack, delayPlaybackHandleIfNotPremounting(), 0);
      currentDelayHandle = delayHandle;
      currentSeek = timeToSeek;
      const iterator = await createVideoIterator(timeToSeek, prewarmedVideoIteratorCache);
      videoIteratorsCreated++;
      videoFrameIterator = iterator;
      if (iterator.isDestroyed()) {
        return;
      }
      if (nonce.isStale()) {
        if (!videoFrameIterator.isDestroyed() && iterator.initialFrame) {
          await drawFrame(iterator.initialFrame);
        }
        return;
      }
      if (videoFrameIterator.isDestroyed()) {
        return;
      }
      if (!iterator.initialFrame) {
        return;
      }
      await drawFrame(iterator.initialFrame);
    } catch (_catch) {
      var _err = _catch, _hasErr = 1;
    } finally {
      __callDispose(__stack, _err, _hasErr);
    }
  };
  const seek2 = async ({
    newTime,
    nonce,
    fps,
    playbackRate,
    isPlaying
  }) => {
    if (!videoFrameIterator) {
      return;
    }
    if (currentSeek !== null && roundTo4Digits(currentSeek) === roundTo4Digits(newTime)) {
      return;
    }
    const previousTime = currentSeek;
    currentSeek = newTime;
    if (getIsLooping()) {
      if (getLoopSegmentMediaEndTimestamp() - newTime < 1) {
        prewarmedVideoIteratorCache.prewarmIteratorForLooping({
          timeToSeek: getStartTime()
        });
      }
    }
    const pendingFrameBehavior = previousTime !== null && isSequentialMediaTimeAdvance({
      previousTime,
      newTime,
      fps,
      playbackRate,
      isPlaying
    }) ? "wait" : "restart-iterator";
    const videoSatisfyResult = await videoFrameIterator.tryToSatisfySeek(newTime, {
      pendingFrameBehavior,
      shouldContinue: () => !nonce.isStale()
    });
    if (videoSatisfyResult.type === "satisfied") {
      await drawFrame(videoSatisfyResult.frame);
      return;
    }
    if (nonce.isStale()) {
      return;
    }
    await startVideoIterator(newTime, nonce);
  };
  return {
    startVideoIterator,
    getVideoIteratorsCreated: () => videoIteratorsCreated,
    seek: seek2,
    destroy: () => {
      clearLastDrawnFrame();
      prewarmedVideoIteratorCache.destroy();
      videoFrameIterator?.destroy();
      if (context && canvas) {
        context.clearRect(0, 0, canvas.width, canvas.height);
      }
      if (currentDelayHandle) {
        currentDelayHandle.unblock();
        currentDelayHandle = null;
      }
      videoFrameIterator = null;
    },
    getVideoFrameIterator: () => videoFrameIterator,
    drawFrame,
    redrawCurrentFrame,
    getFramesRendered: () => framesRendered
  };
};

class MediaPlayer {
  tagType;
  canvas;
  context;
  src;
  logLevel;
  playbackRate;
  toneFrequency;
  globalPlaybackRate;
  audioStreamIndex;
  sharedAudioContext;
  audioIteratorManager = null;
  videoIteratorManager = null;
  playing = false;
  loop = false;
  fps;
  trimBefore;
  trimAfter;
  sequenceDurationInFrames;
  sequenceOffset;
  totalDuration;
  debugOverlay = false;
  nonceManager;
  onVideoFrameCallback = null;
  getEffects;
  getEffectChainState;
  initializationPromise = null;
  premountAwareDelayPlayback;
  seekPromiseChain = Promise.resolve();
  constructor({
    canvas,
    src,
    logLevel,
    sharedAudioContext,
    loop,
    trimBefore,
    trimAfter,
    playbackRate,
    toneFrequency,
    globalPlaybackRate,
    audioStreamIndex,
    fps,
    debugOverlay,
    bufferState,
    isPremounting,
    isPostmounting,
    durationInFrames,
    onVideoFrameCallback,
    playing,
    sequenceOffset,
    credentials,
    requestInit,
    tagType,
    getEffects,
    getEffectChainState
  }) {
    this.canvas = canvas ?? null;
    this.src = src;
    this.logLevel = logLevel;
    this.sharedAudioContext = sharedAudioContext;
    this.playbackRate = playbackRate;
    this.toneFrequency = toneFrequency;
    this.globalPlaybackRate = globalPlaybackRate;
    this.loop = loop;
    this.trimBefore = trimBefore;
    this.trimAfter = trimAfter;
    this.audioStreamIndex = audioStreamIndex;
    this.fps = fps;
    this.debugOverlay = debugOverlay;
    this.premountAwareDelayPlayback = new PremountAwareDelayPlayback({
      bufferState,
      isPremounting,
      isPostmounting
    });
    this.sequenceDurationInFrames = durationInFrames;
    this.nonceManager = makeNonceManager();
    this.onVideoFrameCallback = onVideoFrameCallback;
    this.playing = playing;
    this.sequenceOffset = sequenceOffset;
    const { input: input2, getDuration, release } = acquireSharedInput({
      src: this.src,
      credentials,
      requestInit,
      logLevel
    });
    this.input = input2;
    this.getDuration = getDuration;
    this.releaseInput = release;
    this.tagType = tagType;
    this.getEffects = getEffects;
    this.getEffectChainState = getEffectChainState;
    if (canvas) {
      const context = canvas.getContext("2d", {
        alpha: true
      });
      if (!context) {
        throw new Error("Could not get 2D context from canvas");
      }
      this.context = context;
    } else {
      this.context = null;
    }
  }
  input;
  getDuration;
  releaseInput;
  disposed = false;
  isDisposalError() {
    return this.disposed || this.input.disposed === true;
  }
  initialize(startTimeUnresolved, initialMuted, initialVolume) {
    const promise = this._initialize(startTimeUnresolved, initialMuted, initialVolume);
    this.initializationPromise = promise;
    this.seekPromiseChain = promise;
    return promise;
  }
  getStartTime() {
    return (this.trimBefore ?? 0) / this.fps;
  }
  getSequenceEndTimestamp() {
    return this.sequenceDurationInFrames / this.fps * this.playbackRate + this.getStartTime();
  }
  getSequenceDurationInSeconds() {
    return this.sequenceDurationInFrames / this.fps;
  }
  getMediaEndTimestamp() {
    return calculateEndTime({
      mediaDurationInSeconds: this.totalDuration,
      ifNoMediaDuration: "fail",
      src: this.src,
      trimAfter: this.trimAfter,
      trimBefore: this.trimBefore,
      fps: this.fps
    });
  }
  getLoopSegmentMediaEndTimestamp() {
    return Math.min(this.getMediaEndTimestamp(), this.getSequenceEndTimestamp());
  }
  async _initialize(startTimeUnresolved, initialMuted, initialVolume) {
    let __stack = [];
    try {
      const _ = __using(__stack, this.delayPlaybackHandleIfNotPremounting(), 0);
      try {
        if (this.isDisposalError()) {
          return { type: "disposed" };
        }
        try {
          await this.input.getFormat();
        } catch (error) {
          if (this.isDisposalError()) {
            return { type: "disposed" };
          }
          const err = error;
          if (isNetworkError(err)) {
            throw error;
          }
          Internals72.Log.error({ logLevel: this.logLevel, tag: "@remotion/media" }, `[MediaPlayer] Failed to recognize format for ${this.src}`, error);
          return { type: "unknown-container-format" };
        }
        const [durationInSeconds, videoTrack, audioTracks] = await Promise.all([
          this.getDuration(),
          this.input.getPrimaryVideoTrack(),
          this.input.getAudioTracks()
        ]);
        if (this.isDisposalError()) {
          return { type: "disposed" };
        }
        this.totalDuration = durationInSeconds;
        const audioTrack = await resolveAudioTrack({
          videoTrack,
          audioTracks,
          audioStreamIndex: this.audioStreamIndex
        });
        if (!videoTrack && !audioTrack) {
          return { type: "no-tracks" };
        }
        if (videoTrack && this.tagType === "video") {
          if (await videoTrack.isLive()) {
            throw new Error("Live streams are not currently supported by Remotion. Sorry! Source: " + this.src);
          }
          if (await videoTrack.isRelativeToUnixEpoch()) {
            throw new Error("Streams with UNIX timestamps are not currently supported by Remotion. Sorry! Source: " + this.src);
          }
          const canDecode = await videoTrack.canDecode();
          if (!canDecode) {
            if (videoTrack.codec === "prores") {
              return { type: "cannot-decode-prores" };
            }
            return { type: "cannot-decode" };
          }
          if (this.isDisposalError()) {
            return { type: "disposed" };
          }
          this.videoIteratorManager = await videoIteratorManager({
            videoTrack,
            delayPlaybackHandleIfNotPremounting: this.delayPlaybackHandleIfNotPremounting,
            context: this.context,
            canvas: this.canvas,
            getOnVideoFrameCallback: () => this.onVideoFrameCallback,
            logLevel: this.logLevel,
            drawDebugOverlay: this.drawDebugOverlay,
            getLoopSegmentMediaEndTimestamp: () => this.getLoopSegmentMediaEndTimestamp(),
            getStartTime: () => this.getStartTime(),
            getIsLooping: () => this.loop,
            getEffects: this.getEffects,
            getEffectChainState: this.getEffectChainState
          });
        }
        const startTime = this.getTrimmedTime(startTimeUnresolved);
        if (startTime === null) {
          throw new Error(`should have asserted that the time is not null`);
        }
        if (audioTrack && this.sharedAudioContext) {
          if (await audioTrack.isLive()) {
            throw new Error("Live streams are not currently supported by Remotion. Sorry! Source: " + this.src);
          }
          if (await audioTrack.isRelativeToUnixEpoch()) {
            throw new Error("Streams with UNIX timestamps are not currently supported by Remotion. Sorry! Source: " + this.src);
          }
          const canDecode = await audioTrack.canDecode();
          if (!canDecode) {
            return { type: "cannot-decode" };
          }
          if (this.isDisposalError()) {
            return { type: "disposed" };
          }
          this.audioIteratorManager = audioIteratorManager({
            audioTrack,
            delayPlaybackHandleIfNotPremounting: this.delayPlaybackHandleIfNotPremounting,
            sharedAudioContext: this.sharedAudioContext,
            getMediaEndTimestamp: () => this.getMediaEndTimestamp(),
            getSequenceEndTimestamp: () => this.getSequenceEndTimestamp(),
            getStartTime: () => this.getStartTime(),
            initialMuted,
            initialVolume,
            toneFrequency: this.toneFrequency,
            drawDebugOverlay: this.drawDebugOverlay,
            getSequenceDurationInSeconds: () => this.getSequenceDurationInSeconds()
          });
        }
        const nonce = this.nonceManager.createAsyncOperation();
        try {
          await Promise.all([
            this.audioIteratorManager ? this.audioIteratorManager.startAudioIterator({
              nonce,
              playbackRate: this.playbackRate * this.globalPlaybackRate,
              startFromSecond: startTime,
              unloopedStartFromSecond: startTimeUnresolved,
              scheduleAudioNode: this.scheduleAudioNode,
              getTargetTime: this.getTargetTime,
              logLevel: this.logLevel,
              loop: this.loop,
              unscheduleAudioNode: this.sharedAudioContext.unscheduleAudioNode,
              getAudioContextCurrentTimeMockedInTest: () => this.sharedAudioContext.audioContext.currentTime
            }) : Promise.resolve(),
            this.videoIteratorManager ? this.videoIteratorManager.startVideoIterator(startTime, nonce) : Promise.resolve()
          ]);
        } catch (error) {
          if (this.isDisposalError()) {
            return { type: "disposed" };
          }
          Internals72.Log.error({ logLevel: this.logLevel, tag: "@remotion/media" }, "[MediaPlayer] Failed to start audio and video iterators", error);
        }
        return { type: "success", durationInSeconds };
      } catch (error) {
        const err = error;
        if (isNetworkError(err)) {
          Internals72.Log.error({ logLevel: this.logLevel, tag: "@remotion/media" }, `[MediaPlayer] Network/CORS error for ${this.src}`, err);
          return { type: "network-error" };
        }
        Internals72.Log.error({ logLevel: this.logLevel, tag: "@remotion/media" }, "[MediaPlayer] Failed to initialize", error);
        throw error;
      }
    } catch (_catch) {
      var _err = _catch, _hasErr = 1;
    } finally {
      __callDispose(__stack, _err, _hasErr);
    }
  }
  seekToWithQueue = async (newTime, unloopedNewTime) => {
    const nonce = this.nonceManager.createAsyncOperation();
    await this.seekPromiseChain;
    this.seekPromiseChain = this.seekToDoNotCallDirectly(newTime, unloopedNewTime, nonce);
    await this.seekPromiseChain;
  };
  async seekTo(time) {
    const newTime = this.getTrimmedTime(time);
    if (newTime === null) {
      throw new Error(`should have asserted that the time is not null`);
    }
    await this.seekToWithQueue(newTime, time);
  }
  async seekToDoNotCallDirectly(newTime, unloopedNewTime, nonce) {
    if (nonce.isStale()) {
      return;
    }
    try {
      await Promise.all([
        this.videoIteratorManager?.seek({
          newTime,
          nonce,
          fps: this.fps,
          playbackRate: this.playbackRate,
          isPlaying: this.playing
        }),
        this.audioIteratorManager?.seek({
          newTime,
          unloopedNewTime,
          nonce,
          playbackRate: this.playbackRate * this.globalPlaybackRate,
          localPlaybackRate: this.playbackRate,
          getTargetTime: this.getTargetTime,
          logLevel: this.logLevel,
          loop: this.loop,
          trimBefore: this.trimBefore,
          trimAfter: this.trimAfter,
          sequenceOffset: this.sequenceOffset,
          sequenceDurationInFrames: this.sequenceDurationInFrames,
          fps: this.fps,
          scheduleAudioNode: this.scheduleAudioNode,
          getAudioContextCurrentTimeMockedInTest: () => this.sharedAudioContext.audioContext.currentTime
        })
      ]);
    } catch (error) {
      if (this.isDisposalError()) {
        return;
      }
      throw error;
    }
  }
  play() {
    if (this.playing) {
      return;
    }
    this.playing = true;
    this.drawDebugOverlay();
  }
  delayPlaybackHandleIfNotPremounting = () => {
    return this.premountAwareDelayPlayback.createHandle();
  };
  pause() {
    if (!this.playing) {
      return;
    }
    this.playing = false;
    this.drawDebugOverlay();
  }
  setMuted(muted) {
    this.audioIteratorManager?.setMuted(muted);
  }
  setVolume(volume) {
    if (!this.audioIteratorManager) {
      return;
    }
    this.audioIteratorManager.setVolume(volume);
  }
  getTrimmedTime(unloopedTimeInSeconds) {
    return getTimeInSeconds({
      unloopedTimeInSeconds,
      playbackRate: this.playbackRate,
      loop: this.loop,
      trimBefore: this.trimBefore,
      trimAfter: this.trimAfter,
      mediaDurationInSeconds: this.totalDuration ?? null,
      fps: this.fps,
      ifNoMediaDuration: "infinity",
      src: this.src
    });
  }
  async setTrimBefore(trimBefore, unloopedTimeInSeconds) {
    if (this.trimBefore !== trimBefore) {
      this.trimBefore = trimBefore;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setTrimAfter(trimAfter, unloopedTimeInSeconds) {
    if (this.trimAfter !== trimAfter) {
      this.trimAfter = trimAfter;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  setDebugOverlay(debugOverlay) {
    this.debugOverlay = debugOverlay;
  }
  async setPlaybackRate(rate, unloopedTimeInSeconds) {
    const previousRate = this.playbackRate;
    if (previousRate !== rate) {
      this.playbackRate = rate;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setToneFrequency(toneFrequency, unloopedTimeInSeconds) {
    if (this.toneFrequency !== toneFrequency) {
      this.toneFrequency = toneFrequency;
      if (!this.audioIteratorManager) {
        return;
      }
      this.audioIteratorManager.setToneFrequency(toneFrequency);
      this.audioIteratorManager.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setGlobalPlaybackRate(rate, unloopedTimeInSeconds) {
    const previousRate = this.globalPlaybackRate;
    if (previousRate !== rate) {
      this.globalPlaybackRate = rate;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setFps(fps, unloopedTimeInSeconds) {
    const previousFps = this.fps;
    if (previousFps !== fps) {
      this.fps = fps;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  setIsPremounting(isPremounting) {
    this.premountAwareDelayPlayback.setIsPremounting(isPremounting);
    processNext();
  }
  setIsPostmounting(isPostmounting) {
    this.premountAwareDelayPlayback.setIsPostmounting(isPostmounting);
    processNext();
  }
  async setLoop(loop, unloopedTimeInSeconds) {
    const previousLoop = this.loop;
    if (previousLoop !== loop) {
      this.loop = loop;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setSequenceOffset(offset, unloopedTimeInSeconds) {
    const previousOffset = this.sequenceOffset;
    if (previousOffset !== offset) {
      this.sequenceOffset = offset;
      this.audioIteratorManager?.destroyIterator();
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async setSequenceDurationInFrames(sequenceDurationInFrames, unloopedTimeInSeconds) {
    const previousDuration = this.sequenceDurationInFrames;
    if (previousDuration !== sequenceDurationInFrames) {
      this.sequenceDurationInFrames = sequenceDurationInFrames;
      await this.seekTo(unloopedTimeInSeconds);
    }
  }
  async dispose() {
    if (this.disposed) {
      return;
    }
    this.disposed = true;
    if (this.initializationPromise) {
      try {
        await this.initializationPromise;
      } catch {}
    }
    this.nonceManager.createAsyncOperation();
    this.videoIteratorManager?.destroy();
    this.audioIteratorManager?.destroyIterator();
    this.releaseInput();
  }
  getTargetTime = (mediaTimestamp, currentTime) => {
    if (!this.sharedAudioContext) {
      throw new Error("Shared audio context not found");
    }
    const globalTime = (currentTime - this.sharedAudioContext.audioSyncAnchor.value) * this.globalPlaybackRate;
    const timeInSeconds = globalTime - this.sequenceOffset;
    const anchor = this.loop ? this.audioIteratorManager?.getCurrentAnchor() ?? null : null;
    const localTime = anchor ? anchorToContinuousTime({
      anchor,
      unloopedTimeInSeconds: timeInSeconds,
      playbackRate: this.playbackRate
    }) : this.getTrimmedTime(timeInSeconds);
    if (localTime === null) {
      return null;
    }
    const targetTime = (mediaTimestamp - localTime) / (this.playbackRate * this.globalPlaybackRate);
    return targetTime;
  };
  scheduleAudioNode = (node, mediaTimestamp, originalUnloopedMediaTimestamp, sourceOffsetInSeconds, sourceDurationInSeconds) => {
    if (!this.sharedAudioContext) {
      throw new Error("Shared audio context not found");
    }
    const targetTime = this.getTargetTime(mediaTimestamp, this.sharedAudioContext.audioContext.currentTime);
    const combinedPlaybackRate = this.playbackRate * this.globalPlaybackRate;
    if (targetTime === null) {
      return {
        type: "not-started",
        reason: "no target for" + mediaTimestamp.toFixed(3) + "," + this.sharedAudioContext.audioContext.currentTime.toFixed(3)
      };
    }
    const sequenceStartTime = this.getStartTime();
    const offset = getTrimStartForAudioNode({
      mediaTimestamp,
      targetTime,
      sequenceStartTime,
      combinedPlaybackRate,
      sourceStartOffsetInSeconds: sourceOffsetInSeconds
    });
    const duration = getDurationOfNode({
      sourceDurationInSeconds,
      sourceOffsetInSeconds,
      offset
    });
    const scheduledTime = getScheduledTime({
      mediaTimestamp,
      targetTime,
      sequenceStartTime,
      currentTime: this.sharedAudioContext.audioContext.currentTime
    });
    return this.sharedAudioContext.scheduleAudioNode({
      node,
      mediaTimestamp,
      sourceOffset: sourceOffsetInSeconds,
      scheduledTime,
      duration,
      offset,
      originalUnloopedMediaTimestamp
    });
  };
  setVideoFrameCallback(callback) {
    this.onVideoFrameCallback = callback;
  }
  async redrawVideoEffects() {
    await this.videoIteratorManager?.redrawCurrentFrame();
  }
  drawDebugOverlay = () => {
    if (!this.debugOverlay)
      return;
    if (this.context && this.canvas) {
      drawPreviewOverlay({
        context: this.context,
        audioTime: this.sharedAudioContext?.audioContext.currentTime ?? null,
        audioContextState: this.sharedAudioContext?.audioContext.state ?? null,
        audioSyncAnchor: this.sharedAudioContext?.audioSyncAnchor ?? null,
        audioIteratorManager: this.audioIteratorManager,
        playing: this.playing,
        videoIteratorManager: this.videoIteratorManager,
        playbackRate: this.playbackRate * this.globalPlaybackRate
      });
    }
  };
  audioSyncAnchorChanged = async (unloopedTimeInSeconds) => {
    if (!this.audioIteratorManager) {
      return;
    }
    this.audioIteratorManager.destroyIterator();
    await this.seekTo(unloopedTimeInSeconds);
  };
}
var callOnErrorAndResolve = ({
  onError,
  error,
  disallowFallback,
  isClientSideRendering,
  clientSideError
}) => {
  const result = onError?.(error);
  if (isClientSideRendering) {
    return ["fail", clientSideError];
  }
  if (result) {
    return [result, error];
  }
  if (disallowFallback) {
    return ["fail", error];
  }
  return ["fallback", error];
};
var useCommonEffects = ({
  mediaPlayerRef,
  mediaPlayerReady,
  currentTimeRef,
  playing,
  isPlayerBuffering,
  frame: frame2,
  trimBefore,
  trimAfter,
  effectiveMuted,
  userPreferredVolume,
  playbackRate,
  toneFrequency,
  globalPlaybackRate,
  fps,
  sequenceOffset,
  loop,
  durationInFrames,
  isPremounting,
  isPostmounting,
  currentTime,
  logLevel,
  label: label3
}) => {
  const sharedAudioContext = useContext20(Internals82.SharedAudioContext);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer)
      return;
    if (playing && !isPlayerBuffering) {
      mediaPlayer.play();
    } else {
      mediaPlayer.pause();
    }
  }, [
    isPlayerBuffering,
    playing,
    logLevel,
    mediaPlayerReady,
    frame2,
    mediaPlayerRef
  ]);
  useLayoutEffect3(() => {
    if (!sharedAudioContext)
      return;
    const { remove } = sharedAudioContext.audioSyncAnchorEmitter.subscribe((event) => {
      if (event === "changed") {
        mediaPlayerRef.current?.audioSyncAnchorChanged(currentTimeRef.current).catch(() => {});
      }
    });
    return () => {
      remove();
    };
  }, [sharedAudioContext, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setTrimBefore(trimBefore, currentTimeRef.current);
  }, [trimBefore, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setTrimAfter(trimAfter, currentTimeRef.current);
  }, [trimAfter, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady)
      return;
    mediaPlayer.setMuted(effectiveMuted);
  }, [effectiveMuted, mediaPlayerReady, mediaPlayerRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setVolume(userPreferredVolume);
  }, [userPreferredVolume, mediaPlayerReady, mediaPlayerRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setPlaybackRate(playbackRate, currentTimeRef.current);
  }, [playbackRate, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setToneFrequency(toneFrequency, currentTimeRef.current);
  }, [toneFrequency, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setGlobalPlaybackRate(globalPlaybackRate, currentTimeRef.current);
  }, [globalPlaybackRate, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setLoop(loop, currentTimeRef.current);
  }, [loop, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setSequenceDurationInFrames(durationInFrames, currentTimeRef.current);
  }, [durationInFrames, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer) {
      return;
    }
    mediaPlayer.setIsPremounting(isPremounting);
  }, [isPremounting, mediaPlayerReady, mediaPlayerRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer) {
      return;
    }
    mediaPlayer.setIsPostmounting(isPostmounting);
  }, [isPostmounting, mediaPlayerReady, mediaPlayerRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setFps(fps, currentTimeRef.current);
  }, [fps, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setSequenceOffset(sequenceOffset, currentTimeRef.current);
  }, [sequenceOffset, mediaPlayerReady, mediaPlayerRef, currentTimeRef]);
  useLayoutEffect3(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady)
      return;
    mediaPlayer.seekTo(currentTime).catch(() => {});
    Internals82.Log.trace({ logLevel, tag: "@remotion/media" }, `[${label3}] Updating target time to ${currentTime.toFixed(3)}s`);
  }, [currentTime, logLevel, mediaPlayerReady, label3, mediaPlayerRef]);
};
var {
  useUnsafeVideoConfig,
  SharedAudioContext,
  useMediaVolumeState,
  useFrameForVolumeProp,
  evaluateVolume,
  warnAboutTooHighVolume,
  usePreload,
  SequenceContext,
  usePlaying,
  useBuffering,
  useMediaAudioState
} = Internals92;
var AudioForPreviewAssertedShowing = ({
  src,
  playbackRate,
  logLevel,
  muted,
  volume,
  loopVolumeCurveBehavior,
  loop,
  trimAfter,
  trimBefore,
  name,
  showInTimeline,
  _remotionInternalStack,
  disallowFallbackToHtml5Audio,
  toneFrequency,
  audioStreamIndex,
  fallbackHtml5AudioProps,
  onError,
  credentials,
  requestInit,
  setMediaDurationInSeconds,
  style: style4
}) => {
  const videoConfig = useUnsafeVideoConfig();
  const frame2 = useCurrentFrame();
  const mediaPlayerRef = useRef9(null);
  const initialTrimBeforeRef = useRef9(trimBefore);
  const initialTrimAfterRef = useRef9(trimAfter);
  const initialToneFrequencyRef = useRef9(toneFrequency ?? 1);
  const [initialRequestInit] = useState11(requestInit);
  const [mediaPlayerReady, setMediaPlayerReady] = useState11(false);
  const [shouldFallbackToNativeAudio, setShouldFallbackToNativeAudio] = useState11(false);
  const playing = usePlaying();
  const { playbackRate: globalPlaybackRate } = Internals92.usePlaybackRate();
  const sharedAudioContext = useContext22(SharedAudioContext);
  const buffer = useBufferState();
  const [mediaVolume] = useMediaVolumeState();
  const volumePropFrame = useFrameForVolumeProp(loopVolumeCurveBehavior ?? "repeat");
  const userPreferredVolume = evaluateVolume({
    frame: volumePropFrame,
    volume,
    mediaVolume
  });
  warnAboutTooHighVolume(userPreferredVolume);
  if (!videoConfig) {
    throw new Error("No video config found");
  }
  if (!src) {
    throw new TypeError("No `src` was passed to <NewAudioForPreview>.");
  }
  const currentTime = frame2 / videoConfig.fps;
  const currentTimeRef = useRef9(currentTime);
  currentTimeRef.current = currentTime;
  const preloadedSrc = usePreload(src);
  const parentSequence = useContext22(SequenceContext);
  const isPremounting = Boolean(parentSequence?.premounting);
  const isPostmounting = Boolean(parentSequence?.postmounting);
  const sequenceOffset = (parentSequence?.absoluteFrom ?? 0) / videoConfig.fps;
  const { isMutedForPlayback: effectiveMuted } = useMediaAudioState({
    muted,
    volume: userPreferredVolume,
    audioEnabled: true
  });
  const isPlayerBuffering = useBuffering();
  const initialPlaying = useRef9(playing && !isPlayerBuffering);
  const initialIsPremounting = useRef9(isPremounting);
  const initialIsPostmounting = useRef9(isPostmounting);
  const initialGlobalPlaybackRate = useRef9(globalPlaybackRate);
  const initialPlaybackRate = useRef9(playbackRate);
  const initialMuted = useRef9(effectiveMuted);
  const initialVolume = useRef9(userPreferredVolume);
  const initialDurationInFrames = useRef9(videoConfig.durationInFrames);
  const initialSequenceOffset = useRef9(sequenceOffset);
  useCommonEffects({
    mediaPlayerRef,
    mediaPlayerReady,
    currentTimeRef,
    playing,
    isPlayerBuffering,
    frame: frame2,
    trimBefore,
    trimAfter,
    effectiveMuted,
    userPreferredVolume,
    playbackRate,
    toneFrequency: toneFrequency ?? 1,
    globalPlaybackRate,
    fps: videoConfig.fps,
    sequenceOffset,
    loop,
    durationInFrames: videoConfig.durationInFrames,
    isPremounting,
    isPostmounting,
    currentTime,
    logLevel,
    label: "AudioForPreview"
  });
  useEffect16(() => {
    if (!sharedAudioContext)
      return;
    if (!sharedAudioContext.audioContext)
      return;
    const {
      audioContext,
      gainNode,
      audioSyncAnchor,
      scheduleAudioNode,
      unscheduleAudioNode
    } = sharedAudioContext;
    if (!gainNode) {
      return;
    }
    try {
      const player = new MediaPlayer({
        src: preloadedSrc,
        logLevel,
        sharedAudioContext: {
          audioContext,
          gainNode,
          audioSyncAnchor,
          scheduleAudioNode,
          unscheduleAudioNode
        },
        loop,
        trimAfter: initialTrimAfterRef.current,
        trimBefore: initialTrimBeforeRef.current,
        fps: videoConfig.fps,
        canvas: null,
        playbackRate: initialPlaybackRate.current,
        toneFrequency: initialToneFrequencyRef.current,
        audioStreamIndex: audioStreamIndex ?? null,
        debugOverlay: false,
        bufferState: buffer,
        isPostmounting: initialIsPostmounting.current,
        isPremounting: initialIsPremounting.current,
        globalPlaybackRate: initialGlobalPlaybackRate.current,
        durationInFrames: initialDurationInFrames.current,
        onVideoFrameCallback: null,
        playing: initialPlaying.current,
        sequenceOffset: initialSequenceOffset.current,
        credentials,
        requestInit: initialRequestInit,
        tagType: "audio",
        getEffects: () => [],
        getEffectChainState: () => null
      });
      mediaPlayerRef.current = player;
      player.initialize(currentTimeRef.current, initialMuted.current, initialVolume.current).then((result) => {
        if (result.type === "disposed") {
          return;
        }
        const handleError = (error, fallbackMessage) => {
          const [action, errorToUse] = callOnErrorAndResolve({
            onError,
            error,
            disallowFallback: disallowFallbackToHtml5Audio,
            isClientSideRendering: false,
            clientSideError: error
          });
          if (action === "fail") {
            throw errorToUse;
          } else {
            Internals92.Log.warn({ logLevel, tag: "@remotion/media" }, fallbackMessage);
            setShouldFallbackToNativeAudio(true);
          }
        };
        if (result.type === "unknown-container-format") {
          handleError(new Error(`Unknown container format ${preloadedSrc}.`), `Unknown container format for ${preloadedSrc} (Supported formats: https://www.remotion.dev/docs/mediabunny/formats), falling back to <Html5Audio>`);
          return;
        }
        if (result.type === "network-error") {
          handleError(new Error(`Network error fetching ${preloadedSrc}.`), `Network error fetching ${preloadedSrc}, falling back to <Html5Audio>`);
          return;
        }
        if (result.type === "cannot-decode") {
          handleError(new Error(`Cannot decode ${preloadedSrc}.`), `Cannot decode ${preloadedSrc}, falling back to <Html5Audio>`);
          return;
        }
        if (result.type === "no-tracks") {
          handleError(new Error(`No video or audio tracks found for ${preloadedSrc}.`), `No video or audio tracks found for ${preloadedSrc}, falling back to <Html5Audio>`);
          return;
        }
        if (result.type === "cannot-decode-prores") {
          throw new Error(`Encountered ProRes media for ${preloadedSrc}. But this should never happen, since you used the <Audio> tag. Please report this as a bug.`);
        }
        if (result.type === "success") {
          setMediaPlayerReady(true);
          setMediaDurationInSeconds(result.durationInSeconds);
          Internals92.Log.trace({ logLevel, tag: "@remotion/media" }, `[AudioForPreview] MediaPlayer initialized successfully`);
        }
      }).catch((error) => {
        const [action, errorToUse] = callOnErrorAndResolve({
          onError,
          error,
          disallowFallback: disallowFallbackToHtml5Audio,
          isClientSideRendering: false,
          clientSideError: error
        });
        if (action === "fail") {
          throw errorToUse;
        } else {
          Internals92.Log.error({ logLevel, tag: "@remotion/media" }, "[AudioForPreview] Failed to initialize MediaPlayer", error);
          setShouldFallbackToNativeAudio(true);
        }
      });
    } catch (error) {
      const [action, errorToUse] = callOnErrorAndResolve({
        error,
        onError,
        disallowFallback: disallowFallbackToHtml5Audio,
        isClientSideRendering: false,
        clientSideError: error
      });
      if (action === "fail") {
        throw errorToUse;
      }
      Internals92.Log.error({ logLevel, tag: "@remotion/media" }, "[AudioForPreview] MediaPlayer initialization failed", errorToUse);
      setShouldFallbackToNativeAudio(true);
    }
    return () => {
      if (mediaPlayerRef.current) {
        Internals92.Log.trace({ logLevel, tag: "@remotion/media" }, `[AudioForPreview] Disposing MediaPlayer`);
        mediaPlayerRef.current.dispose();
        mediaPlayerRef.current = null;
      }
      setMediaPlayerReady(false);
      setShouldFallbackToNativeAudio(false);
    };
  }, [
    preloadedSrc,
    logLevel,
    sharedAudioContext,
    loop,
    videoConfig.fps,
    audioStreamIndex,
    disallowFallbackToHtml5Audio,
    buffer,
    onError,
    credentials,
    initialRequestInit,
    setMediaDurationInSeconds
  ]);
  if (shouldFallbackToNativeAudio && !disallowFallbackToHtml5Audio) {
    return /* @__PURE__ */ jsx48(RemotionAudio, {
      src,
      muted: effectiveMuted,
      volume,
      startFrom: trimBefore,
      endAt: trimAfter,
      playbackRate,
      loopVolumeCurveBehavior,
      name,
      loop,
      showInTimeline,
      style: style4 ?? undefined,
      _remotionInternalStack: _remotionInternalStack ?? undefined,
      toneFrequency,
      audioStreamIndex,
      pauseWhenBuffering: fallbackHtml5AudioProps?.pauseWhenBuffering,
      crossOrigin: fallbackHtml5AudioProps?.crossOrigin,
      ...fallbackHtml5AudioProps
    });
  }
  return null;
};
var AudioForPreview = ({
  loop = false,
  src,
  logLevel,
  muted,
  name,
  volume,
  loopVolumeCurveBehavior,
  playbackRate = 1,
  trimAfter,
  trimBefore,
  showInTimeline,
  _remotionInternalStack,
  disallowFallbackToHtml5Audio,
  toneFrequency,
  audioStreamIndex,
  fallbackHtml5AudioProps,
  onError,
  credentials,
  requestInit,
  setMediaDurationInSeconds,
  style: style4
}) => {
  const preloadedSrc = usePreload(src);
  const defaultLogLevel = Internals92.useLogLevel();
  const frame2 = useCurrentFrame();
  const videoConfig = useVideoConfig();
  const currentTime = frame2 / videoConfig.fps;
  const showShow = useMemo24(() => {
    return getTimeInSeconds({
      unloopedTimeInSeconds: currentTime,
      playbackRate,
      loop,
      trimBefore,
      trimAfter,
      mediaDurationInSeconds: Infinity,
      fps: videoConfig.fps,
      ifNoMediaDuration: "infinity",
      src
    }) !== null;
  }, [
    currentTime,
    playbackRate,
    src,
    trimAfter,
    trimBefore,
    videoConfig.fps,
    loop
  ]);
  if (!showShow) {
    return null;
  }
  if (!setMediaDurationInSeconds) {
    throw new Error("setMediaDurationInSeconds is required");
  }
  return /* @__PURE__ */ jsx48(AudioForPreviewAssertedShowing, {
    audioStreamIndex,
    src: preloadedSrc,
    playbackRate,
    logLevel: logLevel ?? defaultLogLevel,
    muted: muted ?? false,
    volume: volume ?? 1,
    loopVolumeCurveBehavior: loopVolumeCurveBehavior ?? "repeat",
    loop,
    trimAfter,
    trimBefore,
    name,
    showInTimeline: showInTimeline ?? true,
    _remotionInternalStack,
    disallowFallbackToHtml5Audio: disallowFallbackToHtml5Audio ?? false,
    toneFrequency,
    onError,
    credentials,
    requestInit,
    fallbackHtml5AudioProps,
    setMediaDurationInSeconds,
    style: style4
  });
};
var makeSerializedQueue = () => {
  let tail = Promise.resolve(undefined);
  return (fn) => {
    const result = tail.then(() => fn());
    tail = result.then(() => {
      return;
    }, () => {
      return;
    });
    return result;
  };
};
var BYTES_PER_SAMPLE = {
  u8: 1,
  s16: 2,
  s32: 4,
  f32: 4,
  "u8-planar": 1,
  "s16-planar": 2,
  "s32-planar": 4,
  "f32-planar": 4
};
var getAudioSampleByteSize = (sample) => {
  return sample.numberOfFrames * sample.numberOfChannels * BYTES_PER_SAMPLE[sample.format];
};
var makeAudioCache = () => {
  const timestamps = [];
  const samples = {};
  const addFrame = (sample) => {
    timestamps.push(sample.timestamp);
    samples[sample.timestamp] = sample;
  };
  const clearBeforeThreshold = (threshold) => {
    for (const timestamp of timestamps.slice()) {
      const endTimestamp = timestamp + samples[timestamp].duration;
      if (endTimestamp < threshold) {
        const isLast = timestamp === timestamps[timestamps.length - 1];
        if (isLast) {
          continue;
        }
        samples[timestamp].close();
        delete samples[timestamp];
        timestamps.splice(timestamps.indexOf(timestamp), 1);
      }
    }
  };
  const deleteAll = () => {
    for (const timestamp of timestamps) {
      samples[timestamp].close();
      delete samples[timestamp];
    }
    timestamps.length = 0;
  };
  const getSamples = (timestamp, durationInSeconds) => {
    const selected = [];
    for (let i = 0;i < timestamps.length; i++) {
      const sampleTimestamp = timestamps[i];
      const sample = samples[sampleTimestamp];
      if (sample.timestamp + sample.duration - 0.0000000001 <= timestamp) {
        continue;
      }
      if (sample.timestamp >= timestamp + durationInSeconds - 0.0000000001) {
        break;
      }
      selected.push(sample);
    }
    return selected;
  };
  const getOpenTimestamps = () => {
    return timestamps;
  };
  const getTotalSize = () => {
    let total = 0;
    for (const timestamp of timestamps) {
      total += getAudioSampleByteSize(samples[timestamp]);
    }
    return total;
  };
  const getOldestTimestamp = () => {
    return timestamps[0];
  };
  const getNewestTimestamp = () => {
    if (timestamps.length === 0) {
      return null;
    }
    const sample = samples[timestamps[timestamps.length - 1]];
    return sample.timestamp + sample.duration;
  };
  return {
    addFrame,
    clearBeforeThreshold,
    deleteAll,
    getSamples,
    getOldestTimestamp,
    getNewestTimestamp,
    getOpenTimestamps,
    getTotalSize
  };
};
var EXTRA_THRESHOLD_IN_SECONDS = 1.5;
var safetyOutOfOrderThreshold = 0.2;
var warned = {};
var warnAboutMatroskaOnce = (src, logLevel) => {
  if (warned[src]) {
    return;
  }
  warned[src] = true;
  Internals102.Log.warn({ logLevel, tag: "@remotion/media" }, `Audio from ${src} will need to be read from the beginning. https://www.remotion.dev/docs/media/support#matroska-limitation`);
};
var makeAudioIterator2 = ({
  audioSampleSink,
  isMatroska,
  startTimestamp,
  src,
  actualMatroskaTimestamps,
  logLevel
}) => {
  const sampleIterator = audioSampleSink.samples(isMatroska ? 0 : Math.max(0, startTimestamp - EXTRA_THRESHOLD_IN_SECONDS));
  if (isMatroska) {
    warnAboutMatroskaOnce(src, logLevel);
  }
  let fullDuration = null;
  const cache = makeAudioCache();
  let lastUsed = Date.now();
  const getNextSample = async () => {
    lastUsed = Date.now();
    const { value: sample, done } = await sampleIterator.next();
    if (done) {
      fullDuration = cache.getNewestTimestamp();
      return null;
    }
    const realTimestamp = actualMatroskaTimestamps.getRealTimestamp(sample.timestamp);
    if (realTimestamp !== null && realTimestamp !== sample.timestamp) {
      sample.setTimestamp(realTimestamp);
    }
    actualMatroskaTimestamps.observeTimestamp(sample.timestamp);
    actualMatroskaTimestamps.observeTimestamp(sample.timestamp + sample.duration);
    cache.addFrame(sample);
    return sample;
  };
  const getSamples = async (timestamp, durationInSeconds) => {
    lastUsed = Date.now();
    if (fullDuration !== null && timestamp > fullDuration) {
      cache.clearBeforeThreshold(fullDuration - safetyOutOfOrderThreshold);
      return [];
    }
    const samples = cache.getSamples(timestamp, durationInSeconds);
    const newestTimestamp = cache.getNewestTimestamp();
    if (newestTimestamp !== null) {
      if (newestTimestamp >= timestamp + durationInSeconds - 0.0000000001) {
        return samples;
      }
    }
    while (true) {
      const sample = await getNextSample();
      const deleteBefore = fullDuration === null ? timestamp : Math.min(timestamp, fullDuration);
      cache.clearBeforeThreshold(deleteBefore - safetyOutOfOrderThreshold);
      if (sample === null) {
        break;
      }
      if (sample.timestamp + sample.duration - 0.0000000001 <= timestamp) {
        continue;
      }
      if (sample.timestamp >= timestamp + durationInSeconds - 0.0000000001) {
        break;
      }
      samples.push(sample);
    }
    return samples;
  };
  const logOpenFrames = () => {
    const openTimestamps = cache.getOpenTimestamps();
    if (openTimestamps.length > 0) {
      const first = openTimestamps[0];
      const last = openTimestamps[openTimestamps.length - 1];
      Internals102.Log.verbose({ logLevel, tag: "@remotion/media" }, "Open audio samples for src", src, `${first.toFixed(3)}...${last.toFixed(3)}`);
    }
  };
  const getCacheStats = () => {
    return {
      count: cache.getOpenTimestamps().length,
      size: cache.getTotalSize()
    };
  };
  const canSatisfyRequestedTime = (timestamp) => {
    const oldestTimestamp = cache.getOldestTimestamp() ?? startTimestamp;
    if (fullDuration !== null && timestamp > fullDuration) {
      return true;
    }
    return oldestTimestamp < timestamp && Math.abs(oldestTimestamp - timestamp) < 10;
  };
  const prepareForDeletion = () => {
    cache.deleteAll();
    sampleIterator.return().then((value) => {
      if (value.value) {
        value.value.close();
      }
    });
    fullDuration = null;
  };
  let op = Promise.resolve([]);
  return {
    src,
    getSamples: (ts, dur) => {
      op = op.then(() => getSamples(ts, dur));
      return op;
    },
    waitForCompletion: async () => {
      await op;
      return true;
    },
    canSatisfyRequestedTime,
    logOpenFrames,
    getCacheStats,
    getLastUsed: () => lastUsed,
    prepareForDeletion,
    startTimestamp,
    clearBeforeThreshold: cache.clearBeforeThreshold,
    getOldestTimestamp: cache.getOldestTimestamp,
    getNewestTimestamp: cache.getNewestTimestamp
  };
};
var makeAudioManager = ({
  getTotalCacheStats
}) => {
  const iterators = [];
  let disposed = false;
  const makeIterator = ({
    timeInSeconds,
    src,
    audioSampleSink,
    isMatroska,
    actualMatroskaTimestamps,
    logLevel
  }) => {
    const iterator = makeAudioIterator2({
      audioSampleSink,
      isMatroska,
      startTimestamp: timeInSeconds,
      src,
      actualMatroskaTimestamps,
      logLevel
    });
    iterators.push(iterator);
    return iterator;
  };
  const getIteratorMostInThePast = () => {
    let mostInThePast = null;
    let mostInThePastIterator = null;
    for (const iterator of iterators) {
      const lastUsed = iterator.getLastUsed();
      if (mostInThePast === null || lastUsed < mostInThePast) {
        mostInThePast = lastUsed;
        mostInThePastIterator = iterator;
      }
    }
    return mostInThePastIterator;
  };
  const deleteOldestIterator = () => {
    const iterator = getIteratorMostInThePast();
    if (iterator) {
      iterator.prepareForDeletion();
      iterators.splice(iterators.indexOf(iterator), 1);
    }
  };
  const deleteDuplicateIterators = (logLevel) => {
    const seenKeys = new Set;
    for (let i = 0;i < iterators.length; i++) {
      const iterator = iterators[i];
      const key4 = `${iterator.src}-${iterator.getOldestTimestamp()}-${iterator.getNewestTimestamp()}`;
      if (seenKeys.has(key4)) {
        iterator.prepareForDeletion();
        iterators.splice(iterators.indexOf(iterator), 1);
        Internals112.Log.verbose({ logLevel, tag: "@remotion/media" }, `Deleted duplicate iterator for ${iterator.src}`);
      }
      seenKeys.add(key4);
    }
  };
  const getIterator = async ({
    src,
    timeInSeconds,
    audioSampleSink,
    isMatroska,
    actualMatroskaTimestamps,
    logLevel,
    maxCacheSize
  }) => {
    if (disposed) {
      throw new Error("Media cache has already been disposed");
    }
    let attempts = 0;
    const maxAttempts = 3;
    while ((await getTotalCacheStats()).totalSize > maxCacheSize && attempts < maxAttempts) {
      deleteOldestIterator();
      attempts++;
    }
    if ((await getTotalCacheStats()).totalSize > maxCacheSize && attempts >= maxAttempts) {
      Internals112.Log.warn({ logLevel, tag: "@remotion/media" }, `Audio cache: Exceeded max cache size after ${maxAttempts} attempts. Still ${(await getTotalCacheStats()).totalSize} bytes used, target was ${maxCacheSize} bytes.`);
    }
    for (const iterator of iterators) {
      if (iterator.src === src && await iterator.waitForCompletion() && iterator.canSatisfyRequestedTime(timeInSeconds)) {
        return iterator;
      }
    }
    for (let i = 0;i < iterators.length; i++) {
      const iterator = iterators[i];
      if (iterator.src === src && iterator.startTimestamp === timeInSeconds) {
        iterator.prepareForDeletion();
        iterators.splice(iterators.indexOf(iterator), 1);
      }
    }
    deleteDuplicateIterators(logLevel);
    if (disposed) {
      throw new Error("Media cache has already been disposed");
    }
    return makeIterator({
      src,
      timeInSeconds,
      audioSampleSink,
      isMatroska,
      actualMatroskaTimestamps,
      logLevel
    });
  };
  const getCacheStats = () => {
    let totalCount = 0;
    let totalSize = 0;
    for (const iterator of iterators) {
      const { count, size: size2 } = iterator.getCacheStats();
      totalCount += count;
      totalSize += size2;
    }
    return { count: totalCount, totalSize };
  };
  const logOpenFrames = () => {
    for (const iterator of iterators) {
      iterator.logOpenFrames();
    }
  };
  const clearAll = () => {
    for (const iterator of iterators) {
      iterator.prepareForDeletion();
    }
    iterators.length = 0;
  };
  const dispose = () => {
    if (disposed) {
      return;
    }
    disposed = true;
    clearAll();
  };
  const enqueue = makeSerializedQueue();
  return {
    getIterator: ({
      src,
      timeInSeconds,
      audioSampleSink,
      isMatroska,
      actualMatroskaTimestamps,
      logLevel,
      maxCacheSize
    }) => {
      return enqueue(() => getIterator({
        src,
        timeInSeconds,
        audioSampleSink,
        isMatroska,
        actualMatroskaTimestamps,
        logLevel,
        maxCacheSize
      }));
    },
    getCacheStats,
    getIteratorMostInThePast,
    logOpenFrames,
    deleteDuplicateIterators,
    clearAll,
    dispose
  };
};
var browserCanUseWebGl2 = null;
var browserCanUseWebGl2Uncached = () => {
  const canvas = new OffscreenCanvas(1, 1);
  const context = canvas.getContext("webgl2");
  return context !== null;
};
var canBrowserUseWebGl2 = () => {
  if (browserCanUseWebGl2 !== null) {
    return browserCanUseWebGl2;
  }
  browserCanUseWebGl2 = browserCanUseWebGl2Uncached();
  return browserCanUseWebGl2;
};
var rememberActualMatroskaTimestamps = (isMatroska) => {
  const observations = [];
  const observeTimestamp = (startTime) => {
    if (!isMatroska) {
      return;
    }
    observations.push(startTime);
  };
  const getRealTimestamp = (observedTimestamp) => {
    if (!isMatroska) {
      return observedTimestamp;
    }
    return observations.find((observation) => Math.abs(observedTimestamp - observation) < 0.001) ?? null;
  };
  return {
    observeTimestamp,
    getRealTimestamp
  };
};
var getFormatOrNullOrNetworkError = async (input2) => {
  try {
    return await input2.getFormat();
  } catch (err) {
    if (isNetworkError(err)) {
      return "network-error";
    }
    return null;
  }
};
var makeSinks = (src, logLevel, credentials, requestInit) => {
  const resolvedRequestInit = resolveRequestInit({ credentials, requestInit });
  const input2 = new Input22({
    formats: ALL_FORMATS22,
    source: new UrlSource2(src, {
      handleUnhandledError: (error) => {
        Internals122.Log.warn({ logLevel, tag: "@remotion/media" }, `A speculative fetch for "${src}" failed:`, error);
      },
      maxCacheSize: getMaxSourceCacheSize(logLevel),
      ...resolvedRequestInit ? { requestInit: resolvedRequestInit } : undefined
    })
  });
  const getSinks = async () => {
    const format = await getFormatOrNullOrNetworkError(input2);
    const isMatroska = format === MATROSKA || format === WEBM;
    const getVideoSinks = async () => {
      if (format === "network-error") {
        return "network-error";
      }
      if (format === null) {
        return "unknown-container-format";
      }
      const videoTrack = await input2.getPrimaryVideoTrack();
      if (!videoTrack) {
        return "no-video-track";
      }
      if (await videoTrack.isLive()) {
        throw new Error("Live streams are not currently supported by Remotion. Sorry! Source: " + src);
      }
      if (await videoTrack.isRelativeToUnixEpoch()) {
        throw new Error("Streams with UNIX timestamps are not currently supported by Remotion. Sorry! Source: " + src);
      }
      const canDecode = await videoTrack.canDecode();
      if (!canDecode) {
        if (videoTrack.codec === "prores") {
          return "cannot-decode-prores";
        }
        return "cannot-decode";
      }
      const sampleSink = new VideoSampleSink(videoTrack);
      const packetSink = new EncodedPacketSink(videoTrack);
      const startPacket = await packetSink.getFirstPacket({
        verifyKeyPackets: true
      });
      const hasAlpha = startPacket?.sideData.alpha;
      if (hasAlpha && !canBrowserUseWebGl2()) {
        Internals122.Log.warn({ logLevel, tag: "@remotion/media" }, `WebGL2 is not available, using the non-fast CPU path to decode alpha for ${src}.`);
      }
      return {
        sampleSink
      };
    };
    let videoSinksPromise = null;
    const getVideoSinksPromise = () => {
      if (videoSinksPromise) {
        return videoSinksPromise;
      }
      videoSinksPromise = getVideoSinks();
      return videoSinksPromise;
    };
    const audioSinksPromise = {};
    const getAudioSinks = async (index) => {
      if (format === null) {
        return "unknown-container-format";
      }
      if (format === "network-error") {
        return "network-error";
      }
      const [videoTrack, audioTracks] = await Promise.all([
        input2.getPrimaryVideoTrack(),
        input2.getAudioTracks()
      ]);
      const audioTrack = await resolveAudioTrack({
        videoTrack,
        audioTracks,
        audioStreamIndex: index
      });
      if (!audioTrack) {
        return "no-audio-track";
      }
      const canDecode = await audioTrack.canDecode();
      if (!canDecode) {
        return "cannot-decode-audio";
      }
      return {
        sampleSink: new AudioSampleSink(audioTrack)
      };
    };
    const getAudioSinksPromise = (index) => {
      const keyIndex = index === null ? -1 : index;
      if (audioSinksPromise[keyIndex]) {
        return audioSinksPromise[keyIndex];
      }
      audioSinksPromise[keyIndex] = getAudioSinks(index);
      return audioSinksPromise[keyIndex];
    };
    return {
      getVideo: () => getVideoSinksPromise(),
      getAudio: (index) => getAudioSinksPromise(index),
      actualMatroskaTimestamps: rememberActualMatroskaTimestamps(isMatroska),
      isMatroska,
      getDuration: () => {
        return getDurationOrCompute(input2);
      }
    };
  };
  return {
    promise: getSinks(),
    dispose: () => input2.dispose()
  };
};
var getSinkCacheKey = ({
  src,
  credentials,
  requestInit
}) => JSON.stringify([
  src,
  credentials,
  getMediaRequestInitFingerprint(requestInit)
]);
var makeSinkManager = () => {
  const sinkPromises = {};
  const inputDisposers = {};
  let disposed = false;
  return {
    getSink: (src, logLevel, credentials, requestInit) => {
      if (disposed) {
        return Promise.reject(new Error("Media cache has already been disposed"));
      }
      const normalizedRequestInit = normalizeMediaRequestInit(requestInit);
      const cacheKey = getSinkCacheKey({
        src,
        credentials,
        requestInit: normalizedRequestInit
      });
      let promise = sinkPromises[cacheKey];
      if (!promise) {
        Internals132.Log.verbose({
          logLevel,
          tag: "@remotion/media"
        }, `Sink for ${src} was not found, creating new sink`);
        const sinks = makeSinks(src, logLevel, credentials, normalizedRequestInit);
        promise = sinks.promise;
        sinkPromises[cacheKey] = promise;
        inputDisposers[cacheKey] = sinks.dispose;
      }
      return promise;
    },
    dispose: () => {
      if (disposed) {
        return;
      }
      disposed = true;
      let firstError = null;
      for (const cacheKey of Object.keys(inputDisposers)) {
        try {
          inputDisposers[cacheKey]();
        } catch (error) {
          firstError ??= error;
        } finally {
          delete inputDisposers[cacheKey];
          delete sinkPromises[cacheKey];
        }
      }
      if (firstError !== null) {
        throw firstError;
      }
    }
  };
};
var renderTimestampRange = (timestamps) => {
  if (timestamps.length === 0) {
    return "(none)";
  }
  if (timestamps.length === 1) {
    return timestamps[0].toFixed(3);
  }
  return `${timestamps[0].toFixed(3)}...${timestamps[timestamps.length - 1].toFixed(3)}`;
};
var BYTES_PER_PIXEL_FOR_OPAQUE_FRAME = 3;
var getAllocationSize = (sample) => {
  if (sample.format === null) {
    return sample.codedHeight * sample.codedWidth * BYTES_PER_PIXEL_FOR_OPAQUE_FRAME;
  }
  return sample.allocationSize();
};
var BIGGEST_ALLOWED_JUMP_FORWARD_SECONDS = 3;
var makeKeyframeBank = async ({
  logLevel: parentLogLevel,
  src,
  videoSampleSink,
  initialTimestampRequest
}) => {
  const sampleIterator = videoSampleSink.samples(roundTo4Digits(initialTimestampRequest));
  const frames = {};
  const frameTimestamps = [];
  let hasReachedEndOfVideo = false;
  let lastUsed = Date.now();
  let allocationSize = 0;
  let pendingOperations = 0;
  const getMeasuredDurationOfFrame = (timestamp) => {
    const index = frameTimestamps.indexOf(timestamp);
    if (index === -1) {
      throw new Error(`Frame ${timestamp} not found`);
    }
    const nextTimestamp = frameTimestamps[index + 1];
    if (nextTimestamp === undefined) {
      return null;
    }
    return nextTimestamp - timestamp;
  };
  const getKnownDurationOfFrame = (timestamp) => {
    const measuredDuration = getMeasuredDurationOfFrame(timestamp);
    if (measuredDuration !== null) {
      return measuredDuration;
    }
    if (!hasReachedEndOfVideo) {
      return null;
    }
    return frames[timestamp].duration ?? 0;
  };
  const getEstimatedDurationOfFrame = (timestamp) => {
    return getMeasuredDurationOfFrame(timestamp) ?? frames[timestamp].duration ?? 0;
  };
  const deleteFrameAtTimestamp = (timestamp) => {
    allocationSize -= getAllocationSize(frames[timestamp]);
    frameTimestamps.splice(frameTimestamps.indexOf(timestamp), 1);
    frames[timestamp].close();
    delete frames[timestamp];
  };
  const deleteFramesBeforeTimestamp = ({
    logLevel,
    timestampInSeconds
  }) => {
    const deletedTimestamps = [];
    for (const frameTimestamp of frameTimestamps.slice()) {
      if (hasReachedEndOfVideo) {
        const isLast = frameTimestamp === frameTimestamps[frameTimestamps.length - 1];
        if (isLast) {
          continue;
        }
      }
      if (!frames[frameTimestamp]) {
        continue;
      }
      const duration = getKnownDurationOfFrame(frameTimestamp);
      if (duration === null) {
        continue;
      }
      if (frameTimestamp + duration < timestampInSeconds) {
        deleteFrameAtTimestamp(frameTimestamp);
        deletedTimestamps.push(frameTimestamp);
      }
    }
    if (deletedTimestamps.length > 0) {
      Internals142.Log.verbose({ logLevel, tag: "@remotion/media" }, `Deleted ${deletedTimestamps.length} frame${deletedTimestamps.length === 1 ? "" : "s"} ${renderTimestampRange(deletedTimestamps)} for src ${src} because it is lower than ${timestampInSeconds}. Remaining: ${renderTimestampRange(frameTimestamps)}`);
    }
  };
  const hasDecodedEnoughForTimestamp = (timestamp) => {
    const lastFrameTimestamp = frameTimestamps[frameTimestamps.length - 1];
    if (lastFrameTimestamp === undefined) {
      return false;
    }
    const lastFrame = frames[lastFrameTimestamp];
    if (!lastFrame) {
      return true;
    }
    if (roundTo4Digits(lastFrameTimestamp) >= roundTo4Digits(timestamp)) {
      return true;
    }
    const duration = getKnownDurationOfFrame(lastFrameTimestamp);
    if (duration === null) {
      return false;
    }
    return roundTo4Digits(lastFrameTimestamp + duration) > roundTo4Digits(timestamp);
  };
  const addFrame = (frame2, logLevel) => {
    if (frames[frame2.timestamp]) {
      deleteFrameAtTimestamp(frame2.timestamp);
    }
    frames[frame2.timestamp] = frame2;
    frameTimestamps.push(frame2.timestamp);
    allocationSize += getAllocationSize(frame2);
    lastUsed = Date.now();
    Internals142.Log.trace({ logLevel, tag: "@remotion/media" }, `Added frame at ${frame2.timestamp}sec to bank`);
  };
  const ensureEnoughFramesForTimestamp = async (timestampInSeconds, logLevel, fps) => {
    while (!hasDecodedEnoughForTimestamp(timestampInSeconds)) {
      deleteFramesBeforeTimestamp({
        logLevel: parentLogLevel,
        timestampInSeconds: timestampInSeconds - getSafeWindowOfMonotonicity(fps)
      });
      const sample = await sampleIterator.next();
      if (sample.value) {
        addFrame(sample.value, logLevel);
      }
      if (sample.done) {
        hasReachedEndOfVideo = true;
        break;
      }
      deleteFramesBeforeTimestamp({
        logLevel: parentLogLevel,
        timestampInSeconds: timestampInSeconds - getSafeWindowOfMonotonicity(fps)
      });
    }
    lastUsed = Date.now();
  };
  const getFrameFromTimestamp = async (timestampInSeconds, fps) => {
    lastUsed = Date.now();
    let adjustedTimestamp = timestampInSeconds;
    if (hasReachedEndOfVideo && roundTo4Digits(adjustedTimestamp) > roundTo4Digits(frameTimestamps[frameTimestamps.length - 1])) {
      adjustedTimestamp = frameTimestamps[frameTimestamps.length - 1];
    }
    await ensureEnoughFramesForTimestamp(adjustedTimestamp, parentLogLevel, fps);
    for (let i = frameTimestamps.length - 1;i >= 0; i--) {
      const sample = frames[frameTimestamps[i]];
      if (!sample) {
        return null;
      }
      if (roundTo4Digits(sample.timestamp) <= roundTo4Digits(adjustedTimestamp) || Math.abs(sample.timestamp - adjustedTimestamp) <= 0.001) {
        return sample;
      }
    }
    return frames[frameTimestamps[0]] ?? null;
  };
  const hasTimestampInSecond = async (timestamp, fps) => {
    return await getFrameFromTimestamp(timestamp, fps) !== null;
  };
  const getOpenFrameCount = () => {
    return {
      size: allocationSize,
      timestamps: frameTimestamps
    };
  };
  const getLastUsed = () => {
    return lastUsed;
  };
  const enqueue = makeSerializedQueue();
  const firstFrame = await sampleIterator.next();
  if (!firstFrame.value) {
    throw new Error("No first frame found");
  }
  const startTimestampInSeconds = firstFrame.value.timestamp;
  Internals142.Log.verbose({ logLevel: parentLogLevel, tag: "@remotion/media" }, `Creating keyframe bank from ${startTimestampInSeconds}sec`);
  addFrame(firstFrame.value, parentLogLevel);
  const getRangeOfTimestamps = () => {
    if (frameTimestamps.length === 0) {
      return null;
    }
    const firstTimestamp = frameTimestamps[0];
    const lastTimestamp = frameTimestamps[frameTimestamps.length - 1];
    const lastFrameDuration = getEstimatedDurationOfFrame(lastTimestamp);
    return {
      firstTimestamp,
      lastTimestamp: lastTimestamp + lastFrameDuration
    };
  };
  const prepareForDeletion = (logLevel, reason) => {
    const range = getRangeOfTimestamps();
    if (range) {
      Internals142.Log.verbose({ logLevel, tag: "@remotion/media" }, `Preparing for deletion (${reason}) of keyframe bank from ${range?.firstTimestamp}sec to ${range?.lastTimestamp}sec`);
    }
    let framesDeleted = 0;
    for (const frameTimestamp of frameTimestamps.slice()) {
      if (!frames[frameTimestamp]) {
        continue;
      }
      deleteFrameAtTimestamp(frameTimestamp);
      framesDeleted++;
    }
    sampleIterator.return();
    frameTimestamps.length = 0;
    return { framesDeleted };
  };
  const canSatisfyTimestamp = (timestamp) => {
    if (frameTimestamps.length === 0) {
      return false;
    }
    const roundedTimestamp = roundTo4Digits(timestamp);
    const firstFrameTimestamp = roundTo4Digits(frameTimestamps[0]);
    const range = getRangeOfTimestamps();
    if (!range) {
      return false;
    }
    const lastFrameTimestamp = roundTo4Digits(frameTimestamps[frameTimestamps.length - 1]);
    const lastFrameEndTimestamp = roundTo4Digits(range.lastTimestamp);
    if (hasReachedEndOfVideo && roundedTimestamp > lastFrameTimestamp) {
      return true;
    }
    if (roundedTimestamp < firstFrameTimestamp) {
      const firstFrameIsInitialFrame = firstFrameTimestamp === startTimestampInSeconds;
      const firstFrameDoesSatisfy = firstFrameIsInitialFrame && roundedTimestamp >= initialTimestampRequest;
      return firstFrameDoesSatisfy;
    }
    if (roundedTimestamp - BIGGEST_ALLOWED_JUMP_FORWARD_SECONDS > lastFrameEndTimestamp) {
      return false;
    }
    return true;
  };
  const keyframeBank = {
    getFrameFromTimestamp: (timestamp, fps) => {
      pendingOperations++;
      return enqueue(() => getFrameFromTimestamp(timestamp, fps)).finally(() => {
        pendingOperations--;
      });
    },
    prepareForDeletion,
    hasTimestampInSecond: (timestamp, fps) => {
      pendingOperations++;
      return enqueue(() => hasTimestampInSecond(timestamp, fps)).finally(() => {
        pendingOperations--;
      });
    },
    addFrame,
    deleteFramesBeforeTimestamp,
    src,
    getOpenFrameCount,
    getLastUsed,
    isBusy: () => pendingOperations > 0,
    canSatisfyTimestamp,
    getRangeOfTimestamps
  };
  return keyframeBank;
};
var RECENTLY_USED_REQUEST_COUNT = 50;
var makeKeyframeManager = ({
  getTotalCacheStats
}) => {
  let sources = {};
  let disposed = false;
  let requestCountForSrc = {};
  const lastRequestForBank = new WeakMap;
  const addKeyframeBank = ({
    src,
    bank,
    logLevel
  }) => {
    if (disposed) {
      bank.prepareForDeletion(logLevel, "media cache was disposed");
      return false;
    }
    sources[src] = sources[src] ?? [];
    sources[src].push(bank);
    return true;
  };
  const logCacheStats = (logLevel) => {
    let count = 0;
    let totalSize = 0;
    for (const src in sources) {
      for (const bank of sources[src]) {
        const { size: size2, timestamps } = bank.getOpenFrameCount();
        count += timestamps.length;
        totalSize += size2;
        if (size2 === 0) {
          continue;
        }
        Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, `Open frames for src ${src}: ${renderTimestampRange(timestamps)}`);
      }
    }
    Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, `Video cache stats: ${count} open frames, ${totalSize} bytes`);
  };
  const getCacheStats = () => {
    let count = 0;
    let totalSize = 0;
    for (const src in sources) {
      for (const bank of sources[src]) {
        const { timestamps, size: size2 } = bank.getOpenFrameCount();
        count += timestamps.length;
        totalSize += size2;
        if (size2 === 0) {
          continue;
        }
      }
    }
    return { count, totalSize };
  };
  const getTheKeyframeBankMostInThePast = () => {
    let mostInThePast = null;
    let mostInThePastBank = null;
    let numberOfBanks = 0;
    for (const src in sources) {
      for (const bank of sources[src]) {
        numberOfBanks++;
        if (bank.isBusy()) {
          continue;
        }
        const index = sources[src].indexOf(bank);
        const lastUsed = bank.getLastUsed();
        if (mostInThePast === null || lastUsed < mostInThePast) {
          mostInThePast = lastUsed;
          mostInThePastBank = { src, bank, index };
        }
      }
    }
    if (!mostInThePastBank) {
      return { mostInThePastBank: null, numberOfBanks };
    }
    return { mostInThePastBank, numberOfBanks };
  };
  const deleteOldestKeyframeBank = (logLevel) => {
    const { mostInThePastBank, numberOfBanks } = getTheKeyframeBankMostInThePast();
    if (numberOfBanks < 2 || mostInThePastBank === null) {
      return { finish: true };
    }
    const { bank, src, index } = mostInThePastBank;
    const range = bank.getRangeOfTimestamps();
    const { framesDeleted } = bank.prepareForDeletion(logLevel, "deleted oldest keyframe bank to stay under max cache size");
    sources[src].splice(index, 1);
    if (range) {
      Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, `Deleted ${framesDeleted} frames for src ${src} from ${range?.firstTimestamp}sec to ${range?.lastTimestamp}sec to free up memory.`);
    }
    return { finish: false };
  };
  const ensureToStayUnderMaxCacheSize = (logLevel, maxCacheSize) => {
    let cacheStats = getTotalCacheStats();
    let attempts = 0;
    const maxAttempts = 3;
    while (cacheStats.totalSize > maxCacheSize && attempts < maxAttempts) {
      const { finish } = deleteOldestKeyframeBank(logLevel);
      if (finish) {
        break;
      }
      Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, "Deleted oldest keyframe bank to stay under max cache size", (cacheStats.totalSize / 1024 / 1024).toFixed(1), "out of", (maxCacheSize / 1024 / 1024).toFixed(1));
      cacheStats = getTotalCacheStats();
      attempts++;
    }
    if (cacheStats.totalSize > maxCacheSize && attempts >= maxAttempts) {
      Internals152.Log.warn({ logLevel, tag: "@remotion/media" }, `Exceeded max cache size after ${maxAttempts} attempts. Remaining cache size: ${(cacheStats.totalSize / 1024 / 1024).toFixed(1)} MB, target was ${(maxCacheSize / 1024 / 1024).toFixed(1)} MB.`);
    }
  };
  const clearKeyframeBanksBeforeTime = ({
    timestampInSeconds,
    src,
    logLevel,
    fps
  }) => {
    const threshold = timestampInSeconds - getSafeWindowOfMonotonicity(fps);
    if (!sources[src]) {
      return;
    }
    const banks = sources[src];
    const currentRequest = requestCountForSrc[src] ?? 0;
    for (const bank of banks) {
      if (bank.isBusy()) {
        continue;
      }
      const range = bank.getRangeOfTimestamps();
      if (!range) {
        continue;
      }
      const lastRequest = lastRequestForBank.get(bank);
      if (lastRequest !== undefined && currentRequest - lastRequest < RECENTLY_USED_REQUEST_COUNT) {
        continue;
      }
      if (range.lastTimestamp < threshold) {
        bank.prepareForDeletion(logLevel, "cleared before threshold " + threshold);
        Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, `[Video] Cleared frames for src ${src} from ${range.firstTimestamp}sec to ${range.lastTimestamp}sec`);
        const bankIndex = banks.indexOf(bank);
        delete sources[src][bankIndex];
      } else {
        bank.deleteFramesBeforeTimestamp({
          timestampInSeconds: threshold,
          logLevel
        });
      }
    }
    sources[src] = sources[src].filter((bank) => bank !== undefined);
    logCacheStats(logLevel);
  };
  const getKeyframeBankOrRefetch = async ({
    timestamp,
    videoSampleSink,
    src,
    logLevel
  }) => {
    const existingBanks = sources[src] ?? [];
    const existingBank = existingBanks?.find((bank) => bank.canSatisfyTimestamp(timestamp));
    if (!existingBank) {
      Internals152.Log.trace({ logLevel, tag: "@remotion/media" }, `Creating new keyframe bank for src ${src} at timestamp ${timestamp}`);
      const newKeyframeBank = await makeKeyframeBank({
        videoSampleSink,
        logLevel,
        src,
        initialTimestampRequest: timestamp
      });
      if (!addKeyframeBank({ src, bank: newKeyframeBank, logLevel })) {
        return null;
      }
      return newKeyframeBank;
    }
    if (existingBank.canSatisfyTimestamp(timestamp)) {
      Internals152.Log.trace({ logLevel, tag: "@remotion/media" }, `Keyframe bank exists and satisfies timestamp ${timestamp}`);
      return existingBank;
    }
    Internals152.Log.verbose({ logLevel, tag: "@remotion/media" }, `Keyframe bank exists but frame at time ${timestamp} does not exist anymore.`);
    existingBank.prepareForDeletion(logLevel, "already existed but evicted");
    sources[src] = sources[src].filter((bank) => bank !== existingBank);
    const replacementKeybank = await makeKeyframeBank({
      videoSampleSink,
      initialTimestampRequest: timestamp,
      logLevel,
      src
    });
    if (!addKeyframeBank({ src, bank: replacementKeybank, logLevel })) {
      return null;
    }
    return replacementKeybank;
  };
  const requestKeyframeBank = async ({
    timestamp,
    videoSampleSink,
    src,
    logLevel,
    maxCacheSize,
    fps
  }) => {
    if (disposed) {
      return null;
    }
    requestCountForSrc[src] = (requestCountForSrc[src] ?? 0) + 1;
    ensureToStayUnderMaxCacheSize(logLevel, maxCacheSize);
    clearKeyframeBanksBeforeTime({
      timestampInSeconds: timestamp,
      src,
      logLevel,
      fps
    });
    const keyframeBank = await getKeyframeBankOrRefetch({
      timestamp,
      videoSampleSink,
      src,
      logLevel
    });
    if (keyframeBank) {
      lastRequestForBank.set(keyframeBank, requestCountForSrc[src]);
    }
    return keyframeBank;
  };
  const clearAll = (logLevel) => {
    const srcs = Object.keys(sources);
    for (const src of srcs) {
      const banks = sources[src];
      for (const bank of banks) {
        bank.prepareForDeletion(logLevel, "clearAll");
      }
      sources[src] = [];
    }
    sources = {};
    requestCountForSrc = {};
  };
  const dispose = (logLevel) => {
    if (disposed) {
      return;
    }
    disposed = true;
    clearAll(logLevel);
  };
  const enqueue = makeSerializedQueue();
  return {
    requestKeyframeBank: ({
      timestamp,
      videoSampleSink,
      src,
      logLevel,
      maxCacheSize,
      fps
    }) => {
      return enqueue(() => requestKeyframeBank({
        timestamp,
        videoSampleSink,
        src,
        logLevel,
        maxCacheSize,
        fps
      }));
    },
    getCacheStats,
    clearAll,
    dispose
  };
};
var getSafeWindowOfMonotonicity = (fps) => 0.2 * 30 / fps;
var makeMediaCache = () => {
  const sinkManager = makeSinkManager();
  const managerInstances = {
    keyframe: null,
    audio: null
  };
  const getCacheStats = () => {
    const { keyframe: currentKeyframeManager, audio: currentAudioManager } = managerInstances;
    if (currentKeyframeManager === null || currentAudioManager === null) {
      throw new Error("Media cache managers have not been initialized");
    }
    const keyframeManagerCacheStats = currentKeyframeManager.getCacheStats();
    const audioManagerCacheStats = currentAudioManager.getCacheStats();
    return {
      count: keyframeManagerCacheStats.count + audioManagerCacheStats.count,
      totalSize: keyframeManagerCacheStats.totalSize + audioManagerCacheStats.totalSize
    };
  };
  const keyframeManagerInstance = makeKeyframeManager({
    getTotalCacheStats: getCacheStats
  });
  const audioManagerInstance = makeAudioManager({
    getTotalCacheStats: getCacheStats
  });
  managerInstances.keyframe = keyframeManagerInstance;
  managerInstances.audio = audioManagerInstance;
  const queueFrameExtraction = makeSerializedQueue();
  const queueAudioExtraction = makeSerializedQueue();
  let disposed = false;
  return {
    sinkManager,
    keyframeManager: keyframeManagerInstance,
    audioManager: audioManagerInstance,
    getTotalCacheStats: getCacheStats,
    isDisposed: () => disposed,
    queueFrameExtraction,
    queueAudioExtraction,
    dispose: (logLevel) => {
      if (disposed) {
        return;
      }
      disposed = true;
      try {
        keyframeManagerInstance.dispose(logLevel);
      } finally {
        try {
          audioManagerInstance.dispose();
        } finally {
          sinkManager.dispose();
        }
      }
    }
  };
};
var globalMediaCache = makeMediaCache();
var useRenderMediaCache = (logLevel) => {
  const renderResourceManager = React210.useContext(Internals162.RenderResourceManagerContext);
  if (renderResourceManager === null) {
    return globalMediaCache;
  }
  return renderResourceManager.getOrCreateResource({
    key: "@remotion/media/cache",
    create: () => {
      const resource = makeMediaCache();
      return {
        resource,
        dispose: () => resource.dispose(logLevel)
      };
    }
  });
};
var useMaxMediaCacheSize = (logLevel) => {
  const context = React210.useContext(Internals162.MaxMediaCacheSizeContext);
  if (context === null) {
    return getMaxVideoCacheSize(logLevel);
  }
  return context;
};
var applyVolume = (array, volume) => {
  if (volume === 1) {
    return;
  }
  for (let i = 0;i < array.length; i++) {
    const newValue = array[i] * volume;
    if (newValue < -32768) {
      array[i] = -32768;
    } else if (newValue > 32767) {
      array[i] = 32767;
    } else {
      array[i] = newValue;
    }
  }
};
var frameForVolumeProp = ({
  behavior,
  loop,
  assetDurationInSeconds,
  fps,
  frame: frame2,
  startsAt
}) => {
  if (!loop) {
    return frame2 + startsAt;
  }
  if (behavior === "extend") {
    return frame2 + startsAt;
  }
  const assetDurationInFrames = Math.floor(assetDurationInSeconds * fps) - startsAt;
  return frame2 % assetDurationInFrames + startsAt;
};
var TARGET_NUMBER_OF_CHANNELS = 2;
var fixFloatingPoint = (value) => {
  if (value % 1 < 0.0000001) {
    return Math.floor(value);
  }
  if (value % 1 > 0.9999999) {
    return Math.ceil(value);
  }
  return value;
};
var clampToS16 = (value) => {
  return Math.max(-32768, Math.min(32767, value));
};
var resampleAudioData = ({
  srcNumberOfChannels,
  sourceChannels,
  destination,
  targetFrames,
  chunkSize
}) => {
  const getSourceValues = (startUnfixed, endUnfixed, channelIndex) => {
    const start = fixFloatingPoint(startUnfixed);
    const end = fixFloatingPoint(endUnfixed);
    const startFloor = Math.floor(start);
    const startCeil = Math.ceil(start);
    const startFraction = start - startFloor;
    const endFraction = end - Math.floor(end);
    const endFloor = Math.floor(end);
    let weightedSum = 0;
    let totalWeight = 0;
    if (startFraction > 0) {
      const firstSample = sourceChannels[startFloor * srcNumberOfChannels + channelIndex];
      weightedSum += firstSample * (1 - startFraction);
      totalWeight += 1 - startFraction;
    }
    for (let k = startCeil;k < endFloor; k++) {
      const num = sourceChannels[k * srcNumberOfChannels + channelIndex];
      weightedSum += num;
      totalWeight += 1;
    }
    if (endFraction > 0) {
      const lastSample = sourceChannels[endFloor * srcNumberOfChannels + channelIndex];
      weightedSum += lastSample * endFraction;
      totalWeight += endFraction;
    }
    const average = weightedSum / totalWeight;
    return average;
  };
  for (let newFrameIndex = 0;newFrameIndex < targetFrames; newFrameIndex++) {
    const start = newFrameIndex * chunkSize;
    const end = start + chunkSize;
    if (TARGET_NUMBER_OF_CHANNELS === srcNumberOfChannels) {
      for (let i = 0;i < srcNumberOfChannels; i++) {
        destination[newFrameIndex * srcNumberOfChannels + i] = getSourceValues(start, end, i);
      }
    }
    if (srcNumberOfChannels === 1) {
      const m = getSourceValues(start, end, 0);
      destination[newFrameIndex * 2 + 0] = m;
      destination[newFrameIndex * 2 + 1] = m;
    } else if (srcNumberOfChannels === 4) {
      const l = getSourceValues(start, end, 0);
      const r = getSourceValues(start, end, 1);
      const sl = getSourceValues(start, end, 2);
      const sr = getSourceValues(start, end, 3);
      const l2 = 0.5 * (l + sl);
      const r2 = 0.5 * (r + sr);
      destination[newFrameIndex * 2 + 0] = l2;
      destination[newFrameIndex * 2 + 1] = r2;
    } else if (srcNumberOfChannels === 6) {
      const l = getSourceValues(start, end, 0);
      const r = getSourceValues(start, end, 1);
      const c = getSourceValues(start, end, 2);
      const sl = getSourceValues(start, end, 4);
      const sr = getSourceValues(start, end, 5);
      const sq = Math.sqrt(1 / 2);
      const norm = 1 / (1 + sq + sq);
      const l2 = (l + sq * (c + sl)) * norm;
      const r2 = (r + sq * (c + sr)) * norm;
      destination[newFrameIndex * 2 + 0] = clampToS16(l2);
      destination[newFrameIndex * 2 + 1] = clampToS16(r2);
    } else {
      for (let i = 0;i < srcNumberOfChannels; i++) {
        destination[newFrameIndex * TARGET_NUMBER_OF_CHANNELS + i] = getSourceValues(start, end, i);
      }
    }
  }
};
var FORMAT = "s16";
var fixFloatingPoint2 = (value) => {
  const decimal = Math.abs(value % 1);
  if (decimal < 0.0000001) {
    return value < 0 ? Math.ceil(value) : Math.floor(value);
  }
  if (decimal > 0.9999999) {
    return value < 0 ? Math.floor(value) : Math.ceil(value);
  }
  return value;
};
var ceilButNotIfFloatingPointIssue = (value) => {
  const fixed = fixFloatingPoint2(value);
  return Math.ceil(fixed);
};
var convertAudioDataToS16 = ({
  audioData,
  trimStartInSeconds,
  trimEndInSeconds,
  audioDataTimestamp,
  isLast
}) => {
  const {
    numberOfChannels: srcNumberOfChannels,
    sampleRate: currentSampleRate,
    numberOfFrames
  } = audioData;
  const frameOffset = Math.floor(fixFloatingPoint2(trimStartInSeconds * audioData.sampleRate));
  const unroundedFrameCount = numberOfFrames - trimEndInSeconds * audioData.sampleRate - frameOffset;
  const frameCount = isLast ? ceilButNotIfFloatingPointIssue(unroundedFrameCount) : Math.round(unroundedFrameCount);
  const srcChannels = new Int16Array(srcNumberOfChannels * numberOfFrames);
  const isF32 = audioData.format === "f32" || audioData.format === "f32-planar";
  if (isF32) {
    const bytesPerPlane = numberOfFrames * 4;
    const f32Buffer = new ArrayBuffer(srcNumberOfChannels * bytesPerPlane);
    for (let ch = 0;ch < srcNumberOfChannels; ch++) {
      audioData.copyTo(new Float32Array(f32Buffer, ch * bytesPerPlane, numberOfFrames), {
        planeIndex: ch,
        frameOffset: 0,
        frameCount: numberOfFrames,
        format: "f32-planar"
      });
    }
    const f32AudioData = new AudioData({
      format: "f32-planar",
      sampleRate: currentSampleRate,
      numberOfFrames,
      numberOfChannels: srcNumberOfChannels,
      timestamp: audioData.timestamp,
      data: f32Buffer
    });
    f32AudioData.copyTo(srcChannels, {
      planeIndex: 0,
      format: FORMAT,
      frameOffset: 0,
      frameCount: numberOfFrames
    });
    f32AudioData.close();
  } else {
    audioData.copyTo(srcChannels, {
      planeIndex: 0,
      format: FORMAT,
      frameOffset: 0,
      frameCount: numberOfFrames
    });
  }
  const timestampOffsetMicroseconds = frameOffset / audioData.sampleRate * 1e6;
  return {
    data: srcChannels.subarray(frameOffset * srcNumberOfChannels, (frameOffset + frameCount) * srcNumberOfChannels),
    numberOfChannels: srcNumberOfChannels,
    numberOfFrames: frameCount,
    sampleRate: currentSampleRate,
    timestamp: audioDataTimestamp * 1e6 + fixFloatingPoint2(timestampOffsetMicroseconds),
    durationInMicroSeconds: fixFloatingPoint2(frameCount / currentSampleRate * 1e6)
  };
};
var resamplePcmS16AudioData = ({
  targetSampleRate,
  audioData,
  playbackRate,
  isLast
}) => {
  const ratio = audioData.sampleRate / targetSampleRate;
  const newNumberOfFrames = isLast ? ceilButNotIfFloatingPointIssue(audioData.numberOfFrames / ratio / playbackRate) : Math.round(audioData.numberOfFrames / ratio / playbackRate);
  if (newNumberOfFrames === 0) {
    throw new Error("Cannot resample - the given sample rate would result in less than 1 sample");
  }
  if (newNumberOfFrames === audioData.numberOfFrames && TARGET_NUMBER_OF_CHANNELS === audioData.numberOfChannels && playbackRate === 1) {
    return {
      data: audioData.data,
      numberOfFrames: newNumberOfFrames,
      timestamp: audioData.timestamp,
      durationInMicroSeconds: fixFloatingPoint2(newNumberOfFrames / targetSampleRate * 1e6)
    };
  }
  const data = new Int16Array(newNumberOfFrames * TARGET_NUMBER_OF_CHANNELS);
  const chunkSize = audioData.numberOfFrames / newNumberOfFrames;
  resampleAudioData({
    srcNumberOfChannels: audioData.numberOfChannels,
    sourceChannels: audioData.data,
    destination: data,
    targetFrames: newNumberOfFrames,
    chunkSize
  });
  const newAudioData = {
    data,
    numberOfFrames: newNumberOfFrames,
    timestamp: audioData.timestamp,
    durationInMicroSeconds: fixFloatingPoint2(newNumberOfFrames / targetSampleRate * 1e6)
  };
  return newAudioData;
};
var combineAudioDataAndClosePrevious = (audioDataArray) => {
  let numberOfFrames = 0;
  let durationInMicroSeconds = 0;
  const { numberOfChannels, sampleRate, timestamp } = audioDataArray[0];
  for (const audioData of audioDataArray) {
    if (audioData.numberOfChannels !== numberOfChannels || audioData.sampleRate !== sampleRate) {
      throw new Error("Cannot combine audio data with different formats");
    }
    numberOfFrames += audioData.numberOfFrames;
    durationInMicroSeconds += audioData.durationInMicroSeconds;
  }
  const arr = new Int16Array(numberOfFrames * numberOfChannels);
  let offset = 0;
  for (const audioData of audioDataArray) {
    arr.set(audioData.data, offset);
    offset += audioData.data.length;
  }
  return {
    data: arr,
    numberOfChannels,
    numberOfFrames,
    sampleRate,
    timestamp: fixFloatingPoint2(timestamp),
    durationInMicroSeconds: fixFloatingPoint2(durationInMicroSeconds)
  };
};
var extractAudioInternal = async ({
  sampleRate,
  src,
  timeInSeconds: unloopedTimeInSeconds,
  durationInSeconds: durationNotYetApplyingPlaybackRate,
  logLevel,
  loop,
  playbackRate,
  audioStreamIndex,
  trimBefore,
  trimAfter,
  fps,
  maxCacheSize,
  credentials,
  requestInit,
  mediaCache
}) => {
  const { getAudio, actualMatroskaTimestamps, isMatroska, getDuration } = await mediaCache.sinkManager.getSink(src, logLevel, credentials, requestInit);
  let mediaDurationInSeconds = null;
  if (loop) {
    mediaDurationInSeconds = await getDuration();
  }
  const audio = await getAudio(audioStreamIndex);
  if (audio === "network-error") {
    return "network-error";
  }
  if (audio === "no-audio-track") {
    return { data: null, durationInSeconds: null };
  }
  if (audio === "cannot-decode-audio") {
    return "cannot-decode";
  }
  if (audio === "unknown-container-format") {
    return "unknown-container-format";
  }
  const timeInSeconds = getTimeInSeconds({
    loop,
    mediaDurationInSeconds,
    unloopedTimeInSeconds,
    src,
    trimAfter,
    playbackRate,
    trimBefore,
    fps,
    ifNoMediaDuration: "fail"
  });
  if (timeInSeconds === null) {
    return { data: null, durationInSeconds: mediaDurationInSeconds };
  }
  try {
    const sampleIterator = await mediaCache.audioManager.getIterator({
      src,
      timeInSeconds,
      audioSampleSink: audio.sampleSink,
      isMatroska,
      actualMatroskaTimestamps,
      logLevel,
      maxCacheSize
    });
    const durationInSeconds = durationNotYetApplyingPlaybackRate * playbackRate;
    const samples = await sampleIterator.getSamples(timeInSeconds, durationInSeconds);
    mediaCache.audioManager.logOpenFrames();
    const audioDataArray = [];
    for (let i = 0;i < samples.length; i++) {
      const sample = samples[i];
      if (Math.abs(sample.timestamp - (timeInSeconds + durationInSeconds)) * sample.sampleRate < 1) {
        continue;
      }
      if (sample.timestamp + sample.duration <= timeInSeconds) {
        continue;
      }
      const isFirstSample = i === 0;
      const isLastSample = i === samples.length - 1;
      const audioDataRaw = sample.toAudioData();
      let trimStartInSeconds = 0;
      let trimEndInSeconds = 0;
      let leadingSilence = null;
      if (isFirstSample) {
        trimStartInSeconds = fixFloatingPoint2(timeInSeconds - sample.timestamp);
        if (trimStartInSeconds < 0) {
          const silenceFrames = Math.ceil(fixFloatingPoint2(-trimStartInSeconds * audioDataRaw.sampleRate));
          leadingSilence = {
            data: new Int16Array(silenceFrames * audioDataRaw.numberOfChannels),
            numberOfChannels: audioDataRaw.numberOfChannels,
            numberOfFrames: silenceFrames,
            sampleRate: audioDataRaw.sampleRate,
            timestamp: timeInSeconds * 1e6,
            durationInMicroSeconds: silenceFrames / audioDataRaw.sampleRate * 1e6
          };
          trimStartInSeconds = 0;
        }
      }
      if (isLastSample) {
        trimEndInSeconds = Math.max(0, sample.timestamp + sample.duration - (timeInSeconds + durationInSeconds));
      }
      const audioData = convertAudioDataToS16({
        audioData: audioDataRaw,
        trimStartInSeconds,
        trimEndInSeconds,
        audioDataTimestamp: sample.timestamp,
        isLast: isLastSample
      });
      audioDataRaw.close();
      if (audioData.numberOfFrames === 0) {
        continue;
      }
      if (leadingSilence) {
        audioDataArray.push(leadingSilence);
      }
      audioDataArray.push(audioData);
    }
    if (audioDataArray.length === 0) {
      return { data: null, durationInSeconds: mediaDurationInSeconds };
    }
    const combined = combineAudioDataAndClosePrevious(audioDataArray);
    const resampled = resamplePcmS16AudioData({
      targetSampleRate: sampleRate,
      audioData: combined,
      playbackRate,
      isLast: true
    });
    return { data: resampled, durationInSeconds: mediaDurationInSeconds };
  } catch (err) {
    const error = err;
    if (isNetworkError(error)) {
      return "network-error";
    }
    if (isUnsupportedConfigurationError(error)) {
      return "cannot-decode";
    }
    throw err;
  }
};
var extractAudio = (params) => {
  return params.mediaCache.queueAudioExtraction(() => extractAudioInternal(params));
};
var extractFrameInternal = async ({
  src,
  timeInSeconds: unloopedTimeInSeconds,
  logLevel,
  loop,
  trimAfter,
  trimBefore,
  playbackRate,
  fps,
  maxCacheSize,
  credentials,
  requestInit,
  mediaCache
}) => {
  const sink = await mediaCache.sinkManager.getSink(src, logLevel, credentials, requestInit);
  const [video, mediaDurationInSecondsRaw] = await Promise.all([
    sink.getVideo(),
    loop ? sink.getDuration() : Promise.resolve(null)
  ]);
  const mediaDurationInSeconds = loop ? mediaDurationInSecondsRaw : null;
  if (video === "no-video-track") {
    throw new Error(`No video track found for ${src}`);
  }
  if (video === "cannot-decode") {
    return { type: "cannot-decode", durationInSeconds: mediaDurationInSeconds };
  }
  if (video === "cannot-decode-prores") {
    return {
      type: "cannot-decode-prores",
      durationInSeconds: mediaDurationInSeconds
    };
  }
  if (video === "unknown-container-format") {
    return { type: "unknown-container-format" };
  }
  if (video === "network-error") {
    return { type: "network-error" };
  }
  if (video === "cannot-decode-alpha") {
    return {
      type: "cannot-decode-alpha",
      durationInSeconds: mediaDurationInSeconds
    };
  }
  const timeInSeconds = getTimeInSeconds({
    loop,
    mediaDurationInSeconds,
    unloopedTimeInSeconds,
    src,
    trimAfter,
    playbackRate,
    trimBefore,
    fps,
    ifNoMediaDuration: "fail"
  });
  if (timeInSeconds === null) {
    return {
      type: "success",
      frame: null,
      rotation: 0,
      durationInSeconds: await sink.getDuration()
    };
  }
  try {
    const keyframeBank = await mediaCache.keyframeManager.requestKeyframeBank({
      videoSampleSink: video.sampleSink,
      timestamp: timeInSeconds,
      src,
      logLevel,
      maxCacheSize,
      fps
    });
    if (!keyframeBank) {
      return {
        type: "success",
        frame: null,
        rotation: 0,
        durationInSeconds: await sink.getDuration()
      };
    }
    const frame2 = await keyframeBank.getFrameFromTimestamp(timeInSeconds, fps);
    const rotation = frame2?.rotation ?? 0;
    return {
      type: "success",
      frame: frame2?.toVideoFrame() ?? null,
      rotation,
      durationInSeconds: await sink.getDuration()
    };
  } catch (err) {
    Internals172.Log.info({ logLevel, tag: "@remotion/media" }, `Error decoding ${src} at time ${timeInSeconds}: ${err}`, err);
    return { type: "cannot-decode", durationInSeconds: mediaDurationInSeconds };
  }
};
var extractFrame = (params) => {
  return params.mediaCache.queueFrameExtraction(() => extractFrameInternal(params));
};
var rotateFrame = async ({
  frame: frame2,
  rotation
}) => {
  if (rotation === 0) {
    const directBitmap = await createImageBitmap(frame2);
    frame2.close();
    return directBitmap;
  }
  const width = rotation === 90 || rotation === 270 ? frame2.displayHeight : frame2.displayWidth;
  const height = rotation === 90 || rotation === 270 ? frame2.displayWidth : frame2.displayHeight;
  const canvas = new OffscreenCanvas(width, height);
  const ctx = canvas.getContext("2d");
  if (!ctx) {
    throw new Error("Could not get 2d context");
  }
  canvas.width = width;
  canvas.height = height;
  if (rotation === 90) {
    ctx.translate(width, 0);
  } else if (rotation === 180) {
    ctx.translate(width, height);
  } else if (rotation === 270) {
    ctx.translate(0, height);
  }
  ctx.rotate(rotation * (Math.PI / 180));
  ctx.drawImage(frame2, 0, 0);
  const bitmap = await createImageBitmap(canvas);
  frame2.close();
  return bitmap;
};
var extractFrameAndAudio = async ({
  sampleRate,
  src,
  timeInSeconds,
  logLevel,
  durationInSeconds,
  playbackRate,
  includeAudio,
  includeVideo,
  loop,
  audioStreamIndex,
  trimAfter,
  trimBefore,
  fps,
  maxCacheSize,
  credentials,
  requestInit,
  mediaCache
}) => {
  try {
    const [video, audio] = await Promise.all([
      includeVideo ? extractFrame({
        src,
        timeInSeconds,
        logLevel,
        loop,
        trimAfter,
        playbackRate,
        trimBefore,
        fps,
        maxCacheSize,
        credentials,
        requestInit,
        mediaCache
      }) : null,
      includeAudio ? extractAudio({
        sampleRate,
        src,
        timeInSeconds,
        durationInSeconds,
        logLevel,
        loop,
        playbackRate,
        audioStreamIndex,
        trimAfter,
        fps,
        trimBefore,
        maxCacheSize,
        credentials,
        requestInit,
        mediaCache
      }) : null
    ]);
    if (video?.type === "cannot-decode") {
      return {
        type: "cannot-decode",
        durationInSeconds: video.durationInSeconds
      };
    }
    if (video?.type === "cannot-decode-prores") {
      return {
        type: "cannot-decode-prores",
        durationInSeconds: video.durationInSeconds
      };
    }
    if (video?.type === "unknown-container-format") {
      return { type: "unknown-container-format" };
    }
    if (video?.type === "cannot-decode-alpha") {
      return {
        type: "cannot-decode-alpha",
        durationInSeconds: video.durationInSeconds
      };
    }
    if (video?.type === "network-error") {
      return { type: "network-error" };
    }
    if (audio === "unknown-container-format") {
      return { type: "unknown-container-format" };
    }
    if (audio === "network-error") {
      return { type: "network-error" };
    }
    if (audio === "cannot-decode") {
      return {
        type: "cannot-decode",
        durationInSeconds: video?.type === "success" ? video.durationInSeconds : null
      };
    }
    return {
      type: "success",
      frame: video?.frame ? await rotateFrame({
        frame: video.frame,
        rotation: video.rotation
      }) : null,
      audio: audio?.data ?? null,
      durationInSeconds: audio?.durationInSeconds ?? null
    };
  } catch (err) {
    const error = err;
    if (isNetworkError(error)) {
      return { type: "network-error" };
    }
    throw err;
  }
};
var emitReadiness = (channel) => {
  channel.postMessage({
    type: "main-tab-ready"
  });
  setInterval(() => {
    channel.postMessage({
      type: "main-tab-ready"
    });
  }, 300);
};
var addBroadcastChannelListener = () => {
  if (!(typeof window !== "undefined" && window.remotion_broadcastChannel && window.remotion_isMainTab)) {
    return;
  }
  window.remotion_broadcastChannel.addEventListener("message", async (event) => {
    const data = event.data;
    if (data.type === "request") {
      try {
        const result = await extractFrameAndAudio({
          sampleRate: data.sampleRate,
          src: data.src,
          timeInSeconds: data.timeInSeconds,
          logLevel: data.logLevel,
          durationInSeconds: data.durationInSeconds,
          playbackRate: data.playbackRate,
          includeAudio: data.includeAudio,
          includeVideo: data.includeVideo,
          loop: data.loop,
          audioStreamIndex: data.audioStreamIndex,
          trimAfter: data.trimAfter,
          trimBefore: data.trimBefore,
          fps: data.fps,
          maxCacheSize: data.maxCacheSize,
          credentials: data.credentials,
          requestInit: data.requestInit,
          mediaCache: globalMediaCache
        });
        if (result.type === "cannot-decode") {
          const cannotDecodeResponse = {
            type: "response-cannot-decode",
            id: data.id,
            durationInSeconds: result.durationInSeconds
          };
          window.remotion_broadcastChannel.postMessage(cannotDecodeResponse);
          return;
        }
        if (result.type === "cannot-decode-prores") {
          const cannotDecodeProresResponse = {
            type: "response-cannot-decode-prores",
            id: data.id,
            durationInSeconds: result.durationInSeconds
          };
          window.remotion_broadcastChannel.postMessage(cannotDecodeProresResponse);
          return;
        }
        if (result.type === "cannot-decode-alpha") {
          const cannotDecodeAlphaResponse = {
            type: "response-cannot-decode-alpha",
            id: data.id,
            durationInSeconds: result.durationInSeconds
          };
          window.remotion_broadcastChannel.postMessage(cannotDecodeAlphaResponse);
          return;
        }
        if (result.type === "network-error") {
          const networkErrorResponse = {
            type: "response-network-error",
            id: data.id
          };
          window.remotion_broadcastChannel.postMessage(networkErrorResponse);
          return;
        }
        if (result.type === "unknown-container-format") {
          const unknownContainerFormatResponse = {
            type: "response-unknown-container-format",
            id: data.id
          };
          window.remotion_broadcastChannel.postMessage(unknownContainerFormatResponse);
          return;
        }
        const { frame: frame2, audio, durationInSeconds } = result;
        const imageBitmap = frame2 ? await createImageBitmap(frame2) : null;
        if (frame2) {
          frame2.close();
        }
        const response = {
          type: "response-success",
          id: data.id,
          frame: imageBitmap,
          audio,
          durationInSeconds: durationInSeconds ?? null
        };
        try {
          window.remotion_broadcastChannel.postMessage(response);
        } finally {
          imageBitmap?.close();
        }
      } catch (error) {
        const response = {
          type: "response-error",
          id: data.id,
          errorStack: error.stack ?? "No stack trace"
        };
        window.remotion_broadcastChannel.postMessage(response);
      }
    } else if (data.type === "main-tab-ready") {} else {
      throw new Error("Invalid message: " + JSON.stringify(data));
    }
  });
  emitReadiness(window.remotion_broadcastChannel);
};
var mainTabIsReadyProm = null;
var waitForMainTabToBeReady = (channel) => {
  if (mainTabIsReadyProm) {
    return mainTabIsReadyProm;
  }
  mainTabIsReadyProm = new Promise((resolve) => {
    const onMessage = (event) => {
      const data = event.data;
      if (data.type === "main-tab-ready") {
        resolve();
        channel.removeEventListener("message", onMessage);
      }
    };
    channel.addEventListener("message", onMessage);
  });
  return mainTabIsReadyProm;
};
addBroadcastChannelListener();
var extractFrameViaBroadcastChannel = async ({
  sampleRate,
  src,
  timeInSeconds,
  logLevel,
  durationInSeconds,
  playbackRate,
  includeAudio,
  includeVideo,
  isClientSideRendering,
  loop,
  audioStreamIndex,
  trimAfter,
  trimBefore,
  fps,
  maxCacheSize,
  credentials,
  requestInit,
  mediaCache
}) => {
  if (isClientSideRendering || window.remotion_isMainTab) {
    return extractFrameAndAudio({
      sampleRate,
      logLevel,
      src,
      timeInSeconds,
      durationInSeconds,
      playbackRate,
      includeAudio,
      includeVideo,
      loop,
      audioStreamIndex,
      trimAfter,
      trimBefore,
      fps,
      maxCacheSize,
      credentials,
      requestInit,
      mediaCache
    });
  }
  await waitForMainTabToBeReady(window.remotion_broadcastChannel);
  const requestId = crypto.randomUUID();
  const resolvePromise = new Promise((resolve, reject) => {
    const onMessage = (event) => {
      const data = event.data;
      if (!data) {
        return;
      }
      if (data.type === "main-tab-ready") {
        return;
      }
      if (data.id !== requestId) {
        return;
      }
      if (data.type === "response-success") {
        resolve({
          type: "success",
          frame: data.frame ? data.frame : null,
          audio: data.audio ? data.audio : null,
          durationInSeconds: data.durationInSeconds ? data.durationInSeconds : null
        });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-error") {
        reject(data.errorStack);
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-cannot-decode") {
        resolve({
          type: "cannot-decode",
          durationInSeconds: data.durationInSeconds
        });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-cannot-decode-prores") {
        resolve({
          type: "cannot-decode-prores",
          durationInSeconds: data.durationInSeconds
        });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-network-error") {
        resolve({ type: "network-error" });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-unknown-container-format") {
        resolve({ type: "unknown-container-format" });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      if (data.type === "response-cannot-decode-alpha") {
        resolve({
          type: "cannot-decode-alpha",
          durationInSeconds: data.durationInSeconds
        });
        window.remotion_broadcastChannel.removeEventListener("message", onMessage);
        return;
      }
      throw new Error(`Invalid message: ${JSON.stringify(data)}`);
    };
    window.remotion_broadcastChannel.addEventListener("message", onMessage);
  });
  const request = {
    type: "request",
    sampleRate,
    src,
    timeInSeconds,
    id: requestId,
    logLevel,
    durationInSeconds,
    playbackRate,
    includeAudio,
    includeVideo,
    loop,
    audioStreamIndex,
    trimAfter,
    trimBefore,
    fps,
    maxCacheSize,
    credentials,
    requestInit: normalizeMediaRequestInit(requestInit)
  };
  window.remotion_broadcastChannel.postMessage(request);
  let timeoutId;
  return Promise.race([
    resolvePromise.then((res) => {
      clearTimeout(timeoutId);
      return res;
    }),
    new Promise((_, reject) => {
      timeoutId = setTimeout(() => {
        reject(new Error(`Timeout while extracting frame at time ${timeInSeconds}sec from ${src}`));
      }, Math.max(3000, window.remotion_puppeteerTimeout - 5000));
    })
  ]);
};
var AudioForRendering = ({
  volume: volumeProp,
  playbackRate,
  src,
  muted,
  loopVolumeCurveBehavior,
  delayRenderRetries,
  delayRenderTimeoutInMilliseconds,
  logLevel: overriddenLogLevel,
  loop,
  fallbackHtml5AudioProps,
  audioStreamIndex,
  showInTimeline,
  style: style4,
  name,
  disallowFallbackToHtml5Audio,
  toneFrequency,
  trimAfter,
  trimBefore,
  onError,
  credentials,
  requestInit
}) => {
  const audioContext = useContext32(Internals182.SharedAudioContext);
  const sampleRate = audioContext?.sampleRate ?? 48000;
  const defaultLogLevel = Internals182.useLogLevel();
  const logLevel = overriddenLogLevel ?? defaultLogLevel;
  const frame2 = useCurrentFrame2();
  const absoluteFrame = Internals182.useTimelinePosition();
  const videoConfig = Internals182.useUnsafeVideoConfig();
  const { registerRenderAsset, unregisterRenderAsset } = useContext32(Internals182.RenderAssetManager);
  const startsAt = Internals182.useMediaStartsAt();
  const environment = useRemotionEnvironment();
  if (!videoConfig) {
    throw new Error("No video config found");
  }
  if (!src) {
    throw new TypeError("No `src` was passed to <Audio>.");
  }
  const { fps } = videoConfig;
  const { delayRender, continueRender } = useDelayRender();
  const [replaceWithHtml5Audio, setReplaceWithHtml5Audio] = useState22(false);
  const [initialRequestInit] = useState22(requestInit);
  const sequenceContext = useContext32(Internals182.SequenceContext);
  const startInVideo = sequenceContext ? sequenceContext.cumulatedFrom + sequenceContext.relativeFrom : 0;
  const id = useMemo25(() => `media-audio-${random(src)}-${sequenceContext?.cumulatedFrom}-${sequenceContext?.relativeFrom}-${sequenceContext?.durationInFrames}`, [
    src,
    sequenceContext?.cumulatedFrom,
    sequenceContext?.relativeFrom,
    sequenceContext?.durationInFrames
  ]);
  const maxCacheSize = useMaxMediaCacheSize(logLevel);
  const mediaCache = useRenderMediaCache(logLevel);
  const audioEnabled = Internals182.useAudioEnabled();
  const { isMutedForPlayback, shouldUseAudio } = Internals182.useMediaAudioState({
    muted: muted ?? false,
    volume: null,
    audioEnabled
  });
  useLayoutEffect22(() => {
    const timestamp = frame2 / fps;
    const durationInSeconds = 1 / fps;
    if (!shouldUseAudio) {
      return;
    }
    if (replaceWithHtml5Audio) {
      return;
    }
    const newHandle = delayRender(`Extracting audio for frame ${frame2}`, {
      retries: delayRenderRetries ?? undefined,
      timeoutInMilliseconds: delayRenderTimeoutInMilliseconds ?? undefined
    });
    extractFrameViaBroadcastChannel({
      sampleRate,
      src,
      timeInSeconds: timestamp,
      durationInSeconds,
      playbackRate: playbackRate ?? 1,
      logLevel,
      includeAudio: shouldUseAudio,
      includeVideo: false,
      isClientSideRendering: environment.isClientSideRendering,
      loop: loop ?? false,
      audioStreamIndex: audioStreamIndex ?? null,
      trimAfter,
      trimBefore,
      fps,
      maxCacheSize,
      credentials,
      requestInit: initialRequestInit,
      mediaCache
    }).then((result) => {
      if (mediaCache.isDisposed()) {
        return;
      }
      const handleError = (error, clientSideError, fallbackMessage) => {
        const [action, errorToUse] = callOnErrorAndResolve({
          onError,
          error,
          disallowFallback: disallowFallbackToHtml5Audio ?? false,
          isClientSideRendering: environment.isClientSideRendering,
          clientSideError
        });
        if (action === "fail") {
          cancelRender2(errorToUse);
        }
        Internals182.Log.warn({ logLevel, tag: "@remotion/media" }, fallbackMessage);
        setReplaceWithHtml5Audio(true);
      };
      if (result.type === "unknown-container-format") {
        handleError(new Error(`Unknown container format ${src}.`), new Error(`Cannot render audio "${src}": Unknown container format. See supported formats: https://www.remotion.dev/docs/mediabunny/formats`), `Unknown container format for ${src} (Supported formats: https://www.remotion.dev/docs/mediabunny/formats), falling back to <Html5Audio>`);
        return;
      }
      if (result.type === "cannot-decode") {
        handleError(new Error(`Cannot decode ${src}.`), new Error(`Cannot render audio "${src}": The audio could not be decoded by the browser.`), `Cannot decode ${src}, falling back to <Html5Audio>`);
        return;
      }
      if (result.type === "cannot-decode-alpha") {
        throw new Error(`Cannot decode alpha component for ${src}, and 'disallowFallbackToHtml5Audio' was set. But this should never happen, since you used the <Audio> tag. Please report this as a bug.`);
      }
      if (result.type === "cannot-decode-prores") {
        throw new Error(`Encountered ProRes media for ${src}. But this should never happen, since you used the <Audio> tag. Please report this as a bug.`);
      }
      if (result.type === "network-error") {
        handleError(new Error(`Network error fetching ${src}.`), new Error(`Cannot render audio "${src}": Network error while fetching the audio (possibly CORS).`), `Network error fetching ${src}, falling back to <Html5Audio>`);
        return;
      }
      const { audio, durationInSeconds: assetDurationInSeconds } = result;
      const volumePropsFrame = frameForVolumeProp({
        behavior: loopVolumeCurveBehavior ?? "repeat",
        loop: loop ?? false,
        assetDurationInSeconds: assetDurationInSeconds ?? 0,
        fps,
        frame: frame2,
        startsAt
      });
      const volume = Internals182.evaluateVolume({
        volume: volumeProp,
        frame: volumePropsFrame,
        mediaVolume: 1
      });
      Internals182.warnAboutTooHighVolume(volume);
      if (audio && volume > 0) {
        applyVolume(audio.data, volume);
        registerRenderAsset({
          type: "inline-audio",
          id,
          audio: environment.isClientSideRendering ? audio.data : Array.from(audio.data),
          frame: absoluteFrame,
          startInVideo,
          timestamp: audio.timestamp,
          duration: audio.durationInMicroSeconds,
          toneFrequency: toneFrequency ?? 1
        });
      }
      continueRender(newHandle);
    }).catch((error) => {
      if (mediaCache.isDisposed()) {
        return;
      }
      cancelRender2(error);
    });
    return () => {
      continueRender(newHandle);
      unregisterRenderAsset(id);
    };
  }, [
    sampleRate,
    absoluteFrame,
    continueRender,
    delayRender,
    delayRenderRetries,
    delayRenderTimeoutInMilliseconds,
    disallowFallbackToHtml5Audio,
    environment.isClientSideRendering,
    fps,
    frame2,
    id,
    logLevel,
    loop,
    loopVolumeCurveBehavior,
    shouldUseAudio,
    playbackRate,
    registerRenderAsset,
    src,
    startInVideo,
    startsAt,
    unregisterRenderAsset,
    volumeProp,
    audioStreamIndex,
    toneFrequency,
    trimAfter,
    trimBefore,
    replaceWithHtml5Audio,
    maxCacheSize,
    onError,
    credentials,
    initialRequestInit,
    mediaCache
  ]);
  if (replaceWithHtml5Audio) {
    return /* @__PURE__ */ jsx210(Html5Audio, {
      src,
      playbackRate,
      muted: isMutedForPlayback,
      loop,
      volume: volumeProp,
      delayRenderRetries,
      delayRenderTimeoutInMilliseconds,
      style: style4,
      loopVolumeCurveBehavior,
      audioStreamIndex,
      useWebAudioApi: fallbackHtml5AudioProps?.useWebAudioApi,
      onError: fallbackHtml5AudioProps?.onError,
      toneFrequency,
      acceptableTimeShiftInSeconds: fallbackHtml5AudioProps?.acceptableTimeShiftInSeconds,
      preservePitch: fallbackHtml5AudioProps?.preservePitch ?? true,
      name,
      showInTimeline
    });
  }
  return null;
};
var { validateMediaProps } = Internals19;
var audioSchema = {
  src: {
    type: "asset",
    assetType: "audio",
    default: undefined,
    description: "Source",
    keyframable: false
  },
  ...Internals19.baseSchema,
  ...Internals19.premountSchema,
  volume: {
    type: "number",
    min: 0,
    max: 20,
    step: 0.01,
    default: 1,
    description: "Volume",
    hiddenFromList: false
  },
  playbackRate: {
    type: "number",
    min: 0.1,
    step: 0.01,
    default: 1,
    description: "Playback rate",
    hiddenFromList: false,
    keyframable: false
  },
  toneFrequency: {
    type: "number",
    min: 0.01,
    max: 2,
    step: 0.01,
    default: 1,
    description: "Pitch",
    hiddenFromList: false,
    keyframable: false
  },
  muted: { type: "boolean", default: false, description: "Muted" },
  loop: { type: "boolean", default: false, description: "Loop" }
};
var AudioInner = (props) => {
  const {
    name,
    showInTimeline,
    controls,
    from,
    durationInFrames,
    freeze,
    hidden,
    style: style4,
    premountFor,
    postmountFor,
    ...otherProps
  } = props;
  const environment = useRemotionEnvironment2();
  const sourceStack = controls ? Internals19.getStackForControls(controls) : null;
  const [mediaVolume] = Internals19.useMediaVolumeState();
  const mediaStartsAt = Internals19.useMediaStartsAt();
  const videoConfig = useVideoConfig2();
  const sequenceDurationInFrames = Math.min(durationInFrames ?? Infinity, Math.max(0, videoConfig.durationInFrames - (from ?? 0)));
  const basicInfo = Internals19.useBasicMediaInTimeline({
    src: props.src,
    volume: props.volume,
    playbackRate: props.playbackRate ?? 1,
    trimBefore: props.trimBefore,
    trimAfter: props.trimAfter,
    sequenceDurationInFrames,
    mediaType: "audio",
    displayName: name ?? "<Audio>",
    mediaVolume,
    mediaStartsAt,
    loop: props.loop ?? false,
    muted: props.muted ?? false
  });
  const [mediaDurationInSeconds, setMediaDurationInSeconds] = useState32(null);
  const loopDisplay = useMemo32(() => getLoopDisplay({
    loop: props.loop ?? false,
    mediaDurationInSeconds,
    playbackRate: props.playbackRate ?? 1,
    trimAfter: props.trimAfter,
    trimBefore: props.trimBefore,
    sequenceDurationInFrames,
    compFps: videoConfig.fps
  }), [
    props.loop,
    mediaDurationInSeconds,
    props.playbackRate,
    props.trimAfter,
    props.trimBefore,
    sequenceDurationInFrames,
    videoConfig.fps
  ]);
  const isMedia = useMemo32(() => ({
    type: "audio",
    data: basicInfo
  }), [basicInfo]);
  const {
    effectivePostmountFor,
    effectivePremountFor,
    freezeFrame,
    isPremountingOrPostmounting,
    postmountingActive,
    premountingActive,
    premountingStyle
  } = Internals19.usePremounting({
    from: from ?? 0,
    durationInFrames: basicInfo.duration,
    premountFor: premountFor ?? null,
    postmountFor: postmountFor ?? null,
    style: style4 ?? null,
    styleWhilePremounted: null,
    styleWhilePostmounted: null,
    hideWhilePremounted: "display-none"
  });
  if (typeof props.src !== "string") {
    throw new TypeError(`The \`<Audio>\` tag requires a string for \`src\`, but got ${JSON.stringify(props.src)} instead.`);
  }
  validateMediaProps({ playbackRate: props.playbackRate, volume: props.volume }, "Audio");
  validateToneFrequency({
    toneFrequency: props.toneFrequency,
    component: "Audio"
  });
  if (sequenceDurationInFrames === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx310(Freeze, {
    frame: freezeFrame,
    active: isPremountingOrPostmounting,
    children: /* @__PURE__ */ jsx310(Sequence, {
      layout: "none",
      from: from ?? 0,
      durationInFrames: basicInfo.duration,
      freeze,
      _remotionInternalIsMedia: isMedia,
      _remotionInternalPremountDisplay: effectivePremountFor || null,
      _remotionInternalPostmountDisplay: effectivePostmountFor || null,
      _remotionInternalIsPremounting: premountingActive,
      _remotionInternalIsPostmounting: postmountingActive,
      name: name ?? "<Audio>",
      _remotionInternalDocumentationLink: name === undefined ? "https://www.remotion.dev/docs/media/audio" : undefined,
      controls,
      _remotionInternalLoopDisplay: loopDisplay,
      showInTimeline: showInTimeline ?? true,
      hidden,
      children: environment.isRendering ? /* @__PURE__ */ jsx310(AudioForRendering, {
        ...otherProps,
        style: premountingStyle ?? undefined
      }) : /* @__PURE__ */ jsx310(AudioForPreview, {
        name,
        ...otherProps,
        style: premountingStyle,
        _remotionInternalStack: sourceStack,
        setMediaDurationInSeconds
      })
    })
  });
};
var Audio = Interactive.withSchema({
  Component: AudioInner,
  componentName: "<Audio>",
  componentIdentity: "dev.remotion.media.Audio",
  schema: audioSchema,
  supportsEffects: false
});
var getVideoSequenceDuration = ({
  durationInFrames,
  loop,
  playbackRate,
  trimAfter,
  trimBefore
}) => {
  if (loop || trimAfter === undefined) {
    return durationInFrames;
  }
  const trimmedDuration = Internals202.calculateMediaDuration({
    trimAfter,
    trimBefore,
    playbackRate,
    mediaDurationInFrames: Infinity
  });
  return Math.min(durationInFrames ?? Infinity, trimmedDuration);
};
var PRORES_DOCS_URL = "https://www.remotion.dev/docs/videos/prores";
var proresDecoderNotEnabledMessage = (src) => {
  return `Cannot decode "${src}": it is encoded with Apple ProRes, which WebCodecs does not support natively. ` + `ProRes decoding is not enabled by default. Register the ProRes decoder by calling ` + `registerProresDecoder() from "@mediabunny/prores" in your entry point, before registerRoot(). ` + `See ${PRORES_DOCS_URL}.`;
};

class ProResDecoderNotEnabledError extends Error {
  constructor(src) {
    super(proresDecoderNotEnabledMessage(src));
    this.name = "ProResDecoderNotEnabledError";
  }
}
var cache = new Map;
var cacheVideoFrame = (src, sourceCanvas) => {
  const { width, height } = sourceCanvas;
  if (width === 0 || height === 0) {
    return;
  }
  let cached = cache.get(src);
  if (!cached || cached.width !== width || cached.height !== height) {
    cached = new OffscreenCanvas(width, height);
    cache.set(src, cached);
  }
  const ctx = cached.getContext("2d");
  if (!ctx) {
    return;
  }
  ctx.clearRect(0, 0, width, height);
  ctx.drawImage(sourceCanvas, 0, 0);
};
var getCachedVideoFrame = (src) => {
  return cache.get(src) ?? null;
};
var OBJECT_FIT_CLASS_PATTERN = /\bobject-(contain|cover|fill|none|scale-down)\b/;
var warnedStyle = false;
var warnedClassName = false;
var warnAboutObjectFitInStyleOrClassName = ({
  style: style4,
  className,
  logLevel
}) => {
  if (!warnedStyle && style4?.objectFit) {
    warnedStyle = true;
    Internals21.Log.warn({ logLevel, tag: "@remotion/media" }, "Use the `objectFit` prop instead of the `style` prop.");
  }
  if (!warnedClassName && className && OBJECT_FIT_CLASS_PATTERN.test(className)) {
    warnedClassName = true;
    Internals21.Log.warn({ logLevel, tag: "@remotion/media" }, "Use the `objectFit` prop instead of `object-*` CSS class names.");
  }
};
var {
  useUnsafeVideoConfig: useUnsafeVideoConfig2,
  SharedAudioContext: SharedAudioContext2,
  useMediaVolumeState: useMediaVolumeState2,
  useFrameForVolumeProp: useFrameForVolumeProp2,
  evaluateVolume: evaluateVolume2,
  warnAboutTooHighVolume: warnAboutTooHighVolume2,
  usePreload: usePreload2,
  SequenceContext: SequenceContext2,
  useEffectChainState,
  usePlaying: usePlaying2,
  useBuffering: useBuffering2,
  useMediaAudioState: useMediaAudioState2
} = Internals222;
var VideoForPreviewAssertedShowing = ({
  src: unpreloadedSrc,
  style: style4,
  playbackRate,
  toneFrequency,
  logLevel,
  className,
  muted,
  volume,
  loopVolumeCurveBehavior,
  onVideoFrame,
  showInTimeline,
  loop,
  trimAfter,
  trimBefore,
  _remotionInternalStack,
  disallowFallbackToOffthreadVideo,
  fallbackOffthreadVideoProps,
  audioStreamIndex,
  debugOverlay,
  headless,
  onError,
  credentials,
  requestInit,
  objectFit: objectFitProp,
  _experimentalInitiallyDrawCachedFrame,
  effects,
  setMediaDurationInSeconds,
  refForOutline,
  ...props
}) => {
  const src = usePreload2(unpreloadedSrc);
  const canvasRef2 = useRef22(null);
  const videoConfig = useUnsafeVideoConfig2();
  const frame2 = useCurrentFrame3();
  const mediaPlayerRef = useRef22(null);
  const initialTrimBeforeRef = useRef22(trimBefore);
  const initialTrimAfterRef = useRef22(trimAfter);
  const initialOnVideoFrameRef = useRef22(onVideoFrame);
  const [initialRequestInit] = useState42(requestInit);
  const [mediaPlayerReady, setMediaPlayerReady] = useState42(false);
  const [shouldFallbackToNativeVideo, setShouldFallbackToNativeVideo] = useState42(false);
  const playing = usePlaying2();
  const { playbackRate: globalPlaybackRate } = Internals222.usePlaybackRate();
  const sharedAudioContext = useContext42(SharedAudioContext2);
  const buffer = useBufferState2();
  const canvasRefCallback = useCallback26((canvas) => {
    canvasRef2.current = canvas;
    refForOutline.current = canvas;
  }, [refForOutline]);
  const fallbackVideoRef = useCallback26((video) => {
    refForOutline.current = video;
  }, [refForOutline]);
  const [mediaVolume] = useMediaVolumeState2();
  const volumePropFrame = useFrameForVolumeProp2(loopVolumeCurveBehavior);
  const userPreferredVolume = evaluateVolume2({
    frame: volumePropFrame,
    volume,
    mediaVolume
  });
  if (!videoConfig) {
    throw new Error("No video config found");
  }
  warnAboutTooHighVolume2(userPreferredVolume);
  const effectChainState = useEffectChainState();
  const effectsRef = useRef22(effects);
  effectsRef.current = effects;
  const onErrorRef = useRef22(onError);
  onErrorRef.current = onError;
  const effectChainStateRef = useRef22(effectChainState);
  effectChainStateRef.current = effectChainState;
  const parentSequence = useContext42(SequenceContext2);
  const isPremounting = Boolean(parentSequence?.premounting);
  const isPostmounting = Boolean(parentSequence?.postmounting);
  const sequenceOffset = (parentSequence?.absoluteFrom ?? 0) / videoConfig.fps;
  const currentTime = frame2 / videoConfig.fps;
  const currentTimeRef = useRef22(currentTime);
  currentTimeRef.current = currentTime;
  const preloadedSrc = usePreload2(src);
  const { isMutedForPlayback: effectiveMuted } = useMediaAudioState2({
    muted,
    volume: userPreferredVolume,
    audioEnabled: true
  });
  const isPlayerBuffering = useBuffering2();
  const initialPlaying = useRef22(playing && !isPlayerBuffering);
  const initialIsPremounting = useRef22(isPremounting);
  const initialIsPostmounting = useRef22(isPostmounting);
  const initialGlobalPlaybackRate = useRef22(globalPlaybackRate);
  const initialPlaybackRate = useRef22(playbackRate);
  const initialToneFrequency = useRef22(toneFrequency);
  const initialMuted = useRef22(effectiveMuted);
  const initialVolume = useRef22(userPreferredVolume);
  const initialSequenceDuration = useRef22(videoConfig.durationInFrames);
  const initialSequenceOffset = useRef22(sequenceOffset);
  const hasDrawnRealFrameRef = useRef22(false);
  const isPremountingRef = useRef22(isPremounting);
  isPremountingRef.current = isPremounting;
  useLayoutEffect32(() => {
    if (!_experimentalInitiallyDrawCachedFrame) {
      return;
    }
    const canvas = canvasRef2.current;
    if (!canvas) {
      return;
    }
    const cached = getCachedVideoFrame(src);
    if (!cached) {
      return;
    }
    canvas.width = cached.width;
    canvas.height = cached.height;
    const ctx = canvas.getContext("2d", {
      alpha: true
    });
    if (!ctx) {
      return;
    }
    ctx.drawImage(cached, 0, 0);
  }, [_experimentalInitiallyDrawCachedFrame, src]);
  useLayoutEffect32(() => {
    if (!_experimentalInitiallyDrawCachedFrame) {
      return;
    }
    return () => {
      const canvas = canvasRef2.current;
      if (!canvas || !hasDrawnRealFrameRef.current || isPremountingRef.current) {
        return;
      }
      cacheVideoFrame(src, canvas);
    };
  }, [_experimentalInitiallyDrawCachedFrame, src]);
  useEffect22(() => {
    const sharedAudioContextForMediaPlayer = sharedAudioContext?.audioContext && sharedAudioContext.gainNode ? {
      audioContext: sharedAudioContext.audioContext,
      gainNode: sharedAudioContext.gainNode,
      audioSyncAnchor: sharedAudioContext.audioSyncAnchor,
      scheduleAudioNode: sharedAudioContext.scheduleAudioNode,
      unscheduleAudioNode: sharedAudioContext.unscheduleAudioNode
    } : null;
    try {
      const player = new MediaPlayer({
        canvas: canvasRef2.current,
        src: preloadedSrc,
        logLevel,
        sharedAudioContext: sharedAudioContextForMediaPlayer,
        loop,
        trimAfter: initialTrimAfterRef.current,
        trimBefore: initialTrimBeforeRef.current,
        fps: videoConfig.fps,
        playbackRate: initialPlaybackRate.current,
        toneFrequency: initialToneFrequency.current,
        audioStreamIndex,
        debugOverlay,
        bufferState: buffer,
        isPremounting: initialIsPremounting.current,
        isPostmounting: initialIsPostmounting.current,
        globalPlaybackRate: initialGlobalPlaybackRate.current,
        durationInFrames: initialSequenceDuration.current,
        onVideoFrameCallback: initialOnVideoFrameRef.current ?? null,
        playing: initialPlaying.current,
        sequenceOffset: initialSequenceOffset.current,
        credentials,
        requestInit: initialRequestInit,
        tagType: "video",
        getEffects: () => effectsRef.current,
        getEffectChainState: (width, height) => effectChainStateRef.current?.get(width, height)
      });
      mediaPlayerRef.current = player;
      player.initialize(currentTimeRef.current, initialMuted.current, initialVolume.current).then((result) => {
        if (result.type === "disposed") {
          return;
        }
        const handleError = (error, fallbackMessage) => {
          const [action, errorToUse] = callOnErrorAndResolve({
            onError: onErrorRef.current,
            error,
            disallowFallback: disallowFallbackToOffthreadVideo,
            isClientSideRendering: false,
            clientSideError: error
          });
          if (action === "fail") {
            throw errorToUse;
          }
          Internals222.Log.warn({ logLevel, tag: "@remotion/media" }, fallbackMessage);
          setShouldFallbackToNativeVideo(true);
        };
        if (result.type === "unknown-container-format") {
          handleError(new Error(`Unknown container format ${preloadedSrc}.`), `Unknown container format for ${preloadedSrc} (Supported formats: https://www.remotion.dev/docs/mediabunny/formats), falling back to <OffthreadVideo>`);
          return;
        }
        if (result.type === "network-error") {
          handleError(new Error(`Network error fetching ${preloadedSrc}.`), `Network error fetching ${preloadedSrc}, falling back to <OffthreadVideo>`);
          return;
        }
        if (result.type === "cannot-decode") {
          handleError(new Error(`Cannot decode ${preloadedSrc}.`), `Cannot decode ${preloadedSrc}, falling back to <OffthreadVideo>`);
          return;
        }
        if (result.type === "cannot-decode-prores") {
          throw new ProResDecoderNotEnabledError(preloadedSrc);
        }
        if (result.type === "no-tracks") {
          handleError(new Error(`No video or audio tracks found for ${preloadedSrc}.`), `No video or audio tracks found for ${preloadedSrc}, falling back to <OffthreadVideo>`);
          return;
        }
        if (result.type === "success") {
          setMediaPlayerReady(true);
          setMediaDurationInSeconds(result.durationInSeconds);
          hasDrawnRealFrameRef.current = true;
        }
      }).catch((error) => {
        if (error instanceof ProResDecoderNotEnabledError) {
          onErrorRef.current?.(error);
          throw error;
        }
        const [action, errorToUse] = callOnErrorAndResolve({
          onError: onErrorRef.current,
          error,
          disallowFallback: disallowFallbackToOffthreadVideo,
          isClientSideRendering: false,
          clientSideError: error
        });
        if (action === "fail") {
          throw errorToUse;
        }
        Internals222.Log.error({ logLevel, tag: "@remotion/media" }, "[VideoForPreview] Failed to initialize MediaPlayer", errorToUse);
        setShouldFallbackToNativeVideo(true);
      });
    } catch (error) {
      const [action, errorToUse] = callOnErrorAndResolve({
        error,
        onError: onErrorRef.current,
        disallowFallback: disallowFallbackToOffthreadVideo,
        isClientSideRendering: false,
        clientSideError: error
      });
      if (action === "fail") {
        throw errorToUse;
      }
      Internals222.Log.error({ logLevel, tag: "@remotion/media" }, "[VideoForPreview] MediaPlayer initialization failed", errorToUse);
      setShouldFallbackToNativeVideo(true);
    }
    return () => {
      if (mediaPlayerRef.current) {
        Internals222.Log.trace({ logLevel, tag: "@remotion/media" }, `[VideoForPreview] Disposing MediaPlayer`);
        mediaPlayerRef.current.dispose();
        mediaPlayerRef.current = null;
      }
      setMediaPlayerReady(false);
      setShouldFallbackToNativeVideo(false);
      hasDrawnRealFrameRef.current = false;
    };
  }, [
    audioStreamIndex,
    buffer,
    debugOverlay,
    disallowFallbackToOffthreadVideo,
    logLevel,
    loop,
    preloadedSrc,
    sharedAudioContext,
    videoConfig.fps,
    credentials,
    initialRequestInit,
    setMediaDurationInSeconds
  ]);
  warnAboutObjectFitInStyleOrClassName({ style: style4, className, logLevel });
  const classNameValue = useMemo42(() => {
    return [Internals222.OBJECTFIT_CONTAIN_CLASS_NAME, className].filter(Internals222.truthy).join(" ");
  }, [className]);
  useCommonEffects({
    mediaPlayerRef,
    mediaPlayerReady,
    currentTimeRef,
    playing,
    isPlayerBuffering,
    frame: frame2,
    trimBefore,
    trimAfter,
    effectiveMuted,
    userPreferredVolume,
    playbackRate,
    toneFrequency,
    globalPlaybackRate,
    fps: videoConfig.fps,
    sequenceOffset,
    loop,
    durationInFrames: videoConfig.durationInFrames,
    isPremounting,
    isPostmounting,
    currentTime,
    logLevel,
    label: "VideoForPreview"
  });
  useLayoutEffect32(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setDebugOverlay(debugOverlay);
  }, [debugOverlay, mediaPlayerReady]);
  useLayoutEffect32(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.setVideoFrameCallback(onVideoFrame ?? null);
  }, [onVideoFrame, mediaPlayerReady]);
  useLayoutEffect32(() => {
    const mediaPlayer = mediaPlayerRef.current;
    if (!mediaPlayer || !mediaPlayerReady) {
      return;
    }
    mediaPlayer.redrawVideoEffects().catch(() => {});
  }, [effects, mediaPlayerReady, mediaPlayerRef]);
  const actualStyle = useMemo42(() => {
    return {
      ...style4,
      objectFit: objectFitProp
    };
  }, [objectFitProp, style4]);
  if (shouldFallbackToNativeVideo && !disallowFallbackToOffthreadVideo) {
    return /* @__PURE__ */ jsx49(Html5Video, {
      ...props,
      ref: fallbackVideoRef,
      src,
      style: actualStyle,
      className,
      muted: effectiveMuted,
      volume,
      trimAfter,
      trimBefore,
      playbackRate,
      toneFrequency,
      loopVolumeCurveBehavior,
      name: "<Html5Video> (fallback)",
      loop,
      showInTimeline,
      _remotionInternalStack: _remotionInternalStack ?? undefined,
      ...fallbackOffthreadVideoProps
    });
  }
  if (headless) {
    return null;
  }
  return /* @__PURE__ */ jsx49("canvas", {
    ...props,
    ref: canvasRefCallback,
    style: actualStyle,
    className: classNameValue
  });
};
var VideoForPreview = (props) => {
  const frame2 = useCurrentFrame3();
  const videoConfig = useVideoConfig3();
  const currentTime = frame2 / videoConfig.fps;
  const showShow = useMemo42(() => {
    return getTimeInSeconds({
      unloopedTimeInSeconds: currentTime,
      playbackRate: props.playbackRate,
      loop: props.loop,
      trimBefore: props.trimBefore,
      trimAfter: props.trimAfter,
      mediaDurationInSeconds: Infinity,
      fps: videoConfig.fps,
      ifNoMediaDuration: "infinity",
      src: props.src
    }) !== null;
  }, [
    currentTime,
    props.loop,
    props.playbackRate,
    props.src,
    videoConfig.fps,
    props.trimBefore,
    props.trimAfter
  ]);
  if (!showShow) {
    return null;
  }
  return /* @__PURE__ */ jsx49(VideoForPreviewAssertedShowing, {
    ...props
  });
};
var VideoForRendering = ({
  volume: volumeProp,
  playbackRate,
  src,
  muted,
  loopVolumeCurveBehavior,
  delayRenderRetries,
  delayRenderTimeoutInMilliseconds,
  onVideoFrame,
  logLevel,
  loop,
  style: style4,
  className,
  fallbackOffthreadVideoProps,
  audioStreamIndex,
  disallowFallbackToOffthreadVideo,
  _remotionInternalStack,
  toneFrequency,
  trimAfterValue,
  trimBeforeValue,
  headless,
  onError,
  credentials,
  requestInit,
  objectFit: objectFitProp,
  effects,
  ...props
}) => {
  if (!src) {
    throw new TypeError("No `src` was passed to <Video>.");
  }
  const audioContext = useContext52(Internals23.SharedAudioContext);
  const sampleRate = audioContext?.sampleRate ?? 48000;
  const frame2 = useCurrentFrame4();
  const absoluteFrame = Internals23.useTimelinePosition();
  const { fps } = useVideoConfig4();
  const { registerRenderAsset, unregisterRenderAsset } = useContext52(Internals23.RenderAssetManager);
  const startsAt = Internals23.useMediaStartsAt();
  const sequenceContext = useContext52(Internals23.SequenceContext);
  const startInVideo = sequenceContext ? sequenceContext.cumulatedFrom + sequenceContext.relativeFrom : 0;
  const id = useMemo52(() => `media-video-${random2(src)}-${sequenceContext?.cumulatedFrom}-${sequenceContext?.relativeFrom}-${sequenceContext?.durationInFrames}`, [
    src,
    sequenceContext?.cumulatedFrom,
    sequenceContext?.relativeFrom,
    sequenceContext?.durationInFrames
  ]);
  const environment = useRemotionEnvironment3();
  const { delayRender, continueRender, cancelRender: cancelRender3 } = useDelayRender2();
  const canvasRef2 = useRef32(null);
  const [replaceWithOffthreadVideo, setReplaceWithOffthreadVideo] = useState52(false);
  const [initialRequestInit] = useState52(requestInit);
  const audioEnabled = Internals23.useAudioEnabled();
  const videoEnabled = Internals23.useVideoEnabled();
  const { isMutedForPlayback, shouldUseAudio } = Internals23.useMediaAudioState({
    muted,
    volume: null,
    audioEnabled
  });
  const maxCacheSize = useMaxMediaCacheSize(logLevel);
  const mediaCache = useRenderMediaCache(logLevel);
  const effectChainState = Internals23.useEffectChainState();
  const [error, setError] = useState52(null);
  if (error) {
    throw error;
  }
  useLayoutEffect4(() => {
    if (!canvasRef2.current && !headless) {
      return;
    }
    if (replaceWithOffthreadVideo) {
      return;
    }
    if (!canvasRef2.current?.getContext && !headless) {
      return setError(new Error("Canvas does not have .getContext() method available. This could be because <Video> was mounted inside an <svg> tag."));
    }
    const timestamp = frame2 / fps;
    const durationInSeconds = 1 / fps;
    const newHandle = delayRender(`Extracting frame at time ${timestamp} from ${src}`, {
      retries: delayRenderRetries ?? undefined,
      timeoutInMilliseconds: delayRenderTimeoutInMilliseconds ?? undefined
    });
    extractFrameViaBroadcastChannel({
      sampleRate,
      src,
      timeInSeconds: timestamp,
      durationInSeconds,
      playbackRate,
      logLevel,
      includeAudio: shouldUseAudio,
      includeVideo: videoEnabled,
      isClientSideRendering: environment.isClientSideRendering,
      loop,
      audioStreamIndex,
      trimAfter: trimAfterValue,
      trimBefore: trimBeforeValue,
      fps,
      maxCacheSize,
      credentials,
      requestInit: initialRequestInit,
      mediaCache
    }).then(async (result) => {
      if (mediaCache.isDisposed()) {
        if (result.type === "success") {
          result.frame?.close();
        }
        return;
      }
      const handleError = (err, clientSideError, fallbackMessage, mediaDurationInSeconds) => {
        if (environment.isClientSideRendering) {
          cancelRender3(clientSideError);
          return;
        }
        const [action, errorToUse] = callOnErrorAndResolve({
          onError,
          error: err,
          disallowFallback: disallowFallbackToOffthreadVideo,
          isClientSideRendering: environment.isClientSideRendering,
          clientSideError: err
        });
        if (action === "fail") {
          cancelRender3(errorToUse);
          return;
        }
        if (window.remotion_isMainTab) {
          Internals23.Log.warn({ logLevel, tag: "@remotion/media" }, fallbackMessage);
        }
        setReplaceWithOffthreadVideo({
          durationInSeconds: mediaDurationInSeconds
        });
      };
      if (result.type === "unknown-container-format") {
        handleError(new Error(`Unknown container format ${src}.`), new Error(`Cannot render video "${src}": Unknown container format. See supported formats: https://www.remotion.dev/docs/mediabunny/formats`), `Unknown container format for ${src} (Supported formats: https://www.remotion.dev/docs/mediabunny/formats), falling back to <OffthreadVideo>`, null);
        return;
      }
      if (result.type === "cannot-decode") {
        handleError(new Error(`Cannot decode ${src}.`), new Error(`Cannot render video "${src}": The video could not be decoded by the browser.`), `Cannot decode ${src}, falling back to <OffthreadVideo>`, result.durationInSeconds);
        return;
      }
      if (result.type === "cannot-decode-prores") {
        const proresError = new ProResDecoderNotEnabledError(src);
        onError?.(proresError);
        cancelRender3(proresError);
        return;
      }
      if (result.type === "cannot-decode-alpha") {
        handleError(new Error(`Cannot decode alpha component for ${src}.`), new Error(`Cannot render video "${src}": The alpha channel could not be decoded by the browser.`), `Cannot decode alpha component for ${src}, falling back to <OffthreadVideo>`, result.durationInSeconds);
        return;
      }
      if (result.type === "network-error") {
        handleError(new Error(`Network error fetching ${src}.`), new Error(`Cannot render video "${src}": Network error while fetching the video (possibly CORS).`), `Network error fetching ${src} (no CORS?), falling back to <OffthreadVideo>`, null);
        return;
      }
      const {
        frame: imageBitmap,
        audio,
        durationInSeconds: assetDurationInSeconds
      } = result;
      if (imageBitmap) {
        onVideoFrame?.(imageBitmap);
        const context = canvasRef2.current?.getContext("2d", {
          alpha: true
        });
        if (context) {
          context.canvas.width = imageBitmap.width;
          context.canvas.height = imageBitmap.height;
          context.canvas.style.aspectRatio = `${context.canvas.width} / ${context.canvas.height}`;
          context.drawImage(imageBitmap, 0, 0);
          if (effects.length > 0) {
            const completed = await Internals23.runEffectChain({
              state: effectChainState.get(imageBitmap.width, imageBitmap.height),
              source: context.canvas,
              effects,
              output: context.canvas,
              width: imageBitmap.width,
              height: imageBitmap.height
            });
            if (!completed || mediaCache.isDisposed()) {
              imageBitmap.close();
              return;
            }
          }
        }
        imageBitmap.close();
      } else if (videoEnabled) {
        const context = canvasRef2.current?.getContext("2d", {
          alpha: true
        });
        if (context) {
          context.clearRect(0, 0, context.canvas.width, context.canvas.height);
        }
      }
      const volumePropsFrame = frameForVolumeProp({
        behavior: loopVolumeCurveBehavior,
        loop,
        assetDurationInSeconds: assetDurationInSeconds ?? 0,
        fps,
        frame: frame2,
        startsAt
      });
      const volume = Internals23.evaluateVolume({
        volume: volumeProp,
        frame: volumePropsFrame,
        mediaVolume: 1
      });
      Internals23.warnAboutTooHighVolume(volume);
      if (audio && volume > 0) {
        applyVolume(audio.data, volume);
        registerRenderAsset({
          type: "inline-audio",
          id,
          audio: environment.isClientSideRendering ? audio.data : Array.from(audio.data),
          frame: absoluteFrame,
          startInVideo,
          timestamp: audio.timestamp,
          duration: audio.durationInMicroSeconds,
          toneFrequency
        });
      }
      continueRender(newHandle);
    }).catch((err) => {
      if (mediaCache.isDisposed()) {
        return;
      }
      cancelRender3(err);
    });
    return () => {
      continueRender(newHandle);
      unregisterRenderAsset(id);
    };
  }, [
    sampleRate,
    absoluteFrame,
    continueRender,
    delayRender,
    delayRenderRetries,
    delayRenderTimeoutInMilliseconds,
    environment.isClientSideRendering,
    fps,
    frame2,
    id,
    logLevel,
    loop,
    loopVolumeCurveBehavior,
    shouldUseAudio,
    onVideoFrame,
    playbackRate,
    registerRenderAsset,
    src,
    startInVideo,
    startsAt,
    unregisterRenderAsset,
    volumeProp,
    replaceWithOffthreadVideo,
    audioStreamIndex,
    disallowFallbackToOffthreadVideo,
    toneFrequency,
    trimAfterValue,
    trimBeforeValue,
    videoEnabled,
    maxCacheSize,
    cancelRender3,
    headless,
    onError,
    credentials,
    effectChainState,
    effects,
    initialRequestInit,
    mediaCache
  ]);
  warnAboutObjectFitInStyleOrClassName({ style: style4, className, logLevel });
  const classNameValue = useMemo52(() => {
    return [Internals23.OBJECTFIT_CONTAIN_CLASS_NAME, className].filter(Internals23.truthy).join(" ");
  }, [className]);
  const styleWithObjectFit = useMemo52(() => {
    return {
      ...style4,
      objectFit: objectFitProp
    };
  }, [objectFitProp, style4]);
  if (replaceWithOffthreadVideo) {
    const fallback = /* @__PURE__ */ jsx52(Internals23.InnerOffthreadVideo, {
      ...props,
      src,
      playbackRate: playbackRate ?? 1,
      muted: isMutedForPlayback,
      acceptableTimeShiftInSeconds: fallbackOffthreadVideoProps?.acceptableTimeShiftInSeconds,
      loopVolumeCurveBehavior: loopVolumeCurveBehavior ?? "repeat",
      delayRenderRetries: delayRenderRetries ?? undefined,
      delayRenderTimeoutInMilliseconds: delayRenderTimeoutInMilliseconds ?? undefined,
      name: "Fallback to <OffthreadVideo>",
      style: styleWithObjectFit,
      allowAmplificationDuringRender: true,
      transparent: fallbackOffthreadVideoProps?.transparent ?? true,
      toneMapped: fallbackOffthreadVideoProps?.toneMapped ?? true,
      audioStreamIndex: audioStreamIndex ?? 0,
      className,
      onVideoFrame,
      volume: volumeProp,
      id,
      onError: fallbackOffthreadVideoProps?.onError,
      toneFrequency,
      showInTimeline: false,
      crossOrigin: fallbackOffthreadVideoProps?.crossOrigin,
      onAutoPlayError: fallbackOffthreadVideoProps?.onAutoPlayError ?? null,
      pauseWhenBuffering: fallbackOffthreadVideoProps?.pauseWhenBuffering ?? false,
      trimAfter: trimAfterValue,
      trimBefore: trimBeforeValue,
      useWebAudioApi: fallbackOffthreadVideoProps?.useWebAudioApi ?? false,
      preservePitch: fallbackOffthreadVideoProps?.preservePitch ?? true,
      startFrom: undefined,
      endAt: undefined,
      _remotionInternalStack,
      _remotionInternalNativeLoopPassed: false
    });
    if (loop) {
      if (!replaceWithOffthreadVideo.durationInSeconds) {
        const err = new Error(`Cannot render video ${src}: @remotion/media was unable to render, and fell back to <OffthreadVideo>. Also, "loop" was set, but <OffthreadVideo> does not support looping and @remotion/media could also not determine the duration of the video.`);
        cancelRender3(err);
        throw err;
      }
      return /* @__PURE__ */ jsx52(Loop, {
        layout: "none",
        durationInFrames: Internals23.calculateMediaDuration({
          trimAfter: trimAfterValue,
          mediaDurationInFrames: replaceWithOffthreadVideo.durationInSeconds * fps,
          playbackRate,
          trimBefore: trimBeforeValue
        }),
        children: fallback
      });
    }
    return fallback;
  }
  if (headless) {
    return null;
  }
  return /* @__PURE__ */ jsx52("canvas", {
    ...props,
    ref: canvasRef2,
    style: styleWithObjectFit,
    className: classNameValue
  });
};
var {
  validateMediaTrimProps,
  resolveTrimProps,
  validateMediaProps: validateMediaProps2,
  useCropStyle
} = Internals24;
var videoSchema = {
  src: {
    type: "asset",
    assetType: "video",
    default: undefined,
    description: "Source",
    keyframable: false
  },
  ...Internals24.baseSchema,
  ...Internals24.premountSchema,
  volume: {
    type: "number",
    min: 0,
    max: 20,
    step: 0.01,
    default: 1,
    description: "Volume",
    hiddenFromList: false
  },
  playbackRate: {
    type: "number",
    min: 0.1,
    step: 0.01,
    default: 1,
    description: "Playback rate",
    hiddenFromList: false,
    keyframable: false
  },
  toneFrequency: {
    type: "number",
    min: 0.01,
    max: 2,
    step: 0.01,
    default: 1,
    description: "Pitch",
    hiddenFromList: false,
    keyframable: false
  },
  muted: { type: "boolean", default: false, description: "Muted" },
  loop: { type: "boolean", default: false, description: "Loop" },
  ...Internals24.transformSchema,
  ...Interactive2.backgroundSchema,
  ...Interactive2.borderSchema,
  ...Interactive2.borderRadiusSchema,
  ...Interactive2.cropSchema
};
var InnerVideo = ({
  src,
  audioStreamIndex,
  className,
  delayRenderRetries,
  delayRenderTimeoutInMilliseconds,
  disallowFallbackToOffthreadVideo,
  fallbackOffthreadVideoProps,
  logLevel,
  loop,
  loopVolumeCurveBehavior,
  muted,
  onVideoFrame,
  playbackRate,
  style: style4,
  trimAfter,
  trimBefore,
  volume,
  _remotionInternalStack,
  toneFrequency,
  showInTimeline,
  debugOverlay,
  headless,
  onError,
  credentials,
  requestInit,
  controls,
  objectFit,
  _experimentalInitiallyDrawCachedFrame,
  effects,
  setMediaDurationInSeconds,
  refForOutline,
  ...props
}) => {
  const environment = useRemotionEnvironment4();
  if (typeof src !== "string") {
    throw new TypeError(`The \`<Video>\` tag requires a string for \`src\`, but got ${JSON.stringify(src)} instead.`);
  }
  validateMediaTrimProps({
    startFrom: undefined,
    endAt: undefined,
    trimBefore,
    trimAfter
  });
  const { trimBeforeValue, trimAfterValue } = resolveTrimProps({
    startFrom: undefined,
    endAt: undefined,
    trimBefore,
    trimAfter
  });
  validateMediaProps2({ playbackRate, volume }, "Video");
  validateToneFrequency({ toneFrequency, component: "Video" });
  if (environment.isRendering) {
    return /* @__PURE__ */ jsx62(VideoForRendering, {
      ...props,
      audioStreamIndex: audioStreamIndex ?? 0,
      className,
      delayRenderRetries: delayRenderRetries ?? null,
      delayRenderTimeoutInMilliseconds: delayRenderTimeoutInMilliseconds ?? null,
      disallowFallbackToOffthreadVideo: disallowFallbackToOffthreadVideo ?? false,
      fallbackOffthreadVideoProps,
      logLevel,
      loop,
      loopVolumeCurveBehavior,
      muted,
      onVideoFrame,
      playbackRate,
      src,
      _remotionInternalStack,
      style: style4,
      volume,
      toneFrequency,
      trimAfterValue,
      trimBeforeValue,
      headless,
      onError,
      credentials,
      requestInit,
      objectFit,
      effects
    });
  }
  return /* @__PURE__ */ jsx62(VideoForPreview, {
    ...props,
    setMediaDurationInSeconds,
    audioStreamIndex: audioStreamIndex ?? 0,
    className,
    logLevel,
    loop,
    loopVolumeCurveBehavior,
    muted,
    onVideoFrame,
    playbackRate,
    toneFrequency: toneFrequency ?? 1,
    src,
    style: style4,
    volume,
    showInTimeline,
    trimAfter: trimAfterValue,
    trimBefore: trimBeforeValue,
    _remotionInternalStack: _remotionInternalStack ?? null,
    disallowFallbackToOffthreadVideo,
    fallbackOffthreadVideoProps,
    debugOverlay: debugOverlay ?? false,
    headless: headless ?? false,
    onError,
    credentials,
    requestInit,
    controls,
    objectFit,
    effects,
    _experimentalInitiallyDrawCachedFrame,
    refForOutline
  });
};
var VideoInner = ({
  src,
  audioStreamIndex,
  className,
  delayRenderRetries,
  delayRenderTimeoutInMilliseconds,
  disallowFallbackToOffthreadVideo,
  fallbackOffthreadVideoProps,
  logLevel,
  loop,
  loopVolumeCurveBehavior,
  muted,
  name,
  onVideoFrame,
  playbackRate,
  showInTimeline,
  style: style4,
  trimAfter,
  trimBefore,
  volume,
  toneFrequency,
  debugOverlay,
  headless,
  onError,
  credentials,
  requestInit,
  controls,
  objectFit,
  _experimentalInitiallyDrawCachedFrame,
  effects,
  durationInFrames,
  from,
  freeze,
  hidden,
  premountFor,
  postmountFor,
  styleWhilePremounted,
  styleWhilePostmounted,
  cropLeft,
  cropRight,
  cropTop,
  cropBottom,
  ...props
}) => {
  const sourceStack = controls ? Internals24.getStackForControls(controls) ?? undefined : undefined;
  const fallbackLogLevel = Internals24.useLogLevel();
  const [mediaVolume] = Internals24.useMediaVolumeState();
  const mediaStartsAt = Internals24.useMediaStartsAt();
  const videoConfig = useVideoConfig5();
  const sequenceDurationInFrames = Math.min(durationInFrames ?? Infinity, Math.max(0, videoConfig.durationInFrames - (from ?? 0)));
  const videoSequenceDuration = getVideoSequenceDuration({
    durationInFrames,
    loop: loop ?? false,
    playbackRate: playbackRate ?? 1,
    trimAfter,
    trimBefore
  });
  const basicInfo = Internals24.useBasicMediaInTimeline({
    src,
    volume,
    playbackRate: playbackRate ?? 1,
    trimBefore,
    trimAfter,
    sequenceDurationInFrames,
    mediaType: "video",
    displayName: name ?? "<Video>",
    mediaVolume,
    mediaStartsAt,
    loop: loop ?? false,
    muted: muted ?? false
  });
  const [mediaDurationInSeconds, setMediaDurationInSeconds] = useState62(null);
  const loopDisplay = useMemo62(() => getLoopDisplay({
    loop: loop ?? false,
    mediaDurationInSeconds,
    playbackRate: playbackRate ?? 1,
    trimAfter,
    trimBefore,
    sequenceDurationInFrames,
    compFps: videoConfig.fps
  }), [
    loop,
    mediaDurationInSeconds,
    playbackRate,
    trimAfter,
    trimBefore,
    sequenceDurationInFrames,
    videoConfig.fps
  ]);
  const isMedia = useMemo62(() => ({
    type: "video",
    data: basicInfo
  }), [basicInfo]);
  const memoizedEffects = Internals24.useMemoizedEffects({
    effects: effects ?? [],
    overrideId: controls?.overrideId ?? null
  });
  const memoizedEffectDefinitions = Internals24.useMemoizedEffectDefinitions(effects ?? []);
  const refForOutline = React62.useRef(null);
  const {
    effectivePostmountFor,
    effectivePremountFor,
    freezeFrame,
    isPremountingOrPostmounting,
    postmountingActive,
    premountingActive,
    premountingStyle
  } = Internals24.usePremounting({
    from: from ?? 0,
    durationInFrames: videoSequenceDuration ?? Infinity,
    premountFor: premountFor ?? null,
    postmountFor: postmountFor ?? null,
    style: style4 ?? null,
    styleWhilePremounted: styleWhilePremounted ?? null,
    styleWhilePostmounted: styleWhilePostmounted ?? null,
    hideWhilePremounted: "display-none"
  });
  const croppedStyle = useCropStyle({
    cropLeft,
    cropRight,
    cropTop,
    cropBottom,
    style: premountingStyle,
    componentName: "<Video />"
  });
  if (sequenceDurationInFrames === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx62(Freeze2, {
    frame: freezeFrame,
    active: isPremountingOrPostmounting,
    children: /* @__PURE__ */ jsx62(Sequence2, {
      layout: "none",
      from: from ?? 0,
      durationInFrames: videoSequenceDuration,
      freeze,
      _remotionInternalIsMedia: isMedia,
      _remotionInternalPremountDisplay: effectivePremountFor || null,
      _remotionInternalPostmountDisplay: effectivePostmountFor || null,
      _remotionInternalIsPremounting: premountingActive,
      _remotionInternalIsPostmounting: postmountingActive,
      name: name ?? "<Video>",
      _remotionInternalDocumentationLink: name === undefined ? "https://www.remotion.dev/docs/media/video" : undefined,
      controls,
      _remotionInternalLoopDisplay: loopDisplay,
      _remotionInternalEffects: memoizedEffectDefinitions,
      outlineRef: refForOutline,
      showInTimeline: showInTimeline ?? true,
      hidden,
      children: /* @__PURE__ */ jsx62(InnerVideo, {
        ...props,
        audioStreamIndex: audioStreamIndex ?? 0,
        className,
        delayRenderRetries: delayRenderRetries ?? null,
        delayRenderTimeoutInMilliseconds: delayRenderTimeoutInMilliseconds ?? null,
        disallowFallbackToOffthreadVideo: disallowFallbackToOffthreadVideo ?? false,
        fallbackOffthreadVideoProps: fallbackOffthreadVideoProps ?? {},
        logLevel: logLevel ?? fallbackLogLevel,
        loop: loop ?? false,
        loopVolumeCurveBehavior: loopVolumeCurveBehavior ?? "repeat",
        muted: muted ?? false,
        onVideoFrame,
        playbackRate: playbackRate ?? 1,
        showInTimeline: showInTimeline ?? true,
        src,
        style: croppedStyle ?? {},
        trimAfter,
        trimBefore,
        volume: volume ?? 1,
        toneFrequency: toneFrequency ?? 1,
        _remotionInternalStack: sourceStack,
        debugOverlay: debugOverlay ?? false,
        headless: headless ?? false,
        onError,
        credentials,
        requestInit,
        controls,
        objectFit: objectFit ?? "contain",
        _experimentalInitiallyDrawCachedFrame: _experimentalInitiallyDrawCachedFrame ?? false,
        effects: memoizedEffects,
        setMediaDurationInSeconds,
        refForOutline
      })
    })
  });
};
var Video = Interactive2.withSchema({
  Component: VideoInner,
  componentName: "<Video>",
  componentIdentity: "dev.remotion.media.Video",
  schema: videoSchema,
  supportsEffects: true
});

// src/components/Preview.tsx
import { PlayerInternals as PlayerInternals8 } from "@remotion/player";
import {
  useCallback as useCallback32,
  useContext as useContext38,
  useEffect as useEffect27,
  useLayoutEffect as useLayoutEffect12,
  useMemo as useMemo40,
  useRef as useRef18,
  useState as useState23
} from "react";
import { createPortal } from "react-dom";
import { Internals as Internals38, staticFile as staticFile2 } from "remotion";

// src/helpers/checkerboard-background.ts
var getCheckerboardBackgroundSize = (size2) => `${size2}px ${size2}px`;
var getCheckerboardBackgroundPos = (size2) => `0 0, ${size2 / 2}px 0, ${size2 / 2}px -${size2 / 2}px, 0px ${size2 / 2}px`;
var checkerboardBackgroundColor = (checkerboard) => {
  if (checkerboard) {
    return WHITE;
  }
  return BLACK;
};
var checkerboardBackgroundImage = (checkerboard) => {
  if (checkerboard) {
    return CHECKERBOARD_BACKGROUND_IMAGE;
  }
  return;
};

// src/components/RenderPreview.tsx
import { useContext as useContext21, useEffect as useEffect21, useState as useState16 } from "react";

// src/components/FilePreview.tsx
import { formatBytes } from "@remotion/studio-shared";
import { useEffect as useEffect20, useState as useState15 } from "react";

// src/components/JSONViewer.tsx
import { useEffect as useEffect17, useState as useState12 } from "react";
import { jsx as jsx50 } from "react/jsx-runtime";
var jsonStyle = {
  marginTop: 14,
  marginBottom: 14,
  fontFamily: "monospace",
  flex: 1
};
var JSONViewer = ({ src }) => {
  const [json, setJson] = useState12(null);
  useEffect17(() => {
    fetch(src).then((res) => res.json()).then((jsonRes) => {
      setJson(JSON.stringify(jsonRes, null, 2));
    });
  }, [src]);
  return /* @__PURE__ */ jsx50(RemTextarea, {
    value: json ?? undefined,
    status: "ok",
    onChange: () => {
      return null;
    },
    style: jsonStyle
  });
};

// src/components/LutPreview.tsx
import { PlayerInternals as PlayerInternals5 } from "@remotion/player";
import { useCallback as useCallback27, useEffect as useEffect18, useRef as useRef10, useState as useState13 } from "react";

// src/helpers/cube-lut.ts
var parseCubeLut = (text) => {
  let type = null;
  let size2 = 0;
  let title2 = null;
  let domainMin = [0, 0, 0];
  let domainMax = [1, 1, 1];
  let data = null;
  let offset = 0;
  const headers = new Set;
  for (const [index, rawLine] of text.split(/\r\n?|\n/).entries()) {
    const line = rawLine.trim();
    if (!line || line.startsWith("#")) {
      continue;
    }
    const titleMatch = line.match(/^TITLE\s+"([^"]*)"\s*(?:#.*)?$/);
    const [keyword, ...values] = line.replace(/#.*$/, "").trim().split(/\s+/);
    const numbers = values.map(Number);
    const isHeader = /^[A-Z][A-Z0-9_]*$/.test(keyword);
    if (isHeader) {
      if (offset > 0) {
        throw new Error(`LUT header after color data on line ${index + 1}.`);
      }
      if (headers.has(keyword)) {
        throw new Error(`Duplicate ${keyword} on line ${index + 1}.`);
      }
      headers.add(keyword);
    }
    if (keyword === "TITLE" && titleMatch) {
      title2 = titleMatch[1];
      continue;
    }
    if (keyword === "LUT_1D_SIZE" || keyword === "LUT_3D_SIZE") {
      if (type !== null) {
        throw new Error("Combined 1D and 3D LUTs are not supported.");
      }
      type = keyword === "LUT_1D_SIZE" ? "1d" : "3d";
      size2 = numbers[0];
      const maxSize = type === "1d" ? 65536 : 256;
      if (numbers.length !== 1 || !Number.isInteger(size2) || size2 < 2 || size2 > maxSize) {
        throw new Error(`${keyword} must be an integer from 2 to ${maxSize}.`);
      }
      data = new Float32Array((type === "1d" ? size2 : size2 ** 3) * 3);
      continue;
    }
    if (keyword === "DOMAIN_MIN" || keyword === "DOMAIN_MAX") {
      if (numbers.length !== 3 || !numbers.every(Number.isFinite)) {
        throw new Error(`${keyword} must contain three finite numbers.`);
      }
      if (keyword === "DOMAIN_MIN") {
        domainMin = [numbers[0], numbers[1], numbers[2]];
      } else {
        domainMax = [numbers[0], numbers[1], numbers[2]];
      }
      continue;
    }
    if (keyword === "LUT_1D_INPUT_RANGE" || keyword === "LUT_3D_INPUT_RANGE") {
      if (numbers.length !== 2 || !numbers.every(Number.isFinite) || numbers[0] >= numbers[1]) {
        throw new Error(`${keyword} must contain an increasing input range.`);
      }
      domainMin = [numbers[0], numbers[0], numbers[0]];
      domainMax = [numbers[1], numbers[1], numbers[1]];
      continue;
    }
    if (isHeader) {
      throw new Error(`Unsupported LUT header "${keyword}" on line ${index + 1}.`);
    }
    if (data === null) {
      throw new Error("Expected LUT_1D_SIZE or LUT_3D_SIZE before color data.");
    }
    const color = [Number(keyword), ...numbers];
    if (color.length !== 3 || !color.every((value) => Number.isFinite(Math.fround(value)))) {
      throw new Error(`Invalid LUT color on line ${index + 1}.`);
    }
    if (offset + 3 > data.length) {
      throw new Error(`Expected ${data.length / 3} LUT colors, got more.`);
    }
    data.set(color, offset);
    offset += 3;
  }
  if (type === null || data === null) {
    throw new Error("Expected LUT_1D_SIZE or LUT_3D_SIZE.");
  }
  if (offset !== data.length) {
    throw new Error(`Expected ${data.length / 3} LUT colors, got ${offset / 3}.`);
  }
  if (domainMin.some((min, channel) => min >= domainMax[channel])) {
    throw new Error("DOMAIN_MAX must be greater than DOMAIN_MIN in every channel.");
  }
  if (domainMin.some((min, channel) => !Number.isFinite(domainMax[channel] - min))) {
    throw new Error("LUT input ranges must be finite.");
  }
  if ((headers.has("LUT_1D_INPUT_RANGE") || headers.has("LUT_3D_INPUT_RANGE")) && (headers.has("DOMAIN_MIN") || headers.has("DOMAIN_MAX") || headers.has(type === "1d" ? "LUT_3D_INPUT_RANGE" : "LUT_1D_INPUT_RANGE"))) {
    throw new Error("Conflicting LUT input range headers.");
  }
  return { type, size: size2, title: title2, domainMin, domainMax, data };
};
var applyCubeLut = (pixels, lut) => {
  const { data, size: size2, domainMin, domainMax, type } = lut;
  const last = size2 - 1;
  const range = domainMin.map((min, channel) => domainMax[channel] - min);
  for (let pixel = 0;pixel < pixels.length; pixel += 4) {
    if (type === "1d") {
      for (let channel = 0;channel < 3; channel++) {
        const position = Math.max(0, Math.min(last, (pixels[pixel + channel] / 255 - domainMin[channel]) / range[channel] * last));
        const low = Math.floor(position);
        const high = Math.min(low + 1, last);
        const fraction = position - low;
        pixels[pixel + channel] = 255 * (data[low * 3 + channel] * (1 - fraction) + data[high * 3 + channel] * fraction);
      }
      continue;
    }
    const r = Math.max(0, Math.min(last, (pixels[pixel] / 255 - domainMin[0]) / range[0] * last));
    const g = Math.max(0, Math.min(last, (pixels[pixel + 1] / 255 - domainMin[1]) / range[1] * last));
    const b = Math.max(0, Math.min(last, (pixels[pixel + 2] / 255 - domainMin[2]) / range[2] * last));
    const r0 = Math.floor(r);
    const g0 = Math.floor(g);
    const b0 = Math.floor(b);
    const r1 = Math.min(r0 + 1, last);
    const g1 = Math.min(g0 + 1, last);
    const b1 = Math.min(b0 + 1, last);
    const rf = r - r0;
    const gf = g - g0;
    const bf = b - b0;
    for (let channel = 0;channel < 3; channel++) {
      const c00 = data[(r0 + g0 * size2 + b0 * size2 * size2) * 3 + channel] * (1 - rf) + data[(r1 + g0 * size2 + b0 * size2 * size2) * 3 + channel] * rf;
      const c10 = data[(r0 + g1 * size2 + b0 * size2 * size2) * 3 + channel] * (1 - rf) + data[(r1 + g1 * size2 + b0 * size2 * size2) * 3 + channel] * rf;
      const c01 = data[(r0 + g0 * size2 + b1 * size2 * size2) * 3 + channel] * (1 - rf) + data[(r1 + g0 * size2 + b1 * size2 * size2) * 3 + channel] * rf;
      const c11 = data[(r0 + g1 * size2 + b1 * size2 * size2) * 3 + channel] * (1 - rf) + data[(r1 + g1 * size2 + b1 * size2 * size2) * 3 + channel] * rf;
      pixels[pixel + channel] = 255 * ((c00 * (1 - gf) + c10 * gf) * (1 - bf) + (c01 * (1 - gf) + c11 * gf) * bf);
    }
  }
};

// src/components/LutPreview.tsx
import { jsx as jsx53, jsxs as jsxs26 } from "react/jsx-runtime";
var sampleImage = "https://remotion.media/transition-bg-blue.jpg";
var previewWidth = 640;
var previewHeight = 427;
var container9 = {
  padding: 24,
  flex: 1,
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  fontFamily: "sans-serif",
  fontSize: 13,
  minHeight: 0
};
var comparison = {
  position: "relative",
  flexShrink: 0,
  userSelect: "none",
  WebkitUserSelect: "none"
};
var canvasStyle = {
  display: "block",
  width: "100%",
  height: "auto",
  borderRadius: 4,
  userSelect: "none",
  WebkitUserSelect: "none"
};
var sliderStyle = {
  ...hoverableStyle({
    idleBackground: TRANSPARENT,
    hoverBackground: TRANSPARENT,
    idleColor: LIGHT_TEXT,
    hoverColor: WHITE
  }),
  position: "absolute",
  inset: 0,
  borderRadius: 4,
  cursor: "default",
  touchAction: "none",
  userSelect: "none",
  WebkitUserSelect: "none"
};
var dividerStyle = {
  position: "absolute",
  top: 0,
  bottom: 0,
  width: 2,
  backgroundColor: CURRENT_COLOR,
  transform: "translateX(-50%)",
  pointerEvents: "none"
};
var handleStyle = {
  position: "absolute",
  top: "50%",
  left: "50%",
  transform: "translate(-50%, -50%)",
  width: 32,
  height: 32,
  borderRadius: "50%",
  border: `2px solid ${CURRENT_COLOR}`,
  backgroundColor: BACKGROUND,
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
};
var message = {
  color: LIGHT_TEXT,
  textAlign: "center",
  overflowWrap: "anywhere"
};
var LutPreview = ({ src }) => {
  const containerRef = useRef10(null);
  const beforeRef = useRef10(null);
  const afterRef = useRef10(null);
  const [state, setState] = useState13({ type: "loading" });
  const [position, setPosition] = useState13(50);
  const { tabIndex } = useZIndex();
  const size2 = PlayerInternals5.useElementSize(containerRef, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const updatePosition = useCallback27((event) => {
    const rect = event.currentTarget.getBoundingClientRect();
    setPosition(Math.max(0, Math.min(100, (event.clientX - rect.left) / rect.width * 100)));
  }, []);
  const onPointerDown = useCallback27((event) => {
    if (event.button !== 0 || !event.isPrimary) {
      return;
    }
    event.currentTarget.setPointerCapture(event.pointerId);
    updatePosition(event);
  }, [updatePosition]);
  const onPointerMove = useCallback27((event) => {
    if (event.currentTarget.hasPointerCapture(event.pointerId)) {
      updatePosition(event);
    }
  }, [updatePosition]);
  const onKeyDown = useCallback27((event) => {
    const step = event.shiftKey ? 10 : 1;
    if (event.key === "Home") {
      setPosition(0);
    } else if (event.key === "End") {
      setPosition(100);
    } else if (event.key === "ArrowLeft" || event.key === "ArrowDown") {
      setPosition((value) => Math.max(0, value - step));
    } else if (event.key === "ArrowRight" || event.key === "ArrowUp") {
      setPosition((value) => Math.min(100, value + step));
    } else {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
  }, []);
  useEffect18(() => {
    const controller = new AbortController;
    const image = new Image;
    image.crossOrigin = "anonymous";
    setState({ type: "loading" });
    const load = async () => {
      const response = await fetch(src, { signal: controller.signal });
      if (!response.ok) {
        throw new Error(`Could not load LUT (HTTP ${response.status}).`);
      }
      const lut = parseCubeLut(await response.text());
      if (controller.signal.aborted) {
        return;
      }
      image.src = sampleImage;
      try {
        await image.decode();
      } catch {
        throw new Error("Could not load the LUT sample image. Check your internet connection.");
      }
      if (controller.signal.aborted) {
        return;
      }
      const before = beforeRef.current?.getContext("2d");
      const after = afterRef.current?.getContext("2d");
      if (!before || !after) {
        throw new Error("Could not create the LUT preview canvas.");
      }
      before.drawImage(image, 0, 0, previewWidth, previewHeight);
      const pixels = before.getImageData(0, 0, previewWidth, previewHeight);
      applyCubeLut(pixels.data, lut);
      after.putImageData(pixels, 0, 0);
      setState({ type: "loaded" });
    };
    load().catch((error) => {
      if (!controller.signal.aborted) {
        setState({
          type: "error",
          error: error instanceof Error ? error.message : String(error)
        });
      }
    });
    return () => {
      controller.abort();
      image.removeAttribute("src");
    };
  }, [src]);
  return /* @__PURE__ */ jsxs26("div", {
    ref: containerRef,
    style: container9,
    children: [
      state.type === "error" ? /* @__PURE__ */ jsxs26("div", {
        style: message,
        role: "alert",
        children: [
          "Could not preview LUT: ",
          state.error
        ]
      }) : state.type === "loading" ? /* @__PURE__ */ jsx53("div", {
        style: message,
        role: "status",
        children: "Loading LUT preview..."
      }) : null,
      /* @__PURE__ */ jsxs26("div", {
        style: {
          ...comparison,
          width: size2 ? Math.max(0, Math.min(960, size2.width - 48, (size2.height - 48) * previewWidth / previewHeight)) : 0,
          display: state.type === "loaded" ? "block" : "none"
        },
        children: [
          /* @__PURE__ */ jsx53("canvas", {
            ref: afterRef,
            width: previewWidth,
            height: previewHeight,
            style: canvasStyle,
            role: "img",
            "aria-label": "Sample image after LUT"
          }),
          /* @__PURE__ */ jsx53("div", {
            style: {
              position: "absolute",
              inset: 0,
              clipPath: `inset(0 ${100 - position}% 0 0)`
            },
            children: /* @__PURE__ */ jsx53("canvas", {
              ref: beforeRef,
              width: previewWidth,
              height: previewHeight,
              style: canvasStyle,
              role: "img",
              "aria-label": "Sample image before LUT"
            })
          }),
          /* @__PURE__ */ jsx53("div", {
            role: "slider",
            "aria-label": "Before and after comparison",
            "aria-valuemin": 0,
            "aria-valuemax": 100,
            "aria-valuenow": Math.round(position),
            "aria-valuetext": `${Math.round(position)}% before, ${100 - Math.round(position)}% after`,
            tabIndex,
            className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
            style: sliderStyle,
            onPointerDown,
            onPointerMove,
            onKeyDown,
            children: /* @__PURE__ */ jsx53("div", {
              "aria-hidden": true,
              style: { ...dividerStyle, left: `${position}%` },
              children: /* @__PURE__ */ jsx53("div", {
                style: handleStyle,
                children: /* @__PURE__ */ jsx53("svg", {
                  width: 20,
                  height: 20,
                  viewBox: "0 0 24 24",
                  fill: "none",
                  children: /* @__PURE__ */ jsx53("path", {
                    d: "M8 8 4 12l4 4M16 8l4 4-4 4",
                    stroke: CURRENT_COLOR,
                    strokeWidth: 2,
                    strokeLinecap: "round",
                    strokeLinejoin: "round"
                  })
                })
              })
            })
          })
        ]
      })
    ]
  });
};

// src/components/TextViewer.tsx
import { useEffect as useEffect19, useState as useState14 } from "react";
import { jsxs as jsxs27 } from "react/jsx-runtime";
var textStyle = {
  margin: 14,
  fontFamily: "monospace",
  flex: 1,
  color: WHITE,
  whiteSpace: "pre-wrap"
};
var TextViewer = ({ src }) => {
  const [txt, setTxt] = useState14("");
  useEffect19(() => {
    fetch(src).then(async (res) => {
      if (!res.ok || !res.body) {
        return;
      }
      const text = await res.text();
      setTxt(text);
    });
  }, [src]);
  return /* @__PURE__ */ jsxs27("div", {
    style: textStyle,
    children: [
      txt,
      " "
    ]
  });
};

// src/components/FilePreview.tsx
import { jsx as jsx55, jsxs as jsxs28, Fragment as Fragment13 } from "react/jsx-runtime";
var msgStyle = {
  fontSize: 13,
  color: WHITE,
  fontFamily: "sans-serif",
  display: "flex",
  justifyContent: "center"
};
var fontPreviewContainerStyle = {
  display: "flex",
  flex: 1,
  flexDirection: "column",
  justifyContent: "center",
  overflow: "hidden",
  padding: 40,
  textAlign: "center"
};
var fontPreviewLargeTextStyle = {
  color: WHITE,
  fontSize: "clamp(80px, 18vh, 180px)",
  lineHeight: 1
};
var fontPreviewTextStyle = {
  color: LIGHT_TEXT,
  fontSize: "clamp(18px, 4vh, 40px)",
  lineHeight: 1.35,
  marginTop: 32,
  wordBreak: "break-word"
};
var fontPreviewId = 0;
var FontPreview = ({ src }) => {
  const [fontFamily] = useState15(() => {
    fontPreviewId++;
    return `RemotionStudioFontPreview${fontPreviewId}`;
  });
  const [fontLoaded, setFontLoaded] = useState15(false);
  const [fontLoadFailed, setFontLoadFailed] = useState15(false);
  useEffect20(() => {
    let cancelled = false;
    const fontFace = new FontFace(fontFamily, `url(${JSON.stringify(src)})`);
    setFontLoaded(false);
    setFontLoadFailed(false);
    fontFace.load().then((loadedFontFace) => {
      if (cancelled) {
        return;
      }
      document.fonts.add(loadedFontFace);
      setFontLoaded(true);
    }).catch(() => {
      if (!cancelled) {
        setFontLoadFailed(true);
      }
    });
    return () => {
      cancelled = true;
      document.fonts.delete(fontFace);
    };
  }, [fontFamily, src]);
  if (fontLoadFailed) {
    return /* @__PURE__ */ jsx55("div", {
      style: msgStyle,
      children: "Could not preview font"
    });
  }
  if (!fontLoaded) {
    return /* @__PURE__ */ jsx55("div", {
      style: msgStyle,
      children: "Loading font preview..."
    });
  }
  return /* @__PURE__ */ jsxs28("div", {
    style: { ...fontPreviewContainerStyle, fontFamily },
    children: [
      /* @__PURE__ */ jsx55("div", {
        style: fontPreviewLargeTextStyle,
        children: "Aa"
      }),
      /* @__PURE__ */ jsxs28("div", {
        style: fontPreviewTextStyle,
        children: [
          "ABCDEFGHIJKLMNOPQRSTUVWXYZ",
          /* @__PURE__ */ jsx55("br", {}),
          "abcdefghijklmnopqrstuvwxyz",
          /* @__PURE__ */ jsx55("br", {}),
          "0123456789"
        ]
      })
    ]
  });
};
var FilePreview = ({ fileType, src, currentAsset, assetMetadata }) => {
  if (!assetMetadata) {
    throw new Error("expected to have assetMetadata");
  }
  if (assetMetadata.type === "not-found") {
    throw new Error('expected to have assetMetadata, got "not-found"');
  }
  if (assetMetadata.type === "metadata-error") {
    throw new Error("unexpected metadata-error in FilePreview");
  }
  if (fileType === "audio") {
    return /* @__PURE__ */ jsx55("audio", {
      src,
      controls: true
    });
  }
  if (fileType === "video") {
    return /* @__PURE__ */ jsx55("video", {
      src,
      controls: true
    });
  }
  if (fileType === "image") {
    return /* @__PURE__ */ jsx55("img", {
      src
    });
  }
  if (fileType === "font") {
    return /* @__PURE__ */ jsx55(FontPreview, {
      src
    });
  }
  if (fileType === "lut") {
    return /* @__PURE__ */ jsx55(LutPreview, {
      src
    }, src);
  }
  if (fileType === "json") {
    return /* @__PURE__ */ jsx55(JSONViewer, {
      src
    });
  }
  if (fileType === "txt") {
    return /* @__PURE__ */ jsx55(TextViewer, {
      src
    });
  }
  return /* @__PURE__ */ jsxs28(Fragment13, {
    children: [
      /* @__PURE__ */ jsx55("div", {
        style: msgStyle,
        children: currentAsset
      }),
      /* @__PURE__ */ jsx55(Spacing, {
        y: 0.5
      }),
      /* @__PURE__ */ jsxs28("div", {
        style: msgStyle,
        children: [
          "Size: ",
          formatBytes(assetMetadata.size),
          " "
        ]
      })
    ]
  });
};

// src/components/RenderPreview.tsx
import { jsx as jsx56, jsxs as jsxs29 } from "react/jsx-runtime";
var msgStyle2 = {
  fontSize: 13,
  color: WHITE,
  fontFamily: "sans-serif",
  display: "flex",
  justifyContent: "center"
};
var errMsgStyle = {
  ...msgStyle2,
  color: LIGHT_TEXT
};
var RenderPreview = ({ path, assetMetadata, getBlob }) => {
  const fileType = getPreviewFileType(path);
  const connectionStatus = useContext21(StudioServerConnectionCtx).previewServerState.type;
  const [blobUrl, setBlobUrl] = useState16(null);
  const [blobError, setBlobError] = useState16(null);
  useEffect21(() => {
    if (!getBlob) {
      setBlobUrl(null);
      setBlobError(null);
      return;
    }
    let cancelled = false;
    let blobUrlToRevoke = null;
    setBlobError(null);
    getBlob().then((blob) => {
      const url = URL.createObjectURL(blob);
      if (cancelled) {
        URL.revokeObjectURL(url);
        return;
      }
      blobUrlToRevoke = url;
      setBlobUrl(url);
    }).catch((err) => {
      if (cancelled) {
        return;
      }
      setBlobError(err instanceof Error ? err : new Error(String(err)));
    });
    return () => {
      cancelled = true;
      if (blobUrlToRevoke) {
        URL.revokeObjectURL(blobUrlToRevoke);
      }
    };
  }, [getBlob]);
  const src = blobUrl ?? remotion_outputsBase + path;
  if (connectionStatus === "disconnected") {
    return /* @__PURE__ */ jsx56("div", {
      style: errMsgStyle,
      children: "Studio server disconnected"
    });
  }
  if (getBlob && blobError) {
    return /* @__PURE__ */ jsxs29("div", {
      style: errMsgStyle,
      children: [
        "Failed to load preview: ",
        blobError.message
      ]
    });
  }
  if (getBlob && !blobUrl) {
    return /* @__PURE__ */ jsx56("div", {
      style: msgStyle2,
      children: "Loading preview..."
    });
  }
  return /* @__PURE__ */ jsx56(FilePreview, {
    assetMetadata,
    currentAsset: path,
    fileType,
    src
  });
};

// src/components/SelectedOutlineOverlay.tsx
import { PlayerInternals as PlayerInternals7 } from "@remotion/player";
import React47, {
  useCallback as useCallback31,
  useContext as useContext36,
  useLayoutEffect as useLayoutEffect11,
  useMemo as useMemo39,
  useRef as useRef17,
  useState as useState21
} from "react";
import {
  Internals as Internals37
} from "remotion";
import { NoReactInternals as NoReactInternals7 } from "remotion/no-react";

// src/helpers/use-is-fullscreen.ts
import { useEffect as useEffect23, useState as useState17 } from "react";
var getIsFullscreen = () => {
  return Boolean(document.fullscreenElement ?? document.webkitFullscreenElement);
};
var useIsFullscreen = () => {
  const [isFullscreen, setIsFullscreen] = useState17(getIsFullscreen);
  useEffect23(() => {
    const onFullscreenChange = () => {
      setIsFullscreen(getIsFullscreen());
    };
    document.addEventListener("fullscreenchange", onFullscreenChange);
    document.addEventListener("webkitfullscreenchange", onFullscreenChange);
    return () => {
      document.removeEventListener("fullscreenchange", onFullscreenChange);
      document.removeEventListener("webkitfullscreenchange", onFullscreenChange);
    };
  }, []);
  return isFullscreen;
};

// src/state/timeline-sequence-hover.ts
import { createContext as createContext9, useCallback as useCallback28, useContext as useContext23, useMemo as useMemo26 } from "react";
var createTimelineSequenceHoverStore = () => {
  let hoveredSequence = null;
  const listeners2 = new Set;
  return {
    getSnapshot: () => hoveredSequence,
    subscribe: (listener) => {
      listeners2.add(listener);
      return () => listeners2.delete(listener);
    },
    setHoveredSequence: (action) => {
      const nextHoveredSequence = typeof action === "function" ? action(hoveredSequence) : action;
      if (nextHoveredSequence === hoveredSequence) {
        return;
      }
      hoveredSequence = nextHoveredSequence;
      for (const listener of listeners2) {
        listener();
      }
    }
  };
};
var TimelineSequenceHoverContext = createContext9(createTimelineSequenceHoverStore());
var useTimelineSequenceHoverState = () => {
  const store = useContext23(TimelineSequenceHoverContext);
  return useSyncExternalStore(store.subscribe, store.getSnapshot, store.getSnapshot);
};
var useSetTimelineSequenceHover = () => {
  return useContext23(TimelineSequenceHoverContext).setHoveredSequence;
};
var useIsTimelineSequenceHovered = (nodePathKey) => {
  const store = useContext23(TimelineSequenceHoverContext);
  const getSnapshot = useCallback28(() => nodePathKey !== null && store.getSnapshot()?.nodePathKey === nodePathKey, [nodePathKey, store]);
  return useSyncExternalStore(store.subscribe, getSnapshot, getSnapshot);
};
var useTimelineSequenceHover = (nodePathInfo) => {
  const store = useContext23(TimelineSequenceHoverContext);
  const sequenceKey = useMemo26(() => nodePathInfo === null ? null : timelineNodePathInfoToKey({ ...nodePathInfo, auxiliaryKeys: [] }), [nodePathInfo]);
  const nodePathKey = useMemo26(() => nodePathInfo === null ? null : timelineSequenceNodePathToKey(nodePathInfo.sequenceSubscriptionKey), [nodePathInfo]);
  const hovered = useIsTimelineSequenceHovered(nodePathKey);
  const onPointerEnter = useCallback28(() => {
    if (sequenceKey === null || nodePathKey === null) {
      return;
    }
    store.setHoveredSequence((currentHover) => {
      if (currentHover?.key === sequenceKey && currentHover.nodePathKey === nodePathKey && currentHover.source === "timeline") {
        return currentHover;
      }
      return {
        key: sequenceKey,
        nodePathKey,
        source: "timeline"
      };
    });
  }, [nodePathKey, sequenceKey, store]);
  const onPointerLeave = useCallback28(() => {
    store.setHoveredSequence((currentHover) => {
      if (currentHover?.source !== "timeline" || currentHover.key !== sequenceKey) {
        return currentHover;
      }
      return null;
    });
  }, [sequenceKey, store]);
  return useMemo26(() => ({ hovered, onPointerEnter, onPointerLeave }), [hovered, onPointerEnter, onPointerLeave]);
};

// src/components/selected-outline-order.ts
var outlinePointEqualityTolerance = 0.5;
var outlineAreaEqualityTolerance = 0.5;
var outlineBoundsOverlapTolerance = 0.5;
var outlinePointsAreEquivalent = (a, b) => {
  return Math.abs(a.x - b.x) <= outlinePointEqualityTolerance && Math.abs(a.y - b.y) <= outlinePointEqualityTolerance;
};
var outlinesHaveEquivalentHitArea = (a, b) => {
  if (a.points.length !== b.points.length) {
    return false;
  }
  for (let startIndex = 0;startIndex < b.points.length; startIndex++) {
    for (const direction of [1, -1]) {
      const pointsMatch = a.points.every((point, index) => {
        const bIndex = (startIndex + direction * index + b.points.length) % b.points.length;
        return outlinePointsAreEquivalent(point, b.points[bIndex]);
      });
      if (pointsMatch) {
        return true;
      }
    }
  }
  return false;
};
var getOutlineHitArea = (outline) => {
  let area = 0;
  for (let i = 0;i < outline.points.length; i++) {
    const current = outline.points[i];
    const next = outline.points[(i + 1) % outline.points.length];
    area += current.x * next.y - next.x * current.y;
  }
  return Math.abs(area) / 2;
};
var getOutlineBounds = (outline) => {
  const xs = outline.points.map((point) => point.x);
  const ys = outline.points.map((point) => point.y);
  return {
    maxX: Math.max(...xs),
    maxY: Math.max(...ys),
    minX: Math.min(...xs),
    minY: Math.min(...ys)
  };
};
var outlineBoundsOverlap = (aBounds, bBounds) => {
  return aBounds.minX <= bBounds.maxX + outlineBoundsOverlapTolerance && aBounds.maxX + outlineBoundsOverlapTolerance >= bBounds.minX && aBounds.minY <= bBounds.maxY + outlineBoundsOverlapTolerance && aBounds.maxY + outlineBoundsOverlapTolerance >= bBounds.minY;
};
var getSequenceId = (target) => {
  return target?.sequence?.id ?? null;
};
var getParentBySequenceId = ({
  sequences,
  targetsByKey
}) => {
  const parentBySequenceId = new Map;
  for (const sequence of sequences) {
    parentBySequenceId.set(sequence.id, sequence.parent);
  }
  for (const target of targetsByKey.values()) {
    const sequenceId = getSequenceId(target);
    if (sequenceId !== null && !parentBySequenceId.has(sequenceId)) {
      parentBySequenceId.set(sequenceId, target.sequence.parent);
    }
  }
  return parentBySequenceId;
};
var isAncestorTarget = ({
  ancestor,
  descendant,
  parentBySequenceId
}) => {
  const ancestorId = getSequenceId(ancestor);
  if (ancestorId === null) {
    return false;
  }
  let parentId = descendant.sequence?.parent ?? null;
  while (parentId !== null) {
    if (parentId === ancestorId) {
      return true;
    }
    parentId = parentBySequenceId.get(parentId) ?? null;
  }
  return false;
};
var orderOutlineGroup = ({
  outlines,
  parentBySequenceId,
  targetsByKey
}) => {
  const incomingEdges = new Map;
  const outgoingEdges = new Map;
  const outlinesByKey = new Map(outlines.map((outline) => [outline.key, outline]));
  for (const outline of outlines) {
    incomingEdges.set(outline.key, new Set);
    outgoingEdges.set(outline.key, new Set);
  }
  const hasPath = ({
    fromKey,
    seen,
    toKey
  }) => {
    if (fromKey === toKey) {
      return true;
    }
    if (seen.has(fromKey)) {
      return false;
    }
    seen.add(fromKey);
    for (const nextKey of outgoingEdges.get(fromKey) ?? []) {
      if (hasPath({ fromKey: nextKey, seen, toKey })) {
        return true;
      }
    }
    return false;
  };
  const addEdge = (before, after, options) => {
    if (before.key === after.key) {
      return;
    }
    const incoming = incomingEdges.get(after.key);
    const outgoing = outgoingEdges.get(before.key);
    if (incoming === undefined || outgoing === undefined) {
      throw new Error("Expected outline to be registered before adding edge");
    }
    if (outgoing.has(after.key)) {
      return;
    }
    if (hasPath({ fromKey: after.key, seen: new Set, toKey: before.key })) {
      if (options?.skipIfCycle) {
        return;
      }
      throw new Error("Could not determine a stable outline rendering order");
    }
    incoming.add(before.key);
    outgoing.add(after.key);
  };
  const addAncestorConstraint = ({
    ancestor,
    descendant,
    descendantContainsSelection,
    equivalentHitArea
  }) => {
    if (equivalentHitArea && !descendantContainsSelection) {
      addEdge(descendant, ancestor);
    } else {
      addEdge(ancestor, descendant);
    }
  };
  const outlineBySequenceId = new Map;
  for (const outline of outlines) {
    const sequenceId = getSequenceId(targetsByKey.get(outline.key));
    if (sequenceId !== null) {
      outlineBySequenceId.set(sequenceId, outline);
    }
  }
  for (const descendant of outlines) {
    const descendantTarget = targetsByKey.get(descendant.key);
    let parentId = descendantTarget?.sequence?.parent ?? null;
    while (parentId !== null) {
      const ancestor = outlineBySequenceId.get(parentId);
      if (ancestor !== undefined) {
        addAncestorConstraint({
          ancestor,
          descendant,
          descendantContainsSelection: (descendantTarget?.selected ?? false) || (descendantTarget?.containsSelection ?? false),
          equivalentHitArea: outlinesHaveEquivalentHitArea(ancestor, descendant)
        });
        break;
      }
      parentId = parentBySequenceId.get(parentId) ?? null;
    }
  }
  const bounds = outlines.map(getOutlineBounds);
  const areas = outlines.map(getOutlineHitArea);
  for (let i = 0;i < outlines.length; i++) {
    const a = outlines[i];
    const aTarget = targetsByKey.get(a.key);
    if (aTarget === undefined) {
      continue;
    }
    for (let j = i + 1;j < outlines.length; j++) {
      if (!outlineBoundsOverlap(bounds[i], bounds[j])) {
        continue;
      }
      const aArea = areas[i];
      const bArea = areas[j];
      if (Math.abs(aArea - bArea) <= outlineAreaEqualityTolerance) {
        continue;
      }
      const b = outlines[j];
      const bTarget = targetsByKey.get(b.key);
      if (bTarget === undefined) {
        continue;
      }
      const aAncestorOfB = isAncestorTarget({
        ancestor: aTarget,
        descendant: bTarget,
        parentBySequenceId
      });
      const bAncestorOfA = isAncestorTarget({
        ancestor: bTarget,
        descendant: aTarget,
        parentBySequenceId
      });
      if (aAncestorOfB || bAncestorOfA) {
        continue;
      }
      if (aArea > bArea) {
        addEdge(a, b, { skipIfCycle: true });
      } else {
        addEdge(b, a, { skipIfCycle: true });
      }
    }
  }
  const emitted = new Set;
  const visiting = new Set;
  const ordered = [];
  const visit = (outline) => {
    if (emitted.has(outline.key)) {
      return;
    }
    if (visiting.has(outline.key)) {
      throw new Error("Could not determine a stable outline rendering order");
    }
    visiting.add(outline.key);
    for (const dependencyKey of incomingEdges.get(outline.key) ?? []) {
      const dependency = outlinesByKey.get(dependencyKey);
      if (dependency === undefined) {
        throw new Error("Expected outline dependency to exist");
      }
      visit(dependency);
    }
    visiting.delete(outline.key);
    emitted.add(outline.key);
    ordered.push(outline);
  };
  for (const outline of outlines) {
    visit(outline);
  }
  return ordered;
};
var orderOutlinesForRendering = ({
  outlines,
  sequences,
  targetsByKey
}) => {
  const parentBySequenceId = getParentBySequenceId({ sequences, targetsByKey });
  return orderOutlineGroup({
    outlines,
    parentBySequenceId,
    targetsByKey
  });
};

// src/components/SelectedOutlineKeyboardControls.tsx
import { PlayerInternals as PlayerInternals6 } from "@remotion/player";
import { useCallback as useCallback29, useContext as useContext25, useEffect as useEffect25, useRef as useRef11 } from "react";
import { Internals as Internals25 } from "remotion";
var SelectedOutlineKeyboardControls = ({ getLatestOutlineTargetByKey, getSelectableOutlines }) => {
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  const { getDragOverrides } = useContext25(Internals25.VisualModeDragOverridesContext);
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext25(Internals25.VisualModeSettersContext);
  const { frameBack, frameForward, getCurrentFrame: getCurrentFrame2, seek: seek2 } = PlayerInternals6.usePlayerMethods();
  const keybindings = useKeybinding();
  const keyboardNudgeSessionRef = useRef11(null);
  const saveKeyboardNudgeSessionRef = useRef11(() => {
    return;
  });
  const saveKeyboardNudgeSession = useCallback29(() => {
    const session = keyboardNudgeSessionRef.current;
    if (session === null) {
      return;
    }
    keyboardNudgeSessionRef.current = null;
    const changes = getSelectedOutlineDragChanges({
      dragStates: session.dragStates,
      lastValues: session.lastValues
    });
    if (changes.length === 0) {
      clearSelectedOutlineDragOverrides({
        clearDragOverrides,
        dragStates: session.dragStates
      });
      return;
    }
    const staticChanges = changes.filter((change) => change.type === "static");
    const keyframedChanges = changes.filter((change) => change.type === "keyframed");
    Promise.all([
      staticChanges.length > 0 ? saveSequenceProps({
        changes: staticChanges,
        addedKeyframes: null,
        movedKeyframes: null,
        setPropStatuses,
        clientId: session.clientId,
        undoLabel: changes.length > 1 ? "Move selected sequences" : "Move sequence",
        redoLabel: changes.length > 1 ? "Move selected sequences back" : "Move sequence back"
      }) : Promise.resolve(),
      callAddKeyframes({
        sequenceKeyframes: keyframedChanges,
        effectKeyframes: [],
        setPropStatuses,
        clientId: session.clientId
      })
    ]).catch((err) => {
      showNotification(`Could not save sequence props: ${err instanceof Error ? err.message : String(err)}`, 4000);
    }).finally(() => {
      clearSelectedOutlineDragOverrides({
        clearDragOverrides,
        dragStates: session.dragStates
      });
    });
  }, [clearDragOverrides, setPropStatuses]);
  useEffect25(() => {
    saveKeyboardNudgeSessionRef.current = saveKeyboardNudgeSession;
  }, [saveKeyboardNudgeSession]);
  useEffect25(() => {
    return () => {
      saveKeyboardNudgeSessionRef.current();
    };
  }, []);
  const seekWithArrowKey = useCallback29((event, direction) => {
    if (direction === "up" || direction === "down") {
      return;
    }
    event.preventDefault();
    if (direction === "left") {
      if (event.altKey) {
        seek2(0);
        ensureFrameIsInViewport({
          direction: "fit-left",
          durationInFrames: getCurrentDuration(),
          frame: 0
        });
      } else if (event.shiftKey) {
        frameBack(getCurrentFps());
        ensureFrameIsInViewport({
          direction: "fit-left",
          durationInFrames: getCurrentDuration(),
          frame: Math.max(0, getCurrentFrame2() - getCurrentFps())
        });
      } else {
        frameBack(1);
        ensureFrameIsInViewport({
          direction: "fit-left",
          durationInFrames: getCurrentDuration(),
          frame: Math.max(0, getCurrentFrame2() - 1)
        });
      }
      return;
    }
    if (event.altKey) {
      seek2(getCurrentDuration() - 1);
      ensureFrameIsInViewport({
        direction: "fit-right",
        durationInFrames: getCurrentDuration() - 1,
        frame: getCurrentDuration() - 1
      });
    } else if (event.shiftKey) {
      frameForward(getCurrentFps());
      ensureFrameIsInViewport({
        direction: "fit-right",
        durationInFrames: getCurrentDuration(),
        frame: Math.min(getCurrentDuration() - 1, getCurrentFrame2() + getCurrentFps())
      });
    } else {
      frameForward(1);
      ensureFrameIsInViewport({
        direction: "fit-right",
        durationInFrames: getCurrentDuration(),
        frame: Math.min(getCurrentDuration() - 1, getCurrentFrame2() + 1)
      });
    }
  }, [frameBack, frameForward, getCurrentFrame2, seek2]);
  const onArrowKeyDown = useCallback29((event) => {
    const direction = getSelectedOutlineKeyboardNudgeDirection(event.key);
    if (direction === null) {
      return;
    }
    const { selectedItems } = currentSelection.current;
    const selectedSequenceKeys = getSelectedSequenceKeys(selectedItems);
    const sequenceKeysContainingSelection = getSequenceKeysContainingSelection(selectedItems);
    const selectableOutlines = getSelectableOutlines();
    const allDragTargets = selectableOutlines.flatMap(({ key: key4 }) => {
      if (!selectedSequenceKeys.has(key4) && !sequenceKeysContainingSelection.has(key4)) {
        return [];
      }
      const drag = getLatestOutlineTargetByKey(key4)?.drag ?? null;
      return drag === null ? [] : [drag];
    });
    if (selectedItems.length === 0 || allDragTargets.length === 0) {
      seekWithArrowKey(event, direction);
      return;
    }
    if (event.altKey) {
      seekWithArrowKey(event, direction);
      return;
    }
    event.preventDefault();
    const activeSession = keyboardNudgeSessionRef.current ?? (() => {
      const [firstDragTarget] = allDragTargets;
      if (firstDragTarget === undefined) {
        throw new Error("Expected a drag target");
      }
      return {
        clientId: firstDragTarget.clientId,
        deltaX: 0,
        deltaY: 0,
        dragStates: getSelectedOutlineDragStates({
          dragTargets: allDragTargets,
          getDragOverrides,
          timelinePosition: getCurrentFrame2()
        }),
        lastValues: new Map
      };
    })();
    keyboardNudgeSessionRef.current = activeSession;
    const nextDeltas = getSelectedOutlineKeyboardNudgeDeltas({
      deltaX: activeSession.deltaX,
      deltaY: activeSession.deltaY,
      direction,
      shiftKey: event.shiftKey
    });
    activeSession.deltaX = nextDeltas.deltaX;
    activeSession.deltaY = nextDeltas.deltaY;
    const lastValues = getSelectedOutlineDragValues({
      dragStates: activeSession.dragStates,
      deltaX: activeSession.deltaX,
      deltaY: activeSession.deltaY
    });
    activeSession.lastValues = lastValues;
    for (const dragState of activeSession.dragStates) {
      const value = lastValues.get(dragState.key);
      if (value === undefined) {
        throw new Error("Expected drag value to be available");
      }
      if (dragState.target.propStatus.status === "keyframed") {
        setDragOverrides(dragState.target.nodePath, translateFieldKey, Internals25.makeKeyframedDragOverride({
          status: dragState.target.propStatus,
          frame: dragState.sourceFrame,
          value
        }));
      } else {
        setDragOverrides(dragState.target.nodePath, translateFieldKey, Internals25.makeStaticDragOverride(value));
      }
    }
  }, [
    currentSelection,
    getCurrentFrame2,
    getDragOverrides,
    getLatestOutlineTargetByKey,
    getSelectableOutlines,
    seekWithArrowKey,
    setDragOverrides
  ]);
  const onArrowKeyUp = useCallback29((event) => {
    const direction = getSelectedOutlineKeyboardNudgeDirection(event.key);
    if (direction === null || keyboardNudgeSessionRef.current === null) {
      return;
    }
    event.preventDefault();
    saveKeyboardNudgeSession();
  }, [saveKeyboardNudgeSession]);
  useEffect25(() => {
    const keyDownBindings = ["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown"].map((key4) => keybindings.registerKeybinding({
      event: "keydown",
      key: key4,
      callback: onArrowKeyDown,
      commandCtrlKey: false,
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    }));
    const keyUpBindings = ["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown"].map((key4) => keybindings.registerKeybinding({
      event: "keyup",
      key: key4,
      callback: onArrowKeyUp,
      commandCtrlKey: false,
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    }));
    return () => {
      for (const binding of [...keyDownBindings, ...keyUpBindings]) {
        binding.unregister();
      }
    };
  }, [keybindings, onArrowKeyDown, onArrowKeyUp]);
  return null;
};

// src/components/SelectedOutlineRenderer.tsx
import React46, {
  useCallback as useCallback30,
  useEffect as useEffect26,
  useLayoutEffect as useLayoutEffect10,
  useMemo as useMemo38,
  useRef as useRef16,
  useState as useState20
} from "react";

// src/components/SelectedOutlineEditingHandles.tsx
import React39, { useMemo as useMemo33 } from "react";

// src/components/selected-outline-control-layout.ts
var handleSize = 8;
var targetHalfSize = 4.5;
var smallOutlineSize = handleSize * 4;
var tinyOutlineSize = handleSize * 2;
var distance = (from, to) => Math.hypot(to.x - from.x, to.y - from.y);
var normalize = (point) => {
  const length = Math.hypot(point.x, point.y);
  if (length < 0.001) {
    return null;
  }
  return { x: point.x / length, y: point.y / length };
};
var getRotationHandlePoint = ({
  corner,
  points,
  radius
}) => {
  const [topLeft, topRight, bottomRight, bottomLeft] = points;
  const { point, adjacent } = {
    "top-left": { point: topLeft, adjacent: [topRight, bottomLeft] },
    "top-right": { point: topRight, adjacent: [topLeft, bottomRight] },
    "bottom-right": { point: bottomRight, adjacent: [topRight, bottomLeft] },
    "bottom-left": { point: bottomLeft, adjacent: [topLeft, bottomRight] }
  }[corner];
  const firstDirection = normalize({
    x: point.x - adjacent[0].x,
    y: point.y - adjacent[0].y
  });
  const secondDirection = normalize({
    x: point.x - adjacent[1].x,
    y: point.y - adjacent[1].y
  });
  const outlineCenter = {
    x: (topLeft.x + topRight.x + bottomRight.x + bottomLeft.x) / 4,
    y: (topLeft.y + topRight.y + bottomRight.y + bottomLeft.y) / 4
  };
  const fallbackDirection = normalize({
    x: point.x - outlineCenter.x,
    y: point.y - outlineCenter.y
  });
  const direction = firstDirection === null || secondDirection === null ? fallbackDirection : normalize({
    x: firstDirection.x + secondDirection.x,
    y: firstDirection.y + secondDirection.y
  });
  if (direction === null) {
    return point;
  }
  return {
    x: point.x + direction.x * radius,
    y: point.y + direction.y * radius
  };
};
var getSelectedOutlineControlLayout = (points) => {
  const [topLeft, topRight, bottomRight, bottomLeft] = points;
  const horizontalExtent = (distance(topLeft, topRight) + distance(bottomLeft, bottomRight)) / 2;
  const verticalExtent = (distance(topLeft, bottomLeft) + distance(topRight, bottomRight)) / 2;
  const isSmallX = horizontalExtent < smallOutlineSize;
  const isSmallY = verticalExtent < smallOutlineSize;
  const isTinyX = horizontalExtent < tinyOutlineSize;
  const isTinyY = verticalExtent < tinyOutlineSize;
  const targetHalfSizeX = isSmallX ? targetHalfSize / 2 : targetHalfSize;
  const targetHalfSizeY = isSmallY ? targetHalfSize / 2 : targetHalfSize;
  const rotationHandleRadius = Math.max(targetHalfSizeX, targetHalfSizeY) * 1.5;
  const scaleEdges = ["top", "right", "bottom", "left"].filter((edge) => {
    if (edge === "top" || edge === "bottom") {
      return !isTinyY;
    }
    return !isTinyX;
  });
  const rotationCorners = ["top-left", "top-right", "bottom-right", "bottom-left"].map((corner) => ({
    corner,
    point: getRotationHandlePoint({
      corner,
      points,
      radius: rotationHandleRadius
    })
  }));
  return {
    rotationCorners: isTinyX || isTinyY ? [] : rotationCorners,
    rotationHandleRadius,
    scaleEdges,
    scaleHitWidth: {
      horizontal: targetHalfSizeY * 2,
      vertical: targetHalfSizeX * 2
    }
  };
};

// src/components/SelectedOutlineCropControls.tsx
import React35, { useContext as useContext26, useLayoutEffect as useLayoutEffect5, useMemo as useMemo28 } from "react";
import { Internals as Internals27 } from "remotion";
import { jsx as jsx57, jsxs as jsxs30, Fragment as Fragment14 } from "react/jsx-runtime";
var CROP_HANDLE_THICKNESS = 5;
var CROP_HANDLE_LENGTH = 20;
var CROP_HANDLE_HIT_WIDTH = 14;
var CROPPED_AWAY_OPACITY = 0.35;
var cropHandles = [
  "top-left",
  "top-right",
  "bottom-left",
  "bottom-right",
  "top",
  "right",
  "bottom",
  "left"
];
var cursorForHandle = (handle) => {
  if (handle === "top" || handle === "bottom") {
    return "ns-resize";
  }
  if (handle === "left" || handle === "right") {
    return "ew-resize";
  }
  return handle === "top-left" || handle === "bottom-right" ? "nwse-resize" : "nesw-resize";
};
var moveTowards = (from, to, distance2) => {
  const vector = vectorBetween(from, to);
  const length = vectorLength(vector);
  if (length === 0) {
    return from;
  }
  const progress = Math.min(1, distance2 / length);
  return {
    x: from.x + vector.x * progress,
    y: from.y + vector.y * progress
  };
};
var pointString = (point) => `${point.x} ${point.y}`;
var getCropHandlePath = ({
  handle,
  points
}) => {
  const [topLeft, topRight, bottomRight, bottomLeft] = points;
  const cornerInfo = {
    "top-left": { corner: topLeft, first: topRight, second: bottomLeft },
    "top-right": { corner: topRight, first: topLeft, second: bottomRight },
    "bottom-left": { corner: bottomLeft, first: topLeft, second: bottomRight },
    "bottom-right": { corner: bottomRight, first: topRight, second: bottomLeft }
  };
  if (handle in cornerInfo) {
    const info = cornerInfo[handle];
    const length = Math.min(CROP_HANDLE_LENGTH, vectorLength(vectorBetween(info.corner, info.first)) / 4, vectorLength(vectorBetween(info.corner, info.second)) / 4);
    const first = moveTowards(info.corner, info.first, length);
    const second = moveTowards(info.corner, info.second, length);
    return `M ${pointString(first)} L ${pointString(info.corner)} L ${pointString(second)}`;
  }
  const edgeInfo = {
    top: [topLeft, topRight],
    right: [topRight, bottomRight],
    bottom: [bottomLeft, bottomRight],
    left: [topLeft, bottomLeft]
  };
  const [start, end] = edgeInfo[handle];
  const center = midpoint(start, end);
  const halfLength = Math.min(CROP_HANDLE_LENGTH / 2, vectorLength(vectorBetween(start, end)) / 8);
  const pathStart = moveTowards(center, start, halfLength);
  const pathEnd = moveTowards(center, end, halfLength);
  return `M ${pointString(pathStart)} L ${pointString(pathEnd)}`;
};
var getCropValues = (target) => ({
  cropLeft: target.fields.cropLeft.value,
  cropRight: target.fields.cropRight.value,
  cropTop: target.fields.cropTop.value,
  cropBottom: target.fields.cropBottom.value
});
var makeCropPreviewMask = (crop) => {
  const x = crop.left * 100;
  const y = crop.top * 100;
  const width = Math.max(0, 1 - crop.left - crop.right) * 100;
  const height = Math.max(0, 1 - crop.top - crop.bottom) * 100;
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="1" height="1" viewBox="0 0 100 100" preserveAspectRatio="none"><rect width="100" height="100" fill="white" fill-opacity="${CROPPED_AWAY_OPACITY}"/><rect x="${x}" y="${y}" width="${width}" height="${height}" fill="white"/></svg>`;
  return `url("data:image/svg+xml,${encodeURIComponent(svg)}")`;
};
var cropPreviewAttribute = "data-remotion-studio-crop-preview";
var cropPreviewId = 0;
var useCropPreview = (target) => {
  useLayoutEffect5(() => {
    if (target === undefined || target.cropDrag === null) {
      return;
    }
    const element = target.ref.current;
    if (!(element instanceof HTMLElement || element instanceof SVGElement)) {
      return;
    }
    const mask = makeCropPreviewMask(target.crop);
    const previewId = String(++cropPreviewId);
    const styleElement = element.ownerDocument.createElement("style");
    styleElement.textContent = `
[${cropPreviewAttribute}="${previewId}"] {
	clip-path: none !important;
	mask-image: ${mask} !important;
	-webkit-mask-image: ${mask} !important;
	mask-position: 0 0 !important;
	-webkit-mask-position: 0 0 !important;
	mask-repeat: no-repeat !important;
	-webkit-mask-repeat: no-repeat !important;
	mask-size: 100% 100% !important;
	-webkit-mask-size: 100% 100% !important;
}`;
    element.setAttribute(cropPreviewAttribute, previewId);
    (element.ownerDocument.head ?? element.ownerDocument.documentElement).append(styleElement);
    return () => {
      if (element.getAttribute(cropPreviewAttribute) === previewId) {
        element.removeAttribute(cropPreviewAttribute);
      }
      styleElement.remove();
    };
  }, [target]);
};
var CropHandle = ({ handle, outline, onDraggingChange, target }) => {
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext26(Internals27.VisualModeSettersContext);
  const { cropDrag } = target;
  const cursor = cursorForHandle(handle);
  const path = useMemo28(() => getCropHandlePath({ handle, points: outline.points }), [handle, outline.points]);
  const onPointerDown = React35.useCallback((event) => {
    if (event.button !== 0 || cropDrag === null) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    if (commitPendingInspectorFields()) {
      return;
    }
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const svgRect = svg.getBoundingClientRect();
    const uncroppedPoints = outline.uncroppedPoints ?? outline.points;
    const initialValues = getCropValues(cropDrag);
    let lastValues = initialValues;
    let dragStarted = false;
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      if (!dragStarted) {
        dragStarted = true;
        onDraggingChange(true);
        forceSpecificCursor(cursor ?? "default");
      }
      const uv = getUvCoordinateForPoint(uncroppedPoints, {
        x: moveEvent.clientX - svgRect.left,
        y: moveEvent.clientY - svgRect.top
      });
      lastValues = getSelectedOutlineCropDragValues({
        crop: initialValues,
        dimensions: outline.dimensions,
        handle,
        uv
      });
      const transformOrigin = getSelectedOutlineCropFollowingTransformOrigin({
        dimensions: outline.dimensions,
        initialCrop: initialValues,
        nextCrop: lastValues,
        transformOrigin: cropDrag.transformOrigin
      });
      for (const fieldKey of Object.values(cropFieldKeys)) {
        const field = cropDrag.fields[fieldKey];
        const value = lastValues[fieldKey];
        if (value === field.value) {
          continue;
        }
        setDragOverrides(cropDrag.nodePath, fieldKey, field.propStatus.status === "keyframed" ? Internals27.makeKeyframedDragOverride({
          status: field.propStatus,
          frame: cropDrag.sourceFrame,
          value
        }) : Internals27.makeStaticDragOverride(value));
      }
      if (transformOrigin !== null) {
        setDragOverrides(cropDrag.nodePath, transformOriginFieldKey, Internals27.makeStaticDragOverride(transformOrigin));
      }
    };
    const onPointerUp = () => {
      if (dragStarted) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      const changes = getSelectedOutlineCropDragChanges({
        dimensions: outline.dimensions,
        target: cropDrag,
        values: lastValues
      });
      const staticChanges = changes.filter((change) => change.type === "static");
      const keyframedChanges = changes.filter((change) => change.type === "keyframed");
      const promise = staticChanges.length === 0 ? callAddKeyframes({
        sequenceKeyframes: keyframedChanges,
        effectKeyframes: [],
        setPropStatuses,
        clientId: cropDrag.clientId
      }) : saveSequenceProps({
        changes: staticChanges,
        addedKeyframes: keyframedChanges,
        movedKeyframes: null,
        setPropStatuses,
        clientId: cropDrag.clientId,
        undoLabel: "Crop sequence",
        redoLabel: "Crop sequence back"
      });
      promise.catch((err) => {
        showNotification(`Could not save crop: ${err instanceof Error ? err.message : String(err)}`, 4000);
      }).finally(() => {
        clearDragOverrides(cropDrag.nodePath);
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    clearDragOverrides,
    cropDrag,
    cursor,
    handle,
    onDraggingChange,
    outline.dimensions,
    outline.points,
    outline.uncroppedPoints,
    setDragOverrides,
    setPropStatuses
  ]);
  return /* @__PURE__ */ jsxs30(Fragment14, {
    children: [
      /* @__PURE__ */ jsx57("path", {
        d: path,
        fill: "none",
        stroke: TRANSPARENT,
        strokeWidth: CROP_HANDLE_HIT_WIDTH,
        strokeLinejoin: "miter",
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "stroke",
        cursor,
        onPointerDown
      }),
      /* @__PURE__ */ jsx57("path", {
        d: path,
        fill: "none",
        stroke: BLUE,
        strokeWidth: CROP_HANDLE_THICKNESS,
        strokeLinejoin: "miter",
        strokeLinecap: "butt",
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "none"
      })
    ]
  });
};
var SelectedOutlineCropControls = ({ outline, onDraggingChange, target }) => {
  useCropPreview(target);
  if (target?.cropDrag === null || target?.cropDrag === undefined) {
    return null;
  }
  return /* @__PURE__ */ jsx57("g", {
    "aria-hidden": "true",
    children: cropHandles.map((handle) => /* @__PURE__ */ jsx57(CropHandle, {
      handle,
      outline,
      onDraggingChange,
      target
    }, handle))
  });
};

// src/components/SelectedOutlineRotationCornerHandle.tsx
import React36, { useContext as useContext27, useMemo as useMemo29, useRef as useRef12 } from "react";
import { Internals as Internals28 } from "remotion";

// src/components/svg-point-to-client-point.ts
var svgPointToClientPoint = (point, rect) => {
  return {
    x: point.x + rect.left,
    y: point.y + rect.top
  };
};

// src/components/SelectedOutlineRotationCornerHandle.tsx
import { jsx as jsx58, jsxs as jsxs31, Fragment as Fragment15 } from "react/jsx-runtime";
var SelectedOutlineRotationCornerHandle = ({
  getAllRotationDragTargets,
  corner,
  dragging,
  handlePoint,
  outline,
  onDraggingChange,
  onContextMenuOpen,
  onContextMenuOpenChange,
  onHoverChange,
  onSelect,
  radius,
  target
}) => {
  const { getDragOverrides } = useContext27(Internals28.VisualModeDragOverridesContext);
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext27(Internals28.VisualModeSettersContext);
  const { editorSnapping } = useContext27(EditorSnappingContext);
  const rotationDrag = target?.rotationDrag ?? null;
  const selected = target?.selected ?? false;
  const containsSelection = target?.containsSelection ?? false;
  const circleRef = useRef12(null);
  const cornerInfo = useMemo29(() => getSelectedOutlineRotationCornerInfo(outline.points, corner), [corner, outline.points]);
  const onPointerDown = React36.useCallback((event) => {
    if (event.button !== 0 || rotationDrag === null) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const interaction = getOutlineSelectionInteraction(event);
    const shouldUpdateSelection = !selected && !containsSelection || interaction.toggleKey;
    if (shouldUpdateSelection && target !== undefined) {
      onSelect(target.selection, {
        shiftKey: false,
        toggleKey: interaction.toggleKey
      });
    }
    if (interaction.toggleKey) {
      return;
    }
    if (commitPendingInspectorFields()) {
      return;
    }
    const startPointer = { x: event.clientX, y: event.clientY };
    const svgRect = svg.getBoundingClientRect();
    const center = svgPointToClientPoint(getSelectedOutlineRotationPivot({
      dimensions: outline.dimensions,
      points: outline.uncroppedPoints ?? outline.points,
      transformOriginValue: rotationDrag.transformOriginValue
    }), svgRect);
    const dragStates = getSelectedOutlineRotationDragStates({
      dragTargets: selected ? getAllRotationDragTargets() : [rotationDrag],
      getDragOverrides,
      timelinePosition: getCurrentFrame()
    });
    let previousAngle = getAngleDegrees(center, {
      x: event.clientX,
      y: event.clientY
    });
    let accumulatedDelta = 0;
    let rotationLocked = event.shiftKey;
    let lastValues = new Map;
    let dragStarted = false;
    const updateRotationDragOverrides = () => {
      const rotationDeltaDegrees = rotationLocked && editorSnapping ? snapSelectedOutlineRotationDeltaDegrees({
        dragStates,
        rotationDeltaDegrees: accumulatedDelta
      }) : accumulatedDelta;
      lastValues = getSelectedOutlineRotationDragValues({
        dragStates,
        rotationDeltaDegrees
      });
      forceSpecificCursor(getRotationCursor(cornerInfo.cursorDegrees + rotationDeltaDegrees));
      for (const dragState of dragStates) {
        const value = lastValues.get(dragState.key);
        if (value === undefined) {
          throw new Error("Expected rotation drag value to be available");
        }
        if (dragState.target.propStatus.status === "keyframed") {
          setDragOverrides(dragState.target.nodePath, rotateFieldKey, Internals28.makeKeyframedDragOverride({
            status: dragState.target.propStatus,
            frame: dragState.sourceFrame,
            value
          }));
        } else {
          setDragOverrides(dragState.target.nodePath, rotateFieldKey, Internals28.makeStaticDragOverride(value));
        }
      }
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const screenDeltaX = moveEvent.clientX - startPointer.x;
      const screenDeltaY = moveEvent.clientY - startPointer.y;
      if (!dragStarted) {
        if (!isSelectedOutlineDragPastThreshold({
          deltaX: screenDeltaX,
          deltaY: screenDeltaY
        })) {
          return;
        }
        dragStarted = true;
        onDraggingChange(true);
      }
      const nextAngle = getAngleDegrees(center, {
        x: moveEvent.clientX,
        y: moveEvent.clientY
      });
      accumulatedDelta += getSelectedOutlineRotationDeltaDegrees({
        from: previousAngle,
        to: nextAngle
      });
      previousAngle = nextAngle;
      rotationLocked = moveEvent.shiftKey;
      updateRotationDragOverrides();
    };
    const onKeyChange = (keyEvent) => {
      if (keyEvent.key !== "Shift") {
        return;
      }
      const nextRotationLocked = keyEvent.type === "keydown";
      if (nextRotationLocked === rotationLocked) {
        return;
      }
      rotationLocked = nextRotationLocked;
      if (dragStarted) {
        updateRotationDragOverrides();
      }
    };
    const onPointerUp = () => {
      window.removeEventListener("keydown", onKeyChange);
      window.removeEventListener("keyup", onKeyChange);
      if (dragStarted) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      const changes = getSelectedOutlineRotationDragChanges({
        dragStates,
        lastValues
      });
      if (changes.length === 0) {
        clearSelectedOutlineRotationDragOverrides({
          clearDragOverrides,
          dragStates
        });
        return;
      }
      const staticChanges = changes.filter((change) => change.type === "static");
      const keyframedChanges = changes.filter((change) => change.type === "keyframed");
      Promise.all([
        staticChanges.length > 0 ? saveSequenceProps({
          changes: staticChanges,
          addedKeyframes: null,
          movedKeyframes: null,
          setPropStatuses,
          clientId: rotationDrag.clientId,
          undoLabel: changes.length > 1 ? "Rotate selected sequences" : "Rotate sequence",
          redoLabel: changes.length > 1 ? "Rotate selected sequences back" : "Rotate sequence back"
        }) : Promise.resolve(),
        callAddKeyframes({
          sequenceKeyframes: keyframedChanges,
          effectKeyframes: [],
          setPropStatuses,
          clientId: rotationDrag.clientId
        })
      ]).catch((err) => {
        showNotification(`Could not save sequence props: ${err instanceof Error ? err.message : String(err)}`, 4000);
      }).finally(() => {
        clearSelectedOutlineRotationDragOverrides({
          clearDragOverrides,
          dragStates
        });
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
    window.addEventListener("keydown", onKeyChange);
    window.addEventListener("keyup", onKeyChange);
  }, [
    clearDragOverrides,
    containsSelection,
    cornerInfo,
    editorSnapping,
    getDragOverrides,
    getAllRotationDragTargets,
    onDraggingChange,
    outline.dimensions,
    outline.points,
    outline.uncroppedPoints,
    onSelect,
    rotationDrag,
    selected,
    setPropStatuses,
    setDragOverrides,
    target
  ]);
  if (rotationDrag === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs31(Fragment15, {
    children: [
      /* @__PURE__ */ jsx58("circle", {
        ref: circleRef,
        cx: handlePoint.x,
        cy: handlePoint.y,
        r: radius,
        fill: TRANSPARENT,
        stroke: TRANSPARENT,
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "all",
        cursor: cornerInfo.cursor,
        "data-remotion-studio-rotation-corner": corner,
        "data-remotion-studio-rotation-corner-contains-selection": containsSelection,
        onPointerEnter: () => {
          if (!dragging) {
            onHoverChange(outline.key);
          }
        },
        onPointerLeave: () => {
          if (!dragging) {
            onHoverChange(null);
          }
        },
        onPointerDown
      }),
      /* @__PURE__ */ jsx58(ContextMenuForTarget, {
        triggerRef: circleRef,
        getItems: onContextMenuOpen,
        onOpenChange: onContextMenuOpenChange
      })
    ]
  });
};

// src/components/SelectedOutlineScaleEdgeLine.tsx
import React37, { useContext as useContext28, useMemo as useMemo30, useRef as useRef13 } from "react";
import { Internals as Internals29 } from "remotion";
import { jsx as jsx59, jsxs as jsxs32, Fragment as Fragment16 } from "react/jsx-runtime";
var SelectedOutlineScaleEdgeLine = ({
  getAllScaleDragTargets,
  dragging,
  edge,
  hitWidth,
  outline,
  onDraggingChange,
  onContextMenuOpen,
  onContextMenuOpenChange,
  onHoverChange,
  onSelect,
  target
}) => {
  const { getDragOverrides } = useContext28(Internals29.VisualModeDragOverridesContext);
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext28(Internals29.VisualModeSettersContext);
  const scaleDrag = target?.scaleDrag ?? null;
  const selected = target?.selected ?? false;
  const containsSelection = target?.containsSelection ?? false;
  const lineRef = useRef13(null);
  const edgeInfo = useMemo30(() => getSelectedOutlineScaleEdgeInfo(outline.points, edge), [edge, outline.points]);
  const onPointerDown = React37.useCallback((event) => {
    if (event.button !== 0 || scaleDrag === null || edgeInfo === null) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const interaction = getOutlineSelectionInteraction(event);
    const shouldUpdateSelection = !selected && !containsSelection || interaction.shiftKey || interaction.toggleKey;
    if (shouldUpdateSelection && target !== undefined) {
      onSelect(target.selection, interaction);
    }
    if (interaction.shiftKey || interaction.toggleKey) {
      return;
    }
    if (commitPendingInspectorFields()) {
      return;
    }
    const startPointer = { x: event.clientX, y: event.clientY };
    const dragStates = getSelectedOutlineScaleDragStates({
      dragTargets: selected ? getAllScaleDragTargets() : [scaleDrag],
      getDragOverrides,
      timelinePosition: getCurrentFrame()
    });
    let lastValues = new Map;
    let dragStarted = false;
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const delta = {
        x: moveEvent.clientX - startPointer.x,
        y: moveEvent.clientY - startPointer.y
      };
      if (!dragStarted) {
        if (!isSelectedOutlineDragPastThreshold({
          deltaX: delta.x,
          deltaY: delta.y
        })) {
          return;
        }
        dragStarted = true;
        onDraggingChange(true);
        forceSpecificCursor(edgeInfo.cursor);
      }
      const projectedDelta = dot(delta, edgeInfo.normal);
      const scaleFactor = Math.max(0.001, 1 + projectedDelta / edgeInfo.extent);
      lastValues = getSelectedOutlineScaleDragValues({
        dragStates,
        axis: edgeInfo.axis,
        scaleFactor
      });
      for (const dragState of dragStates) {
        const value = lastValues.get(dragState.key);
        if (value === undefined) {
          throw new Error("Expected scale drag value to be available");
        }
        if (dragState.target.propStatus.status === "keyframed") {
          setDragOverrides(dragState.target.nodePath, scaleFieldKey, Internals29.makeKeyframedDragOverride({
            status: dragState.target.propStatus,
            frame: dragState.sourceFrame,
            value
          }));
        } else {
          setDragOverrides(dragState.target.nodePath, scaleFieldKey, Internals29.makeStaticDragOverride(value));
        }
      }
    };
    const onPointerUp = () => {
      if (dragStarted) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      const changes = getSelectedOutlineScaleDragChanges({
        dragStates,
        lastValues
      });
      if (changes.length === 0) {
        clearSelectedOutlineScaleDragOverrides({
          clearDragOverrides,
          dragStates
        });
        return;
      }
      const staticChanges = changes.filter((change) => change.type === "static");
      const keyframedChanges = changes.filter((change) => change.type === "keyframed");
      Promise.all([
        staticChanges.length > 0 ? saveSequenceProps({
          changes: staticChanges,
          addedKeyframes: null,
          movedKeyframes: null,
          setPropStatuses,
          clientId: scaleDrag.clientId,
          undoLabel: changes.length > 1 ? "Scale selected sequences" : "Scale sequence",
          redoLabel: changes.length > 1 ? "Scale selected sequences back" : "Scale sequence back"
        }) : Promise.resolve(),
        callAddKeyframes({
          sequenceKeyframes: keyframedChanges,
          effectKeyframes: [],
          setPropStatuses,
          clientId: scaleDrag.clientId
        })
      ]).catch((err) => {
        showNotification(`Could not save sequence props: ${err instanceof Error ? err.message : String(err)}`, 4000);
      }).finally(() => {
        clearSelectedOutlineScaleDragOverrides({
          clearDragOverrides,
          dragStates
        });
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    clearDragOverrides,
    containsSelection,
    getAllScaleDragTargets,
    edgeInfo,
    getDragOverrides,
    onDraggingChange,
    onSelect,
    scaleDrag,
    selected,
    setPropStatuses,
    setDragOverrides,
    target
  ]);
  if (scaleDrag === null || edgeInfo === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs32(Fragment16, {
    children: [
      /* @__PURE__ */ jsx59("line", {
        ref: lineRef,
        x1: edgeInfo.start.x,
        y1: edgeInfo.start.y,
        x2: edgeInfo.end.x,
        y2: edgeInfo.end.y,
        stroke: TRANSPARENT,
        strokeWidth: hitWidth,
        strokeLinecap: "butt",
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "stroke",
        cursor: edgeInfo.cursor,
        "data-remotion-studio-scale-edge": edge,
        "data-remotion-studio-scale-edge-contains-selection": target?.containsSelection ?? false,
        onPointerEnter: () => {
          if (!dragging) {
            onHoverChange(outline.key);
          }
        },
        onPointerLeave: () => {
          if (!dragging) {
            onHoverChange(null);
          }
        },
        onPointerDown
      }),
      /* @__PURE__ */ jsx59(ContextMenuForTarget, {
        triggerRef: lineRef,
        getItems: onContextMenuOpen,
        onOpenChange: onContextMenuOpenChange
      })
    ]
  });
};

// src/components/use-selected-outline-control-target.ts
import React38, { useLayoutEffect as useLayoutEffect7, useMemo as useMemo31, useRef as useRef14 } from "react";
var useSelectedOutlineControlTarget = ({
  getLatestTargetByKey,
  layoutTarget
}) => {
  const setHoveredSequence = useSetTimelineSequenceHover();
  const targetRef = useRef14(layoutTarget);
  useLayoutEffect7(() => {
    targetRef.current = layoutTarget;
  }, [layoutTarget]);
  const getTarget = React38.useCallback(() => {
    const currentTarget = targetRef.current;
    if (currentTarget === undefined) {
      return;
    }
    return getLatestTargetByKey(currentTarget.key);
  }, [getLatestTargetByKey]);
  const getLayoutTarget = React38.useCallback(() => targetRef.current, []);
  const hoveredNodePathKey = useMemo31(() => layoutTarget === undefined ? null : timelineSequenceNodePathToKey(layoutTarget.nodePathInfo.sequenceSubscriptionKey), [layoutTarget]);
  const hovered = useIsTimelineSequenceHovered(hoveredNodePathKey);
  const controlTarget = layoutTarget !== undefined && (layoutTarget.containsSelection || hovered) ? getLatestTargetByKey(layoutTarget.key) : undefined;
  const onHoverChange = React38.useCallback((key4) => {
    setHoveredSequence((currentHover) => {
      if (key4 !== null) {
        const hoverTarget = targetRef.current;
        if (hoverTarget === undefined || hoverTarget.key !== key4) {
          return currentHover;
        }
        return {
          key: key4,
          nodePathKey: timelineSequenceNodePathToKey(hoverTarget.nodePathInfo.sequenceSubscriptionKey),
          source: "canvas"
        };
      }
      return currentHover?.source === "canvas" ? null : currentHover;
    });
  }, [setHoveredSequence]);
  return { controlTarget, getLayoutTarget, getTarget, hovered, onHoverChange };
};

// src/components/SelectedOutlineEditingHandles.tsx
import { jsx as jsx60, jsxs as jsxs33, Fragment as Fragment17 } from "react/jsx-runtime";
var SelectedOutlineEditingHandles = ({
  dragging,
  getAllRotationDragTargets,
  getAllScaleDragTargets,
  getContextMenuOpenByKey,
  getLatestTargetByKey,
  layoutTarget,
  onContextMenuOpenChange,
  onDraggingChange,
  onSelect,
  outline
}) => {
  const { controlTarget, hovered, onHoverChange } = useSelectedOutlineControlTarget({
    getLatestTargetByKey,
    layoutTarget
  });
  const controlLayout = useMemo33(() => getSelectedOutlineControlLayout(outline.points), [outline.points]);
  const onContextMenuOpen = React39.useCallback(() => {
    return getContextMenuOpenByKey(outline.key)?.() ?? false;
  }, [getContextMenuOpenByKey, outline.key]);
  return /* @__PURE__ */ jsxs33(Fragment17, {
    children: [
      /* @__PURE__ */ jsx60(SelectedOutlineCropControls, {
        outline,
        onDraggingChange,
        target: controlTarget
      }),
      controlTarget?.cropDrag === null && (layoutTarget?.containsSelection || hovered) ? controlLayout.scaleEdges.map((edge) => /* @__PURE__ */ jsx60(SelectedOutlineScaleEdgeLine, {
        getAllScaleDragTargets,
        dragging,
        edge,
        hitWidth: edge === "top" || edge === "bottom" ? controlLayout.scaleHitWidth.horizontal : controlLayout.scaleHitWidth.vertical,
        outline,
        onContextMenuOpen,
        onContextMenuOpenChange,
        onDraggingChange,
        onHoverChange,
        onSelect,
        target: controlTarget
      }, edge)) : null,
      controlTarget?.cropDrag === null && (layoutTarget?.containsSelection || hovered) ? controlLayout.rotationCorners.map(({ corner, point }) => /* @__PURE__ */ jsx60(SelectedOutlineRotationCornerHandle, {
        getAllRotationDragTargets,
        corner,
        dragging,
        handlePoint: point,
        outline,
        onContextMenuOpen,
        onContextMenuOpenChange,
        onDraggingChange,
        onHoverChange,
        onSelect,
        radius: controlLayout.rotationHandleRadius,
        target: controlTarget
      }, corner)) : null
    ]
  });
};

// src/components/SelectedOutlineElement.tsx
import React42, { useContext as useContext31, useLayoutEffect as useLayoutEffect8 } from "react";
import { Internals as Internals33 } from "remotion";

// src/helpers/get-sequence-double-click-action.ts
var getSequenceDoubleClickAction = ({
  button,
  canOpenSource,
  numberOfConnectedCompositions,
  sequenceWasDragged
}) => {
  if (sequenceWasDragged) {
    return null;
  }
  if (button !== 0) {
    return null;
  }
  if (numberOfConnectedCompositions === 1) {
    return "open-connected-composition";
  }
  return canOpenSource ? "open-source" : null;
};
var getConnectedCompositionFrame = ({
  timelinePosition,
  sequence,
  sequenceFrameOffset
}) => {
  const relativeFrame = timelinePosition - sequence.from;
  if (relativeFrame < 0 || relativeFrame >= sequence.duration) {
    return null;
  }
  return sequence.frozenFrame ?? relativeFrame + sequenceFrameOffset;
};

// src/components/SelectedOutlineCanvasRotation.tsx
import React40, { useContext as useContext29 } from "react";
import { Internals as Internals30 } from "remotion";

// src/components/canvas-rotation-cursor.ts
var canvasRotationCursorSvg = `<svg width="29" height="30" viewBox="0 0 83 85" fill="none" xmlns="http://www.w3.org/2000/svg">
<g filter="url(#shadow)">
<path fill-rule="evenodd" clip-rule="evenodd" d="M58.673 35.4941C58.6612 35.1059 58.6552 34.9899 58.6346 34.4705L58.605 33.8746C58.608 33.9357 58.5753 33.3889 58.5813 33.4469L58.5695 33.2635L54.9726 33.508V33.5201C54.9874 33.7371 54.9992 33.9389 55.011 34.1587C55.011 34.1556 55.0851 36.3955 55.0851 36.3924L55.0881 36.4688L55.0851 36.4382L55.0881 36.5268V36.591V36.5604V36.6521V36.6246V36.7163V36.6857L55.0911 36.7927L55.0881 36.7621L55.0911 36.8844V36.8538V36.9913V36.9607V37.1441L55.0908 37.1166V37.3C55.0908 40.1263 54.8952 43.0781 54.4568 46.0083C55.7693 45.6263 57.0555 45.1771 58.273 44.6517C58.5605 42.2072 58.6966 39.7506 58.6966 37.3031C58.6936 36.9149 58.6906 36.527 58.6848 36.1389L58.673 35.4941ZM21.7249 51.5816L11.6923 48.5781L13.6418 46.4055C11.1501 43.9916 9.3219 40.9118 9.3219 37.3C9.33972 34.501 10.3589 32.133 12.0242 29.9697C14.4835 26.8805 17.9916 24.5982 21.9176 22.9633C21.5738 24.4421 21.2893 25.9362 21.0613 27.4426C19.3486 28.3349 17.7752 29.386 16.4095 30.6144C14.7415 32.1421 13.3428 34.0304 13.0051 36.3039C12.7799 37.8317 13.0852 39.2831 13.7724 40.6338C14.3562 41.7765 15.1886 42.8063 16.0896 43.674L18.0364 41.5015L21.7249 51.5816ZM34.2108 19.8313C36.6997 19.5167 39.1175 19.3792 41.3219 19.3792C46.5515 19.3792 51.8463 20.0576 56.8656 21.5854C63.7397 23.6785 73.2715 28.711 73.3219 37.3001C73.3041 40.096 72.2879 42.464 70.6226 44.6274C64.2492 52.639 50.8152 55.221 41.3218 55.221C38.6848 55.221 35.9232 55.056 33.3307 54.6647L33.8196 50.9828C36.2492 51.3495 38.8536 51.5022 41.3248 51.5022C49.3544 51.5022 59.9855 49.6047 66.2374 43.9856C68.1455 42.2377 69.6953 40.0224 69.7189 37.3C69.6804 32.8542 65.5411 29.6123 62.0922 27.7239C55.9115 24.3841 48.2344 23.0948 41.3218 23.0948C38.7026 23.0948 35.8047 23.2965 32.8715 23.7639C33.2479 22.4132 33.6923 21.0965 34.2108 19.8313ZM50.8745 58.1816C52.3146 57.9493 53.7634 57.6652 55.1974 57.317C52.6789 63.6206 48.1633 70.2572 41.3218 70.3C38.6077 70.2816 36.3114 69.2337 34.2137 67.5163C26.4448 60.9408 23.9441 47.09 23.9441 37.3C23.9441 31.91 24.602 26.4466 26.0834 21.2705C28.1131 14.1817 32.9931 4.35195 41.322 4.3C45.63 4.3 49.1057 7.12951 51.5916 10.5272L53.8967 8.76404L55.7041 19.3699L46.3885 14.5025L48.6936 12.7393C46.9723 10.3347 44.3528 8.01873 41.3217 8.01873C37.0106 8.05836 33.8669 12.3302 32.0358 15.8868C28.7972 22.2546 27.5469 30.1715 27.5469 37.3001C27.5469 45.5806 29.387 56.544 34.8388 62.9912C36.5337 64.9589 38.6788 66.5571 41.3217 66.5815C45.6328 66.5418 48.7765 62.2731 50.6076 58.7165L50.8745 58.1816Z" fill="black" stroke="white" stroke-width="3" stroke-linejoin="round" paint-order="stroke"/>
</g>
<defs>
<filter id="shadow" x="0" y="0" width="83" height="85" filterUnits="userSpaceOnUse">
<feDropShadow dx="0" dy="5" stdDeviation="3.15" flood-color="black" flood-opacity="0.25"/>
</filter>
</defs>
</svg>`;
var canvasRotationCursor = `url("data:image/svg+xml,${encodeURIComponent(canvasRotationCursorSvg)}") 15 13, alias`;

// src/components/SelectedOutlineCanvasRotation.tsx
import { jsx as jsx61 } from "react/jsx-runtime";
var rotationDegreesPerPixel = 0.5;
var canvas2DRotationCursor = getRotationCursor(0);
var SelectedOutlineCanvasRotation = ({
  getLatestTargetByKey,
  layoutTarget,
  onDraggingChange,
  outline,
  transform3DMode
}) => {
  const { getDragOverrides } = useContext29(Internals30.VisualModeDragOverridesContext);
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext29(Internals30.VisualModeSettersContext);
  const { editorSnapping } = useContext29(EditorSnappingContext);
  const [commandKeyPressed, setCommandKeyPressed] = React40.useState(false);
  const cursor = transform3DMode ? canvasRotationCursor : canvas2DRotationCursor;
  React40.useEffect(() => {
    const commandKey = isMac ? "Meta" : "Control";
    const onKeyDown = (event) => {
      if (event.key === commandKey) {
        setCommandKeyPressed(true);
      }
    };
    const onKeyUp = (event) => {
      if (event.key === commandKey) {
        setCommandKeyPressed(false);
      }
    };
    const onBlur = () => {
      setCommandKeyPressed(false);
    };
    window.addEventListener("keydown", onKeyDown);
    window.addEventListener("keyup", onKeyUp);
    window.addEventListener("blur", onBlur);
    return () => {
      window.removeEventListener("keydown", onKeyDown);
      window.removeEventListener("keyup", onKeyUp);
      window.removeEventListener("blur", onBlur);
    };
  }, []);
  const onPointerDown = React40.useCallback((event) => {
    if (event.button !== 0) {
      return;
    }
    const target = getLatestTargetByKey(layoutTarget.key);
    const rotationDrag = target?.rotationDrag ?? null;
    if (rotationDrag === null || !target?.selectedForRotation) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    if (commitPendingInspectorFields()) {
      return;
    }
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const previousSvgCursor = svg.style.cursor;
    const dragCursor = rotationDrag.transform3DMode ? canvasRotationCursor : canvas2DRotationCursor;
    svg.style.cursor = dragCursor;
    const startPointer = { x: event.clientX, y: event.clientY };
    const center = svgPointToClientPoint(getSelectedOutlineRotationPivot({
      dimensions: outline.dimensions,
      points: outline.uncroppedPoints ?? outline.points,
      transformOriginValue: rotationDrag.transformOriginValue
    }), svg.getBoundingClientRect());
    const dragStates = getSelectedOutlineRotationDragStates({
      dragTargets: [rotationDrag],
      getDragOverrides,
      timelinePosition: getCurrentFrame()
    });
    let previousAngle = getAngleDegrees(center, startPointer);
    let accumulatedDelta = 0;
    let lastValues = new Map;
    let dragStarted = false;
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const screenDeltaX = moveEvent.clientX - startPointer.x;
      const screenDeltaY = moveEvent.clientY - startPointer.y;
      if (!dragStarted) {
        if (!isSelectedOutlineDragPastThreshold({
          deltaX: screenDeltaX,
          deltaY: screenDeltaY
        })) {
          return;
        }
        dragStarted = true;
        forceSpecificCursor(dragCursor);
        onDraggingChange(true);
      }
      if (rotationDrag.transform3DMode) {
        const startRotation = dragStates[0]?.startRotation ?? [0, 0, 0];
        let rotationXDeltaDegrees = -screenDeltaY * rotationDegreesPerPixel;
        let rotationYDeltaDegrees = screenDeltaX * rotationDegreesPerPixel;
        if (moveEvent.shiftKey && editorSnapping) {
          rotationXDeltaDegrees = Math.round((startRotation[0] + rotationXDeltaDegrees) / 15) * 15 - startRotation[0];
          rotationYDeltaDegrees = Math.round((startRotation[1] + rotationYDeltaDegrees) / 15) * 15 - startRotation[1];
        }
        lastValues = getSelectedOutline3DRotationDragValues({
          dragStates,
          rotationXDeltaDegrees,
          rotationYDeltaDegrees
        });
      } else {
        const nextAngle = getAngleDegrees(center, {
          x: moveEvent.clientX,
          y: moveEvent.clientY
        });
        accumulatedDelta += getSelectedOutlineRotationDeltaDegrees({
          from: previousAngle,
          to: nextAngle
        });
        previousAngle = nextAngle;
        const rotationDeltaDegrees = moveEvent.shiftKey && editorSnapping ? snapSelectedOutlineRotationDeltaDegrees({
          dragStates,
          rotationDeltaDegrees: accumulatedDelta
        }) : accumulatedDelta;
        lastValues = getSelectedOutlineRotationDragValues({
          dragStates,
          rotationDeltaDegrees
        });
      }
      for (const dragState of dragStates) {
        const value = lastValues.get(dragState.key);
        if (value === undefined) {
          throw new Error("Expected rotation drag value to be available");
        }
        if (dragState.target.propStatus.status === "keyframed") {
          setDragOverrides(dragState.target.nodePath, rotateFieldKey, Internals30.makeKeyframedDragOverride({
            status: dragState.target.propStatus,
            frame: dragState.sourceFrame,
            value
          }));
        } else {
          setDragOverrides(dragState.target.nodePath, rotateFieldKey, Internals30.makeStaticDragOverride(value));
        }
      }
    };
    const onPointerUp = () => {
      svg.style.cursor = previousSvgCursor;
      if (dragStarted) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      const changes = getSelectedOutlineRotationDragChanges({
        dragStates,
        lastValues
      });
      if (changes.length === 0) {
        clearSelectedOutlineRotationDragOverrides({
          clearDragOverrides,
          dragStates
        });
        return;
      }
      const staticChanges = changes.filter((change) => change.type === "static");
      const keyframedChanges = changes.filter((change) => change.type === "keyframed");
      Promise.all([
        staticChanges.length > 0 ? saveSequenceProps({
          changes: staticChanges,
          addedKeyframes: null,
          movedKeyframes: null,
          setPropStatuses,
          clientId: rotationDrag.clientId,
          undoLabel: "Rotate sequence",
          redoLabel: "Rotate sequence back"
        }) : Promise.resolve(),
        callAddKeyframes({
          sequenceKeyframes: keyframedChanges,
          effectKeyframes: [],
          setPropStatuses,
          clientId: rotationDrag.clientId
        })
      ]).catch((error) => {
        showNotification(`Could not save sequence props: ${error instanceof Error ? error.message : String(error)}`, 4000);
      }).finally(() => {
        clearSelectedOutlineRotationDragOverrides({
          clearDragOverrides,
          dragStates
        });
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: svg,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    clearDragOverrides,
    editorSnapping,
    getDragOverrides,
    getLatestTargetByKey,
    layoutTarget.key,
    onDraggingChange,
    outline.dimensions,
    outline.points,
    outline.uncroppedPoints,
    setDragOverrides,
    setPropStatuses
  ]);
  return /* @__PURE__ */ jsx61("polygon", {
    points: outline.points.map((point) => `${point.x},${point.y}`).join(" "),
    fill: TRANSPARENT,
    pointerEvents: commandKeyPressed ? "none" : "all",
    cursor: commandKeyPressed ? "default" : cursor,
    onPointerDown,
    "data-remotion-studio-canvas-rotation": true
  });
};

// src/components/SelectedOutlinePolygon.tsx
import React41, { useContext as useContext30, useMemo as useMemo35, useRef as useRef15, useState as useState18 } from "react";
import { Internals as Internals31 } from "remotion";
import { jsx as jsx63 } from "react/jsx-runtime";
var SELECTED_OUTLINE_KEY_ATTR = "data-remotion-studio-selected-outline-key";
var SelectedOutlinePolygonUnmemoized = ({
  compositionHeight,
  compositionWidth,
  containsSelection,
  directlySelected,
  dragging,
  getAllDragOutlines,
  getAllDragTargets,
  getLayoutTarget,
  getTarget,
  hasTarget,
  hovered,
  outline,
  onDraggingChange,
  onHoverChange,
  onSnapPointsChange,
  onSelect,
  onDoubleClickTarget,
  scale,
  showSelectedOutline,
  translateWithCommandKey
}) => {
  const { previewServerState } = useContext30(StudioServerConnectionCtx);
  const dragAwareDoubleClick = useMemo35(() => createDragAwareDoubleClickTracker(), []);
  const { canvasContent } = useContext30(Internals31.CompositionManager);
  const { getDragOverrides } = useContext30(Internals31.VisualModeDragOverridesContext);
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext30(Internals31.VisualModeSettersContext);
  const { editorSnapping } = useContext30(EditorSnappingContext);
  const { editorShowGuides, guidesList } = useContext30(EditorShowGuidesContext);
  const polygonRef = useRef15(null);
  const points = useMemo35(() => outline.points.map(pointToString).join(" "), [outline.points]);
  const [effectDropHovered, setEffectDropHovered] = useState18(false);
  const visible = showSelectedOutline || hovered;
  const getEffectDropTarget = React41.useCallback(() => {
    if (previewServerState.type !== "connected" || !isStudioInteractivityEnabled()) {
      return null;
    }
    const target = getLayoutTarget();
    if (target?.sequence.controls?.supportsEffects !== true) {
      return null;
    }
    const nodePath = target.nodePathInfo.sequenceSubscriptionKey;
    return {
      clientId: previewServerState.clientId,
      fileName: nodePath.absolutePath,
      nodePath
    };
  }, [getLayoutTarget, previewServerState]);
  const onPointerDown = React41.useCallback((event) => {
    const target = getTarget();
    if (event.button !== 0 || target === undefined) {
      return;
    }
    const { drag, selected } = target;
    event.preventDefault();
    event.stopPropagation();
    const temporaryTranslate = translateWithCommandKey && (selected || containsSelection) && (isMac ? event.metaKey : event.ctrlKey);
    const interaction = temporaryTranslate ? { shiftKey: false, toggleKey: false } : getOutlineSelectionInteraction(event);
    const shouldUpdateSelection = !selected || interaction.shiftKey || interaction.toggleKey;
    const ownerSvg = polygonRef.current?.ownerSVGElement;
    let pointerInsideSelectedOutline = false;
    if (ownerSvg) {
      const screenPoint = ownerSvg.createSVGPoint();
      screenPoint.x = event.clientX;
      screenPoint.y = event.clientY;
      pointerInsideSelectedOutline = Array.from(ownerSvg.querySelectorAll('polygon[data-remotion-directly-selected-outline="true"]')).some((selectedPolygon) => {
        const screenTransform = selectedPolygon.getScreenCTM();
        if (screenTransform === null) {
          return false;
        }
        const polygonPoint = screenPoint.matrixTransform(screenTransform.inverse());
        return selectedPolygon.isPointInFill(polygonPoint) || selectedPolygon.isPointInStroke(polygonPoint);
      });
    }
    const deferSelection = !selected && !interaction.shiftKey && !interaction.toggleKey && (containsSelection || pointerInsideSelectedOutline);
    if (!deferSelection && shouldUpdateSelection) {
      onSelect(target.selection, interaction);
    }
    if (interaction.shiftKey || interaction.toggleKey || drag === null && !deferSelection) {
      return;
    }
    if (commitPendingInspectorFields()) {
      if (deferSelection) {
        onSelect(target.selection, interaction);
      }
      return;
    }
    const startPointerX = event.clientX;
    const startPointerY = event.clientY;
    const dragExistingSelection = selected || deferSelection;
    const dragTargets = dragExistingSelection ? getAllDragTargets() : drag === null ? [] : [drag];
    if (dragTargets.length === 0) {
      if (deferSelection) {
        onSelect(target.selection, interaction);
      }
      return;
    }
    const dragStates = getSelectedOutlineDragStates({
      dragTargets,
      getDragOverrides,
      timelinePosition: getCurrentFrame()
    });
    const dragOutlines = dragExistingSelection ? getAllDragOutlines() : [outline];
    const [{ clientId }] = dragTargets;
    let lastValues = new Map;
    let currentPointerX = startPointerX;
    let currentPointerY = startPointerY;
    let axisLocked = false;
    let dragStarted = false;
    let snappingDisabled = !temporaryTranslate && (event.metaKey || event.ctrlKey);
    let snapTargets = null;
    const updateDragOverrides = () => {
      const screenDeltaX = currentPointerX - startPointerX;
      const screenDeltaY = currentPointerY - startPointerY;
      if (!dragStarted) {
        if (!isSelectedOutlineDragPastThreshold({
          deltaX: screenDeltaX,
          deltaY: screenDeltaY
        })) {
          return;
        }
        dragStarted = true;
        onDraggingChange(true);
        forceSpecificCursor("default");
      }
      const axisLockedDirection = axisLocked ? Math.abs(screenDeltaX) >= Math.abs(screenDeltaY) ? "horizontal" : "vertical" : null;
      const dragDelta = applySelectedOutlineDragAxisLock({
        deltaX: screenDeltaX / scale,
        deltaY: screenDeltaY / scale,
        axisLocked
      });
      let { deltaX, deltaY } = dragDelta;
      if (editorSnapping && !snappingDisabled) {
        snapTargets ??= getSelectedOutlineSnapTargets({
          compositionHeight,
          compositionWidth,
          guides: editorShowGuides && canvasContent?.type === "composition" ? guidesList.filter((guide) => guide.compositionId === canvasContent.compositionId) : []
        });
        const snapResult = findSelectedOutlineSnap({
          allowX: axisLockedDirection !== "vertical",
          allowY: axisLockedDirection !== "horizontal",
          deltaX,
          deltaY,
          outlines: dragOutlines,
          scale,
          targets: snapTargets
        });
        if (snapResult.snapOffsetX !== null) {
          deltaX += snapResult.snapOffsetX;
        }
        if (snapResult.snapOffsetY !== null) {
          deltaY += snapResult.snapOffsetY;
        }
        onSnapPointsChange(snapResult.activeSnapPoints);
      } else {
        onSnapPointsChange([]);
      }
      lastValues = getSelectedOutlineDragValues({
        dragStates,
        deltaX,
        deltaY
      });
      for (const dragState of dragStates) {
        const value = lastValues.get(dragState.key);
        if (value === undefined) {
          throw new Error("Expected drag value to be available");
        }
        if (dragState.target.propStatus.status === "keyframed") {
          setDragOverrides(dragState.target.nodePath, translateFieldKey, Internals31.makeKeyframedDragOverride({
            status: dragState.target.propStatus,
            frame: dragState.sourceFrame,
            value
          }));
        } else {
          setDragOverrides(dragState.target.nodePath, translateFieldKey, Internals31.makeStaticDragOverride(value));
        }
      }
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      currentPointerX = moveEvent.clientX;
      currentPointerY = moveEvent.clientY;
      axisLocked = moveEvent.shiftKey;
      snappingDisabled = !temporaryTranslate && (moveEvent.metaKey || moveEvent.ctrlKey);
      updateDragOverrides();
    };
    const onKeyChange = (keyEvent) => {
      if (keyEvent.key !== "Shift") {
        return;
      }
      const nextAxisLocked = keyEvent.type === "keydown";
      if (nextAxisLocked === axisLocked) {
        return;
      }
      axisLocked = nextAxisLocked;
      updateDragOverrides();
    };
    const onPointerUp = (reason, endEvent) => {
      dragAwareDoubleClick.endPointerGesture(dragStarted);
      window.removeEventListener("keydown", onKeyChange);
      window.removeEventListener("keyup", onKeyChange);
      if (dragStarted) {
        stopForcingSpecificCursor();
        onSnapPointsChange([]);
        onDraggingChange(false);
      }
      const changes = getSelectedOutlineDragChanges({
        dragStates,
        lastValues
      });
      if (changes.length === 0) {
        clearSelectedOutlineDragOverrides({ clearDragOverrides, dragStates });
        if (deferSelection && !dragStarted && isPointerSessionRelease(reason, endEvent)) {
          onSelect(target.selection, interaction);
        }
        return;
      }
      const staticChanges = changes.filter((change) => change.type === "static");
      const keyframedChanges = changes.filter((change) => change.type === "keyframed");
      Promise.all([
        staticChanges.length > 0 ? saveSequenceProps({
          changes: staticChanges,
          addedKeyframes: null,
          movedKeyframes: null,
          setPropStatuses,
          clientId,
          undoLabel: changes.length > 1 ? "Move selected sequences" : "Move sequence",
          redoLabel: changes.length > 1 ? "Move selected sequences back" : "Move sequence back"
        }) : Promise.resolve(),
        callAddKeyframes({
          sequenceKeyframes: keyframedChanges,
          effectKeyframes: [],
          setPropStatuses,
          clientId
        })
      ]).catch((err) => {
        showNotification(`Could not save sequence props: ${err instanceof Error ? err.message : String(err)}`, 4000);
      }).finally(() => {
        clearSelectedOutlineDragOverrides({ clearDragOverrides, dragStates });
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
    window.addEventListener("keydown", onKeyChange);
    window.addEventListener("keyup", onKeyChange);
  }, [
    canvasContent,
    clearDragOverrides,
    compositionHeight,
    compositionWidth,
    containsSelection,
    dragAwareDoubleClick,
    editorShowGuides,
    editorSnapping,
    getAllDragOutlines,
    getAllDragTargets,
    getDragOverrides,
    getTarget,
    guidesList,
    onDraggingChange,
    onSelect,
    onSnapPointsChange,
    outline,
    scale,
    setPropStatuses,
    setDragOverrides,
    translateWithCommandKey
  ]);
  const performDoubleClick = React41.useCallback((event) => {
    const target = getTarget();
    if (target === undefined) {
      return;
    }
    if (!onDoubleClickTarget(target, event.button, dragAwareDoubleClick.consumePointerGestureWasDragged())) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
  }, [dragAwareDoubleClick, getTarget, onDoubleClickTarget]);
  const onClick = React41.useCallback((event) => {
    if (!dragAwareDoubleClick.acceptClickAsDoubleClick(event)) {
      return;
    }
    performDoubleClick(event);
  }, [dragAwareDoubleClick, performDoubleClick]);
  const onEffectDragOver = React41.useCallback((event) => {
    if (!hasEffectDragType(event.dataTransfer)) {
      return;
    }
    const effectDrop = getEffectDropTarget();
    if (effectDrop === null) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.dataTransfer.dropEffect = "copy";
    setEffectDropHovered(true);
  }, [getEffectDropTarget]);
  const onEffectDragLeave = React41.useCallback((event) => {
    if (event.currentTarget.contains(event.relatedTarget)) {
      return;
    }
    setEffectDropHovered(false);
  }, []);
  const onEffectDrop = React41.useCallback(async (event) => {
    if (!hasEffectDragType(event.dataTransfer)) {
      return;
    }
    const effectDrop = getEffectDropTarget();
    if (effectDrop === null) {
      return;
    }
    const dragData = getEffectDragData(event.dataTransfer);
    if (!dragData) {
      if (hasExplicitEffectDragType(event.dataTransfer)) {
        event.preventDefault();
        event.stopPropagation();
        setEffectDropHovered(false);
        showNotification("Could not read effect drag data", 3000);
      }
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    setEffectDropHovered(false);
    await addEffectFromDragData({
      dragData,
      fileName: effectDrop.fileName,
      nodePath: effectDrop.nodePath,
      clientId: effectDrop.clientId
    });
  }, [getEffectDropTarget]);
  return /* @__PURE__ */ jsx63("polygon", {
    ref: polygonRef,
    ...{
      [PREVENT_CLEAR_SELECTION_ON_POINTER_DOWN_ATTR]: "true",
      [SELECTED_OUTLINE_KEY_ATTR]: outline.key
    },
    "data-remotion-directly-selected-outline": directlySelected ? "true" : undefined,
    points,
    fill: effectDropHovered ? TIMELINE_DROP_BLUE_ALPHA_12 : TRANSPARENT,
    stroke: BLUE,
    strokeOpacity: visible || effectDropHovered ? 1 : 0,
    strokeWidth: 2,
    vectorEffect: "non-scaling-stroke",
    pointerEvents: hasTarget ? "all" : undefined,
    onPointerEnter: () => {
      if (!dragging) {
        onHoverChange(outline.key);
      }
    },
    onPointerLeave: () => {
      if (!dragging) {
        onHoverChange(null);
      }
    },
    onPointerDown,
    onPointerDownCapture: dragAwareDoubleClick.beginPointerGesture,
    onClick,
    onDragOver: onEffectDragOver,
    onDragLeave: onEffectDragLeave,
    onDrop: onEffectDrop
  });
};
var SelectedOutlinePolygon = React41.memo(SelectedOutlinePolygonUnmemoized);

// src/components/Timeline/disable-sequence-interactivity.ts
import { NoReactInternals as NoReactInternals4 } from "remotion/no-react";
var disableSequenceInteractivity = ({
  clientId,
  fileName,
  nodePath,
  setPropStatuses
}) => {
  return saveSequenceProps({
    addedKeyframes: null,
    movedKeyframes: null,
    changes: [
      {
        fileName,
        nodePath,
        fieldKey: "showInTimeline",
        value: false,
        defaultValue: "true",
        schema: NoReactInternals4.sequenceSchema
      }
    ],
    setPropStatuses,
    clientId,
    undoLabel: "Disable interactivity",
    redoLabel: "Disable interactivity again"
  });
};

// src/components/Timeline/get-sequence-context-menu-items.ts
import { NoReactInternals as NoReactInternals5 } from "remotion/no-react";
var normalizeMenuDividers = (items) => {
  const normalized = [];
  for (const item of items) {
    if (item.type === "divider") {
      const previousItem = normalized[normalized.length - 1];
      if (!previousItem || previousItem.type === "divider") {
        continue;
      }
    }
    normalized.push(item);
  }
  if (normalized[normalized.length - 1]?.type === "divider") {
    normalized.pop();
  }
  return normalized;
};
var interactiveSvgComponentIdentity = "dev.remotion.remotion.Interactive.Svg";
var findCopyableFrameElement = (element) => {
  if (element?.tagName === "CANVAS" || element?.tagName === "VIDEO") {
    return element;
  }
  return element?.querySelector("canvas, video") ?? null;
};
var copyImageToClipboard = async (element) => {
  let canvas;
  if (element.tagName === "CANVAS") {
    const sourceCanvas = element;
    canvas = document.createElement("canvas");
    canvas.width = sourceCanvas.width;
    canvas.height = sourceCanvas.height;
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("Could not create canvas context");
    }
    context.drawImage(sourceCanvas, 0, 0);
  } else if (element.tagName === "IMG") {
    const image = element;
    if (image.naturalWidth === 0 || image.naturalHeight === 0) {
      throw new Error("Image has not loaded");
    }
    canvas = document.createElement("canvas");
    canvas.width = image.naturalWidth;
    canvas.height = image.naturalHeight;
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("Could not create canvas context");
    }
    context.drawImage(image, 0, 0);
  } else if (element.tagName === "VIDEO") {
    const video = element;
    if (video.videoWidth === 0 || video.videoHeight === 0) {
      throw new Error("Video frame has not loaded");
    }
    canvas = document.createElement("canvas");
    canvas.width = video.videoWidth;
    canvas.height = video.videoHeight;
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("Could not create canvas context");
    }
    context.drawImage(video, 0, 0);
  } else {
    throw new Error("Expected an image, video, or canvas element");
  }
  const blob = new Promise((resolve, reject) => {
    canvas.toBlob((result) => {
      if (result) {
        resolve(result);
      } else {
        reject(new Error("Could not convert image to PNG"));
      }
    }, "image/png");
  });
  await navigator.clipboard.write([
    new ClipboardItem({
      "image/png": blob
    })
  ]);
};
var getMultiSequenceContextMenuItems = ({
  deleteDisabled,
  duplicateDisabled,
  onDeleteSelectedSequences,
  onDuplicateSelectedSequences
}) => {
  return [
    {
      type: "item",
      id: "duplicate-selected-sequences",
      keyHint: null,
      label: "Duplicate selected",
      leftItem: null,
      disabled: duplicateDisabled,
      onClick: onDuplicateSelectedSequences,
      quickSwitcherLabel: null,
      subMenu: null,
      value: "duplicate-selected-sequences"
    },
    {
      type: "divider",
      id: "duplicate-delete-selected-sequences-divider"
    },
    {
      type: "item",
      id: "delete-selected-sequences",
      keyHint: null,
      label: "Delete selected",
      leftItem: null,
      disabled: deleteDisabled,
      onClick: onDeleteSelectedSequences,
      quickSwitcherLabel: null,
      subMenu: null,
      value: "delete-selected-sequences"
    }
  ];
};
var getSequenceContextMenuItems = ({
  assetLinkInfo,
  canOpenInEditor,
  copyImageElement,
  deleteDisabled,
  disableInteractivityDisabled,
  duplicateDisabled,
  isProgrammaticallyDuplicated,
  includeSourceEditItems,
  codingAgentInfo,
  editorInfo,
  onConfigureApps,
  onDeleteSequenceFromSource,
  onDisableSequenceInteractivity,
  onDuplicateSequenceFromSource,
  openInCodingAgent: openInCodingAgent2,
  openInEditor: openInEditor3,
  originalLocation,
  selectAsset,
  sequence,
  sourceActions = []
}) => {
  const isInteractiveSvg = sequence.controls?.componentIdentity === interactiveSvgComponentIdentity;
  const installedEditors = editorInfo?.installedEditors ?? [];
  const defaultEditorId = getPreferredEditorId(editorInfo);
  const defaultEditor = installedEditors.find((editor) => editor.id === defaultEditorId);
  const editorName = defaultEditor?.nameWithType ?? null;
  const defaultOpenInTarget = getDefaultOpenInTarget({ canOpenInEditor });
  const gitSourceName = window.remotion_gitSource ? getGitSourceName(window.remotion_gitSource) : null;
  const defaultOpenInName = defaultOpenInTarget === "editor" ? editorName : gitSourceName;
  const copyImageLabel = copyImageElement && sequence.type === "image" ? "Copy image" : "Copy frame";
  const contextForAgents = formatContextForAgents({
    location: originalLocation,
    name: sequence.displayName || sequence.controls?.componentName || null,
    root: window.remotion_cwd
  });
  const openInCodingAgentWithContext = (codingAgentId, codingAgentName) => {
    openInCodingAgent2(codingAgentId, codingAgentName, codingAgentId === "copilot" ? null : contextForAgents);
  };
  const openInMenuItems = getOpenInMenuItems({
    canOpenDesktopApps: onConfigureApps !== null,
    codingAgentInfo,
    editorDisabled: !canOpenInEditor || !originalLocation,
    editorInfo,
    excludeCodingAgentId: null,
    excludeEditorId: defaultEditorId,
    excludeGitSource: defaultOpenInTarget === "git-source",
    fileManagerDisabled: !originalLocation?.source,
    folder: false,
    gitSourceDisabled: !originalLocation,
    onConfigureApps,
    onOpenInCodingAgent: openInCodingAgentWithContext,
    onOpenInEditor: openInEditor3,
    onOpenInFileExplorer: () => {
      if (!originalLocation?.source) {
        return;
      }
      openInFileExplorer({ directory: originalLocation.source }).catch((err) => {
        showNotification(`Could not open file: ${err.message}`, 2000);
      });
    },
    onOpenInGitClient: () => {
      return;
    },
    onOpenInGitSource: () => {
      openGitSource({ folder: false, location: originalLocation });
    },
    onOpenInTerminal: null
  });
  const items = [
    defaultOpenInTarget && defaultOpenInName ? {
      type: "item",
      id: defaultOpenInTarget === "editor" ? "open-in-editor" : "open-in-git-source",
      keyHint: null,
      label: `Open in ${defaultOpenInName}`,
      leftItem: null,
      disabled: !originalLocation,
      onClick: () => {
        if (defaultOpenInTarget === "editor") {
          openInEditor3(null);
          return;
        }
        openGitSource({ folder: false, location: originalLocation });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      value: defaultOpenInTarget === "editor" ? "open-in-editor" : "open-in-git-source"
    } : null,
    openInMenuItems.length > 0 ? {
      type: "item",
      id: "open-in-another-app",
      keyHint: null,
      label: "Open in...",
      leftItem: null,
      disabled: false,
      onClick: () => {
        return;
      },
      quickSwitcherLabel: null,
      subMenu: {
        items: openInMenuItems,
        leaveLeftSpace: true,
        preselectIndex: false
      },
      value: "open-in-another-app"
    } : null,
    getCopyContextForAgentsMenuItem({ contextForAgents }),
    assetLinkInfo ? {
      type: "item",
      id: "show-asset",
      keyHint: null,
      label: "Show asset",
      leftItem: null,
      disabled: false,
      onClick: () => {
        openTimelineAssetLink(assetLinkInfo, selectAsset);
      },
      quickSwitcherLabel: null,
      subMenu: null,
      value: "show-asset"
    } : null,
    copyImageElement ? {
      type: "divider",
      id: "copy-image-divider"
    } : null,
    copyImageElement ? {
      type: "item",
      id: copyImageLabel === "Copy image" ? "copy-image" : "copy-frame",
      keyHint: null,
      label: copyImageLabel,
      leftItem: null,
      disabled: false,
      onClick: () => {
        copyImageToClipboard(copyImageElement).then(() => {
          showNotification(`Copied ${copyImageLabel === "Copy image" ? "image" : "frame"} to clipboard`, 1000);
        }).catch((err) => {
          showNotification(`Could not copy ${copyImageLabel === "Copy image" ? "image" : "frame"}: ${err.message}`, 2000);
        });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      value: copyImageLabel === "Copy image" ? "copy-image" : "copy-frame"
    } : null,
    assetLinkInfo || copyImageElement ? {
      type: "divider",
      id: "sequence-link-divider"
    } : null,
    isInteractiveSvg ? {
      type: "item",
      id: "copy-svg",
      keyHint: null,
      label: "Copy SVG",
      leftItem: null,
      disabled: !sequence.refForOutline?.current,
      onClick: () => {
        const svg = sequence.refForOutline?.current;
        if (!svg) {
          return;
        }
        navigator.clipboard.writeText(svg.outerHTML).then(() => {
          showNotification("Copied SVG to clipboard", 1000);
        }).catch((err) => {
          showNotification(`Could not copy to clipboard: ${err.message}`, 1000);
        });
      },
      quickSwitcherLabel: null,
      subMenu: null,
      value: "copy-svg"
    } : null,
    isInteractiveSvg ? {
      type: "divider",
      id: "copy-svg-divider"
    } : null,
    sourceActions.length > 0 ? {
      type: "divider",
      id: "sequence-source-actions-divider"
    } : null,
    ...sourceActions,
    includeSourceEditItems ? {
      type: "item",
      id: "disable-interactivity",
      keyHint: null,
      label: "Disable interactivity",
      leftItem: null,
      disabled: disableInteractivityDisabled,
      onClick: onDisableSequenceInteractivity,
      quickSwitcherLabel: null,
      subMenu: null,
      value: "disable-interactivity"
    } : null,
    includeSourceEditItems ? {
      type: "item",
      id: "duplicate-sequence",
      keyHint: null,
      label: "Duplicate",
      leftItem: null,
      disabled: duplicateDisabled || isProgrammaticallyDuplicated,
      onClick: onDuplicateSequenceFromSource,
      quickSwitcherLabel: null,
      subMenu: null,
      value: "duplicate-sequence"
    } : null,
    includeSourceEditItems ? {
      type: "divider",
      id: "sequence-duplicate-delete-divider"
    } : null,
    includeSourceEditItems ? {
      type: "item",
      id: "delete-sequence",
      keyHint: null,
      label: isProgrammaticallyDuplicated ? "Delete all" : "Delete",
      leftItem: null,
      disabled: deleteDisabled,
      onClick: onDeleteSequenceFromSource,
      quickSwitcherLabel: null,
      subMenu: null,
      value: "delete-sequence"
    } : null
  ].filter(NoReactInternals5.truthy);
  return normalizeMenuDividers(items);
};

// src/components/SelectedOutlineElement.tsx
import { jsx as jsx65, jsxs as jsxs34, Fragment as Fragment18 } from "react/jsx-runtime";
var SelectedOutlineElementUnmemoized = ({
  compositionHeight,
  compositionWidth,
  dragging,
  getAllDragOutlines,
  getAllDragTargets,
  getLatestTargetByKey,
  layoutTarget,
  outline,
  onDraggingChange,
  onSnapPointsChange,
  onSelect,
  registerContextMenuOpen,
  scale
}) => {
  const { previewServerState } = useContext31(StudioServerConnectionCtx);
  const {
    canConfigureApps,
    canOpenInEditor: editorAvailable,
    defaultEditorId,
    editorInfo
  } = useEditorOpening(previewServerState.type === "connected");
  const codingAgentInfo = useDefaultCodingAgentInfo(canConfigureApps);
  const defaultOpenInTarget = getDefaultOpenInTarget({
    canOpenInEditor: editorAvailable
  });
  const { setPropStatuses } = useContext31(Internals33.VisualModeSettersContext);
  const updateResolvedStackTrace = useContext31(Internals33.SequenceStackTracesUpdateContext);
  const confirm = useConfirmationDialog();
  const deleteTimelineItems = useDeleteTimelineItems();
  const selectAsset = useSelectAsset();
  const selectComposition = useSelectComposition();
  const { compositions } = useContext31(Internals33.CompositionManager);
  const { setSelectedModal } = useContext31(SetSelectedModalContext);
  const { setManuallyEnabled } = useContext31(Transform3DModeStateContext);
  const { controlTarget, getLayoutTarget, getTarget, hovered, onHoverChange } = useSelectedOutlineControlTarget({
    getLatestTargetByKey,
    layoutTarget
  });
  const resolveOriginalLocation = React42.useCallback(async (resolveTarget) => {
    const stack2 = resolveTarget.sequence.getStack();
    if (!stack2) {
      return null;
    }
    let originalLocation = null;
    try {
      originalLocation = await getOriginalLocationFromStack(stack2, "sequence");
    } catch (err) {
      showNotification(err.message, 2000);
    }
    updateResolvedStackTrace(stack2, originalLocation);
    return originalLocation;
  }, [updateResolvedStackTrace]);
  const onDoubleClickTarget = React42.useCallback((doubleClickTarget, button, sequenceWasDragged) => {
    const connectedCompositions = getConnectedCompositions({
      compositions,
      singleChildComponent: doubleClickTarget.sequence.singleChildComponent
    });
    const action = getSequenceDoubleClickAction({
      button,
      canOpenSource: defaultOpenInTarget !== null,
      numberOfConnectedCompositions: connectedCompositions.length,
      sequenceWasDragged
    });
    if (action === null) {
      return false;
    }
    if (action === "open-connected-composition") {
      selectComposition(connectedCompositions[0], true);
      return true;
    }
    const openTargetSource = async () => {
      const originalLocation = await resolveOriginalLocation(doubleClickTarget);
      if (originalLocation === null) {
        return;
      }
      if (defaultOpenInTarget === "editor" && defaultEditorId !== null) {
        await openOriginalPositionInEditor(originalLocation, defaultEditorId);
        return;
      }
      if (defaultOpenInTarget === "git-source") {
        openGitSource({ folder: false, location: originalLocation });
      }
    };
    openTargetSource().catch((err) => {
      showNotification(err.message, 2000);
    });
    return true;
  }, [
    compositions,
    defaultEditorId,
    defaultOpenInTarget,
    resolveOriginalLocation,
    selectComposition
  ]);
  const onContextMenuOpen = React42.useCallback(async () => {
    const contextMenuTarget = getTarget();
    if (contextMenuTarget === undefined) {
      return false;
    }
    if (!contextMenuTarget.selected) {
      onSelect(contextMenuTarget.selection, {
        shiftKey: false,
        toggleKey: false
      });
    }
    const originalLocation = await resolveOriginalLocation(contextMenuTarget);
    const nodePath = contextMenuTarget.nodePathInfo.sequenceSubscriptionKey;
    const mediaSrc = contextMenuTarget.sequence.type === "audio" || contextMenuTarget.sequence.type === "video" || contextMenuTarget.sequence.type === "image" ? contextMenuTarget.sequence.src : null;
    const assetLinkInfo = mediaSrc ? getTimelineAssetLinkInfo(mediaSrc) : null;
    const canOpenInEditor = Boolean(originalLocation && editorAvailable);
    const sourceEditingEnabled = isStudioInteractivityEnabled();
    const disableInteractivityDisabled = !sourceEditingEnabled || !contextMenuTarget.sequence.showInTimeline;
    const sourceEditDisabled = !sourceEditingEnabled || !contextMenuTarget.sequence.controls || !nodePath.absolutePath;
    const isProgrammaticallyDuplicated = contextMenuTarget.nodePathInfo.numberOfSequencesWithThisNodePath > 1;
    const canAddEffect = contextMenuTarget.nodePathInfo.supportsEffects && !sourceEditDisabled && previewServerState.type === "connected";
    const canCrop = contextMenuTarget.canCrop && !sourceEditDisabled;
    const canRotate = !sourceEditDisabled;
    const outlineElement = contextMenuTarget.sequence.refForOutline?.current ?? null;
    return getSequenceContextMenuItems({
      assetLinkInfo,
      canOpenInEditor,
      codingAgentInfo,
      copyImageElement: contextMenuTarget.sequence.type === "image" ? outlineElement : findCopyableFrameElement(outlineElement),
      deleteDisabled: sourceEditDisabled,
      disableInteractivityDisabled,
      duplicateDisabled: sourceEditDisabled || isProgrammaticallyDuplicated,
      editorInfo,
      includeSourceEditItems: sourceEditingEnabled,
      isProgrammaticallyDuplicated,
      onConfigureApps: canConfigureApps ? () => {
        setSelectedModal({
          type: "settings",
          initialTab: "apps",
          initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
        });
      } : null,
      onDeleteSequenceFromSource: () => {
        if (sourceEditDisabled || previewServerState.type !== "connected") {
          return;
        }
        deleteTimelineItems([
          {
            type: "sequence",
            nodePathInfo: contextMenuTarget.nodePathInfo
          }
        ]);
      },
      onDisableSequenceInteractivity: () => {
        if (disableInteractivityDisabled || previewServerState.type !== "connected") {
          return;
        }
        disableSequenceInteractivity({
          fileName: nodePath.absolutePath,
          nodePath,
          setPropStatuses,
          clientId: previewServerState.clientId
        });
      },
      onDuplicateSequenceFromSource: () => {
        if (sourceEditDisabled) {
          return;
        }
        duplicateSequencesFromSource([contextMenuTarget.nodePathInfo], confirm).catch(() => {
          return;
        });
      },
      openInCodingAgent: (codingAgentId, codingAgentName, contextForAgents) => {
        openInCodingAgent(codingAgentId, contextForAgents).then((response) => {
          if (!response.success) {
            showNotification(`Could not open ${codingAgentName}`, 2000);
          }
        }).catch((err) => {
          showNotification(err.message, 2000);
        });
      },
      openInEditor: (editorId) => {
        const resolvedEditorId = editorId ?? defaultEditorId;
        if (!originalLocation || !resolvedEditorId || !editorAvailable) {
          return;
        }
        openOriginalPositionInEditor(originalLocation, resolvedEditorId).catch((err) => {
          showNotification(err.message, 2000);
        });
      },
      originalLocation,
      selectAsset,
      sequence: contextMenuTarget.sequence,
      sourceActions: sourceEditingEnabled ? [
        ...contextMenuTarget.nodePathInfo.supportsEffects ? [
          {
            type: "item",
            id: "add-effect",
            keyHint: null,
            label: "Add effect...",
            leftItem: null,
            disabled: !canAddEffect,
            onClick: () => {
              if (!canAddEffect || previewServerState.type !== "connected") {
                return;
              }
              setSelectedModal({
                type: "add-effect",
                clientId: previewServerState.clientId,
                fileName: nodePath.absolutePath,
                nodePath
              });
            },
            quickSwitcherLabel: null,
            subMenu: null,
            value: "add-effect"
          }
        ] : [],
        {
          type: "item",
          id: "crop",
          keyHint: null,
          label: isProgrammaticallyDuplicated ? "Crop all" : "Crop",
          leftItem: null,
          disabled: !canCrop,
          onClick: () => {
            if (!canCrop) {
              return;
            }
            onSelect({
              type: "sequence-prop",
              nodePathInfo: {
                ...contextMenuTarget.nodePathInfo,
                auxiliaryKeys: ["controls", cropFieldKeys.left]
              },
              key: cropFieldKeys.left
            }, { shiftKey: false, toggleKey: false });
          },
          quickSwitcherLabel: null,
          subMenu: null,
          value: "crop"
        },
        {
          type: "item",
          id: "rotate",
          keyHint: null,
          label: isProgrammaticallyDuplicated ? "Rotate all" : "Rotate",
          leftItem: null,
          disabled: !canRotate,
          onClick: () => {
            if (!canRotate) {
              return;
            }
            setManuallyEnabled(getTimelineSequenceSelectionKey(contextMenuTarget.nodePathInfo), true);
            onSelect({
              type: "sequence-prop",
              nodePathInfo: {
                ...contextMenuTarget.nodePathInfo,
                auxiliaryKeys: ["controls", rotateFieldKey]
              },
              key: rotateFieldKey
            }, { shiftKey: false, toggleKey: false });
          },
          quickSwitcherLabel: null,
          subMenu: null,
          value: "rotate"
        },
        {
          type: "divider",
          id: "rotate-divider"
        }
      ] : []
    });
  }, [
    canConfigureApps,
    codingAgentInfo,
    confirm,
    deleteTimelineItems,
    editorInfo,
    getTarget,
    onSelect,
    defaultEditorId,
    editorAvailable,
    previewServerState,
    resolveOriginalLocation,
    setManuallyEnabled,
    selectAsset,
    setSelectedModal,
    setPropStatuses
  ]);
  useLayoutEffect8(() => {
    registerContextMenuOpen(outline.key, onContextMenuOpen);
    return () => {
      registerContextMenuOpen(outline.key, null);
    };
  }, [onContextMenuOpen, outline.key, registerContextMenuOpen]);
  return /* @__PURE__ */ jsxs34(Fragment18, {
    children: [
      /* @__PURE__ */ jsx65(SelectedOutlinePolygon, {
        compositionHeight,
        compositionWidth,
        containsSelection: layoutTarget?.containsSelection === true,
        directlySelected: layoutTarget?.selected === true,
        dragging,
        getAllDragOutlines,
        getAllDragTargets,
        getLayoutTarget,
        getTarget,
        hasTarget: layoutTarget !== undefined,
        hovered,
        outline,
        onDraggingChange,
        onHoverChange,
        onSnapPointsChange,
        onSelect,
        onDoubleClickTarget,
        scale,
        showSelectedOutline: layoutTarget?.showSelectedOutline ?? false,
        translateWithCommandKey: layoutTarget?.selectedForRotation === true && Boolean(controlTarget?.rotationDrag)
      }),
      layoutTarget?.selectedForRotation && controlTarget?.rotationDrag ? /* @__PURE__ */ jsx65(SelectedOutlineCanvasRotation, {
        getLatestTargetByKey,
        layoutTarget,
        onDraggingChange,
        outline,
        transform3DMode: controlTarget.rotationDrag.transform3DMode
      }) : null
    ]
  });
};
var SelectedOutlineElement = React42.memo(SelectedOutlineElementUnmemoized);

// src/components/SelectedOutlineSnapIndicators.tsx
import { useLayoutEffect as useLayoutEffect9, useState as useState19 } from "react";
import { jsx as jsx66 } from "react/jsx-runtime";
var SelectedOutlineSnapIndicators = ({ compositionHeight, compositionWidth, scale, updateSnapPointsRef }) => {
  const [activeSnapPoints, setActiveSnapPoints] = useState19([]);
  useLayoutEffect9(() => {
    updateSnapPointsRef.current = setActiveSnapPoints;
    return () => {
      if (updateSnapPointsRef.current === setActiveSnapPoints) {
        updateSnapPointsRef.current = () => {
          return;
        };
      }
    };
  }, [updateSnapPointsRef]);
  if (activeSnapPoints.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx66("g", {
    pointerEvents: "none",
    children: activeSnapPoints.map((snapPoint) => {
      if (snapPoint.target.axis === "x") {
        const x = snapPoint.target.position * scale;
        return /* @__PURE__ */ jsx66("line", {
          x1: x,
          x2: x,
          y1: 0,
          y2: compositionHeight * scale,
          stroke: selectedOutlineSnapIndicatorColor,
          strokeWidth: 1,
          vectorEffect: "non-scaling-stroke"
        }, `${snapPoint.target.axis}-${snapPoint.target.type}-${snapPoint.target.position}-${snapPoint.edge}`);
      }
      const y = snapPoint.target.position * scale;
      return /* @__PURE__ */ jsx66("line", {
        x1: 0,
        x2: compositionWidth * scale,
        y1: y,
        y2: y,
        stroke: selectedOutlineSnapIndicatorColor,
        strokeWidth: 1,
        vectorEffect: "non-scaling-stroke"
      }, `${snapPoint.target.axis}-${snapPoint.target.type}-${snapPoint.target.position}-${snapPoint.edge}`);
    })
  });
};

// src/components/SelectedOutlineTransformOriginHandle.tsx
import React44, { useContext as useContext33, useMemo as useMemo36 } from "react";
import { Internals as Internals35 } from "remotion";
import { NoReactInternals as NoReactInternals6 } from "remotion/no-react";
import { jsx as jsx67, jsxs as jsxs35 } from "react/jsx-runtime";
var SelectedOutlineTransformOriginHandle = ({ getLatestTargetByKey, layoutTarget, outline, onDraggingChange }) => {
  const { setDragOverrides, clearDragOverrides, setPropStatuses } = useContext33(Internals35.VisualModeSettersContext);
  const { editorSnapping } = useContext33(EditorSnappingContext);
  const target = layoutTarget?.selectedForTransformOrigin || layoutTarget?.selectedForRotation ? getLatestTargetByKey(layoutTarget.key) : undefined;
  const transformOriginDrag = target?.transformOriginDrag ?? null;
  const transformOriginValue = transformOriginDrag?.originValue ?? (layoutTarget?.selectedForRotation ? layoutTarget.transformOriginValue : null);
  const crop = target?.crop;
  const transformOriginPoints = outline.uncroppedPoints ?? outline.points;
  const parsed = useMemo36(() => transformOriginValue === null ? null : parseTransformOrigin(transformOriginValue), [transformOriginValue]);
  const uv = useMemo36(() => {
    if (parsed === null || outline.dimensions === null) {
      return null;
    }
    return parsedTransformOriginToUv({
      parsed,
      width: outline.dimensions.width,
      height: outline.dimensions.height
    });
  }, [outline.dimensions, parsed]);
  const position = useMemo36(() => {
    if (layoutTarget?.selectedForRotation) {
      return getSelectedOutlineRotationPivot({
        dimensions: outline.dimensions,
        points: transformOriginPoints,
        transformOriginValue: layoutTarget.transformOriginValue
      });
    }
    return uv === null ? null : getUvHandlePosition(transformOriginPoints, uv);
  }, [
    layoutTarget?.selectedForRotation,
    layoutTarget?.transformOriginValue,
    outline.dimensions,
    transformOriginPoints,
    uv
  ]);
  const onPointerDown = React44.useCallback((event) => {
    if (event.button !== 0 || transformOriginDrag === null || parsed === null || uv === null || outline.dimensions === null) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const rotation = parseCssRotation(transformOriginDrag.rotateValue);
    if (rotation === null) {
      return;
    }
    const { dimensions } = outline;
    if (dimensions === null) {
      return;
    }
    const scale = NoReactInternals6.parseScaleValue(transformOriginDrag.scaleValue);
    const startTranslate = parseTranslate(transformOriginDrag.translateValue);
    const svgRect = svg.getBoundingClientRect();
    let last = null;
    let currentPointerX = event.clientX;
    let currentPointerY = event.clientY;
    let axisLocked = event.shiftKey;
    onDraggingChange(true);
    forceSpecificCursor("crosshair");
    const updateFromPointerPosition = () => {
      const point = {
        x: currentPointerX - svgRect.left,
        y: currentPointerY - svgRect.top
      };
      const rawUv = getUvCoordinateForPoint(transformOriginPoints, point);
      const lockedAxis = getSelectedOutlineTransformOriginLockedAxis({
        axisLocked,
        dimensions,
        startUv: uv,
        uv: rawUv
      });
      const axisLockedUv = applySelectedOutlineTransformOriginAxisLock({
        lockedAxis,
        startUv: uv,
        uv: rawUv
      });
      const snapPoint = lockedAxis === null ? point : getUvHandlePosition(transformOriginPoints, axisLockedUv);
      const snappedUv = editorSnapping ? snapSelectedOutlineTransformOriginUv({
        crop: crop ?? null,
        point: snapPoint,
        points: transformOriginPoints,
        thresholdPx: null,
        uv: axisLockedUv
      }) : axisLockedUv;
      const nextUv = applySelectedOutlineTransformOriginAxisLock({
        lockedAxis,
        startUv: uv,
        uv: snappedUv
      });
      const deltaOrigin = [
        (nextUv[0] - uv[0]) * dimensions.width,
        (nextUv[1] - uv[1]) * dimensions.height
      ];
      const [nextTranslateX, nextTranslateY] = compensateTranslateForTransformOrigin({
        startTranslate: [startTranslate[0], startTranslate[1]],
        deltaOrigin,
        rotation,
        scale
      });
      const origin = serializeTransformOrigin({
        uv: nextUv,
        z: parsed.z
      });
      const translate = serializeTranslate([
        nextTranslateX,
        nextTranslateY,
        startTranslate[2]
      ]);
      last = { uv: nextUv, origin, translate };
      setDragOverrides(transformOriginDrag.nodePath, transformOriginFieldKey, transformOriginDrag.originPropStatus.status === "keyframed" ? Internals35.makeKeyframedDragOverride({
        status: transformOriginDrag.originPropStatus,
        frame: transformOriginDrag.sourceFrame,
        value: origin
      }) : Internals35.makeStaticDragOverride(origin));
      setDragOverrides(transformOriginDrag.nodePath, translateFieldKey, transformOriginDrag.translatePropStatus.status === "keyframed" ? transformOriginDrag.originPropStatus.status === "keyframed" ? Internals35.makeKeyframedDragOverride({
        status: transformOriginDrag.translatePropStatus,
        frame: transformOriginDrag.sourceFrame,
        value: translate
      }) : {
        type: "keyframed",
        status: {
          ...transformOriginDrag.translatePropStatus,
          keyframes: transformOriginDrag.translatePropStatus.keyframes.map((keyframe) => {
            const keyframeTranslate = parseTranslate(String(keyframe.value));
            return {
              ...keyframe,
              value: serializeTranslate([
                keyframeTranslate[0] + nextTranslateX - startTranslate[0],
                keyframeTranslate[1] + nextTranslateY - startTranslate[1],
                keyframeTranslate[2]
              ])
            };
          })
        }
      } : Internals35.makeStaticDragOverride(translate));
    };
    updateFromPointerPosition();
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      currentPointerX = moveEvent.clientX;
      currentPointerY = moveEvent.clientY;
      axisLocked = moveEvent.shiftKey;
      updateFromPointerPosition();
    };
    const onKeyChange = (keyEvent) => {
      if (keyEvent.key !== "Shift") {
        return;
      }
      const nextAxisLocked = keyEvent.type === "keydown";
      if (nextAxisLocked === axisLocked) {
        return;
      }
      axisLocked = nextAxisLocked;
      updateFromPointerPosition();
    };
    const onPointerUp = () => {
      window.removeEventListener("keydown", onKeyChange);
      window.removeEventListener("keyup", onKeyChange);
      stopForcingSpecificCursor();
      onDraggingChange(false);
      if (last === null || uvsEqual(last.uv, uv)) {
        clearDragOverrides(transformOriginDrag.nodePath);
        return;
      }
      const { staticChanges, keyframedChanges } = getSelectedOutlineTransformOriginDragChanges({
        target: transformOriginDrag,
        startTranslate,
        origin: last.origin,
        translate: last.translate
      });
      if (staticChanges.length === 0 && keyframedChanges.length === 0) {
        clearDragOverrides(transformOriginDrag.nodePath);
        return;
      }
      const promise = staticChanges.length === 0 ? callAddKeyframes({
        sequenceKeyframes: keyframedChanges,
        effectKeyframes: [],
        setPropStatuses,
        clientId: transformOriginDrag.clientId
      }) : saveSequenceProps({
        changes: staticChanges,
        addedKeyframes: keyframedChanges,
        movedKeyframes: null,
        setPropStatuses,
        clientId: transformOriginDrag.clientId,
        undoLabel: "Move transform origin",
        redoLabel: "Move transform origin back"
      });
      promise.catch((err) => {
        showNotification(`Could not save transform origin: ${err instanceof Error ? err.message : String(err)}`, 4000);
      }).finally(() => {
        clearDragOverrides(transformOriginDrag.nodePath);
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
    window.addEventListener("keydown", onKeyChange);
    window.addEventListener("keyup", onKeyChange);
  }, [
    clearDragOverrides,
    crop,
    editorSnapping,
    onDraggingChange,
    outline,
    parsed,
    setDragOverrides,
    setPropStatuses,
    transformOriginDrag,
    transformOriginPoints,
    uv
  ]);
  if (position === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs35("g", {
    "data-remotion-studio-transform-origin-handle": true,
    pointerEvents: transformOriginDrag === null ? "none" : "all",
    cursor: transformOriginDrag === null ? undefined : "crosshair",
    onPointerDown,
    "aria-hidden": "true",
    style: {
      filter: SELECTED_OUTLINE_DROP_SHADOW
    },
    children: [
      /* @__PURE__ */ jsx67("circle", {
        cx: position.x,
        cy: position.y,
        r: 4,
        stroke: BLUE,
        fill: "none",
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke"
      }),
      /* @__PURE__ */ jsx67("line", {
        x1: position.x - 8,
        y1: position.y,
        x2: position.x + 8,
        y2: position.y,
        stroke: BLUE,
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke"
      }),
      /* @__PURE__ */ jsx67("line", {
        x1: position.x,
        y1: position.y - 8,
        x2: position.x,
        y2: position.y + 8,
        stroke: BLUE,
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke"
      })
    ]
  });
};

// src/components/SelectedOutlineUvControls.tsx
import React45, { useContext as useContext35, useMemo as useMemo37 } from "react";
import { Internals as Internals36 } from "remotion";
import { jsx as jsx68, jsxs as jsxs36, Fragment as Fragment19 } from "react/jsx-runtime";
var getSvgPointFromPointerEvent = ({
  event,
  rect
}) => {
  return {
    x: event.clientX - rect.left,
    y: event.clientY - rect.top
  };
};
var uvHandleRadius = 4.25;
var selectedUvHandleRadius = 6.8;
var ellipseControlRadius = 5.2;
var uvHandleStyle = {
  filter: SELECTED_OUTLINE_UV_DROP_SHADOW
};
var getSelectedOutlineUvHandleTimelineSelection = ({
  effectIndex,
  fieldKey,
  nodePathInfo
}) => {
  return {
    type: "sequence-effect-prop",
    nodePathInfo: {
      ...nodePathInfo,
      auxiliaryKeys: ["effects", String(effectIndex), fieldKey]
    },
    i: effectIndex,
    key: fieldKey
  };
};
var SelectedUvHandleConnectionLines = ({ handles, outline }) => {
  const lines = useMemo37(() => getUvHandleConnectionLines({ handles, points: outline.points }), [handles, outline.points]);
  const ellipses = useMemo37(() => getUvHandleConnectionEllipses({
    handles,
    dimensions: outline.dimensions,
    points: outline.points
  }), [handles, outline.dimensions, outline.points]);
  return /* @__PURE__ */ jsxs36(Fragment19, {
    children: [
      ellipses.map((ellipse) => /* @__PURE__ */ jsx68("polyline", {
        points: ellipse.points.map((point) => `${point.x},${point.y}`).join(" "),
        fill: "none",
        stroke: BLUE,
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "none"
      }, ellipse.key)),
      lines.map((line) => /* @__PURE__ */ jsx68("line", {
        x1: line.from.x,
        y1: line.from.y,
        x2: line.to.x,
        y2: line.to.y,
        stroke: BLUE,
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "none"
      }, line.key))
    ]
  });
};
var fieldDefaultToString = (value) => value !== undefined ? JSON.stringify(value) : null;
var shouldSaveNumericEffectValue = ({
  defaultValue,
  field,
  value
}) => {
  if (value === null) {
    return false;
  }
  if (field.propStatus.status === "keyframed") {
    return field.value !== value;
  }
  const stringifiedValue = JSON.stringify(value);
  return field.propStatus.codeValue !== value && !(defaultValue === stringifiedValue && field.propStatus.codeValue === undefined);
};
var saveNumericEffectField = ({
  clearEffectDragOverrides,
  clientId,
  effectIndex,
  field,
  lastValue,
  nodePath,
  schema,
  setPropStatuses,
  sourceFrame
}) => {
  const defaultValue = fieldDefaultToString(field.fieldDefault);
  if (!shouldSaveNumericEffectValue({
    defaultValue,
    field,
    value: lastValue
  })) {
    clearEffectDragOverrides(nodePath, effectIndex);
    return;
  }
  (field.propStatus.status === "keyframed" ? callAddEffectKeyframe({
    fileName: nodePath.absolutePath,
    nodePath,
    effectIndex,
    fieldKey: field.fieldKey,
    sourceFrame,
    value: lastValue,
    schema,
    setPropStatuses,
    clientId
  }) : saveEffectProp({
    type: "value",
    fileName: nodePath.absolutePath,
    nodePath,
    effectIndex,
    fieldKey: field.fieldKey,
    value: lastValue,
    defaultValue,
    schema,
    setPropStatuses,
    clientId
  })).finally(() => {
    clearEffectDragOverrides(nodePath, effectIndex);
  });
};
var getLocalEllipseAxisValue = ({
  axis,
  center,
  dimensions,
  point,
  rotation
}) => {
  const radians = rotation / 180 * Math.PI;
  const deltaX = point[0] - center[0];
  const deltaY = point[1] - center[1];
  const axisX = axis === "width" ? Math.cos(radians) : -Math.sin(radians);
  const axisY = axis === "width" ? Math.sin(radians) : Math.cos(radians);
  if (dimensions !== null && dimensions.width > 0 && dimensions.height > 0) {
    const pixelDeltaX = deltaX * dimensions.width;
    const pixelDeltaY = deltaY * dimensions.height;
    const pixelAxisValue = pixelDeltaX * axisX + pixelDeltaY * axisY;
    return pixelAxisValue / (axis === "width" ? dimensions.width : dimensions.height);
  }
  return deltaX * axisX + deltaY * axisY;
};
var getLocalEllipseWidthProgress = ({
  center,
  dimensions,
  point,
  rotation,
  width
}) => {
  if (width <= 0.000001) {
    return 0;
  }
  return getLocalEllipseAxisValue({
    axis: "width",
    center,
    dimensions,
    point,
    rotation
  }) / (width / 2);
};
var SelectedUvEllipseStartHandle = ({ control, onDraggingChange, outline }) => {
  const { setEffectDragOverrides, clearEffectDragOverrides, setPropStatuses } = useContext35(Internals36.VisualModeSettersContext);
  const { startControl } = control;
  const field = startControl?.field;
  const onPointerDown = React45.useCallback((event) => {
    if (startControl === null || field === undefined) {
      return;
    }
    if (event.button !== 0) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const svgRect = svg.getBoundingClientRect();
    const startPointerX = event.clientX;
    const startPointerY = event.clientY;
    let dragging = false;
    let lastValue = null;
    const updateFromPointerEvent = (pointerEvent) => {
      const point = getSvgPointFromPointerEvent({
        event: pointerEvent,
        rect: svgRect
      });
      const uv = getUvCoordinateForPoint(outline.points, point);
      const nextValue = roundNumericUvEllipseValue(getLocalEllipseWidthProgress({
        center: control.handle.value,
        dimensions: outline.dimensions,
        point: uv,
        rotation: control.rotation,
        width: control.width
      }), field.fieldSchema);
      lastValue = nextValue;
      setEffectDragOverrides(control.handle.nodePath, control.handle.effectIndex, field.fieldKey, field.propStatus.status === "keyframed" ? Internals36.makeKeyframedDragOverride({
        status: field.propStatus,
        frame: control.handle.sourceFrame,
        value: nextValue
      }) : Internals36.makeStaticDragOverride(nextValue));
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const deltaX = moveEvent.clientX - startPointerX;
      const deltaY = moveEvent.clientY - startPointerY;
      if (!dragging) {
        if (!isSelectedOutlineDragPastThreshold({ deltaX, deltaY })) {
          return;
        }
        dragging = true;
        forceSpecificCursor(startControl.cursor);
        onDraggingChange(true);
      }
      updateFromPointerEvent(moveEvent);
    };
    const onPointerUp = () => {
      if (dragging) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      saveNumericEffectField({
        clearEffectDragOverrides,
        clientId: control.handle.clientId,
        effectIndex: control.handle.effectIndex,
        field,
        lastValue,
        nodePath: control.handle.nodePath,
        schema: control.handle.schema,
        setPropStatuses,
        sourceFrame: control.handle.sourceFrame
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    clearEffectDragOverrides,
    control.handle,
    control.rotation,
    control.width,
    field,
    onDraggingChange,
    outline.dimensions,
    outline.points,
    setEffectDragOverrides,
    setPropStatuses,
    startControl
  ]);
  if (startControl === null || field === undefined) {
    return null;
  }
  return /* @__PURE__ */ jsx68("circle", {
    cx: startControl.position.x,
    cy: startControl.position.y,
    r: ellipseControlRadius,
    fill: WHITE,
    stroke: BLUE,
    strokeWidth: 2,
    style: uvHandleStyle,
    vectorEffect: "non-scaling-stroke",
    pointerEvents: "all",
    cursor: startControl.cursor,
    onPointerDown
  });
};
var SelectedUvEllipseResizeHandle = ({ axis, control, onDraggingChange, outline }) => {
  const { setEffectDragOverrides, clearEffectDragOverrides, setPropStatuses } = useContext35(Internals36.VisualModeSettersContext);
  const resizeControl = control.resize.find((item) => item.axis === axis);
  const field = resizeControl?.field;
  const onPointerDown = React45.useCallback((event) => {
    if (resizeControl === undefined || field === undefined) {
      return;
    }
    if (event.button !== 0) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const svgRect = svg.getBoundingClientRect();
    const startPointerX = event.clientX;
    const startPointerY = event.clientY;
    let dragging = false;
    let lastValue = null;
    const updateFromPointerEvent = (pointerEvent) => {
      const point = getSvgPointFromPointerEvent({
        event: pointerEvent,
        rect: svgRect
      });
      const uv = getUvCoordinateForPoint(outline.points, point);
      const axisValue = getLocalEllipseAxisValue({
        axis,
        center: control.handle.value,
        dimensions: outline.dimensions,
        point: uv,
        rotation: control.rotation
      });
      const nextValue = roundNumericUvEllipseValue(Math.max(0, axisValue * 2), field.fieldSchema);
      lastValue = nextValue;
      setEffectDragOverrides(control.handle.nodePath, control.handle.effectIndex, field.fieldKey, field.propStatus.status === "keyframed" ? Internals36.makeKeyframedDragOverride({
        status: field.propStatus,
        frame: control.handle.sourceFrame,
        value: nextValue
      }) : Internals36.makeStaticDragOverride(nextValue));
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const deltaX = moveEvent.clientX - startPointerX;
      const deltaY = moveEvent.clientY - startPointerY;
      if (!dragging) {
        if (!isSelectedOutlineDragPastThreshold({ deltaX, deltaY })) {
          return;
        }
        dragging = true;
        forceSpecificCursor(resizeControl.cursor);
        onDraggingChange(true);
      }
      updateFromPointerEvent(moveEvent);
    };
    const onPointerUp = () => {
      if (dragging) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      saveNumericEffectField({
        clearEffectDragOverrides,
        clientId: control.handle.clientId,
        effectIndex: control.handle.effectIndex,
        field,
        lastValue,
        nodePath: control.handle.nodePath,
        schema: control.handle.schema,
        setPropStatuses,
        sourceFrame: control.handle.sourceFrame
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    axis,
    clearEffectDragOverrides,
    control.handle,
    control.rotation,
    field,
    onDraggingChange,
    outline.dimensions,
    outline.points,
    resizeControl,
    setEffectDragOverrides,
    setPropStatuses
  ]);
  if (resizeControl === undefined || field === undefined) {
    return null;
  }
  return /* @__PURE__ */ jsx68("circle", {
    cx: resizeControl.position.x,
    cy: resizeControl.position.y,
    r: ellipseControlRadius,
    fill: WHITE,
    stroke: BLUE,
    strokeWidth: 2,
    style: uvHandleStyle,
    vectorEffect: "non-scaling-stroke",
    pointerEvents: "all",
    cursor: resizeControl.cursor,
    onPointerDown
  });
};
var snapRotationDegrees = (value) => Math.round(value / selectedOutlineRotationSnapStepDegrees) * selectedOutlineRotationSnapStepDegrees;
var trimLineToCircleEdges = ({
  from,
  fromRadius,
  to,
  toRadius
}) => {
  const deltaX = to.x - from.x;
  const deltaY = to.y - from.y;
  const distance2 = Math.hypot(deltaX, deltaY);
  if (distance2 <= fromRadius + toRadius || distance2 <= 0.000001) {
    const middle = {
      x: (from.x + to.x) / 2,
      y: (from.y + to.y) / 2
    };
    return { from: middle, to: middle };
  }
  const unitX = deltaX / distance2;
  const unitY = deltaY / distance2;
  return {
    from: {
      x: from.x + unitX * fromRadius,
      y: from.y + unitY * fromRadius
    },
    to: {
      x: to.x - unitX * toRadius,
      y: to.y - unitY * toRadius
    }
  };
};
var SelectedUvEllipseRotationHandle = ({ control, onDraggingChange }) => {
  const { setEffectDragOverrides, clearEffectDragOverrides, setPropStatuses } = useContext35(Internals36.VisualModeSettersContext);
  const { editorSnapping } = useContext35(EditorSnappingContext);
  const { rotationControl } = control;
  const field = rotationControl?.field;
  const onPointerDown = React45.useCallback((event) => {
    if (rotationControl === null || field === undefined) {
      return;
    }
    if (event.button !== 0) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const svgRect = svg.getBoundingClientRect();
    const { center } = control;
    const startPointer = { x: event.clientX, y: event.clientY };
    let previousAngle = getAngleDegrees(center, {
      x: event.clientX - svgRect.left,
      y: event.clientY - svgRect.top
    });
    const startCursorDegrees = previousAngle;
    let accumulatedDelta = 0;
    let rotationLocked = event.shiftKey;
    let dragging = false;
    let lastValue = null;
    const updateRotationDragOverride = () => {
      const rawValue = control.rotation + accumulatedDelta;
      const snappedValue = rotationLocked && editorSnapping ? snapRotationDegrees(rawValue) : rawValue;
      const nextValue = roundNumericUvEllipseValue(snappedValue, field.fieldSchema);
      const cursorDelta = getSelectedOutlineRotationDeltaDegrees({
        from: control.rotation,
        to: nextValue
      });
      lastValue = nextValue;
      forceSpecificCursor(getRotationCursor(startCursorDegrees + cursorDelta));
      setEffectDragOverrides(control.handle.nodePath, control.handle.effectIndex, field.fieldKey, field.propStatus.status === "keyframed" ? Internals36.makeKeyframedDragOverride({
        status: field.propStatus,
        frame: control.handle.sourceFrame,
        value: nextValue
      }) : Internals36.makeStaticDragOverride(nextValue));
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      const deltaX = moveEvent.clientX - startPointer.x;
      const deltaY = moveEvent.clientY - startPointer.y;
      if (!dragging) {
        if (!isSelectedOutlineDragPastThreshold({ deltaX, deltaY })) {
          return;
        }
        dragging = true;
        onDraggingChange(true);
      }
      const nextAngle = getAngleDegrees(center, {
        x: moveEvent.clientX - svgRect.left,
        y: moveEvent.clientY - svgRect.top
      });
      accumulatedDelta += getSelectedOutlineRotationDeltaDegrees({
        from: previousAngle,
        to: nextAngle
      });
      previousAngle = nextAngle;
      rotationLocked = moveEvent.shiftKey;
      updateRotationDragOverride();
    };
    const onKeyChange = (keyEvent) => {
      if (keyEvent.key !== "Shift") {
        return;
      }
      const nextRotationLocked = keyEvent.type === "keydown";
      if (nextRotationLocked === rotationLocked) {
        return;
      }
      rotationLocked = nextRotationLocked;
      if (dragging) {
        updateRotationDragOverride();
      }
    };
    const onPointerUp = () => {
      window.removeEventListener("keydown", onKeyChange);
      window.removeEventListener("keyup", onKeyChange);
      if (dragging) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      saveNumericEffectField({
        clearEffectDragOverrides,
        clientId: control.handle.clientId,
        effectIndex: control.handle.effectIndex,
        field,
        lastValue,
        nodePath: control.handle.nodePath,
        schema: control.handle.schema,
        setPropStatuses,
        sourceFrame: control.handle.sourceFrame
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
    window.addEventListener("keydown", onKeyChange);
    window.addEventListener("keyup", onKeyChange);
  }, [
    clearEffectDragOverrides,
    control,
    editorSnapping,
    field,
    onDraggingChange,
    rotationControl,
    setEffectDragOverrides,
    setPropStatuses
  ]);
  if (rotationControl === null || field === undefined) {
    return null;
  }
  const heightControl = control.resize.find((item) => item.axis === "height");
  if (heightControl === undefined) {
    return null;
  }
  const connector = trimLineToCircleEdges({
    from: heightControl.position,
    fromRadius: ellipseControlRadius,
    to: rotationControl.position,
    toRadius: ellipseControlRadius
  });
  return /* @__PURE__ */ jsxs36(Fragment19, {
    children: [
      /* @__PURE__ */ jsx68("line", {
        x1: connector.from.x,
        y1: connector.from.y,
        x2: connector.to.x,
        y2: connector.to.y,
        stroke: BLUE,
        strokeWidth: 2,
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "none"
      }),
      /* @__PURE__ */ jsx68("circle", {
        cx: rotationControl.position.x,
        cy: rotationControl.position.y,
        r: ellipseControlRadius,
        fill: WHITE,
        stroke: BLUE,
        strokeWidth: 2,
        style: uvHandleStyle,
        vectorEffect: "non-scaling-stroke",
        pointerEvents: "all",
        cursor: getRotationCursor(getAngleDegrees(control.center, rotationControl.position)),
        onPointerDown
      })
    ]
  });
};
var SelectedUvHandleCircle = ({ handle, nodePathInfo, onDraggingChange, onSelect, outline }) => {
  const { setEffectDragOverrides, clearEffectDragOverrides, setPropStatuses } = useContext35(Internals36.VisualModeSettersContext);
  const { editorSnapping } = useContext35(EditorSnappingContext);
  const position = useMemo37(() => getUvHandlePosition(outline.points, handle.value), [handle.value, outline.points]);
  const onPointerDown = React45.useCallback((event) => {
    if (event.button !== 0) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const interaction = getOutlineSelectionInteraction(event);
    onSelect(getSelectedOutlineUvHandleTimelineSelection({
      effectIndex: handle.effectIndex,
      fieldKey: handle.fieldKey,
      nodePathInfo
    }), interaction);
    if (interaction.shiftKey || interaction.toggleKey) {
      return;
    }
    const svg = event.currentTarget.ownerSVGElement;
    if (svg === null) {
      return;
    }
    const svgRect = svg.getBoundingClientRect();
    const startPointerX = event.clientX;
    const startPointerY = event.clientY;
    let lastValue = null;
    let dragging = false;
    const defaultValue = handle.fieldDefault !== undefined ? JSON.stringify(handle.fieldDefault) : null;
    const updateFromPointerEvent = (pointerEvent) => {
      const point = getSvgPointFromPointerEvent({
        event: pointerEvent,
        rect: svgRect
      });
      const rawUv = getUvCoordinateForPoint(outline.points, point);
      const snappedUv = editorSnapping ? snapSelectedOutlineUv({
        point,
        points: outline.points,
        uv: rawUv
      }) : rawUv;
      const nextValue = roundUvCoordinate(constrainUv(snappedUv, handle.fieldSchema), handle.fieldSchema);
      lastValue = nextValue;
      setEffectDragOverrides(handle.nodePath, handle.effectIndex, handle.fieldKey, handle.propStatus.status === "keyframed" ? Internals36.makeKeyframedDragOverride({
        status: handle.propStatus,
        frame: handle.sourceFrame,
        value: nextValue
      }) : Internals36.makeStaticDragOverride(nextValue));
    };
    const updateDragFromPointerEvent = (pointerEvent) => {
      if (!dragging) {
        const deltaX = pointerEvent.clientX - startPointerX;
        const deltaY = pointerEvent.clientY - startPointerY;
        if (!isSelectedOutlineDragPastThreshold({ deltaX, deltaY })) {
          return;
        }
        dragging = true;
        forceSpecificCursor("default");
        onDraggingChange(true);
      }
      updateFromPointerEvent(pointerEvent);
    };
    const onPointerMove = (moveEvent) => {
      moveEvent.preventDefault();
      updateDragFromPointerEvent(moveEvent);
    };
    const onPointerUp = () => {
      if (dragging) {
        stopForcingSpecificCursor();
        onDraggingChange(false);
      }
      const stringifiedValue = lastValue === null ? null : JSON.stringify(lastValue);
      const shouldSave = (() => {
        if (lastValue === null) {
          return false;
        }
        if (handle.propStatus.status === "keyframed") {
          return !tuplesEqual(handle.value, lastValue);
        }
        return !tuplesEqual(handle.propStatus.codeValue, lastValue) && !(defaultValue === stringifiedValue && handle.propStatus.codeValue === undefined);
      })();
      if (!shouldSave) {
        clearEffectDragOverrides(handle.nodePath, handle.effectIndex);
        return;
      }
      (handle.propStatus.status === "keyframed" ? callAddEffectKeyframe({
        fileName: handle.nodePath.absolutePath,
        nodePath: handle.nodePath,
        effectIndex: handle.effectIndex,
        fieldKey: handle.fieldKey,
        sourceFrame: handle.sourceFrame,
        value: lastValue,
        schema: handle.schema,
        setPropStatuses,
        clientId: handle.clientId
      }) : saveEffectProp({
        type: "value",
        fileName: handle.nodePath.absolutePath,
        nodePath: handle.nodePath,
        effectIndex: handle.effectIndex,
        fieldKey: handle.fieldKey,
        value: lastValue,
        defaultValue,
        schema: handle.schema,
        setPropStatuses,
        clientId: handle.clientId
      })).finally(() => {
        clearEffectDragOverrides(handle.nodePath, handle.effectIndex);
      });
    };
    startCapturedPointerSession({
      event,
      captureTarget: event.currentTarget,
      onMove: onPointerMove,
      onEnd: onPointerUp
    });
  }, [
    clearEffectDragOverrides,
    editorSnapping,
    handle,
    nodePathInfo,
    onDraggingChange,
    onSelect,
    outline.points,
    setPropStatuses,
    setEffectDragOverrides
  ]);
  return /* @__PURE__ */ jsx68("circle", {
    cx: position.x,
    cy: position.y,
    r: handle.isSelected ? selectedUvHandleRadius : uvHandleRadius,
    fill: WHITE,
    stroke: BLUE,
    strokeWidth: 2,
    style: uvHandleStyle,
    vectorEffect: "non-scaling-stroke",
    pointerEvents: "all",
    cursor: "default",
    onPointerDown
  });
};
var useSelectedOutlineUvTarget = (layoutTarget) => {
  const timelinePosition = Internals36.Timeline.useTimelinePosition();
  const { selectedItems } = useTimelineSelection();
  const { propStatuses } = useContext35(Internals36.VisualModePropStatusesContext);
  const { getEffectDragOverrides } = useContext35(Internals36.VisualModeDragOverridesContext);
  const { previewServerState } = useContext35(StudioServerConnectionCtx);
  const selectedEffectsBySequenceKey = useMemo37(() => getSelectedEffectFieldsBySequenceKey(selectedItems), [selectedItems]);
  return useMemo37(() => {
    return {
      containsSelection: layoutTarget.containsSelection,
      nodePathInfo: layoutTarget.nodePathInfo,
      uvHandles: getSelectedUvHandles({
        propStatuses,
        clientId: previewServerState.type === "connected" ? previewServerState.clientId : null,
        getEffectDragOverrides,
        nodePath: layoutTarget.nodePathInfo.sequenceSubscriptionKey,
        selectedEffects: selectedEffectsBySequenceKey.get(layoutTarget.key),
        sequence: layoutTarget.sequence,
        sourceFrame: timelinePosition - layoutTarget.keyframeDisplayOffset
      })
    };
  }, [
    getEffectDragOverrides,
    layoutTarget,
    previewServerState,
    propStatuses,
    selectedEffectsBySequenceKey,
    timelinePosition
  ]);
};
var SelectedOutlineUvHandleConnectionLayerActive = ({ layoutTarget, outline }) => {
  const target = useSelectedOutlineUvTarget(layoutTarget);
  if (!target.containsSelection || target.uvHandles.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx68(SelectedUvHandleConnectionLines, {
    handles: target.uvHandles,
    outline
  });
};
var SelectedOutlineUvHandleConnectionLayer = ({ layoutTarget, outline }) => {
  if (layoutTarget?.selectedForUvHandles !== true) {
    return null;
  }
  return /* @__PURE__ */ jsx68(SelectedOutlineUvHandleConnectionLayerActive, {
    layoutTarget,
    outline
  });
};
var SelectedOutlineUvHandleCircleLayerActive = ({ layoutTarget, onDraggingChange, onSelect, outline }) => {
  const target = useSelectedOutlineUvTarget(layoutTarget);
  const ellipseControls = useMemo37(() => getUvEllipseInteractiveControls({
    handles: target.uvHandles,
    dimensions: outline.dimensions,
    points: outline.points
  }), [outline.dimensions, outline.points, target.uvHandles]);
  if (!target.containsSelection || target.uvHandles.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs36(Fragment19, {
    children: [
      ellipseControls.map((control) => /* @__PURE__ */ jsxs36(React45.Fragment, {
        children: [
          /* @__PURE__ */ jsx68(SelectedUvEllipseStartHandle, {
            control,
            onDraggingChange,
            outline
          }),
          /* @__PURE__ */ jsx68(SelectedUvEllipseResizeHandle, {
            axis: "width",
            control,
            onDraggingChange,
            outline
          }),
          /* @__PURE__ */ jsx68(SelectedUvEllipseResizeHandle, {
            axis: "height",
            control,
            onDraggingChange,
            outline
          }),
          /* @__PURE__ */ jsx68(SelectedUvEllipseRotationHandle, {
            control,
            onDraggingChange
          })
        ]
      }, `${control.handle.effectIndex}-${control.handle.fieldKey}-ellipse-controls`)),
      target.uvHandles.map((handle) => /* @__PURE__ */ jsx68(SelectedUvHandleCircle, {
        handle,
        nodePathInfo: target.nodePathInfo,
        onDraggingChange,
        onSelect,
        outline
      }, `${handle.effectIndex}-${handle.fieldKey}`))
    ]
  });
};
var SelectedOutlineUvHandleCircleLayer = ({ layoutTarget, onDraggingChange, onSelect, outline }) => {
  if (layoutTarget?.selectedForUvHandles !== true) {
    return null;
  }
  return /* @__PURE__ */ jsx68(SelectedOutlineUvHandleCircleLayerActive, {
    layoutTarget,
    onDraggingChange,
    onSelect,
    outline
  });
};

// src/components/SelectedOutlineRenderer.tsx
import { jsx as jsx69, jsxs as jsxs37 } from "react/jsx-runtime";
var outlineContainer = {
  position: "absolute",
  inset: 0,
  pointerEvents: "none",
  overflow: "visible"
};
var SelectedOutlineRendererUnmemoized = ({
  compositionHeight,
  compositionWidth,
  dragging,
  getLatestOutlineTargetByKey,
  outlineTargets,
  onDraggingChange,
  onContextMenuOpenChange,
  onSelect,
  scale,
  sequences,
  updateOutlinesRef
}) => {
  const [outlines, setOutlines] = useState20([]);
  const outlinesRef = useRef16(outlines);
  const overlayRef = useRef16(null);
  const resizeObserverRef = useRef16(null);
  const resizeObserverAnimationFrameRef = useRef16(null);
  const observedOutlineElementsRef = useRef16(new Set);
  const contextMenuOpenHandlersRef = useRef16(new Map);
  const registerContextMenuOpen = useCallback30((key4, handler) => {
    if (handler === null) {
      contextMenuOpenHandlersRef.current.delete(key4);
    } else {
      contextMenuOpenHandlersRef.current.set(key4, handler);
    }
  }, []);
  const getContextMenuOpenByKey = useCallback30((key4) => contextMenuOpenHandlersRef.current.get(key4), []);
  const getDelegatedContextMenuItems = useCallback30((event) => {
    if (!(event.target instanceof Element)) {
      return false;
    }
    const polygon = event.target.closest(`polygon[${SELECTED_OUTLINE_KEY_ATTR}]`);
    if (polygon?.ownerSVGElement !== overlayRef.current) {
      return false;
    }
    const key4 = polygon.getAttribute(SELECTED_OUTLINE_KEY_ATTR);
    return key4 === null ? false : getContextMenuOpenByKey(key4)?.() ?? false;
  }, [getContextMenuOpenByKey]);
  const hoveredSequence = useTimelineSequenceHoverState();
  const setHoveredSequence = useSetTimelineSequenceHover();
  const hoveredNodePathKey = hoveredSequence?.nodePathKey ?? null;
  const hoveredTimelineNodePathKey = hoveredSequence?.source === "timeline" ? hoveredNodePathKey : null;
  const updateOutlines = useCallback30(() => {
    if (overlayRef.current === null || outlineTargets.length === 0) {
      if (outlinesRef.current.length === 0) {
        return;
      }
      outlinesRef.current = [];
      setOutlines(outlinesRef.current);
      return;
    }
    const nextOutlines = measureOutlines(overlayRef.current, outlineTargets, hoveredTimelineNodePathKey);
    if (outlinesAreEqual(outlinesRef.current, nextOutlines)) {
      return;
    }
    outlinesRef.current = nextOutlines;
    setOutlines(nextOutlines);
  }, [hoveredTimelineNodePathKey, outlineTargets]);
  useLayoutEffect10(() => {
    updateOutlinesRef.current = updateOutlines;
    updateOutlines();
    return () => {
      if (updateOutlinesRef.current === updateOutlines) {
        updateOutlinesRef.current = () => {
          return;
        };
      }
    };
  }, [updateOutlines, updateOutlinesRef]);
  useLayoutEffect10(() => {
    if (typeof ResizeObserver === "undefined") {
      return;
    }
    const resizeObserver = new ResizeObserver(() => {
      if (resizeObserverAnimationFrameRef.current !== null) {
        return;
      }
      resizeObserverAnimationFrameRef.current = requestAnimationFrame(() => {
        resizeObserverAnimationFrameRef.current = null;
        updateOutlinesRef.current();
      });
    });
    resizeObserverRef.current = resizeObserver;
    return () => {
      if (resizeObserverAnimationFrameRef.current !== null) {
        cancelAnimationFrame(resizeObserverAnimationFrameRef.current);
        resizeObserverAnimationFrameRef.current = null;
      }
      resizeObserver.disconnect();
      resizeObserverRef.current = null;
      observedOutlineElementsRef.current = new Set;
    };
  }, [updateOutlinesRef]);
  useLayoutEffect10(() => {
    const resizeObserver = resizeObserverRef.current;
    if (resizeObserver === null) {
      return;
    }
    const nextObservedElements = new Set;
    if (overlayRef.current !== null) {
      nextObservedElements.add(overlayRef.current);
    }
    for (const target of outlineTargets) {
      if (target.ref.current !== null) {
        nextObservedElements.add(target.ref.current);
      }
    }
    for (const element of observedOutlineElementsRef.current) {
      if (!nextObservedElements.has(element)) {
        resizeObserver.unobserve(element);
      }
    }
    for (const element of nextObservedElements) {
      if (!observedOutlineElementsRef.current.has(element)) {
        resizeObserver.observe(element);
      }
    }
    observedOutlineElementsRef.current = nextObservedElements;
  }, [outlineTargets]);
  const targetsByKey = useMemo38(() => {
    return new Map(outlineTargets.map((target) => [target.key, target]));
  }, [outlineTargets]);
  useEffect26(() => {
    if (hoveredSequence?.source === "canvas" && !outlineTargets.some((target) => target.key === hoveredSequence.key)) {
      setHoveredSequence((currentHover) => currentHover?.source === "canvas" ? null : currentHover);
    }
  }, [hoveredSequence, outlineTargets, setHoveredSequence]);
  const outlineRenderingOrderRef = useRef16([]);
  const outlinesForRendering = useMemo38(() => {
    if (!dragging || outlineRenderingOrderRef.current.length === 0) {
      const orderedOutlines = orderOutlinesForRendering({
        outlines,
        sequences,
        targetsByKey
      });
      outlineRenderingOrderRef.current = orderedOutlines.map((outline) => outline.key);
      return orderedOutlines;
    }
    const currentOutlinesByKey = new Map(outlines.map((outline) => [outline.key, outline]));
    const frozenKeys = new Set(outlineRenderingOrderRef.current);
    const newOutlines = outlines.filter((outline) => !frozenKeys.has(outline.key));
    outlineRenderingOrderRef.current = [
      ...outlineRenderingOrderRef.current,
      ...newOutlines.map((outline) => outline.key)
    ];
    return outlineRenderingOrderRef.current.flatMap((key4) => {
      const outline = currentOutlinesByKey.get(key4);
      return outline === undefined ? [] : [outline];
    });
  }, [dragging, outlines, sequences, targetsByKey]);
  const outlinesByKey = useMemo38(() => {
    return new Map(outlines.map((outline) => [outline.key, outline]));
  }, [outlines]);
  const {
    outlinesForEditingHandles,
    outlinesForTransformOrigin,
    outlinesForUvHandles
  } = useMemo38(() => {
    const editingHandles = [];
    const transformOrigin = [];
    const uvHandles = [];
    for (const outline of outlinesForRendering) {
      const target = targetsByKey.get(outline.key);
      if (target?.containsSelection === true || target !== undefined && timelineSequenceNodePathToKey(target.nodePathInfo.sequenceSubscriptionKey) === hoveredNodePathKey) {
        editingHandles.push(outline);
      }
      if (target?.selectedForUvHandles === true) {
        uvHandles.push(outline);
      }
      if (target?.selectedForTransformOrigin === true || target?.selectedForRotation === true) {
        transformOrigin.push(outline);
      }
    }
    return {
      outlinesForEditingHandles: editingHandles,
      outlinesForTransformOrigin: transformOrigin,
      outlinesForUvHandles: uvHandles
    };
  }, [hoveredNodePathKey, outlinesForRendering, targetsByKey]);
  const targetsRef = useRef16(outlineTargets);
  const outlinesByKeyRef = useRef16(outlinesByKey);
  useLayoutEffect10(() => {
    targetsRef.current = outlineTargets;
    outlinesByKeyRef.current = outlinesByKey;
  }, [outlineTargets, outlinesByKey]);
  const getAllDragTargets = useCallback30(() => targetsRef.current.flatMap((target) => {
    if (!target.selected && !target.containsSelection) {
      return [];
    }
    const drag = getLatestOutlineTargetByKey(target.key)?.drag ?? null;
    return drag === null ? [] : [drag];
  }), [getLatestOutlineTargetByKey]);
  const getAllDragOutlines = useCallback30(() => targetsRef.current.flatMap((target) => {
    if (!target.selected && !target.containsSelection || (getLatestOutlineTargetByKey(target.key)?.drag ?? null) === null) {
      return [];
    }
    const outline = outlinesByKeyRef.current.get(target.key);
    return outline === undefined ? [] : [outline];
  }), [getLatestOutlineTargetByKey]);
  const getAllScaleDragTargets = useCallback30(() => targetsRef.current.flatMap((target) => {
    const scaleDrag = target.selected ? getLatestOutlineTargetByKey(target.key)?.scaleDrag ?? null : null;
    return scaleDrag === null ? [] : [scaleDrag];
  }), [getLatestOutlineTargetByKey]);
  const getAllRotationDragTargets = useCallback30(() => targetsRef.current.flatMap((target) => {
    const rotationDrag = target.selected ? getLatestOutlineTargetByKey(target.key)?.rotationDrag ?? null : null;
    return rotationDrag === null ? [] : [rotationDrag];
  }), [getLatestOutlineTargetByKey]);
  const updateSnapPointsRef = useRef16(() => {
    return;
  });
  const onSnapPointsChange = useCallback30((snapPoints) => updateSnapPointsRef.current(snapPoints), []);
  return /* @__PURE__ */ jsxs37("svg", {
    ref: overlayRef,
    style: outlineContainer,
    width: "100%",
    height: "100%",
    "aria-hidden": "true",
    children: [
      /* @__PURE__ */ jsx69(ContextMenuForTarget, {
        triggerRef: overlayRef,
        getItems: getDelegatedContextMenuItems,
        onOpenChange: onContextMenuOpenChange
      }),
      /* @__PURE__ */ jsx69(SelectedOutlineSnapIndicators, {
        compositionHeight,
        compositionWidth,
        scale,
        updateSnapPointsRef
      }),
      outlinesForRendering.map((outline) => /* @__PURE__ */ jsx69(SelectedOutlineElement, {
        compositionHeight,
        compositionWidth,
        dragging,
        getAllDragOutlines,
        getAllDragTargets,
        getLatestTargetByKey: getLatestOutlineTargetByKey,
        outline,
        onDraggingChange,
        onSnapPointsChange,
        onSelect,
        registerContextMenuOpen,
        scale,
        layoutTarget: targetsByKey.get(outline.key)
      }, outline.key)),
      outlinesForEditingHandles.map((outline) => /* @__PURE__ */ jsx69(SelectedOutlineEditingHandles, {
        dragging,
        getAllRotationDragTargets,
        getAllScaleDragTargets,
        getContextMenuOpenByKey,
        getLatestTargetByKey: getLatestOutlineTargetByKey,
        layoutTarget: targetsByKey.get(outline.key),
        onContextMenuOpenChange,
        onDraggingChange,
        onSelect,
        outline
      }, `${outline.key}-editing-handles`)),
      outlinesForUvHandles.map((outline) => /* @__PURE__ */ jsx69(SelectedOutlineUvHandleConnectionLayer, {
        outline,
        layoutTarget: targetsByKey.get(outline.key)
      }, `${outline.key}-uv-connection-lines`)),
      outlinesForUvHandles.map((outline) => /* @__PURE__ */ jsx69(SelectedOutlineUvHandleCircleLayer, {
        onDraggingChange,
        onSelect,
        outline,
        layoutTarget: targetsByKey.get(outline.key)
      }, `${outline.key}-uv-handles`)),
      outlinesForTransformOrigin.map((outline) => /* @__PURE__ */ jsx69(SelectedOutlineTransformOriginHandle, {
        outline,
        onDraggingChange,
        getLatestTargetByKey: getLatestOutlineTargetByKey,
        layoutTarget: targetsByKey.get(outline.key)
      }, `${outline.key}-transform-origin`))
    ]
  });
};
var SelectedOutlineRenderer = React46.memo(SelectedOutlineRendererUnmemoized);

// src/components/SelectedOutlineOverlay.tsx
import { jsx as jsx70, jsxs as jsxs38, Fragment as Fragment20 } from "react/jsx-runtime";
var getEffectiveCropValue = ({
  activeSchema,
  dragOverrides,
  fieldKey,
  frame: frame2,
  propStatuses,
  runtimeValues
}) => {
  const fieldSchema = activeSchema?.[fieldKey];
  if (fieldSchema?.type !== "number") {
    return 0;
  }
  const propStatus = propStatuses?.[fieldKey];
  const value = propStatus?.status === "static" || propStatus?.status === "keyframed" ? Internals37.getEffectiveVisualModeValue({
    propStatus,
    dragOverrideValue: dragOverrides[fieldKey],
    defaultValue: fieldSchema.default,
    frame: frame2,
    shouldResortToDefaultValueIfUndefined: true
  }) : runtimeValues[fieldKey] ?? fieldSchema.default;
  return typeof value === "number" && Number.isFinite(value) ? value : 0;
};
var getCropDragFields = ({
  activeSchema,
  cropValues,
  propStatuses
}) => {
  if (!canEditSelectedOutlineCrop({
    schema: activeSchema,
    propStatuses
  })) {
    return null;
  }
  const fields = {};
  for (const fieldKey of Object.values(cropFieldKeys)) {
    const fieldSchema = activeSchema?.[fieldKey];
    const propStatus = propStatuses?.[fieldKey];
    const canEditStatus = propStatus?.status === "static" || propStatus?.status === "keyframed" && propStatus.interpolationFunction === "interpolate";
    if (fieldSchema?.type !== "number" || !canEditStatus) {
      return null;
    }
    fields[fieldKey] = {
      defaultValue: fieldSchema.default,
      fieldSchema,
      propStatus,
      value: cropValues[fieldKey]
    };
  }
  return fields;
};
var calculateOutlineTargets = ({
  connectedClientId,
  editorShowOutlines,
  getDragOverrides,
  getScaleLockState,
  isFullscreen,
  manuallyEnabled3DTransformSequenceKeys,
  mode,
  previewSelectionAvailable,
  propStatuses,
  runtimeValuesByStore,
  selectableOutlines,
  selectedCropInfo,
  selectedEffectsBySequenceKey,
  selectedRotationInfo,
  selectedSequenceKeys,
  selectedTransformOriginInfo,
  sequenceKeysContainingSelection,
  studioInteractivityEnabled,
  targetKey,
  targetTimelinePosition
}) => {
  if (isFullscreen || !isStudioSelectionEnabled() || !previewSelectionAvailable || !editorShowOutlines) {
    return [];
  }
  const previewInteractive = connectedClientId !== null && studioInteractivityEnabled;
  const firstNodePathInfoBySourceNode = new Map;
  const selectedSourceNodeKeys = new Set;
  for (const { key: key4, nodePathInfo } of selectableOutlines) {
    const sourceNodeKey = timelineSequenceNodePathToKey(nodePathInfo.sequenceSubscriptionKey);
    if (selectedSequenceKeys.has(key4)) {
      selectedSourceNodeKeys.add(sourceNodeKey);
    }
    const currentFirst = firstNodePathInfoBySourceNode.get(sourceNodeKey);
    if (currentFirst === undefined || nodePathInfo.index < currentFirst.index) {
      firstNodePathInfoBySourceNode.set(sourceNodeKey, nodePathInfo);
    }
  }
  return selectableOutlines.flatMap((selectableOutline) => {
    const { key: key4, keyframeDisplayOffset, nodePathInfo, sequence } = selectableOutline;
    if (targetKey !== null && targetKey !== key4) {
      return [];
    }
    if (sequence.refForOutline === null) {
      throw new Error("Expected sequence to have a ref for outline");
    }
    const selected = selectedSequenceKeys.has(key4);
    const containsSelection = sequenceKeysContainingSelection.has(key4);
    const nodePath = nodePathInfo.sequenceSubscriptionKey;
    const sourceNodeKey = timelineSequenceNodePathToKey(nodePath);
    const showSelectedOutline = containsSelection || selectedSourceNodeKeys.has(sourceNodeKey);
    const selectionNodePathInfo = firstNodePathInfoBySourceNode.get(sourceNodeKey);
    if (selectionNodePathInfo === undefined) {
      throw new Error("Expected a first sequence for the source node");
    }
    const { controls } = sequence;
    const nodePropStatuses = Internals37.getPropStatusesCtx(propStatuses, nodePath);
    const firstKeyframedStatus = Object.values(nodePropStatuses ?? {}).find((status) => status.status === "keyframed");
    const nodeKeyframeDisplayOffset = getKeyframeDisplayOffset({
      propStatus: firstKeyframedStatus,
      keyframeDisplayOffset
    });
    const sourceFrame = targetTimelinePosition - nodeKeyframeDisplayOffset;
    const dragOverrides = getDragOverrides(nodePath) ?? {};
    const runtimeValues = controls ? runtimeValuesByStore.get(controls.runtimeValues) ?? controls.runtimeValues.getSnapshot() : {};
    const activeSchema = controls ? getSelectedOutlineActiveSchema({
      schema: controls.schema,
      currentRuntimeValueDotNotation: runtimeValues,
      dragOverrides,
      propStatus: nodePropStatuses,
      frame: sourceFrame
    }) : null;
    const cropValues = {
      cropLeft: getEffectiveCropValue({
        activeSchema,
        dragOverrides,
        fieldKey: cropFieldKeys.left,
        frame: sourceFrame,
        propStatuses: nodePropStatuses,
        runtimeValues
      }),
      cropRight: getEffectiveCropValue({
        activeSchema,
        dragOverrides,
        fieldKey: cropFieldKeys.right,
        frame: sourceFrame,
        propStatuses: nodePropStatuses,
        runtimeValues
      }),
      cropTop: getEffectiveCropValue({
        activeSchema,
        dragOverrides,
        fieldKey: cropFieldKeys.top,
        frame: sourceFrame,
        propStatuses: nodePropStatuses,
        runtimeValues
      }),
      cropBottom: getEffectiveCropValue({
        activeSchema,
        dragOverrides,
        fieldKey: cropFieldKeys.bottom,
        frame: sourceFrame,
        propStatuses: nodePropStatuses,
        runtimeValues
      })
    };
    const crop = Internals37.resolveSequenceCrop(cropValues);
    const selectedForTransformOrigin = selectedTransformOriginInfo?.sequenceKey === key4;
    const selectedForCrop = selectedCropInfo?.sequenceKey === key4;
    const selectedForRotation = selectedRotationInfo?.sequenceKey === key4;
    const selectedForUvHandles = selectedEffectsBySequenceKey.has(key4);
    const fieldSchema = activeSchema?.[translateFieldKey];
    const propStatus = nodePropStatuses?.[translateFieldKey];
    const scaleFieldSchema = activeSchema?.[scaleFieldKey];
    const scalePropStatus = nodePropStatuses?.[scaleFieldKey];
    const rotationFieldSchema = activeSchema?.[rotateFieldKey];
    const rotationPropStatus = nodePropStatuses?.[rotateFieldKey];
    const transformOriginFieldSchema = activeSchema?.[transformOriginFieldKey];
    const transformOriginPropStatus = nodePropStatuses?.[transformOriginFieldKey];
    const transformOriginValueForRotation = transformOriginFieldSchema?.type === "transform-origin" && (transformOriginPropStatus?.status === "static" || transformOriginPropStatus?.status === "keyframed") ? String(Internals37.getEffectiveVisualModeValue({
      propStatus: transformOriginPropStatus,
      dragOverrideValue: dragOverrides[transformOriginFieldKey],
      defaultValue: transformOriginFieldSchema.default,
      frame: sourceFrame,
      shouldResortToDefaultValueIfUndefined: true
    }) ?? transformOriginFieldSchema.default) : "50% 50%";
    const layoutTarget = {
      key: key4,
      containsSelection,
      crop,
      keyframeDisplayOffset: nodeKeyframeDisplayOffset,
      nodePathInfo,
      ref: sequence.refForOutline,
      selected,
      selectedForCrop,
      selectedForRotation,
      selectedForTransformOrigin,
      selectedForUvHandles,
      showSelectedOutline,
      transformOriginValue: transformOriginValueForRotation,
      selection: {
        type: "sequence",
        nodePathInfo: selectionNodePathInfo
      },
      sequence
    };
    if (mode === "layout") {
      return [layoutTarget];
    }
    const cropFields = getCropDragFields({
      activeSchema,
      cropValues,
      propStatuses: nodePropStatuses
    });
    const canDragStatus = propStatus?.status === "static" || propStatus?.status === "keyframed" && propStatus.interpolationFunction === "interpolate";
    const canRotationDragStatus = rotationPropStatus?.status === "static" || rotationPropStatus?.status === "keyframed" && rotationPropStatus.interpolationFunction === "interpolate";
    const canDrag = previewInteractive && controls !== null && fieldSchema?.type === "translate" && canDragStatus;
    const canScaleDragStatus = scalePropStatus?.status === "static" || scalePropStatus?.status === "keyframed" && scalePropStatus.interpolationFunction === "interpolate";
    const canScaleDrag = previewInteractive && controls !== null && scaleFieldSchema?.type === "scale" && canScaleDragStatus;
    const canRotationDrag = previewInteractive && controls !== null && rotationFieldSchema?.type === "rotation-css" && canRotationDragStatus;
    const transform3DMode = manuallyEnabled3DTransformSequenceKeys.has(key4) || [
      "style.translate",
      "style.scale",
      "style.rotate",
      "style.transformOrigin"
    ].some((fieldKey) => propStatusHas3DTransformValue({
      fieldKey,
      propStatus: nodePropStatuses?.[fieldKey],
      runtimeValue: runtimeValues[fieldKey]
    }));
    const transformOriginSourceFrame = selectedTransformOriginInfo?.displayFrame === null || selectedTransformOriginInfo?.displayFrame === undefined ? sourceFrame : selectedTransformOriginInfo.displayFrame - getKeyframeDisplayOffset({
      propStatus: transformOriginPropStatus,
      keyframeDisplayOffset
    });
    const canTransformOriginStatus = transformOriginPropStatus?.status === "static" || transformOriginPropStatus?.status === "keyframed" && transformOriginPropStatus.interpolationFunction === "interpolate";
    const canTransformOriginTranslateStatus = propStatus?.status === "static" || propStatus?.status === "keyframed" && propStatus.interpolationFunction === "interpolate";
    const canTransformOriginDrag = previewInteractive && (selectedForTransformOrigin || selectedForRotation) && controls !== null && transformOriginFieldSchema?.type === "transform-origin" && fieldSchema?.type === "translate" && canTransformOriginStatus && canTransformOriginTranslateStatus;
    const cropSourceFrame = selectedCropInfo?.displayFrame === null || selectedCropInfo?.displayFrame === undefined ? sourceFrame : selectedCropInfo.displayFrame - nodeKeyframeDisplayOffset;
    const canCropDrag = previewInteractive && selectedForCrop && controls !== null && cropFields !== null;
    return [
      {
        ...layoutTarget,
        canCrop: previewInteractive && controls !== null && cropFields !== null,
        cropDrag: canCropDrag ? {
          clientId: connectedClientId,
          fields: cropFields,
          nodePath,
          schema: controls.schema,
          sourceFrame: cropSourceFrame,
          transformOrigin: transformOriginFieldSchema?.type === "transform-origin" && transformOriginPropStatus !== undefined ? {
            defaultValue: transformOriginFieldSchema.default,
            propStatus: transformOriginPropStatus,
            value: transformOriginValueForRotation
          } : null
        } : null,
        drag: canDrag ? {
          propStatus,
          clientId: connectedClientId,
          fieldDefault: fieldSchema.default,
          keyframeDisplayOffset: getKeyframeDisplayOffset({
            propStatus,
            keyframeDisplayOffset
          }),
          nodePath,
          schema: controls.schema
        } : null,
        scaleDrag: canScaleDrag ? {
          propStatus: scalePropStatus,
          clientId: connectedClientId,
          fieldDefault: scaleFieldSchema.default,
          fieldSchema: scaleFieldSchema,
          keyframeDisplayOffset: getKeyframeDisplayOffset({
            propStatus: scalePropStatus,
            keyframeDisplayOffset
          }),
          linked: getScaleLockState({
            nodePath,
            fieldKey: scaleFieldKey,
            defaultValue: (() => {
              const dragOverrideValue = dragOverrides[scaleFieldKey];
              const effectiveValue = Internals37.getEffectiveVisualModeValue({
                propStatus: scalePropStatus,
                dragOverrideValue,
                defaultValue: scaleFieldSchema.default,
                shouldResortToDefaultValueIfUndefined: true
              });
              const [x, y] = NoReactInternals7.parseScaleValue(effectiveValue);
              return x === y;
            })()
          }),
          nodePath,
          schema: controls.schema
        } : null,
        rotationDrag: canRotationDrag ? {
          propStatus: rotationPropStatus,
          clientId: connectedClientId,
          fieldDefault: rotationFieldSchema.default,
          fieldSchema: rotationFieldSchema,
          keyframeDisplayOffset: getKeyframeDisplayOffset({
            propStatus: rotationPropStatus,
            keyframeDisplayOffset
          }),
          nodePath,
          schema: controls.schema,
          transform3DMode,
          transformOriginValue: transformOriginValueForRotation
        } : null,
        transformOriginDrag: canTransformOriginDrag ? {
          clientId: connectedClientId,
          keyframeDisplayOffset: getKeyframeDisplayOffset({
            propStatus: transformOriginPropStatus,
            keyframeDisplayOffset
          }),
          nodePath,
          originDefault: transformOriginFieldSchema.default,
          originPropStatus: transformOriginPropStatus,
          originValue: String(Internals37.getEffectiveVisualModeValue({
            propStatus: transformOriginPropStatus,
            dragOverrideValue: dragOverrides[transformOriginFieldKey],
            defaultValue: transformOriginFieldSchema.default,
            frame: transformOriginSourceFrame,
            shouldResortToDefaultValueIfUndefined: true
          }) ?? transformOriginFieldSchema.default),
          rotateValue: String(rotationPropStatus?.status === "static" || rotationPropStatus?.status === "keyframed" ? Internals37.getEffectiveVisualModeValue({
            propStatus: rotationPropStatus,
            dragOverrideValue: dragOverrides[rotateFieldKey],
            defaultValue: rotationFieldSchema?.type === "rotation-css" ? rotationFieldSchema.default : "0deg",
            frame: transformOriginSourceFrame,
            shouldResortToDefaultValueIfUndefined: true
          }) ?? "0deg" : "0deg"),
          scaleValue: scalePropStatus?.status === "static" || scalePropStatus?.status === "keyframed" ? String(Internals37.getEffectiveVisualModeValue({
            propStatus: scalePropStatus,
            dragOverrideValue: dragOverrides[scaleFieldKey],
            defaultValue: scaleFieldSchema?.type === "scale" ? scaleFieldSchema.default : 1,
            frame: transformOriginSourceFrame,
            shouldResortToDefaultValueIfUndefined: true
          }) ?? 1) : "1",
          schema: controls.schema,
          sourceFrame: transformOriginSourceFrame,
          translateDefault: fieldSchema.default,
          translatePropStatus: propStatus,
          translateValue: String(Internals37.getEffectiveVisualModeValue({
            propStatus,
            dragOverrideValue: dragOverrides[translateFieldKey],
            defaultValue: fieldSchema.default,
            frame: transformOriginSourceFrame,
            shouldResortToDefaultValueIfUndefined: true
          }) ?? fieldSchema.default)
        } : null
      }
    ];
  });
};
var ActiveSelectedOutlineOverlayUnmemoized = ({
  calculateOutlineTargetsForCurrentState,
  compositionHeight,
  compositionWidth,
  draggingOutline,
  getLatestOutlineTargetByKey,
  getSelectableOutlines,
  hoveredTimelineNodePathKey,
  measureAllOutlines,
  onDraggingChange,
  onContextMenuOpenChange,
  onSelect,
  scale,
  selectedSequenceKeys,
  sequenceKeysContainingSelection,
  sequences,
  translationX,
  translationY
}) => {
  const timelinePosition = Internals37.Timeline.useTimelinePosition();
  const updateOutlinesRef = useRef17(() => {
    return;
  });
  const selectableOutlines = useMemo39(() => {
    return getSelectableOutlines(timelinePosition);
  }, [getSelectableOutlines, timelinePosition]);
  const selectableOutlinesForLayout = useMemo39(() => {
    if (measureAllOutlines) {
      return selectableOutlines;
    }
    const activeNodePathKeys = new Set;
    for (const { key: key4, nodePathInfo } of selectableOutlines) {
      const nodePathKey = timelineSequenceNodePathToKey(nodePathInfo.sequenceSubscriptionKey);
      if (selectedSequenceKeys.has(key4) || sequenceKeysContainingSelection.has(key4) || nodePathKey === hoveredTimelineNodePathKey) {
        activeNodePathKeys.add(nodePathKey);
      }
    }
    return selectableOutlines.filter(({ nodePathInfo }) => activeNodePathKeys.has(timelineSequenceNodePathToKey(nodePathInfo.sequenceSubscriptionKey)));
  }, [
    hoveredTimelineNodePathKey,
    measureAllOutlines,
    selectableOutlines,
    selectedSequenceKeys,
    sequenceKeysContainingSelection
  ]);
  const outlineRuntimeControls = useMemo39(() => {
    return selectableOutlinesForLayout.flatMap(({ key: key4, sequence }) => {
      if (!selectedSequenceKeys.has(key4) && !sequenceKeysContainingSelection.has(key4)) {
        return [];
      }
      return sequence.controls ? [sequence.controls] : [];
    });
  }, [
    selectableOutlinesForLayout,
    selectedSequenceKeys,
    sequenceKeysContainingSelection
  ]);
  const outlineRuntimeSnapshots = useRuntimeValueSnapshots(outlineRuntimeControls);
  const outlineRuntimeValuesByStore = useMemo39(() => new Map(outlineRuntimeControls.map((controls, index) => [
    controls.runtimeValues,
    outlineRuntimeSnapshots[index]
  ])), [outlineRuntimeControls, outlineRuntimeSnapshots]);
  const outlineTargets = useMemo39(() => calculateOutlineTargetsForCurrentState({
    mode: "layout",
    runtimeValuesByStore: outlineRuntimeValuesByStore,
    selectableOutlines: selectableOutlinesForLayout,
    targetKey: null,
    targetTimelinePosition: timelinePosition
  }), [
    calculateOutlineTargetsForCurrentState,
    outlineRuntimeValuesByStore,
    selectableOutlinesForLayout,
    timelinePosition
  ]);
  useLayoutEffect11(() => {
    updateOutlinesRef.current();
  }, [scale, translationX, translationY]);
  return /* @__PURE__ */ jsx70(SelectedOutlineRenderer, {
    compositionHeight,
    compositionWidth,
    dragging: draggingOutline,
    getLatestOutlineTargetByKey,
    outlineTargets,
    onDraggingChange,
    onContextMenuOpenChange,
    onSelect,
    scale,
    sequences,
    updateOutlinesRef
  });
};
var ActiveSelectedOutlineOverlay = React47.memo(ActiveSelectedOutlineOverlayUnmemoized);
var SelectedOutlineOverlayUnmemoized = ({
  canvasHovered,
  compositionHeight,
  compositionWidth,
  scale,
  translationX,
  translationY
}) => {
  const { selectedItems, selectItem } = useTimelineSelection();
  const { sequences } = useContext36(Internals37.SequenceManager);
  const { compositions } = useContext36(Internals37.CompositionManager);
  const { propStatuses } = useContext36(Internals37.VisualModePropStatusesContext);
  const { previewServerState } = useContext36(StudioServerConnectionCtx);
  const { overrideIdToNodePathMappings } = useContext36(Internals37.OverrideIdsToNodePathsGettersContext);
  const { getDragOverrides } = useContext36(Internals37.VisualModeDragOverridesContext);
  const { getScaleLockState } = useContext36(ScaleLockContext);
  const { manuallyEnabledSequenceKeys } = useContext36(Transform3DModeStateContext);
  const { editorShowOutlines } = useContext36(EditorShowOutlinesContext);
  const setHoveredSequence = useSetTimelineSequenceHover();
  const hoveredSequence = useTimelineSequenceHoverState();
  const isFullscreen = useIsFullscreen();
  const { getCurrentFrame: getCurrentFrame2 } = PlayerInternals7.usePlayerMethods();
  const [draggingOutline, setDraggingOutline] = useState21(false);
  const [canvasContextMenuOpen, setCanvasContextMenuOpen] = useState21(false);
  const previewSelectionAvailable = previewServerState.type === "connected" || window.remotion_isReadOnlyStudio;
  const selectedSequenceKeys = useMemo39(() => getSelectedSequenceKeys(selectedItems), [selectedItems]);
  const sequenceKeysContainingSelection = useMemo39(() => getSequenceKeysContainingSelection(selectedItems), [selectedItems]);
  const selectedEffectsBySequenceKey = useMemo39(() => getSelectedEffectFieldsBySequenceKey(selectedItems), [selectedItems]);
  const selectedTransformOriginInfo = useMemo39(() => getSelectedTransformOriginInfo(selectedItems), [selectedItems]);
  const selectedRotationInfo = useMemo39(() => getSelectedRotationInfo(selectedItems), [selectedItems]);
  const selectedCropInfo = useMemo39(() => getSelectedCropInfo(selectedItems), [selectedItems]);
  const getSelectableOutlines = useCallback31((timelinePosition) => {
    if (isFullscreen || !isStudioSelectionEnabled() || !previewSelectionAvailable || !editorShowOutlines) {
      return [];
    }
    return getSequencesWithSelectableOutlines({
      sequences,
      overrideIdsToNodePaths: overrideIdToNodePathMappings,
      compositions,
      timelinePosition
    });
  }, [
    compositions,
    editorShowOutlines,
    isFullscreen,
    overrideIdToNodePathMappings,
    previewSelectionAvailable,
    sequences
  ]);
  const calculateOutlineTargetsForCurrentState = useCallback31((options) => calculateOutlineTargets({
    ...options,
    connectedClientId: previewServerState.type === "connected" ? previewServerState.clientId : null,
    editorShowOutlines,
    getDragOverrides,
    getScaleLockState,
    isFullscreen,
    manuallyEnabled3DTransformSequenceKeys: manuallyEnabledSequenceKeys,
    previewSelectionAvailable,
    propStatuses,
    selectedCropInfo,
    selectedEffectsBySequenceKey,
    selectedRotationInfo,
    selectedSequenceKeys,
    selectedTransformOriginInfo,
    sequenceKeysContainingSelection,
    studioInteractivityEnabled: isStudioInteractivityEnabled()
  }), [
    editorShowOutlines,
    getDragOverrides,
    getScaleLockState,
    isFullscreen,
    manuallyEnabledSequenceKeys,
    previewSelectionAvailable,
    previewServerState,
    propStatuses,
    selectedCropInfo,
    selectedEffectsBySequenceKey,
    selectedRotationInfo,
    selectedSequenceKeys,
    selectedTransformOriginInfo,
    sequenceKeysContainingSelection
  ]);
  const getSelectableOutlinesRef = useRef17(getSelectableOutlines);
  const selectableOutlinesCacheRef = useRef17(null);
  useLayoutEffect11(() => {
    getSelectableOutlinesRef.current = getSelectableOutlines;
  }, [getSelectableOutlines]);
  const getSelectableOutlinesAtFrame = useCallback31((timelinePosition) => {
    const currentGetSelectableOutlines = getSelectableOutlinesRef.current;
    const cached = selectableOutlinesCacheRef.current;
    if (cached?.timelinePosition === timelinePosition && cached.getSelectableOutlines === currentGetSelectableOutlines) {
      return cached.selectableOutlines;
    }
    const selectableOutlines = currentGetSelectableOutlines(timelinePosition);
    selectableOutlinesCacheRef.current = {
      getSelectableOutlines: currentGetSelectableOutlines,
      selectableOutlines,
      timelinePosition
    };
    return selectableOutlines;
  }, []);
  const getLatestOutlineTargetByKey = useCallback31((key4) => {
    const timelinePosition = getCurrentFrame2();
    const target = calculateOutlineTargetsForCurrentState({
      mode: "controls",
      runtimeValuesByStore: new Map,
      selectableOutlines: getSelectableOutlinesAtFrame(timelinePosition),
      targetKey: key4,
      targetTimelinePosition: timelinePosition
    })[0];
    return target;
  }, [
    calculateOutlineTargetsForCurrentState,
    getCurrentFrame2,
    getSelectableOutlinesAtFrame
  ]);
  const getCurrentSelectableOutlines = useCallback31(() => getSelectableOutlinesAtFrame(getCurrentFrame2()), [getCurrentFrame2, getSelectableOutlinesAtFrame]);
  const onDraggingChange = useCallback31((dragging) => {
    setDraggingOutline(dragging);
    if (dragging) {
      setHoveredSequence((currentHover) => currentHover?.source === "canvas" ? null : currentHover);
    }
  }, [setHoveredSequence]);
  const selectOutlineItem = useCallback31((item, interaction) => {
    selectItem(item, interaction, undefined, { reveal: true });
  }, [selectItem]);
  const measurementActive = canvasHovered || draggingOutline || canvasContextMenuOpen || sequenceKeysContainingSelection.size > 0 || hoveredSequence?.source === "timeline";
  const measureAllOutlines = canvasHovered || draggingOutline || canvasContextMenuOpen;
  const hoveredTimelineNodePathKey = hoveredSequence?.source === "timeline" ? hoveredSequence.nodePathKey : null;
  useLayoutEffect11(() => {
    if (measurementActive) {
      return;
    }
    setHoveredSequence((currentHover) => currentHover?.source === "canvas" ? null : currentHover);
  }, [measurementActive, setHoveredSequence]);
  return /* @__PURE__ */ jsxs38(Fragment20, {
    children: [
      /* @__PURE__ */ jsx70(SelectedOutlineKeyboardControls, {
        getLatestOutlineTargetByKey,
        getSelectableOutlines: getCurrentSelectableOutlines
      }),
      measurementActive ? /* @__PURE__ */ jsx70(ActiveSelectedOutlineOverlay, {
        calculateOutlineTargetsForCurrentState,
        compositionHeight,
        compositionWidth,
        draggingOutline,
        getLatestOutlineTargetByKey,
        getSelectableOutlines,
        hoveredTimelineNodePathKey,
        measureAllOutlines,
        onDraggingChange,
        onContextMenuOpenChange: setCanvasContextMenuOpen,
        onSelect: selectOutlineItem,
        scale,
        selectedSequenceKeys,
        sequenceKeysContainingSelection,
        sequences,
        translationX,
        translationY
      }) : null
    ]
  });
};
var SelectedOutlineOverlay = React47.memo(SelectedOutlineOverlayUnmemoized);

// src/components/StaticFilePreview.tsx
import { useContext as useContext37 } from "react";
import { staticFile } from "remotion";
import { jsx as jsx71, jsxs as jsxs39 } from "react/jsx-runtime";
var msgStyle3 = {
  fontSize: 13,
  color: WHITE,
  fontFamily: "sans-serif",
  display: "flex",
  justifyContent: "center"
};
var errMsgStyle2 = {
  ...msgStyle3,
  color: LIGHT_TEXT
};
var StaticFilePreview = ({ currentAsset, assetMetadata }) => {
  const fileType = getPreviewFileType(currentAsset);
  const staticFileSrc = staticFile(currentAsset);
  const staticFiles = useStaticFiles();
  const connectionStatus = useContext37(StudioServerConnectionCtx).previewServerState.type;
  const exists = staticFiles.find((file) => file.name === currentAsset);
  if (connectionStatus === "disconnected") {
    return /* @__PURE__ */ jsx71("div", {
      style: errMsgStyle2,
      children: "Studio server disconnected"
    });
  }
  if (!exists) {
    return /* @__PURE__ */ jsxs39("div", {
      style: errMsgStyle2,
      children: [
        currentAsset,
        " does not exist in your public folder."
      ]
    });
  }
  if (!currentAsset) {
    return null;
  }
  return /* @__PURE__ */ jsx71(FilePreview, {
    currentAsset,
    fileType,
    src: addAssetCacheBust({
      fetchedAt: assetMetadata && assetMetadata.type === "found" ? assetMetadata.fetchedAt : 0,
      src: staticFileSrc
    }),
    assetMetadata
  });
};

// src/components/Preview.tsx
import { jsx as jsx72, jsxs as jsxs40 } from "react/jsx-runtime";
var centeredContainer = {
  display: "flex",
  flex: 1,
  justifyContent: "center",
  alignItems: "center"
};
var label3 = {
  fontFamily: "sans-serif",
  fontSize: 14,
  color: LIGHT_TEXT
};
var assetMetadataErrorContainer = {
  marginLeft: "auto",
  marginRight: "auto",
  width: "100%",
  position: "absolute",
  height: "100%",
  overflowY: "auto"
};
var checkerboardSize = 49;
var PIXEL_GRID_MIN_SCALE = 4;
var containerStyle = (options) => {
  return {
    transform: `scale(${options.scale})`,
    marginLeft: options.xCorrection,
    marginTop: options.yCorrection,
    width: options.width,
    height: options.height,
    display: "flex",
    overflow: "hidden",
    position: "absolute",
    backgroundColor: checkerboardBackgroundColor(options.checkerboard),
    backgroundImage: checkerboardBackgroundImage(options.checkerboard),
    backgroundSize: getCheckerboardBackgroundSize(checkerboardSize),
    backgroundPosition: getCheckerboardBackgroundPos(checkerboardSize)
  };
};
var PixelGrid = ({ scale }) => {
  const { editorShowPixelGrid } = useContext38(EditorShowPixelGridContext);
  if (!editorShowPixelGrid || scale < PIXEL_GRID_MIN_SCALE) {
    return null;
  }
  return /* @__PURE__ */ jsx72("div", {
    "aria-hidden": "true",
    className: "css-reset",
    "data-testid": "pixel-grid",
    style: {
      position: "absolute",
      inset: 0,
      pointerEvents: "none",
      opacity: 0.35,
      backgroundImage: `linear-gradient(to right, ${RULER_COLOR} 1px, transparent 1px), linear-gradient(to bottom, ${RULER_COLOR} 1px, transparent 1px)`,
      backgroundSize: `${scale}px ${scale}px`
    }
  });
};
var VideoPreview = ({
  canvasSize,
  contentDimensions,
  canvasContent,
  assetMetadata,
  onRetryAssetMetadata
}) => {
  if (assetMetadata && assetMetadata.type === "not-found") {
    return /* @__PURE__ */ jsx72("div", {
      style: centeredContainer,
      children: /* @__PURE__ */ jsx72("div", {
        style: label3,
        children: "File does not exist"
      })
    });
  }
  if (assetMetadata && assetMetadata.type === "metadata-error") {
    return /* @__PURE__ */ jsx72("div", {
      style: assetMetadataErrorContainer,
      className: VERTICAL_SCROLLBAR_CLASSNAME,
      children: /* @__PURE__ */ jsx72(ErrorLoader, {
        canHaveDismissButton: false,
        keyboardShortcuts: false,
        error: assetMetadata.error,
        onRetry: onRetryAssetMetadata ?? null,
        calculateMetadata: false
      }, assetMetadata.error.stack)
    });
  }
  if (contentDimensions === null || canvasContent.type !== "composition" && assetMetadata === null) {
    return /* @__PURE__ */ jsx72("div", {
      style: centeredContainer,
      children: /* @__PURE__ */ jsx72(Spinner, {
        duration: 0.5,
        size: 24
      })
    });
  }
  return /* @__PURE__ */ jsx72(CompWhenItHasDimensions, {
    contentDimensions,
    canvasSize,
    canvasContent,
    assetMetadata
  });
};
var CompWhenItHasDimensions = ({ contentDimensions, canvasSize, canvasContent, assetMetadata }) => {
  const { size: previewSize } = useContext38(Internals38.PreviewSizeContext);
  const { currentAssetMetadata } = useContext38(Internals38.CompositionManager);
  const [canvasHovered, setCanvasHovered] = useState23(false);
  const compositionContainerRef = useRef18(null);
  useEffect27(() => {
    const compositionContainer = compositionContainerRef.current;
    if (compositionContainer === null) {
      setCanvasHovered(false);
      return;
    }
    const onPointerEnter = () => setCanvasHovered(true);
    const onPointerLeave = () => setCanvasHovered(false);
    setCanvasHovered(compositionContainer.matches(":hover"));
    compositionContainer.addEventListener("pointerenter", onPointerEnter);
    compositionContainer.addEventListener("pointerleave", onPointerLeave);
    return () => {
      compositionContainer.removeEventListener("pointerenter", onPointerEnter);
      compositionContainer.removeEventListener("pointerleave", onPointerLeave);
    };
  }, [canvasContent.type]);
  const { centerX, centerY, yCorrection, xCorrection, scale } = useMemo40(() => {
    if (contentDimensions === "none") {
      return {
        centerX: 0,
        centerY: 0,
        yCorrection: 0,
        xCorrection: 0,
        scale: 1
      };
    }
    return canvasContent.type === "composition" ? calculateStudioCanvasTransformation({
      canvasSize,
      compositionHeight: contentDimensions.height,
      compositionWidth: contentDimensions.width,
      previewSize: previewSize.size
    }) : PlayerInternals8.calculateCanvasTransformation({
      canvasSize,
      compositionHeight: contentDimensions.height,
      compositionWidth: contentDimensions.width,
      previewSize: previewSize.size
    });
  }, [canvasContent.type, canvasSize, contentDimensions, previewSize.size]);
  const assetFileType = canvasContent.type === "asset" ? getPreviewFileType(canvasContent.asset) : null;
  const mediaCompositionFileType = (assetFileType === "audio" || assetFileType === "video") && assetMetadata?.type === "found" && assetMetadata.mediaMetadata !== null && canvasContent.type === "asset" && currentAssetMetadata?.asset === canvasContent.asset && contentDimensions !== "none" ? assetFileType : null;
  const outer = useMemo40(() => {
    return {
      width: contentDimensions === "none" ? "100%" : contentDimensions.width * scale,
      height: contentDimensions === "none" ? "100%" : contentDimensions.height * scale,
      display: "flex",
      flexDirection: "column",
      position: "absolute",
      left: centerX - previewSize.translation.x,
      top: centerY - previewSize.translation.y,
      overflow: canvasContent.type === "composition" ? "visible" : "hidden",
      justifyContent: canvasContent.type === "asset" && mediaCompositionFileType === null ? "center" : "flex-start",
      alignItems: canvasContent.type === "asset" && mediaCompositionFileType === null && assetFileType === "audio" ? "center" : "normal"
    };
  }, [
    contentDimensions,
    scale,
    centerX,
    previewSize.translation.x,
    previewSize.translation.y,
    centerY,
    canvasContent,
    mediaCompositionFileType,
    assetFileType
  ]);
  if (canvasContent.type === "asset") {
    if (mediaCompositionFileType !== null && assetMetadata?.type === "found" && contentDimensions !== "none") {
      return /* @__PURE__ */ jsxs40("div", {
        ref: compositionContainerRef,
        className: "remotion-studio-composition-container",
        style: outer,
        children: [
          mediaCompositionFileType === "audio" ? /* @__PURE__ */ jsx72(AudioFileIcon, {
            "aria-label": "Audio asset",
            color: LIGHT_TEXT,
            role: "img",
            style: {
              height: 64,
              left: "50%",
              opacity: 0.25,
              pointerEvents: "none",
              position: "absolute",
              top: "50%",
              transform: "translate(-50%, -50%)",
              width: 64
            }
          }) : null,
          /* @__PURE__ */ jsx72(PortalContainer, {
            contentDimensions,
            offscreen: mediaCompositionFileType === "audio",
            scale,
            xCorrection,
            yCorrection
          }),
          /* @__PURE__ */ jsx72(AssetMediaComposition, {
            asset: canvasContent.asset,
            fetchedAt: assetMetadata.fetchedAt,
            fileType: mediaCompositionFileType
          })
        ]
      });
    }
    return /* @__PURE__ */ jsx72("div", {
      style: outer,
      children: /* @__PURE__ */ jsx72(StaticFilePreview, {
        assetMetadata,
        currentAsset: canvasContent.asset
      })
    });
  }
  if (canvasContent.type === "output") {
    return /* @__PURE__ */ jsx72("div", {
      style: outer,
      children: /* @__PURE__ */ jsx72(RenderPreview, {
        path: canvasContent.path,
        assetMetadata
      })
    });
  }
  if (canvasContent.type === "output-blob") {
    return /* @__PURE__ */ jsx72("div", {
      style: outer,
      children: /* @__PURE__ */ jsx72(RenderPreview, {
        path: canvasContent.displayName,
        assetMetadata,
        getBlob: canvasContent.getBlob
      })
    });
  }
  return /* @__PURE__ */ jsxs40("div", {
    ref: compositionContainerRef,
    className: "remotion-studio-composition-container",
    style: outer,
    children: [
      /* @__PURE__ */ jsx72(PortalContainer, {
        contentDimensions,
        offscreen: false,
        scale,
        xCorrection,
        yCorrection
      }),
      /* @__PURE__ */ jsx72(PixelGrid, {
        scale
      }),
      /* @__PURE__ */ jsx72(SelectedOutlineOverlay, {
        canvasHovered,
        compositionHeight: contentDimensions.height,
        compositionWidth: contentDimensions.width,
        scale,
        translationX: previewSize.translation.x,
        translationY: previewSize.translation.y
      })
    ]
  });
};
var AssetMediaComposition = ({ asset, fetchedAt, fileType }) => {
  const src = addAssetCacheBust({
    fetchedAt,
    src: staticFile2(asset)
  });
  const style4 = {
    width: "100%",
    height: "100%"
  };
  return createPortal(/* @__PURE__ */ jsx72(Internals38.CanUseRemotionHooksProvider, {
    children: /* @__PURE__ */ jsx72(Internals38.DisableInteractivityProvider, {
      children: /* @__PURE__ */ jsx72("div", {
        "data-testid": "asset-media-preview",
        style: style4,
        children: fileType === "video" ? /* @__PURE__ */ jsx72(Video, {
          name: asset,
          src,
          style: style4
        }) : /* @__PURE__ */ jsx72(Audio, {
          name: asset,
          src
        })
      })
    })
  }), Internals38.portalNode());
};
var PortalContainer = ({ offscreen, scale, xCorrection, yCorrection, contentDimensions }) => {
  const { checkerboard } = useContext38(CheckerboardContext);
  const { clearSelection } = useTimelineSelection();
  const portalContainer = useRef18(null);
  const style4 = useMemo40(() => {
    if (offscreen) {
      return {
        height: contentDimensions.height,
        left: -999999,
        overflow: "hidden",
        position: "fixed",
        top: 0,
        width: contentDimensions.width
      };
    }
    return containerStyle({
      checkerboard,
      scale,
      xCorrection,
      yCorrection,
      width: contentDimensions.width,
      height: contentDimensions.height
    });
  }, [
    checkerboard,
    contentDimensions.height,
    contentDimensions.width,
    offscreen,
    scale,
    xCorrection,
    yCorrection
  ]);
  useLayoutEffect12(() => {
    const { current } = portalContainer;
    current?.appendChild(Internals38.portalNode());
    return () => {
      const portalNode = Internals38.portalNode();
      if (current && portalNode.parentNode === current) {
        current.removeChild(portalNode);
        Internals38.setPortalNodeCurrentScale(1);
      }
    };
  }, []);
  useLayoutEffect12(() => {
    Internals38.setPortalNodeCurrentScale(scale);
  }, [scale]);
  const onPointerDown = useCallback32((event) => {
    if (!shouldClearSelectionOnPointerDown(event)) {
      return;
    }
    clearSelection();
  }, [clearSelection]);
  useEffect27(() => {
    const { current } = portalContainer;
    if (!current) {
      return;
    }
    current.addEventListener("pointerdown", onPointerDown);
    return () => {
      current.removeEventListener("pointerdown", onPointerDown);
    };
  }, [onPointerDown]);
  return /* @__PURE__ */ jsx72("div", {
    ref: portalContainer,
    style: style4
  });
};

// src/components/ResetZoomButton.tsx
import { useCallback as useCallback33 } from "react";
import { jsx as jsx73 } from "react/jsx-runtime";
var button = {
  appearance: "none",
  backgroundColor: INPUT_BACKGROUND,
  border: `1px solid ${BLACK_ALPHA_60}`,
  borderRadius: 4,
  color: WHITE,
  flexDirection: "row",
  fontFamily: "inherit",
  fontSize: 14
};
var buttonContainer = {
  cursor: "pointer",
  fontSize: 14,
  padding: 10
};
var ResetZoomButton = ({ onClick }) => {
  const onPointerDown = useCallback33((event) => {
    event.stopPropagation();
  }, []);
  return /* @__PURE__ */ jsx73("button", {
    style: button,
    type: "button",
    onClick,
    onPointerDown,
    children: /* @__PURE__ */ jsx73("div", {
      className: "css-reset",
      style: buttonContainer,
      children: "Reset zoom"
    })
  });
};

// src/components/SvgImportDialog.tsx
import { useCallback as useCallback34, useContext as useContext39, useEffect as useEffect28, useRef as useRef19 } from "react";
import { jsx as jsx74, jsxs as jsxs41 } from "react/jsx-runtime";
var content = {
  padding: 16,
  fontFamily: "inherit",
  fontSize: 14,
  lineHeight: 1.4,
  color: "inherit",
  minWidth: 560
};
var inlineCode = {
  fontFamily: "monospace",
  fontSize: "inherit",
  lineHeight: "inherit",
  color: "inherit"
};
var useSvgImportDialog = () => {
  const { setSelectedModal } = useContext39(SetSelectedModalContext);
  return useCallback34(() => {
    return new Promise((resolve) => {
      let settled = false;
      const settle = (result) => {
        if (settled) {
          return;
        }
        settled = true;
        resolve(result);
      };
      setSelectedModal({
        type: "svg-import-dialog",
        id: String(Math.random()),
        onImage: () => settle("image"),
        onInline: () => settle("inline"),
        onDismiss: () => settle(null)
      });
    });
  }, [setSelectedModal]);
};
var SvgImportDialog = ({ state }) => {
  const { setSelectedModal } = useContext39(SetSelectedModalContext);
  const settledRef = useRef19(false);
  const closeCurrentModal = useCallback34(() => {
    setSelectedModal((modal) => modal?.type === "svg-import-dialog" && modal.id === state.id ? null : modal);
  }, [setSelectedModal, state.id]);
  const settle = useCallback34((result) => {
    if (settledRef.current) {
      return;
    }
    settledRef.current = true;
    closeCurrentModal();
    if (result === "image") {
      state.onImage();
    } else if (result === "inline") {
      state.onInline();
    } else {
      state.onDismiss();
    }
  }, [closeCurrentModal, state]);
  useEffect28(() => {
    return () => {
      if (settledRef.current) {
        return;
      }
      settledRef.current = true;
      state.onDismiss();
    };
  }, [state]);
  const onImage = useCallback34(() => {
    settle("image");
  }, [settle]);
  const onInline = useCallback34(() => {
    settle("inline");
  }, [settle]);
  const onDismiss = useCallback34(() => {
    settle(null);
  }, [settle]);
  return /* @__PURE__ */ jsxs41(ModalContainer, {
    onOutsideClick: onDismiss,
    onEscape: onDismiss,
    children: [
      /* @__PURE__ */ jsx74(ModalHeader, {
        title: "Add .svg",
        onClose: onDismiss
      }),
      /* @__PURE__ */ jsx74("div", {
        style: content,
        children: "Do you want to import this as an image or as inline SVG?"
      }),
      /* @__PURE__ */ jsx74(ModalFooterContainer, {
        children: /* @__PURE__ */ jsxs41(Row, {
          align: "center",
          children: [
            /* @__PURE__ */ jsx74(Flex, {}),
            /* @__PURE__ */ jsxs41(Button, {
              onClick: onImage,
              children: [
                "Add to ",
                /* @__PURE__ */ jsx74("code", {
                  style: inlineCode,
                  children: "public"
                }),
                " and import as",
                " ",
                /* @__PURE__ */ jsx74("code", {
                  style: inlineCode,
                  children: "<CanvasImage>"
                })
              ]
            }),
            /* @__PURE__ */ jsx74(Spacing, {
              x: 1
            }),
            /* @__PURE__ */ jsxs41(ModalButton, {
              onClick: onInline,
              autoFocus: true,
              children: [
                "Import as inline ",
                /* @__PURE__ */ jsx74("code", {
                  style: inlineCode,
                  children: "<svg>"
                })
              ]
            })
          ]
        })
      })
    ]
  });
};

// src/components/Canvas.tsx
import { jsx as jsx75, jsxs as jsxs42, Fragment as Fragment21 } from "react/jsx-runtime";
var getCanvasDragPreviewMetadata = (mimeTypes) => {
  const composition = getCompositionDragPreviewMetadata(mimeTypes);
  if (composition !== null) {
    return {
      ...composition,
      width: composition.width ?? undefined,
      height: composition.height ?? undefined,
      durationInFrames: composition.durationInFrames ?? undefined
    };
  }
  return StudioProtocolInternals.getDragPreviewMetadata(mimeTypes);
};
var getContainerStyle = (editorZoomGestures) => ({
  flex: 1,
  display: "flex",
  overflow: "hidden",
  position: "relative",
  backgroundColor: BACKGROUND,
  ...editorZoomGestures ? { touchAction: "none" } : {}
});
var resetZoom = {
  position: "absolute",
  top: SPACING_UNIT * 2,
  right: SPACING_UNIT * 2
};
var ZOOM_PX_FACTOR = 0.003;
var calculateCanvasScale = ({
  addFitPadding,
  canvasSize,
  compositionHeight,
  compositionWidth,
  previewSize
}) => {
  const options = {
    canvasSize,
    compositionHeight,
    compositionWidth,
    previewSize
  };
  return addFitPadding ? calculateStudioScale(options) : Internals39.calculateScale(options);
};
var getDropPosition = ({
  addFitPadding,
  clientX,
  clientY,
  contentDimensions,
  unbounded,
  previewSize,
  size: size2
}) => {
  if (contentDimensions === null || contentDimensions === "none") {
    return null;
  }
  const scale = calculateCanvasScale({
    addFitPadding,
    canvasSize: size2,
    compositionHeight: contentDimensions.height,
    compositionWidth: contentDimensions.width,
    previewSize: previewSize.size
  });
  const getCenterPoint = unbounded ? getUnboundedCenterPointWhileScrolling : getCenterPointWhileScrolling;
  const { centerX, centerY } = getCenterPoint({
    size: size2,
    clientX,
    clientY,
    compositionWidth: contentDimensions.width,
    compositionHeight: contentDimensions.height,
    scale,
    translation: previewSize.translation
  });
  return { centerX, centerY };
};
var isDragEventInsideCanvas = (event) => {
  const { current } = canvasRef;
  if (!current) {
    return false;
  }
  const targetIsNode = event.target instanceof Node;
  if (targetIsNode && current.contains(event.target)) {
    return true;
  }
  const rect = current.getBoundingClientRect();
  return event.clientX >= rect.left && event.clientX <= rect.right && event.clientY >= rect.top && event.clientY <= rect.bottom;
};
var Canvas = ({ canvasContent, size: size2 }) => {
  const { setSize, size: previewSize } = useContext40(Internals39.PreviewSizeContext);
  const { editorZoomGestures } = useContext40(EditorZoomGesturesContext);
  const previewSnapshotRef = useRef20({
    previewSize,
    canvasSize: size2,
    contentDimensions: null
  });
  const pinchBaseZoomRef = useRef20(null);
  const suppressWheelFromWebKitPinchRef = useRef20(false);
  const touchPinchRef = useRef20(null);
  const keybindings = useKeybinding();
  const chooseSvgImportMode = useSvgImportDialog();
  const { setSelectedModal } = useContext40(SetSelectedModalContext);
  const config = Internals39.useUnsafeVideoConfig();
  const areRulersVisible = useIsRulerVisible();
  const { editorShowGuides } = useContext40(EditorShowGuidesContext);
  const { editorSnapping } = useContext40(EditorSnappingContext);
  const { compositions } = useContext40(Internals39.CompositionManager);
  const { setCurrentAssetMetadata } = useContext40(Internals39.CompositionSetters);
  const { previewServerState, subscribeToEvent } = useContext40(StudioServerConnectionCtx);
  const previewServerClientId = previewServerState.type === "connected" ? previewServerState.clientId : null;
  const [isAddingAsset, setIsAddingAsset] = useState25(false);
  const [compositionDropPreview, setCompositionDropPreview] = useState25(null);
  const [pendingElementInstallRequests, setPendingElementInstallRequests] = useState25([]);
  const [activeElementInstallRequest, setActiveElementInstallRequest] = useState25(null);
  const lastFocusedAtRef = useRef20(typeof document === "undefined" || document.hasFocus() ? Date.now() : null);
  const [assetResolution, setAssetResolution] = useState25(null);
  const metadataRequestRef = useRef20(0);
  const currentCompositionId = canvasContent.type === "composition" ? canvasContent.compositionId : null;
  const currentComposition = useMemo41(() => {
    if (currentCompositionId === null) {
      return null;
    }
    return compositions.find((composition) => composition.id === currentCompositionId) ?? null;
  }, [compositions, currentCompositionId]);
  const resolvedCompositionLocation = useResolvedStack(currentComposition?.stack ?? null);
  const compositionFile = resolvedCompositionLocation?.source ?? null;
  const compositionComponentInfo = useCachedCompositionComponentInfo({
    compositionFile,
    compositionId: currentCompositionId
  });
  const canReceiveElementInstallRequest = previewServerClientId !== null && !window.remotion_isReadOnlyStudio && currentCompositionId !== null && compositionFile !== null;
  const canInsertIntoCurrentComposition = canReceiveElementInstallRequest && compositionComponentInfo?.canAddSequence === true;
  const canDropAssets = canInsertIntoCurrentComposition && !isAddingAsset;
  const cannotAddSequence = compositionComponentInfo?.canAddSequence === false;
  const contentDimensions = useMemo41(() => {
    if ((canvasContent.type === "asset" || canvasContent.type === "output" || canvasContent.type === "output-blob") && assetResolution && assetResolution.type === "found") {
      return assetResolution.dimensions;
    }
    if (config) {
      return { width: config.width, height: config.height };
    }
    return null;
  }, [assetResolution, config, canvasContent]);
  const isFit = previewSize.size === "auto";
  const addFitPadding = canvasContent.type === "composition";
  previewSnapshotRef.current = {
    previewSize,
    canvasSize: size2,
    contentDimensions
  };
  const onWheel = useCallback35((e) => {
    const ev = e;
    if (!editorZoomGestures) {
      return;
    }
    if (!size2) {
      return;
    }
    if (!contentDimensions || contentDimensions === "none") {
      return;
    }
    const wantsToZoom = ev.ctrlKey || ev.metaKey;
    if (!wantsToZoom && isFit) {
      return;
    }
    if (suppressWheelFromWebKitPinchRef.current && wantsToZoom) {
      ev.preventDefault();
      return;
    }
    ev.preventDefault();
    setSize((prevSize) => {
      const scale = calculateCanvasScale({
        addFitPadding,
        canvasSize: size2,
        compositionHeight: contentDimensions.height,
        compositionWidth: contentDimensions.width,
        previewSize: prevSize.size
      });
      if (wantsToZoom) {
        const oldSize = prevSize.size === "auto" ? scale : prevSize.size;
        const smoothened = smoothenZoom(oldSize);
        const added = smoothened + ev.deltaY * ZOOM_PX_FACTOR;
        const unsmoothened = unsmoothenZoom(added);
        return applyZoomAroundFocalPoint({
          addFitPadding,
          canvasSize: size2,
          contentDimensions,
          previewSizeBefore: prevSize,
          oldNumericSize: oldSize,
          newNumericSize: unsmoothened,
          clientX: ev.clientX,
          clientY: ev.clientY
        });
      }
      const effectiveTranslation = getEffectiveTranslation({
        translation: prevSize.translation,
        canvasSize: size2,
        compositionHeight: contentDimensions.height,
        compositionWidth: contentDimensions.width,
        scale
      });
      return {
        ...prevSize,
        translation: getEffectiveTranslation({
          translation: {
            x: effectiveTranslation.x + ev.deltaX,
            y: effectiveTranslation.y + ev.deltaY
          },
          canvasSize: size2,
          compositionHeight: contentDimensions.height,
          compositionWidth: contentDimensions.width,
          scale
        })
      };
    });
  }, [
    addFitPadding,
    editorZoomGestures,
    contentDimensions,
    isFit,
    setSize,
    size2
  ]);
  useEffect29(() => {
    const { current } = canvasRef;
    if (!current) {
      return;
    }
    current.addEventListener("wheel", onWheel, { passive: false });
    return () => {
      current.removeEventListener("wheel", onWheel);
    };
  }, [onWheel]);
  const supportsWebKitPinch = typeof window !== "undefined" && "GestureEvent" in window;
  useEffect29(() => {
    const { current } = canvasRef;
    if (!current || !editorZoomGestures || !supportsWebKitPinch) {
      return;
    }
    const endWebKitPinch = () => {
      pinchBaseZoomRef.current = null;
      suppressWheelFromWebKitPinchRef.current = false;
    };
    const onGestureStart = (event) => {
      const e = event;
      const snap = previewSnapshotRef.current;
      const canvasSz = snap.canvasSize;
      const cdim = snap.contentDimensions;
      if (!canvasSz || !cdim || cdim === "none") {
        return;
      }
      e.preventDefault();
      suppressWheelFromWebKitPinchRef.current = true;
      const fitted = calculateCanvasScale({
        addFitPadding,
        canvasSize: canvasSz,
        compositionHeight: cdim.height,
        compositionWidth: cdim.width,
        previewSize: snap.previewSize.size
      });
      pinchBaseZoomRef.current = snap.previewSize.size === "auto" ? fitted : snap.previewSize.size;
    };
    const onGestureChange = (event) => {
      const e = event;
      const base = pinchBaseZoomRef.current;
      const snap = previewSnapshotRef.current;
      const canvasSz = snap.canvasSize;
      const cdim = snap.contentDimensions;
      if (base === null || !canvasSz || !cdim || cdim === "none") {
        return;
      }
      const dimensions = cdim;
      e.preventDefault();
      setSize((prevSize) => {
        const scale = calculateCanvasScale({
          addFitPadding,
          canvasSize: canvasSz,
          compositionHeight: dimensions.height,
          compositionWidth: dimensions.width,
          previewSize: prevSize.size
        });
        const oldNumeric = prevSize.size === "auto" ? scale : prevSize.size;
        return applyZoomAroundFocalPoint({
          addFitPadding,
          canvasSize: canvasSz,
          contentDimensions: dimensions,
          previewSizeBefore: prevSize,
          oldNumericSize: oldNumeric,
          newNumericSize: base * e.scale,
          clientX: e.clientX,
          clientY: e.clientY
        });
      });
    };
    const onGestureEnd = () => {
      endWebKitPinch();
    };
    current.addEventListener("gesturestart", onGestureStart, {
      passive: false
    });
    current.addEventListener("gesturechange", onGestureChange, {
      passive: false
    });
    current.addEventListener("gestureend", onGestureEnd);
    current.addEventListener("gesturecancel", onGestureEnd);
    return () => {
      current.removeEventListener("gesturestart", onGestureStart);
      current.removeEventListener("gesturechange", onGestureChange);
      current.removeEventListener("gestureend", onGestureEnd);
      current.removeEventListener("gesturecancel", onGestureEnd);
    };
  }, [addFitPadding, editorZoomGestures, setSize, supportsWebKitPinch]);
  useEffect29(() => {
    const { current } = canvasRef;
    if (!current || !editorZoomGestures) {
      return;
    }
    const onTouchStart = (event) => {
      if (event.touches.length !== 2) {
        touchPinchRef.current = null;
        return;
      }
      const snap = previewSnapshotRef.current;
      if (!snap.canvasSize || !snap.contentDimensions || snap.contentDimensions === "none") {
        return;
      }
      const [t0, t1] = [event.touches[0], event.touches[1]];
      const initialDistance = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY);
      if (initialDistance < 0.000001) {
        return;
      }
      const fitted = calculateCanvasScale({
        addFitPadding,
        canvasSize: snap.canvasSize,
        compositionHeight: snap.contentDimensions.height,
        compositionWidth: snap.contentDimensions.width,
        previewSize: snap.previewSize.size
      });
      const initialZoom = snap.previewSize.size === "auto" ? fitted : snap.previewSize.size;
      touchPinchRef.current = { initialDistance, initialZoom };
    };
    const onTouchMove = (event) => {
      const pinch = touchPinchRef.current;
      const snap = previewSnapshotRef.current;
      if (pinch === null || event.touches.length !== 2 || !snap.canvasSize || !snap.contentDimensions || snap.contentDimensions === "none") {
        return;
      }
      event.preventDefault();
      const [t0, t1] = [event.touches[0], event.touches[1]];
      const dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY);
      const ratio = dist / pinch.initialDistance;
      const clientX = (t0.clientX + t1.clientX) / 2;
      const clientY = (t0.clientY + t1.clientY) / 2;
      setSize((prevSize) => {
        const canvasSz = snap.canvasSize;
        const cdim = snap.contentDimensions;
        const scale = calculateCanvasScale({
          addFitPadding,
          canvasSize: canvasSz,
          compositionHeight: cdim.height,
          compositionWidth: cdim.width,
          previewSize: prevSize.size
        });
        const oldNumeric = prevSize.size === "auto" ? scale : prevSize.size;
        return applyZoomAroundFocalPoint({
          addFitPadding,
          canvasSize: canvasSz,
          contentDimensions: cdim,
          previewSizeBefore: prevSize,
          oldNumericSize: oldNumeric,
          newNumericSize: pinch.initialZoom * ratio,
          clientX,
          clientY
        });
      });
    };
    const onTouchEnd = (event) => {
      if (event.touches.length < 2) {
        touchPinchRef.current = null;
      }
    };
    current.addEventListener("touchstart", onTouchStart, { passive: true });
    current.addEventListener("touchmove", onTouchMove, { passive: false });
    current.addEventListener("touchend", onTouchEnd);
    current.addEventListener("touchcancel", onTouchEnd);
    return () => {
      current.removeEventListener("touchstart", onTouchStart);
      current.removeEventListener("touchmove", onTouchMove);
      current.removeEventListener("touchend", onTouchEnd);
      current.removeEventListener("touchcancel", onTouchEnd);
    };
  }, [addFitPadding, editorZoomGestures, setSize]);
  const onReset = useCallback35(() => {
    setSize(() => {
      return {
        translation: {
          x: 0,
          y: 0
        },
        size: "auto"
      };
    });
  }, [setSize]);
  const onZoomIn = useCallback35(() => {
    if (!contentDimensions || contentDimensions === "none") {
      return;
    }
    if (!size2) {
      return;
    }
    setSize((prevSize) => {
      const scale = calculateCanvasScale({
        addFitPadding,
        canvasSize: size2,
        compositionHeight: contentDimensions.height,
        compositionWidth: contentDimensions.width,
        previewSize: prevSize.size
      });
      return {
        translation: {
          x: 0,
          y: 0
        },
        size: Math.min(MAX_ZOOM, scale * 2)
      };
    });
  }, [addFitPadding, contentDimensions, setSize, size2]);
  const onZoomOut = useCallback35(() => {
    if (!contentDimensions || contentDimensions === "none") {
      return;
    }
    if (!size2) {
      return;
    }
    setSize((prevSize) => {
      const scale = calculateCanvasScale({
        addFitPadding,
        canvasSize: size2,
        compositionHeight: contentDimensions.height,
        compositionWidth: contentDimensions.width,
        previewSize: prevSize.size
      });
      return {
        translation: {
          x: 0,
          y: 0
        },
        size: Math.max(MIN_ZOOM, scale / 2)
      };
    });
  }, [addFitPadding, contentDimensions, setSize, size2]);
  useEffect29(() => {
    const resetBinding = keybindings.registerKeybinding({
      event: "keydown",
      action: "resetZoom",
      callback: onReset,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const zoomIn = keybindings.registerKeybinding({
      event: "keydown",
      action: "zoomIn",
      callback: onZoomIn,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const zoomOut = keybindings.registerKeybinding({
      event: "keydown",
      action: "zoomOut",
      callback: onZoomOut,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      resetBinding.unregister();
      zoomIn.unregister();
      zoomOut.unregister();
    };
  }, [keybindings, onReset, onZoomIn, onZoomOut]);
  const fetchMetadata = useCallback35(async () => {
    const request = ++metadataRequestRef.current;
    setAssetResolution(null);
    if (canvasContent.type === "composition") {
      setCurrentAssetMetadata(null);
      return;
    }
    const metadata = await getAssetMetadata(canvasContent, canvasContent.type === "asset");
    if (request !== metadataRequestRef.current) {
      return;
    }
    setAssetResolution(metadata);
    setCurrentAssetMetadata(getAssetPreviewMetadata({ canvasContent, metadata }));
  }, [canvasContent, setCurrentAssetMetadata]);
  useEffect29(() => {
    if (canvasContent.type !== "asset") {
      return;
    }
    const file = watchStaticFile(canvasContent.asset, () => {
      fetchMetadata();
    });
    return () => {
      file.cancel();
    };
  }, [canvasContent, fetchMetadata]);
  useEffect29(() => {
    fetchMetadata();
  }, [fetchMetadata]);
  const updateElementInstallTarget = useCallback35((requestId) => {
    if (previewServerClientId === null) {
      return;
    }
    callApi("/api/update-element-install-target", {
      requestId,
      clientId: previewServerClientId,
      compositionFile: canReceiveElementInstallRequest ? compositionFile : null,
      compositionId: canReceiveElementInstallRequest ? currentCompositionId : null,
      lastFocusedAt: lastFocusedAtRef.current,
      readOnly: window.remotion_isReadOnlyStudio,
      studioUrl: window.location.href
    }).catch(() => {
      return;
    });
  }, [
    canReceiveElementInstallRequest,
    compositionFile,
    currentCompositionId,
    previewServerClientId
  ]);
  useEffect29(() => {
    const markFocused = () => {
      lastFocusedAtRef.current = Date.now();
    };
    window.addEventListener("focus", markFocused);
    document.addEventListener("pointerdown", markFocused, { capture: true });
    return () => {
      window.removeEventListener("focus", markFocused);
      document.removeEventListener("pointerdown", markFocused, { capture: true });
    };
  }, []);
  useEffect29(() => {
    return subscribeToEvent("request-element-install-target", (event) => {
      if (event.type !== "request-element-install-target") {
        return;
      }
      updateElementInstallTarget(event.requestId);
    });
  }, [subscribeToEvent, updateElementInstallTarget]);
  useEffect29(() => {
    if (previewServerClientId === null) {
      return;
    }
    return subscribeToEvent("element-install-request", (event) => {
      if (event.type !== "element-install-request" || event.request.clientId !== previewServerClientId) {
        return;
      }
      enqueueElementInstallRequest(event.request);
    });
  }, [previewServerClientId, subscribeToEvent]);
  useEffect29(() => {
    const onMessage = (event) => {
      const elementLibrary = document.querySelector("iframe[data-remotion-element-library]");
      if (event.source !== elementLibrary?.contentWindow || !StudioProtocolInternals.isAllowedStudioProtocolPageOrigin(event.origin)) {
        return;
      }
      const payload = StudioProtocolInternals.parseStudioProtocolIframeInstallRequest(event.data);
      const responsePort = event.ports[0];
      if (payload === null || responsePort === undefined) {
        return;
      }
      const canInstall = canReceiveElementInstallRequest && compositionFile !== null && currentCompositionId !== null && previewServerClientId !== null;
      const request = canInstall ? {
        clientId: previewServerClientId,
        compositionFile,
        compositionId: currentCompositionId,
        createdAt: Date.now(),
        element: {
          ...payload.element,
          durationInFrames: payload.element.durationInFrames ?? null,
          initialProps: payload.element.initialProps ?? null,
          installationMode: payload.element.installationMode ?? null
        },
        from: null,
        id: crypto.randomUUID(),
        position: null,
        source: { origin: event.origin, type: "studio-protocol" }
      } : null;
      const result = canInstall ? {
        success: true,
        status: "awaiting-confirmation",
        target: {
          compositionId: currentCompositionId,
          projectName: window.remotion_projectName,
          studioOrigin: window.location.origin,
          studioVersion: window.remotion_version
        }
      } : {
        success: false,
        code: "no-installable-target",
        message: "Focus a composition in a Remotion Studio that is not read-only, then try again."
      };
      const timeout = window.setTimeout(() => responsePort.close(), 1000);
      responsePort.onmessage = () => {
        window.clearTimeout(timeout);
        responsePort.close();
        if (request !== null) {
          enqueueElementInstallRequest(request);
        }
      };
      responsePort.postMessage(result);
    };
    window.addEventListener("message", onMessage);
    return () => window.removeEventListener("message", onMessage);
  }, [
    canReceiveElementInstallRequest,
    compositionFile,
    currentCompositionId,
    previewServerClientId
  ]);
  useEffect29(() => {
    return subscribeToElementInstallRequests((request) => {
      const isDragAndDrop = request.source.type === "drag-and-drop";
      const requestWithDefaults = isDragAndDrop ? request : {
        ...request,
        from: request.from ?? getFromForDrop({
          durationInFrames: request.element.durationInFrames,
          from: getCurrentFrame(),
          preferCompositionStart: true
        }),
        position: request.position ?? getElementPositionForDrop({
          dimensions: request.element.dimensions,
          dropPosition: contentDimensions === null || contentDimensions === "none" ? null : {
            centerX: contentDimensions.width / 2,
            centerY: contentDimensions.height / 2
          }
        })
      };
      setPendingElementInstallRequests((requests) => [
        ...requests,
        requestWithDefaults
      ]);
    });
  }, [contentDimensions]);
  useEffect29(() => {
    if (!canReceiveElementInstallRequest || compositionFile === null || currentCompositionId === null) {
      return;
    }
    const initialElement = getBrowserStudioOperations()?.consumeInitialElement() ?? null;
    if (initialElement === null) {
      return;
    }
    enqueueElementInstallRequest({
      clientId: "browser-studio",
      compositionFile,
      compositionId: currentCompositionId,
      createdAt: Date.now(),
      element: initialElement.element,
      from: null,
      id: crypto.randomUUID(),
      position: null,
      source: {
        origin: initialElement.sourceOrigin,
        type: "browser-studio-link"
      }
    });
  }, [canReceiveElementInstallRequest, compositionFile, currentCompositionId]);
  useEffect29(() => {
    if (activeElementInstallRequest !== null || pendingElementInstallRequests.length === 0) {
      return;
    }
    const [nextRequest, ...remainingRequests] = pendingElementInstallRequests;
    if (!nextRequest) {
      throw new Error("Expected pending Element install request");
    }
    setActiveElementInstallRequest(nextRequest);
    setPendingElementInstallRequests(remainingRequests);
  }, [activeElementInstallRequest, pendingElementInstallRequests]);
  const closeElementInstallDialog = useCallback35(() => {
    setSelectedModal(null);
    setActiveElementInstallRequest(null);
  }, [setSelectedModal]);
  useEffect29(() => {
    if (activeElementInstallRequest === null) {
      return;
    }
    let canceled = false;
    const handleInstallRequest = async () => {
      try {
        const [newPreflight, currentPreflight] = await Promise.all([
          prepareElementInstall({
            installationName: null,
            destination: {
              type: "new-composition",
              compositionFile: null
            },
            element: activeElementInstallRequest.element
          }),
          prepareElementInstall({
            installationName: null,
            destination: {
              type: "current-composition",
              compositionFile: activeElementInstallRequest.compositionFile,
              compositionId: activeElementInstallRequest.compositionId
            },
            element: activeElementInstallRequest.element
          })
        ]);
        if (canceled) {
          return;
        }
        if (!newPreflight.success) {
          showNotification(`Could not review Element installation: ${newPreflight.reason}`, 4000);
          setActiveElementInstallRequest(null);
          return;
        }
        const declaredDependencies = Array.from(new Map(activeElementInstallRequest.element.dependencies.map((dependency) => [dependency.name, dependency])).values());
        const missingPackages = getMissingPackages(declaredDependencies).map((dependency) => dependency.version === null ? dependency.name : `${dependency.name}@${dependency.version}`);
        const { source } = activeElementInstallRequest;
        const sourceLabel = source.type === "studio-protocol" ? source.origin : source.type === "browser-studio-link" ? source.origin ?? "Unverified Browser Studio link" : "Unverified drag-and-drop payload";
        const sourceIsUnverified = source.type === "drag-and-drop" || source.type === "browser-studio-link" && source.origin === null;
        const currentPlan = currentPreflight.success ? currentPreflight.plan : null;
        setSelectedModal({
          type: "element-install",
          currentPlan,
          missingPackages,
          newPlan: newPreflight.plan,
          onClose: closeElementInstallDialog,
          request: activeElementInstallRequest,
          sourceIsUnverified,
          sourceLabel
        });
      } catch (error) {
        if (canceled) {
          return;
        }
        showNotification(`Could not review Element installation: ${error instanceof Error ? error.message : String(error)}`, 4000);
        setActiveElementInstallRequest(null);
      }
    };
    handleInstallRequest();
    return () => {
      canceled = true;
    };
  }, [
    activeElementInstallRequest,
    closeElementInstallDialog,
    setSelectedModal
  ]);
  const onDragOver = useCallback35((event) => {
    if (!isSupportedDropEvent(event) || !isDragEventInsideCanvas(event)) {
      setCompositionDropPreview(null);
      return;
    }
    const mayBeCanvasCapture = isFileDragEvent(event) && !window.remotion_isReadOnlyStudio;
    const canDropElementIntoNewComposition = cannotAddSequence && canReceiveElementInstallRequest && !isAddingAsset && hasElementDragType(event.dataTransfer);
    if (!canDropAssets && !cannotAddSequence && !mayBeCanvasCapture) {
      setCompositionDropPreview(null);
      return;
    }
    event.preventDefault();
    if (event.dataTransfer) {
      event.dataTransfer.dropEffect = canDropAssets || canDropElementIntoNewComposition || mayBeCanvasCapture ? "copy" : "none";
    }
    if (!canDropAssets || contentDimensions === null || contentDimensions === "none") {
      setCompositionDropPreview(null);
      return;
    }
    const metadata = getCanvasDragPreviewMetadata(event.dataTransfer?.types ?? []);
    if (metadata?.type !== "composition" && metadata?.type !== "element" || metadata.width === undefined || metadata.height === undefined) {
      setCompositionDropPreview(null);
      return;
    }
    let dropPosition = getDropPosition({
      addFitPadding,
      clientX: event.clientX,
      clientY: event.clientY,
      contentDimensions,
      unbounded: metadata.type === "composition",
      previewSize,
      size: size2
    });
    if (dropPosition === null) {
      setCompositionDropPreview(null);
      return;
    }
    const compositionDimensions = {
      width: metadata.width,
      height: metadata.height
    };
    if (metadata.type === "composition" && editorSnapping && !event.metaKey && !event.ctrlKey) {
      dropPosition = snapCompositionDropPosition({
        compositionDimensions,
        destinationDimensions: contentDimensions,
        dropPosition,
        scale: calculateCanvasScale({
          addFitPadding,
          canvasSize: size2,
          compositionHeight: contentDimensions.height,
          compositionWidth: contentDimensions.width,
          previewSize: previewSize.size
        })
      });
    }
    setCompositionDropPreview((currentPreview) => {
      if (currentPreview?.compositionDimensions.width === compositionDimensions.width && currentPreview.compositionDimensions.height === compositionDimensions.height && currentPreview.dropPosition.centerX === dropPosition.centerX && currentPreview.dropPosition.centerY === dropPosition.centerY) {
        return currentPreview;
      }
      return {
        compositionDimensions,
        dropPosition
      };
    });
  }, [
    addFitPadding,
    canDropAssets,
    cannotAddSequence,
    canReceiveElementInstallRequest,
    contentDimensions,
    editorSnapping,
    isAddingAsset,
    previewSize,
    size2
  ]);
  const onDragLeave = useCallback35((event) => {
    const canvas = canvasRef.current;
    if (canvas !== null) {
      const rect = canvas.getBoundingClientRect();
      if (event.clientX >= rect.left && event.clientX <= rect.right && event.clientY >= rect.top && event.clientY <= rect.bottom || event.relatedTarget instanceof Node && canvas.contains(event.relatedTarget)) {
        return;
      }
    }
    setCompositionDropPreview(null);
  }, []);
  const onDragEnd = useCallback35(() => {
    setCompositionDropPreview(null);
  }, []);
  const onDrop = useCallback35(async (event) => {
    setCompositionDropPreview(null);
    if (!isSupportedDropEvent(event) || !isDragEventInsideCanvas(event)) {
      return;
    }
    const localFiles = isFileDragEvent(event) ? Array.from(event.dataTransfer?.files ?? []) : null;
    if (localFiles !== null && !window.remotion_isReadOnlyStudio) {
      event.preventDefault();
      event.stopPropagation();
      if (localFiles.length === 1) {
        setIsAddingAsset(true);
        try {
          if (await handleCanvasCaptureDrop({
            files: localFiles,
            setSelectedModal
          })) {
            return;
          }
        } finally {
          setIsAddingAsset(false);
        }
      }
    }
    const canDropElementIntoNewComposition = cannotAddSequence && canReceiveElementInstallRequest && !isAddingAsset && getElementDragData(event.dataTransfer) !== null;
    if (cannotAddSequence && !canDropElementIntoNewComposition) {
      event.preventDefault();
      event.stopPropagation();
      showNotification("Cannot insert this item: This composition component cannot accept a new <Sequence>.", 3000);
      return;
    }
    if (!canDropAssets && !canDropElementIntoNewComposition || compositionFile === null || currentCompositionId === null || config === null) {
      return;
    }
    if (event.dataTransfer && getEffectDragData(event.dataTransfer) !== null) {
      event.preventDefault();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    setIsAddingAsset(true);
    try {
      const metadata = getCanvasDragPreviewMetadata(event.dataTransfer?.types ?? []);
      const isComposition = metadata?.type === "composition";
      let dropPosition = getDropPosition({
        addFitPadding,
        clientX: event.clientX,
        clientY: event.clientY,
        contentDimensions,
        unbounded: isComposition,
        previewSize,
        size: size2
      });
      if (dropPosition !== null && isComposition && metadata.width !== undefined && metadata.height !== undefined && contentDimensions !== null && contentDimensions !== "none" && editorSnapping && !event.metaKey && !event.ctrlKey) {
        dropPosition = snapCompositionDropPosition({
          compositionDimensions: {
            width: metadata.width,
            height: metadata.height
          },
          destinationDimensions: contentDimensions,
          dropPosition,
          scale: calculateCanvasScale({
            addFitPadding,
            canvasSize: size2,
            compositionHeight: contentDimensions.height,
            compositionWidth: contentDimensions.width,
            previewSize: previewSize.size
          })
        });
      }
      await handleDrop({
        chooseSvgImportMode,
        compositionFile,
        compositionId: currentCompositionId,
        destinationDimensions: contentDimensions === "none" ? null : contentDimensions,
        dropPosition,
        event,
        fps: config.fps,
        from: getCurrentFrame(),
        localFiles,
        preferCompositionStart: true
      });
    } finally {
      setIsAddingAsset(false);
    }
  }, [
    addFitPadding,
    canDropAssets,
    cannotAddSequence,
    canReceiveElementInstallRequest,
    chooseSvgImportMode,
    compositionFile,
    config,
    contentDimensions,
    currentCompositionId,
    editorSnapping,
    isAddingAsset,
    previewSize,
    size2,
    setSelectedModal
  ]);
  const onPaste = useCallback35(async (event) => {
    const { activeElement } = document;
    if (!canDropAssets || compositionFile === null || currentCompositionId === null || config === null || event.clipboardData === null || activeElement instanceof HTMLInputElement || activeElement instanceof HTMLTextAreaElement || activeElement instanceof HTMLElement && activeElement.isContentEditable) {
      return;
    }
    const dropPosition = contentDimensions === null || contentDimensions === "none" ? null : {
      centerX: contentDimensions.width / 2,
      centerY: contentDimensions.height / 2
    };
    const figmaHtml = getClipboardFigmaHtml(event.clipboardData);
    if (figmaHtml !== null) {
      event.preventDefault();
      setIsAddingAsset(true);
      try {
        await importFigmaClipboard({
          compositionFile,
          compositionId: currentCompositionId,
          destinationDimensions: contentDimensions === "none" ? null : contentDimensions,
          dropPosition,
          html: figmaHtml
        });
      } finally {
        setIsAddingAsset(false);
      }
      return;
    }
    const svgMarkup = getClipboardSvgMarkup(event.clipboardData);
    if (svgMarkup !== null) {
      event.preventDefault();
      setIsAddingAsset(true);
      try {
        await insertSvgMarkup({
          compositionFile,
          compositionId: currentCompositionId,
          destinationDimensions: contentDimensions === "none" ? null : contentDimensions,
          dropPosition,
          markup: svgMarkup
        });
      } finally {
        setIsAddingAsset(false);
      }
      return;
    }
    const files = getClipboardImageFiles({
      clipboardData: event.clipboardData,
      existingFileNames: getStaticFiles().map((file) => file.name)
    });
    if (files.length === 0) {
      return;
    }
    event.preventDefault();
    const svgImportMode = hasSvgFile(files) ? await chooseSvgImportMode() : "image";
    if (svgImportMode === null) {
      return;
    }
    setIsAddingAsset(true);
    try {
      await importAssets({
        files,
        fps: config.fps,
        compositionFile,
        compositionId: currentCompositionId,
        destinationDimensions: contentDimensions === "none" ? null : contentDimensions,
        dropPosition,
        from: null,
        preferCompositionStart: null,
        svgImportMode
      });
    } finally {
      setIsAddingAsset(false);
    }
  }, [
    canDropAssets,
    chooseSvgImportMode,
    compositionFile,
    config,
    contentDimensions,
    currentCompositionId
  ]);
  useEffect29(() => {
    document.addEventListener("dragover", onDragOver, { capture: true });
    document.addEventListener("dragleave", onDragLeave, { capture: true });
    document.addEventListener("dragend", onDragEnd, { capture: true });
    document.addEventListener("drop", onDrop, { capture: true });
    return () => {
      document.removeEventListener("dragover", onDragOver, { capture: true });
      document.removeEventListener("dragleave", onDragLeave, { capture: true });
      document.removeEventListener("dragend", onDragEnd, { capture: true });
      document.removeEventListener("drop", onDrop, { capture: true });
    };
  }, [onDragEnd, onDragLeave, onDragOver, onDrop]);
  useEffect29(() => {
    if (!canDropAssets || !keybindings.isHighestContext || areKeyboardShortcutsDisabled()) {
      return;
    }
    document.addEventListener("paste", onPaste);
    return () => document.removeEventListener("paste", onPaste);
  }, [canDropAssets, keybindings.isHighestContext, onPaste]);
  const compositionDropPreviewStyle = useMemo41(() => {
    if (compositionDropPreview === null || contentDimensions === null || contentDimensions === "none") {
      return null;
    }
    const box = getCompositionDropPreviewBox({
      canvasSize: size2,
      destinationDimensions: contentDimensions,
      preview: compositionDropPreview,
      previewSize
    });
    return {
      ...box,
      position: "absolute",
      boxSizing: "border-box",
      border: BORDER_TIMELINE_DROP_BLUE,
      backgroundColor: TIMELINE_DROP_BLUE_ALPHA_16,
      pointerEvents: "none",
      zIndex: 1
    };
  }, [compositionDropPreview, contentDimensions, previewSize, size2]);
  return /* @__PURE__ */ jsxs42(Fragment21, {
    children: [
      /* @__PURE__ */ jsxs42("div", {
        ref: canvasRef,
        style: getContainerStyle(editorZoomGestures),
        children: [
          size2 ? /* @__PURE__ */ jsx75(VideoPreview, {
            canvasContent,
            contentDimensions,
            canvasSize: size2,
            assetMetadata: assetResolution,
            onRetryAssetMetadata: fetchMetadata
          }) : null,
          compositionDropPreviewStyle === null ? null : /* @__PURE__ */ jsx75("div", {
            className: "css-reset",
            "data-testid": "composition-drop-preview",
            style: compositionDropPreviewStyle
          }),
          isFit ? null : /* @__PURE__ */ jsx75("div", {
            style: resetZoom,
            className: "css-reset",
            children: /* @__PURE__ */ jsx75(ResetZoomButton, {
              onClick: onReset
            })
          }),
          editorShowGuides && canvasContent.type === "composition" && /* @__PURE__ */ jsx75(EditorGuides_default, {
            canvasSize: size2,
            contentDimensions,
            assetMetadata: assetResolution
          })
        ]
      }),
      areRulersVisible && /* @__PURE__ */ jsx75(EditorRulers, {
        canCreateGuides: canvasContent.type === "composition",
        contentDimensions,
        canvasSize: size2,
        assetMetadata: assetResolution,
        containerRef: canvasRef
      })
    ]
  });
};

// src/components/FramePersistor.tsx
import { useEffect as useEffect30 } from "react";
import { Internals as Internals40, useVideoConfig as useVideoConfig6 } from "remotion";
var FramePersistor = () => {
  const playing = Internals40.usePlaying();
  const config = useVideoConfig6();
  const frame2 = Internals40.Timeline.useTimelinePosition();
  const setFrame = Internals40.useTimelineSetFrame();
  useEffect30(() => {
    if (!playing) {
      setFrame((f) => {
        const newObj = f[config.id] === frame2 ? f : { ...f, [config.id]: frame2 };
        Internals40.persistCurrentFrame(newObj);
        return newObj;
      });
    }
  }, [config.id, frame2, playing, setFrame]);
  return null;
};

// src/components/RefreshCompositionOverlay.tsx
import { Internals as Internals41 } from "remotion";

// src/components/RunningCalculateMetadata.tsx
import { jsx as jsx76, jsxs as jsxs43 } from "react/jsx-runtime";
var loaderLabel = {
  fontSize: 14,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  lineHeight: 1.5
};
var container10 = {
  backgroundColor: BACKGROUND,
  display: "inline-flex",
  justifyContent: "center",
  alignItems: "center",
  flexDirection: "row",
  padding: 20
};
var RunningCalculateMetadata = () => {
  return /* @__PURE__ */ jsxs43("div", {
    style: container10,
    children: [
      /* @__PURE__ */ jsx76(Spinner, {
        size: 24,
        duration: 1
      }),
      /* @__PURE__ */ jsx76(Spacing, {
        x: 2
      }),
      /* @__PURE__ */ jsxs43("div", {
        style: loaderLabel,
        children: [
          "Running ",
          /* @__PURE__ */ jsx76("code", {
            style: inlineCodeSnippet,
            children: "calculateMetadata()"
          }),
          "..."
        ]
      })
    ]
  });
};

// src/components/RefreshCompositionOverlay.tsx
import { jsx as jsx77 } from "react/jsx-runtime";
var container11 = {
  justifyContent: "flex-end",
  alignItems: "flex-start",
  padding: 20,
  pointerEvents: "none"
};
var shadow = {
  boxShadow: SHADOW_BLACK
};
var RefreshCompositionOverlay = () => {
  return /* @__PURE__ */ jsx77(Internals41.AbsoluteFillElement, {
    style: container11,
    children: /* @__PURE__ */ jsx77("div", {
      style: shadow,
      children: /* @__PURE__ */ jsx77(RunningCalculateMetadata, {})
    })
  });
};

// src/components/RenderErrorContext.ts
import { createContext as createContext10 } from "react";
var RenderErrorContext = createContext10({
  error: null
});

// src/components/CanvasOrLoading.tsx
import { jsx as jsx78, jsxs as jsxs44, Fragment as Fragment22 } from "react/jsx-runtime";
var container12 = {
  color: WHITE,
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
  display: "flex",
  backgroundColor: BACKGROUND,
  flexDirection: "column"
};
var CanvasOrLoading = ({ size: size2 }) => {
  const resolved = Internals43.useResolvedVideoConfig(null);
  const { setZoom } = useContext41(TimelineZoomCtx);
  const { canvasContent } = useContext41(Internals43.CompositionManager);
  const { compositionListState } = useContext41(CompositionListContext);
  const { error: renderError } = useContext41(RenderErrorContext);
  useEffect31(() => {
    if (resolved?.type !== "success" && resolved?.type !== "success-and-refreshing") {
      return;
    }
    const c = resolved.result;
    setTimeout(() => {
      ensureFrameIsInViewport({
        direction: "center",
        frame: getCurrentFrame(),
        durationInFrames: c.durationInFrames
      });
    });
  }, [resolved, setZoom]);
  if (renderError) {
    return /* @__PURE__ */ jsx78(ErrorLoading, {
      error: renderError,
      calculateMetadataContext: false,
      onRetry: null
    });
  }
  if (!canvasContent) {
    if (compositionListState !== "ready") {
      return null;
    }
    const compname = window.location.pathname.replace("/", "");
    return /* @__PURE__ */ jsx78("div", {
      style: container12,
      className: "css-reset",
      children: /* @__PURE__ */ jsxs44("div", {
        style: loaderLabel,
        children: [
          "Composition with ID ",
          compname,
          " not found."
        ]
      })
    });
  }
  const content2 = /* @__PURE__ */ jsxs44(Fragment22, {
    children: [
      /* @__PURE__ */ jsx78(ZoomPersistor, {}),
      /* @__PURE__ */ jsx78(Canvas, {
        size: size2,
        canvasContent
      }),
      resolved?.type === "success-and-refreshing" ? /* @__PURE__ */ jsx78(RefreshCompositionOverlay, {}) : null
    ]
  });
  if (canvasContent.type === "output" || canvasContent.type === "output-blob") {
    return content2;
  }
  if (canvasContent.type === "asset" && resolved === null) {
    return content2;
  }
  if (!resolved) {
    return null;
  }
  if (resolved.type === "loading") {
    return /* @__PURE__ */ jsx78("div", {
      style: container12,
      className: "css-reset",
      children: /* @__PURE__ */ jsx78(RunningCalculateMetadata, {})
    });
  }
  if (resolved.type === "error") {
    return /* @__PURE__ */ jsx78(ErrorLoading, {
      error: resolved.error,
      calculateMetadataContext: true,
      onRetry: () => Internals43.resolveCompositionsRef.current?.reloadCurrentlySelectedComposition()
    });
  }
  return /* @__PURE__ */ jsxs44(Fragment22, {
    children: [
      /* @__PURE__ */ jsx78(FramePersistor, {}),
      " ",
      content2
    ]
  });
};
var loaderContainer = {
  marginLeft: "auto",
  marginRight: "auto",
  width: "100%",
  position: "absolute",
  height: "100%",
  overflowY: "auto"
};
var ErrorLoading = ({ error, calculateMetadataContext, onRetry }) => {
  return /* @__PURE__ */ jsx78("div", {
    style: loaderContainer,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: /* @__PURE__ */ jsx78(ErrorLoader, {
      canHaveDismissButton: false,
      keyboardShortcuts: true,
      error,
      onRetry,
      calculateMetadata: calculateMetadataContext
    }, error.stack)
  });
};

// src/components/Splitter/SplitterContext.tsx
import React53 from "react";
var getSplitterFlexBounds = ({
  availableSize,
  maxAntiFlexerSize,
  maxFlex,
  maxFlexerSize,
  minAntiFlexerSize,
  minFlex,
  minFlexerSize
}) => {
  if (availableSize === null || availableSize <= 0) {
    return { minFlex, maxFlex };
  }
  const minimum = Math.min(1, Math.max(minFlex, minFlexerSize === null ? 0 : minFlexerSize / availableSize, maxAntiFlexerSize === null ? 0 : 1 - maxAntiFlexerSize / availableSize));
  const maximum = Math.max(minimum, Math.min(maxFlex, maxFlexerSize === null ? 1 : maxFlexerSize / availableSize, minAntiFlexerSize === null ? 1 : 1 - minAntiFlexerSize / availableSize));
  return { minFlex: minimum, maxFlex: maximum };
};
var getClampedSplitterFlex = ({
  flexValue,
  ...bounds
}) => {
  const { minFlex, maxFlex } = getSplitterFlexBounds(bounds);
  return Math.min(maxFlex, Math.max(minFlex, flexValue));
};
var SplitterContext = React53.createContext({
  flexValue: 1,
  collapsedDuringDrag: null,
  setCollapsedDuringDrag: () => {
    return;
  },
  ref: { current: null },
  setFlexValue: () => {
    return;
  },
  isDragging: { current: false },
  orientation: "horizontal",
  maxFlex: 1,
  minFlex: 1,
  maxFlexerSize: null,
  minFlexerSize: null,
  maxAntiFlexerSize: null,
  minAntiFlexerSize: null,
  defaultFlex: 1,
  id: "--",
  persistFlex: () => {
    return;
  }
});
var SplitterLayoutContext = React53.createContext(null);

// src/components/CanvasIfSizeIsAvailable.tsx
import { jsx as jsx79 } from "react/jsx-runtime";
var CanvasIfSizeIsAvailable = () => {
  const layout = useContext43(SplitterLayoutContext);
  const refreshCanvas = useContext43(RefreshCanvasSizeContext);
  useLayoutEffect13(() => {
    refreshCanvas?.();
  }, [layout, refreshCanvas]);
  const rulersAreVisible = useIsRulerVisible();
  const context = useContext43(Internals45.CurrentScaleContext);
  const sizeWithRulersApplied = useMemo43(() => {
    const size2 = context && context.type === "canvas-size" ? context.canvasSize : null;
    if (!rulersAreVisible) {
      return size2;
    }
    if (!size2) {
      return null;
    }
    return applyRulerInsetsToCanvasSize({ rulersAreVisible, size: size2 });
  }, [context, rulersAreVisible]);
  if (!sizeWithRulersApplied) {
    return null;
  }
  return /* @__PURE__ */ jsx79(CanvasOrLoading, {
    size: sizeWithRulersApplied
  });
};

// src/components/CurrentCompositionSideEffects.tsx
import { useContext as useContext45, useEffect as useEffect32 } from "react";
import { Internals as Internals46 } from "remotion";

// src/helpers/document-title.ts
import { NoReactInternals as NoReactInternals8 } from "remotion/no-react";
var currentItemName = null;
var currentModal = null;
var renderJobs = [];
var setCurrentCanvasContentId = (id) => {
  if (!id) {
    currentItemName = id;
    updateTitle();
    return;
  }
  const idWithoutFolder = id.split("/").pop();
  currentItemName = idWithoutFolder;
  updateTitle();
};
var setRenderJobs = (jobs) => {
  renderJobs = jobs;
  updateTitle();
};
var setCurrentModal = (modal) => {
  currentModal = modal;
  updateTitle();
};
var productName = "Remotion Studio";
var suffix = `- ${productName}`;
var updateTitle = () => {
  if (currentModal?.type === "element-install") {
    getNavigationWindow().document.title = `\uD83D\uDCE6 Install ${currentModal.request.element.displayName} - ${productName}`;
    return;
  }
  if (!currentItemName) {
    getNavigationWindow().document.title = productName;
    return;
  }
  const currentCompTitle = `${currentItemName} / ${window.remotion_projectName}`;
  getNavigationWindow().document.title = [
    getProgressInBrackets(currentItemName, renderJobs),
    `${currentCompTitle} ${suffix}`
  ].filter(NoReactInternals8.truthy).join(" ");
};
var getProgressInBrackets = (selectedCompositionId, jobs) => {
  const currentRender = jobs.find((job) => job.status === "running");
  if (!currentRender) {
    return null;
  }
  if (currentRender.status !== "running") {
    throw new Error("expected running job");
  }
  let progInPercent;
  if (isClientRenderJob(currentRender)) {
    const { encodedFrames, totalFrames } = currentRender.progress;
    progInPercent = totalFrames > 0 ? Math.ceil(encodedFrames / totalFrames * 100) : 0;
  } else {
    progInPercent = Math.ceil(currentRender.progress.value * 100);
  }
  const progressInBrackets = currentRender.compositionId === selectedCompositionId ? `[${progInPercent}%]` : `[${progInPercent}% ${currentRender.compositionId}]`;
  return progressInBrackets;
};

// src/components/CurrentCompositionSideEffects.tsx
var TitleUpdater = () => {
  const renderQueue = useContext45(RenderQueueContext);
  const { canvasContent } = useContext45(Internals46.CompositionManager);
  const selectedModal = useContext45(SelectedModalContext);
  const { jobs } = renderQueue;
  useEffect32(() => {
    if (!canvasContent) {
      setCurrentCanvasContentId(null);
      return;
    }
    if (canvasContent.type === "composition") {
      setCurrentCanvasContentId(canvasContent.compositionId);
      return;
    }
    if (canvasContent.type === "output") {
      setCurrentCanvasContentId(canvasContent.path);
      return;
    }
    if (canvasContent.type === "output-blob") {
      setCurrentCanvasContentId(canvasContent.displayName);
      return;
    }
    setCurrentCanvasContentId(canvasContent.asset);
  }, [canvasContent]);
  useEffect32(() => {
    setRenderJobs(jobs);
  }, [jobs]);
  useEffect32(() => {
    setCurrentModal(selectedModal);
  }, [selectedModal]);
  return null;
};

// src/components/ExplorerPanel.tsx
import { useCallback as useCallback40, useImperativeHandle as useImperativeHandle5, useRef as useRef25, useState as useState28 } from "react";

// src/helpers/use-asset-drag-events.ts
import { useCallback as useCallback36, useEffect as useEffect33, useMemo as useMemo45, useRef as useRef21 } from "react";
import { NoReactInternals as NoReactInternals9 } from "remotion/no-react";
var isAssetUploadDragEvent = (event) => {
  return Array.from(event.dataTransfer.types).includes("Files") || StudioProtocolInternals.getDragPreviewMetadata(event.dataTransfer.types)?.type === "render-output";
};
var getRenderOutputDragData = (dataTransfer) => {
  const parsed = StudioProtocolInternals.parseDragData(dataTransfer);
  return parsed?.type === "render-output" ? parsed.data : null;
};
function useAssetDragEvents({
  name,
  parentFolder,
  dropLocation,
  setDropLocation
}) {
  const dragDepthRef = useRef21(0);
  const combinedParents = useMemo45(() => {
    return [parentFolder, name].filter(NoReactInternals9.truthy).join("/");
  }, [name, parentFolder]);
  const isDropDiv = useMemo45(() => {
    return dropLocation === combinedParents;
  }, [combinedParents, dropLocation]);
  const onDragEnter = useCallback36((event) => {
    if (!isAssetUploadDragEvent(event)) {
      return;
    }
    if (dragDepthRef.current === 0) {
      setDropLocation((currentDropLocation) => currentDropLocation?.includes(combinedParents) ? currentDropLocation : combinedParents);
    }
    dragDepthRef.current++;
  }, [combinedParents, dragDepthRef, setDropLocation]);
  const onDragLeave = useCallback36((event) => {
    if (!isAssetUploadDragEvent(event)) {
      return;
    }
    dragDepthRef.current--;
    if (dragDepthRef.current === 0) {
      setDropLocation((currentPath) => currentPath === combinedParents ? parentFolder : currentPath);
    }
  }, [combinedParents, dragDepthRef, parentFolder, setDropLocation]);
  useEffect33(() => {
    if (dropLocation === null) {
      dragDepthRef.current = 0;
    }
  }, [dropLocation]);
  return {
    isDropDiv,
    onDragEnter,
    onDragLeave
  };
}
var use_asset_drag_events_default = useAssetDragEvents;

// src/components/AssetSelector.tsx
import { useCallback as useCallback38, useContext as useContext47, useMemo as useMemo47, useState as useState27 } from "react";

// src/api/copy-render-output-to-asset.ts
var copyRenderOutputToAsset = ({
  outputPath,
  assetPath
}) => {
  return callApi("/api/copy-render-output-to-asset", {
    outputPath,
    assetPath
  });
};

// src/components/AssetSelectorItem.tsx
import {
  useCallback as useCallback37,
  useContext as useContext46,
  useLayoutEffect as useLayoutEffect14,
  useMemo as useMemo46,
  useRef as useRef23,
  useState as useState26
} from "react";
import { Internals as Internals47, staticFile as staticFile3 } from "remotion";
import { NoReactInternals as NoReactInternals10 } from "remotion/no-react";
import { jsx as jsx80, jsxs as jsxs45, Fragment as Fragment23 } from "react/jsx-runtime";
var iconStyle3 = {
  width: 18,
  height: 18,
  flexShrink: 0
};
var itemStyle2 = {
  paddingTop: 5,
  paddingBottom: 5,
  fontSize: 13,
  display: "flex",
  textDecoration: "none",
  cursor: "default",
  alignItems: "center",
  marginBottom: 1,
  marginLeft: 4,
  appearance: "none",
  border: "none",
  borderRadius: 4,
  width: "calc(100% - 4px)",
  textAlign: "left",
  height: COMPACT_CONTROL_ROW_HEIGHT,
  userSelect: "none",
  WebkitUserSelect: "none"
};
var labelStyle2 = {
  textAlign: "left",
  textDecoration: "none",
  fontSize: 13,
  flex: "1 1 0%",
  whiteSpace: "nowrap",
  overflow: "hidden",
  textOverflow: "ellipsis"
};
var revealIconStyle = {
  height: 12,
  color: CURRENT_COLOR
};
var ellipsisIconStyle = {
  style: {
    height: 12
  }
};
var inlineActionsStyle = {
  alignItems: "center",
  display: "flex"
};
var renderContextMenuAction = (color) => {
  return /* @__PURE__ */ jsx80(EllipsisIcon, {
    svgProps: ellipsisIconStyle,
    fill: color
  });
};
var getCanDragAsset = ({
  readOnlyStudio,
  relativePath
}) => {
  return !readOnlyStudio && getAssetElementFromPath(relativePath) !== null;
};
var getAssetFolderContextMenuItems = ({
  folderPath,
  fileManagerName,
  copyFolderPath,
  copyAbsolutePath,
  openFolderInExplorer,
  uploadAssets,
  fileExplorerAvailable,
  fileExplorerDisabled,
  mutationsDisabled
}) => {
  const items = [
    fileExplorerAvailable ? {
      id: "open-asset-folder-in-explorer",
      keyHint: null,
      label: `Show in ${fileManagerName}`,
      leftItem: null,
      onClick: openFolderInExplorer,
      quickSwitcherLabel: `Show asset folder in ${fileManagerName}`,
      subMenu: null,
      type: "item",
      value: "open-asset-folder-in-explorer",
      disabled: fileExplorerDisabled
    } : null,
    {
      id: "copy-asset-folder-path",
      keyHint: null,
      label: "Copy folder path",
      leftItem: null,
      onClick: copyFolderPath,
      quickSwitcherLabel: "Copy asset folder path",
      subMenu: null,
      type: "item",
      value: "copy-asset-folder-path"
    },
    copyAbsolutePath ? {
      id: "copy-asset-folder-absolute-path",
      keyHint: null,
      label: "Copy absolute path",
      leftItem: null,
      onClick: copyAbsolutePath,
      quickSwitcherLabel: "Copy asset folder absolute path",
      subMenu: null,
      type: "item",
      value: "copy-asset-folder-absolute-path"
    } : null,
    {
      type: "divider",
      id: "asset-folder-upload-divider"
    },
    {
      id: "upload-assets-to-folder",
      keyHint: null,
      label: "Upload...",
      leftItem: null,
      onClick: uploadAssets,
      quickSwitcherLabel: `Upload assets to ${folderPath}...`,
      subMenu: null,
      type: "item",
      value: "upload-assets-to-folder",
      disabled: mutationsDisabled
    }
  ];
  return items.filter(NoReactInternals10.truthy);
};
var AssetFolderItem = ({
  tabIndex,
  item,
  level,
  parentFolder,
  toggleFolder,
  dropLocation,
  setDropLocation,
  readOnlyStudio,
  uploadAssets
}) => {
  const openFolderTimerRef = useRef23(null);
  const connectionStatus = useContext46(StudioServerConnectionCtx).previewServerState.type;
  const fileManagerName = getFileManagerName(window.remotion_fileSystemPlatform);
  const folderPath = useMemo46(() => {
    return parentFolder ? `${parentFolder}/${item.name}` : item.name;
  }, [item.name, parentFolder]);
  const { isDropDiv, onDragEnter, onDragLeave } = use_asset_drag_events_default({
    name: item.name,
    parentFolder,
    dropLocation,
    setDropLocation
  });
  const folderStyle = useMemo46(() => {
    return {
      ...itemStyle2,
      paddingLeft: 4 + level * 8,
      ...hoverableStyle({
        idleBackground: TRANSPARENT,
        hoverBackground: WHITE_ALPHA_06,
        idleColor: LIGHT_TEXT,
        hoverColor: WHITE
      })
    };
  }, [level]);
  const onClick = useCallback37(() => {
    toggleFolder(item.name, parentFolder);
  }, [item.name, parentFolder, toggleFolder]);
  const copyFolderPath = useCallback37(() => {
    copyText(folderPath).then(() => {
      showNotification(`Copied '${folderPath}' to clipboard`, 1000);
    }).catch((err) => {
      showNotification(`Could not copy: ${err.message}`, 2000);
    });
  }, [folderPath]);
  const copyAbsolutePath = useCallback37(() => {
    if (window.remotion_publicFolderExists === null) {
      return;
    }
    const content2 = `${window.remotion_publicFolderExists}/${folderPath}`;
    copyText(content2).then(() => {
      showNotification(`Copied '${content2}' to clipboard`, 1000);
    }).catch((err) => {
      showNotification(`Could not copy: ${err.message}`, 2000);
    });
  }, [folderPath]);
  const openFolderInExplorer = useCallback37(() => {
    if (!window.remotion_publicFolderExists) {
      showNotification("Could not find the public folder", 2000);
      return;
    }
    openInFileExplorer({
      directory: `${window.remotion_publicFolderExists}/${folderPath}`
    }).catch((err) => {
      showNotification(`Could not open folder: ${err.message}`, 2000);
    });
  }, [folderPath]);
  const uploadAssetsToFolder = useCallback37(() => {
    uploadAssets(folderPath);
  }, [folderPath, uploadAssets]);
  const { mutationsDisabled, fileExplorerDisabled } = getAssetActionAvailability({
    browserStudioCanMutateAssets: getBrowserStudioOperations() === null ? null : true,
    readOnlyStudio,
    connectionStatus,
    publicFolderExists: window.remotion_publicFolderExists
  });
  const getContextMenuItems = useCallback37(() => {
    return getAssetFolderContextMenuItems({
      folderPath,
      fileManagerName,
      copyFolderPath,
      copyAbsolutePath: window.remotion_publicFolderExists === null ? null : copyAbsolutePath,
      openFolderInExplorer,
      uploadAssets: uploadAssetsToFolder,
      fileExplorerAvailable: getBrowserStudioOperations() === null,
      fileExplorerDisabled,
      mutationsDisabled
    });
  }, [
    copyAbsolutePath,
    copyFolderPath,
    fileExplorerDisabled,
    fileManagerName,
    folderPath,
    mutationsDisabled,
    openFolderInExplorer,
    uploadAssetsToFolder
  ]);
  const Icon = item.expanded ? ExpandedFolderIcon : CollapsedFolderIcon;
  return /* @__PURE__ */ jsxs45("div", {
    onDragEnter,
    onDragLeave,
    style: {
      backgroundColor: isDropDiv ? WHITE_ALPHA_06 : BACKGROUND
    },
    children: [
      /* @__PURE__ */ jsx80(ContextMenu, {
        getItems: getContextMenuItems,
        children: /* @__PURE__ */ jsx80(Row, {
          align: "center",
          children: /* @__PURE__ */ jsxs45("div", {
            style: folderStyle,
            className: `${HOVERABLE_CLASS_NAME} ${HOVER_GROUP_CLASS_NAME}`,
            tabIndex,
            title: item.name,
            onClick,
            onDragEnter: (event) => {
              if (!isAssetUploadDragEvent(event)) {
                return;
              }
              if (!item.expanded) {
                openFolderTimerRef.current = window.setTimeout(() => {
                  toggleFolder(item.name, parentFolder);
                }, 1000);
              }
            },
            onDragLeave: (event) => {
              if (!isAssetUploadDragEvent(event)) {
                return;
              }
              if (openFolderTimerRef.current) {
                clearTimeout(openFolderTimerRef.current);
              }
            },
            children: [
              /* @__PURE__ */ jsx80(Icon, {
                style: iconStyle3,
                color: CURRENT_COLOR
              }),
              /* @__PURE__ */ jsx80(Spacing, {
                x: 1
              }),
              /* @__PURE__ */ jsx80("div", {
                style: labelStyle2,
                children: item.name
              }),
              /* @__PURE__ */ jsxs45("div", {
                className: HOVER_GROUP_REVEAL_CLASS_NAME,
                style: inlineActionsStyle,
                children: [
                  /* @__PURE__ */ jsx80(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx80(InlineDropdown, {
                    variant: null,
                    title: "More actions",
                    renderAction: renderContextMenuAction,
                    getItems: getContextMenuItems,
                    style: NO_HOVER_BACKGROUND_STYLE,
                    className: FOCUS_VISIBLE_ONLY_CLASS_NAME
                  })
                ]
              })
            ]
          })
        })
      }),
      item.expanded ? /* @__PURE__ */ jsx80(AssetFolderTree, {
        item: item.items,
        name: item.name,
        level,
        parentFolder,
        tabIndex,
        toggleFolder,
        dropLocation,
        setDropLocation,
        readOnlyStudio,
        uploadAssets
      }, item.name) : null
    ]
  });
};
var AssetFolderTree = ({
  item,
  level,
  name,
  parentFolder,
  toggleFolder,
  tabIndex,
  dropLocation,
  setDropLocation,
  readOnlyStudio,
  uploadAssets
}) => {
  const combinedParents = useMemo46(() => {
    return [parentFolder, name].filter(NoReactInternals10.truthy).join("/");
  }, [name, parentFolder]);
  return /* @__PURE__ */ jsxs45("div", {
    children: [
      item.folders.map((folder) => {
        return /* @__PURE__ */ jsx80(AssetFolderItem, {
          item: folder,
          tabIndex,
          level: level + 1,
          parentFolder: combinedParents,
          toggleFolder,
          dropLocation,
          setDropLocation,
          readOnlyStudio,
          uploadAssets
        }, folder.name);
      }),
      item.files.map((file) => {
        return /* @__PURE__ */ jsx80(AssetSelectorItem, {
          item: file,
          tabIndex,
          level,
          parentFolder: combinedParents,
          readOnlyStudio
        }, file.src);
      })
    ]
  });
};
var AssetSelectorItem = ({ item, tabIndex, level, parentFolder, readOnlyStudio }) => {
  const [isDragging, setIsDragging] = useState26(false);
  const { setCanvasContent } = useContext46(Internals47.CompositionSetters);
  const { canvasContent } = useContext46(Internals47.CompositionManager);
  const relativePath = useMemo46(() => {
    return parentFolder ? parentFolder + "/" + item.name : item.name;
  }, [parentFolder, item.name]);
  const { fileExplorerDisabled, fileManagerName, getContextMenuItems } = useAssetContextMenuItems({
    relativePath,
    readOnlyStudio
  });
  const previewFileType = useMemo46(() => {
    return getPreviewFileType(relativePath);
  }, [relativePath]);
  const selected = useMemo46(() => {
    if (canvasContent && canvasContent.type === "asset") {
      return canvasContent.asset === relativePath;
    }
    return false;
  }, [canvasContent, relativePath]);
  const canDragAsset = useMemo46(() => {
    return getCanDragAsset({ readOnlyStudio, relativePath });
  }, [readOnlyStudio, relativePath]);
  const rowRef = useRef23(null);
  useLayoutEffect14(() => {
    maybeScrollAssetSidebarRowIntoView({
      element: rowRef.current,
      assetPath: relativePath,
      selected
    });
  }, [relativePath, selected]);
  const onClick = useCallback37(() => {
    markAssetSidebarScrollFromRowClick(relativePath);
    setCanvasContent({ type: "asset", asset: relativePath });
    pushUrl(`/assets/${relativePath}`);
  }, [relativePath, setCanvasContent]);
  const onDragStart = useCallback37((e) => {
    if (!canDragAsset) {
      e.preventDefault();
      return;
    }
    setIsDragging(true);
    e.dataTransfer.effectAllowed = "copy";
    const src = staticFile3(relativePath);
    const imageMetadata = previewFileType === "image" ? getCachedImageMetadata(src) : null;
    const mediaMetadata = previewFileType === "audio" || previewFileType === "video" ? getCachedMediaMetadata(src) : null;
    const width = imageMetadata?.width ?? mediaMetadata?.width ?? null;
    const height = imageMetadata?.height ?? mediaMetadata?.height ?? null;
    const hasDimensions = width !== null && height !== null && Number.isInteger(width) && Number.isInteger(height) && width > 0 && height > 0;
    const durationInSeconds = mediaMetadata?.duration !== undefined && Number.isFinite(mediaMetadata.duration) && mediaMetadata.duration > 0 ? mediaMetadata.duration : null;
    const dragData = StudioProtocolInternals.makeDragData({
      type: "asset",
      assetPath: relativePath,
      width: hasDimensions ? width : null,
      height: hasDimensions ? height : null,
      durationInSeconds
    });
    e.dataTransfer.setData(dragData.mimeType, dragData.payload);
  }, [canDragAsset, previewFileType, relativePath]);
  const onDragEnd = useCallback37(() => {
    setIsDragging(false);
  }, []);
  const style4 = useMemo46(() => {
    return {
      ...itemStyle2,
      ...hoverableStyle({
        idleBackground: selected ? WHITE_ALPHA_06 : TRANSPARENT,
        hoverBackground: WHITE_ALPHA_06,
        idleColor: selected ? WHITE : LIGHT_TEXT,
        hoverColor: WHITE
      }),
      paddingLeft: 12 + level * 8
    };
  }, [level, selected]);
  const renderFileExplorerAction = useCallback37((color) => {
    return /* @__PURE__ */ jsx80(ExpandedFolderIcon, {
      style: revealIconStyle,
      color
    });
  }, []);
  const openAssetInExplorer = useCallback37(() => {
    if (!window.remotion_publicFolderExists) {
      showNotification("Could not find the public folder", 2000);
      return;
    }
    openInFileExplorer({
      directory: window.remotion_publicFolderExists + "/" + relativePath
    }).catch((err) => {
      showNotification(`Could not open file: ${err.message}`, 2000);
    });
  }, [relativePath]);
  const revealInExplorer = useCallback37((e) => {
    e.stopPropagation();
    openAssetInExplorer();
  }, [openAssetInExplorer]);
  return /* @__PURE__ */ jsx80(ContextMenu, {
    getItems: getContextMenuItems,
    children: /* @__PURE__ */ jsx80(Row, {
      align: "center",
      children: /* @__PURE__ */ jsxs45("div", {
        ref: rowRef,
        style: style4,
        className: `${HOVERABLE_CLASS_NAME} ${HOVER_GROUP_CLASS_NAME}`,
        onClick,
        draggable: canDragAsset,
        onDragStart,
        onDragEnd,
        tabIndex,
        title: item.name,
        children: [
          /* @__PURE__ */ jsx80(AssetFileIcon, {
            fileType: previewFileType,
            style: iconStyle3,
            color: CURRENT_COLOR
          }),
          /* @__PURE__ */ jsx80(Spacing, {
            x: 1
          }),
          /* @__PURE__ */ jsx80("div", {
            style: labelStyle2,
            children: item.name
          }),
          !isDragging ? /* @__PURE__ */ jsxs45("div", {
            className: HOVER_GROUP_REVEAL_CLASS_NAME,
            style: inlineActionsStyle,
            children: [
              /* @__PURE__ */ jsx80(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx80(InlineDropdown, {
                variant: null,
                title: "More actions",
                renderAction: renderContextMenuAction,
                getItems: getContextMenuItems,
                style: NO_HOVER_BACKGROUND_STYLE,
                className: FOCUS_VISIBLE_ONLY_CLASS_NAME
              }),
              fileExplorerDisabled ? null : /* @__PURE__ */ jsxs45(Fragment23, {
                children: [
                  /* @__PURE__ */ jsx80(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx80(InlineAction, {
                    variant: null,
                    title: `Show in ${fileManagerName}`,
                    renderAction: renderFileExplorerAction,
                    onClick: revealInExplorer,
                    style: NO_HOVER_BACKGROUND_STYLE,
                    className: FOCUS_VISIBLE_ONLY_CLASS_NAME
                  })
                ]
              })
            ]
          }) : null
        ]
      })
    })
  });
};

// src/components/AssetSelector.tsx
import { jsx as jsx81, jsxs as jsxs46 } from "react/jsx-runtime";
var container13 = {
  display: "flex",
  flexDirection: "column",
  flex: 1,
  overflow: "hidden",
  backgroundColor: BACKGROUND
};
var emptyState = {
  display: "flex",
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
  textAlign: "center",
  padding: "0 12px"
};
var label4 = {
  color: LIGHT_TEXT,
  lineHeight: 1.5,
  fontSize: 14
};
var baseList = {
  overflowY: "auto",
  paddingTop: 4,
  paddingBottom: 4
};
var AssetSelector = ({ readOnlyStudio }) => {
  const { tabIndex } = useZIndex();
  const { assetFoldersExpanded, setAssetFoldersExpanded } = useContext47(FolderContext);
  const [dropLocation, setDropLocation] = useState27(null);
  const connectionStatus = useContext47(StudioServerConnectionCtx).previewServerState.type;
  const shouldAllowUpload = getBrowserStudioOperations() !== null || connectionStatus === "connected" && !readOnlyStudio;
  const list2 = useMemo47(() => {
    return {
      ...baseList,
      height: "100%"
    };
  }, []);
  const staticFiles = useStaticFiles();
  const publicFolderExists = window.remotion_publicFolderExists;
  const assetTree = useMemo47(() => {
    return buildAssetFolderStructure(staticFiles, null, assetFoldersExpanded);
  }, [assetFoldersExpanded, staticFiles]);
  const writeFilesToPublicFolder = useCallback38(async ({
    files,
    assetPath
  }) => {
    const makePath = (file) => {
      return [assetPath, file.name].filter(Boolean).join("/");
    };
    const differentExistingFile = files.find((file) => {
      const filePath = makePath(file);
      return staticFiles.some((staticFile4) => staticFile4.name === filePath && staticFile4.sizeInBytes !== file.size);
    });
    if (differentExistingFile) {
      showNotification(`File with name ${makePath(differentExistingFile)} already exists and is different`, 4000);
      return false;
    }
    for (const file of files) {
      const body = await file.arrayBuffer();
      await writeStaticFile({
        contents: body,
        filePath: makePath(file)
      });
    }
    return true;
  }, [staticFiles]);
  const toggleFolder = useCallback38((folderName, parentName) => {
    setAssetFoldersExpanded((p) => {
      const key4 = [parentName, folderName].filter(Boolean).join("/");
      const foldersExpandedState = toggleBooleanMapKey(p, key4);
      persistExpandedFolders("assets", foldersExpandedState);
      return foldersExpandedState;
    });
  }, [setAssetFoldersExpanded]);
  const { isDropDiv, onDragEnter, onDragLeave } = use_asset_drag_events_default({
    name: null,
    parentFolder: null,
    dropLocation,
    setDropLocation
  });
  const onDragOver = useCallback38((e) => {
    if (!isAssetUploadDragEvent(e)) {
      return;
    }
    e.preventDefault();
  }, []);
  const onDrop = useCallback38(async (e) => {
    try {
      if (!isAssetUploadDragEvent(e)) {
        setDropLocation(null);
        return;
      }
      e.preventDefault();
      e.stopPropagation();
      const assetPath = dropLocation ?? null;
      const renderOutput = getRenderOutputDragData(e.dataTransfer);
      if (renderOutput) {
        const destination = [assetPath, renderOutput.fileName].filter(Boolean).join("/");
        const result = await copyRenderOutputToAsset({
          outputPath: renderOutput.outputPath,
          assetPath: destination
        });
        if (!result.created) {
          showNotification(`${destination} already exists`, 3000);
        }
        return;
      }
      const files = Array.from(e.dataTransfer.files);
      if (files.length === 0) {
        setDropLocation(null);
        return;
      }
      await writeFilesToPublicFolder({ files, assetPath });
    } catch (error) {
      showNotification(`Error during upload: ${error}`, 3000);
    } finally {
      setDropLocation(null);
    }
  }, [dropLocation, writeFilesToPublicFolder]);
  const uploadAssets = useCallback38(async (assetPath) => {
    try {
      const files = await pickFilesToImport();
      if (files.length === 0) {
        return;
      }
      const wereWritten = await writeFilesToPublicFolder({
        files,
        assetPath
      });
      if (wereWritten) {
        const destination = assetPath ? `'${assetPath}'` : "public folder";
        showNotification(files.length === 1 ? `Uploaded ${files[0].name} to ${destination}` : `Uploaded ${files.length} assets to ${destination}`, 3000);
      }
    } catch (error) {
      showNotification(`Error during upload: ${error}`, 3000);
    }
  }, [writeFilesToPublicFolder]);
  const getAssetActions = useCallback38(() => {
    return [
      {
        id: "upload-assets",
        keyHint: null,
        label: "Upload...",
        leftItem: null,
        onClick: () => uploadAssets(null),
        quickSwitcherLabel: "Upload assets...",
        subMenu: null,
        type: "item",
        value: "upload-assets",
        disabled: !shouldAllowUpload
      }
    ];
  }, [shouldAllowUpload, uploadAssets]);
  return /* @__PURE__ */ jsxs46("div", {
    "data-asset-selector": true,
    style: container13,
    onDragOver: shouldAllowUpload ? onDragOver : undefined,
    onDrop: shouldAllowUpload ? onDrop : undefined,
    children: [
      /* @__PURE__ */ jsx81(ExplorerQuickSwitcherTrigger, {
        mode: "assets",
        showShortcut: true,
        tabIndex,
        getActions: getAssetActions
      }),
      staticFiles.length === 0 ? publicFolderExists ? /* @__PURE__ */ jsx81("div", {
        style: emptyState,
        children: /* @__PURE__ */ jsxs46("div", {
          style: label4,
          children: [
            "To add assets, place a file in the",
            " ",
            /* @__PURE__ */ jsx81("code", {
              style: inlineCodeSnippet,
              children: "public"
            }),
            " folder of your project or drag and drop a file here."
          ]
        })
      }) : /* @__PURE__ */ jsx81("div", {
        style: emptyState,
        children: /* @__PURE__ */ jsxs46("div", {
          style: label4,
          children: [
            "To add assets, create a folder called",
            " ",
            /* @__PURE__ */ jsx81("code", {
              style: inlineCodeSnippet,
              children: "public"
            }),
            " in the root of your project and place a file in it."
          ]
        })
      }) : /* @__PURE__ */ jsx81("div", {
        className: "__remotion-vertical-scrollbar",
        style: {
          ...list2,
          backgroundColor: isDropDiv ? WHITE_ALPHA_06 : BACKGROUND
        },
        onDragEnter,
        onDragLeave,
        children: /* @__PURE__ */ jsx81(AssetFolderTree, {
          item: assetTree,
          level: 0,
          parentFolder: null,
          name: null,
          tabIndex,
          toggleFolder,
          dropLocation,
          setDropLocation,
          readOnlyStudio,
          uploadAssets
        })
      })
    ]
  });
};

// src/components/CompSelectorRef.tsx
import { useCallback as useCallback39, useContext as useContext48, useImperativeHandle as useImperativeHandle4, useMemo as useMemo48 } from "react";
import { Internals as Internals48 } from "remotion";
import { jsx as jsx82 } from "react/jsx-runtime";
var CompSelectorRef = ({ children }) => {
  const { compositions } = useContext48(Internals48.CompositionManager);
  const { compositionFoldersExpanded, setCompositionFoldersExpanded } = useContext48(FolderContext);
  const selectComposition = useSelectComposition();
  const toggleFolder = useCallback39((folderName, parentName) => {
    setCompositionFoldersExpanded((p) => {
      const key4 = openFolderKey({ folderName, parentName });
      const foldersExpandedState = toggleBooleanMapKey(p, key4);
      persistExpandedFolders("compositions", foldersExpandedState);
      return foldersExpandedState;
    });
  }, [setCompositionFoldersExpanded]);
  useImperativeHandle4(Internals48.compositionSelectorRef, () => {
    return {
      expandComposition: (compName) => {
        const compositionToExpand = compositions.find((c) => c.id === compName);
        if (!compositionToExpand) {
          return;
        }
        const { folderName, parentFolderName } = compositionToExpand;
        if (folderName === null) {
          return;
        }
        setCompositionFoldersExpanded((previousState) => {
          const keysToExpand = getKeysToExpand(folderName, parentFolderName);
          const foldersExpandedState = {
            ...previousState
          };
          for (const key4 of keysToExpand) {
            foldersExpandedState[key4] = true;
          }
          persistExpandedFolders("compositions", foldersExpandedState);
          return foldersExpandedState;
        });
      },
      selectComposition: (compName) => {
        const comp = compositions.find((c) => c.id === compName);
        if (!comp) {
          throw new Error(`Composition ${compName} not found`);
        }
        selectComposition(comp, true);
      },
      toggleFolder: (folderName, parentName) => {
        toggleFolder(folderName, parentName);
      }
    };
  }, [
    compositions,
    selectComposition,
    setCompositionFoldersExpanded,
    toggleFolder
  ]);
  const contextValue = useMemo48(() => {
    return {
      foldersExpanded: compositionFoldersExpanded,
      setFoldersExpanded: (foldersExpanded) => {
        setCompositionFoldersExpanded(foldersExpanded);
        persistExpandedFolders("compositions", foldersExpanded);
      },
      toggleFolder
    };
  }, [compositionFoldersExpanded, setCompositionFoldersExpanded, toggleFolder]);
  return /* @__PURE__ */ jsx82(ExpandedFoldersContext.Provider, {
    value: contextValue,
    children
  });
};

// src/components/ExplorerPanel.tsx
import { jsx as jsx83, jsxs as jsxs47 } from "react/jsx-runtime";
var container14 = {
  height: "100%",
  width: "100%",
  maxWidth: "100%",
  display: "flex",
  flexDirection: "column",
  flex: 1
};
var localStorageKey = "remotion.sidebarPanel";
var getSelectedPanel = () => {
  const panel = localStorage.getItem(localStorageKey);
  if (panel === "assets") {
    return "assets";
  }
  return "compositions";
};
var tabsContainer = {
  backgroundColor: BACKGROUND
};
var persistSelectedOptionsSidebarPanel2 = (panel) => {
  localStorage.setItem(localStorageKey, panel);
};
var ExplorerPanel = ({ readOnlyStudio }) => {
  const [panel, setPanel] = useState28(() => getSelectedPanel());
  const assetTabFocusTimer = useRef25(null);
  const onCompositionsSelected = useCallback40(() => {
    setPanel("compositions");
    persistSelectedOptionsSidebarPanel2("compositions");
  }, []);
  const onAssetsSelected = useCallback40(() => {
    setPanel("assets");
    persistSelectedOptionsSidebarPanel2("assets");
  }, []);
  const onAssetDragEnter = useCallback40((event) => {
    if (isAssetUploadDragEvent(event) && assetTabFocusTimer.current === null) {
      assetTabFocusTimer.current = window.setTimeout(() => {
        onAssetsSelected();
        assetTabFocusTimer.current = null;
      }, 500);
    }
  }, [onAssetsSelected]);
  const onAssetDragLeave = useCallback40((event) => {
    if (isAssetUploadDragEvent(event) && assetTabFocusTimer.current !== null) {
      window.clearTimeout(assetTabFocusTimer.current);
      assetTabFocusTimer.current = null;
    }
  }, []);
  useImperativeHandle5(explorerSidebarTabs, () => {
    return {
      getSelectedPanel: () => panel,
      selectAssetsPanel: () => {
        setPanel("assets");
        persistSelectedOptionsSidebarPanel2("assets");
      },
      selectCompositionPanel: () => {
        setPanel("compositions");
        persistSelectedOptionsSidebarPanel2("compositions");
      }
    };
  }, [panel]);
  const onPointerDown = useCallback40((e) => {
    e.stopPropagation();
  }, []);
  return /* @__PURE__ */ jsx83(CompSelectorRef, {
    children: /* @__PURE__ */ jsxs47("div", {
      style: container14,
      className: "css-reset",
      onPointerDown,
      children: [
        /* @__PURE__ */ jsx83("div", {
          style: tabsContainer,
          children: /* @__PURE__ */ jsxs47(Tabs, {
            children: [
              /* @__PURE__ */ jsx83(Tab, {
                selected: panel === "compositions",
                onClick: onCompositionsSelected,
                children: "Compositions"
              }),
              /* @__PURE__ */ jsx83(Tab, {
                selected: panel === "assets",
                onClick: onAssetsSelected,
                onDragEnter: onAssetDragEnter,
                onDragLeave: onAssetDragLeave,
                children: "Assets"
              })
            ]
          })
        }),
        panel === "compositions" ? /* @__PURE__ */ jsx83(CompositionSelector, {}) : /* @__PURE__ */ jsx83(AssetSelector, {
          readOnlyStudio
        })
      ]
    })
  });
};

// src/components/PreviewToolbar.tsx
import { useCallback as useCallback52, useContext as useContext60, useState as useState33 } from "react";
import { Internals as Internals58 } from "remotion";

// src/helpers/is-current-selected-still.ts
import { useContext as useContext49 } from "react";
import { Internals as Internals49 } from "remotion";
var useIsStill = () => {
  const resolved = Internals49.useResolvedVideoConfig(null);
  if (!resolved || resolved.type !== "success") {
    return false;
  }
  return isCompositionStill(resolved.result);
};
var useIsVideoComposition = () => {
  const isStill = useIsStill();
  const { canvasContent, currentAssetMetadata } = useContext49(Internals49.CompositionManager);
  if (canvasContent === null) {
    return false;
  }
  if (isStill) {
    return false;
  }
  return canvasContent.type === "composition" || canvasContent.type === "asset" && currentAssetMetadata?.asset === canvasContent.asset;
};

// src/state/loop.ts
var key4 = "remotion.loop";
var persistLoopOption = (option) => {
  localStorage.setItem(key4, String(option));
};
var loadLoopOption = () => {
  const item = localStorage.getItem(key4);
  return item !== "false";
};

// src/components/CheckboardToggle.tsx
import { useCallback as useCallback41, useContext as useContext50 } from "react";
import { jsx as jsx84, jsxs as jsxs48 } from "react/jsx-runtime";
var buttonStyle4 = { width: 26 };
var CheckboardToggle = () => {
  const { checkerboard, setCheckerboard } = useContext50(CheckerboardContext);
  const onClick = useCallback41(() => {
    setCheckerboard((c) => {
      return !c;
    });
  }, [setCheckerboard]);
  const shortcut = useKeyboardShortcutLabel("toggleCheckerboard");
  const ariaKeyShortcuts = useKeyboardShortcutAriaKeyShortcuts("toggleCheckerboard");
  const accessibilityLabel2 = "Show transparency as checkerboard";
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  return /* @__PURE__ */ jsx84(ActionTooltip, {
    label: "Checkerboard",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx84(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel2,
      "aria-pressed": checkerboard,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaKeyShortcuts || undefined,
      onClick,
      style: buttonStyle4,
      children: (color) => /* @__PURE__ */ jsxs48("svg", {
        "aria-hidden": "true",
        focusable: "false",
        xmlns: "http://www.w3.org/2000/svg",
        viewBox: "0 0 512 512",
        style: { width: 18, height: 18 },
        fill: "none",
        children: [
          /* @__PURE__ */ jsx84("path", {
            fill: checkerboard ? BLUE : color,
            d: "M256 48h184c13.3 0 24 10.7 24 24v184H256V48zM48 256h208v208H72c-13.3 0-24-10.7-24-24V256z"
          }),
          /* @__PURE__ */ jsx84("rect", {
            x: "48",
            y: "48",
            width: "416",
            height: "416",
            rx: "24",
            stroke: checkerboard ? BLUE : color,
            strokeWidth: "32"
          }),
          /* @__PURE__ */ jsx84("path", {
            d: "M256 48v416M48 256h416",
            stroke: checkerboard ? BLUE : color,
            strokeWidth: "32"
          })
        ]
      })
    })
  });
};

// src/components/FpsCounter.tsx
import {
  useEffect as useEffect34,
  useLayoutEffect as useLayoutEffect15,
  useMemo as useMemo49,
  useRef as useRef26,
  useState as useState29
} from "react";
import { Internals as Internals50 } from "remotion";
import { jsxs as jsxs49 } from "react/jsx-runtime";
var label5 = {
  color: LIGHT_TEXT,
  fontSize: 13,
  fontFamily: "Arial, Helvetica, sans-serif",
  whiteSpace: "nowrap"
};
var pushWithMaxSize = (arr, value, maxSize) => {
  arr.push(value);
  return arr.slice(-maxSize);
};
var FpsCounter = ({ playbackSpeed }) => {
  const videoConfig = Internals50.useUnsafeVideoConfig();
  const playing = Internals50.usePlaying();
  const frame2 = Internals50.Timeline.useTimelinePosition();
  const [marker, rerender] = useState29({});
  const [fps, setFps] = useState29(0);
  const previousUpdates = useRef26([]);
  const fpsRef = useRef26(0);
  const playingRef = useRef26(playing);
  useLayoutEffect15(() => {
    fpsRef.current = 0;
    previousUpdates.current = [];
    playingRef.current = playing;
  }, [playing]);
  useLayoutEffect15(() => {
    if (playingRef.current === false)
      return;
    previousUpdates.current = pushWithMaxSize(previousUpdates.current, performance.now(), 15);
    if (previousUpdates.current.length < 2)
      return;
    const diff = Math.max(...previousUpdates.current) - Math.min(...previousUpdates.current);
    const averageDistanceBetween = diff / (previousUpdates.current.length - 1);
    fpsRef.current = 1000 / averageDistanceBetween;
    if (previousUpdates.current.length === 2)
      setFps(fpsRef.current);
  }, [frame2]);
  useEffect34(() => {
    if (playing) {
      const t = setTimeout(() => {
        rerender({});
        setFps(fpsRef.current);
      }, 1000);
      return () => clearTimeout(t);
    }
  }, [marker, playing]);
  const style4 = useMemo49(() => {
    if (!videoConfig) {
      return {};
    }
    const expectedFps = Math.abs(playbackSpeed) * videoConfig.fps;
    return {
      ...label5,
      color: fps < expectedFps * 0.9 ? RED : LIGHT_TEXT
    };
  }, [fps, playbackSpeed, videoConfig]);
  if (fps === 0) {
    return null;
  }
  if (playing === false) {
    return null;
  }
  if (videoConfig === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs49("div", {
    style: style4,
    children: [
      fps.toFixed(1),
      " FPS"
    ]
  });
};

// src/components/FullscreenToggle.tsx
import { useCallback as useCallback42, useContext as useContext51, useEffect as useEffect35 } from "react";
import { Internals as Internals51 } from "remotion";
import { jsx as jsx85 } from "react/jsx-runtime";
var FullScreenToggle = ({ hidden }) => {
  const keybindings = useKeybinding();
  const { setSize } = useContext51(Internals51.PreviewSizeContext);
  const onClick = useCallback42(() => {
    drawRef.current?.requestFullscreen();
    if (document.fullscreenElement)
      setSize(() => ({
        size: "auto",
        translation: {
          x: 0,
          y: 0
        }
      }));
  }, [setSize]);
  const shortcut = useKeyboardShortcutLabel("enterFullscreen");
  const ariaKeyShortcuts = useKeyboardShortcutAriaKeyShortcuts("enterFullscreen");
  const accessibilityLabel2 = "Enter fullscreen preview";
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  useEffect35(() => {
    const f = keybindings.registerKeybinding({
      event: "keydown",
      action: "enterFullscreen",
      callback: onClick,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      f.unregister();
    };
  }, [keybindings, onClick]);
  return hidden ? /* @__PURE__ */ jsx85("button", {
    id: "fullscreen-toggle",
    type: "button",
    style: { display: "none" },
    onClick
  }) : /* @__PURE__ */ jsx85(ActionTooltip, {
    label: "Fullscreen",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: true,
    children: /* @__PURE__ */ jsx85(ControlButton, {
      id: "fullscreen-toggle",
      title: "",
      "aria-label": accessibilityLabel2,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaKeyShortcuts || undefined,
      onClick,
      children: (color) => /* @__PURE__ */ jsx85(FullscreenIcon, {
        color,
        style: { width: 18, height: 18 }
      })
    })
  });
};

// src/components/LoopToggle.tsx
import { useCallback as useCallback43 } from "react";
import { jsx as jsx86 } from "react/jsx-runtime";
var accessibilityLabel2 = "Loop";
var buttonStyle5 = { width: 30 };
var toggleLoop = (setLoop) => {
  setLoop((currentLoop) => {
    persistLoopOption(!currentLoop);
    return !currentLoop;
  });
};
var LoopToggle = ({ loop, setLoop }) => {
  const shortcut = useKeyboardShortcutLabel("toggleLoop");
  const ariaShortcut = useKeyboardShortcutAriaKeyShortcuts("toggleLoop");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const onClick = useCallback43(() => {
    toggleLoop(setLoop);
  }, [setLoop]);
  return /* @__PURE__ */ jsx86(ActionTooltip, {
    label: accessibilityLabel2,
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx86(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel2,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaShortcut || undefined,
      "aria-pressed": loop,
      onClick,
      style: buttonStyle5,
      children: (color) => /* @__PURE__ */ jsx86("svg", {
        viewBox: "0 0 512 512",
        style: { width: 18, height: 18 },
        children: /* @__PURE__ */ jsx86("path", {
          fill: loop ? BLUE : color,
          d: "M384 150.3V41.6l64 53.3v2l-64 53.3zM352 23.5V80H192C86 80 0 166 0 272c0 8.8 7.2 16 16 16s16-7.2 16-16c0-88.4 71.6-160 160-160h160v56.5c0 13 10.5 23.5 23.5 23.5 5.5 0 10.8-1.9 15-5.4l78-65c7.3-6.1 11.5-15.1 11.5-24.6v-2c0-9.5-4.2-18.5-11.5-24.6l-78-65c-4.2-3.5-9.5-5.4-15-5.4-13 0-23.5 10.5-23.5 23.5zM128 415.9v54.5l-64-53.3v-2l64-53.3v54.1zM160 400v-56.5c0-13-10.5-23.5-23.5-23.5-5.5 0-10.8 1.9-15 5.4l-78 65C36.2 396.5 32 405.5 32 415v2c0 9.5 4.2 18.5 11.5 24.6l78 65c4.2 3.5 9.5 5.4 15 5.4 13 0 23.5-10.5 23.5-23.5V432h160c106 0 192-86 192-192 0-8.8-7.2-16-16-16s-16 7.2-16 16c0 88.4-71.6 160-160 160H160z"
        })
      })
    })
  });
};

// src/components/MuteToggle.tsx
import { useCallback as useCallback44 } from "react";

// src/icons/media-volume.tsx
import { jsx as jsx87, jsxs as jsxs50 } from "react/jsx-runtime";
var size2 = 21;
var speakerPath = "M112 320H48c-8.8 0-16-7.2-16-16v-96c0-8.8 7.2-16 16-16h64c8.5 0 16.6-3.4 22.6-9.4L252.7 64.6c.4-.4.9-.6 1.4-.6 1.1 0 1.9.9 1.9 1.9V446c0 1.1-.9 1.9-1.9 1.9-.5 0-1-.2-1.4-.6L134.6 329.4c-6-6-14.1-9.4-22.6-9.4zm0-160H48c-26.5 0-48 21.5-48 48v96c0 26.5 21.5 48 48 48h64l118.1 118.1c6.4 6.4 15 9.9 24 9.9 18.7 0 33.9-15.2 33.9-33.9V65.9C288 47.2 272.8 32 254.1 32c-9 0-17.6 3.6-24 9.9L112 160zm243.2 22.4c-5.3 7.1-3.9 17.1 3.2 22.4 15.6 11.7 25.6 30.3 25.6 51.2s-10 39.5-25.6 51.2c-7.1 5.3-8.5 15.3-3.2 22.4s15.3 8.5 22.4 3.2C400.9 315.3 416 287.4 416 256s-15.1-59.3-38.4-76.8c-7.1-5.3-17.1-3.9-22.4 3.2zm87-74.5c-6.8-5.6-16.9-4.7-22.5 2.1s-4.7 16.9 2.1 22.5C457.4 161.9 480 206.3 480 256s-22.6 94.1-58.2 123.4c-6.8 5.6-7.8 15.7-2.1 22.5s15.7 7.8 22.5 2.2C484.8 368.9 512 315.6 512 256s-27.2-112.9-69.8-148.1z";
var VolumeOffIcon = ({ color }) => {
  return /* @__PURE__ */ jsxs50("svg", {
    width: size2,
    height: size2,
    viewBox: "0 0 576 512",
    children: [
      /* @__PURE__ */ jsx87("g", {
        transform: "translate(32 0)",
        children: /* @__PURE__ */ jsx87("path", {
          d: speakerPath,
          fill: color
        })
      }),
      /* @__PURE__ */ jsx87("path", {
        d: "M16-16 560 528",
        fill: "none",
        stroke: color,
        strokeLinecap: "round",
        strokeWidth: "32"
      })
    ]
  });
};
var VolumeOnIcon = ({ color }) => {
  return /* @__PURE__ */ jsx87("svg", {
    width: size2,
    height: size2,
    viewBox: "0 0 576 512",
    children: /* @__PURE__ */ jsx87("g", {
      transform: "translate(32 0)",
      children: /* @__PURE__ */ jsx87("path", {
        d: speakerPath,
        fill: color
      })
    })
  });
};

// src/state/mute.ts
var key5 = "remotion.mute";
var persistMuteOption = (option) => {
  localStorage.setItem(key5, String(option));
};
var loadMuteOption = () => {
  const item = localStorage.getItem(key5);
  return item === "true";
};

// src/components/MuteToggle.tsx
import { jsx as jsx88 } from "react/jsx-runtime";
var buttonStyle6 = { width: 30 };
var toggleMute = (setMuted) => {
  setMuted((muted) => {
    persistMuteOption(!muted);
    return !muted;
  });
};
var MuteToggle = ({ muted, setMuted }) => {
  const shortcut = useKeyboardShortcutLabel("toggleMute");
  const ariaShortcut = useKeyboardShortcutAriaKeyShortcuts("toggleMute");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const onClick = useCallback44(() => {
    toggleMute(setMuted);
  }, [setMuted]);
  const accessibilityLabel3 = muted ? "Unmute video" : "Mute video";
  return /* @__PURE__ */ jsx88(ActionTooltip, {
    label: "Mute",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx88(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel3,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaShortcut || undefined,
      onClick,
      style: buttonStyle6,
      children: (color) => muted ? /* @__PURE__ */ jsx88(VolumeOffIcon, {
        color: BLUE
      }) : /* @__PURE__ */ jsx88(VolumeOnIcon, {
        color
      })
    })
  });
};

// src/components/OutlineToggle.tsx
import { useCallback as useCallback45, useContext as useContext53 } from "react";
import { jsx as jsx89, jsxs as jsxs51 } from "react/jsx-runtime";
var buttonStyle7 = { width: 26 };
var OutlineToggle = () => {
  const { editorShowOutlines, setEditorShowOutlines } = useContext53(EditorShowOutlinesContext);
  const onClick = useCallback45(() => {
    setEditorShowOutlines((current) => !current);
  }, [setEditorShowOutlines]);
  const shortcut = useKeyboardShortcutLabel("toggleOutlines");
  const ariaShortcut = useKeyboardShortcutAriaKeyShortcuts("toggleOutlines");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const accessibilityLabel3 = editorShowOutlines ? "Hide outlines" : "Show outlines";
  return /* @__PURE__ */ jsx89(ActionTooltip, {
    label: "Outlines",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx89(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel3,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaShortcut || undefined,
      "aria-pressed": editorShowOutlines,
      onClick,
      style: buttonStyle7,
      children: (color) => {
        const iconColor = editorShowOutlines ? BLUE : color;
        return /* @__PURE__ */ jsxs51("svg", {
          style: { width: 17, height: 17 },
          viewBox: "0 0 512 512",
          fill: "none",
          "aria-hidden": "true",
          focusable: "false",
          children: [
            /* @__PURE__ */ jsx89("path", {
              d: "M64 64H448V448H64V64Z",
              stroke: iconColor,
              strokeWidth: "36"
            }),
            [
              [16, 16],
              [400, 16],
              [16, 400],
              [400, 400]
            ].map(([x, y]) => /* @__PURE__ */ jsx89("rect", {
              x,
              y,
              width: "96",
              height: "96",
              rx: "24",
              fill: BACKGROUND_HEX,
              stroke: iconColor,
              strokeWidth: "32"
            }, `${x}-${y}`))
          ]
        });
      }
    })
  });
};

// src/components/PlaybackKeyboardShortcutsManager.tsx
import { PlayerInternals as PlayerInternals9 } from "@remotion/player";
import { useCallback as useCallback46, useEffect as useEffect36 } from "react";
var PlaybackKeyboardShortcutsManager = ({ setPlaybackRate, setMuted, setLoop }) => {
  const keybindings = useKeybinding();
  const isVideoComposition = useIsVideoComposition();
  useEffect36(() => {
    if (!isVideoComposition) {
      return;
    }
    const mute = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleMute",
      callback: (event) => {
        if (!event.repeat)
          toggleMute(setMuted);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const loop = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleLoop",
      callback: (event) => {
        if (!event.repeat)
          toggleLoop(setLoop);
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      mute.unregister();
      loop.unregister();
    };
  }, [isVideoComposition, keybindings, setLoop, setMuted]);
  const { play: play2, pause: pause2, isPlaying } = PlayerInternals9.usePlayerMethods();
  const onJKey = useCallback46(() => {
    const wasPlaying = isPlaying();
    setPlaybackRate((prevPlaybackRate) => {
      if (!wasPlaying) {
        return -1;
      }
      if (prevPlaybackRate > -1) {
        return -1;
      }
      if (prevPlaybackRate > -2) {
        return -2;
      }
      return -4;
    });
    play2();
  }, [isPlaying, play2, setPlaybackRate]);
  const onKKey = useCallback46(() => {
    setPlaybackRate(1);
    pause2();
  }, [pause2, setPlaybackRate]);
  const onLKey = useCallback46(() => {
    const wasPlaying = isPlaying();
    setPlaybackRate((prevPlaybackRate) => {
      if (!wasPlaying) {
        return 1;
      }
      if (prevPlaybackRate < 1) {
        return 1;
      }
      if (prevPlaybackRate < 2) {
        return 2;
      }
      return 4;
    });
    play2();
  }, [isPlaying, play2, setPlaybackRate]);
  useEffect36(() => {
    const jKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "reversePlayback",
      callback: onJKey,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const kKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "pausePlayback",
      callback: onKKey,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const lKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "playForward",
      callback: onLKey,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      jKey.unregister();
      kKey.unregister();
      lKey.unregister();
    };
  }, [keybindings, onJKey, onKKey, onLKey]);
  return null;
};

// src/components/PlaybackRatePersistor.tsx
import { useEffect as useEffect37 } from "react";
import { Internals as Internals53 } from "remotion";

// src/state/playbackrate.ts
var key6 = "remotion.playbackrate";
var commonPlaybackRates = [
  -4,
  -2,
  -1,
  -0.5,
  -0.25,
  0.25,
  0.5,
  1,
  1.5,
  2,
  4
];
var persistPlaybackRate = (option) => {
  localStorage.setItem(key6, String(option));
};
var loadPlaybackRate = () => {
  if (typeof window !== "undefined") {
    return 1;
  }
  const item = localStorage.getItem(key6);
  if (item === null) {
    return 1;
  }
  return Number(item);
};

// src/components/PlaybackRatePersistor.tsx
var PlaybackRatePersistor = () => {
  const { setPlaybackRate, playbackRate } = Internals53.usePlaybackRate();
  useEffect37(() => {
    setPlaybackRate(loadPlaybackRate());
  }, [setPlaybackRate]);
  useEffect37(() => {
    persistPlaybackRate(playbackRate);
  }, [playbackRate]);
  return null;
};

// src/components/PlaybackRateSelector.tsx
import { useContext as useContext55, useMemo as useMemo50 } from "react";
import { Internals as Internals55 } from "remotion";

// src/icons/playback-rate.tsx
import { jsx as jsx90 } from "react/jsx-runtime";
var iconStyle4 = {
  width: 20,
  height: 20,
  flexShrink: 0
};
var defaultPlaybackRatePath = "M320 96C443.7 96 544 196.3 544 320C544 443.7 443.7 544 320 544C196.3 544 96 443.7 96 320C96 196.3 196.3 96 320 96zM320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320C64 461.4 178.6 576 320 576zM320 384C337.7 384 352 398.3 352 416C352 433.7 337.7 448 320 448C302.3 448 288 433.7 288 416C288 398.3 302.3 384 320 384zM320 480C355.3 480 384 451.3 384 416C384 386.2 363.6 361.1 336 354L336 160C336 151.2 328.8 144 320 144C311.2 144 304 151.2 304 160L304 354C276.4 361.1 256 386.2 256 416C256 451.3 284.7 480 320 480zM208 232C221.3 232 232 221.3 232 208C232 194.7 221.3 184 208 184C194.7 184 184 194.7 184 208C184 221.3 194.7 232 208 232zM184 320C184 306.7 173.3 296 160 296C146.7 296 136 306.7 136 320C136 333.3 146.7 344 160 344C173.3 344 184 333.3 184 320zM480 344C493.3 344 504 333.3 504 320C504 306.7 493.3 296 480 296C466.7 296 456 306.7 456 320C456 333.3 466.7 344 480 344zM456 208C456 194.7 445.3 184 432 184C418.7 184 408 194.7 408 208C408 221.3 418.7 232 432 232C445.3 232 456 221.3 456 208z";
var halfPlaybackRatePath = "M320 544C443.7 544 544 443.7 544 320C544 196.3 443.7 96 320 96C196.3 96 96 196.3 96 320C96 443.7 196.3 544 320 544zM320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64zM344 160C344 173.3 333.3 184 320 184C306.7 184 296 173.3 296 160C296 146.7 306.7 136 320 136C333.3 136 344 146.7 344 160zM288 416C288 433.7 302.3 448 320 448C337.7 448 352 433.7 352 416C352 398.3 337.7 384 320 384C302.3 384 288 398.3 288 416zM256 416C256 397.4 263.9 380.7 276.5 369L193.9 215.6L186.3 201.5L214.5 186.3L222.1 200.4L304.7 353.8C309.6 352.6 314.7 352 320 352C355.3 352 384 380.7 384 416C384 451.3 355.3 480 320 480C284.7 480 256 451.3 256 416zM456 208C456 221.3 445.3 232 432 232C418.7 232 408 221.3 408 208C408 194.7 418.7 184 432 184C445.3 184 456 194.7 456 208zM480 296C493.3 296 504 306.7 504 320C504 333.3 493.3 344 480 344C466.7 344 456 333.3 456 320C456 306.7 466.7 296 480 296zM184 320C184 333.3 173.3 344 160 344C146.7 344 136 333.3 136 320C136 306.7 146.7 296 160 296C173.3 296 184 306.7 184 320z";
var quarterPlaybackRatePath = "M320 96C443.7 96 544 196.3 544 320C544 443.7 443.7 544 320 544C196.3 544 96 443.7 96 320C96 196.3 196.3 96 320 96zM320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320C64 461.4 178.6 576 320 576zM344 160C344 146.7 333.3 136 320 136C306.7 136 296 146.7 296 160C296 173.3 306.7 184 320 184C333.3 184 344 173.3 344 160zM288 416C288 398.3 302.3 384 320 384C337.7 384 352 398.3 352 416C352 433.7 337.7 448 320 448C302.3 448 288 433.7 288 416zM384 416C384 380.7 355.3 352 320 352C302.5 352 286.6 359 275.1 370.4L168.2 306.3L154.5 298.1L138 325.5L151.7 333.7L258.5 397.8C256.8 403.6 255.9 409.7 255.9 416C255.9 451.3 284.6 480 319.9 480C355.2 480 383.9 451.3 383.9 416zM232 208C232 194.7 221.3 184 208 184C194.7 184 184 194.7 184 208C184 221.3 194.7 232 208 232C221.3 232 232 221.3 232 208zM480 344C493.3 344 504 333.3 504 320C504 306.7 493.3 296 480 296C466.7 296 456 306.7 456 320C456 333.3 466.7 344 480 344zM456 208C456 194.7 445.3 184 432 184C418.7 184 408 194.7 408 208C408 221.3 418.7 232 432 232C445.3 232 456 221.3 456 208z";
var mediumFastPlaybackRatePath = "M320 96C443.7 96 544 196.3 544 320C544 443.7 443.7 544 320 544C196.3 544 96 443.7 96 320C96 196.3 196.3 96 320 96zM320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320C64 461.4 178.6 576 320 576zM344 160C344 146.7 333.3 136 320 136C306.7 136 296 146.7 296 160C296 173.3 306.7 184 320 184C333.3 184 344 173.3 344 160zM288 416C288 398.3 302.3 384 320 384C337.7 384 352 398.3 352 416C352 433.7 337.7 448 320 448C302.3 448 288 433.7 288 416zM384 416C384 397.4 376.1 380.7 363.5 369L446.1 215.6L453.7 201.5L425.5 186.3L417.9 200.4L335.3 353.8C330.4 352.6 325.3 352 320 352C284.7 352 256 380.7 256 416C256 451.3 284.7 480 320 480C355.3 480 384 451.3 384 416zM232 208C232 194.7 221.3 184 208 184C194.7 184 184 194.7 184 208C184 221.3 194.7 232 208 232C221.3 232 232 221.3 232 208zM160 344C173.3 344 184 333.3 184 320C184 306.7 173.3 296 160 296C146.7 296 136 306.7 136 320C136 333.3 146.7 344 160 344zM504 320C504 306.7 493.3 296 480 296C466.7 296 456 306.7 456 320C456 333.3 466.7 344 480 344C493.3 344 504 333.3 504 320z";
var fastPlaybackRatePath = "M320 544C443.7 544 544 443.7 544 320C544 196.3 443.7 96 320 96C196.3 96 96 196.3 96 320C96 443.7 196.3 544 320 544zM320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64zM344 160C344 173.3 333.3 184 320 184C306.7 184 296 173.3 296 160C296 146.7 306.7 136 320 136C333.3 136 344 146.7 344 160zM288 416C288 433.7 302.3 448 320 448C337.7 448 352 433.7 352 416C352 398.3 337.7 384 320 384C302.3 384 288 398.3 288 416zM256 416C256 380.7 284.7 352 320 352C337.5 352 353.4 359 364.9 370.4L471.8 306.3L485.5 298.1L502 325.5L488.3 333.7L381.5 397.8C383.2 403.6 384.1 409.7 384.1 416C384.1 451.3 355.4 480 320.1 480C284.8 480 256.1 451.3 256.1 416zM456 208C456 221.3 445.3 232 432 232C418.7 232 408 221.3 408 208C408 194.7 418.7 184 432 184C445.3 184 456 194.7 456 208zM160 296C173.3 296 184 306.7 184 320C184 333.3 173.3 344 160 344C146.7 344 136 333.3 136 320C136 306.7 146.7 296 160 296zM232 208C232 221.3 221.3 232 208 232C194.7 232 184 221.3 184 208C184 194.7 194.7 184 208 184C221.3 184 232 194.7 232 208z";
var PlaybackRateIcon = ({ color, playbackRate }) => {
  let path = defaultPlaybackRatePath;
  if (playbackRate === 0.5) {
    path = halfPlaybackRatePath;
  } else if (playbackRate === 0.25) {
    path = quarterPlaybackRatePath;
  } else if (playbackRate > 2) {
    path = fastPlaybackRatePath;
  } else if (playbackRate > 1) {
    path = mediumFastPlaybackRatePath;
  }
  return /* @__PURE__ */ jsx90("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 640",
    style: iconStyle4,
    children: /* @__PURE__ */ jsx90("path", {
      fill: color,
      d: path
    })
  });
};

// src/components/PlaybackRateSelector.tsx
import { jsx as jsx91 } from "react/jsx-runtime";
var getPlaybackRateLabel = (playbackRate) => {
  return `${playbackRate}x`;
};
var accessibilityLabel3 = "Playback rate";
var usePlaybackRateMenuItems = ({
  playbackRate,
  setPlaybackRate
}) => {
  const items = useMemo50(() => {
    const divider = {
      type: "divider",
      id: "divider"
    };
    const values = commonPlaybackRates.map((newPlaybackRate) => {
      return {
        id: String(newPlaybackRate),
        label: getPlaybackRateLabel(newPlaybackRate),
        onClick: () => {
          return setPlaybackRate(() => {
            persistPlaybackRate(newPlaybackRate);
            return newPlaybackRate;
          });
        },
        type: "item",
        value: newPlaybackRate,
        keyHint: null,
        leftItem: String(playbackRate) === String(newPlaybackRate) ? /* @__PURE__ */ jsx91(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      };
    });
    const middle = Math.floor(commonPlaybackRates.length / 2);
    return [...values.slice(0, middle), divider, ...values.slice(middle)];
  }, [playbackRate, setPlaybackRate]);
  return {
    items,
    selectedId: String(playbackRate)
  };
};
var PlaybackRateSelector = ({
  playbackRate,
  setPlaybackRate
}) => {
  const { canvasContent } = useContext55(Internals55.CompositionManager);
  const isStill = useIsStill();
  const { items, selectedId } = usePlaybackRateMenuItems({
    playbackRate,
    setPlaybackRate
  });
  if (isStill || canvasContent === null) {
    return null;
  }
  return /* @__PURE__ */ jsx91(TimelineCombobox, {
    title: accessibilityLabel3,
    tooltipDelay: 800,
    labelWidth: 30,
    selectedId,
    values: items,
    renderLeftItem: (color) => /* @__PURE__ */ jsx91(PlaybackRateIcon, {
      color,
      playbackRate
    }),
    unhoveredIconColor: WHITE_ALPHA_80
  });
};

// src/components/PlayPause.tsx
import { PlayerInternals as PlayerInternals10 } from "@remotion/player";
import React61, { useCallback as useCallback47, useEffect as useEffect38, useState as useState30 } from "react";
import { Internals as Internals56 } from "remotion";

// src/icons/jump-to-start.tsx
import { jsx as jsx92 } from "react/jsx-runtime";
var JumpToStart = ({
  color = CURRENT_COLOR,
  ...props
}) => {
  return /* @__PURE__ */ jsx92("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512",
    ...props,
    children: /* @__PURE__ */ jsx92("path", {
      fill: color,
      d: "M0 415.1V96.03c0-17.67 14.33-31.1 31.1-31.1C49.67 64.03 64 78.36 64 96.03v131.8l171.5-156.5C256.1 54.28 288 68.66 288 96.03v131.9l171.5-156.5C480.1 54.28 512 68.66 512 96.03v319.9c0 27.37-31.88 41.74-52.5 24.62L288 285.2v130.7c0 27.37-31.88 41.74-52.5 24.62L64 285.2v130.7c0 17.67-14.33 31.1-31.1 31.1C14.33 447.1 0 433.6 0 415.1z"
    })
  });
};

// src/icons/pause.tsx
import { jsx as jsx93 } from "react/jsx-runtime";
var Pause = ({
  color = CURRENT_COLOR,
  ...props
}) => /* @__PURE__ */ jsx93("svg", {
  ...props,
  "aria-hidden": "true",
  focusable: "false",
  "data-prefix": "fas",
  "data-icon": "pause",
  className: "svg-inline--fa fa-pause fa-w-14",
  role: "img",
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 448 512",
  children: /* @__PURE__ */ jsx93("path", {
    fill: color,
    d: "M144 479H48c-26.5 0-48-21.5-48-48V79c0-26.5 21.5-48 48-48h96c26.5 0 48 21.5 48 48v352c0 26.5-21.5 48-48 48zm304-48V79c0-26.5-21.5-48-48-48h-96c-26.5 0-48 21.5-48 48v352c0 26.5 21.5 48 48 48h96c26.5 0 48-21.5 48-48z"
  })
});

// src/icons/play.tsx
import { jsx as jsx94 } from "react/jsx-runtime";
var Play = ({
  color = CURRENT_COLOR,
  ...props
}) => /* @__PURE__ */ jsx94("svg", {
  ...props,
  "aria-hidden": "true",
  focusable: "false",
  "data-prefix": "fas",
  "data-icon": "play",
  className: "svg-inline--fa fa-play fa-w-14",
  role: "img",
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 448 512",
  children: /* @__PURE__ */ jsx94("path", {
    fill: color,
    d: "M424.4 214.7L72.4 6.6C43.8-10.3 0 6.1 0 47.9V464c0 37.5 40.7 60.1 72.4 41.3l352-208c31.4-18.5 31.5-64.1 0-82.6z"
  })
});

// src/icons/step-back.tsx
import { jsx as jsx95 } from "react/jsx-runtime";
var StepBack = ({
  color = CURRENT_COLOR,
  ...props
}) => {
  return /* @__PURE__ */ jsx95("svg", {
    viewBox: "0 0 448 512",
    ...props,
    children: /* @__PURE__ */ jsx95("path", {
      fill: color,
      d: "M64 468V44c0-6.6 5.4-12 12-12h48c6.6 0 12 5.4 12 12v176.4l195.5-181C352.1 22.3 384 36.6 384 64v384c0 27.4-31.9 41.7-52.5 24.6L136 292.7V468c0 6.6-5.4 12-12 12H76c-6.6 0-12-5.4-12-12z"
    })
  });
};

// src/icons/step-forward.tsx
import { jsx as jsx96 } from "react/jsx-runtime";
var StepForward = ({
  color = CURRENT_COLOR,
  ...props
}) => {
  return /* @__PURE__ */ jsx96("svg", {
    viewBox: "0 0 448 512",
    ...props,
    children: /* @__PURE__ */ jsx96("path", {
      fill: color,
      d: "M384 44v424c0 6.6-5.4 12-12 12h-48c-6.6 0-12-5.4-12-12V291.6l-195.5 181C95.9 489.7 64 475.4 64 448V64c0-27.4 31.9-41.7 52.5-24.6L312 219.3V44c0-6.6 5.4-12 12-12h48c6.6 0 12 5.4 12 12z"
    })
  });
};

// src/components/PlayPause.tsx
import { jsx as jsx97, jsxs as jsxs52, Fragment as Fragment24 } from "react/jsx-runtime";
var backStyle = {
  height: 18
};
var forwardBackStyle = {
  height: 16
};
var iconButton = {
  height: 14,
  width: 14
};
var browserMediaControlsBehavior = {
  mode: "register-media-session"
};
var PlaybackManager = ({ playbackRate, loop, muted, inFrame, outFrame, getCurrentFrame: getCurrentFrame2 }) => {
  PlayerInternals10.usePlayback({
    loop,
    playbackRate,
    moveToBeginningWhenEnded: true,
    inFrame,
    outFrame,
    getCurrentFrame: getCurrentFrame2,
    browserMediaControlsBehavior,
    muted
  });
  return null;
};
var PlayPauseInner = ({
  playbackRate,
  loop,
  bufferStateDelayInMilliseconds,
  muted,
  hideNavigationControls
}) => {
  const { inFrame, outFrame } = useTimelineInOutFramePosition();
  const videoConfig = Internals56.useUnsafeVideoConfig();
  const [showBufferIndicator, setShowBufferState] = useState30(false);
  const {
    play: play2,
    pause: pause2,
    pauseAndReturnToPlayStart,
    frameBack,
    seek: seek2,
    frameForward,
    emitter,
    getCurrentFrame: getCurrentFrame2
  } = PlayerInternals10.usePlayerMethods();
  const playing = Internals56.usePlaying();
  const isStill = useIsStill();
  const jumpToBeginningShortcut = useKeyboardShortcutLabel("jumpToBeginning");
  const jumpToBeginningAriaShortcut = useKeyboardShortcutAriaKeyShortcuts("jumpToBeginning");
  const playPauseShortcut = useKeyboardShortcutLabel("playPause");
  const playPauseAriaShortcut = useKeyboardShortcutAriaKeyShortcuts("playPause");
  useEffect38(() => {
    if (isStill) {
      pause2();
    }
  }, [isStill, pause2]);
  const onSpace = useCallback47((e) => {
    if (playing) {
      pause2();
    } else {
      play2();
    }
    e.preventDefault();
  }, [pause2, play2, playing]);
  const onEnter = useCallback47((e) => {
    if (playing) {
      e.preventDefault();
      pauseAndReturnToPlayStart();
    }
  }, [pauseAndReturnToPlayStart, playing]);
  const oneFrameBack = useCallback47(() => {
    frameBack(1);
    ensureFrameIsInViewport({
      direction: "fit-left",
      durationInFrames: getCurrentDuration(),
      frame: Math.max(0, getCurrentFrame2() - 1)
    });
  }, [frameBack, getCurrentFrame2]);
  const oneFrameForward = useCallback47(() => {
    frameForward(1);
    ensureFrameIsInViewport({
      direction: "fit-right",
      durationInFrames: getCurrentDuration(),
      frame: Math.min(getCurrentDuration() - 1, getCurrentFrame2() + 1)
    });
  }, [frameForward, getCurrentFrame2]);
  const jumpToStart = useCallback47(() => {
    seek2(inFrame ?? 0);
  }, [seek2, inFrame]);
  const jumpToEnd = useCallback47(() => {
    seek2(outFrame ?? getCurrentDuration() - 1);
  }, [seek2, outFrame]);
  const keybindings = useKeybinding();
  useEffect38(() => {
    const commandArrowLeft = keybindings.registerKeybinding({
      event: "keydown",
      key: "ArrowLeft",
      callback: oneFrameBack,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const commandArrowRight = keybindings.registerKeybinding({
      event: "keydown",
      key: "ArrowRight",
      callback: oneFrameForward,
      commandCtrlKey: true,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const space = keybindings.registerKeybinding({
      event: "keydown",
      action: "playPause",
      callback: onSpace,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const enter = keybindings.registerKeybinding({
      event: "keydown",
      action: "pauseAndReturnToPlaybackStart",
      callback: onEnter,
      preventDefault: false,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const a = keybindings.registerKeybinding({
      event: "keydown",
      action: "jumpToBeginning",
      callback: jumpToStart,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    const e = keybindings.registerKeybinding({
      event: "keydown",
      action: "jumpToEnd",
      callback: jumpToEnd,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      commandArrowLeft.unregister();
      commandArrowRight.unregister();
      space.unregister();
      enter.unregister();
      a.unregister();
      e.unregister();
    };
  }, [
    jumpToEnd,
    jumpToStart,
    keybindings,
    onEnter,
    onSpace,
    oneFrameBack,
    oneFrameForward
  ]);
  useEffect38(() => {
    let timeout = null;
    let stopped = false;
    const onBuffer = () => {
      requestAnimationFrame(() => {
        stopped = false;
        timeout = setTimeout(() => {
          if (!stopped) {
            setShowBufferState(true);
          }
        }, bufferStateDelayInMilliseconds);
      });
    };
    const onResume = () => {
      requestAnimationFrame(() => {
        setShowBufferState(false);
        stopped = true;
        if (timeout) {
          clearTimeout(timeout);
        }
      });
    };
    emitter.addEventListener("waiting", onBuffer);
    emitter.addEventListener("resume", onResume);
    return () => {
      emitter.removeEventListener("waiting", onBuffer);
      emitter.removeEventListener("resume", onResume);
      setShowBufferState(false);
      if (timeout) {
        clearTimeout(timeout);
      }
      stopped = true;
    };
  }, [bufferStateDelayInMilliseconds, emitter]);
  return /* @__PURE__ */ jsxs52(Fragment24, {
    children: [
      /* @__PURE__ */ jsx97(PlaybackManager, {
        loop,
        playbackRate,
        muted,
        inFrame,
        outFrame,
        getCurrentFrame: getCurrentFrame2
      }),
      hideNavigationControls ? null : /* @__PURE__ */ jsx97(ActionTooltip, {
        label: "Go to beginning",
        shortcut: jumpToBeginningShortcut,
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx97(ControlButton, {
          "aria-label": "Go to beginning",
          "aria-keyshortcuts": jumpToBeginningAriaShortcut || undefined,
          title: "",
          disabled: !videoConfig,
          onClick: jumpToStart,
          children: (color) => /* @__PURE__ */ jsx97(JumpToStart, {
            style: backStyle,
            color
          })
        })
      }),
      hideNavigationControls ? null : /* @__PURE__ */ jsx97(ActionTooltip, {
        label: "Previous frame",
        shortcut: "←",
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx97(ControlButton, {
          "aria-label": "Go back 1 frame",
          "aria-keyshortcuts": "ArrowLeft",
          title: "",
          disabled: !videoConfig,
          onClick: oneFrameBack,
          children: (color) => /* @__PURE__ */ jsx97(StepBack, {
            style: forwardBackStyle,
            color
          })
        })
      }),
      /* @__PURE__ */ jsx97(ActionTooltip, {
        label: playing ? "Pause" : "Play",
        shortcut: playPauseShortcut,
        delay: 800,
        dismissOnClick: false,
        children: /* @__PURE__ */ jsx97(ControlButton, {
          "aria-label": playing ? "Pause" : "Play",
          "aria-keyshortcuts": playPauseAriaShortcut || undefined,
          title: "",
          onClick: playing ? pause2 : play2,
          disabled: !videoConfig,
          children: (color) => playing ? showBufferIndicator ? /* @__PURE__ */ jsx97(PlayerInternals10.BufferingIndicator, {
            type: "studio",
            color
          }) : /* @__PURE__ */ jsx97(Pause, {
            style: iconButton,
            color
          }) : /* @__PURE__ */ jsx97(Play, {
            style: iconButton,
            color
          })
        })
      }),
      hideNavigationControls ? null : /* @__PURE__ */ jsx97(ActionTooltip, {
        label: "Next frame",
        shortcut: "→",
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx97(ControlButton, {
          "aria-label": "Go forward 1 frame",
          "aria-keyshortcuts": "ArrowRight",
          title: "",
          disabled: !videoConfig,
          onClick: oneFrameForward,
          children: (color) => /* @__PURE__ */ jsx97(StepForward, {
            style: forwardBackStyle,
            color
          })
        })
      })
    ]
  });
};
var PlayPause = React61.memo(PlayPauseInner);

// src/components/PreviewToolbarOverflowButton.tsx
import { useCallback as useCallback48, useContext as useContext56, useMemo as useMemo51 } from "react";
import { jsx as jsx98 } from "react/jsx-runtime";
var clickButton = (id) => {
  document.getElementById(id)?.click();
};
var PreviewToolbarOverflowButton = ({
  showFullscreen,
  showPlaybackRate,
  showLoop,
  showCanvasViewControls,
  showCompositionControls,
  playbackRate,
  setPlaybackRate,
  loop,
  setLoop
}) => {
  const keyboardShortcutsDisabled = areKeyboardShortcutsDisabled();
  const fullscreenShortcut = useKeyboardShortcutLabel("enterFullscreen");
  const loopShortcut = useKeyboardShortcutLabel("toggleLoop");
  const outlinesShortcut = useKeyboardShortcutLabel("toggleOutlines");
  const checkerboardShortcut = useKeyboardShortcutLabel("toggleCheckerboard");
  const { checkerboard, setCheckerboard } = useContext56(CheckerboardContext);
  const { editorShowOutlines, setEditorShowOutlines } = useContext56(EditorShowOutlinesContext);
  const { editorShowGuides, setEditorShowGuides } = useContext56(EditorShowGuidesContext);
  const { editorShowRulers, setEditorShowRulers } = useContext56(EditorShowRulersContext);
  const {
    items: previewSizeItems,
    selectedId: selectedPreviewSize,
    zoomable
  } = usePreviewSizeMenuItems();
  const { items: playbackRateItems, selectedId: selectedPlaybackRate } = usePlaybackRateMenuItems({ playbackRate, setPlaybackRate });
  const iconStyle5 = useMemo51(() => {
    return {
      style: {
        width: 14,
        height: 14,
        flexShrink: 0
      }
    };
  }, []);
  const renderAction = useCallback48((color) => /* @__PURE__ */ jsx98(EllipsisIcon, {
    fill: color,
    svgProps: iconStyle5
  }), [iconStyle5]);
  const values = useMemo51(() => {
    const items = [];
    if (zoomable) {
      items.push({
        type: "item",
        id: "preview-size",
        label: "Preview Size",
        value: "preview-size",
        onClick: () => {
          return;
        },
        keyHint: null,
        leftItem: null,
        subMenu: {
          items: previewSizeItems,
          leaveLeftSpace: true,
          preselectIndex: previewSizeItems.findIndex((item) => item.id === selectedPreviewSize)
        },
        quickSwitcherLabel: null
      });
    }
    if (showPlaybackRate) {
      items.push({
        type: "item",
        id: "playback-rate",
        label: "Playback Rate",
        value: "playback-rate",
        onClick: () => {
          return;
        },
        keyHint: null,
        leftItem: null,
        subMenu: {
          items: playbackRateItems,
          leaveLeftSpace: true,
          preselectIndex: playbackRateItems.findIndex((item) => item.id === selectedPlaybackRate)
        },
        quickSwitcherLabel: null
      });
    }
    if (showLoop) {
      items.push({
        type: "item",
        id: "loop",
        label: "Loop",
        value: "loop",
        onClick: () => toggleLoop(setLoop),
        keyHint: keyboardShortcutsDisabled || loopShortcut === "" ? null : loopShortcut,
        leftItem: loop ? /* @__PURE__ */ jsx98(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    if (showCanvasViewControls) {
      items.push({
        type: "item",
        id: "checkerboard",
        label: "Transparency as checkerboard",
        value: "checkerboard",
        onClick: () => setCheckerboard((current) => !current),
        keyHint: keyboardShortcutsDisabled || checkerboardShortcut === "" ? null : checkerboardShortcut,
        leftItem: checkerboard ? /* @__PURE__ */ jsx98(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    if (showCompositionControls) {
      items.push({
        type: "item",
        id: "outlines",
        label: "Outlines",
        value: "outlines",
        onClick: () => setEditorShowOutlines((current) => !current),
        keyHint: keyboardShortcutsDisabled ? null : outlinesShortcut || null,
        leftItem: editorShowOutlines ? /* @__PURE__ */ jsx98(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    if (showCanvasViewControls) {
      items.push({
        type: "item",
        id: "rulers",
        label: "Rulers",
        value: "rulers",
        onClick: () => setEditorShowRulers((current) => !current),
        keyHint: null,
        leftItem: editorShowRulers ? /* @__PURE__ */ jsx98(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    if (showCompositionControls) {
      items.push({
        type: "item",
        id: "guides",
        label: "Guides",
        value: "guides",
        onClick: () => setEditorShowGuides((current) => !current),
        keyHint: null,
        leftItem: editorShowGuides ? /* @__PURE__ */ jsx98(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    if (showFullscreen) {
      items.push({
        type: "item",
        id: "fullscreen",
        label: "Fullscreen",
        value: "fullscreen",
        onClick: () => clickButton("fullscreen-toggle"),
        keyHint: keyboardShortcutsDisabled || fullscreenShortcut === "" ? null : fullscreenShortcut,
        leftItem: null,
        subMenu: null,
        quickSwitcherLabel: null
      });
    }
    return items;
  }, [
    checkerboardShortcut,
    fullscreenShortcut,
    loopShortcut,
    previewSizeItems,
    playbackRateItems,
    selectedPlaybackRate,
    selectedPreviewSize,
    showFullscreen,
    showLoop,
    showPlaybackRate,
    showCanvasViewControls,
    showCompositionControls,
    loop,
    checkerboard,
    editorShowGuides,
    editorShowOutlines,
    outlinesShortcut,
    editorShowRulers,
    setCheckerboard,
    setEditorShowGuides,
    setEditorShowOutlines,
    setEditorShowRulers,
    setLoop,
    zoomable,
    keyboardShortcutsDisabled
  ]);
  if (values.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx98(ActionTooltip, {
    label: "View options",
    shortcut: null,
    delay: 800,
    dismissOnClick: true,
    children: /* @__PURE__ */ jsx98(InlineDropdown, {
      variant: null,
      renderAction,
      values,
      title: "More actions",
      unhoveredColor: WHITE_ALPHA_80
    })
  });
};

// src/components/RenderButton.tsx
import { PlayerInternals as PlayerInternals11 } from "@remotion/player";
import { REACT_REFRESH_FINISHED_EVENT } from "@remotion/studio-shared";
import React65, { useCallback as useCallback49, useContext as useContext57, useMemo as useMemo53, useState as useState31 } from "react";
import { Internals as Internals57 } from "remotion";
import { jsx as jsx99, jsxs as jsxs53, Fragment as Fragment25 } from "react/jsx-runtime";
var segmentedButtonStyle2 = {
  height: 28
};
var mainButtonContent = {
  paddingLeft: 4,
  paddingRight: 6,
  minWidth: 0
};
var label6 = {
  color: "inherit",
  fontFamily: "inherit",
  fontSize: 14,
  lineHeight: "21px",
  minWidth: 0,
  overflow: "hidden",
  textOverflow: "ellipsis",
  userSelect: "none",
  WebkitUserSelect: "none",
  whiteSpace: "nowrap"
};
var compactMainSegmentStyle2 = {
  padding: "0 6px"
};
var defaultMainSegmentStyle2 = {
  fontFamily: "inherit",
  fontSize: 14,
  padding: "0 7px"
};
var compactDropdownSegmentStyle = {
  padding: 0,
  width: 20
};
var defaultDropdownSegmentStyle = {
  padding: "0 6px"
};
var compactMainButtonContent = {
  ...mainButtonContent,
  paddingLeft: 0,
  paddingRight: 0
};
var compactLabel = {
  ...label6,
  fontSize: 12,
  lineHeight: "16px"
};
var RENDER_TYPE_STORAGE_KEY = "remotion.renderType";
var getInitialRenderType = (readOnlyStudio) => {
  if (readOnlyStudio) {
    return "client-render";
  }
  try {
    const stored = localStorage.getItem(RENDER_TYPE_STORAGE_KEY);
    if (stored === "server-render" || stored === "client-render") {
      return stored;
    }
  } catch {}
  return "server-render";
};
var RenderButtonInner = ({ readOnlyStudio, size: controlSize = "default", narrow = false }) => {
  const { inFrame, outFrame } = useTimelineInOutFramePosition();
  const { setSelectedModal } = useContext57(SetSelectedModalContext);
  const [preferredRenderType, setPreferredRenderType] = useState31(() => getInitialRenderType(readOnlyStudio));
  const connectionStatus = useContext57(StudioServerConnectionCtx).previewServerState.type;
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const canServerRender = connectionStatus === "connected" && !isBrowserStudio;
  const renderType = useMemo53(() => {
    if (readOnlyStudio) {
      return preferredRenderType === "render-command" ? "render-command" : "client-render";
    }
    if (connectionStatus === "disconnected" || isBrowserStudio) {
      return "client-render";
    }
    return preferredRenderType;
  }, [connectionStatus, isBrowserStudio, preferredRenderType, readOnlyStudio]);
  const renderShortcut = useKeyboardShortcutLabel("render");
  const shortcut = areKeyboardShortcutsDisabled() || renderShortcut === "" ? "" : `(${renderShortcut})`;
  const tooltip = renderType === "render-command" ? "Copy a CLI command to render this composition " + shortcut : "Export the current composition " + shortcut;
  const iconStyle5 = useMemo53(() => {
    return {
      style: {
        height: controlSize === "compact" ? 18 : 16,
        width: controlSize === "compact" ? 18 : 16,
        flexShrink: 0
      }
    };
  }, [controlSize]);
  const video = Internals57.useVideo();
  const { canvasContent } = useContext57(Internals57.CompositionManager);
  const { getCurrentFrame: getCurrentFrame2 } = PlayerInternals11.usePlayerMethods();
  const { props } = useContext57(Internals57.EditorPropsContext);
  const openServerRenderModal = useCallback49((copyCommandOnly) => {
    if (!video) {
      return null;
    }
    const defaults = window.remotion_renderDefaults;
    if (!defaults) {
      throw new TypeError("Expected defaults");
    }
    setSelectedModal({
      type: "server-render",
      readOnlyStudio: copyCommandOnly,
      compositionId: video.id,
      initialFrame: getCurrentFrame2(),
      initialStillImageFormat: defaults.stillImageFormat,
      initialVideoImageFormat: null,
      initialJpegQuality: defaults.jpegQuality,
      initialScale: window.remotion_renderDefaults?.scale ?? 1,
      initialLogLevel: defaults.logLevel,
      initialConcurrency: defaults.concurrency,
      maxConcurrency: defaults.maxConcurrency,
      minConcurrency: defaults.minConcurrency,
      initialMuted: defaults.muted,
      initialEnforceAudioTrack: defaults.enforceAudioTrack,
      initialProResProfile: defaults.proResProfile,
      initialx264Preset: defaults.x264Preset,
      initialGopSize: defaults.gopSize,
      initialPixelFormat: null,
      initialAudioBitrate: defaults.audioBitrate,
      initialVideoBitrate: defaults.videoBitrate,
      initialEveryNthFrame: defaults.everyNthFrame,
      initialNumberOfGifLoops: defaults.numberOfGifLoops,
      initialDelayRenderTimeout: defaults.delayRenderTimeout,
      defaultConfigurationAudioCodec: defaults.audioCodec,
      initialEnvVariables: window.process.env,
      initialDisableWebSecurity: defaults.disableWebSecurity,
      initialDarkMode: defaults.darkMode,
      initialOpenGlRenderer: defaults.openGlRenderer,
      initialHeadless: defaults.headless,
      initialIgnoreCertificateErrors: defaults.ignoreCertificateErrors,
      initialOffthreadVideoCacheSizeInBytes: defaults.offthreadVideoCacheSizeInBytes,
      initialOffthreadVideoThreads: defaults.offthreadVideoThreads,
      defaultProps: props[video.id] ?? video.defaultProps,
      inFrameMark: inFrame,
      outFrameMark: outFrame,
      initialColorSpace: defaults.colorSpace,
      initialMultiProcessOnLinux: defaults.multiProcessOnLinux,
      defaultConfigurationVideoCodec: defaults.codec,
      initialEncodingBufferSize: defaults.encodingBufferSize,
      initialEncodingMaxRate: defaults.encodingMaxRate,
      initialUserAgent: defaults.userAgent,
      initialBeep: defaults.beepOnFinish,
      initialRepro: defaults.repro,
      initialForSeamlessAacConcatenation: defaults.forSeamlessAacConcatenation,
      renderTypeOfLastRender: null,
      defaulMetadata: defaults.metadata,
      initialHardwareAcceleration: defaults.hardwareAcceleration,
      initialSampleRate: defaults.sampleRate,
      initialChromeMode: defaults.chromeMode,
      initialMediaCacheSizeInBytes: defaults.mediaCacheSizeInBytes,
      renderDefaults: defaults
    });
  }, [video, setSelectedModal, props, inFrame, outFrame, getCurrentFrame2]);
  const openClientRenderModal = useCallback49(async () => {
    if (!video) {
      return null;
    }
    const defaults = window.remotion_renderDefaults;
    if (!defaults) {
      throw new TypeError("Expected defaults");
    }
    if (!readOnlyStudio && !isBrowserStudio) {
      let onReconciliation = null;
      const nextReconciliation = new Promise((resolve) => {
        onReconciliation = resolve;
        window.addEventListener(REACT_REFRESH_FINISHED_EVENT, onReconciliation, { once: true });
      });
      try {
        const { didInvalidate } = await callApi("/api/invalidate-bundle", {});
        if (didInvalidate) {
          await nextReconciliation;
        }
      } catch (error) {
        showNotification(`Could not prepare client-side render: ${error.message}`, 4000);
        return null;
      } finally {
        if (onReconciliation) {
          window.removeEventListener(REACT_REFRESH_FINISHED_EVENT, onReconciliation);
        }
      }
    }
    setSelectedModal({
      type: "web-render",
      compositionId: video.id,
      initialFrame: getCurrentFrame2(),
      defaultProps: props[video.id] ?? video.defaultProps,
      inFrameMark: inFrame,
      outFrameMark: outFrame,
      initialLogLevel: defaults.logLevel,
      initialStillImageFormat: defaults.stillImageFormat,
      initialScale: defaults.scale,
      initialDelayRenderTimeout: defaults.delayRenderTimeout,
      initialDefaultOutName: null,
      initialContainer: null,
      initialVideoCodec: null,
      initialAudioCodec: null,
      initialAudioBitrate: null,
      initialVideoBitrate: null,
      initialHardwareAcceleration: null,
      initialKeyframeIntervalInSeconds: null,
      initialTransparent: null,
      initialMuted: null,
      initialMediaCacheSizeInBytes: defaults.mediaCacheSizeInBytes,
      initialAllowHtmlInCanvas: defaults.allowHtmlInCanvas,
      initialPageResponsiveness: "medium"
    });
  }, [
    video,
    readOnlyStudio,
    isBrowserStudio,
    setSelectedModal,
    props,
    inFrame,
    outFrame,
    getCurrentFrame2
  ]);
  const onClick = useCallback49(() => {
    if (renderType === "render-command") {
      openServerRenderModal(true);
      return;
    }
    if (renderType === "server-render") {
      openServerRenderModal(false);
    } else {
      openClientRenderModal();
    }
  }, [renderType, openServerRenderModal, openClientRenderModal]);
  const handleRenderTypeChange = useCallback49((newType) => {
    setPreferredRenderType(newType);
    try {
      localStorage.setItem(RENDER_TYPE_STORAGE_KEY, newType);
    } catch {}
    if (newType === "server-render") {
      openServerRenderModal(false);
    } else if (newType === "render-command") {
      openServerRenderModal(true);
    } else {
      openClientRenderModal();
    }
  }, [openClientRenderModal, openServerRenderModal]);
  const dropdownValues = useMemo53(() => {
    if (readOnlyStudio) {
      return [
        {
          type: "item",
          id: "client-render",
          label: "Render in browser",
          value: "client-render",
          onClick: () => handleRenderTypeChange("client-render"),
          keyHint: null,
          leftItem: null,
          subMenu: null,
          quickSwitcherLabel: null
        },
        {
          type: "item",
          id: "render-command",
          label: "Render via CLI",
          value: "render-command",
          onClick: () => handleRenderTypeChange("render-command"),
          keyHint: null,
          leftItem: null,
          subMenu: null,
          quickSwitcherLabel: null
        }
      ];
    }
    return [
      ...canServerRender ? [
        {
          type: "item",
          id: "server-render",
          label: "Server-side render",
          value: "server-render",
          onClick: () => handleRenderTypeChange("server-render"),
          keyHint: null,
          leftItem: null,
          subMenu: null,
          quickSwitcherLabel: null
        }
      ] : [],
      {
        type: "item",
        id: "client-render",
        label: "Client-side render",
        value: "client-render",
        onClick: () => handleRenderTypeChange("client-render"),
        keyHint: null,
        leftItem: null,
        subMenu: null,
        quickSwitcherLabel: null
      }
    ];
  }, [canServerRender, handleRenderTypeChange, readOnlyStudio]);
  const renderLabel = renderType === "server-render" ? "Render" : renderType === "render-command" ? "Render via CLI" : "Render in browser";
  const showRenderLabel = !narrow || renderType !== "server-render";
  const segments = useMemo53(() => {
    return [
      {
        ariaLabel: renderLabel,
        buttonId: "render-modal-button",
        disabled: false,
        idleColor: WHITE_ALPHA_80,
        onClick,
        onPointerDown: null,
        renderContent: (color) => /* @__PURE__ */ jsxs53(Row, {
          align: "center",
          style: controlSize === "compact" ? compactMainButtonContent : mainButtonContent,
          children: [
            /* @__PURE__ */ jsx99(ThinRenderIcon, {
              fill: color,
              svgProps: iconStyle5
            }),
            showRenderLabel ? /* @__PURE__ */ jsxs53(Fragment25, {
              children: [
                /* @__PURE__ */ jsx99(Spacing, {
                  x: controlSize === "compact" ? 0.75 : 1
                }),
                /* @__PURE__ */ jsx99("span", {
                  style: controlSize === "compact" ? compactLabel : label6,
                  children: renderLabel
                })
              ]
            }) : null
          ]
        }),
        segmentId: "render",
        style: controlSize === "compact" ? compactMainSegmentStyle2 : defaultMainSegmentStyle2,
        title: showRenderLabel ? tooltip : null,
        tooltipLabel: showRenderLabel ? null : renderLabel,
        type: "action"
      },
      {
        ariaLabel: "Select render type",
        buttonId: null,
        disabled: false,
        idleColor: WHITE_ALPHA_80,
        leaveLeftSpace: false,
        onOpenChange: null,
        renderContent: (color) => /* @__PURE__ */ jsx99(CaretDown, {
          color
        }),
        segmentId: "render-type",
        selectedId: renderType,
        style: controlSize === "compact" ? compactDropdownSegmentStyle : defaultDropdownSegmentStyle,
        title: "Select render type",
        tooltipLabel: null,
        type: "menu",
        values: dropdownValues
      }
    ];
  }, [
    controlSize,
    dropdownValues,
    iconStyle5,
    onClick,
    renderLabel,
    renderType,
    showRenderLabel,
    tooltip
  ]);
  if (!video || canvasContent?.type !== "composition") {
    return null;
  }
  return /* @__PURE__ */ jsxs53(Fragment25, {
    children: [
      /* @__PURE__ */ jsx99("button", {
        style: { display: "none" },
        id: "render-modal-button-server",
        disabled: !canServerRender,
        onClick: () => openServerRenderModal(false),
        type: "button"
      }),
      " ",
      /* @__PURE__ */ jsx99("button", {
        style: { display: "none" },
        id: "render-modal-button-client",
        onClick: openClientRenderModal,
        type: "button"
      }),
      /* @__PURE__ */ jsx99("button", {
        style: { display: "none" },
        id: "render-modal-button-command",
        onClick: () => openServerRenderModal(true),
        type: "button"
      }),
      /* @__PURE__ */ jsx99(SegmentedButton, {
        segments,
        style: segmentedButtonStyle2,
        title: showRenderLabel ? tooltip : null
      })
    ]
  });
};
var RenderButton = React65.memo(RenderButtonInner);

// src/components/RulersAndGuidesToggle.tsx
import { useCallback as useCallback50, useContext as useContext58 } from "react";
import { jsx as jsx100 } from "react/jsx-runtime";
var buttonStyle8 = { width: 26 };
var RulersAndGuidesToggle = ({ showGuides }) => {
  const { editorShowGuides, setEditorShowGuides } = useContext58(EditorShowGuidesContext);
  const { editorShowRulers, setEditorShowRulers } = useContext58(EditorShowRulersContext);
  const rulersOrGuidesAreVisible = editorShowRulers || showGuides && editorShowGuides;
  const onClick = useCallback50(() => {
    setEditorShowRulers(() => !rulersOrGuidesAreVisible);
    if (showGuides) {
      setEditorShowGuides(() => !rulersOrGuidesAreVisible);
    }
  }, [
    rulersOrGuidesAreVisible,
    setEditorShowGuides,
    setEditorShowRulers,
    showGuides
  ]);
  const shortcut = useKeyboardShortcutLabel("toggleRulersAndGuides");
  const ariaShortcut = useKeyboardShortcutAriaKeyShortcuts("toggleRulersAndGuides");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const accessibilityLabel4 = showGuides ? rulersOrGuidesAreVisible ? "Hide rulers and guides" : "Show rulers and guides" : editorShowRulers ? "Hide rulers" : "Show rulers";
  return /* @__PURE__ */ jsx100(ActionTooltip, {
    label: showGuides ? "Rulers and Guides" : "Rulers",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx100(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel4,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaShortcut || undefined,
      "aria-pressed": rulersOrGuidesAreVisible,
      onClick,
      style: buttonStyle8,
      children: (color) => /* @__PURE__ */ jsx100("svg", {
        xmlns: "http://www.w3.org/2000/svg",
        viewBox: "0 0 576 512",
        style: { width: 18, height: 18 },
        "aria-hidden": "true",
        focusable: "false",
        children: /* @__PURE__ */ jsx100("path", {
          fill: rulersOrGuidesAreVisible ? BLUE : color,
          d: "M525.9 154.2L186.5 493.6c-6.2 6.2-16.4 6.2-22.6 0L50.7 380.5c-6.2-6.2-6.2-16.4 0-22.6l33.9-33.9 56.6 56.6c6.2 6.2 16.4 6.2 22.6 0s6.2-16.4 0-22.6l-56.6-56.6 39.6-39.6 33.9 33.9c6.2 6.2 16.4 6.2 22.6 0s6.2-16.4 0-22.6l-33.9-33.9 39.6-39.6 56.6 56.6c6.2 6.2 16.4 6.2 22.6 0s6.2-16.4 0-22.6l-56.6-56.6 39.6-39.6 33.9 33.9c6.2 6.2 16.4 6.2 22.6 0s6.2-16.4 0-22.6l-33.9-33.9 39.6-39.6 56.6 56.6c6.2 6.2 16.4 6.2 22.6 0s6.2-16.4 0-22.6l-56.6-56.6 33.9-33.9c6.2-6.2 16.4-6.2 22.6 0L525.9 131.5c6.2 6.2 6.2 16.4 0 22.6zM73.3 289.9L28.1 335.2c-18.7 18.7-18.7 49.1 0 67.9L141.2 516.2c18.7 18.7 49.1 18.7 67.9 0L548.5 176.8c18.7-18.7 18.7-49.1 0-67.9L435.4-4.2C416.6-23 386.2-23 367.5-4.2 77.1 286.2 140.3 223 73.3 289.9z"
        })
      })
    })
  });
};

// src/components/SnappingToggle.tsx
import { useCallback as useCallback51, useContext as useContext59 } from "react";

// src/icons/magnet.tsx
import { jsx as jsx101 } from "react/jsx-runtime";
var MagnetIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx101("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 448 512",
    ...props,
    children: /* @__PURE__ */ jsx101("path", {
      fill: color,
      transform: "rotate(180 224 256)",
      d: "M48 64c-8.8 0-16 7.2-16 16l0 80 80 0 0-80c0-8.8-7.2-16-16-16L48 64zM32 288c0 106 86 192 192 192s192-86 192-192l0-96-80 0 0 96c0 61.9-50.1 112-112 112S112 349.9 112 288l0-96-80 0 0 96zM416 160l0-80c0-8.8-7.2-16-16-16l-48 0c-8.8 0-16 7.2-16 16l0 80 80 0zM0 80C0 53.5 21.5 32 48 32l48 0c26.5 0 48 21.5 48 48l0 208c0 44.2 35.8 80 80 80s80-35.8 80-80l0-208c0-26.5 21.5-48 48-48l48 0c26.5 0 48 21.5 48 48l0 208c0 123.7-100.3 224-224 224S0 411.7 0 288L0 80z"
    })
  });
};

// src/components/SnappingToggle.tsx
import { jsx as jsx102 } from "react/jsx-runtime";
var buttonStyle9 = { width: 26 };
var SnappingToggle = () => {
  const { editorSnapping, setEditorSnapping } = useContext59(EditorSnappingContext);
  const onClick = useCallback51(() => {
    setEditorSnapping((current) => !current);
  }, [setEditorSnapping]);
  const shortcut = useKeyboardShortcutLabel("toggleSnapping");
  const ariaKeyShortcuts = useKeyboardShortcutAriaKeyShortcuts("toggleSnapping");
  const accessibilityLabel4 = editorSnapping ? "Disable snapping" : "Enable snapping";
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  return /* @__PURE__ */ jsx102(ActionTooltip, {
    label: "Snapping",
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: false,
    children: /* @__PURE__ */ jsx102(ControlButton, {
      title: "",
      "aria-label": accessibilityLabel4,
      "aria-pressed": editorSnapping,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaKeyShortcuts || undefined,
      onClick,
      style: buttonStyle9,
      children: (color) => /* @__PURE__ */ jsx102(MagnetIcon, {
        style: { width: 18, height: 18, transform: "translateY(1px)" },
        color: editorSnapping ? BLUE : color,
        "aria-hidden": "true",
        focusable: "false"
      })
    })
  });
};

// src/components/PreviewToolbar.tsx
import { jsx as jsx103, jsxs as jsxs54, Fragment as Fragment26 } from "react/jsx-runtime";
var TOOLBAR_HEIGHT = 40;
var container15 = {
  display: "flex",
  position: "relative",
  justifyContent: "center",
  borderTop: BORDER_BLACK_ALPHA_50,
  alignItems: "center",
  flexDirection: "row",
  background: BACKGROUND,
  height: TOOLBAR_HEIGHT
};
var centeredPlayButton = {
  position: "absolute",
  left: "50%",
  top: 0,
  height: TOOLBAR_HEIGHT,
  display: "flex",
  alignItems: "center",
  transform: "translateX(-50%)"
};
var sideContainer = {
  width: 300,
  height: 30,
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var padding = {
  width: TIMELINE_PADDING
};
var toolbarControl = {
  display: "flex",
  flexShrink: 0
};
var PreviewToolbarControl = ({ children }) => {
  const onPointerDown = useCallback52((e) => {
    e.stopPropagation();
  }, []);
  return /* @__PURE__ */ jsx103("div", {
    style: toolbarControl,
    onPointerDown,
    children
  });
};
var PreviewToolbar = ({ readOnlyStudio, bufferStateDelayInMilliseconds }) => {
  const { playbackRate, setPlaybackRate } = Internals58.usePlaybackRate();
  const { playerMuted } = useContext60(Internals58.MediaVolumeContext);
  const { setPlayerMuted } = useContext60(Internals58.SetMediaVolumeContext);
  const { canvasContent } = useContext60(Internals58.CompositionManager);
  const isVideoComposition = useIsVideoComposition();
  const showCompositionControls = canvasContent?.type === "composition";
  const showCanvasViewControls = showCompositionControls || canvasContent?.type === "asset" && getPreviewFileType(canvasContent.asset) === "video";
  const isStill = useIsStill();
  const [loop, setLoop] = useState33(loadLoopOption());
  const isFullscreenSupported = checkFullscreenSupport();
  const isMobileLayout = useMobileLayout();
  const playPause = /* @__PURE__ */ jsx103(PreviewToolbarControl, {
    children: /* @__PURE__ */ jsx103(PlayPause, {
      bufferStateDelayInMilliseconds,
      loop,
      playbackRate,
      muted: playerMuted,
      hideNavigationControls: isMobileLayout
    })
  });
  return /* @__PURE__ */ jsxs54("div", {
    style: container15,
    className: "css-reset",
    children: [
      /* @__PURE__ */ jsxs54("div", {
        style: sideContainer,
        children: [
          /* @__PURE__ */ jsx103("div", {
            style: padding
          }),
          isMobileLayout ? null : /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(SizeSelector, {})
          }),
          !isMobileLayout && (isStill || isVideoComposition) ? /* @__PURE__ */ jsxs54(Fragment26, {
            children: [
              /* @__PURE__ */ jsx103(Spacing, {
                x: 2
              }),
              /* @__PURE__ */ jsx103(PreviewToolbarControl, {
                children: /* @__PURE__ */ jsx103(PlaybackRateSelector, {
                  setPlaybackRate,
                  playbackRate
                })
              })
            ]
          }) : null,
          isMobileLayout ? /* @__PURE__ */ jsxs54(Fragment26, {
            children: [
              /* @__PURE__ */ jsx103(PreviewToolbarControl, {
                children: /* @__PURE__ */ jsx103(PreviewToolbarOverflowButton, {
                  showFullscreen: Boolean(canvasContent && isFullscreenSupported),
                  showPlaybackRate: isVideoComposition,
                  showLoop: isVideoComposition,
                  showCanvasViewControls,
                  showCompositionControls,
                  playbackRate,
                  setPlaybackRate,
                  loop,
                  setLoop
                })
              }),
              /* @__PURE__ */ jsx103(Spacing, {
                x: 1.5
              })
            ]
          }) : null,
          isVideoComposition && isMobileLayout ? /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(MuteToggle, {
              muted: playerMuted,
              setMuted: setPlayerMuted
            })
          }) : null
        ]
      }),
      /* @__PURE__ */ jsx103(Flex, {}),
      isVideoComposition && isMobileLayout ? /* @__PURE__ */ jsx103("div", {
        style: centeredPlayButton,
        children: playPause
      }) : null,
      isVideoComposition && !isMobileLayout ? /* @__PURE__ */ jsxs54(Fragment26, {
        children: [
          /* @__PURE__ */ jsx103(Spacing, {
            x: 2
          }),
          playPause,
          /* @__PURE__ */ jsx103(Spacing, {
            x: 2
          }),
          /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(LoopToggle, {
              loop,
              setLoop
            })
          }),
          /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(MuteToggle, {
              muted: playerMuted,
              setMuted: setPlayerMuted
            })
          }),
          /* @__PURE__ */ jsx103(Spacing, {
            x: 2
          }),
          /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(TimelineInOutPointToggle, {})
          }),
          /* @__PURE__ */ jsx103(Spacing, {
            x: 2
          })
        ]
      }) : null,
      showCanvasViewControls ? /* @__PURE__ */ jsxs54(Fragment26, {
        children: [
          isMobileLayout ? null : /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(CheckboardToggle, {})
          }),
          isMobileLayout || !showCompositionControls ? null : /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(OutlineToggle, {})
          }),
          isMobileLayout ? null : /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(RulersAndGuidesToggle, {
              showGuides: showCompositionControls
            })
          }),
          readOnlyStudio || isMobileLayout || !showCompositionControls ? null : /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(SnappingToggle, {})
          })
        ]
      }) : null,
      /* @__PURE__ */ jsx103(Spacing, {
        x: 2
      }),
      canvasContent && isFullscreenSupported ? /* @__PURE__ */ jsx103(PreviewToolbarControl, {
        children: /* @__PURE__ */ jsx103(FullScreenToggle, {
          hidden: isMobileLayout
        })
      }) : null,
      /* @__PURE__ */ jsx103(Flex, {}),
      /* @__PURE__ */ jsxs54("div", {
        style: sideContainer,
        children: [
          /* @__PURE__ */ jsx103(Flex, {}),
          isMobileLayout ? null : /* @__PURE__ */ jsx103(FpsCounter, {
            playbackSpeed: playbackRate
          }),
          /* @__PURE__ */ jsx103(Spacing, {
            x: 2
          }),
          /* @__PURE__ */ jsx103(PreviewToolbarControl, {
            children: /* @__PURE__ */ jsx103(RenderButton, {
              readOnlyStudio,
              size: "compact",
              narrow: isMobileLayout
            })
          }),
          /* @__PURE__ */ jsx103(Spacing, {
            x: 1.5
          })
        ]
      }),
      /* @__PURE__ */ jsx103(PlaybackKeyboardShortcutsManager, {
        setPlaybackRate,
        setMuted: setPlayerMuted,
        setLoop
      }),
      /* @__PURE__ */ jsx103(PlaybackRatePersistor, {})
    ]
  });
};

// src/components/Splitter/SplitterContainer.tsx
import { PlayerInternals as PlayerInternals12 } from "@remotion/player";
import React70, {
  useContext as useContext62,
  useLayoutEffect as useLayoutEffect16,
  useMemo as useMemo55,
  useRef as useRef28,
  useState as useState35
} from "react";

// src/state/timeline.ts
var localStorageKey2 = (id) => `remotion.editor.timelineFlex.${id}`;
var persistTimelineFlex = (value, id) => {
  localStorage.setItem(localStorageKey2(id), String(value));
};
var loadTimelineFlex = (id) => {
  const item = localStorage.getItem(localStorageKey2(id));
  return item ? parseFloat(item) : null;
};
var useTimelineFlex = (id) => {
  return [
    loadTimelineFlex(id),
    (value) => persistTimelineFlex(value, id)
  ];
};

// src/components/Splitter/SplitterHandle.tsx
import { useContext as useContext61, useEffect as useEffect39, useRef as useRef27 } from "react";
import { jsx as jsx104 } from "react/jsx-runtime";
var SPLITTER_HANDLE_SIZE = 3;
var containerRow = {
  height: SPLITTER_HANDLE_SIZE
};
var collapsedContainer = {
  width: 0,
  height: 0
};
var containerColumn = {
  width: SPLITTER_HANDLE_SIZE
};
var SplitterHandle = ({ allowToCollapse, onCollapse, onCollapseDuringDrag }) => {
  const context = useContext61(SplitterContext);
  if (!context) {
    throw new Error("Cannot find splitter context");
  }
  const ref = useRef27(null);
  const latest = useRef27({
    context,
    allowToCollapse,
    onCollapse,
    onCollapseDuringDrag
  });
  latest.current = { context, allowToCollapse, onCollapse, onCollapseDuringDrag };
  useEffect39(() => {
    const { current } = ref;
    if (!current) {
      return;
    }
    let endDrag = null;
    const onPointerDown = (e) => {
      if (e.button !== 0) {
        return;
      }
      e.stopPropagation();
      const dragContext = latest.current.context;
      const start = { x: e.clientX, y: e.clientY };
      const { width: containerWidth, height: containerHeight } = dragContext.ref.current?.getBoundingClientRect() ?? {
        width: 0,
        height: 0
      };
      const availableSize = (dragContext.orientation === "vertical" ? containerWidth : containerHeight) - SPLITTER_HANDLE_SIZE;
      const { minFlex, maxFlex } = getSplitterFlexBounds({
        availableSize,
        maxAntiFlexerSize: dragContext.maxAntiFlexerSize,
        maxFlex: dragContext.maxFlex,
        maxFlexerSize: dragContext.maxFlexerSize,
        minAntiFlexerSize: dragContext.minAntiFlexerSize,
        minFlex: dragContext.minFlex,
        minFlexerSize: dragContext.minFlexerSize
      });
      const startFlex = Math.min(maxFlex, Math.max(minFlex, dragContext.flexValue));
      dragContext.isDragging.current = start;
      forceSpecificCursor(dragContext.orientation === "horizontal" ? "row-resize" : "col-resize");
      const getNewValue = (ev, clamp) => {
        if (!dragContext.ref.current) {
          throw new Error("domRect is not mounted");
        }
        const { width, height } = dragContext.ref.current.getBoundingClientRect();
        const change = dragContext.orientation === "vertical" ? (ev.clientX - start.x) / (width - SPLITTER_HANDLE_SIZE) : (ev.clientY - start.y) / (height - SPLITTER_HANDLE_SIZE);
        const newFlex = startFlex + change;
        if (clamp) {
          return Math.min(maxFlex, Math.max(minFlex, newFlex));
        }
        return newFlex;
      };
      const getCollapsedSide = (ev) => {
        const collapse = latest.current.allowToCollapse;
        const unclamped = getNewValue(ev, false);
        if (collapse === "left" && unclamped < dragContext.minFlex / 2) {
          return "left";
        }
        if (collapse === "right" && 1 - unclamped < (1 - dragContext.maxFlex) / 2) {
          return "right";
        }
        return null;
      };
      let lastFlex = startFlex;
      let lastCollapsedSide = null;
      const onPointerMove = (ev) => {
        if (!dragContext.isDragging.current) {
          return;
        }
        lastCollapsedSide = getCollapsedSide(ev);
        lastFlex = getNewValue(ev, true);
        latest.current.onCollapseDuringDrag?.(lastCollapsedSide);
        dragContext.setCollapsedDuringDrag(lastCollapsedSide);
        dragContext.setFlexValue(lastFlex);
      };
      endDrag = startCapturedPointerSession({
        event: e,
        captureTarget: current,
        onMove: onPointerMove,
        onEnd: (reason, endEvent) => {
          if (isPointerSessionRelease(reason, endEvent)) {
            lastFlex = getNewValue(endEvent, true);
            lastCollapsedSide = getCollapsedSide(endEvent);
          }
          if (reason !== "manual" && dragContext.isDragging.current) {
            const savedFlex = lastCollapsedSide === null ? lastFlex : startFlex;
            dragContext.setFlexValue(savedFlex);
            dragContext.persistFlex(savedFlex);
            if (lastCollapsedSide !== null) {
              latest.current.onCollapse();
            }
          }
          latest.current.onCollapseDuringDrag?.(null);
          dragContext.setCollapsedDuringDrag(null);
          dragContext.isDragging.current = false;
          stopForcingSpecificCursor();
          endDrag = null;
        }
      });
    };
    current.addEventListener("pointerdown", onPointerDown);
    return () => {
      current.removeEventListener("pointerdown", onPointerDown);
      endDrag?.();
    };
  }, []);
  return /* @__PURE__ */ jsx104("div", {
    ref,
    className: [
      "remotion-splitter",
      context.orientation === "horizontal" ? "remotion-splitter-horizontal" : "remotion-splitter-vertical"
    ].join(" "),
    style: context.collapsedDuringDrag !== null ? collapsedContainer : context.orientation === "horizontal" ? containerRow : containerColumn
  });
};

// src/components/Splitter/SplitterContainer.tsx
import { jsx as jsx105 } from "react/jsx-runtime";
var containerRow2 = {
  display: "flex",
  flexDirection: "row",
  flex: 1,
  height: "100%",
  width: "100%"
};
var containerColumn2 = {
  display: "flex",
  flexDirection: "column",
  flex: 1,
  height: 0
};
var SplitterContainer = ({
  orientation,
  children,
  defaultFlex,
  maxFlex,
  minFlex,
  maxFlexerSize,
  minFlexerSize,
  maxAntiFlexerSize,
  minAntiFlexerSize,
  id
}) => {
  const parentLayout = useContext62(SplitterLayoutContext);
  const [initialTimelineFlex, persistFlex] = useTimelineFlex(id);
  const [flexValue, setFlexValue] = useState35(initialTimelineFlex ?? defaultFlex);
  const [collapsedDuringDrag, setCollapsedDuringDrag] = useState35(null);
  const ref = useRef28(null);
  const isDragging = useRef28(false);
  const size3 = PlayerInternals12.useElementSize(ref, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const availableSize = size3 ? (orientation === "vertical" ? size3.width : size3.height) - SPLITTER_HANDLE_SIZE : null;
  const effectiveFlexValue = getClampedSplitterFlex({
    availableSize,
    flexValue,
    maxAntiFlexerSize,
    maxFlex,
    maxFlexerSize,
    minAntiFlexerSize,
    minFlex,
    minFlexerSize
  });
  const value = useMemo55(() => {
    return {
      flexValue: effectiveFlexValue,
      collapsedDuringDrag,
      setCollapsedDuringDrag,
      ref,
      setFlexValue,
      isDragging,
      orientation,
      id,
      maxFlex,
      minFlex,
      maxFlexerSize,
      minFlexerSize,
      maxAntiFlexerSize,
      minAntiFlexerSize,
      defaultFlex,
      persistFlex
    };
  }, [
    defaultFlex,
    collapsedDuringDrag,
    effectiveFlexValue,
    id,
    maxFlex,
    maxFlexerSize,
    minFlexerSize,
    maxAntiFlexerSize,
    minAntiFlexerSize,
    minFlex,
    orientation,
    persistFlex,
    ref
  ]);
  const childCount = React70.Children.toArray(children).length;
  const layout = useMemo55(() => ({
    parentLayout,
    effectiveFlexValue,
    collapsedDuringDrag,
    orientation,
    width: size3?.width,
    height: size3?.height,
    childCount
  }), [
    parentLayout,
    effectiveFlexValue,
    collapsedDuringDrag,
    orientation,
    size3?.width,
    size3?.height,
    childCount
  ]);
  const refreshSize = size3?.refresh;
  useLayoutEffect16(() => {
    refreshSize?.();
  }, [layout, refreshSize]);
  return /* @__PURE__ */ jsx105(SplitterLayoutContext.Provider, {
    value: layout,
    children: /* @__PURE__ */ jsx105(SplitterContext.Provider, {
      value,
      children: /* @__PURE__ */ jsx105("div", {
        ref,
        style: orientation === "horizontal" ? containerColumn2 : containerRow2,
        children
      })
    })
  });
};

// src/components/Splitter/SplitterElement.tsx
import { useContext as useContext63, useMemo as useMemo56 } from "react";
import { interpolateColors, random as random3 } from "remotion";
import { jsx as jsx106, jsxs as jsxs55, Fragment as Fragment27 } from "react/jsx-runtime";
var SplitterElement = ({ children, type, sticky }) => {
  const context = useContext63(SplitterContext);
  const maxSize = type === "flexer" ? context.maxFlexerSize : context.maxAntiFlexerSize;
  const minSize = type === "flexer" ? context.minFlexerSize : context.minAntiFlexerSize;
  const style4 = useMemo56(() => {
    return {
      flex: (type === "flexer" ? context.flexValue : 1 - context.flexValue) * 1000,
      display: context.collapsedDuringDrag === (type === "flexer" ? "left" : "right") ? "none" : "flex",
      position: "relative",
      overflow: "hidden",
      flexDirection: "column",
      maxWidth: context.orientation === "vertical" ? maxSize ?? undefined : undefined,
      maxHeight: context.orientation === "horizontal" ? maxSize ?? undefined : undefined,
      minWidth: context.orientation === "vertical" ? minSize ?? undefined : undefined,
      minHeight: context.orientation === "horizontal" ? minSize ?? undefined : undefined
    };
  }, [
    context.collapsedDuringDrag,
    context.flexValue,
    context.orientation,
    maxSize,
    minSize,
    type
  ]);
  const stickStyle = useMemo56(() => {
    return {
      position: "absolute",
      left: (type === "flexer" ? 0 : context.flexValue) * 100 + "%",
      width: (type === "flexer" ? context.flexValue : 1 - context.flexValue) * 100 + "%",
      backgroundColor: interpolateColors(random3(context.flexValue), [0, 1], [RED, "blue"])
    };
  }, [context.flexValue, type]);
  return /* @__PURE__ */ jsxs55(Fragment27, {
    children: [
      /* @__PURE__ */ jsx106("div", {
        style: style4,
        children
      }),
      /* @__PURE__ */ jsx106("div", {
        style: stickStyle,
        children: sticky ?? null
      })
    ]
  });
};

// src/components/TopPanel.tsx
import { jsx as jsx107, jsxs as jsxs56 } from "react/jsx-runtime";
var container16 = {
  height: "100%",
  width: "100%",
  display: "flex",
  flexDirection: "column",
  flex: 1
};
var row2 = {
  display: "flex",
  flexDirection: "row",
  flex: 1,
  minHeight: 0
};
var MAX_SIDEBAR_WIDTH = 350;
var MIN_SIDEBAR_WIDTH = 250;
var useResponsiveSidebarStatus = () => {
  const { sidebarCollapsedStateLeft } = useContext64(SidebarContext);
  const isMobileLayout = useMobileLayout();
  const responsiveLeftStatus = useBreakpoint(1200) ? "collapsed" : "expanded";
  const actualStateLeft = useMemo57(() => {
    if (isMobileLayout) {
      return "collapsed";
    }
    if (sidebarCollapsedStateLeft === "collapsed") {
      return "collapsed";
    }
    if (sidebarCollapsedStateLeft === "expanded") {
      return "expanded";
    }
    return responsiveLeftStatus;
  }, [isMobileLayout, sidebarCollapsedStateLeft, responsiveLeftStatus]);
  return actualStateLeft;
};
var TopPanelInner = ({ readOnlyStudio, onMounted, drawRef: drawRef2, bufferStateDelayInMilliseconds }) => {
  const {
    setSidebarCollapsedState,
    sidebarCollapsedStateRight,
    setSidebarCollapsedDuringDrag
  } = useContext64(SidebarContext);
  const rulersAreVisible = useIsRulerVisible();
  const { canvasContent } = useContext64(Internals59.CompositionManager);
  const actualStateLeft = useResponsiveSidebarStatus();
  const actualStateRight = useMemo57(() => {
    if (sidebarCollapsedStateRight === "collapsed") {
      return "collapsed";
    }
    return "expanded";
  }, [sidebarCollapsedStateRight]);
  useEffect40(() => {
    onMounted();
  }, [onMounted]);
  const canvasContainerStyle = useMemo57(() => ({
    flex: 1,
    display: "flex",
    paddingTop: rulersAreVisible ? RULER_WIDTH : 0,
    paddingLeft: rulersAreVisible ? RULER_WIDTH : 0
  }), [rulersAreVisible]);
  const onCollapseLeft = useCallback53(() => {
    setSidebarCollapsedState({ left: "collapsed", right: null });
  }, [setSidebarCollapsedState]);
  const onCollapseRight = useCallback53(() => {
    setSidebarCollapsedState({ left: null, right: "collapsed" });
  }, [setSidebarCollapsedState]);
  return /* @__PURE__ */ jsx107(ObserveDefaultProps, {
    compositionId: canvasContent?.type === "composition" ? canvasContent.compositionId : null,
    readOnlyStudio,
    children: /* @__PURE__ */ jsxs56("div", {
      style: container16,
      children: [
        /* @__PURE__ */ jsx107("div", {
          style: row2,
          children: /* @__PURE__ */ jsxs56(SplitterContainer, {
            minFlex: 0.15,
            maxFlex: 0.4,
            defaultFlex: 0.2,
            maxFlexerSize: MAX_SIDEBAR_WIDTH,
            minFlexerSize: null,
            maxAntiFlexerSize: null,
            minAntiFlexerSize: null,
            id: "sidebar-to-preview",
            orientation: "vertical",
            children: [
              actualStateLeft === "expanded" ? /* @__PURE__ */ jsx107(SplitterElement, {
                sticky: null,
                type: "flexer",
                children: /* @__PURE__ */ jsx107(ExplorerPanel, {
                  readOnlyStudio
                })
              }) : null,
              actualStateLeft === "expanded" ? /* @__PURE__ */ jsx107(SplitterHandle, {
                allowToCollapse: "left",
                onCollapse: onCollapseLeft,
                onCollapseDuringDrag: setSidebarCollapsedDuringDrag
              }) : null,
              /* @__PURE__ */ jsx107(SplitterElement, {
                sticky: null,
                type: "anti-flexer",
                children: /* @__PURE__ */ jsxs56(SplitterContainer, {
                  minFlex: 0.5,
                  maxFlex: 0.8,
                  defaultFlex: 0.7,
                  maxFlexerSize: null,
                  minFlexerSize: null,
                  maxAntiFlexerSize: MAX_SIDEBAR_WIDTH,
                  minAntiFlexerSize: MIN_SIDEBAR_WIDTH,
                  id: "canvas-to-right-sidebar",
                  orientation: "vertical",
                  children: [
                    /* @__PURE__ */ jsx107(SplitterElement, {
                      sticky: null,
                      type: "flexer",
                      children: /* @__PURE__ */ jsx107("div", {
                        ref: drawRef2,
                        style: canvasContainerStyle,
                        children: /* @__PURE__ */ jsx107(CanvasIfSizeIsAvailable, {})
                      })
                    }),
                    actualStateRight === "expanded" ? /* @__PURE__ */ jsx107(SplitterHandle, {
                      allowToCollapse: "right",
                      onCollapse: onCollapseRight,
                      onCollapseDuringDrag: setSidebarCollapsedDuringDrag
                    }) : null,
                    actualStateRight === "expanded" ? /* @__PURE__ */ jsx107(SplitterElement, {
                      sticky: null,
                      type: "anti-flexer",
                      children: /* @__PURE__ */ jsx107(OptionsPanel, {
                        readOnlyStudio
                      })
                    }) : null
                  ]
                })
              })
            ]
          })
        }),
        /* @__PURE__ */ jsx107(PreviewToolbar, {
          bufferStateDelayInMilliseconds,
          readOnlyStudio
        }),
        /* @__PURE__ */ jsx107(TitleUpdater, {})
      ]
    })
  });
};
var TopPanel = React72.memo(TopPanelInner);

// src/components/SidebarCollapserControls.tsx
import { jsx as jsx108 } from "react/jsx-runtime";
var style4 = {
  width: 16,
  height: 16,
  minWidth: 16,
  border: BORDER_CURRENT_COLOR,
  borderRadius: 3,
  color: CURRENT_COLOR,
  position: "relative"
};
var SidebarCollapserControl = ({ side }) => {
  const {
    setSidebarCollapsedState,
    sidebarCollapsedStateRight,
    sidebarCollapsedDuringDrag
  } = useContext65(SidebarContext);
  const keybindings = useKeybinding();
  const leftSidebarStatus = useResponsiveSidebarStatus();
  const leftIcon = useCallback54((color) => {
    return {
      width: "35%",
      height: "100%",
      borderRight: "1px solid " + color,
      background: leftSidebarStatus === "expanded" && sidebarCollapsedDuringDrag !== "left" ? color : TRANSPARENT
    };
  }, [leftSidebarStatus, sidebarCollapsedDuringDrag]);
  const rightIcon = useCallback54((color) => {
    return {
      width: "35%",
      height: "100%",
      right: 0,
      position: "absolute",
      borderLeft: "1px solid " + color,
      background: sidebarCollapsedStateRight === "expanded" && sidebarCollapsedDuringDrag !== "right" ? color : TRANSPARENT
    };
  }, [sidebarCollapsedStateRight, sidebarCollapsedDuringDrag]);
  const toggleLeft = useCallback54(() => {
    setSidebarCollapsedState({
      left: (s) => {
        if (s === "responsive") {
          return leftSidebarStatus === "collapsed" ? "expanded" : "collapsed";
        }
        return s === "collapsed" ? "expanded" : "collapsed";
      },
      right: null
    });
  }, [leftSidebarStatus, setSidebarCollapsedState]);
  const toggleRight = useCallback54(() => {
    setSidebarCollapsedState({
      right: (s) => s === "collapsed" ? "expanded" : "collapsed",
      left: null
    });
  }, [setSidebarCollapsedState]);
  const toggleBoth = useCallback54(() => {
    if (sidebarCollapsedStateRight === leftSidebarStatus) {
      setSidebarCollapsedState({
        left: (s) => {
          if (s === "responsive") {
            return leftSidebarStatus === "collapsed" ? "expanded" : "collapsed";
          }
          return s === "collapsed" ? "expanded" : "collapsed";
        },
        right: (s) => s === "collapsed" ? "expanded" : "collapsed"
      });
    } else if (sidebarCollapsedStateRight === "expanded") {
      toggleRight();
    } else if (leftSidebarStatus === "expanded") {
      toggleLeft();
    }
  }, [
    leftSidebarStatus,
    setSidebarCollapsedState,
    sidebarCollapsedStateRight,
    toggleLeft,
    toggleRight
  ]);
  useEffect41(() => {
    if (side === "left") {
      const left3 = keybindings.registerKeybinding({
        event: "keydown",
        action: "toggleLeftSidebar",
        callback: toggleLeft,
        preventDefault: true,
        triggerIfInputFieldFocused: false,
        keepRegisteredWhenNotHighestContext: false
      });
      const zen = keybindings.registerKeybinding({
        event: "keydown",
        action: "toggleBothSidebars",
        callback: toggleBoth,
        preventDefault: true,
        triggerIfInputFieldFocused: false,
        keepRegisteredWhenNotHighestContext: false
      });
      return () => {
        left3.unregister();
        zen.unregister();
      };
    }
    const right = keybindings.registerKeybinding({
      event: "keydown",
      action: "toggleRightSidebar",
      callback: toggleRight,
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      right.unregister();
    };
  }, [keybindings, side, toggleBoth, toggleLeft, toggleRight]);
  const action = side === "left" ? "toggleLeftSidebar" : "toggleRightSidebar";
  const shortcut = useKeyboardShortcutLabel(action);
  const ariaShortcut = useKeyboardShortcutAriaKeyShortcuts(action);
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  const expanded = (side === "left" ? leftSidebarStatus : sidebarCollapsedStateRight) === "expanded" && sidebarCollapsedDuringDrag !== side;
  const label7 = `${expanded ? "Collapse" : "Expand"} ${side} sidebar`;
  const colorStyle = useCallback54((color) => {
    return {
      ...style4,
      color
    };
  }, []);
  const toggleLeftAction = useCallback54((color) => {
    return /* @__PURE__ */ jsx108("div", {
      "data-sidebar-toggle": "left",
      style: colorStyle(color),
      children: /* @__PURE__ */ jsx108("div", {
        style: leftIcon(color)
      })
    });
  }, [colorStyle, leftIcon]);
  const toggleRightAction = useCallback54((color) => {
    return /* @__PURE__ */ jsx108("div", {
      "data-sidebar-toggle": "right",
      style: colorStyle(color),
      children: /* @__PURE__ */ jsx108("div", {
        style: rightIcon(color)
      })
    });
  }, [colorStyle, rightIcon]);
  return /* @__PURE__ */ jsx108(ActionTooltip, {
    label: label7,
    shortcut: shortcutsDisabled ? null : shortcut,
    delay: 800,
    dismissOnClick: true,
    children: /* @__PURE__ */ jsx108(InlineAction, {
      variant: null,
      onClick: side === "left" ? toggleLeft : toggleRight,
      renderAction: side === "left" ? toggleLeftAction : toggleRightAction,
      unhoveredColor: WHITE_ALPHA_80,
      style: side === "left" ? { marginRight: 4 } : undefined,
      "aria-label": label7,
      "aria-expanded": expanded,
      "aria-keyshortcuts": shortcutsDisabled ? undefined : ariaShortcut || undefined
    })
  });
};

// src/components/UndoRedoButtons.tsx
import {
  useCallback as useCallback55,
  useContext as useContext66,
  useEffect as useEffect42,
  useRef as useRef29,
  useState as useState36
} from "react";

// src/icons/redo.tsx
import { jsx as jsx109 } from "react/jsx-runtime";
var RedoIcon = ({
  color = CURRENT_COLOR,
  ...props
}) => {
  return /* @__PURE__ */ jsx109("svg", {
    viewBox: "0 0 512 512",
    ...props,
    children: /* @__PURE__ */ jsx109("path", {
      fill: color,
      d: "M496 0c-8.8 0-16 7.2-16 16l0 103.8-37-39.1C394.4 29.2 326.8 0 256 0 114.6 0 0 114.6 0 256S114.6 512 256 512c98.3 0 183.6-55.4 226.5-136.5 4.1-7.8 1.1-17.5-6.7-21.6s-17.5-1.1-21.6 6.7C416.6 431.6 341.9 480 256 480 132.3 480 32 379.7 32 256S132.3 32 256 32c62 0 121.2 25.5 163.8 70.6L458.9 144 352 144c-8.8 0-16 7.2-16 16s7.2 16 16 16l144 0c8.8 0 16-7.2 16-16l0-144c0-8.8-7.2-16-16-16z"
    })
  });
};

// src/icons/undo.tsx
import { jsx as jsx110 } from "react/jsx-runtime";
var UndoIcon = ({
  color = CURRENT_COLOR,
  ...props
}) => {
  return /* @__PURE__ */ jsx110("svg", {
    viewBox: "0 0 512 512",
    ...props,
    children: /* @__PURE__ */ jsx110("path", {
      fill: color,
      d: "M16 0c8.8 0 16 7.2 16 16l0 103.8 37-39.1C117.6 29.2 185.2 0 256 0 397.4 0 512 114.6 512 256S397.4 512 256 512c-98.3 0-183.6-55.4-226.5-136.5-4.1-7.8-1.1-17.5 6.7-21.6s17.5-1.1 21.6 6.7C95.4 431.6 170.1 480 256 480 379.7 480 480 379.7 480 256S379.7 32 256 32c-62 0-121.2 25.5-163.8 70.6L53.1 144 160 144c8.8 0 16 7.2 16 16s-7.2 16-16 16L16 176c-8.8 0-16-7.2-16-16L0 16C0 7.2 7.2 0 16 0z"
    })
  });
};

// src/components/UndoRedoButtons.tsx
import { jsx as jsx111, jsxs as jsxs57, Fragment as Fragment28 } from "react/jsx-runtime";
var iconStyle5 = {
  width: 14,
  height: 14
};
var UndoRedoButtons = () => {
  const [undoFile, setUndoFile] = useState36(null);
  const [redoFile, setRedoFile] = useState36(null);
  const { subscribeToEvent } = useContext66(StudioServerConnectionCtx);
  const keybindings = useKeybinding();
  const undoInFlight = useRef29(false);
  const redoInFlight = useRef29(false);
  useEffect42(() => {
    const unsub = subscribeToEvent("undo-redo-stack-changed", (event) => {
      if (event.type !== "undo-redo-stack-changed") {
        return;
      }
      setUndoFile(event.undoFile);
      setRedoFile(event.redoFile);
    });
    return () => unsub();
  }, [subscribeToEvent]);
  const onUndo = useCallback55(() => {
    if (undoInFlight.current) {
      return;
    }
    undoInFlight.current = true;
    const browserStudioOperations = getBrowserStudioOperations();
    const promise = browserStudioOperations ? browserStudioOperations.undo() : callApi("/api/undo", {});
    promise.then((response) => {
      if (response.success && response.route !== null) {
        pushUrl(response.route);
      }
    }).catch(() => {}).finally(() => {
      undoInFlight.current = false;
    });
  }, []);
  const onRedo = useCallback55(() => {
    if (redoInFlight.current) {
      return;
    }
    redoInFlight.current = true;
    const browserStudioOperations = getBrowserStudioOperations();
    const promise = browserStudioOperations ? browserStudioOperations.redo() : callApi("/api/redo", {});
    promise.then((response) => {
      if (response.success && response.route !== null) {
        pushUrl(response.route);
      }
    }).catch(() => {}).finally(() => {
      redoInFlight.current = false;
    });
  }, []);
  useEffect42(() => {
    const undo = keybindings.registerKeybinding({
      event: "keydown",
      action: "undo",
      callback: () => {
        if (undoFile) {
          onUndo();
        }
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: true
    });
    const redo = keybindings.registerKeybinding({
      event: "keydown",
      action: "redo",
      callback: () => {
        if (redoFile) {
          onRedo();
        }
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: true
    });
    return () => {
      undo.unregister();
      redo.unregister();
    };
  }, [keybindings, onRedo, onUndo, redoFile, undoFile]);
  const undoShortcut = useKeyboardShortcutLabel("undo");
  const redoShortcut = useKeyboardShortcutLabel("redo");
  const undoTooltip = areKeyboardShortcutsDisabled() || undoShortcut === "" ? "Undo" : `Undo (${undoShortcut})`;
  const redoTooltip = areKeyboardShortcutsDisabled() || redoShortcut === "" ? "Redo" : `Redo (${redoShortcut})`;
  const renderUndo = useCallback55((color) => {
    return /* @__PURE__ */ jsx111(UndoIcon, {
      style: iconStyle5,
      color
    });
  }, []);
  const renderRedo = useCallback55((color) => {
    return /* @__PURE__ */ jsx111(RedoIcon, {
      style: iconStyle5,
      color
    });
  }, []);
  const canUndo = undoFile !== null;
  const canRedo = redoFile !== null;
  if (!canUndo && !canRedo) {
    return null;
  }
  return /* @__PURE__ */ jsxs57(Fragment28, {
    children: [
      /* @__PURE__ */ jsx111(InlineAction, {
        variant: null,
        onClick: onUndo,
        renderAction: renderUndo,
        title: undoTooltip,
        disabled: !canUndo,
        unhoveredColor: WHITE_ALPHA_80
      }),
      /* @__PURE__ */ jsx111(InlineAction, {
        variant: null,
        onClick: onRedo,
        renderAction: renderRedo,
        title: redoTooltip,
        disabled: !canRedo,
        unhoveredColor: WHITE_ALPHA_80
      })
    ]
  });
};

// src/components/MenuToolbar.tsx
import { jsx as jsx112, jsxs as jsxs58 } from "react/jsx-runtime";
var row3 = {
  alignItems: "center",
  flexDirection: "row",
  display: "flex",
  color: WHITE,
  borderBottom: BORDER_BLACK,
  boxSizing: "border-box",
  fontSize: 13,
  height: MENU_TOOLBAR_HEIGHT,
  paddingLeft: 6,
  paddingRight: 6,
  backgroundColor: BACKGROUND
};
var flex = {
  flex: 1
};
var menuItems = {
  display: "inline-flex",
  height: 24
};
var MenuToolbar = ({ readOnlyStudio }) => {
  const [selected, setSelected] = useState37(null);
  const { previewServerState } = useContext67(StudioServerConnectionCtx);
  const { setSelectedModal } = useContext67(SetSelectedModalContext);
  const browserStudioOperations = getBrowserStudioOperations();
  const canUndoAndRedo = !readOnlyStudio || previewServerState.type === "connected" && browserStudioOperations !== null;
  const showUpdates = canShowUpdates({
    connectionStatus: previewServerState.type,
    isBrowserStudio: browserStudioOperations !== null,
    readOnlyStudio
  });
  const mobileLayout = useMobileLayout();
  const fixedWidthRight = useMemo58(() => {
    return {
      ...mobileLayout ? { width: "fit-content" } : {
        width: "330px"
      },
      display: "flex",
      alignItems: "center",
      justifyContent: "flex-end"
    };
  }, [mobileLayout]);
  const fixedWidthLeft = useMemo58(() => {
    return {
      ...mobileLayout ? { minWidth: "0px" } : {
        minWidth: "330px"
      },
      display: "flex",
      alignItems: "center",
      justifyContent: "flex-start"
    };
  }, [mobileLayout]);
  const itemClicked = useCallback56((itemId) => {
    setSelected(itemId);
  }, [setSelected]);
  const itemHovered = useCallback56((itemId) => {
    if (selected) {
      setSelected(itemId);
    }
  }, [selected, setSelected]);
  const closeMenu = useCallback56(() => {
    setSelected(null);
  }, []);
  const structure = useMenuStructure(closeMenu, readOnlyStudio);
  const menus = useMemo58(() => {
    return structure.map((s) => s.id);
  }, [structure]);
  const onPreviousMenu = useCallback56(() => {
    setSelected((s) => {
      if (s === null) {
        return null;
      }
      return menus[(menus.indexOf(s) + 1) % menus.length];
    });
  }, [menus]);
  const onNextMenu = useCallback56(() => {
    setSelected((s) => {
      if (s === null) {
        return null;
      }
      if (menus.indexOf(s) === 0) {
        return menus[menus.length - 1];
      }
      return menus[(menus.indexOf(s) - 1) % menus.length];
    });
  }, [menus]);
  const onItemQuit = useCallback56(() => {
    setSelected(null);
  }, [setSelected]);
  const onPointerDown = useCallback56((e) => {
    e.stopPropagation();
  }, []);
  const openQuickSwitcher = useCallback56(() => {
    setSelectedModal({
      type: "quick-switcher",
      mode: "compositions",
      invocationTimestamp: Date.now(),
      assetSelection: null,
      compositionSelection: null
    });
  }, [setSelectedModal]);
  const renderSearchIcon = useCallback56((color) => {
    return /* @__PURE__ */ jsx112(SearchIcon, {
      color,
      width: 18,
      height: 18
    });
  }, []);
  const quickSwitcherShortcut = useKeyboardShortcutLabel("quickSwitcher");
  const quickSwitcherAriaShortcut = useKeyboardShortcutAriaKeyShortcuts("quickSwitcher");
  const shortcutsDisabled = areKeyboardShortcutsDisabled();
  return /* @__PURE__ */ jsxs58(Row, {
    align: "center",
    className: "css-reset",
    style: row3,
    onPointerDown,
    children: [
      /* @__PURE__ */ jsxs58("div", {
        style: fixedWidthLeft,
        children: [
          mobileLayout ? /* @__PURE__ */ jsx112(ActionTooltip, {
            label: "Quick switcher",
            shortcut: shortcutsDisabled ? null : quickSwitcherShortcut,
            delay: 800,
            dismissOnClick: true,
            children: /* @__PURE__ */ jsx112(InlineAction, {
              variant: null,
              onClick: openQuickSwitcher,
              renderAction: renderSearchIcon,
              "aria-label": "Quick switcher",
              "aria-keyshortcuts": shortcutsDisabled ? undefined : quickSwitcherAriaShortcut || undefined
            })
          }) : /* @__PURE__ */ jsx112(SidebarCollapserControl, {
            side: "left"
          }),
          /* @__PURE__ */ jsx112("div", {
            style: menuItems,
            children: structure.map((s) => {
              return /* @__PURE__ */ jsx112(MenuItem, {
                selected: selected === s.id,
                onItemSelected: itemClicked,
                onItemHovered: itemHovered,
                id: s.id,
                label: s.label,
                onItemQuit,
                menu: s,
                onPreviousMenu,
                onNextMenu,
                leaveLeftPadding: s.leaveLeftPadding
              }, s.id);
            })
          })
        ]
      }),
      mobileLayout ? null : /* @__PURE__ */ jsx112("div", {
        style: flex
      }),
      /* @__PURE__ */ jsx112(MenuBuildIndicator, {
        mobileLayout
      }),
      /* @__PURE__ */ jsx112("div", {
        style: flex
      }),
      /* @__PURE__ */ jsxs58("div", {
        style: fixedWidthRight,
        children: [
          canUndoAndRedo ? /* @__PURE__ */ jsx112(UndoRedoButtons, {}) : null,
          /* @__PURE__ */ jsx112(SettingsButton, {
            showUpdates
          }),
          /* @__PURE__ */ jsx112(SidebarCollapserControl, {
            side: "right"
          })
        ]
      })
    ]
  });
};

// src/components/Timeline/Timeline.tsx
import React106, {
  useCallback as useCallback72,
  useContext as useContext92,
  useLayoutEffect as useLayoutEffect25,
  useMemo as useMemo86,
  useRef as useRef48,
  useState as useState53
} from "react";
import { Internals as Internals90 } from "remotion";

// src/components/Timeline/SequencePropsObserver.tsx
import { useContext as useContext68, useEffect as useEffect43, useLayoutEffect as useLayoutEffect17, useRef as useRef30 } from "react";
import { Internals as Internals61 } from "remotion";

// src/components/Timeline/sequence-props-subscription-store.ts
import { getAllSchemaKeys, getAssetSchemaKeys } from "@remotion/studio-shared";
import { Internals as Internals60 } from "remotion";
var makeKey = ({
  fileName,
  line,
  column,
  componentIdentity,
  sequenceKeys,
  assetKeys,
  effectKeys,
  videoConfigValues,
  stack: stack2
}) => `${fileName}\x00${line}\x00${column}\x00${componentIdentity ?? ""}\x00${sequenceKeys.join("\x00")}\x00${assetKeys.join("\x00")}\x00${effectKeys.map((keys) => keys.join("\x00")).join("\x00\x00")}\x00${JSON.stringify(videoConfigValues)}\x00${stack2 ?? ""}`;
var entries = new Map;
var refreshListeners = new Map;
var subscribeToSequencePropsRefresh = (overrideId, listener) => {
  const listeners2 = refreshListeners.get(overrideId) ?? new Set;
  listeners2.add(listener);
  refreshListeners.set(overrideId, listeners2);
  return () => {
    listeners2.delete(listener);
    if (listeners2.size === 0) {
      refreshListeners.delete(overrideId);
    }
  };
};
var refreshSequencePropsSubscription = (overrideId) => {
  for (const listener of refreshListeners.get(overrideId) ?? []) {
    listener();
  }
};
var acquireSequencePropsSubscription = ({
  fileName,
  line,
  column,
  schema,
  componentIdentity,
  effects,
  nodePath,
  clientId,
  applyOnce,
  applyEach,
  videoConfigValues,
  stack: stack2
}) => {
  const sequenceKeys = getAllSchemaKeys(schema);
  const assetKeys = getAssetSchemaKeys(schema);
  const effectKeys = effects.map((effect) => getAllSchemaKeys(effect));
  const key7 = makeKey({
    fileName,
    line,
    column,
    componentIdentity,
    sequenceKeys,
    assetKeys,
    effectKeys,
    videoConfigValues,
    stack: stack2
  });
  let entry = entries.get(key7);
  if (!entry) {
    const promise = subscribeToSequenceProps({
      fileName,
      line,
      column,
      nodePath,
      componentIdentity,
      keys: getAllSchemaKeys(schema),
      assetKeys,
      effects: effectKeys,
      clientId,
      videoConfigValues
    });
    const created = {
      refCount: 0,
      promise,
      fileName,
      clientId,
      applyOnce
    };
    entries.set(key7, created);
    entry = created;
    promise.then((result) => {
      const current = entries.get(key7);
      if (current !== created || !current.applyOnce) {
        return;
      }
      const cb = current.applyOnce;
      current.applyOnce = null;
      cb(result);
    }).catch((err) => {
      const current = entries.get(key7);
      if (current !== created) {
        return;
      }
      current.applyOnce = null;
      Internals60.Log.error(err);
    });
  }
  entry.refCount++;
  const acquired = entry;
  acquired.promise.then(applyEach).catch(() => {});
  let released = false;
  return {
    release: () => {
      if (released) {
        return;
      }
      released = true;
      acquired.refCount--;
      if (acquired.refCount > 0) {
        return;
      }
      if (entries.get(key7) === acquired) {
        entries.delete(key7);
      }
      acquired.promise.then((result) => {
        if (!result.success) {
          return;
        }
        return unsubscribeFromSequenceProps({
          fileName: acquired.fileName,
          nodePath: result.nodePath,
          clientId: acquired.clientId,
          sequenceKeys,
          assetKeys,
          effectKeys
        });
      }).catch(() => {});
    }
  };
};

// src/components/Timeline/SequencePropsObserver.tsx
var SequencePropsObserver = () => {
  const { subscribeToEvent } = useContext68(StudioServerConnectionCtx);
  const { remapPropStatuses, setPropStatuses } = useContext68(Internals61.VisualModeSettersContext);
  const propStatusesRef = useContext68(Internals61.VisualModePropStatusesRefContext);
  const { overrideIdToNodePathMappings } = useContext68(Internals61.OverrideIdsToNodePathsGettersContext);
  const overrideIdToNodePathMappingsRef = useRef30(overrideIdToNodePathMappings);
  overrideIdToNodePathMappingsRef.current = overrideIdToNodePathMappings;
  const { setOverrideIdToNodePath } = useContext68(Internals61.OverrideIdsToNodePathsSettersContext);
  const { fastRefreshes } = useContext68(FastRefreshContext);
  const { migrateExpandedTracksForSubscriptionKey } = useContext68(ExpandedTracksSetterContext);
  const { remapSelectionNodePaths } = useTimelineSelection();
  useEffect43(() => {
    const handleEvent = (event) => {
      if (event.type !== "sequence-props-updated") {
        return;
      }
      setPropStatuses(event.nodePath, () => event.result);
    };
    const unsubscribe = subscribeToEvent("sequence-props-updated", handleEvent);
    return () => {
      unsubscribe();
    };
  }, [setPropStatuses, subscribeToEvent]);
  useLayoutEffect17(() => {
    const mutations = takePendingSequenceNodePathMutations();
    if (mutations.length === 0) {
      return;
    }
    remapSelectionNodePaths(mutations);
    const statusRemappings = [];
    const overrideUpdates = [];
    const overrideIdsToRefresh = new Set;
    for (const [overrideId, previousNodePath] of Object.entries(overrideIdToNodePathMappingsRef.current)) {
      let { nodePath } = previousNodePath;
      let wasRemapped = false;
      let wasDeleted = false;
      let runtimeNodePathExists = true;
      let runtimeNodePathNeedsRefresh = false;
      const previousNodePathString = JSON.stringify(previousNodePath.nodePath);
      for (const mutation of mutations) {
        for (const file of mutation.files) {
          if (file.absolutePath !== previousNodePath.absolutePath) {
            continue;
          }
          const runtimeNodePathIsSource = file.remappings.some((item) => item.oldNodePath !== null && JSON.stringify(item.oldNodePath) === previousNodePathString);
          const runtimeNodePathIsDestination = file.remappings.some((item) => item.newNodePath !== null && JSON.stringify(item.newNodePath) === previousNodePathString);
          const runtimeNodePathWasInserted = file.remappings.some((item) => item.oldNodePath === null && item.newNodePath !== null && JSON.stringify(item.newNodePath) === previousNodePathString);
          const runtimeNodePathWasUpdated = file.remappings.some((item) => item.oldNodePath !== null && item.newNodePath !== null && JSON.stringify(item.oldNodePath) === previousNodePathString && JSON.stringify(item.newNodePath) === previousNodePathString);
          if (runtimeNodePathIsSource) {
            runtimeNodePathExists = runtimeNodePathIsDestination;
            runtimeNodePathNeedsRefresh = runtimeNodePathWasInserted || runtimeNodePathWasUpdated;
          } else if (runtimeNodePathIsDestination) {
            runtimeNodePathExists = true;
            runtimeNodePathNeedsRefresh = runtimeNodePathWasInserted || runtimeNodePathWasUpdated;
          }
          if (wasDeleted) {
            continue;
          }
          const remapping = file.remappings.find((item) => item.oldNodePath !== null && JSON.stringify(item.oldNodePath) === JSON.stringify(nodePath));
          if (!remapping) {
            continue;
          }
          wasRemapped = true;
          if (remapping.newNodePath === null) {
            wasDeleted = true;
            continue;
          }
          nodePath = remapping.newNodePath;
        }
      }
      if (!wasRemapped) {
        continue;
      }
      const nextNodePath = wasDeleted ? null : { ...previousNodePath, nodePath };
      const previousStatusKey = Internals61.makeSequencePropsSubscriptionKey(previousNodePath);
      statusRemappings.push({
        previousNodePath,
        nodePath: nextNodePath,
        result: propStatusesRef.current[previousStatusKey] ?? null
      });
      overrideUpdates.push({
        overrideId,
        nodePath: runtimeNodePathExists ? previousNodePath : null
      });
      if (runtimeNodePathExists && runtimeNodePathNeedsRefresh) {
        overrideIdsToRefresh.add(overrideId);
      }
    }
    remapPropStatuses(statusRemappings);
    for (const event of overrideUpdates) {
      setOverrideIdToNodePath(event.overrideId, event.nodePath);
    }
    for (const overrideId of overrideIdsToRefresh) {
      refreshSequencePropsSubscription(overrideId);
    }
    for (const event of statusRemappings) {
      if (event.nodePath === null) {
        continue;
      }
      migrateExpandedTracksForSubscriptionKey(event.previousNodePath, event.nodePath);
    }
  }, [
    fastRefreshes,
    migrateExpandedTracksForSubscriptionKey,
    propStatusesRef,
    remapSelectionNodePaths,
    remapPropStatuses,
    setOverrideIdToNodePath
  ]);
  return null;
};

// src/components/Timeline/should-show-track-in-timeline.ts
var shouldShowTrackInTimeline = (track, durationInFrames) => {
  if (!track.sequence.showInTimeline) {
    return false;
  }
  if (track.sequence.duration <= 0) {
    return false;
  }
  if (track.sequence.from > durationInFrames) {
    return false;
  }
  return true;
};

// src/components/Timeline/should-subscribe-to-sequence-props.ts
var shouldSubscribeToSequenceProps = (sequence, previewConnected) => {
  return sequence.showInTimeline && Boolean(sequence.controls) && previewConnected && Boolean(sequence.getStack());
};

// src/components/Timeline/SubscribeToNodePaths.tsx
import { useMemo as useMemo60 } from "react";
import { NoReactInternals as NoReactInternals11 } from "remotion/no-react";

// src/components/Timeline/use-sequence-props-subscription.ts
import {
  getAllSchemaKeys as getAllSchemaKeys2,
  stringifySequenceSubscriptionKey
} from "@remotion/studio-shared";
import { useContext as useContext69, useEffect as useEffect44, useMemo as useMemo59, useRef as useRef31, useState as useState38 } from "react";
import { Internals as Internals63 } from "remotion";

// src/components/Timeline/should-subscribe-to-source-file.ts
var shouldSubscribeToSourceFile = (source) => {
  const normalized = source.replaceAll("\\", "/");
  if (normalized === ".." || normalized.startsWith("../")) {
    return false;
  }
  return !normalized.split("/").includes("node_modules");
};

// src/components/Timeline/use-sequence-props-subscription.ts
var useSequencePropsSubscription = ({
  originalLocation,
  overrideId,
  componentIdentity,
  schema,
  effects,
  preferMappedNodePath,
  stack: stack2,
  videoConfigValues
}) => {
  const { setPropStatuses } = useContext69(Internals63.VisualModeSettersContext);
  const { setOverrideIdToNodePath } = useContext69(Internals63.OverrideIdsToNodePathsSettersContext);
  const { overrideIdToNodePathMappings } = useContext69(Internals63.OverrideIdsToNodePathsGettersContext);
  const { migrateExpandedTracksForSubscriptionKey } = useContext69(ExpandedTracksSetterContext);
  const { previewServerState: state } = useContext69(StudioServerConnectionCtx);
  const [refreshToken, setRefreshToken] = useState38(0);
  const previousNodePathRef = useRef31(null);
  const overrideIdToNodePathMappingsRef = useRef31(overrideIdToNodePathMappings);
  overrideIdToNodePathMappingsRef.current = overrideIdToNodePathMappings;
  const clientId = state.type === "connected" ? state.clientId : undefined;
  const effectsSignature = useMemo59(() => effects.map((effect) => getAllSchemaKeys2(effect).join("\x00")).join("\x00\x00"), [effects]);
  const validatedLocation = useMemo59(() => {
    if (!originalLocation || !originalLocation.source || !originalLocation.line) {
      return null;
    }
    return {
      source: originalLocation.source,
      line: originalLocation.line,
      column: originalLocation.column ?? 0
    };
  }, [originalLocation]);
  const locationSource = validatedLocation?.source ?? null;
  const locationLine = validatedLocation?.line ?? null;
  const locationColumn = validatedLocation?.column ?? null;
  useEffect44(() => {
    return subscribeToSequencePropsRefresh(overrideId, () => {
      setRefreshToken((token) => token + 1);
    });
  }, [overrideId]);
  useEffect44(() => {
    if (!clientId || !locationSource || !shouldSubscribeToSourceFile(locationSource) || !locationLine || locationColumn === null || !schema || videoConfigValues === null) {
      return;
    }
    const nodePathAtResubscribe = overrideIdToNodePathMappingsRef.current[overrideId] ?? null;
    const { release } = acquireSequencePropsSubscription({
      fileName: locationSource,
      line: locationLine,
      column: locationColumn,
      schema,
      componentIdentity,
      effects,
      nodePath: preferMappedNodePath ? nodePathAtResubscribe?.nodePath ?? null : null,
      clientId,
      stack: stack2,
      videoConfigValues,
      applyOnce: (result) => {
        if (!result.success) {
          return;
        }
        setPropStatuses(result.nodePath, () => result.status);
      },
      applyEach: (result) => {
        if (!result.success) {
          return;
        }
        const newNodePath = result.nodePath;
        const newNodePathKey = stringifySequenceSubscriptionKey(newNodePath);
        const previousNodePath = previousNodePathRef.current ?? nodePathAtResubscribe;
        const previousNodePathKey = previousNodePath ? stringifySequenceSubscriptionKey(previousNodePath) : null;
        if (previousNodePathKey === newNodePathKey) {
          return;
        }
        if (previousNodePath) {
          migrateExpandedTracksForSubscriptionKey(previousNodePath, newNodePath);
        }
        previousNodePathRef.current = newNodePath;
        setOverrideIdToNodePath(overrideId, newNodePath);
      }
    });
    return () => {
      release();
    };
  }, [
    clientId,
    componentIdentity,
    effects,
    effectsSignature,
    locationColumn,
    locationLine,
    locationSource,
    migrateExpandedTracksForSubscriptionKey,
    overrideId,
    preferMappedNodePath,
    refreshToken,
    schema,
    setPropStatuses,
    setOverrideIdToNodePath,
    stack2,
    videoConfigValues
  ]);
};

// src/components/Timeline/SubscribeToNodePaths.tsx
var SubscribeToNodePaths = ({
  overrideId,
  componentIdentity,
  schema,
  getStack,
  effects,
  videoConfigValues
}) => {
  const { resolvedLocation, stack: stack2, preferMappedNodePath } = useResolveStackAndReactToChange(getStack, overrideId);
  const effectSubscriptions = useMemo60(() => {
    return effects.map((effect) => {
      return effect.schema;
    }).filter(NoReactInternals11.truthy);
  }, [effects]);
  useSequencePropsSubscription({
    overrideId,
    componentIdentity,
    schema,
    effects: effectSubscriptions,
    originalLocation: resolvedLocation,
    preferMappedNodePath,
    stack: stack2,
    videoConfigValues
  });
  return null;
};

// src/components/Timeline/timeline-asset-drop-context.ts
import { createContext as createContext11 } from "react";
var TimelineAssetDropFrameContext = createContext11(null);

// src/components/Timeline/timeline-display-groups.ts
var addTimelineDisplayGroups = (tracks) => {
  const stacksBySequenceId = new Map;
  const resolvedNodePathKeysByStack = new Map;
  for (const track of tracks) {
    const stack2 = track.sequence.controls === null ? null : track.sequence.getStack();
    stacksBySequenceId.set(track.sequence.id, stack2);
    if (stack2 === null || track.nodePathInfo === null) {
      continue;
    }
    const nodePathKey = `node-path:${timelineSequenceNodePathToKey(track.nodePathInfo.sequenceSubscriptionKey)}`;
    const nodePathKeys = resolvedNodePathKeysByStack.get(stack2) ?? new Set;
    nodePathKeys.add(nodePathKey);
    resolvedNodePathKeysByStack.set(stack2, nodePathKeys);
  }
  const displayKeys = tracks.map((track) => {
    if (track.nodePathInfo !== null) {
      return `node-path:${timelineSequenceNodePathToKey(track.nodePathInfo.sequenceSubscriptionKey)}`;
    }
    const stack2 = stacksBySequenceId.get(track.sequence.id) ?? null;
    if (stack2 === null) {
      return null;
    }
    const resolvedNodePathKeys = resolvedNodePathKeysByStack.get(stack2);
    if (resolvedNodePathKeys?.size === 1) {
      const resolvedNodePathKey = resolvedNodePathKeys.values().next().value;
      if (resolvedNodePathKey === undefined) {
        throw new Error("Expected a resolved timeline node path key");
      }
      return resolvedNodePathKey;
    }
    return `stack:${stack2}`;
  });
  const counts = new Map;
  for (const key7 of displayKeys) {
    if (key7 !== null) {
      counts.set(key7, (counts.get(key7) ?? 0) + 1);
    }
  }
  return tracks.map((track, index) => {
    const key7 = displayKeys[index];
    if (key7 === null) {
      return { ...track, displayGroup: null };
    }
    const numberOfSequences = counts.get(key7);
    if (numberOfSequences === undefined) {
      throw new Error("Expected a timeline display group count");
    }
    return {
      ...track,
      displayGroup: {
        key: key7,
        numberOfSequences
      }
    };
  });
};

// src/components/Timeline/TimelineDragHandler.tsx
import { PlayerInternals as PlayerInternals15 } from "@remotion/player";
import React80, {
  useCallback as useCallback59,
  useContext as useContext73,
  useEffect as useEffect47,
  useMemo as useMemo65,
  useRef as useRef36,
  useState as useState40
} from "react";
import { Internals as Internals68, useVideoConfig as useVideoConfig8 } from "remotion";

// src/components/Timeline/TimelineTimeIndicators.tsx
import React79, { useContext as useContext72, useLayoutEffect as useLayoutEffect18, useMemo as useMemo63, useRef as useRef35 } from "react";
import { Internals as Internals67 } from "remotion";

// src/components/TimeValue.tsx
import { PlayerInternals as PlayerInternals14 } from "@remotion/player";
import {
  useCallback as useCallback58,
  useContext as useContext71,
  useEffect as useEffect46,
  useImperativeHandle as useImperativeHandle6,
  useRef as useRef33
} from "react";
import { Internals as Internals66, useCurrentFrame as useCurrentFrame5 } from "remotion";

// src/components/Timeline/TimelineTickFormatProvider.tsx
import { createContext as createContext12, useEffect as useEffect45, useMemo as useMemo61, useState as useState39 } from "react";
import { jsx as jsx113 } from "react/jsx-runtime";
var STORAGE_KEY = "remotion.timelineShowFrames";
var TimelineTickFormatContext = createContext12({ showFrames: false, setShowFrames: () => {
  return;
} });
var TimelineTickFormatProvider = ({ children }) => {
  const [showFrames, setShowFrames] = useState39(() => localStorage.getItem(STORAGE_KEY) === "true");
  useEffect45(() => {
    localStorage.setItem(STORAGE_KEY, String(showFrames));
  }, [showFrames]);
  const value = useMemo61(() => ({ showFrames, setShowFrames }), [showFrames]);
  return /* @__PURE__ */ jsx113(TimelineTickFormatContext.Provider, {
    value,
    children
  });
};

// src/components/Timeline/TimelineZoomControls.tsx
import { PlayerInternals as PlayerInternals13 } from "@remotion/player";
import React77, { useCallback as useCallback57, useContext as useContext70 } from "react";
import { Internals as Internals65 } from "remotion";
import { jsx as jsx114, jsxs as jsxs59 } from "react/jsx-runtime";
var TIMELINE_ZOOM_BUTTON_FACTOR = 1.2;
var container17 = {
  color: BLACK,
  flexDirection: "row",
  display: "flex",
  alignItems: "center"
};
var buttonStyle10 = {
  fontSize: 24
};
var TimelineZoomSlider = ({ maxWidth, minZoom, timelineViewportWidth }) => {
  const { setZoom, zoom: zoomMap } = useContext70(TimelineZoomCtx);
  const videoConfig = Internals65.useUnsafeVideoConfig();
  const { tabIndex } = useZIndex();
  const isVideoComposition = useIsVideoComposition();
  const onChange = useCallback57((e) => {
    if (videoConfig === null) {
      return;
    }
    setZoom(videoConfig.id, () => sliderValueToTimelineZoom({
      sliderValue: Number(e.target.value),
      minZoom
    }), {
      anchorFrame: null,
      anchorContentX: null
    });
  }, [minZoom, setZoom, videoConfig]);
  if (!isVideoComposition || videoConfig === null) {
    return null;
  }
  const zoom = getTimelineZoom({
    durationInFrames: videoConfig?.durationInFrames ?? 1,
    timelineViewportWidth,
    zoom: zoomMap[videoConfig.id] ?? null
  });
  const roundedZoom = Math.round(zoom * 100) / 100;
  return /* @__PURE__ */ jsx114("input", {
    style: maxWidth === undefined ? undefined : { maxWidth },
    title: `Timeline zoom (${roundedZoom}px/frame)`,
    alt: `Timeline zoom (${roundedZoom}px/frame)`,
    type: "range",
    min: TIMELINE_ZOOM_SLIDER_PROPS.min,
    max: TIMELINE_ZOOM_SLIDER_PROPS.max,
    step: TIMELINE_ZOOM_SLIDER_PROPS.step,
    value: timelineZoomToSliderValue({ zoom, minZoom }),
    onChange,
    className: "__remotion-timeline-slider",
    tabIndex
  });
};
var TimelineZoomControlsInner = ({ sliderMaxWidth }) => {
  const { setZoom } = useContext70(TimelineZoomCtx);
  const videoConfig = Internals65.useUnsafeVideoConfig();
  const timelineSize = PlayerInternals13.useElementSize(scrollableRef, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const timelineViewportWidth = timelineSize?.width ?? scrollableRef.current?.clientWidth ?? 0;
  const minZoom = getTimelineMinZoom({
    durationInFrames: videoConfig?.durationInFrames ?? 1,
    timelineViewportWidth
  });
  const onMinusClicked = useCallback57(() => {
    if (videoConfig === null) {
      return;
    }
    setZoom(videoConfig.id, (z2) => z2 / TIMELINE_ZOOM_BUTTON_FACTOR, {
      anchorFrame: null,
      anchorContentX: null
    });
  }, [setZoom, videoConfig]);
  const onPlusClicked = useCallback57(() => {
    if (videoConfig === null) {
      return;
    }
    setZoom(videoConfig.id, (z2) => z2 * TIMELINE_ZOOM_BUTTON_FACTOR, {
      anchorFrame: null,
      anchorContentX: null
    });
  }, [setZoom, videoConfig]);
  const isVideoComposition = useIsVideoComposition();
  if (!isVideoComposition || videoConfig === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs59("div", {
    style: container17,
    children: [
      /* @__PURE__ */ jsx114(ActionTooltip, {
        label: "Zoom out",
        shortcut: null,
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx114(ControlButton, {
          onClick: onMinusClicked,
          style: buttonStyle10,
          title: "",
          "aria-label": "Zoom out timeline",
          type: "button",
          children: (color) => /* @__PURE__ */ jsx114(CanvasZoomOutIcon, {
            color
          })
        })
      }),
      /* @__PURE__ */ jsx114(Spacing, {
        x: 0.5
      }),
      /* @__PURE__ */ jsx114(TimelineZoomSlider, {
        maxWidth: sliderMaxWidth,
        minZoom,
        timelineViewportWidth
      }),
      /* @__PURE__ */ jsx114(Spacing, {
        x: 0.5
      }),
      /* @__PURE__ */ jsx114(ActionTooltip, {
        label: "Zoom in",
        shortcut: null,
        delay: 800,
        dismissOnClick: true,
        children: /* @__PURE__ */ jsx114(ControlButton, {
          onClick: onPlusClicked,
          style: buttonStyle10,
          title: "",
          "aria-label": "Zoom in timeline",
          type: "button",
          children: (color) => /* @__PURE__ */ jsx114(CanvasZoomIcon, {
            color
          })
        })
      })
    ]
  });
};
var TimelineZoomControls = React77.memo(TimelineZoomControlsInner);

// src/components/TimeValue.tsx
import { jsx as jsx115, jsxs as jsxs60 } from "react/jsx-runtime";
var text = {
  color: WHITE,
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  fontVariantNumeric: "tabular-nums",
  lineHeight: 1,
  width: "100%"
};
var currentTimeTypography = {
  color: LIGHT_TEXT,
  display: "inline-block",
  fontSize: 14,
  fontVariantNumeric: "tabular-nums",
  fontWeight: 400,
  lineHeight: 1,
  fontFamily: "monospace"
};
var currentTimeInputStyle = {
  ...currentTimeTypography,
  padding: 0
};
var currentTimeStack = {
  display: "flex",
  flexDirection: "column",
  alignItems: "flex-start",
  lineHeight: 1,
  padding: "5px 7px 5px 1px",
  transform: "translateY(-1px)"
};
var currentTimeButtonStyle = {
  display: "block",
  padding: 0,
  border: "none",
  lineHeight: "21px",
  "--remotion-cli-internals-blue-hovered": WHITE
};
var currentTimeSubtitle = {
  ...hoverableStyle({
    idleBackground: TRANSPARENT,
    hoverBackground: TRANSPARENT,
    idleColor: LIGHT_TEXT,
    hoverColor: WHITE
  }),
  background: TRANSPARENT,
  border: "none",
  padding: 0,
  cursor: "default",
  display: "block",
  fontFamily: "monospace",
  fontSize: 10,
  fontWeight: 400,
  lineHeight: 1,
  marginTop: -2,
  textAlign: "left"
};
var TimeValue = () => {
  const frame2 = useCurrentFrame5();
  const { showFrames, setShowFrames } = useContext71(TimelineTickFormatContext);
  const toggleTickFormat = useCallback58(() => {
    setShowFrames((previous) => !previous);
  }, [setShowFrames]);
  const config = Internals66.useUnsafeVideoConfig();
  const isStill = useIsStill();
  const { seek: seek2, play: play2, pause: pause2, toggle } = PlayerInternals14.usePlayerMethods();
  const keybindings = useKeybinding();
  const ref = useRef33(null);
  const onTextChange = useCallback58((newVal) => {
    seek2(parseInt(newVal, 10));
  }, [seek2]);
  const onValueChange = useCallback58((val) => {
    seek2(val);
  }, [seek2]);
  const formatter = useCallback58((value) => {
    return config ? renderFrame(Number(value), config.fps) : String(value);
  }, [config]);
  useImperativeHandle6(Internals66.timeValueRef, () => ({
    goToFrame: () => {
      ref.current?.click();
    },
    seek: seek2,
    play: play2,
    pause: pause2,
    toggle
  }), [seek2, play2, pause2, toggle]);
  useEffect46(() => {
    const gKey = keybindings.registerKeybinding({
      event: "keydown",
      action: "goToFrame",
      callback: () => {
        ref.current?.click();
      },
      preventDefault: true,
      triggerIfInputFieldFocused: false,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      gKey.unregister();
    };
  }, [keybindings]);
  if (!config) {
    return null;
  }
  if (isStill) {
    return null;
  }
  return /* @__PURE__ */ jsxs60("div", {
    style: text,
    children: [
      /* @__PURE__ */ jsxs60("div", {
        style: currentTimeStack,
        children: [
          /* @__PURE__ */ jsx115(InputDragger, {
            ref,
            "aria-label": String(frame2),
            value: frame2,
            onTextChange,
            onValueChange,
            formatter,
            formatterStyle: currentTimeTypography,
            buttonStyle: currentTimeButtonStyle,
            rightAlign: false,
            status: "ok",
            style: currentTimeInputStyle
          }),
          /* @__PURE__ */ jsx115("button", {
            type: "button",
            className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
            style: currentTimeSubtitle,
            onClick: toggleTickFormat,
            "aria-label": "Show timeline ticks as frames",
            "aria-pressed": showFrames,
            title: showFrames ? "Show timeline ticks as timecode" : "Show timeline ticks as frames",
            children: frame2
          })
        ]
      }),
      /* @__PURE__ */ jsx115(Spacing, {
        x: 2
      }),
      /* @__PURE__ */ jsx115(Flex, {}),
      /* @__PURE__ */ jsx115(TimelineZoomControls, {
        sliderMaxWidth: 80
      }),
      /* @__PURE__ */ jsx115(Spacing, {
        x: 0.5
      })
    ]
  });
};

// src/components/Timeline/TimelineTimeIndicators.tsx
import { jsx as jsx116 } from "react/jsx-runtime";
var TIMELINE_TIME_INDICATOR_HEIGHT = 39;
var container18 = {
  height: TIMELINE_TIME_INDICATOR_HEIGHT,
  position: "absolute",
  backgroundColor: TIMELINE_TICKS_BACKGROUND,
  top: 0,
  borderBottom: `${TIMELINE_ITEM_BORDER_BOTTOM}px solid ${TIMELINE_TRACK_SEPARATOR}`
};
var TICK_LABEL_FONT_SIZE = 12;
var TICK_LABEL_MARGIN_LEFT = 8;
var TICK_LABEL_MIN_GAP = 16;
var MIN_SPACING_BETWEEN_FRAME_TICKS_PX = 5;
var MIN_SPACING_BETWEEN_TIME_TICKS_PX = 16;
var MIN_SPACING_BETWEEN_MEDIUM_FRAME_TICKS_PX = 24;
var MIN_SPACING_BETWEEN_MEDIUM_TIME_TICKS_PX = 48;
var timeValue = {
  height: TIMELINE_TIME_INDICATOR_HEIGHT,
  position: "absolute",
  top: 0,
  width: "calc(100% + 1.5px)",
  paddingLeft: 10,
  display: "flex",
  alignItems: "center",
  backgroundColor: BACKGROUND,
  borderBottom: `${TIMELINE_ITEM_BORDER_BOTTOM}px solid ${TIMELINE_TRACK_SEPARATOR}`
};
var TimelineTimePlaceholders = () => {
  return /* @__PURE__ */ jsx116("div", {
    style: timeValue,
    children: /* @__PURE__ */ jsx116(TimeValue, {})
  });
};
var NICE_SECOND_INTERVALS = [
  1,
  2,
  5,
  10,
  15,
  30,
  60,
  2 * 60,
  3 * 60,
  5 * 60,
  10 * 60,
  15 * 60,
  20 * 60,
  30 * 60,
  60 * 60
];
var getNiceSecondInterval = (rawNthSecond) => {
  for (const n of NICE_SECOND_INTERVALS) {
    if (n >= rawNthSecond) {
      return n;
    }
  }
  return Math.ceil(rawNthSecond / 3600) * 3600;
};
var getIntegerDivisors = (value) => {
  const lowerDivisors = [];
  const upperDivisors = [];
  for (let candidate = 1;candidate <= Math.sqrt(value); candidate++) {
    if (value % candidate !== 0) {
      continue;
    }
    lowerDivisors.push(candidate);
    if (candidate !== value / candidate) {
      upperDivisors.unshift(value / candidate);
    }
  }
  return [...lowerDivisors, ...upperDivisors];
};
var getFrameIntervals = (fps) => {
  if (Number.isInteger(fps)) {
    return getIntegerDivisors(fps);
  }
  return [1, 2, 5, 10, 15, 20, 30, 60].filter((interval) => interval < fps);
};
var getSecondIntervals = (labelEverySeconds) => {
  const hourlyIntervals = getIntegerDivisors(labelEverySeconds / 3600).map((hours) => hours * 3600);
  return [...new Set([...NICE_SECOND_INTERVALS, ...hourlyIntervals])].sort((a, b) => a - b);
};
var getTimelineTickScale = ({
  fps,
  frameInterval,
  rawSecondMarkerEveryNth
}) => {
  const labelEverySeconds = getNiceSecondInterval(rawSecondMarkerEveryNth);
  const frameIntervals = getFrameIntervals(fps);
  const rawFrameInterval = MIN_SPACING_BETWEEN_FRAME_TICKS_PX / frameInterval;
  const minorFrameInterval = frameIntervals.find((interval) => interval >= rawFrameInterval && interval < fps);
  if (minorFrameInterval !== undefined) {
    const rawMediumFrameInterval = MIN_SPACING_BETWEEN_MEDIUM_FRAME_TICKS_PX / frameInterval;
    const mediumFrameInterval = frameIntervals.find((interval) => interval >= rawMediumFrameInterval && interval < fps && interval % minorFrameInterval === 0);
    return {
      labelEverySeconds,
      mediumTickEvery: mediumFrameInterval === undefined ? null : { interval: mediumFrameInterval, unit: "frames" },
      minorTickEvery: { interval: minorFrameInterval, unit: "frames" }
    };
  }
  const pixelsPerSecond = frameInterval * fps;
  const secondIntervals = getSecondIntervals(labelEverySeconds);
  const rawMinorSecondInterval = MIN_SPACING_BETWEEN_TIME_TICKS_PX / pixelsPerSecond;
  const minorSecondInterval = secondIntervals.find((interval) => interval >= rawMinorSecondInterval && interval < labelEverySeconds && labelEverySeconds % interval === 0);
  if (minorSecondInterval === undefined) {
    return {
      labelEverySeconds,
      mediumTickEvery: null,
      minorTickEvery: null
    };
  }
  const rawMediumSecondInterval = MIN_SPACING_BETWEEN_MEDIUM_TIME_TICKS_PX / pixelsPerSecond;
  const mediumSecondInterval = secondIntervals.find((interval) => interval >= rawMediumSecondInterval && interval < labelEverySeconds && interval % minorSecondInterval === 0 && labelEverySeconds % interval === 0);
  return {
    labelEverySeconds,
    mediumTickEvery: mediumSecondInterval === undefined ? null : { interval: mediumSecondInterval, unit: "seconds" },
    minorTickEvery: { interval: minorSecondInterval, unit: "seconds" }
  };
};
var TimelineTimeIndicators = () => {
  const sliderTrack = useContext72(TimelineWidthContext);
  const video = Internals67.useVideo();
  if (sliderTrack === null) {
    return null;
  }
  if (video === null) {
    return null;
  }
  return /* @__PURE__ */ jsx116(TimelineTimeIndicatorsInner, {
    durationInFrames: video.durationInFrames,
    fps: video.fps,
    windowWidth: sliderTrack
  });
};
var TimelineTimeIndicatorsInner = React79.memo(({ windowWidth, durationInFrames, fps }) => {
  const canvasRef2 = useRef35(null);
  const { showFrames } = useContext72(TimelineTickFormatContext);
  useLayoutEffect18(() => {
    const canvas = canvasRef2.current;
    const scrollable = scrollableRef.current;
    if (!canvas || !scrollable) {
      return;
    }
    const context = canvas.getContext("2d");
    if (!context) {
      return;
    }
    const frameInterval = getFrameIncrementFromWidth(durationInFrames, windowWidth);
    const maxTickLabelWidth = (showFrames ? `${durationInFrames - 1}f` : renderFrame(durationInFrames - 1, fps)).length * TICK_LABEL_FONT_SIZE * 0.6;
    const tickScale = getTimelineTickScale({
      fps,
      frameInterval,
      rawSecondMarkerEveryNth: (TICK_LABEL_MARGIN_LEFT + maxTickLabelWidth + TICK_LABEL_MIN_GAP) / (frameInterval * fps)
    });
    const draw = () => {
      const { clientWidth: width, scrollLeft } = scrollable;
      const pixelRatio = window.devicePixelRatio;
      const canvasWidth = Math.ceil(width * pixelRatio);
      const canvasHeight = TIMELINE_TIME_INDICATOR_HEIGHT * pixelRatio;
      if (canvas.width !== canvasWidth || canvas.height !== canvasHeight) {
        canvas.width = canvasWidth;
        canvas.height = canvasHeight;
        canvas.style.width = `${width}px`;
        canvas.style.height = `${TIMELINE_TIME_INDICATOR_HEIGHT}px`;
      }
      context.resetTransform();
      context.clearRect(0, 0, canvas.width, canvas.height);
      context.scale(pixelRatio, pixelRatio);
      context.fillStyle = WHITE_ALPHA_15;
      const firstFrame = Math.max(0, Math.floor((scrollLeft - TIMELINE_PADDING) / frameInterval));
      const lastFrame = Math.min(durationInFrames - 1, Math.ceil((scrollLeft + width - TIMELINE_PADDING) / frameInterval));
      const xForFrame = (frame2) => Math.round(frameInterval * frame2 + TIMELINE_PADDING - scrollLeft) + 0.5;
      const drawTick = (frame2, height) => {
        const x = xForFrame(frame2);
        context.fillRect(x, 0, 1, height);
      };
      const firstLabelSecond = Math.ceil(firstFrame / fps / tickScale.labelEverySeconds) * tickScale.labelEverySeconds;
      const seconds = Math.floor(durationInFrames / fps);
      for (let second = firstLabelSecond;second < seconds && second * fps <= lastFrame; second += tickScale.labelEverySeconds) {
        const frame2 = second * fps;
        drawTick(frame2, 15);
        if (second > 0) {
          context.fillStyle = LIGHT_TEXT;
          context.font = `${TICK_LABEL_FONT_SIZE}px ${getComputedStyle(canvas).fontFamily}`;
          context.textBaseline = "top";
          context.fillText(showFrames ? `${Math.round(frame2)}f` : renderFrame(frame2, fps), xForFrame(frame2) + TICK_LABEL_MARGIN_LEFT, 7);
          context.fillStyle = WHITE_ALPHA_15;
        }
      }
      const { minorTickEvery, mediumTickEvery } = tickScale;
      if (minorTickEvery?.unit === "frames") {
        const firstTick = Math.ceil(firstFrame / minorTickEvery.interval) * minorTickEvery.interval;
        for (let frame2 = firstTick;frame2 <= lastFrame; frame2 += minorTickEvery.interval) {
          if (frame2 % (tickScale.labelEverySeconds * fps) === 0) {
            continue;
          }
          drawTick(frame2, Number.isInteger(fps) && frame2 % fps === 0 || mediumTickEvery?.unit === "frames" && frame2 % mediumTickEvery.interval === 0 ? 5 : 2);
        }
      }
      if (minorTickEvery?.unit === "seconds") {
        const firstSecond = Math.ceil(firstFrame / fps / minorTickEvery.interval) * minorTickEvery.interval;
        for (let second = firstSecond;second < seconds && second * fps <= lastFrame; second += minorTickEvery.interval) {
          if (second % tickScale.labelEverySeconds === 0) {
            continue;
          }
          drawTick(second * fps, mediumTickEvery?.unit === "seconds" && second % mediumTickEvery.interval === 0 ? 5 : 2);
        }
      }
    };
    const onScroll = () => {
      draw();
    };
    const resizeObserver = new ResizeObserver(draw);
    draw();
    scrollable.addEventListener("scroll", onScroll);
    resizeObserver.observe(scrollable);
    return () => {
      scrollable.removeEventListener("scroll", onScroll);
      resizeObserver.disconnect();
    };
  }, [durationInFrames, fps, showFrames, windowWidth]);
  const style5 = useMemo63(() => {
    return {
      ...container18,
      width: windowWidth,
      minWidth: "100%",
      overflow: "hidden",
      pointerEvents: "none"
    };
  }, [windowWidth]);
  return /* @__PURE__ */ jsx116("div", {
    style: style5,
    children: /* @__PURE__ */ jsx116("canvas", {
      ref: canvasRef2,
      style: { position: "absolute", top: 0, left: 0 }
    })
  });
});

// src/components/Timeline/TimelineDragHandler.tsx
import { jsx as jsx117 } from "react/jsx-runtime";
var inner = {
  overflowY: "auto",
  overflowX: "hidden"
};
var container19 = {
  userSelect: "none",
  WebkitUserSelect: "none",
  position: "absolute",
  height: "100%",
  top: 0
};
var style5 = {
  width: "100%",
  height: "100%",
  userSelect: "none",
  WebkitUserSelect: "none",
  position: "absolute"
};
var getClientXWithScroll = (x) => {
  return x + scrollableRef.current?.scrollLeft;
};
var TimelineDragHandler = () => {
  const video = Internals68.useUnsafeVideoConfig();
  const timelineSize = PlayerInternals15.useElementSize(scrollableRef, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const { zoom: zoomMap } = useContext73(TimelineZoomCtx);
  const { canvasContent, currentAssetMetadata } = useContext73(Internals68.CompositionManager);
  const videoId = video?.id;
  const containerStyle2 = useMemo65(() => {
    if (!canvasContent) {
      return {};
    }
    const durationInFrames = video?.durationInFrames ?? 1;
    const zoom = getTimelineZoom({
      durationInFrames,
      timelineViewportWidth: timelineSize?.width ?? scrollableRef.current?.clientWidth ?? 0,
      zoom: videoId ? zoomMap[videoId] ?? null : null
    });
    return {
      ...container19,
      width: getTimelineWidth({ durationInFrames, zoom }),
      height: TIMELINE_TIME_INDICATOR_HEIGHT
    };
  }, [
    canvasContent,
    timelineSize?.width,
    video?.durationInFrames,
    videoId,
    zoomMap
  ]);
  const hasPlayableContent = canvasContent?.type === "composition" || canvasContent?.type === "asset" && currentAssetMetadata?.asset === canvasContent.asset;
  if (!hasPlayableContent) {
    return null;
  }
  return /* @__PURE__ */ jsx117("div", {
    ref: sliderAreaRef,
    style: containerStyle2,
    ...{ [TIMELINE_SCRUBBER_ATTR]: true },
    children: video && isStudioSelectionEnabled() ? /* @__PURE__ */ jsx117(TimelineDragHandlerInnerMemo, {}) : null
  });
};
var TimelineDragHandlerInner = () => {
  const videoConfig = useVideoConfig8();
  const size3 = PlayerInternals15.useElementSize(scrollableRef, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const { isHighestContext } = useZIndex();
  const setFrame = Internals68.useTimelineSetFrame();
  const width = getTimelineContentWidth();
  const left3 = size3?.left ?? 0;
  const [dragging, setDragging] = useState40({
    dragging: false
  });
  const { isPlaying, play: play2, pause: pause2, seek: seek2 } = PlayerInternals15.usePlayerMethods();
  const autoScroller = useRef36(null);
  const onPointerDown = useCallback59((e) => {
    if (e.button !== 0) {
      return;
    }
    if (!isHighestContext) {
      return;
    }
    if (!videoConfig) {
      return;
    }
    e.stopPropagation();
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    const frame2 = getFrameFromX({
      clientX: getClientXWithScroll(e.clientX) - left3,
      durationInFrames: videoConfig.durationInFrames,
      width,
      extrapolate: "clamp"
    });
    seek2(frame2);
    setDragging({
      dragging: true,
      wasPlaying: isPlaying(),
      button: e.button,
      pointerId: e.pointerId,
      target: e.currentTarget
    });
    e.currentTarget.setPointerCapture?.(e.pointerId);
    pause2();
  }, [isHighestContext, videoConfig, left3, width, seek2, isPlaying, pause2]);
  const onEdgeScrollTick = useCallback59((directions) => {
    if (!videoConfig || directions.x === null) {
      return;
    }
    const nextFrame = directions.x === "left" ? getFrameWhileScrollingLeft({
      durationInFrames: videoConfig.durationInFrames,
      width
    }) : getFrameWhileScrollingRight({
      durationInFrames: videoConfig.durationInFrames,
      width
    });
    const scrollPos = directions.x === "left" ? getScrollPositionForCursorOnLeftEdge({
      nextFrame,
      durationInFrames: videoConfig.durationInFrames
    }) : getScrollPositionForCursorOnRightEdge({
      nextFrame,
      durationInFrames: videoConfig.durationInFrames
    });
    setCurrentFrame(nextFrame);
    scrollToTimelineXOffset(scrollPos);
    redrawTimelineSliderFast.current?.draw(nextFrame);
    seek2(nextFrame);
  }, [videoConfig, width, seek2]);
  const onPointerMoveScrubbing = useCallback59((e) => {
    if (!videoConfig) {
      return;
    }
    if (!dragging.dragging) {
      return;
    }
    const directions = autoScroller.current?.update(e) ?? {
      x: null,
      y: null
    };
    if (directions.x !== null) {
      return;
    }
    const frame2 = getFrameFromX({
      clientX: getClientXWithScroll(e.clientX) - left3,
      durationInFrames: videoConfig.durationInFrames,
      width,
      extrapolate: "clamp"
    });
    seek2(frame2);
  }, [videoConfig, dragging.dragging, left3, width, seek2]);
  const onPointerUpScrubbing = useCallback59((e) => {
    autoScroller.current?.stop();
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    if (!videoConfig) {
      return;
    }
    if (!dragging.dragging) {
      return;
    }
    setDragging({
      dragging: false
    });
    const frame2 = getFrameFromX({
      clientX: getClientXWithScroll(e.clientX) - left3,
      durationInFrames: videoConfig.durationInFrames,
      width,
      extrapolate: "clamp"
    });
    setFrame((c) => {
      if (c[videoConfig.id] === frame2) {
        return c;
      }
      const newObj = { ...c, [videoConfig.id]: frame2 };
      Internals68.persistCurrentFrame(newObj);
      return newObj;
    });
    if (dragging.wasPlaying) {
      play2();
    }
  }, [dragging, left3, play2, videoConfig, setFrame, width]);
  const onPointerCancelScrubbing = useCallback59(() => {
    autoScroller.current?.stop();
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    if (!dragging.dragging) {
      return;
    }
    setDragging({ dragging: false });
    if (dragging.wasPlaying) {
      play2();
    }
  }, [dragging, play2]);
  useEffect47(() => {
    if (!dragging.dragging) {
      return;
    }
    const scroller = startTimelineEdgeAutoScroll({
      includeHorizontal: true,
      includeVertical: false,
      verticalTopOffset: 0,
      onTick: onEdgeScrollTick
    });
    autoScroller.current = scroller;
    const endSession = startCapturedPointerSession({
      event: dragging,
      captureTarget: dragging.target,
      onMove: onPointerMoveScrubbing,
      onEnd: (reason, endEvent) => {
        if (isPointerSessionRelease(reason, endEvent)) {
          onPointerUpScrubbing(endEvent);
        } else {
          onPointerCancelScrubbing();
        }
      }
    });
    return () => {
      scroller.stop();
      if (autoScroller.current === scroller) {
        autoScroller.current = null;
      }
      endSession();
    };
  }, [
    dragging,
    onEdgeScrollTick,
    onPointerCancelScrubbing,
    onPointerMoveScrubbing,
    onPointerUpScrubbing
  ]);
  const ref = useRef36(null);
  useEffect47(() => {
    const currentRef = ref.current;
    if (!currentRef) {
      return;
    }
    const { current } = timelineVerticalScroll;
    if (!current) {
      return;
    }
    const onScroll = () => {
      currentRef.style.top = current.scrollTop + "px";
    };
    current.addEventListener("scroll", onScroll);
    return () => {
      current.removeEventListener("scroll", onScroll);
    };
  }, []);
  return /* @__PURE__ */ jsx117("div", {
    ref,
    style: { ...style5, minWidth: size3?.width ?? 0 },
    onPointerDown,
    children: /* @__PURE__ */ jsx117("div", {
      style: inner,
      className: VERTICAL_SCROLLBAR_CLASSNAME
    })
  });
};
var TimelineDragHandlerInnerMemo = React80.memo(TimelineDragHandlerInner);

// src/components/Timeline/TimelineHeightContainer.tsx
import React83, { useCallback as useCallback61, useMemo as useMemo68 } from "react";

// ../../node_modules/.bun/@tanstack+react-virtual@3.14.9+67f6792bdf102c28/node_modules/@tanstack/react-virtual/dist/esm/index.js
import * as React81 from "react";
import { flushSync } from "react-dom";

// ../../node_modules/.bun/@tanstack+virtual-core@3.17.7/node_modules/@tanstack/virtual-core/dist/esm/lazy-measurements.js
function createLazyMeasurementsView(count, flat, getItemKey) {
  const cache2 = new Array(count);
  return new Proxy(cache2, {
    get(target, prop, receiver) {
      if (typeof prop === "string") {
        const c = prop.charCodeAt(0);
        if (c >= 48 && c <= 57) {
          const i = +prop;
          if (Number.isInteger(i) && i >= 0 && i < count) {
            let v = target[i];
            if (!v) {
              const s = flat[i * 2];
              v = target[i] = {
                index: i,
                key: getItemKey(i),
                start: s,
                size: flat[i * 2 + 1],
                end: s + flat[i * 2 + 1],
                lane: 0
              };
            }
            return v;
          }
        }
        if (prop === "length")
          return count;
      }
      return Reflect.get(target, prop, receiver);
    }
  });
}

// ../../node_modules/.bun/@tanstack+virtual-core@3.17.7/node_modules/@tanstack/virtual-core/dist/esm/utils.js
function memo2(getDeps, fn, opts) {
  let deps = opts.initialDeps ?? [];
  let result;
  let isInitial = true;
  function memoizedFunction() {
    var _a;
    const debugEnabled = false;
    let depTime = 0;
    if (debugEnabled)
      depTime = Date.now();
    const newDeps = getDeps();
    const depsChanged = newDeps.length !== deps.length || newDeps.some((dep, index) => deps[index] !== dep);
    if (!depsChanged) {
      return result;
    }
    deps = newDeps;
    let resultTime = 0;
    if (debugEnabled)
      resultTime = Date.now();
    result = fn(...newDeps);
    if (debugEnabled) {
      const depEndTime = Math.round((Date.now() - depTime) * 100) / 100;
      const resultEndTime = Math.round((Date.now() - resultTime) * 100) / 100;
      const resultFpsPercentage = resultEndTime / 16;
      const pad = (str, num) => {
        str = String(str);
        while (str.length < num) {
          str = " " + str;
        }
        return str;
      };
      console.info(`%c⏱ ${pad(resultEndTime, 5)} /${pad(depEndTime, 5)} ms`, `
            font-size: .6rem;
            font-weight: bold;
            color: hsl(${Math.max(0, Math.min(120 - 120 * resultFpsPercentage, 120))}deg 100% 31%);`, opts == null ? undefined : opts.key);
    }
    if ((opts == null ? undefined : opts.onChange) && !(isInitial && opts.skipInitialOnChange)) {
      opts.onChange(result);
    }
    isInitial = false;
    return result;
  }
  memoizedFunction.updateDeps = (newDeps) => {
    deps = newDeps;
  };
  return memoizedFunction;
}
function notUndefined(value, msg) {
  if (value === undefined) {
    throw new Error(`Unexpected undefined${msg ? `: ${msg}` : ""}`);
  } else {
    return value;
  }
}
var approxEqual = (a, b) => Math.abs(a - b) < 1.01;
var debounce = (targetWindow, fn, ms) => {
  let timeoutId;
  return function(...args) {
    targetWindow.clearTimeout(timeoutId);
    timeoutId = targetWindow.setTimeout(() => fn.apply(this, args), ms);
  };
};

// ../../node_modules/.bun/@tanstack+virtual-core@3.17.7/node_modules/@tanstack/virtual-core/dist/esm/index.js
var _isIOSResult;
var isIOSWebKit = () => {
  if (_isIOSResult !== undefined)
    return _isIOSResult;
  if (typeof navigator === "undefined")
    return _isIOSResult = false;
  if (/iP(hone|od|ad)/.test(navigator.userAgent))
    return _isIOSResult = true;
  const mtp = navigator.maxTouchPoints;
  return _isIOSResult = navigator.platform === "MacIntel" && mtp !== undefined && mtp > 0;
};
var getRect = (element) => {
  const { offsetWidth, offsetHeight } = element;
  return { width: offsetWidth, height: offsetHeight };
};
var defaultKeyExtractor = (index) => index;
var defaultRangeExtractor = (range) => {
  const start = Math.max(range.startIndex - range.overscan, 0);
  const end = Math.min(range.endIndex + range.overscan, range.count - 1);
  const len = end - start + 1;
  const arr = new Array(len);
  for (let i = 0;i < len; i++) {
    arr[i] = start + i;
  }
  return arr;
};
var observeElementRect = (instance, cb) => {
  const element = instance.scrollElement;
  if (!element) {
    return;
  }
  const targetWindow = instance.targetWindow;
  if (!targetWindow) {
    return;
  }
  const handler = (rect) => {
    const { width, height } = rect;
    cb({ width: Math.round(width), height: Math.round(height) });
  };
  handler(getRect(element));
  if (!targetWindow.ResizeObserver) {
    return () => {};
  }
  const observer = new targetWindow.ResizeObserver((entries2) => {
    const run = () => {
      const entry = entries2[0];
      if (entry == null ? undefined : entry.borderBoxSize) {
        const box = entry.borderBoxSize[0];
        if (box) {
          handler({ width: box.inlineSize, height: box.blockSize });
          return;
        }
      }
      handler(getRect(element));
    };
    instance.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
  });
  observer.observe(element, { box: "border-box" });
  return () => {
    observer.unobserve(element);
  };
};
var addEventListenerOptions = {
  passive: true
};
var supportsScrollend = typeof window == "undefined" ? true : ("onscrollend" in window);
var observeOffset = (instance, cb, readOffset) => {
  const element = instance.scrollElement;
  if (!element) {
    return;
  }
  const targetWindow = instance.targetWindow;
  if (!targetWindow) {
    return;
  }
  const registerScrollendEvent = instance.options.useScrollendEvent && supportsScrollend;
  let offset = 0;
  const fallback = registerScrollendEvent ? null : debounce(targetWindow, () => cb(offset, false), instance.options.isScrollingResetDelay);
  const createHandler = (isScrolling) => () => {
    offset = readOffset(element);
    fallback == null || fallback();
    cb(offset, isScrolling);
  };
  const handler = createHandler(true);
  const endHandler = createHandler(false);
  element.addEventListener("scroll", handler, addEventListenerOptions);
  if (registerScrollendEvent) {
    element.addEventListener("scrollend", endHandler, addEventListenerOptions);
  }
  return () => {
    element.removeEventListener("scroll", handler);
    if (registerScrollendEvent) {
      element.removeEventListener("scrollend", endHandler);
    }
  };
};
var observeElementOffset = (instance, cb) => observeOffset(instance, cb, (el) => {
  const { horizontal, isRtl } = instance.options;
  return horizontal ? el.scrollLeft * (isRtl && -1 || 1) : el.scrollTop;
});
var measureElement = (element, entry, instance) => {
  if (instance.options.useCachedMeasurements) {
    const index = instance.indexFromElement(element);
    const key7 = instance.options.getItemKey(index);
    return instance.itemSizeCache.get(key7) ?? instance.options.estimateSize(index);
  }
  if (entry == null ? undefined : entry.borderBoxSize) {
    const box = entry.borderBoxSize[0];
    if (box) {
      const size3 = Math.round(box[instance.options.horizontal ? "inlineSize" : "blockSize"]);
      return size3;
    }
  }
  if (!entry) {
    const index = instance.indexFromElement(element);
    const key7 = instance.options.getItemKey(index);
    const cachedSize = instance.itemSizeCache.get(key7);
    if (cachedSize !== undefined) {
      return cachedSize;
    }
  }
  return element[instance.options.horizontal ? "offsetWidth" : "offsetHeight"];
};
var scrollWithAdjustments = (offset, {
  adjustments = 0,
  behavior
}, instance) => {
  var _a, _b;
  (_b = (_a = instance.scrollElement) == null ? undefined : _a.scrollTo) == null || _b.call(_a, {
    [instance.options.horizontal ? "left" : "top"]: offset + adjustments,
    behavior
  });
};
var elementScroll = scrollWithAdjustments;

class Virtualizer {
  constructor(opts) {
    this.unsubs = [];
    this.scrollElement = null;
    this.targetWindow = null;
    this.isScrolling = false;
    this.scrollState = null;
    this.measurementsCache = [];
    this._flatMeasurements = null;
    this.itemSizeCache = /* @__PURE__ */ new Map;
    this.itemSizeCacheVersion = 0;
    this.laneAssignments = /* @__PURE__ */ new Map;
    this.pendingMin = null;
    this.prevLanes = undefined;
    this.lanesChangedFlag = false;
    this.lanesSettling = false;
    this.pendingScrollAnchor = null;
    this.scrollRect = null;
    this.scrollOffset = null;
    this.scrollDirection = null;
    this.scrollAdjustments = 0;
    this._iosDeferredAdjustment = 0;
    this._iosTouching = false;
    this._iosJustTouchEnded = false;
    this._iosTouchEndTimerId = null;
    this._intendedScrollOffset = null;
    this.elementsCache = /* @__PURE__ */ new Map;
    this.now = () => {
      var _a, _b, _c;
      return ((_c = (_b = (_a = this.targetWindow) == null ? undefined : _a.performance) == null ? undefined : _b.now) == null ? undefined : _c.call(_b)) ?? Date.now();
    };
    this.observer = /* @__PURE__ */ (() => {
      let _ro = null;
      const get = () => {
        if (_ro) {
          return _ro;
        }
        if (!this.targetWindow || !this.targetWindow.ResizeObserver) {
          return null;
        }
        return _ro = new this.targetWindow.ResizeObserver((entries2) => {
          entries2.forEach((entry) => {
            const run = () => {
              const node = entry.target;
              const index = this.indexFromElement(node);
              if (!node.isConnected) {
                this.observer.unobserve(node);
                for (const [cacheKey, cachedNode] of this.elementsCache) {
                  if (cachedNode === node) {
                    this.elementsCache.delete(cacheKey);
                    break;
                  }
                }
                return;
              }
              if (this.shouldMeasureDuringScroll(index)) {
                this.resizeItem(index, this.options.measureElement(node, entry, this));
              }
            };
            this.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
          });
        });
      };
      return {
        disconnect: () => {
          var _a;
          (_a = get()) == null || _a.disconnect();
          _ro = null;
        },
        observe: (target) => {
          var _a;
          return (_a = get()) == null ? undefined : _a.observe(target, { box: "border-box" });
        },
        unobserve: (target) => {
          var _a;
          return (_a = get()) == null ? undefined : _a.unobserve(target);
        }
      };
    })();
    this.range = null;
    this.setOptions = (opts2) => {
      var _a, _b;
      const merged = {
        debug: false,
        initialOffset: 0,
        overscan: 1,
        paddingStart: 0,
        paddingEnd: 0,
        scrollPaddingStart: 0,
        scrollPaddingEnd: 0,
        horizontal: false,
        getItemKey: defaultKeyExtractor,
        rangeExtractor: defaultRangeExtractor,
        onChange: () => {},
        measureElement,
        initialRect: { width: 0, height: 0 },
        scrollMargin: 0,
        gap: 0,
        indexAttribute: "data-index",
        initialMeasurementsCache: [],
        lanes: 1,
        anchorTo: "start",
        followOnAppend: false,
        scrollEndThreshold: 1,
        isScrollingResetDelay: 150,
        enabled: true,
        isRtl: false,
        useScrollendEvent: false,
        useAnimationFrameWithResizeObserver: false,
        laneAssignmentMode: "estimate",
        useCachedMeasurements: false
      };
      for (const key7 in opts2) {
        const v = opts2[key7];
        if (v !== undefined)
          merged[key7] = v;
      }
      const prevOptions = this.options;
      let anchor = null;
      let followOnAppend = null;
      let edgeKeysChanged = false;
      if (prevOptions !== undefined && prevOptions.enabled && merged.enabled && merged.anchorTo === "end" && this.scrollElement !== null) {
        const prevCount = prevOptions.count;
        const nextCount = merged.count;
        const measurements = this.getMeasurements();
        const prevFirstKey = prevCount > 0 ? ((_a = measurements[0]) == null ? undefined : _a.key) ?? prevOptions.getItemKey(0) : null;
        const prevLastKey = prevCount > 0 ? ((_b = measurements[prevCount - 1]) == null ? undefined : _b.key) ?? prevOptions.getItemKey(prevCount - 1) : null;
        const didCountChange = nextCount !== prevCount;
        const didEdgeKeysChange = didCountChange || prevCount > 0 && nextCount > 0 && (merged.getItemKey(0) !== prevFirstKey || merged.getItemKey(nextCount - 1) !== prevLastKey);
        if (didEdgeKeysChange) {
          edgeKeysChanged = true;
          const item = prevCount > 0 ? this.getVirtualItemForOffset(this.getScrollOffset()) ?? measurements[0] : null;
          if (item) {
            anchor = [item.key, this.getScrollOffset() - item.start];
          }
          const behavior = merged.followOnAppend === true ? "auto" : merged.followOnAppend || null;
          if (behavior && nextCount > prevCount && this.isAtEnd(prevOptions.scrollEndThreshold) && (prevCount === 0 || merged.getItemKey(nextCount - 1) !== prevLastKey)) {
            followOnAppend = behavior;
          }
        }
      }
      this.options = merged;
      if (edgeKeysChanged) {
        this.pendingMin = 0;
        this.itemSizeCacheVersion++;
      }
      let anchorResolved = false;
      let anchorDelta = 0;
      if (anchor && this.scrollOffset !== null) {
        const [anchorKey, anchorOffset] = anchor;
        const newMeasurements = this.getMeasurements();
        const { count, getItemKey } = this.options;
        let idx = 0;
        while (idx < count && getItemKey(idx) !== anchorKey) {
          idx++;
        }
        if (idx < count) {
          const anchorItem = newMeasurements[idx];
          if (anchorItem) {
            const newOffset = Math.max(0, anchorItem.start + anchorOffset);
            if (newOffset !== this.scrollOffset) {
              anchorDelta = newOffset - this.scrollOffset;
              this.scrollOffset = newOffset;
              anchorResolved = true;
            }
          }
        }
      }
      if (anchorResolved || followOnAppend) {
        this.pendingScrollAnchor = [
          anchorResolved ? anchor[0] : null,
          anchorResolved ? anchor[1] : 0,
          followOnAppend,
          anchorDelta
        ];
      }
    };
    this.notify = (sync) => {
      var _a, _b;
      (_b = (_a = this.options).onChange) == null || _b.call(_a, this, sync);
    };
    this.maybeNotify = memo2(() => {
      this.calculateRange();
      return [
        this.isScrolling,
        this.range ? this.range.startIndex : null,
        this.range ? this.range.endIndex : null
      ];
    }, (isScrolling) => {
      this.notify(isScrolling);
    }, {
      key: false,
      debug: () => this.options.debug,
      initialDeps: [
        this.isScrolling,
        this.range ? this.range.startIndex : null,
        this.range ? this.range.endIndex : null
      ]
    });
    this.cleanup = () => {
      this.unsubs.filter(Boolean).forEach((d) => d());
      this.unsubs = [];
      this.observer.disconnect();
      if (this.rafId != null && this.targetWindow) {
        this.targetWindow.cancelAnimationFrame(this.rafId);
        this.rafId = null;
      }
      this.scrollState = null;
      this._iosDeferredAdjustment = 0;
      this._iosTouching = false;
      this._iosJustTouchEnded = false;
      this.scrollElement = null;
      this.targetWindow = null;
    };
    this._didMount = () => {
      return () => {
        this.cleanup();
      };
    };
    this._willUpdate = () => {
      var _a;
      const scrollElement = this.options.enabled ? this.options.getScrollElement() : null;
      if (this.scrollElement !== scrollElement) {
        this.cleanup();
        if (!scrollElement) {
          this.maybeNotify();
          return;
        }
        this.scrollElement = scrollElement;
        if (this.scrollElement && "ownerDocument" in this.scrollElement) {
          this.targetWindow = this.scrollElement.ownerDocument.defaultView;
        } else {
          this.targetWindow = ((_a = this.scrollElement) == null ? undefined : _a.window) ?? null;
        }
        this.elementsCache.forEach((cached) => {
          this.observer.observe(cached);
        });
        this.unsubs.push(this.options.observeElementRect(this, (rect) => {
          this.scrollRect = rect;
          this.maybeNotify();
        }));
        this.unsubs.push(this.options.observeElementOffset(this, (offset, isScrolling) => {
          if (isScrolling && this._intendedScrollOffset === null && offset === this.scrollOffset) {
            return;
          }
          if (this._intendedScrollOffset !== null && Math.abs(offset - this._intendedScrollOffset) < 1.5) {
            offset = this._intendedScrollOffset;
          }
          this._intendedScrollOffset = null;
          this.scrollAdjustments = 0;
          const prevOffset = this.getScrollOffset();
          this.scrollDirection = isScrolling ? prevOffset === offset ? this.scrollDirection : prevOffset < offset ? "forward" : "backward" : null;
          this.scrollOffset = offset;
          this.isScrolling = isScrolling;
          this._flushIosDeferredIfReady();
          if (this.scrollState) {
            this.scheduleScrollReconcile();
          }
          this.maybeNotify();
        }));
        if ("addEventListener" in this.scrollElement) {
          const scrollEl = this.scrollElement;
          const onTouchStart = () => {
            this._iosTouching = true;
            this._iosJustTouchEnded = false;
            if (this._iosTouchEndTimerId !== null && this.targetWindow != null) {
              this.targetWindow.clearTimeout(this._iosTouchEndTimerId);
              this._iosTouchEndTimerId = null;
            }
          };
          const onTouchEnd = () => {
            this._iosTouching = false;
            if (!isIOSWebKit() || this.targetWindow == null) {
              return;
            }
            this._iosJustTouchEnded = true;
            this._iosTouchEndTimerId = this.targetWindow.setTimeout(() => {
              this._iosJustTouchEnded = false;
              this._iosTouchEndTimerId = null;
              this._flushIosDeferredIfReady();
            }, 150);
          };
          scrollEl.addEventListener("touchstart", onTouchStart, addEventListenerOptions);
          scrollEl.addEventListener("touchend", onTouchEnd, addEventListenerOptions);
          this.unsubs.push(() => {
            scrollEl.removeEventListener("touchstart", onTouchStart);
            scrollEl.removeEventListener("touchend", onTouchEnd);
            if (this._iosTouchEndTimerId !== null && this.targetWindow != null) {
              this.targetWindow.clearTimeout(this._iosTouchEndTimerId);
              this._iosTouchEndTimerId = null;
            }
          });
        }
        this._scrollToOffset(this.getScrollOffset(), {
          adjustments: undefined,
          behavior: undefined
        });
      }
      const anchor = this.pendingScrollAnchor;
      this.pendingScrollAnchor = null;
      if (anchor && this.scrollElement && this.options.enabled) {
        const [key7, _offset, followOnAppend, anchorDelta] = anchor;
        if (key7 !== null && !followOnAppend) {
          if (isIOSWebKit() && (this.isScrolling || this._iosTouching || this._iosJustTouchEnded)) {
            if (anchorDelta !== 0) {
              this._iosDeferredAdjustment += anchorDelta;
            }
          } else {
            this._scrollToOffset(this.getScrollOffset(), {
              adjustments: undefined,
              behavior: undefined
            });
          }
        }
        if (followOnAppend) {
          this.scrollToEnd({ behavior: followOnAppend });
        }
      }
    };
    this._flushIosDeferredIfReady = () => {
      if (this._iosDeferredAdjustment === 0)
        return;
      if (this.isScrolling)
        return;
      if (this._iosTouching)
        return;
      if (this._iosJustTouchEnded)
        return;
      const cur = this.getScrollOffset();
      const max = this.getMaxScrollOffset();
      if (cur < 0 || cur > max)
        return;
      if (this._iosDeferredAdjustment < 0 && cur >= max - 1) {
        this._iosDeferredAdjustment = 0;
        return;
      }
      const delta = this._iosDeferredAdjustment;
      this._iosDeferredAdjustment = 0;
      this._scrollToOffset(cur, {
        adjustments: this.scrollAdjustments += delta,
        behavior: undefined
      });
    };
    this.rafId = null;
    this.getSize = () => {
      if (!this.options.enabled) {
        this.scrollRect = null;
        return 0;
      }
      this.scrollRect = this.scrollRect ?? this.options.initialRect;
      return this.scrollRect[this.options.horizontal ? "width" : "height"];
    };
    this.getScrollOffset = () => {
      if (!this.options.enabled) {
        this.scrollOffset = null;
        return 0;
      }
      this.scrollOffset = this.scrollOffset ?? (typeof this.options.initialOffset === "function" ? this.options.initialOffset() : this.options.initialOffset);
      return this.scrollOffset;
    };
    this.getMeasurementOptions = memo2(() => [
      this.options.count,
      this.options.paddingStart,
      this.options.scrollMargin,
      this.options.getItemKey,
      this.options.enabled,
      this.options.lanes,
      this.options.laneAssignmentMode,
      this.options.gap
    ], (count, paddingStart, scrollMargin, getItemKey, enabled, lanes, laneAssignmentMode, gap) => {
      const lanesChanged = this.prevLanes !== undefined && this.prevLanes !== lanes;
      if (lanesChanged) {
        this.lanesChangedFlag = true;
      }
      this.prevLanes = lanes;
      this.pendingMin = null;
      return {
        count,
        paddingStart,
        scrollMargin,
        getItemKey,
        enabled,
        lanes,
        laneAssignmentMode,
        gap
      };
    }, {
      key: false
    });
    this.getMeasurements = memo2(() => [this.getMeasurementOptions(), this.itemSizeCacheVersion], ({
      count,
      paddingStart,
      scrollMargin,
      getItemKey,
      enabled,
      lanes,
      laneAssignmentMode,
      gap
    }, _itemSizeCacheVersion) => {
      const itemSizeCache = this.itemSizeCache;
      if (!enabled) {
        this.measurementsCache = [];
        this.itemSizeCache.clear();
        this.laneAssignments.clear();
        return [];
      }
      if (this.laneAssignments.size > count) {
        for (const index of this.laneAssignments.keys()) {
          if (index >= count) {
            this.laneAssignments.delete(index);
          }
        }
      }
      if (this.lanesChangedFlag) {
        this.lanesChangedFlag = false;
        this.lanesSettling = true;
        this.measurementsCache = [];
        this.itemSizeCache.clear();
        this.laneAssignments.clear();
        this.pendingMin = null;
      }
      if (this.measurementsCache.length === 0 && !this.lanesSettling) {
        this.measurementsCache = this.options.initialMeasurementsCache;
        this.measurementsCache.forEach((item) => {
          this.itemSizeCache.set(item.key, item.size);
        });
      }
      const min = this.lanesSettling ? 0 : this.pendingMin ?? 0;
      this.pendingMin = null;
      if (this.lanesSettling && this.measurementsCache.length === count) {
        this.lanesSettling = false;
      }
      if (lanes === 1) {
        const need = count * 2;
        let flat = this._flatMeasurements;
        if (!flat || flat.length < need) {
          const next = new Float64Array(need);
          if (flat && min > 0)
            next.set(flat.subarray(0, min * 2));
          flat = next;
          this._flatMeasurements = flat;
        }
        let runningStart;
        if (min === 0) {
          runningStart = paddingStart + scrollMargin;
        } else {
          const prevIdx = min - 1;
          runningStart = flat[prevIdx * 2] + flat[prevIdx * 2 + 1] + gap;
        }
        for (let i = min;i < count; i++) {
          const key7 = getItemKey(i);
          const measuredSize = itemSizeCache.get(key7);
          const size3 = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i);
          flat[i * 2] = runningStart;
          flat[i * 2 + 1] = size3;
          runningStart += size3 + gap;
        }
        const view = createLazyMeasurementsView(count, flat, getItemKey);
        this.measurementsCache = view;
        return view;
      }
      const measurements = this.measurementsCache.slice(0, min);
      const laneLastIndex = new Array(lanes).fill(undefined);
      const laneEnds = new Float64Array(lanes);
      let filledLanes = 0;
      for (let m = 0;m < min; m++) {
        const item = measurements[m];
        if (item) {
          if (laneLastIndex[item.lane] === undefined)
            filledLanes++;
          laneLastIndex[item.lane] = m;
          laneEnds[item.lane] = item.end;
        }
      }
      for (let i = min;i < count; i++) {
        const key7 = getItemKey(i);
        const cachedLane = this.laneAssignments.get(i);
        let lane;
        let start;
        const shouldCacheLane = laneAssignmentMode === "estimate" || itemSizeCache.has(key7);
        if (cachedLane !== undefined && this.options.lanes > 1) {
          lane = cachedLane;
          const prevIndex = laneLastIndex[lane];
          const prevInLane = prevIndex !== undefined ? measurements[prevIndex] : undefined;
          start = prevInLane ? prevInLane.end + gap : paddingStart + scrollMargin;
        } else if (filledLanes === lanes) {
          let bestLane = 0;
          let bestEnd = laneEnds[0];
          let bestIdx = laneLastIndex[0];
          for (let l = 1;l < lanes; l++) {
            const e = laneEnds[l];
            if (e < bestEnd || e === bestEnd && laneLastIndex[l] < bestIdx) {
              bestLane = l;
              bestEnd = e;
              bestIdx = laneLastIndex[l];
            }
          }
          lane = bestLane;
          start = bestEnd + gap;
          if (shouldCacheLane) {
            this.laneAssignments.set(i, lane);
          }
        } else {
          lane = i % this.options.lanes;
          start = paddingStart + scrollMargin;
          if (shouldCacheLane) {
            this.laneAssignments.set(i, lane);
          }
        }
        const measuredSize = itemSizeCache.get(key7);
        const size3 = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i);
        const end = start + size3;
        measurements[i] = {
          index: i,
          start,
          size: size3,
          end,
          key: key7,
          lane
        };
        if (laneLastIndex[lane] === undefined)
          filledLanes++;
        laneLastIndex[lane] = i;
        laneEnds[lane] = end;
      }
      this.measurementsCache = measurements;
      return measurements;
    }, {
      key: false,
      debug: () => this.options.debug
    });
    this.calculateRange = memo2(() => [
      this.getMeasurements(),
      this.getSize(),
      this.getScrollOffset(),
      this.options.lanes
    ], (measurements, outerSize, scrollOffset, lanes) => {
      if (measurements.length === 0 || outerSize === 0) {
        this.range = null;
        return null;
      }
      this.range = calculateRangeImpl(measurements, outerSize, scrollOffset, lanes, lanes === 1 && this._flatMeasurements != null ? this._flatMeasurements : null);
      return this.range;
    }, {
      key: false,
      debug: () => this.options.debug
    });
    this.getVirtualIndexes = memo2(() => {
      let startIndex = null;
      let endIndex = null;
      const range = this.calculateRange();
      if (range) {
        startIndex = range.startIndex;
        endIndex = range.endIndex;
      }
      this.maybeNotify.updateDeps([this.isScrolling, startIndex, endIndex]);
      return [
        this.options.rangeExtractor,
        this.options.overscan,
        this.options.count,
        startIndex,
        endIndex
      ];
    }, (rangeExtractor, overscan, count, startIndex, endIndex) => {
      return startIndex === null || endIndex === null ? [] : rangeExtractor({
        startIndex,
        endIndex,
        overscan,
        count
      });
    }, {
      key: false,
      debug: () => this.options.debug
    });
    this.indexFromElement = (node) => {
      const attributeName = this.options.indexAttribute;
      const indexStr = node.getAttribute(attributeName);
      if (!indexStr) {
        console.warn(`Missing attribute name '${attributeName}={index}' on measured element.`);
        return -1;
      }
      return parseInt(indexStr, 10);
    };
    this.shouldMeasureDuringScroll = (index) => {
      var _a;
      if (!this.scrollState || this.scrollState.behavior !== "smooth") {
        return true;
      }
      const scrollIndex = this.scrollState.index ?? ((_a = this.getVirtualItemForOffset(this.scrollState.lastTargetOffset)) == null ? undefined : _a.index);
      if (scrollIndex !== undefined && this.range) {
        const bufferSize = Math.max(this.options.overscan, Math.ceil((this.range.endIndex - this.range.startIndex) / 2));
        const minIndex = Math.max(0, scrollIndex - bufferSize);
        const maxIndex = Math.min(this.options.count - 1, scrollIndex + bufferSize);
        return index >= minIndex && index <= maxIndex;
      }
      return true;
    };
    this.measureElement = (node) => {
      if (!node) {
        this.elementsCache.forEach((cached, key22) => {
          if (!cached.isConnected) {
            this.observer.unobserve(cached);
            this.elementsCache.delete(key22);
          }
        });
        return;
      }
      const index = this.indexFromElement(node);
      const key7 = this.options.getItemKey(index);
      const prevNode = this.elementsCache.get(key7);
      if (prevNode !== node) {
        if (prevNode) {
          this.observer.unobserve(prevNode);
        }
        this.observer.observe(node);
        this.elementsCache.set(key7, node);
      }
      if ((!this.isScrolling || this.scrollState) && this.shouldMeasureDuringScroll(index)) {
        this.resizeItem(index, this.options.measureElement(node, undefined, this));
      }
    };
    this.resizeItem = (index, size3) => {
      var _a, _b;
      if (index < 0 || index >= this.options.count)
        return;
      let cachedSize;
      let itemStart;
      let key7;
      const flat = this._flatMeasurements;
      if (this.options.lanes === 1 && flat !== null) {
        key7 = this.options.getItemKey(index);
        itemStart = flat[index * 2];
        cachedSize = flat[index * 2 + 1];
      } else {
        const item = this.measurementsCache[index];
        if (!item)
          return;
        key7 = item.key;
        itemStart = item.start;
        cachedSize = item.size;
      }
      const itemSize = this.itemSizeCache.get(key7) ?? cachedSize;
      const delta = size3 - itemSize;
      if (delta !== 0) {
        const wasAtEnd = this.options.anchorTo === "end" && ((_a = this.scrollState) == null ? undefined : _a.behavior) !== "smooth" && this.getVirtualDistanceFromEnd() <= this.options.scrollEndThreshold;
        const prevTotalSize = wasAtEnd ? this.getTotalSize() : 0;
        const scrollOffsetWithAdj = this.getScrollOffset() + this.scrollAdjustments;
        const isFirstMeasure = !this.itemSizeCache.has(key7);
        const defaultShouldAdjust = isFirstMeasure ? itemStart < scrollOffsetWithAdj : itemStart + itemSize <= scrollOffsetWithAdj && this.scrollDirection !== "backward";
        const shouldAdjustScroll = ((_b = this.scrollState) == null ? undefined : _b.behavior) !== "smooth" && (this.shouldAdjustScrollPositionOnItemSizeChange !== undefined ? this.shouldAdjustScrollPositionOnItemSizeChange(this.measurementsCache[index] ?? {
          index,
          key: key7,
          start: itemStart,
          size: cachedSize,
          end: itemStart + cachedSize,
          lane: 0
        }, delta, this) : defaultShouldAdjust);
        if (this.pendingMin === null || index < this.pendingMin) {
          this.pendingMin = index;
        }
        this.itemSizeCache.set(key7, size3);
        this.itemSizeCacheVersion++;
        let adjustedSync = false;
        if (wasAtEnd) {
          adjustedSync = this.applyScrollAdjustment(this.getTotalSize() - prevTotalSize);
        } else if (shouldAdjustScroll) {
          adjustedSync = this.applyScrollAdjustment(delta);
        }
        this.notify(adjustedSync);
      }
    };
    this.getVirtualItems = memo2(() => [this.getVirtualIndexes(), this.getMeasurements()], (indexes, measurements) => {
      const virtualItems = [];
      for (let k = 0, len = indexes.length;k < len; k++) {
        const i = indexes[k];
        const measurement = measurements[i];
        virtualItems.push(measurement);
      }
      return virtualItems;
    }, {
      key: false,
      debug: () => this.options.debug
    });
    this.getVirtualItemForOffset = (offset) => {
      const measurements = this.getMeasurements();
      if (measurements.length === 0) {
        return;
      }
      const flat = this._flatMeasurements;
      const useFlat = this.options.lanes === 1 && flat != null;
      const idx = findNearestBinarySearch(0, measurements.length - 1, useFlat ? (i) => flat[i * 2] : (i) => notUndefined(measurements[i]).start, offset);
      return notUndefined(measurements[idx]);
    };
    this.getMaxScrollOffset = () => {
      if (!this.scrollElement)
        return 0;
      if ("scrollHeight" in this.scrollElement) {
        return this.options.horizontal ? this.scrollElement.scrollWidth - this.scrollElement.clientWidth : this.scrollElement.scrollHeight - this.scrollElement.clientHeight;
      } else {
        const doc = this.scrollElement.document.documentElement;
        return this.options.horizontal ? doc.scrollWidth - this.scrollElement.innerWidth : doc.scrollHeight - this.scrollElement.innerHeight;
      }
    };
    this.getVirtualDistanceFromEnd = () => {
      return Math.max(this.getTotalSize() - this.getSize() - this.getScrollOffset(), 0);
    };
    this.getDistanceFromEnd = () => {
      return Math.max(this.getMaxScrollOffset() - this.getScrollOffset(), 0);
    };
    this.isAtEnd = (threshold = this.options.scrollEndThreshold) => {
      return this.getDistanceFromEnd() <= threshold;
    };
    this.getOffsetForAlignment = (toOffset, align, itemSize = 0) => {
      if (!this.scrollElement)
        return 0;
      const size3 = this.getSize();
      const scrollOffset = this.getScrollOffset();
      if (align === "auto") {
        align = toOffset >= scrollOffset + size3 ? "end" : "start";
      }
      if (align === "center") {
        toOffset += (itemSize - size3) / 2;
      } else if (align === "end") {
        toOffset -= size3;
      }
      const maxOffset = this.getMaxScrollOffset();
      return Math.max(Math.min(maxOffset, toOffset), 0);
    };
    this.getOffsetForIndex = (index, align = "auto") => {
      index = Math.max(0, Math.min(index, this.options.count - 1));
      const size3 = this.getSize();
      const scrollOffset = this.getScrollOffset();
      const item = this.measurementsCache[index];
      if (!item)
        return;
      if (align === "auto") {
        if (item.end >= scrollOffset + size3 - this.options.scrollPaddingEnd) {
          align = "end";
        } else if (item.start <= scrollOffset + this.options.scrollPaddingStart) {
          align = "start";
        } else {
          return [scrollOffset, align];
        }
      }
      if (align === "end" && index === this.options.count - 1) {
        return [this.getMaxScrollOffset(), align];
      }
      const toOffset = align === "end" ? item.end + this.options.scrollPaddingEnd : item.start - this.options.scrollPaddingStart;
      return [
        this.getOffsetForAlignment(toOffset, align, item.size),
        align
      ];
    };
    this.scrollToOffset = (toOffset, { align = "start", behavior = "auto" } = {}) => {
      this._iosDeferredAdjustment = 0;
      const offset = this.getOffsetForAlignment(toOffset, align);
      const now = this.now();
      this.scrollState = {
        index: null,
        align,
        behavior,
        startedAt: now,
        lastTargetOffset: offset,
        stableFrames: 0
      };
      this._scrollToOffset(offset, { adjustments: undefined, behavior });
      this.scheduleScrollReconcile();
    };
    this.scrollToIndex = (index, {
      align: initialAlign = "auto",
      behavior = "auto"
    } = {}) => {
      this._iosDeferredAdjustment = 0;
      index = Math.max(0, Math.min(index, this.options.count - 1));
      const offsetInfo = this.getOffsetForIndex(index, initialAlign);
      if (!offsetInfo) {
        return;
      }
      const [offset, align] = offsetInfo;
      const now = this.now();
      this.scrollState = {
        index,
        align,
        behavior,
        startedAt: now,
        lastTargetOffset: offset,
        stableFrames: 0
      };
      this._scrollToOffset(offset, { adjustments: undefined, behavior });
      this.scheduleScrollReconcile();
    };
    this.scrollBy = (delta, { behavior = "auto" } = {}) => {
      const offset = this.getScrollOffset() + delta;
      const now = this.now();
      this.scrollState = {
        index: null,
        align: "start",
        behavior,
        startedAt: now,
        lastTargetOffset: offset,
        stableFrames: 0
      };
      this._scrollToOffset(offset, { adjustments: undefined, behavior });
      this.scheduleScrollReconcile();
    };
    this.scrollToEnd = ({ behavior = "auto" } = {}) => {
      if (this.options.count > 0) {
        this.scrollToIndex(this.options.count - 1, {
          align: "end",
          behavior
        });
        return;
      }
      this.scrollToOffset(Math.max(this.getTotalSize() - this.getSize(), 0), {
        behavior
      });
    };
    this.getTotalSize = () => {
      var _a;
      const measurements = this.getMeasurements();
      let end;
      if (measurements.length === 0) {
        end = this.options.paddingStart;
      } else if (this.options.lanes === 1) {
        const lastIdx = measurements.length - 1;
        const flat = this._flatMeasurements;
        if (flat != null) {
          end = flat[lastIdx * 2] + flat[lastIdx * 2 + 1];
        } else {
          end = ((_a = measurements[lastIdx]) == null ? undefined : _a.end) ?? 0;
        }
      } else {
        const endByLane = Array(this.options.lanes).fill(null);
        let endIndex = measurements.length - 1;
        while (endIndex >= 0 && endByLane.some((val) => val === null)) {
          const item = measurements[endIndex];
          if (endByLane[item.lane] === null) {
            endByLane[item.lane] = item.end;
          }
          endIndex--;
        }
        end = Math.max(...endByLane.filter((val) => val !== null));
      }
      return Math.max(end - this.options.scrollMargin + this.options.paddingEnd, 0);
    };
    this.takeSnapshot = () => {
      const snapshot = [];
      if (this.itemSizeCache.size === 0)
        return snapshot;
      const m = this.getMeasurements();
      for (const item of m) {
        if (item && this.itemSizeCache.has(item.key)) {
          snapshot.push({
            index: item.index,
            key: item.key,
            start: item.start,
            size: item.size,
            end: item.end,
            lane: item.lane
          });
        }
      }
      return snapshot;
    };
    this._scrollToOffset = (offset, {
      adjustments,
      behavior
    }) => {
      this._intendedScrollOffset = offset + (adjustments ?? 0);
      this.options.scrollToFn(offset, { behavior, adjustments }, this);
    };
    this.measure = () => {
      this.pendingMin = null;
      this.itemSizeCache.clear();
      this.laneAssignments.clear();
      this.itemSizeCacheVersion++;
      this.notify(false);
    };
    this.setOptions(opts);
  }
  applyScrollAdjustment(delta, behavior) {
    if (delta === 0)
      return false;
    if (false) {}
    if (isIOSWebKit() && (this.isScrolling || this._iosTouching || this._iosJustTouchEnded)) {
      this._iosDeferredAdjustment += delta;
      return false;
    } else {
      this._scrollToOffset(this.getScrollOffset(), {
        adjustments: this.scrollAdjustments += delta,
        behavior
      });
      if (this.scrollOffset !== null) {
        this.scrollOffset += this.scrollAdjustments;
        if (this.scrollOffset < 0)
          this.scrollOffset = 0;
        this.scrollAdjustments = 0;
      }
      return true;
    }
  }
  scheduleScrollReconcile() {
    if (!this.targetWindow) {
      this.scrollState = null;
      return;
    }
    if (this.rafId != null)
      return;
    this.rafId = this.targetWindow.requestAnimationFrame(() => {
      this.rafId = null;
      this.reconcileScroll();
    });
  }
  reconcileScroll() {
    if (!this.scrollState)
      return;
    const el = this.scrollElement;
    if (!el)
      return;
    const MAX_RECONCILE_MS = 5000;
    if (this.now() - this.scrollState.startedAt > MAX_RECONCILE_MS) {
      this.scrollState = null;
      return;
    }
    const offsetInfo = this.scrollState.index != null ? this.getOffsetForIndex(this.scrollState.index, this.scrollState.align) : undefined;
    const targetOffset = offsetInfo ? offsetInfo[0] : this.scrollState.lastTargetOffset;
    const STABLE_FRAMES = 1;
    const targetChanged = targetOffset !== this.scrollState.lastTargetOffset;
    if (!targetChanged && approxEqual(targetOffset, this.getScrollOffset())) {
      this.scrollState.stableFrames++;
      if (this.scrollState.stableFrames >= STABLE_FRAMES) {
        if (this.getScrollOffset() !== targetOffset) {
          this._scrollToOffset(targetOffset, {
            adjustments: undefined,
            behavior: "auto"
          });
        }
        this.scrollState = null;
        return;
      }
    } else {
      this.scrollState.stableFrames = 0;
      if (targetChanged) {
        const viewport = this.getSize() || 600;
        const distance2 = Math.abs(targetOffset - this.getScrollOffset());
        const keepSmooth = this.scrollState.behavior === "smooth" && distance2 > viewport;
        this.scrollState.lastTargetOffset = targetOffset;
        if (!keepSmooth) {
          this.scrollState.behavior = "auto";
        }
        this._scrollToOffset(targetOffset, {
          adjustments: undefined,
          behavior: keepSmooth ? "smooth" : "auto"
        });
      }
    }
    this.scheduleScrollReconcile();
  }
}
var findNearestBinarySearch = (low, high, getCurrentValue, value) => {
  while (low <= high) {
    const middle = (low + high) / 2 | 0;
    const currentValue = getCurrentValue(middle);
    if (currentValue < value) {
      low = middle + 1;
    } else if (currentValue > value) {
      high = middle - 1;
    } else {
      return middle;
    }
  }
  if (low > 0) {
    return low - 1;
  } else {
    return 0;
  }
};
function findNearestBinarySearchFlat(flat, high, value) {
  let low = 0;
  while (low <= high) {
    const middle = (low + high) / 2 | 0;
    const currentValue = flat[middle * 2];
    if (currentValue < value) {
      low = middle + 1;
    } else if (currentValue > value) {
      high = middle - 1;
    } else {
      return middle;
    }
  }
  return low > 0 ? low - 1 : 0;
}
function calculateRangeImpl(measurements, outerSize, scrollOffset, lanes, flat) {
  const lastIndex = measurements.length - 1;
  if (measurements.length <= lanes) {
    return { startIndex: 0, endIndex: lastIndex };
  }
  if (lanes === 1 && flat !== null) {
    const startIndex2 = findNearestBinarySearchFlat(flat, lastIndex, scrollOffset);
    let endIndex2 = startIndex2;
    const limit = scrollOffset + outerSize;
    while (endIndex2 < lastIndex && flat[endIndex2 * 2] + flat[endIndex2 * 2 + 1] < limit) {
      endIndex2++;
    }
    return { startIndex: startIndex2, endIndex: endIndex2 };
  }
  const getStart = (index) => measurements[index].start;
  let startIndex = findNearestBinarySearch(0, lastIndex, getStart, scrollOffset);
  let endIndex = startIndex;
  if (lanes === 1) {
    while (endIndex < lastIndex && measurements[endIndex].end < scrollOffset + outerSize) {
      endIndex++;
    }
  } else if (lanes > 1) {
    const endPerLane = Array(lanes).fill(0);
    while (endIndex < lastIndex && endPerLane.some((pos) => pos < scrollOffset + outerSize)) {
      const item = measurements[endIndex];
      endPerLane[item.lane] = item.end;
      endIndex++;
    }
    const startPerLane = Array(lanes).fill(scrollOffset + outerSize);
    while (startIndex >= 0 && startPerLane.some((pos) => pos >= scrollOffset)) {
      const item = measurements[startIndex];
      startPerLane[item.lane] = item.start;
      startIndex--;
    }
    startIndex = Math.max(0, startIndex - startIndex % lanes);
    endIndex = Math.min(lastIndex, endIndex + (lanes - 1 - endIndex % lanes));
  }
  return { startIndex, endIndex };
}

// ../../node_modules/.bun/@tanstack+react-virtual@3.14.9+67f6792bdf102c28/node_modules/@tanstack/react-virtual/dist/esm/index.js
var useIsomorphicLayoutEffect = typeof document !== "undefined" ? React81.useLayoutEffect : React81.useEffect;
function useVirtualizerBase({
  useFlushSync = true,
  directDomUpdates = false,
  directDomUpdatesMode = "transform",
  ...options
}) {
  const rerender = React81.useReducer((x) => x + 1, 0)[1];
  const directRef = React81.useRef({
    enabled: directDomUpdates,
    mode: directDomUpdatesMode,
    container: null,
    lastSize: null,
    lastPositions: /* @__PURE__ */ new WeakMap,
    prevRange: null
  });
  directRef.current.enabled = directDomUpdates;
  directRef.current.mode = directDomUpdatesMode;
  const applyContainerSize = (instance2) => {
    const state = directRef.current;
    if (!state.enabled || !state.container)
      return;
    const totalSize = instance2.getTotalSize();
    if (totalSize !== state.lastSize) {
      state.lastSize = totalSize;
      const sizeAxis = instance2.options.horizontal ? "width" : "height";
      state.container.style[sizeAxis] = `${totalSize}px`;
    }
  };
  const applyDirectStyles = (instance2) => {
    const state = directRef.current;
    if (!state.enabled || !state.container)
      return;
    applyContainerSize(instance2);
    const horizontal = !!instance2.options.horizontal;
    const useTransform = state.mode === "transform";
    const posAxis = horizontal ? "left" : "top";
    const scrollMargin = instance2.options.scrollMargin;
    const items = instance2.getVirtualItems();
    for (const item of items) {
      const next = item.start - scrollMargin;
      const el = instance2.elementsCache.get(item.key);
      if (!el)
        continue;
      if (state.lastPositions.get(el) === next)
        continue;
      state.lastPositions.set(el, next);
      if (useTransform) {
        el.style.transform = horizontal ? `translate3d(${next}px, 0, 0)` : `translate3d(0, ${next}px, 0)`;
      } else {
        el.style[posAxis] = `${next}px`;
      }
    }
  };
  const resolvedOptions = {
    ...options,
    onChange: (instance2, sync) => {
      var _a;
      const state = directRef.current;
      let shouldRerender = true;
      if (state.enabled) {
        applyDirectStyles(instance2);
        const range = instance2.range;
        const prev = state.prevRange;
        shouldRerender = !prev || prev.isScrolling !== instance2.isScrolling || prev.startIndex !== (range == null ? undefined : range.startIndex) || prev.endIndex !== (range == null ? undefined : range.endIndex);
        if (shouldRerender) {
          state.prevRange = range ? {
            startIndex: range.startIndex,
            endIndex: range.endIndex,
            isScrolling: instance2.isScrolling
          } : null;
        }
      }
      if (shouldRerender) {
        if (useFlushSync && sync) {
          flushSync(rerender);
        } else {
          rerender();
        }
      }
      (_a = options.onChange) == null || _a.call(options, instance2, sync);
    }
  };
  const [instance] = React81.useState(() => {
    const v = new Virtualizer(resolvedOptions);
    return Object.assign(v, {
      containerRef: (node) => {
        const state = directRef.current;
        state.container = node;
        state.lastSize = null;
        if (node && state.enabled) {
          const total = v.getTotalSize();
          state.lastSize = total;
          const axis = v.options.horizontal ? "width" : "height";
          node.style[axis] = `${total}px`;
        }
      }
    });
  });
  instance.setOptions(resolvedOptions);
  useIsomorphicLayoutEffect(() => {
    return instance._didMount();
  }, []);
  useIsomorphicLayoutEffect(() => {
    applyContainerSize(instance);
    return instance._willUpdate();
  });
  useIsomorphicLayoutEffect(() => {
    applyDirectStyles(instance);
  });
  return instance;
}
function useVirtualizer(options) {
  return useVirtualizerBase({
    observeElementRect,
    observeElementOffset,
    scrollToFn: elementScroll,
    ...options
  });
}

// src/components/Timeline/TimelineVirtualization.tsx
import {
  createContext as createContext13,
  useCallback as useCallback60,
  useContext as useContext75,
  useEffect as useEffect49,
  useLayoutEffect as useLayoutEffect20,
  useMemo as useMemo67,
  useRef as useRef39
} from "react";

// src/components/Timeline/MaxTimelineTracks.tsx
import { jsxs as jsxs61 } from "react/jsx-runtime";
var MAX_TIMELINE_TRACKS_NOTICE_HEIGHT = 24;
var container20 = {
  height: MAX_TIMELINE_TRACKS_NOTICE_HEIGHT,
  display: "flex",
  alignItems: "center",
  color: WHITE_ALPHA_60,
  fontFamily: "sans-serif",
  fontSize: 12,
  backgroundColor: WHITE_ALPHA_10,
  paddingLeft: TIMELINE_PADDING + 5
};
var MaxTimelineTracksReached = () => {
  const maxTimelineTracks = getStudioMaxTimelineTracks();
  return /* @__PURE__ */ jsxs61("div", {
    style: container20,
    children: [
      "Limited display to ",
      maxTimelineTracks,
      " tracks to sustain performance.",
      "",
      "You can change this by setting Config.setMaxTimelineTracks() in your remotion.config.ts file."
    ]
  });
};

// src/components/Timeline/use-timeline-height.ts
import { useContext as useContext74, useMemo as useMemo66, useRef as useRef38 } from "react";
import { Internals as Internals69 } from "remotion";
var useTimelineTrackHeights = ({
  timeline
}) => {
  const { getIsExpanded } = useContext74(ExpandedTracksGetterContext);
  const { previewServerState } = useContext74(StudioServerConnectionCtx);
  const { propStatuses } = useContext74(Internals69.VisualModePropStatusesContext);
  const { getDragOverrides, getEffectDragOverrides } = useContext74(Internals69.VisualModeDragOverridesContext);
  const { selectedItems } = useTimelineSelection();
  const previewServerConnected = previewServerState.type === "connected";
  const selectedRowKeys = useMemo66(() => getSelectedTimelineExpandedRowKeys(selectedItems), [selectedItems]);
  const expandedControls = useMemo66(() => timeline.flatMap((track) => {
    if (!previewServerConnected || track.nodePathInfo === null || !getIsExpanded(track.nodePathInfo) || track.sequence.controls === null) {
      return [];
    }
    return [track.sequence.controls];
  }), [getIsExpanded, previewServerConnected, timeline]);
  const runtimeValueSnapshots = useRuntimeValueSnapshots(expandedControls);
  const runtimeValuesByStore = useMemo66(() => new Map(expandedControls.map((controls, index) => [
    controls.runtimeValues,
    runtimeValueSnapshots[index]
  ])), [expandedControls, runtimeValueSnapshots]);
  const heights = useMemo66(() => {
    return timeline.map((track) => {
      const isExpanded = previewServerConnected && track.nodePathInfo !== null && getIsExpanded(track.nodePathInfo);
      const layerHeight = getTimelineLayerHeight(track.sequence.type) + TIMELINE_ITEM_BORDER_BOTTOM;
      const expandedHeight = (() => {
        if (!isExpanded || track.nodePathInfo === null) {
          return 0;
        }
        const { nodePathInfo } = track;
        const tree = buildTimelineTree({
          sequence: track.sequence,
          nodePathInfo,
          getDragOverrides,
          getEffectDragOverrides,
          propStatuses,
          includeTextContent: false,
          includeSourceControls: false,
          runtimeValues: track.sequence.controls ? runtimeValuesByStore.get(track.sequence.controls.runtimeValues) ?? null : null
        });
        const filteredTree = filterTimelineExpandedTree({
          nodes: tree,
          shouldShowNode: (node) => isTimelineExpandedNodeSelected({
            nodePathInfo: node.nodePathInfo,
            selectedRowKeys
          }) || getNodeHasKeyframes({
            node,
            nodePath: nodePathInfo.sequenceSubscriptionKey,
            propStatuses,
            getDragOverrides,
            getEffectDragOverrides
          })
        });
        const flat = flattenVisibleTreeNodes({
          nodes: filteredTree,
          getIsExpanded
        });
        if (flat.length === 0) {
          return 0;
        }
        const totalRowsHeight = flat.reduce((sum, { node }) => sum + getTreeRowHeight(node), 0);
        const separators = Math.max(0, flat.length - 1);
        return totalRowsHeight + separators + TIMELINE_ITEM_BORDER_BOTTOM;
      })();
      return layerHeight + expandedHeight;
    });
  }, [
    timeline,
    previewServerConnected,
    getIsExpanded,
    propStatuses,
    getDragOverrides,
    getEffectDragOverrides,
    selectedRowKeys,
    runtimeValuesByStore
  ]);
  const stableHeights = useRef38(heights);
  if (stableHeights.current.length !== heights.length || heights.some((height, index) => stableHeights.current[index] !== height)) {
    stableHeights.current = heights;
  }
  return stableHeights.current;
};

// src/components/Timeline/TimelineVirtualization.tsx
import { jsx as jsx118 } from "react/jsx-runtime";
var TimelineVirtualizationContext = createContext13(null);
var getSelectionTrackKey = (selection) => {
  if (selection.type === "guide") {
    return null;
  }
  return getTimelineSequenceSelectionKey(selection.nodePathInfo);
};
var TimelineVirtualizationProvider = ({ children, hasBeenCut, isStill, timeline }) => {
  const trackHeights = useTimelineTrackHeights({ timeline });
  const { revealRequest, selectedItems } = useTimelineSelection();
  const paddingStart = isStill ? 0 : TIMELINE_TIME_INDICATOR_HEIGHT;
  const paddingEnd = TIMELINE_ITEM_BORDER_BOTTOM + (hasBeenCut ? MAX_TIMELINE_TRACKS_NOTICE_HEIGHT : 0);
  const layout = useMemo67(() => {
    const siblingIndexes = new Array(timeline.length);
    const subtreeEndIndexes = new Array(timeline.length).fill(timeline.length);
    const openTracks = [];
    const siblingCounts = new Map;
    for (let index = 0;index < timeline.length; index++) {
      const { depth } = timeline[index];
      while (openTracks.length > 0 && openTracks[openTracks.length - 1].depth >= depth) {
        const completed = openTracks.pop();
        subtreeEndIndexes[completed.index] = index;
      }
      const parentIndex = openTracks[openTracks.length - 1]?.index ?? null;
      const siblingIndex = siblingCounts.get(parentIndex) ?? 0;
      siblingIndexes[index] = siblingIndex;
      siblingCounts.set(parentIndex, siblingIndex + 1);
      openTracks.push({ depth, index });
    }
    const offsets = new Array(timeline.length + 1);
    offsets[0] = paddingStart;
    for (let index = 0;index < trackHeights.length; index++) {
      offsets[index + 1] = offsets[index] + trackHeights[index];
    }
    const rows = timeline.map((track, index) => ({
      afterDropLineOffset: offsets[subtreeEndIndexes[index]] - offsets[index],
      siblingIndex: siblingIndexes[index],
      track
    }));
    const rootTrackIndexes = new Map;
    for (let index = 0;index < timeline.length; index++) {
      const { nodePathInfo } = timeline[index];
      if (nodePathInfo !== null) {
        rootTrackIndexes.set(getTimelineSequenceSelectionKey(nodePathInfo), index);
      }
    }
    return {
      rootTrackIndexes,
      rows,
      tracksEnd: offsets[offsets.length - 1]
    };
  }, [paddingStart, timeline, trackHeights]);
  const selectedTrackIndexes = useMemo67(() => {
    const indexes = new Set;
    if (selectedItems.length !== 1) {
      return indexes;
    }
    for (const selectedItem of selectedItems) {
      const key7 = getSelectionTrackKey(selectedItem);
      const index = key7 === null ? undefined : layout.rootTrackIndexes.get(key7);
      if (index !== undefined) {
        indexes.add(index);
      }
    }
    return indexes;
  }, [layout.rootTrackIndexes, selectedItems]);
  const trackHeightsRef = useRef39(trackHeights);
  trackHeightsRef.current = trackHeights;
  const timelineRef = useRef39(timeline);
  timelineRef.current = timeline;
  const estimateSize = useCallback60((index) => trackHeightsRef.current[index] ?? 0, []);
  const getItemKey = useCallback60((index) => timelineRef.current[index]?.sequence.id ?? index, []);
  const rangeExtractor = useCallback60((range) => {
    const indexes = new Set(defaultRangeExtractor(range));
    for (const selectedIndex of selectedTrackIndexes) {
      indexes.add(selectedIndex);
    }
    return [...indexes].sort((a, b) => a - b);
  }, [selectedTrackIndexes]);
  const getScrollElement = useCallback60(() => timelineVerticalScroll.current, []);
  const virtualizer = useVirtualizer({
    count: timeline.length,
    estimateSize,
    getItemKey,
    getScrollElement,
    overscan: 8,
    paddingEnd,
    paddingStart,
    rangeExtractor
  });
  useLayoutEffect20(() => {
    virtualizer.measure();
  }, [trackHeights, virtualizer]);
  useEffect49(() => {
    if (revealRequest === null) {
      return;
    }
    const key7 = getSelectionTrackKey(revealRequest.item);
    const index = key7 === null ? undefined : layout.rootTrackIndexes.get(key7);
    if (index !== undefined && virtualizer.getOffsetForIndex(index, "auto")?.[1] !== "auto") {
      virtualizer.scrollToIndex(index, { align: "center" });
    }
  }, [layout.rootTrackIndexes, revealRequest, virtualizer]);
  const virtualItems = virtualizer.getVirtualItems();
  const value = useMemo67(() => ({
    rows: layout.rows,
    totalSize: layout.tracksEnd + paddingEnd,
    tracksEnd: layout.tracksEnd,
    virtualItems
  }), [layout.rows, layout.tracksEnd, paddingEnd, virtualItems]);
  return /* @__PURE__ */ jsx118(TimelineVirtualizationContext.Provider, {
    value,
    children
  });
};
var useTimelineVirtualization = () => {
  const context = useContext75(TimelineVirtualizationContext);
  if (context === null) {
    throw new Error("useTimelineVirtualization must be used inside TimelineVirtualizationProvider");
  }
  return context;
};

// src/components/Timeline/TimelineHeightContainer.tsx
import { jsx as jsx119 } from "react/jsx-runtime";
var baseStyle2 = {
  display: "flex",
  flex: 1,
  minHeight: "100%",
  overflowX: "hidden",
  backgroundColor: TIMELINE_BACKGROUND
};
var TimelineHeightContainerInner = ({ children }) => {
  const { totalSize } = useTimelineVirtualization();
  const onPointerDownCapture = useCallback61((event) => {
    const { activeElement } = document;
    if (event.button === 0 && activeElement instanceof HTMLButtonElement && event.target instanceof Node && !activeElement.contains(event.target)) {
      activeElement.blur();
    }
  }, []);
  const style6 = useMemo68(() => ({ ...baseStyle2, height: totalSize }), [totalSize]);
  return /* @__PURE__ */ jsx119("div", {
    style: style6,
    onPointerDownCapture,
    children
  });
};
var TimelineHeightContainer = React83.memo(TimelineHeightContainerInner);

// src/components/Timeline/TimelineInOutDragHandler.tsx
import { PlayerInternals as PlayerInternals16 } from "@remotion/player";
import React86, { useCallback as useCallback62, useContext as useContext78, useEffect as useEffect50, useState as useState43 } from "react";
import { Internals as Internals71, useVideoConfig as useVideoConfig10 } from "remotion";

// src/components/Timeline/TimelineInOutPointer.tsx
import { createRef as createRef5, useContext as useContext76 } from "react";
import { Internals as Internals70 } from "remotion";
import { jsx as jsx120, jsxs as jsxs62, Fragment as Fragment29 } from "react/jsx-runtime";
var areaHighlight = {
  position: "absolute",
  backgroundColor: BLACK_ALPHA_50,
  height: "100%",
  bottom: 0,
  top: 0,
  pointerEvents: "none"
};
var inMarkerAreaRef = createRef5();
var outMarkerAreaRef = createRef5();
var TimelineInOutPointer = () => {
  const { inFrame, outFrame } = useTimelineInOutFramePosition();
  const videoConfig = Internals70.useUnsafeVideoConfig();
  const timelineWidth = useContext76(TimelineWidthContext);
  if (!videoConfig || timelineWidth === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs62(Fragment29, {
    children: [
      inFrame !== null && /* @__PURE__ */ jsx120("div", {
        ref: inMarkerAreaRef,
        style: {
          ...areaHighlight,
          left: 0,
          width: getXPositionOfItemInTimelineImperatively(inFrame, videoConfig.durationInFrames, timelineWidth)
        }
      }),
      outFrame !== null && /* @__PURE__ */ jsx120("div", {
        ref: outMarkerAreaRef,
        style: {
          ...areaHighlight,
          left: getXPositionOfItemInTimelineImperatively(outFrame, videoConfig.durationInFrames, timelineWidth),
          width: timelineWidth - getXPositionOfItemInTimelineImperatively(outFrame, videoConfig.durationInFrames + 1, timelineWidth)
        }
      })
    ]
  });
};

// src/components/Timeline/TimelineInOutPointerHandle.tsx
import { createRef as createRef6, useContext as useContext77, useMemo as useMemo69 } from "react";
import { useVideoConfig as useVideoConfig9 } from "remotion";
import { jsx as jsx121 } from "react/jsx-runtime";
var line = {
  height: "100%",
  width: 1,
  position: "absolute",
  backgroundColor: WHITE_ALPHA_10,
  cursor: "ew-resize",
  paddingLeft: 1,
  paddingRight: 1
};
var inPointerHandle = createRef6();
var outPointerHandle = createRef6();
var InnerTimelineInOutPointerHandle = ({ atFrame, dragging, onPointerDown, timelineWidth, type }) => {
  const videoConfig = useVideoConfig9();
  const style6 = useMemo69(() => {
    return {
      ...line,
      backgroundColor: dragging ? WHITE_ALPHA_70 : WHITE_ALPHA_10,
      transform: `translateX(${getXPositionOfItemInTimelineImperatively(atFrame, videoConfig.durationInFrames, timelineWidth)}px)`,
      top: 0
    };
  }, [atFrame, dragging, timelineWidth, videoConfig.durationInFrames]);
  return /* @__PURE__ */ jsx121("div", {
    ref: type === "in" ? inPointerHandle : outPointerHandle,
    style: style6,
    onPointerDown,
    ...{ [TIMELINE_SCRUBBER_ATTR]: true }
  });
};
var TimelineInOutPointerHandle = ({
  dragging,
  onPointerDown,
  type,
  atFrame
}) => {
  const timelineWidth = useContext77(TimelineWidthContext);
  if (timelineWidth === null) {
    return null;
  }
  return /* @__PURE__ */ jsx121(InnerTimelineInOutPointerHandle, {
    atFrame,
    dragging,
    onPointerDown,
    timelineWidth,
    type
  });
};

// src/components/Timeline/TimelineInOutDragHandler.tsx
import { jsx as jsx122, jsxs as jsxs63, Fragment as Fragment30 } from "react/jsx-runtime";
var getClientXWithScroll2 = (x) => {
  return x + scrollableRef.current?.scrollLeft;
};
var TimelineInOutDragHandler = () => {
  const video = Internals71.useUnsafeVideoConfig();
  const { canvasContent } = useContext78(Internals71.CompositionManager);
  if (!canvasContent || canvasContent.type !== "composition") {
    return null;
  }
  return video ? /* @__PURE__ */ jsx122(TimelineInOutDragHandlerInnerMemo, {}) : null;
};
var TimelineInOutDragHandlerInner = () => {
  const videoConfig = useVideoConfig10();
  const size3 = PlayerInternals16.useElementSize(scrollableRef, {
    triggerOnWindowResize: true,
    shouldApplyCssTransforms: true
  });
  const { isHighestContext } = useZIndex();
  const [inOutDragging, setInOutDragging] = useState43({
    dragging: false
  });
  const timelineWidth = useContext78(TimelineWidthContext);
  const get = useCallback62((frame2) => {
    if (timelineWidth === null) {
      throw new Error("timeline width is not yet determined");
    }
    return getXPositionOfItemInTimelineImperatively(frame2, videoConfig.durationInFrames, timelineWidth);
  }, [timelineWidth, videoConfig.durationInFrames]);
  const width = getTimelineContentWidth();
  const left3 = size3?.left ?? 0;
  const { inFrame, outFrame } = useTimelineInOutFramePosition();
  const { setInAndOutFrames } = useTimelineSetInOutFramePosition();
  const onInPointerDown = useCallback62((e) => {
    if (e.button !== 0) {
      return;
    }
    if (!isHighestContext) {
      return;
    }
    if (!videoConfig) {
      return;
    }
    if (inFrame === null) {
      throw new Error("expected in frame");
    }
    e.stopPropagation();
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    const inMarker = get(inFrame);
    const outMarker = outFrame === null ? Infinity : get(outFrame - 1);
    forceSpecificCursor("ew-resize");
    setInOutDragging({
      dragging: "in",
      initialOffset: getClientXWithScroll2(e.clientX),
      boundaries: [-Infinity, outMarker - inMarker],
      button: e.button,
      pointerId: e.pointerId,
      target: e.currentTarget
    });
    e.currentTarget.setPointerCapture?.(e.pointerId);
  }, [isHighestContext, videoConfig, inFrame, get, outFrame]);
  const onOutPointerDown = useCallback62((e) => {
    if (e.button !== 0) {
      return;
    }
    if (!isHighestContext) {
      return;
    }
    if (!videoConfig) {
      return;
    }
    if (outFrame === null) {
      throw new Error("expected out frame");
    }
    e.stopPropagation();
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    const outMarker = get(outFrame);
    const inMarker = inFrame === null ? -Infinity : get(inFrame + 1);
    forceSpecificCursor("ew-resize");
    setInOutDragging({
      dragging: "out",
      initialOffset: getClientXWithScroll2(e.clientX),
      boundaries: [inMarker - outMarker, Infinity],
      button: e.button,
      pointerId: e.pointerId,
      target: e.currentTarget
    });
    e.currentTarget.setPointerCapture?.(e.pointerId);
  }, [isHighestContext, videoConfig, inFrame, get, outFrame]);
  const onPointerMoveInOut = useCallback62((e) => {
    if (!videoConfig) {
      return;
    }
    if (!inOutDragging.dragging) {
      return;
    }
    const offset = Math.max(inOutDragging.boundaries[0], Math.min(inOutDragging.boundaries[1], getClientXWithScroll2(e.clientX) - inOutDragging.initialOffset));
    if (inOutDragging.dragging === "in") {
      if (!inPointerHandle.current) {
        throw new Error("in pointer handle");
      }
      if (!inMarkerAreaRef.current) {
        throw new Error("expected inMarkerAreaRef");
      }
      if (!inFrame) {
        throw new Error("expected inframes");
      }
      inPointerHandle.current.style.transform = `translateX(${get(inFrame) + offset}px)`;
      inMarkerAreaRef.current.style.width = String(get(inFrame) + offset) + "px";
    }
    if (inOutDragging.dragging === "out") {
      if (!outPointerHandle.current) {
        throw new Error("in pointer handle");
      }
      if (!outMarkerAreaRef.current) {
        throw new Error("in outMarkerAreaRef");
      }
      if (!outFrame) {
        throw new Error("expected outframes");
      }
      outPointerHandle.current.style.transform = `translateX(${get(outFrame) + offset}px)`;
      outMarkerAreaRef.current.style.left = String(get(outFrame) + offset) + "px";
      outMarkerAreaRef.current.style.width = String(width - get(outFrame) - offset) + "px";
    }
  }, [get, inFrame, inOutDragging, outFrame, videoConfig, width]);
  const onPointerUpInOut = useCallback62((e) => {
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    stopForcingSpecificCursor();
    if (!videoConfig) {
      return;
    }
    if (!inOutDragging.dragging) {
      return;
    }
    setInOutDragging({
      dragging: false
    });
    const frame2 = getFrameFromX({
      clientX: getClientXWithScroll2(e.clientX) - left3,
      durationInFrames: videoConfig.durationInFrames,
      width,
      extrapolate: "extend"
    });
    if (inOutDragging.dragging === "in") {
      if (frame2 < 1) {
        return setInAndOutFrames((prev) => ({
          ...prev,
          [videoConfig.id]: {
            ...prev[videoConfig.id] ?? defaultInOutValue,
            inFrame: null
          }
        }));
      }
      const maxFrame = outFrame === null ? Infinity : outFrame - 1;
      setInAndOutFrames((prev) => ({
        ...prev,
        [videoConfig.id]: {
          ...prev[videoConfig.id] ?? defaultInOutValue,
          inFrame: Math.min(maxFrame, frame2)
        }
      }));
    } else {
      if (frame2 > videoConfig.durationInFrames - 2) {
        return setInAndOutFrames((prev) => ({
          ...prev,
          [videoConfig.id]: {
            ...prev[videoConfig.id] ?? defaultInOutValue,
            outFrame: null
          }
        }));
      }
      const minFrame = inFrame === null ? -Infinity : inFrame + 1;
      setInAndOutFrames((prev) => ({
        ...prev,
        [videoConfig.id]: {
          ...prev[videoConfig.id] ?? defaultInOutValue,
          outFrame: Math.max(minFrame, frame2)
        }
      }));
    }
  }, [
    inFrame,
    inOutDragging.dragging,
    left3,
    outFrame,
    setInAndOutFrames,
    videoConfig,
    width
  ]);
  const onPointerCancelInOut = useCallback62(() => {
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    stopForcingSpecificCursor();
    setInOutDragging({
      dragging: false
    });
  }, []);
  useEffect50(() => {
    if (inOutDragging.dragging === false) {
      return;
    }
    return startCapturedPointerSession({
      event: inOutDragging,
      captureTarget: inOutDragging.target,
      onMove: onPointerMoveInOut,
      onEnd: (reason, endEvent) => {
        if (isPointerSessionRelease(reason, endEvent)) {
          onPointerUpInOut(endEvent);
        } else {
          onPointerCancelInOut();
        }
      }
    });
  }, [
    inOutDragging,
    onPointerCancelInOut,
    onPointerMoveInOut,
    onPointerUpInOut
  ]);
  useEffect50(() => {
    return () => {
      document.body.style.userSelect = "";
      document.body.style.webkitUserSelect = "";
      stopForcingSpecificCursor();
    };
  }, []);
  const getInContextMenuItems = useCallback62(() => {
    return [
      {
        id: "hide-in",
        keyHint: null,
        label: "Clear In marker",
        leftItem: null,
        onClick: (_, e) => {
          e?.stopPropagation();
          e?.preventDefault();
          setInAndOutFrames((prev) => ({
            ...prev,
            [videoConfig.id]: {
              ...prev[videoConfig.id] ?? defaultInOutValue,
              inFrame: null
            }
          }));
        },
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "hide-in"
      }
    ];
  }, [setInAndOutFrames, videoConfig.id]);
  const getOutContextMenuItems = useCallback62(() => {
    return [
      {
        id: "hide-out",
        keyHint: null,
        label: "Clear Out marker",
        leftItem: null,
        onClick: (_, e) => {
          e?.stopPropagation();
          e?.preventDefault();
          setInAndOutFrames((prev) => ({
            ...prev,
            [videoConfig.id]: {
              ...prev[videoConfig.id] ?? defaultInOutValue,
              outFrame: null
            }
          }));
        },
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "hide-out"
      }
    ];
  }, [setInAndOutFrames, videoConfig.id]);
  return /* @__PURE__ */ jsxs63(Fragment30, {
    children: [
      inFrame !== null && /* @__PURE__ */ jsx122(ContextMenu, {
        getItems: getInContextMenuItems,
        children: /* @__PURE__ */ jsx122(TimelineInOutPointerHandle, {
          type: "in",
          atFrame: inFrame,
          dragging: inOutDragging.dragging === "in",
          onPointerDown: onInPointerDown
        })
      }),
      outFrame !== null && /* @__PURE__ */ jsx122(ContextMenu, {
        getItems: getOutContextMenuItems,
        children: /* @__PURE__ */ jsx122(TimelineInOutPointerHandle, {
          type: "out",
          dragging: inOutDragging.dragging === "out",
          atFrame: outFrame,
          onPointerDown: onOutPointerDown
        })
      })
    ]
  });
};
var TimelineInOutDragHandlerInnerMemo = React86.memo(TimelineInOutDragHandlerInner);

// src/components/Timeline/TimelineLayerChildren.tsx
import {
  createContext as createContext14,
  useCallback as useCallback63,
  useContext as useContext79,
  useMemo as useMemo70,
  useState as useState45
} from "react";

// src/components/Timeline/TimelineCollapseToggle.tsx
import { jsx as jsx123 } from "react/jsx-runtime";
var container21 = {
  height: 10,
  width: 10,
  display: "flex",
  justifyContent: "center",
  alignItems: "center"
};
var Icon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx123("svg", {
    viewBox: "0 0 8 10",
    ...props,
    style: { height: 8, width: 6.4 },
    children: /* @__PURE__ */ jsx123("path", {
      d: "M 0 0 L 8 5 L 0 10 z",
      fill: color
    })
  });
};
var TimelineCollapseToggle = ({ collapsed, color }) => {
  return /* @__PURE__ */ jsx123("div", {
    style: collapsed ? container21 : { ...container21, transform: "rotate(90deg)" },
    children: /* @__PURE__ */ jsx123(Icon, {
      color
    })
  });
};

// src/components/Timeline/TimelineLayerChildren.tsx
import { jsx as jsx124 } from "react/jsx-runtime";
var toggleLayout = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  width: 12,
  height: 16,
  marginRight: 0,
  flexShrink: 0
};
var STORAGE_KEY2 = "remotion.editor.collapsedLayerChildren.v1";
var TimelineLayerChildrenContext = createContext14(null);
var TimelineLayerChildrenProvider = TimelineLayerChildrenContext.Provider;
var useTimelineLayerChildren = (tracks, sequences, compositionId) => {
  const [collapsed, setCollapsed] = useState45(() => {
    try {
      const parsed = JSON.parse(window.localStorage.getItem(STORAGE_KEY2) ?? "{}");
      if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
        return {};
      }
      return Object.fromEntries(Object.entries(parsed).filter(([, storedValue]) => storedValue === true));
    } catch {
      return {};
    }
  });
  const toggle = useCallback63((key7) => {
    setCollapsed((previous) => {
      const next = toggleBooleanMapKey(previous, key7);
      try {
        window.localStorage.setItem(STORAGE_KEY2, JSON.stringify(next));
      } catch {}
      return next;
    });
  }, []);
  const hierarchy = useMemo70(() => {
    const byId = new Map(sequences.map((sequence) => [sequence.id, sequence]));
    const siblingIndices = new Map;
    const siblingCounts = new Map;
    for (const sequence of sequences) {
      const index = siblingCounts.get(sequence.parent) ?? 0;
      siblingIndices.set(sequence.id, index);
      siblingCounts.set(sequence.parent, index + 1);
    }
    const keys = new Map;
    const parents = new Set;
    const ancestors = new Map;
    for (const track of tracks) {
      const parentIds = [];
      let { parent } = track.sequence;
      while (parent !== null && !parentIds.includes(parent)) {
        parentIds.push(parent);
        parents.add(parent);
        parent = byId.get(parent)?.parent ?? null;
      }
      ancestors.set(track.sequence.id, parentIds);
      const identity = track.nodePathInfo ? ["source", timelineNodePathInfoToKey(track.nodePathInfo)] : track.displayGroup ? ["stack", track.displayGroup.key] : [
        "position",
        ...[...parentIds].reverse().concat(track.sequence.id).map((id) => siblingIndices.get(id))
      ];
      keys.set(track.sequence.id, JSON.stringify([compositionId, identity]));
    }
    return { keys, parents, ancestors };
  }, [compositionId, sequences, tracks]);
  const visibleTracks = useMemo70(() => tracks.filter((track) => {
    return !(hierarchy.ancestors.get(track.sequence.id) ?? []).some((id) => {
      const key7 = hierarchy.keys.get(id);
      return key7 !== undefined && collapsed[key7];
    });
  }), [hierarchy, collapsed, tracks]);
  const value = useMemo70(() => ({
    collapsed,
    keys: hierarchy.keys,
    parents: hierarchy.parents,
    toggle
  }), [collapsed, hierarchy, toggle]);
  return { visibleTracks, value };
};
var TimelineLayerChildrenToggle = ({ sequence }) => {
  const context = useContext79(TimelineLayerChildrenContext);
  const key7 = context?.keys.get(sequence.id);
  const isCollapsed = key7 !== undefined && Boolean(context?.collapsed[key7]);
  const onClick = useCallback63((event) => {
    event.stopPropagation();
    if (key7 !== undefined) {
      context?.toggle(key7);
    }
  }, [context, key7]);
  const stopPropagation = useCallback63((event) => event.stopPropagation(), []);
  if (!context?.parents.has(sequence.id) || key7 === undefined) {
    return /* @__PURE__ */ jsx124("div", {
      style: toggleLayout
    });
  }
  const label7 = `${isCollapsed ? "Expand" : "Collapse"} children of ${sequence.displayName}`;
  return /* @__PURE__ */ jsx124("button", {
    type: "button",
    className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
    "aria-label": label7,
    "aria-expanded": !isCollapsed,
    title: label7,
    onClick,
    onPointerDown: stopPropagation,
    onDoubleClick: stopPropagation,
    style: {
      ...hoverableStyle({
        idleBackground: TRANSPARENT,
        hoverBackground: TRANSPARENT,
        idleColor: LIGHT_TEXT,
        hoverColor: WHITE
      }),
      ...toggleLayout,
      border: "none",
      padding: 0,
      cursor: "default"
    },
    children: /* @__PURE__ */ jsx124(TimelineCollapseToggle, {
      collapsed: isCollapsed,
      color: "currentColor"
    })
  });
};

// src/components/Timeline/TimelineList.tsx
import React92 from "react";

// src/components/Timeline/TimelineSequenceItem.tsx
import React91, {
  useCallback as useCallback65,
  useContext as useContext81,
  useEffect as useEffect52,
  useMemo as useMemo75,
  useRef as useRef41,
  useState as useState47
} from "react";
import { Internals as Internals76 } from "remotion";

// src/helpers/are-sequence-node-path-infos-equal.ts
import { stringifySequenceSubscriptionKey as stringifySequenceSubscriptionKey2 } from "@remotion/studio-shared";
var areSequenceNodePathInfosEqual = (first, second) => {
  if (first.index !== second.index || first.numberOfSequencesWithThisNodePath !== second.numberOfSequencesWithThisNodePath || first.supportsEffects !== second.supportsEffects || stringifySequenceSubscriptionKey2(first.sequenceSubscriptionKey) !== stringifySequenceSubscriptionKey2(second.sequenceSubscriptionKey) || first.auxiliaryKeys.length !== second.auxiliaryKeys.length || !first.auxiliaryKeys.every((key7, index) => key7 === second.auxiliaryKeys[index])) {
    return false;
  }
  const firstVideoConfig = first.sequenceSubscriptionKey.videoConfigValues;
  const secondVideoConfig = second.sequenceSubscriptionKey.videoConfigValues;
  return firstVideoConfig === secondVideoConfig || firstVideoConfig !== null && secondVideoConfig !== null && firstVideoConfig.durationInFrames === secondVideoConfig.durationInFrames && firstVideoConfig.fps === secondVideoConfig.fps && firstVideoConfig.height === secondVideoConfig.height && firstVideoConfig.width === secondVideoConfig.width;
};

// src/components/reorder-sequence-api.ts
var reorderSequence = (request) => {
  const browserStudioOperations = getBrowserStudioOperations();
  return browserStudioOperations ? browserStudioOperations.reorderSequence(request) : callApi("/api/reorder-sequence", request);
};

// src/components/Timeline/get-sequence-split-menu-item.tsx
import { jsx as jsx125 } from "react/jsx-runtime";
var getSequenceSplitMenuItem = ({
  nodePathInfo,
  sequence,
  propStatuses,
  splitFrame,
  canEditSource,
  hasMultipleSelection
}) => {
  if (hasMultipleSelection) {
    return null;
  }
  const eligibility = nodePathInfo ? getTimelineSequenceSplitEligibility({
    selection: { type: "sequence", nodePathInfo },
    sequence,
    propStatuses,
    splitFrame
  }) : {
    canSplit: false,
    reason: "Sequence has no editable source node"
  };
  const disabledReason = !canEditSource ? "Sequence source is unavailable" : propStatuses === undefined ? "Waiting for sequence prop status" : eligibility.canSplit ? null : eligibility.reason;
  return {
    type: "item",
    id: "split-sequence",
    label: /* @__PURE__ */ jsx125("span", {
      style: {
        fontFamily: "inherit",
        fontSize: "inherit",
        lineHeight: "inherit",
        color: "inherit"
      },
      title: disabledReason ?? "Split at the playhead",
      children: "Split clip"
    }),
    value: "split-sequence",
    keyHint: null,
    leftItem: null,
    quickSwitcherLabel: null,
    subMenu: null,
    disabled: disabledReason !== null,
    onClick: () => {
      if (disabledReason !== null || !eligibility.canSplit) {
        return;
      }
      splitTimelineSequenceFromSource({
        nodePathInfo: eligibility.nodePathInfo,
        splitFrame
      }).catch(() => {
        return;
      });
    }
  };
};

// src/components/Timeline/TimelineDuplicateCount.tsx
import { jsxs as jsxs64 } from "react/jsx-runtime";
var badgeStyle = {
  alignItems: "center",
  border: `1px solid ${WHITE_ALPHA_20}`,
  borderRadius: 4,
  boxSizing: "border-box",
  color: WHITE_ALPHA_60,
  display: "inline-flex",
  flexShrink: 0,
  fontSize: 10,
  fontWeight: 300,
  height: 14,
  justifyContent: "center",
  lineHeight: "12px",
  minWidth: 18,
  padding: "0 3px",
  userSelect: "none",
  WebkitUserSelect: "none"
};
var TimelineDuplicateCount = ({ count }) => {
  const label7 = `${count} other programmatically duplicated ${count === 1 ? "instance is" : "instances are"} hidden`;
  return /* @__PURE__ */ jsxs64("span", {
    style: badgeStyle,
    title: label7,
    "aria-label": label7,
    children: [
      "+",
      count
    ]
  });
};

// src/components/Timeline/TimelineExpandedSection.tsx
import React88, { useMemo as useMemo71 } from "react";
import { jsx as jsx126, jsxs as jsxs65 } from "react/jsx-runtime";
var expandedSectionBase = {
  backgroundColor: TIMELINE_BACKGROUND,
  color: WHITE,
  fontFamily: "Arial, Helvetica, sans-serif",
  fontSize: 12,
  display: "flex",
  flexDirection: "column",
  borderBottom: `1px solid ${TIMELINE_TRACK_SEPARATOR}`
};
var separator = {
  height: 0,
  borderBottom: `1px solid ${TIMELINE_TRACK_SEPARATOR}`
};
var TimelineExpandedSection = ({
  sequence,
  validatedLocation,
  nodePathInfo,
  nestedDepth,
  keyframeDisplayOffset
}) => {
  const { filteredTree, getIsExpanded, toggleTrack } = useTimelineExpandedTree({
    sequence,
    nodePathInfo,
    includeTextContent: false,
    includeSourceControls: false
  });
  const flat = useMemo71(() => flattenVisibleTreeNodes({ nodes: filteredTree, getIsExpanded }), [filteredTree, getIsExpanded]);
  const expandedHeight = useMemo71(() => {
    const totalRowsHeight = flat.reduce((sum, { node }) => sum + getTreeRowHeight(node), 0);
    const separators = Math.max(0, flat.length - 1);
    return totalRowsHeight + separators;
  }, [flat]);
  const style6 = useMemo71(() => {
    return {
      ...expandedSectionBase,
      height: expandedHeight + TIMELINE_ITEM_BORDER_BOTTOM
    };
  }, [expandedHeight]);
  const { schema } = sequence.controls;
  if (flat.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx126(TimelineRowSelectedBackgroundContext.Provider, {
    value: TIMELINE_EXPANDED_SELECTED_BACKGROUND,
    children: /* @__PURE__ */ jsx126("div", {
      style: style6,
      children: flat.map(({ node, depth }, i) => {
        return /* @__PURE__ */ jsxs65(React88.Fragment, {
          children: [
            i > 0 ? /* @__PURE__ */ jsx126("div", {
              style: separator
            }) : null,
            /* @__PURE__ */ jsx126(TimelineExpandedRow, {
              node,
              depth,
              nestedDepth,
              getIsExpanded,
              toggleTrack,
              validatedLocation,
              nodePath: nodePathInfo.sequenceSubscriptionKey,
              schema,
              keyframeDisplayOffset,
              keyframeControlsMode: "timeline"
            })
          ]
        }, JSON.stringify(node.nodePathInfo));
      })
    })
  });
};

// src/components/Timeline/TimelineMediaInfo.tsx
import { useMemo as useMemo72 } from "react";
import { Internals as Internals73 } from "remotion";
import { jsx as jsx127 } from "react/jsx-runtime";
var lineStyle = {
  whiteSpace: "nowrap",
  overflow: "hidden",
  textOverflow: "ellipsis",
  minWidth: 0,
  fontSize: 12,
  color: WHITE_ALPHA_30,
  display: "inline-block"
};
var useAssetLink = (src) => {
  const linkInfo = useMemo72(() => getTimelineAssetLinkInfo(src), [src]);
  const fileNameStyle = useMemo72(() => ({
    ...lineStyle,
    color: WHITE_ALPHA_30,
    textDecoration: "none",
    display: "inline-block",
    overflow: "hidden",
    whiteSpace: "pre",
    textOverflow: "ellipsis",
    userSelect: "none",
    WebkitUserSelect: "none"
  }), []);
  return {
    linkInfo,
    fileNameStyle
  };
};
var TimelineMediaInfo = ({ src }) => {
  const fileName = useMemo72(() => Internals73.getAssetDisplayName(src), [src]);
  const { linkInfo, fileNameStyle } = useAssetLink(src);
  return /* @__PURE__ */ jsx127("div", {
    style: fileNameStyle,
    title: linkInfo ? linkInfo.title : fileName,
    children: fileName
  });
};

// src/components/Timeline/TimelineSequenceName.tsx
import { useCallback as useCallback64, useEffect as useEffect51, useMemo as useMemo73, useRef as useRef40, useState as useState46 } from "react";
import { jsx as jsx128, jsxs as jsxs66, Fragment as Fragment31 } from "react/jsx-runtime";
var MAX_DISPLAY_NAME_LENGTH = 1000;
var MAX_RENAME_INPUT_WIDTH = 240;
var RENAME_INPUT_CLASS_NAME = "remotion-timeline-sequence-name-input";
var getTruncatedDisplayName = (displayName) => {
  if (displayName.length > MAX_DISPLAY_NAME_LENGTH) {
    return displayName.slice(0, MAX_DISPLAY_NAME_LENGTH) + "...";
  }
  return displayName;
};
var TimelineSequenceName = ({
  displayName,
  fallbackDisplayName,
  selected,
  containsSelection,
  editing,
  onCancelEditing,
  onSaveName
}) => {
  const inputRef = useRef40(null);
  const [draftName, setDraftName] = useState46(displayName);
  const cancelNextBlurRef = useRef40(false);
  const style6 = useMemo73(() => {
    return {
      alignItems: "center",
      alignSelf: "stretch",
      ...getTimelineSelectedLabelStyle(selected, false),
      display: "inline-flex",
      flexShrink: 1,
      fontFamily: "inherit",
      fontSize: 12,
      lineHeight: "normal",
      minWidth: 0,
      overflow: "hidden",
      whiteSpace: "nowrap",
      textOverflow: "ellipsis",
      color: getTimelineColor(selected, false),
      userSelect: "none",
      WebkitUserSelect: "none",
      textDecoration: "none",
      boxShadow: containsSelection && !selected ? `inset 0 0 0 2px ${TIMELINE_SELECTED_LABEL_BACKGROUND}` : undefined
    };
  }, [selected, containsSelection]);
  const inputStyle = useMemo73(() => {
    return {
      ...style6,
      background: TRANSPARENT,
      border: 0,
      color: getTimelineColor(false, false),
      fontFamily: "inherit",
      fontSize: 12,
      outline: "none",
      paddingBottom: 0,
      paddingTop: 0,
      boxSizing: "border-box",
      maxWidth: MAX_RENAME_INPUT_WIDTH,
      minWidth: 0,
      userSelect: "text",
      WebkitUserSelect: "text"
    };
  }, [style6]);
  const editableDisplayName = displayName || fallbackDisplayName;
  const text2 = getTruncatedDisplayName(editableDisplayName);
  useEffect51(() => {
    if (!editing) {
      setDraftName(editableDisplayName);
      return;
    }
    const input2 = inputRef.current;
    if (!input2) {
      return;
    }
    input2.focus();
    input2.select();
  }, [editableDisplayName, editing]);
  const save = useCallback64(() => {
    onSaveName(draftName).catch(() => {
      return;
    });
  }, [draftName, onSaveName]);
  const onKeyDown = useCallback64((e) => {
    if (e.key === "Escape") {
      cancelNextBlurRef.current = true;
      e.preventDefault();
      onCancelEditing();
      return;
    }
    if (e.key === "Enter") {
      cancelNextBlurRef.current = true;
      e.preventDefault();
      save();
    }
  }, [onCancelEditing, save]);
  const onBlur = useCallback64(() => {
    if (cancelNextBlurRef.current) {
      cancelNextBlurRef.current = false;
      return;
    }
    save();
  }, [save]);
  if (editing) {
    return /* @__PURE__ */ jsxs66(Fragment31, {
      children: [
        /* @__PURE__ */ jsx128("style", {
          children: `.${RENAME_INPUT_CLASS_NAME}::selection { background: ${WHITE_ALPHA_72}; color: ${BLACK}; }`
        }),
        /* @__PURE__ */ jsx128("input", {
          ref: inputRef,
          className: RENAME_INPUT_CLASS_NAME,
          value: draftName,
          onChange: (e) => setDraftName(e.target.value),
          onBlur,
          onKeyDown,
          size: Math.max(1, draftName.length),
          style: inputStyle
        })
      ]
    });
  }
  return /* @__PURE__ */ jsx128("div", {
    title: text2,
    style: style6,
    children: text2
  });
};

// src/components/Timeline/use-asset-timeline-context-menu.ts
import { useContext as useContext80, useMemo as useMemo74 } from "react";
import { Internals as Internals75 } from "remotion";
var useAssetTimelineContextMenu = () => {
  const { canvasContent } = useContext80(Internals75.CompositionManager);
  const { previewServerState } = useContext80(StudioServerConnectionCtx);
  return useMemo74(() => {
    if (canvasContent?.type !== "asset") {
      return null;
    }
    const copyContextItem = getCopyContextForAgentsMenuItem({
      contextForAgents: window.remotion_publicFolderExists ? `Asset: ${window.remotion_publicFolderExists}/${canvasContent.asset}` : `Asset: staticFile(${JSON.stringify(canvasContent.asset)})`
    });
    if (getBrowserStudioOperations() !== null) {
      return [copyContextItem];
    }
    return [
      {
        type: "item",
        id: "open-asset-in-explorer",
        label: `Show in ${getFileManagerName(window.remotion_fileSystemPlatform)}`,
        keyHint: null,
        leftItem: null,
        quickSwitcherLabel: null,
        subMenu: null,
        value: "open-asset-in-explorer",
        disabled: Boolean(window.remotion_isReadOnlyStudio) || !window.remotion_publicFolderExists || previewServerState.type !== "connected",
        onClick: () => {
          openInFileExplorer({
            directory: window.remotion_publicFolderExists + "/" + canvasContent.asset
          }).catch((err) => {
            showNotification(`Could not open file: ${err.message}`, 2000);
          });
        }
      },
      copyContextItem
    ];
  }, [canvasContent, previewServerState.type]);
};

// src/components/Timeline/TimelineSequenceItem.tsx
import { jsx as jsx129, jsxs as jsxs67, Fragment as Fragment32 } from "react/jsx-runtime";
var labelContainerStyle = {
  alignItems: "center",
  alignSelf: "stretch",
  display: "flex",
  flex: 1,
  flexDirection: "row",
  minWidth: 0
};
var connectedCompositionIconStyle = {
  flexShrink: 0,
  height: 12,
  width: 12
};
var effectDropHighlight = {
  backgroundColor: TIMELINE_DROP_BLUE_ALPHA_16,
  outline: BORDER_TIMELINE_DROP_BLUE,
  outlineOffset: -1
};
var TimelineSequenceExpandArrowInner = ({ disabled, isExpanded, nodePathInfo, selectedItems, sequence }) => {
  const { toggleTrack } = useContext81(ExpandedTracksSetterContext);
  const hasExpandableContent = useTimelineSequenceHasExpandableContent({
    sequence,
    nodePathInfo,
    selectedItems
  });
  const onToggleExpand = useCallback65(() => toggleTrack(nodePathInfo), [nodePathInfo, toggleTrack]);
  if (!hasExpandableContent) {
    return /* @__PURE__ */ jsx129(TimelineExpandArrowSpacer, {});
  }
  return /* @__PURE__ */ jsx129(TimelineExpandArrowButton, {
    isExpanded,
    onClick: onToggleExpand,
    label: "track properties",
    disabled
  });
};
var areTimelineSequenceExpandArrowPropsEqual = (first, second) => {
  return first.disabled === second.disabled && first.isExpanded === second.isExpanded && first.selectedItems === second.selectedItems && first.sequence.controls?.schema === second.sequence.controls?.schema && first.sequence.controls?.runtimeValues === second.sequence.controls?.runtimeValues && first.sequence.effects === second.sequence.effects && areSequenceNodePathInfosEqual(first.nodePathInfo, second.nodePathInfo);
};
var TimelineSequenceExpandArrow = React91.memo(TimelineSequenceExpandArrowInner, areTimelineSequenceExpandArrowPropsEqual);
var sequenceReorderWrapper = {
  position: "relative"
};
var sequenceReorderLineBase = {
  backgroundColor: TIMELINE_BLUE,
  height: 2,
  left: 0,
  pointerEvents: "none",
  position: "absolute",
  right: 0,
  zIndex: 1
};
var sequenceReorderAfterLineWrapper = {
  height: 0,
  left: 0,
  position: "absolute",
  right: 0
};
var sequenceReorderAfterLine = {
  ...sequenceReorderLineBase,
  top: -1
};
var getDestinationIndex = ({
  fromIndex,
  insertionIndex
}) => {
  return insertionIndex > fromIndex ? insertionIndex - 1 : insertionIndex;
};
var sequencePointerDropTargets = new Map;
var getSequencePointerDropTargetAtPoint = ({
  clientX,
  clientY
}) => {
  for (const element of document.elementsFromPoint(clientX, clientY)) {
    let current = element;
    while (current) {
      const dropTarget = sequencePointerDropTargets.get(current);
      if (dropTarget) {
        return dropTarget;
      }
      current = current.parentElement;
    }
  }
  return null;
};
var SEQUENCE_REORDER_POINTER_THRESHOLD = 3;
var TimelineSequenceItemInner = ({
  afterDropLineOffset,
  connectedCompositions,
  nestedDepth,
  sequence,
  nodePathInfo,
  keyframeDisplayOffset,
  sequenceFrameOffset,
  siblingIndex,
  numberOfHiddenDuplicates,
  showProvisionalVisibilityToggle
}) => {
  const nodePath = nodePathInfo?.sequenceSubscriptionKey ?? null;
  const { hovered, onPointerEnter, onPointerLeave } = useTimelineSequenceHover(nodePathInfo);
  const { previewServerState } = useContext81(StudioServerConnectionCtx);
  const previewConnected = previewServerState.type === "connected";
  const previewInteractive = previewConnected && isStudioInteractivityEnabled();
  const isStill = useIsStill();
  const canMutateEffects = previewConnected && isStudioSelectionEnabled() && canUseEffectOperations();
  const { getIsExpanded } = useContext81(ExpandedTracksGetterContext);
  const { setPropStatuses } = useContext81(Internals76.VisualModeSettersContext);
  const { setSelectedModal } = useContext81(SetSelectedModalContext);
  const { isHighestContext } = useKeybinding();
  const selectAsset = useSelectAsset();
  const assetContextMenu = useAssetTimelineContextMenu();
  const selectComposition = useSelectComposition();
  const deleteTimelineItems = useDeleteTimelineItems();
  const { onSelect, selectable, selected, selectedItems, selectItem } = useTimelineRowSelection(nodePathInfo);
  const selectedSequenceNodePathInfos = useMemo75(() => {
    if (!selected || selectedItems.length < 2 || selectedItems.some((item) => item.type !== "sequence")) {
      return null;
    }
    return selectedItems.flatMap((item) => item.type === "sequence" ? [item.nodePathInfo] : []);
  }, [selected, selectedItems]);
  const containsSelection = useTimelineRowContainsSelection(nodePathInfo);
  const [effectDropHovered, setEffectDropHovered] = useState47(false);
  const [isRenaming, setIsRenaming] = useState47(false);
  const [sequenceDropIndicator, setSequenceDropIndicator] = useState47(null);
  const sequenceReorderWrapperRef = useRef41(null);
  const stopSequencePointerSession = useRef41(null);
  const suppressNextClick = useRef41(false);
  const {
    canConfigureApps,
    canOpenInEditor,
    canOpenSource,
    codingAgentInfo,
    editorInfo,
    openInCodingAgent: openInCodingAgent2,
    openInEditor: openInEditor3,
    openSource,
    originalLocation
  } = useOpenSequenceInApps(sequence);
  const validatedLocation = useMemo75(() => {
    if (!originalLocation || !originalLocation.source || !originalLocation.line) {
      return null;
    }
    return {
      source: originalLocation.source,
      line: originalLocation.line,
      column: originalLocation.column ?? 0
    };
  }, [originalLocation]);
  const {
    canRename: canRenameThisSequence,
    displayName,
    fallbackDisplayName,
    propStatusesForOverride,
    saveName
  } = useRenameSequence({
    clientId: previewInteractive && previewServerState.type === "connected" ? previewServerState.clientId : null,
    nodePathInfo,
    sequence,
    validatedLocation
  });
  const timelineDisplayName = sequence.displayName === "" && connectedCompositions.length === 1 ? connectedCompositions[0].id : displayName;
  const canDeleteFromSource = Boolean(nodePath && validatedLocation?.source);
  const nodePathKey = useMemo75(() => nodePath ? Internals76.makeSequencePropsSubscriptionKey(nodePath) : null, [nodePath]);
  const parentId = sequence.parent ?? null;
  const canReorderSequence = previewInteractive && Boolean(nodePath && nodePathKey && validatedLocation?.source) && nodePathInfo?.numberOfSequencesWithThisNodePath === 1;
  const canHandleSequenceDrag = previewInteractive;
  const confirm = useConfirmationDialog();
  const deleteDisabled = useMemo75(() => !previewInteractive || !sequence.controls || !canDeleteFromSource, [previewInteractive, sequence.controls, canDeleteFromSource]);
  const isProgrammaticallyDuplicated = (nodePathInfo?.numberOfSequencesWithThisNodePath ?? 1) > 1;
  const duplicateDisabled = deleteDisabled || isProgrammaticallyDuplicated;
  const disableInteractivityDisabled = !previewInteractive || !sequence.showInTimeline || !nodePath || !validatedLocation?.source;
  const onDuplicateSequenceFromSource = useCallback65(() => {
    if (duplicateDisabled || !validatedLocation?.source || !nodePathInfo) {
      return;
    }
    duplicateSequencesFromSource([nodePathInfo], confirm).catch(() => {
      return;
    });
  }, [confirm, duplicateDisabled, nodePathInfo, validatedLocation?.source]);
  const onDuplicateSelectedSequences = useCallback65(() => {
    if (!previewInteractive || selectedSequenceNodePathInfos === null) {
      return;
    }
    duplicateSequencesFromSource(selectedSequenceNodePathInfos, confirm).catch(() => {
      return;
    });
  }, [confirm, previewInteractive, selectedSequenceNodePathInfos]);
  const onDeleteSequenceFromSource = useCallback65(() => {
    if (deleteDisabled || !validatedLocation?.source || !nodePath || !nodePathInfo) {
      return;
    }
    deleteTimelineItems([{ type: "sequence", nodePathInfo }]);
  }, [
    deleteDisabled,
    deleteTimelineItems,
    nodePath,
    validatedLocation?.source,
    nodePathInfo
  ]);
  const onDeleteSelectedSequences = useCallback65(() => {
    if (!previewInteractive || selectedSequenceNodePathInfos === null) {
      return;
    }
    deleteTimelineItems(selectedSequenceNodePathInfos.map((selectedNodePathInfo) => ({
      type: "sequence",
      nodePathInfo: selectedNodePathInfo
    })));
  }, [deleteTimelineItems, previewInteractive, selectedSequenceNodePathInfos]);
  const onDisableSequenceInteractivity = useCallback65(() => {
    if (disableInteractivityDisabled || !nodePath || !validatedLocation?.source || previewServerState.type !== "connected") {
      return;
    }
    disableSequenceInteractivity({
      fileName: validatedLocation.source,
      nodePath,
      setPropStatuses,
      clientId: previewServerState.clientId
    });
  }, [
    disableInteractivityDisabled,
    nodePath,
    previewServerState,
    setPropStatuses,
    validatedLocation?.source
  ]);
  const getSequenceDropTarget = useCallback65((clientY, dragData) => {
    if (!nodePath || !nodePathKey || !validatedLocation?.source) {
      return {
        type: "invalid",
        reason: "This sequence cannot be reordered from source."
      };
    }
    if (dragData.nodePathKey === nodePathKey) {
      return {
        type: "invalid",
        reason: "Drop onto another sequence to reorder."
      };
    }
    if (nodePathInfo?.numberOfSequencesWithThisNodePath !== 1) {
      return {
        type: "invalid",
        reason: "Programmatically duplicated sequences cannot be reordered."
      };
    }
    if (dragData.parentId !== parentId || dragData.fileName !== validatedLocation.source) {
      return {
        type: "invalid",
        reason: "Sequences can only be reordered with direct JSX siblings."
      };
    }
    const element = sequenceReorderWrapperRef.current;
    if (!element) {
      return null;
    }
    const rect = element.getBoundingClientRect();
    const before = clientY < rect.top + rect.height / 2;
    const insertionIndex = before ? siblingIndex : siblingIndex + 1;
    const toIndex = getDestinationIndex({
      fromIndex: dragData.siblingIndex,
      insertionIndex
    });
    if (toIndex === dragData.siblingIndex) {
      return {
        type: "invalid",
        reason: "This sequence is already in that position."
      };
    }
    return {
      type: "valid",
      dragData,
      position: before ? "before" : "after"
    };
  }, [
    nodePath,
    nodePathInfo?.numberOfSequencesWithThisNodePath,
    nodePathKey,
    parentId,
    siblingIndex,
    validatedLocation?.source
  ]);
  const reorderSequenceFromPointer = useCallback65(async (dropTarget) => {
    if (!canReorderSequence || previewServerState.type !== "connected" || !nodePath || !validatedLocation?.source) {
      return;
    }
    try {
      const result = await reorderSequence({
        fileName: validatedLocation.source,
        sourceNodePath: dropTarget.dragData.nodePath,
        targetNodePath: nodePath,
        position: dropTarget.position,
        clientId: previewServerState.clientId
      });
      if (!result.success) {
        showNotification(result.reason, 4000);
      }
    } catch (err) {
      showNotification(err.message, 4000);
    }
  }, [
    canReorderSequence,
    nodePath,
    previewServerState,
    validatedLocation?.source
  ]);
  useEffect52(() => {
    const element = sequenceReorderWrapperRef.current;
    if (!element || !canHandleSequenceDrag) {
      return;
    }
    const pointerDropTarget = {
      getDropTarget: getSequenceDropTarget,
      reorder: (dropTarget) => {
        reorderSequenceFromPointer(dropTarget).catch(() => {
          return;
        });
      },
      setDropIndicator: setSequenceDropIndicator
    };
    sequencePointerDropTargets.set(element, pointerDropTarget);
    return () => {
      sequencePointerDropTargets.delete(element);
    };
  }, [
    canHandleSequenceDrag,
    getSequenceDropTarget,
    reorderSequenceFromPointer
  ]);
  useEffect52(() => {
    return () => stopSequencePointerSession.current?.();
  }, []);
  const onSequencePointerDown = useCallback65((e) => {
    if (!(e.target instanceof Element)) {
      return;
    }
    if (e.button !== 0 || !e.isPrimary || e.target.closest("[data-remotion-sequence-reorder-row]") !== e.currentTarget || e.target.closest('button, input, textarea, select, [contenteditable="true"], [data-timeline-layer-eye]') || !canReorderSequence || !nodePath || !nodePathKey || !validatedLocation?.source) {
      return;
    }
    stopSequencePointerSession.current?.();
    const dragData = {
      fileName: validatedLocation.source,
      nodePath,
      nodePathKey,
      parentId,
      siblingIndex
    };
    const sourceElement = e.currentTarget;
    const sourceRect = sourceElement.getBoundingClientRect();
    const startX = e.clientX;
    const startY = e.clientY;
    let currentClientY = startY;
    let dragPreview = null;
    let activeDropTarget = null;
    let activeDrop = null;
    let didDrag = false;
    let previousUserSelect = "";
    let previousWebkitUserSelect = "";
    const updateDropTarget = (clientY) => {
      const pointerDropTarget = getSequencePointerDropTargetAtPoint({
        clientX: startX,
        clientY
      });
      const dropTarget = pointerDropTarget?.getDropTarget(clientY, dragData);
      if (activeDropTarget !== pointerDropTarget) {
        activeDropTarget?.setDropIndicator(null);
      }
      activeDropTarget = pointerDropTarget;
      activeDrop = dropTarget?.type === "valid" ? dropTarget : null;
      activeDropTarget?.setDropIndicator(activeDrop?.position ?? null);
    };
    const verticalScroll = timelineVerticalScroll.current;
    const autoScroll = startTimelineEdgeAutoScroll({
      includeHorizontal: false,
      includeVertical: true,
      verticalTopOffset: isStill ? 0 : TIMELINE_TIME_INDICATOR_HEIGHT,
      onTick: (directions) => {
        if (directions.y === null || !verticalScroll) {
          return;
        }
        verticalScroll.scrollTop += directions.y === "up" ? -EDGE_SCROLL_VERTICAL_INCREMENT : EDGE_SCROLL_VERTICAL_INCREMENT;
        updateDropTarget(currentClientY);
      }
    });
    stopSequencePointerSession.current = startDeferredCapturedPointerSession({
      captureTarget: sourceElement,
      event: e.nativeEvent,
      onMove: (pointerEvent, capturePointer) => {
        currentClientY = pointerEvent.clientY;
        if (!didDrag && Math.hypot(pointerEvent.clientX - startX, pointerEvent.clientY - startY) < SEQUENCE_REORDER_POINTER_THRESHOLD) {
          return;
        }
        if (!didDrag) {
          didDrag = true;
          capturePointer();
          previousUserSelect = document.body.style.userSelect;
          previousWebkitUserSelect = document.body.style.webkitUserSelect;
          document.body.style.userSelect = "none";
          document.body.style.webkitUserSelect = "none";
          dragPreview = sourceElement.cloneNode(true);
          dragPreview.setAttribute("aria-hidden", "true");
          dragPreview.setAttribute("data-remotion-sequence-reorder-preview", "true");
          dragPreview.removeAttribute("data-remotion-sequence-reorder-row");
          Object.assign(dragPreview.style, {
            height: `${sourceRect.height}px`,
            left: `${sourceRect.left}px`,
            margin: "0",
            opacity: "0.8",
            pointerEvents: "none",
            position: "fixed",
            top: `${sourceRect.top}px`,
            width: `${sourceRect.width}px`,
            zIndex: "2147483647"
          });
          document.body.appendChild(dragPreview);
        }
        pointerEvent.preventDefault();
        dragPreview.style.transform = `translate3d(0, ${pointerEvent.clientY - startY}px, 0)`;
        updateDropTarget(pointerEvent.clientY);
        autoScroll.update({ clientX: startX, clientY: pointerEvent.clientY });
      },
      onEnd: (reason, pointerEvent) => {
        const released = isPointerSessionRelease(reason, pointerEvent);
        if (released && didDrag) {
          updateDropTarget(pointerEvent.clientY);
        }
        autoScroll.stop();
        activeDropTarget?.setDropIndicator(null);
        dragPreview?.remove();
        stopSequencePointerSession.current = null;
        if (!didDrag) {
          return;
        }
        document.body.style.userSelect = previousUserSelect;
        document.body.style.webkitUserSelect = previousWebkitUserSelect;
        suppressNextClick.current = true;
        setTimeout(() => {
          suppressNextClick.current = false;
        }, 0);
        if (released && activeDrop) {
          activeDropTarget?.reorder(activeDrop);
        }
      }
    });
  }, [
    canReorderSequence,
    isStill,
    nodePath,
    nodePathKey,
    parentId,
    siblingIndex,
    validatedLocation?.source
  ]);
  const onSequenceClickCapture = useCallback65((e) => {
    if (!suppressNextClick.current) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    suppressNextClick.current = false;
  }, []);
  const mediaSrc = sequence.type === "audio" || sequence.type === "video" || sequence.type === "image" ? sequence.src : null;
  const isExpanded = previewConnected && nodePathInfo !== null && getIsExpanded(nodePathInfo);
  const codeHiddenStatus = propStatusesForOverride?.hidden;
  const runtimeHidden = useRuntimeValue(sequence.controls, "hidden");
  const isItemHidden = useMemo75(() => {
    const propStatus = codeHiddenStatus && codeHiddenStatus.status === "static" ? codeHiddenStatus.codeValue : undefined;
    const effective = propStatus ?? runtimeHidden;
    return effective ?? false;
  }, [codeHiddenStatus, runtimeHidden]);
  const onToggleVisibility = useCallback65((type) => {
    if (!sequence.controls || !nodePath || !validatedLocation || !propStatusesForOverride || !codeHiddenStatus || codeHiddenStatus.status !== "static" || previewServerState.type !== "connected") {
      return;
    }
    const newValue = type !== "enable";
    const { schema } = sequence.controls;
    const fieldSchema = schema.hidden;
    const defaultValue = fieldSchema && fieldSchema.type === "boolean" ? JSON.stringify(fieldSchema.default) : null;
    saveSequenceProps({
      addedKeyframes: null,
      movedKeyframes: null,
      changes: [
        {
          fileName: validatedLocation.source,
          nodePath,
          fieldKey: "hidden",
          value: newValue,
          defaultValue,
          schema
        }
      ],
      setPropStatuses,
      clientId: previewServerState.clientId,
      undoLabel: newValue ? "Hide sequence" : "Show sequence",
      redoLabel: newValue ? "Hide sequence again" : "Show sequence again"
    });
  }, [
    codeHiddenStatus,
    propStatusesForOverride,
    nodePath,
    previewServerState,
    sequence.controls,
    setPropStatuses,
    validatedLocation
  ]);
  const outerHeight = useMemo75(() => getTimelineLayerHeight(sequence.type) + TIMELINE_ITEM_BORDER_BOTTOM, [sequence.type]);
  const inner2 = useMemo75(() => {
    return {
      height: TIMELINE_LIST_ITEM_ROW_HEIGHT,
      color: WHITE,
      fontFamily: "Arial, Helvetica, sans-serif",
      wordBreak: "break-all",
      textAlign: "left",
      flexShrink: 0
    };
  }, []);
  const rowStyle = useMemo75(() => {
    return effectDropHovered ? {
      ...inner2,
      ...effectDropHighlight
    } : inner2;
  }, [effectDropHovered, inner2]);
  const hasExpandableContent = isStudioInteractivityEnabled() && (Boolean(sequence.controls) || sequence.effects.length > 0);
  const canToggleVisibility = previewInteractive && Boolean(sequence.controls) && nodePath !== null && validatedLocation !== null && codeHiddenStatus !== undefined && codeHiddenStatus !== null && codeHiddenStatus.status === "static";
  const showVisibilityToggle = canToggleVisibility || previewInteractive && showProvisionalVisibilityToggle;
  const onSequenceDoubleClick = useCallback65((e) => {
    if (isTimelineSelectionModifierEvent(e)) {
      e.stopPropagation();
      return;
    }
    const action = getSequenceDoubleClickAction({
      button: e.button,
      canOpenSource,
      numberOfConnectedCompositions: connectedCompositions.length,
      sequenceWasDragged: false
    });
    if (action === null) {
      return;
    }
    e.stopPropagation();
    if (action === "open-connected-composition") {
      const timelinePosition = getCurrentFrame();
      selectComposition(connectedCompositions[0], true, getConnectedCompositionFrame({
        timelinePosition,
        sequence,
        sequenceFrameOffset
      }));
      return;
    }
    openSource();
  }, [
    canOpenSource,
    connectedCompositions,
    openSource,
    selectComposition,
    sequence,
    sequenceFrameOffset
  ]);
  const canHandleSequenceDoubleClick = connectedCompositions.length === 1 || canOpenSource;
  const canRenameSelectedSequence = canRenameThisSequence && selected && selectedItems.length === 1 && selectedItems[0]?.type === "sequence";
  const onCancelRenaming = useCallback65(() => {
    setIsRenaming(false);
  }, []);
  const onSaveName = useCallback65(async (name) => {
    setIsRenaming(false);
    await saveName(name);
  }, [saveName]);
  React91.useEffect(() => {
    if (!canRenameSelectedSequence || !getStudioKeyboardShortcutsEnabled()) {
      setIsRenaming(false);
      return;
    }
    const onKeyDown = (e) => {
      if (!isHighestContext) {
        return;
      }
      const commandKey = window.navigator.platform.startsWith("Mac") ? e.metaKey : e.ctrlKey;
      if (commandKey || e.key.toLowerCase() !== "enter") {
        return;
      }
      if (document.activeElement instanceof HTMLInputElement || document.activeElement instanceof HTMLTextAreaElement || document.activeElement instanceof HTMLButtonElement) {
        return;
      }
      e.preventDefault();
      e.stopImmediatePropagation();
      setIsRenaming(true);
    };
    window.addEventListener("keydown", onKeyDown, true);
    return () => {
      window.removeEventListener("keydown", onKeyDown, true);
    };
  }, [canRenameSelectedSequence, isHighestContext]);
  const onRenameSequence = useCallback65(() => {
    if (!canRenameThisSequence) {
      return;
    }
    setIsRenaming(true);
  }, [canRenameThisSequence]);
  const canAddEffect = nodePathInfo?.supportsEffects === true && canMutateEffects && Boolean(validatedLocation?.source);
  const canCrop = useRuntimeValueSelector({
    controls: sequence.controls,
    selector: (runtimeValues) => {
      if (!previewInteractive || !sequence.controls || !nodePathInfo || !propStatusesForOverride || !validatedLocation?.source) {
        return false;
      }
      const activeSchema = Internals76.flattenActiveSchema(sequence.controls.schema, (key7) => runtimeValues[key7]);
      return canEditSelectedOutlineCrop({
        schema: activeSchema,
        propStatuses: propStatusesForOverride
      });
    }
  });
  const canRotate = nodePathInfo !== null;
  const { setManuallyEnabled } = useContext81(Transform3DModeStateContext);
  const onAddEffect = useCallback65(() => {
    if (!canAddEffect || previewServerState.type !== "connected" || !nodePath || !validatedLocation?.source) {
      return;
    }
    setSelectedModal({
      type: "add-effect",
      clientId: previewServerState.clientId,
      fileName: validatedLocation.source,
      nodePath
    });
  }, [
    canAddEffect,
    nodePath,
    previewServerState,
    setSelectedModal,
    validatedLocation?.source
  ]);
  const onCrop = useCallback65(() => {
    if (!canCrop || !nodePathInfo) {
      return;
    }
    selectItem({
      type: "sequence-prop",
      nodePathInfo: {
        ...nodePathInfo,
        auxiliaryKeys: ["controls", cropFieldKeys.left]
      },
      key: cropFieldKeys.left
    }, undefined, undefined, { reveal: true });
  }, [canCrop, nodePathInfo, selectItem]);
  const onRotate = useCallback65(() => {
    if (!canRotate || !nodePathInfo) {
      return;
    }
    setManuallyEnabled(getTimelineSequenceSelectionKey(nodePathInfo), true);
    selectItem({
      type: "sequence-prop",
      nodePathInfo: {
        ...nodePathInfo,
        auxiliaryKeys: ["controls", rotateFieldKey]
      },
      key: rotateFieldKey
    }, undefined, undefined, { reveal: true });
  }, [canRotate, nodePathInfo, selectItem, setManuallyEnabled]);
  const getContextMenuItems = useCallback65(() => {
    if (assetContextMenu !== null) {
      return assetContextMenu;
    }
    if (selectable && !selected) {
      onSelect({ shiftKey: false, toggleKey: false });
    }
    if (selectedSequenceNodePathInfos !== null) {
      return getMultiSequenceContextMenuItems({
        deleteDisabled: !previewInteractive,
        duplicateDisabled: !previewInteractive,
        onDeleteSelectedSequences,
        onDuplicateSelectedSequences
      });
    }
    const splitMenuItem = getSequenceSplitMenuItem({
      nodePathInfo,
      sequence,
      propStatuses: propStatusesForOverride,
      splitFrame: getCurrentFrame(),
      canEditSource: previewInteractive && Boolean(validatedLocation?.source),
      hasMultipleSelection: selected && selectedItems.length > 1
    });
    const freezeFrameMenuItem = getSequenceFreezeFrameMenuItem({
      clientId: previewInteractive && previewServerState.type === "connected" ? previewServerState.clientId : null,
      nodePath,
      propStatusesForOverride,
      sequence,
      sequenceFrameOffset,
      setPropStatuses,
      timelinePosition: getCurrentFrame(),
      validatedSource: validatedLocation?.source ?? null
    });
    return getSequenceContextMenuItems({
      assetLinkInfo: mediaSrc ? getTimelineAssetLinkInfo(mediaSrc) : null,
      canOpenInEditor,
      codingAgentInfo,
      copyImageElement: null,
      deleteDisabled,
      disableInteractivityDisabled,
      duplicateDisabled,
      editorInfo,
      includeSourceEditItems: isStudioInteractivityEnabled(),
      isProgrammaticallyDuplicated,
      onConfigureApps: canConfigureApps ? () => {
        setSelectedModal({
          type: "settings",
          initialTab: "apps",
          initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
        });
      } : null,
      onDeleteSequenceFromSource,
      onDisableSequenceInteractivity,
      onDuplicateSequenceFromSource,
      openInCodingAgent: openInCodingAgent2,
      openInEditor: openInEditor3,
      originalLocation,
      selectAsset,
      sequence,
      sourceActions: isStudioInteractivityEnabled() ? [
        ...nodePathInfo?.supportsEffects ? [
          {
            type: "item",
            id: "add-effect",
            keyHint: null,
            label: "Add effect...",
            leftItem: null,
            disabled: !canAddEffect,
            onClick: onAddEffect,
            quickSwitcherLabel: null,
            subMenu: null,
            value: "add-effect"
          }
        ] : [],
        ...canCrop ? [
          {
            type: "item",
            id: "crop",
            keyHint: null,
            label: isProgrammaticallyDuplicated ? "Crop all" : "Crop",
            leftItem: null,
            disabled: false,
            onClick: onCrop,
            quickSwitcherLabel: null,
            subMenu: null,
            value: "crop"
          }
        ] : [],
        ...canRotate ? [
          {
            type: "item",
            id: "rotate",
            keyHint: null,
            label: isProgrammaticallyDuplicated ? "Rotate all" : "Rotate",
            leftItem: null,
            disabled: false,
            onClick: onRotate,
            quickSwitcherLabel: null,
            subMenu: null,
            value: "rotate"
          }
        ] : [],
        ...canCrop || canRotate ? [
          {
            type: "divider",
            id: "transform-controls-divider"
          }
        ] : [],
        {
          type: "item",
          id: "rename-sequence",
          keyHint: null,
          label: "Rename...",
          leftItem: null,
          disabled: !canRenameThisSequence,
          onClick: () => {
            onRenameSequence();
          },
          quickSwitcherLabel: null,
          subMenu: null,
          value: "rename-sequence"
        },
        ...splitMenuItem ? [splitMenuItem] : [],
        ...freezeFrameMenuItem ? [freezeFrameMenuItem] : []
      ] : []
    });
  }, [
    assetContextMenu,
    canAddEffect,
    canCrop,
    canRotate,
    canConfigureApps,
    canOpenInEditor,
    canRenameThisSequence,
    codingAgentInfo,
    deleteDisabled,
    disableInteractivityDisabled,
    duplicateDisabled,
    editorInfo,
    isProgrammaticallyDuplicated,
    mediaSrc,
    nodePath,
    nodePathInfo,
    onAddEffect,
    onCrop,
    onRotate,
    onDeleteSequenceFromSource,
    onDeleteSelectedSequences,
    onDisableSequenceInteractivity,
    onDuplicateSequenceFromSource,
    onDuplicateSelectedSequences,
    onRenameSequence,
    onSelect,
    openInCodingAgent2,
    openInEditor3,
    originalLocation,
    previewInteractive,
    previewServerState,
    propStatusesForOverride,
    selectAsset,
    selectable,
    selected,
    selectedItems.length,
    selectedSequenceNodePathInfos,
    sequence,
    sequenceFrameOffset,
    setSelectedModal,
    setPropStatuses,
    validatedLocation?.source
  ]);
  const canDropEffect = canMutateEffects && nodePath !== null && validatedLocation !== null && sequence.controls?.supportsEffects === true;
  const sequenceReorderLineStyle = useMemo75(() => {
    if (sequenceDropIndicator !== "before") {
      return null;
    }
    return {
      ...sequenceReorderLineBase,
      top: -1
    };
  }, [sequenceDropIndicator]);
  const onEffectDragOver = useCallback65((e) => {
    if (!canDropEffect || !hasEffectDragType(e.dataTransfer)) {
      return;
    }
    e.preventDefault();
    e.dataTransfer.dropEffect = "copy";
    setEffectDropHovered(true);
  }, [canDropEffect]);
  const onEffectDragLeave = useCallback65((e) => {
    if (e.currentTarget.contains(e.relatedTarget)) {
      return;
    }
    setEffectDropHovered(false);
  }, []);
  const onEffectDrop = useCallback65(async (e) => {
    if (!canDropEffect || previewServerState.type !== "connected" || nodePath === null || validatedLocation === null || !hasEffectDragType(e.dataTransfer)) {
      return;
    }
    const dragData = getEffectDragData(e.dataTransfer);
    if (!dragData) {
      if (hasExplicitEffectDragType(e.dataTransfer)) {
        e.preventDefault();
        e.stopPropagation();
        setEffectDropHovered(false);
        showNotification("Could not read effect drag data", 3000);
      }
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    setEffectDropHovered(false);
    await addEffectFromDragData({
      dragData,
      fileName: validatedLocation.source,
      nodePath,
      clientId: previewServerState.clientId
    });
  }, [canDropEffect, nodePath, previewServerState, validatedLocation]);
  const trackRow = /* @__PURE__ */ jsx129(TimelineRowChrome, {
    depth: nestedDepth,
    eye: showVisibilityToggle ? /* @__PURE__ */ jsx129(TimelineLayerEye, {
      type: sequence.type === "audio" ? "speaker" : "eye",
      hidden: isItemHidden,
      onInvoked: canToggleVisibility ? onToggleVisibility : null
    }) : /* @__PURE__ */ jsx129(TimelineLayerEyeSpacer, {}),
    arrow: /* @__PURE__ */ jsx129(TimelineLayerChildrenToggle, {
      sequence
    }),
    style: rowStyle,
    selected,
    selectable,
    onSelect,
    showSelectedBackground: true,
    containsSelection,
    hovered,
    outerHeight,
    onDragLeave: canDropEffect ? onEffectDragLeave : undefined,
    onDragOver: canDropEffect ? onEffectDragOver : undefined,
    onDrop: canDropEffect ? onEffectDrop : undefined,
    onDoubleClick: canHandleSequenceDoubleClick ? onSequenceDoubleClick : undefined,
    onPointerEnter,
    onPointerLeave,
    children: /* @__PURE__ */ jsxs67("div", {
      style: labelContainerStyle,
      children: [
        connectedCompositions.length > 0 ? /* @__PURE__ */ jsxs67(Fragment32, {
          children: [
            /* @__PURE__ */ jsx129(CompositionOrStillIcon, {
              color: getTimelineColor(false, false),
              composition: connectedCompositions[0],
              style: connectedCompositionIconStyle
            }),
            /* @__PURE__ */ jsx129(Spacing, {
              x: 0.5
            })
          ]
        }) : null,
        /* @__PURE__ */ jsx129(TimelineSequenceName, {
          displayName: timelineDisplayName,
          fallbackDisplayName,
          selected,
          containsSelection,
          editing: isRenaming,
          onCancelEditing: onCancelRenaming,
          onSaveName
        }),
        numberOfHiddenDuplicates > 0 ? /* @__PURE__ */ jsxs67(Fragment32, {
          children: [
            /* @__PURE__ */ jsx129(Spacing, {
              x: 0.5
            }),
            /* @__PURE__ */ jsx129(TimelineDuplicateCount, {
              count: numberOfHiddenDuplicates
            })
          ]
        }) : null,
        hasExpandableContent && nodePathInfo !== null ? /* @__PURE__ */ jsxs67(Fragment32, {
          children: [
            /* @__PURE__ */ jsx129(Spacing, {
              x: 0.5
            }),
            /* @__PURE__ */ jsx129(TimelineSequenceExpandArrow, {
              disabled: !previewInteractive,
              isExpanded,
              nodePathInfo,
              selectedItems,
              sequence
            })
          ]
        }) : null,
        mediaSrc ? /* @__PURE__ */ jsxs67(Fragment32, {
          children: [
            /* @__PURE__ */ jsx129(Spacing, {
              x: 0.5
            }),
            " ",
            /* @__PURE__ */ jsx129(TimelineMediaInfo, {
              src: mediaSrc
            })
          ]
        }) : null
      ]
    })
  });
  const reorderableTrackRow = canHandleSequenceDrag ? /* @__PURE__ */ jsxs67("div", {
    ref: sequenceReorderWrapperRef,
    "data-remotion-sequence-reorder-row": true,
    onClickCapture: onSequenceClickCapture,
    onPointerDownCapture: onSequencePointerDown,
    style: sequenceReorderWrapper,
    children: [
      sequenceReorderLineStyle ? /* @__PURE__ */ jsx129("div", {
        style: sequenceReorderLineStyle
      }) : null,
      trackRow,
      sequenceDropIndicator === "after" ? /* @__PURE__ */ jsx129("div", {
        style: {
          ...sequenceReorderAfterLineWrapper,
          top: afterDropLineOffset
        },
        children: /* @__PURE__ */ jsx129("div", {
          style: sequenceReorderAfterLine
        })
      }) : null
    ]
  }) : trackRow;
  return /* @__PURE__ */ jsxs67(Fragment32, {
    children: [
      previewConnected || window.remotion_isReadOnlyStudio ? /* @__PURE__ */ jsx129(ContextMenu, {
        getItems: getContextMenuItems,
        children: reorderableTrackRow
      }) : reorderableTrackRow,
      previewConnected && isStudioInteractivityEnabled() && isExpanded && hasExpandableContent && nodePathInfo && validatedLocation ? /* @__PURE__ */ jsx129(TimelineExpandedSection, {
        sequence,
        validatedLocation,
        nodePathInfo,
        nestedDepth,
        keyframeDisplayOffset
      }) : null
    ]
  });
};
var TimelineSequenceItem = React91.memo(TimelineSequenceItemInner);

// src/components/Timeline/TimelineList.tsx
import { jsx as jsx130 } from "react/jsx-runtime";
var container22 = {
  flex: 1,
  background: BACKGROUND,
  position: "relative"
};
var noConnectedCompositions = [];
var TimelineListTrack = React92.memo(({ row: row4 }) => {
  const { afterDropLineOffset, siblingIndex, track } = row4;
  return /* @__PURE__ */ jsx130(TimelineSequenceItem, {
    afterDropLineOffset,
    siblingIndex,
    connectedCompositions: track.connectedCompositions ?? noConnectedCompositions,
    nestedDepth: track.depth,
    sequence: track.sequence,
    nodePathInfo: track.nodePathInfo,
    keyframeDisplayOffset: track.keyframeDisplayOffset,
    sequenceFrameOffset: track.sequenceFrameOffset,
    numberOfHiddenDuplicates: Math.max(0, (track.displayGroup?.numberOfSequences ?? 1) - 1),
    showProvisionalVisibilityToggle: track.nodePathInfo === null && track.displayGroup !== null
  });
});
var TimelineList = () => {
  const { rows, tracksEnd, virtualItems } = useTimelineVirtualization();
  return /* @__PURE__ */ jsx130("div", {
    style: { ...container22, height: tracksEnd },
    children: virtualItems.map((virtualItem) => /* @__PURE__ */ jsx130("div", {
      style: {
        height: virtualItem.size,
        left: 0,
        position: "absolute",
        top: virtualItem.start,
        width: "100%"
      },
      children: /* @__PURE__ */ jsx130(TimelineListTrack, {
        row: rows[virtualItem.index]
      })
    }, virtualItem.key))
  });
};

// src/components/Timeline/TimelinePinchZoom.tsx
import { useCallback as useCallback66, useContext as useContext82, useEffect as useEffect53, useRef as useRef42 } from "react";
import { Internals as Internals77 } from "remotion";
var ZOOM_WHEEL_SENSITIVITY = 0.06;
var TimelinePinchZoom = () => {
  const isVideoComposition = useIsVideoComposition();
  const videoConfig = Internals77.useUnsafeVideoConfig();
  const { zoom, setZoom } = useContext82(TimelineZoomCtx);
  const { editorZoomGestures } = useContext82(EditorZoomGesturesContext);
  const zoomRef = useRef42(zoom);
  zoomRef.current = zoom;
  const getCurrentTimelineZoom = useCallback66((compositionId) => {
    return getTimelineZoom({
      durationInFrames: videoConfig?.durationInFrames ?? 1,
      timelineViewportWidth: scrollableRef.current?.clientWidth ?? 0,
      zoom: zoomRef.current[compositionId] ?? null
    });
  }, [videoConfig?.durationInFrames]);
  const pinchBaseZoomRef = useRef42(null);
  const suppressWheelFromWebKitPinchRef = useRef42(false);
  const touchPinchRef = useRef42(null);
  const onWheel = useCallback66((e) => {
    if (!editorZoomGestures || !isVideoComposition) {
      return;
    }
    const { ctrlKey, metaKey, clientX, deltaY, deltaMode } = e;
    const wantsToZoom = ctrlKey || metaKey;
    if (!wantsToZoom) {
      return;
    }
    if (suppressWheelFromWebKitPinchRef.current && wantsToZoom) {
      e.preventDefault();
      return;
    }
    if (!videoConfig || videoConfig.durationInFrames < 2) {
      return;
    }
    const scrollEl = scrollableRef.current;
    if (!scrollEl) {
      return;
    }
    e.preventDefault();
    const anchorContentX = viewportClientXToScrollContentX({
      clientX,
      scrollEl
    });
    let scaledDeltaY = deltaY;
    if (deltaMode === WheelEvent.DOM_DELTA_LINE) {
      scaledDeltaY *= 16;
    } else if (deltaMode === WheelEvent.DOM_DELTA_PAGE) {
      scaledDeltaY *= scrollEl.clientHeight;
    }
    setZoom(videoConfig.id, (z2) => z2 * Math.exp(-scaledDeltaY * ZOOM_WHEEL_SENSITIVITY), { anchorFrame: null, anchorContentX });
  }, [editorZoomGestures, isVideoComposition, videoConfig, setZoom]);
  const supportsWebKitPinch = typeof window !== "undefined" && "GestureEvent" in window;
  useEffect53(() => {
    const el = timelineVerticalScroll.current;
    if (!el) {
      return;
    }
    el.addEventListener("wheel", onWheel, { passive: false });
    return () => {
      el.removeEventListener("wheel", onWheel);
    };
  }, [onWheel]);
  useEffect53(() => {
    const el = timelineVerticalScroll.current;
    if (!el || !editorZoomGestures || !supportsWebKitPinch) {
      return;
    }
    const endWebKitPinch = () => {
      pinchBaseZoomRef.current = null;
      suppressWheelFromWebKitPinchRef.current = false;
    };
    const onGestureStart = (event) => {
      const e = event;
      if (!isVideoComposition) {
        return;
      }
      if (!videoConfig || videoConfig.durationInFrames < 2) {
        return;
      }
      const scrollEl = scrollableRef.current;
      if (!scrollEl) {
        return;
      }
      e.preventDefault();
      suppressWheelFromWebKitPinchRef.current = true;
      pinchBaseZoomRef.current = getCurrentTimelineZoom(videoConfig.id);
    };
    const onGestureChange = (event) => {
      const e = event;
      const base = pinchBaseZoomRef.current;
      if (base === null || !isVideoComposition || !videoConfig || videoConfig.durationInFrames < 2) {
        return;
      }
      const scrollEl = scrollableRef.current;
      if (!scrollEl) {
        return;
      }
      e.preventDefault();
      const anchorContentX = viewportClientXToScrollContentX({
        clientX: e.clientX,
        scrollEl
      });
      setZoom(videoConfig.id, () => base * e.scale, {
        anchorFrame: null,
        anchorContentX
      });
    };
    const onGestureEnd = () => {
      endWebKitPinch();
    };
    el.addEventListener("gesturestart", onGestureStart, { passive: false });
    el.addEventListener("gesturechange", onGestureChange, { passive: false });
    el.addEventListener("gestureend", onGestureEnd);
    el.addEventListener("gesturecancel", onGestureEnd);
    return () => {
      el.removeEventListener("gesturestart", onGestureStart);
      el.removeEventListener("gesturechange", onGestureChange);
      el.removeEventListener("gestureend", onGestureEnd);
      el.removeEventListener("gesturecancel", onGestureEnd);
    };
  }, [
    editorZoomGestures,
    supportsWebKitPinch,
    isVideoComposition,
    videoConfig,
    getCurrentTimelineZoom,
    setZoom
  ]);
  useEffect53(() => {
    const el = timelineVerticalScroll.current;
    if (!el || !editorZoomGestures) {
      return;
    }
    const onTouchStart = (event) => {
      if (event.touches.length !== 2) {
        touchPinchRef.current = null;
        return;
      }
      if (!isVideoComposition || !videoConfig || videoConfig.durationInFrames < 2) {
        return;
      }
      const [t0, t1] = [event.touches[0], event.touches[1]];
      const initialDistance = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY);
      if (initialDistance < 0.000001) {
        return;
      }
      touchPinchRef.current = {
        initialDistance,
        initialZoom: getCurrentTimelineZoom(videoConfig.id)
      };
    };
    const onTouchMove = (event) => {
      const pinch = touchPinchRef.current;
      if (pinch === null || event.touches.length !== 2 || !videoConfig || videoConfig.durationInFrames < 2) {
        return;
      }
      const scrollEl = scrollableRef.current;
      if (!scrollEl) {
        return;
      }
      event.preventDefault();
      const [t0, t1] = [event.touches[0], event.touches[1]];
      const dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY);
      const ratio = dist / pinch.initialDistance;
      const clientX = (t0.clientX + t1.clientX) / 2;
      const anchorContentX = viewportClientXToScrollContentX({
        clientX,
        scrollEl
      });
      setZoom(videoConfig.id, () => pinch.initialZoom * ratio, {
        anchorFrame: null,
        anchorContentX
      });
    };
    const onTouchEnd = (event) => {
      if (event.touches.length < 2) {
        touchPinchRef.current = null;
      }
    };
    el.addEventListener("touchstart", onTouchStart, { passive: true });
    el.addEventListener("touchmove", onTouchMove, { passive: false });
    el.addEventListener("touchend", onTouchEnd);
    el.addEventListener("touchcancel", onTouchEnd);
    return () => {
      el.removeEventListener("touchstart", onTouchStart);
      el.removeEventListener("touchmove", onTouchMove);
      el.removeEventListener("touchend", onTouchEnd);
      el.removeEventListener("touchcancel", onTouchEnd);
    };
  }, [
    editorZoomGestures,
    isVideoComposition,
    videoConfig,
    getCurrentTimelineZoom,
    setZoom
  ]);
  return null;
};

// src/components/Timeline/TimelinePlayCursorSyncer.tsx
import { useContext as useContext83, useEffect as useEffect54 } from "react";
import { Internals as Internals78 } from "remotion";
var lastTimelinePositionWhileScrolling = null;
var TimelinePlayCursorSyncer = () => {
  const video = Internals78.useVideo();
  const timelinePosition = Internals78.Timeline.useTimelinePosition();
  const playing = Internals78.usePlaying();
  const { playbackRate } = Internals78.usePlaybackRate();
  const { zoom: zoomMap } = useContext83(TimelineZoomCtx);
  const zoom = getTimelineZoom({
    durationInFrames: video?.durationInFrames ?? 1,
    timelineViewportWidth: scrollableRef.current?.clientWidth ?? 0,
    zoom: video ? zoomMap[video.id] ?? null : null
  });
  if (video) {
    setCurrentFrame(timelinePosition);
    setCurrentZoom(zoom);
    setCurrentDuration(video.durationInFrames);
    setCurrentFps(video.fps);
  }
  useEffect54(() => {
    if (!video) {
      return;
    }
    if (!playing) {
      return;
    }
    ensureFrameIsInViewport({
      direction: playbackRate > 0 ? "page-right" : "page-left",
      durationInFrames: video.durationInFrames,
      frame: timelinePosition
    });
  }, [playbackRate, playing, timelinePosition, video]);
  useEffect54(() => {
    const { current } = scrollableRef;
    if (!current) {
      return;
    }
    if (playing) {
      lastTimelinePositionWhileScrolling = {
        scrollLeft: current.scrollLeft,
        frame: getCurrentFrame(),
        zoomLevel: getCurrentZoom(),
        durationInFrames: getCurrentDuration()
      };
    } else if (lastTimelinePositionWhileScrolling !== null) {
      if (isCursorInViewport({
        frame: getCurrentFrame(),
        durationInFrames: getCurrentDuration()
      })) {
        return;
      }
      if (lastTimelinePositionWhileScrolling.zoomLevel === getCurrentZoom() && lastTimelinePositionWhileScrolling.durationInFrames === getCurrentDuration()) {
        current.scrollLeft = lastTimelinePositionWhileScrolling.scrollLeft;
      } else {
        ensureFrameIsInViewport({
          direction: "center",
          durationInFrames: getCurrentDuration(),
          frame: lastTimelinePositionWhileScrolling.frame
        });
      }
    }
  }, [playing]);
  return null;
};

// src/components/Timeline/TimelineScrollable.tsx
import { useMemo as useMemo76 } from "react";

// src/components/Timeline/TimelineAssetDropIndicator.tsx
import { useContext as useContext84 } from "react";
import { Internals as Internals79 } from "remotion";
import { jsx as jsx131 } from "react/jsx-runtime";
var indicator = {
  backgroundColor: RULER_COLOR,
  bottom: 0,
  pointerEvents: "none",
  position: "absolute",
  top: 0,
  width: 1,
  zIndex: 10
};
var TimelineAssetDropIndicator = () => {
  const frame2 = useContext84(TimelineAssetDropFrameContext);
  const timelineWidth = useContext84(TimelineWidthContext);
  const videoConfig = Internals79.useUnsafeVideoConfig();
  if (frame2 === null || timelineWidth === null || videoConfig === null) {
    return null;
  }
  const width = sliderAreaRef.current?.clientWidth ?? timelineWidth;
  const left3 = getXPositionOfItemInTimelineImperatively(frame2, videoConfig.durationInFrames, width);
  return /* @__PURE__ */ jsx131("div", {
    "data-timeline-asset-drop-indicator": "true",
    style: {
      ...indicator,
      transform: `translateX(${left3 - 0.5}px)`
    }
  });
};

// src/components/Timeline/TimelineViewport.tsx
import {
  createContext as createContext15,
  useCallback as useCallback67,
  useLayoutEffect as useLayoutEffect21,
  useState as useState48
} from "react";
import { jsx as jsx132 } from "react/jsx-runtime";
var TimelineViewportContext = createContext15(null);
var getTimelineRenderWindow = (scrollable) => {
  const viewportWidth = scrollable.clientWidth;
  if (viewportWidth === 0) {
    return { left: 0, width: 0 };
  }
  const currentSection = Math.floor((scrollable.scrollLeft + viewportWidth / 2) / viewportWidth);
  const left3 = Math.max(0, (currentSection - 1) * viewportWidth);
  return {
    left: left3,
    width: viewportWidth * 3
  };
};
var TimelineViewportProvider = ({ children, scrollable }) => {
  const [renderWindow, setRenderWindow] = useState48({
    left: 0,
    width: window.innerWidth * 3
  });
  const update = useCallback67(() => {
    const element = scrollable.current;
    if (!element) {
      return;
    }
    const next = getTimelineRenderWindow(element);
    setRenderWindow((previous) => {
      if (previous.left === next.left && previous.width === next.width) {
        return previous;
      }
      return next;
    });
  }, [scrollable]);
  useLayoutEffect21(() => {
    let element = null;
    let resizeObserver = null;
    const animationFrame = requestAnimationFrame(() => {
      element = scrollable.current;
      if (!element) {
        return;
      }
      resizeObserver = new ResizeObserver(update);
      resizeObserver.observe(element);
      update();
      element.addEventListener("scroll", update);
    });
    return () => {
      cancelAnimationFrame(animationFrame);
      resizeObserver?.disconnect();
      element?.removeEventListener("scroll", update);
    };
  }, [scrollable, update]);
  return /* @__PURE__ */ jsx132(TimelineViewportContext.Provider, {
    value: renderWindow,
    children
  });
};

// src/components/Timeline/TimelineScrollable.tsx
import { jsx as jsx133, jsxs as jsxs68 } from "react/jsx-runtime";
var outer = {
  width: "100%",
  height: "100%",
  overflowX: "auto",
  overflowY: "hidden",
  overscrollBehaviorX: "none",
  position: "relative",
  backgroundColor: TIMELINE_BACKGROUND
};
var marqueeStyle = {
  backgroundColor: TIMELINE_MARQUEE_BLUE_ALPHA_16,
  border: BORDER_TIMELINE_MARQUEE_BLUE,
  boxSizing: "border-box",
  pointerEvents: "none",
  position: "fixed",
  zIndex: 10
};
var TimelineScrollable = ({ children }) => {
  const { marqueeRect, onPointerDownCapture } = useTimelineMarqueeSelection();
  const containerStyle2 = useMemo76(() => {
    return {
      width: "100%",
      minHeight: "100%"
    };
  }, []);
  return /* @__PURE__ */ jsxs68("div", {
    ref: scrollableRef,
    "data-timeline-scrollable": "true",
    style: outer,
    className: HORIZONTAL_SCROLLBAR_CLASSNAME,
    onPointerDownCapture,
    children: [
      /* @__PURE__ */ jsx133(TimelineViewportProvider, {
        scrollable: scrollableRef,
        children: /* @__PURE__ */ jsx133("div", {
          style: containerStyle2,
          children
        })
      }),
      /* @__PURE__ */ jsx133(TimelineAssetDropIndicator, {}),
      marqueeRect === null ? null : /* @__PURE__ */ jsx133("div", {
        style: {
          ...marqueeStyle,
          height: marqueeRect.bottom - marqueeRect.top,
          left: marqueeRect.left,
          top: marqueeRect.top,
          width: marqueeRect.right - marqueeRect.left
        }
      })
    ]
  });
};

// src/components/Timeline/TimelineSequenceRightEdgeDragHandle.tsx
import { sortItemsByCommitOrder } from "@remotion/canvas";
import {
  stringifySequenceExpandedRowKey,
  stringifySequenceSubscriptionKey as stringifySequenceSubscriptionKey3
} from "@remotion/studio-shared";
import React96, { useCallback as useCallback68, useContext as useContext85, useEffect as useEffect55, useRef as useRef43 } from "react";
import { Internals as Internals80 } from "remotion";
import { NoReactInternals as NoReactInternals12 } from "remotion/no-react";
import { jsx as jsx134 } from "react/jsx-runtime";
var HANDLE_INSET = 6;
var HANDLE_OUTSET = 8;
var timelineSequenceEdgeDragThresholdPx = 4;
var timelineSequenceFromDragSnapThresholdPx = 10;
var TimelineSequenceMediaDurationDragLimitsContext = React96.createContext(null);
var TimelineSequenceMediaDurationDragLimitsProvider = ({ children }) => {
  const limits = useRef43(new Map);
  return /* @__PURE__ */ jsx134(TimelineSequenceMediaDurationDragLimitsContext.Provider, {
    value: limits,
    children
  });
};
var getTimelineSequenceMediaDurationDragLimits = ({
  cascadedStart,
  displayDurationInFrames,
  displayStart,
  effectiveMaxMediaDuration,
  explicitDurationInFrames,
  hasImplicitDuration,
  naturalMediaDuration,
  timelineDurationInFrames
}) => {
  if (naturalMediaDuration === null || !Number.isFinite(naturalMediaDuration) || naturalMediaDuration <= 0) {
    return null;
  }
  const initialDuration = explicitDurationInFrames ?? Math.ceil(displayStart + Math.max(0, Math.min(displayDurationInFrames, timelineDurationInFrames - displayStart, effectiveMaxMediaDuration ?? Infinity)) - cascadedStart);
  const maximumDuration = Math.ceil(displayStart + naturalMediaDuration - cascadedStart) - (hasImplicitDuration ? 1 : 0);
  if (hasImplicitDuration && initialDuration > maximumDuration) {
    return null;
  }
  return { initialDuration, maximumDuration };
};
var getTimelineSequenceEdgeSelectionInteraction = ({
  button: button2,
  selected,
  shiftKey,
  metaKey,
  ctrlKey
}) => {
  if (button2 !== 0 || !shouldSelectTimelineRowOnPointerDown({
    selected,
    shiftKey,
    metaKey,
    ctrlKey
  })) {
    return null;
  }
  return { shiftKey, toggleKey: metaKey || ctrlKey };
};
var baseStyle3 = {
  position: "absolute",
  top: 0,
  bottom: 0,
  width: `calc(${HANDLE_OUTSET}px + min(${HANDLE_INSET}px, 25%))`,
  cursor: "ew-resize",
  zIndex: 1,
  touchAction: "none"
};
var getKeyframedSequenceDragTargets = ({
  nodePath,
  sequence,
  propStatuses,
  isDescendant
}) => {
  const status = propStatuses[Internals80.makeSequencePropsSubscriptionKey(nodePath)];
  if (status === null || status === undefined || !status.canUpdate) {
    return { effectKeyframes: [], sequenceKeyframes: [] };
  }
  const sequenceSchema = sequence.controls?.schema;
  const sequenceKeyframes = sequenceSchema === undefined ? [] : Object.entries(status.props).flatMap(([fieldKey, propStatus]) => propStatus.status === "keyframed" && (!isDescendant || propStatus.keyframeDisplayOffsetAdjustment !== null) ? [
    {
      fileName: nodePath.absolutePath,
      fieldKey,
      isDescendant,
      nodePath,
      schema: sequenceSchema,
      status: propStatus
    }
  ] : []);
  const effectKeyframes = status.effects.flatMap((effectStatus) => {
    if (!effectStatus.canUpdate) {
      return [];
    }
    const effectSchema = sequence.effects[effectStatus.effectIndex]?.schema;
    if (effectSchema === undefined) {
      return [];
    }
    return Object.entries(effectStatus.props).flatMap(([fieldKey, propStatus]) => propStatus.status === "keyframed" && (!isDescendant || propStatus.keyframeDisplayOffsetAdjustment !== null) ? [
      {
        effectIndex: effectStatus.effectIndex,
        fileName: nodePath.absolutePath,
        fieldKey,
        isDescendant,
        nodePath,
        schema: effectSchema,
        status: propStatus
      }
    ] : []);
  });
  return { effectKeyframes, sequenceKeyframes };
};
var shiftKeyframedStatus = ({
  status,
  deltaFrames,
  isDescendant
}) => {
  if (isDescendant) {
    const adjustment = status.keyframeDisplayOffsetAdjustment;
    if (adjustment === null) {
      throw new Error("Expected descendant keyframes to use an outer frame clock");
    }
    return {
      ...status,
      keyframeDisplayOffsetAdjustment: null,
      keyframes: status.keyframes.map((keyframe) => ({
        ...keyframe,
        frame: keyframe.frame + adjustment
      }))
    };
  }
  return {
    ...status,
    keyframes: status.keyframes.map((keyframe) => ({
      ...keyframe,
      frame: keyframe.frame + deltaFrames
    }))
  };
};
var canUpdateDurationInFrames = ({
  propStatuses,
  nodePath
}) => {
  const status = Internals80.getPropStatusesCtx(propStatuses, nodePath)?.durationInFrames?.status;
  return status === "static";
};
var canUpdateFrom = ({
  propStatuses,
  nodePath
}) => {
  const status = Internals80.getPropStatusesCtx(propStatuses, nodePath)?.from?.status;
  return status === "static";
};
var canUpdateTrimBefore = ({
  propStatuses,
  nodePath
}) => {
  const status = Internals80.getPropStatusesCtx(propStatuses, nodePath)?.trimBefore?.status;
  return status === "static";
};
var isTransitionSeriesSequence = (sequence) => sequence.controls?.componentIdentity === "dev.remotion.transitions.TransitionSeries.Sequence";
var isSeriesSequence = (sequence) => sequence.controls?.componentIdentity === "dev.remotion.remotion.Series.Sequence";
var isCascadingSequence = (sequence) => isSeriesSequence(sequence) || isTransitionSeriesSequence(sequence);
var isTransitionSeriesTransition = (sequence) => sequence?.controls?.componentIdentity === "dev.remotion.transitions.TransitionSeries.Transition";
var getMinimumSequenceDuration = ({
  sequence,
  sequences
}) => {
  if (!isTransitionSeriesSequence(sequence)) {
    return 1;
  }
  const siblings = sortItemsByCommitOrder(sequences.filter((candidate) => candidate.parent === sequence.parent && (isTransitionSeriesSequence(candidate) || isTransitionSeriesTransition(candidate))), (candidate) => candidate.timelineOrder);
  const sequenceIndex = siblings.findIndex((candidate) => candidate.id === sequence.id);
  if (sequenceIndex === -1) {
    return 1;
  }
  const previous = siblings[sequenceIndex - 1];
  const next = siblings[sequenceIndex + 1];
  return Math.max(1, isTransitionSeriesTransition(previous) ? previous.duration : 1, isTransitionSeriesTransition(next) ? next.duration : 1);
};
var isTimelineSequenceDurationDraggable = (sequence) => {
  const isInteractiveCascadingSequence = isCascadingSequence(sequence);
  return (sequence.type === "sequence" || sequence.type === "image" || sequence.type === "audio" || sequence.type === "video") && !sequence.loopDisplay && (!sequence.isInsideSeries || isInteractiveCascadingSequence) && Boolean(sequence.controls);
};
var canResizeTimelineSequenceDuration = ({
  status
}) => {
  return status?.status === "static";
};
var isTimelineSequenceLeftEdgeDraggable = (sequence) => {
  return (!sequence.isInsideSeries || isCascadingSequence(sequence)) && Boolean(sequence.controls) && (sequence.type === "sequence" || sequence.type === "image" || sequence.type === "audio" || sequence.type === "video");
};
var playbackRateComponentIdentities = new Set([
  "dev.remotion.gif.Gif",
  "dev.remotion.media.Audio",
  "dev.remotion.media.Video",
  "dev.remotion.remotion.AnimatedImage"
]);
var getTrimBeforePlaybackRate = (sequence) => {
  const componentIdentity = sequence.controls?.componentIdentity;
  if (componentIdentity === null || componentIdentity === undefined || !playbackRateComponentIdentities.has(componentIdentity)) {
    return 1;
  }
  const runtimePlaybackRate = sequence.controls?.runtimeValues.getSnapshot().playbackRate;
  return typeof runtimePlaybackRate === "number" ? runtimePlaybackRate : 1;
};
var isFromDraggableSequence = (sequence) => {
  return !sequence.loopDisplay && !sequence.isInsideSeries && Boolean(sequence.controls);
};
var getTimelineSequenceDurationDragValue = ({
  initialDuration,
  deltaFrames,
  maximumDuration,
  minimumDuration = 1
}) => Math.min(maximumDuration, Math.max(minimumDuration, initialDuration + deltaFrames));
var getTimelineSequenceLeftEdgeDragDelta = ({
  initialDuration,
  initialTrimBefore,
  deltaFrames,
  playbackRate,
  minimumDuration = 1
}) => {
  const minDeltaFrames = 0 - initialTrimBefore / playbackRate;
  const maxDeltaFrames = initialDuration - minimumDuration;
  return Math.max(minDeltaFrames, Math.min(deltaFrames, maxDeltaFrames));
};
var getTimelineSequenceLeftEdgeDragValues = ({
  initialDuration,
  initialFrom,
  initialTrimBefore,
  deltaFrames,
  playbackRate,
  minimumDuration = 1
}) => {
  const clampedDeltaFrames = getTimelineSequenceLeftEdgeDragDelta({
    initialDuration,
    initialTrimBefore,
    deltaFrames,
    playbackRate,
    minimumDuration
  });
  return {
    durationInFrames: initialDuration - clampedDeltaFrames,
    from: initialFrom + clampedDeltaFrames,
    trimBefore: clampedDeltaFrames === -initialTrimBefore / playbackRate ? 0 : initialTrimBefore + clampedDeltaFrames * playbackRate
  };
};
var getTimelineSequenceLeftEdgeDragChanges = ({
  targets,
  deltaFrames
}) => {
  return targets.flatMap((target) => {
    const nextValues = getTimelineSequenceLeftEdgeDragValues({
      initialDuration: target.initialDuration,
      initialFrom: target.initialFrom,
      initialTrimBefore: target.initialTrimBefore,
      deltaFrames,
      playbackRate: target.playbackRate,
      minimumDuration: target.minimumDuration
    });
    const changes = [];
    if (target.positionField !== null && nextValues.from !== target.initialFrom) {
      changes.push({
        fileName: target.fileName,
        nodePath: target.nodePath,
        fieldKey: target.positionField,
        value: nextValues.from,
        defaultValue: "0",
        schema: target.schema
      });
    }
    if (nextValues.durationInFrames !== target.initialDuration) {
      changes.push({
        fileName: target.fileName,
        nodePath: target.nodePath,
        fieldKey: "durationInFrames",
        value: nextValues.durationInFrames,
        defaultValue: null,
        schema: target.schema
      });
    }
    if (nextValues.trimBefore !== target.initialTrimBefore) {
      changes.push({
        fileName: target.fileName,
        nodePath: target.nodePath,
        fieldKey: "trimBefore",
        value: nextValues.trimBefore,
        defaultValue: "0",
        schema: target.schema
      });
    }
    return changes;
  });
};
var getTimelineSequenceDurationDragChanges = ({
  targets,
  deltaFrames
}) => {
  return targets.flatMap((target) => {
    const nextValue = getTimelineSequenceDurationDragValue({
      initialDuration: target.initialDuration,
      deltaFrames,
      maximumDuration: target.maximumDuration,
      minimumDuration: target.minimumDuration
    });
    if (nextValue === target.initialDuration) {
      return [];
    }
    return [
      {
        fileName: target.fileName,
        nodePath: target.nodePath,
        fieldKey: "durationInFrames",
        value: nextValue,
        defaultValue: null,
        schema: target.schema
      }
    ];
  });
};
var getTimelineSequenceFromDragValue = ({
  initialFrom,
  deltaFrames
}) => initialFrom + deltaFrames;
var getTimelineSequenceFromDragDelta = ({
  deltaFrames,
  timelineDurationInFrames,
  pxPerFrame,
  snappingEnabled,
  targets
}) => {
  let closestSnap;
  for (const target of targets) {
    if (!snappingEnabled || !target.canSnapToTimelineStart) {
      continue;
    }
    const nextFrom = getTimelineSequenceFromDragValue({
      initialFrom: target.initialFrom,
      deltaFrames
    });
    const distancePx = Math.abs(nextFrom * pxPerFrame);
    if (distancePx > timelineSequenceFromDragSnapThresholdPx || closestSnap && closestSnap.distancePx <= distancePx) {
      continue;
    }
    closestSnap = {
      deltaFrames: -target.initialFrom,
      distancePx
    };
  }
  const minimumDelta = Math.max(...targets.map((target) => target.minimumDeltaFrames));
  const maximumDelta = Math.min(...targets.map((target) => timelineDurationInFrames - 1 - target.initialTimelineStart));
  return Math.max(minimumDelta, Math.min(maximumDelta, closestSnap?.deltaFrames ?? deltaFrames));
};
var getTimelineSequenceFromDragChanges = ({
  targets,
  deltaFrames
}) => {
  return targets.flatMap((target) => {
    const nextValue = getTimelineSequenceFromDragValue({
      initialFrom: target.initialFrom,
      deltaFrames
    });
    if (nextValue === target.initialFrom) {
      return [];
    }
    return [
      {
        fileName: target.fileName,
        nodePath: target.nodePath,
        fieldKey: "from",
        value: nextValue,
        defaultValue: "0",
        schema: NoReactInternals12.sequenceSchema
      }
    ];
  });
};
var getTimelineSequenceFromDragKeyframeMoves = ({
  targets,
  deltaFrames
}) => {
  if (deltaFrames === 0) {
    return { effectKeyframes: [], sequenceKeyframes: [] };
  }
  return {
    sequenceKeyframes: targets.flatMap((target) => target.sequenceKeyframes.flatMap((keyframeTarget) => keyframeTarget.status.keyframes.map((keyframe) => ({
      fileName: keyframeTarget.fileName,
      nodePath: keyframeTarget.nodePath,
      fieldKey: keyframeTarget.fieldKey,
      fromFrame: keyframe.frame,
      toFrame: keyframe.frame + deltaFrames,
      schema: keyframeTarget.schema,
      keyframeDisplayOffsetAdjustmentDelta: keyframeTarget.isDescendant ? -deltaFrames : undefined
    })))),
    effectKeyframes: targets.flatMap((target) => target.effectKeyframes.flatMap((keyframeTarget) => keyframeTarget.status.keyframes.map((keyframe) => ({
      fileName: keyframeTarget.fileName,
      nodePath: keyframeTarget.nodePath,
      effectIndex: keyframeTarget.effectIndex,
      fieldKey: keyframeTarget.fieldKey,
      fromFrame: keyframe.frame,
      toFrame: keyframe.frame + deltaFrames,
      schema: keyframeTarget.schema,
      keyframeDisplayOffsetAdjustmentDelta: keyframeTarget.isDescendant ? -deltaFrames : undefined
    }))))
  };
};
var findSequenceTrack = ({
  tracks,
  nodePathInfo
}) => {
  const key7 = stringifySequenceExpandedRowKey(nodePathInfo.sequenceSubscriptionKey);
  return tracks.find((candidate) => {
    if (candidate.nodePathInfo === null) {
      return false;
    }
    return stringifySequenceExpandedRowKey(candidate.nodePathInfo.sequenceSubscriptionKey) === key7 && candidate.nodePathInfo.index === nodePathInfo.index;
  });
};
var getTimelineSequenceDurationDragTargets = ({
  draggedNodePathInfo,
  draggedSequenceMediaDurationDragLimits,
  selectedSequenceMediaDurationDragLimits,
  selectedItems,
  sequences,
  overrideIdsToNodePaths,
  propStatuses,
  timelineDurationInFrames
}) => {
  const draggedSelectionKey = getTimelineSequenceSelectionKey(draggedNodePathInfo);
  const selectedSequenceItems = selectedItems.filter((item) => item.type === "sequence");
  const draggedItemIsSelected = selectedSequenceItems.some((item) => getTimelineSequenceSelectionKey(item.nodePathInfo) === draggedSelectionKey);
  if (draggedItemIsSelected && selectedSequenceItems.length !== selectedItems.length) {
    return null;
  }
  const targetNodePathInfos = draggedItemIsSelected && selectedSequenceItems.length > 1 ? selectedSequenceItems.map((item) => item.nodePathInfo) : [draggedNodePathInfo];
  const tracks = calculateTimeline({ sequences, overrideIdsToNodePaths });
  const targets = new Map;
  for (const nodePathInfo of targetNodePathInfos) {
    const track = findSequenceTrack({ tracks, nodePathInfo });
    const originalSequence = track ? sequences.find((sequence) => sequence.id === track.sequence.id) : null;
    if (!track || !track.nodePathInfo || !originalSequence || !isTimelineSequenceDurationDraggable(originalSequence)) {
      return null;
    }
    const nodePath = track.nodePathInfo.sequenceSubscriptionKey;
    const durationStatus = Internals80.getPropStatusesCtx(propStatuses, nodePath)?.durationInFrames;
    if (!canResizeTimelineSequenceDuration({
      status: durationStatus
    })) {
      return null;
    }
    const { controls } = originalSequence;
    if (!controls) {
      return null;
    }
    const key7 = stringifySequenceSubscriptionKey3(nodePath);
    if (!targets.has(key7)) {
      const selectionKey = getTimelineSequenceSelectionKey(nodePathInfo);
      const isMedia = originalSequence.type === "audio" || originalSequence.type === "video";
      const isDraggedSequence = selectionKey === draggedSelectionKey;
      const mediaDurationDragLimits = isMedia ? isDraggedSequence ? draggedSequenceMediaDurationDragLimits : selectedSequenceMediaDurationDragLimits?.get(selectionKey) ?? null : null;
      if (isMedia && mediaDurationDragLimits === null) {
        return null;
      }
      const minimumDuration = Math.max(1 - originalSequence.from, getMinimumSequenceDuration({ sequence: originalSequence, sequences }));
      const initialDuration = mediaDurationDragLimits?.initialDuration ?? originalSequence.duration;
      const constrainedMaximumDuration = Math.min(mediaDurationDragLimits?.maximumDuration ?? Infinity, timelineDurationInFrames - track.cascadedStart);
      const maximumDuration = isMedia ? Math.max(minimumDuration, initialDuration, constrainedMaximumDuration) : Math.max(minimumDuration, constrainedMaximumDuration);
      targets.set(key7, {
        fileName: nodePath.absolutePath,
        initialDuration,
        maximumDuration,
        minimumDuration,
        nodePath,
        schema: controls.schema
      });
    }
  }
  return [...targets.values()];
};
var getTimelineSequenceLeftEdgeDragTargets = ({
  draggedNodePathInfo,
  selectedItems,
  sequences,
  overrideIdsToNodePaths,
  propStatuses
}) => {
  const draggedSelectionKey = getTimelineSequenceSelectionKey(draggedNodePathInfo);
  const selectedSequenceItems = selectedItems.filter((item) => item.type === "sequence");
  const draggedItemIsSelected = selectedSequenceItems.some((item) => getTimelineSequenceSelectionKey(item.nodePathInfo) === draggedSelectionKey);
  if (draggedItemIsSelected && selectedSequenceItems.length !== selectedItems.length) {
    return null;
  }
  const targetNodePathInfos = draggedItemIsSelected && selectedSequenceItems.length > 1 ? selectedSequenceItems.map((item) => item.nodePathInfo) : [draggedNodePathInfo];
  const tracks = calculateTimeline({ sequences, overrideIdsToNodePaths });
  const targets = new Map;
  for (const nodePathInfo of targetNodePathInfos) {
    const track = findSequenceTrack({ tracks, nodePathInfo });
    const originalSequence = track ? sequences.find((sequence) => sequence.id === track.sequence.id) : null;
    if (!track || !track.nodePathInfo || !originalSequence || !isTimelineSequenceLeftEdgeDraggable(originalSequence)) {
      return null;
    }
    const nodePath = track.nodePathInfo.sequenceSubscriptionKey;
    const trimsMedia = originalSequence.type === "audio" || originalSequence.type === "video";
    const positionField = isCascadingSequence(originalSequence) ? null : "from";
    if (positionField === "from" && !canUpdateFrom({ propStatuses, nodePath }) || !canUpdateDurationInFrames({ propStatuses, nodePath }) || !canUpdateTrimBefore({ propStatuses, nodePath })) {
      return null;
    }
    const { controls } = originalSequence;
    if (!controls) {
      return null;
    }
    const key7 = stringifySequenceSubscriptionKey3(nodePath);
    if (!targets.has(key7)) {
      targets.set(key7, {
        fileName: nodePath.absolutePath,
        initialDuration: originalSequence.duration,
        initialFrom: positionField === "from" ? originalSequence.from : 0,
        initialTrimBefore: trimsMedia ? originalSequence.trimBefore ?? Math.max(0, originalSequence.startMediaFrom) : originalSequence.trimBefore ?? 0,
        minimumDuration: getMinimumSequenceDuration({
          sequence: originalSequence,
          sequences
        }),
        nodePath,
        playbackRate: getTrimBeforePlaybackRate(originalSequence),
        positionField,
        schema: controls.schema
      });
    }
  }
  return [...targets.values()];
};
var getTimelineSequenceFromDragTargets = ({
  draggedNodePathInfo,
  selectedItems,
  sequences,
  overrideIdsToNodePaths,
  propStatuses
}) => {
  const draggedSelectionKey = getTimelineSequenceSelectionKey(draggedNodePathInfo);
  const selectedSequenceItems = selectedItems.filter((item) => item.type === "sequence");
  const draggedItemIsSelected = selectedSequenceItems.some((item) => getTimelineSequenceSelectionKey(item.nodePathInfo) === draggedSelectionKey);
  if (draggedItemIsSelected && selectedSequenceItems.length !== selectedItems.length) {
    return null;
  }
  const targetNodePathInfos = draggedItemIsSelected && selectedSequenceItems.length > 1 ? selectedSequenceItems.map((item) => item.nodePathInfo) : [draggedNodePathInfo];
  const tracks = calculateTimeline({ sequences, overrideIdsToNodePaths });
  const sequencesById = new Map(sequences.map((sequence) => [sequence.id, sequence]));
  const selectedSequences = targetNodePathInfos.flatMap((nodePathInfo) => {
    const track = findSequenceTrack({ tracks, nodePathInfo });
    const originalSequence = track ? sequencesById.get(track.sequence.id) : undefined;
    if (!track?.nodePathInfo || !originalSequence) {
      return [];
    }
    return [
      {
        nodePath: track.nodePathInfo.sequenceSubscriptionKey,
        originalSequence,
        initialTimelineStart: track.cascadedStart
      }
    ];
  });
  if (selectedSequences.length !== targetNodePathInfos.length) {
    return null;
  }
  const selectedSequenceIds = new Set(selectedSequences.map(({ originalSequence }) => originalSequence.id));
  const sequencesWithoutSelectedAncestors = selectedSequences.filter(({ originalSequence }) => {
    let parentId = originalSequence.parent;
    while (parentId !== null) {
      if (selectedSequenceIds.has(parentId)) {
        return false;
      }
      parentId = sequencesById.get(parentId)?.parent ?? null;
    }
    return true;
  });
  const targets = new Map;
  const includedKeyframeNodePaths = new Set;
  for (const {
    nodePath,
    originalSequence,
    initialTimelineStart
  } of sequencesWithoutSelectedAncestors) {
    if (!isFromDraggableSequence(originalSequence)) {
      return null;
    }
    if (!canUpdateFrom({ propStatuses, nodePath })) {
      return null;
    }
    const key7 = stringifySequenceSubscriptionKey3(nodePath);
    if (!targets.has(key7)) {
      const descendants = sequences.filter((candidate) => {
        let parentId = candidate.id;
        while (parentId !== null) {
          if (parentId === originalSequence.id) {
            return true;
          }
          parentId = sequencesById.get(parentId)?.parent ?? null;
        }
        return false;
      });
      const descendantKeyframes = descendants.flatMap((sequence) => {
        const overrideId = sequence.controls?.overrideId;
        const descendantNodePath = overrideId ? overrideIdsToNodePaths[overrideId] : undefined;
        if (descendantNodePath === undefined) {
          return [];
        }
        const descendantNodePathKey = stringifySequenceSubscriptionKey3(descendantNodePath);
        if (includedKeyframeNodePaths.has(descendantNodePathKey)) {
          return [];
        }
        includedKeyframeNodePaths.add(descendantNodePathKey);
        return [
          getKeyframedSequenceDragTargets({
            nodePath: descendantNodePath,
            sequence,
            propStatuses,
            isDescendant: sequence.id !== originalSequence.id
          })
        ];
      });
      const effectKeyframes = descendantKeyframes.flatMap((descendant) => descendant.effectKeyframes);
      const sequenceKeyframes = descendantKeyframes.flatMap((descendant) => descendant.sequenceKeyframes);
      targets.set(key7, {
        canSnapToTimelineStart: originalSequence.parent === null,
        minimumDeltaFrames: 1 - originalSequence.duration - originalSequence.from,
        initialTimelineStart,
        effectKeyframes,
        fileName: nodePath.absolutePath,
        initialFrom: originalSequence.from,
        nodePath,
        sequenceKeyframes
      });
    }
  }
  return [...targets.values()];
};
var clearLeftEdgeDragOverrides = ({
  clearDragOverrides,
  targets
}) => {
  for (const target of targets) {
    clearDragOverrides(target.nodePath);
  }
};
var clearDurationDragOverrides = ({
  clearDragOverrides,
  targets
}) => {
  for (const target of targets) {
    clearDragOverrides(target.nodePath);
  }
};
var clearFromDragOverrides = ({
  clearDragOverrides,
  clearEffectDragOverrides,
  targets
}) => {
  for (const target of targets) {
    clearDragOverrides(target.nodePath);
    for (const keyframeTarget of target.sequenceKeyframes) {
      clearDragOverrides(keyframeTarget.nodePath);
    }
    const clearedEffects = new Set;
    for (const keyframeTarget of target.effectKeyframes) {
      const key7 = `${stringifySequenceSubscriptionKey3(keyframeTarget.nodePath)}:${keyframeTarget.effectIndex}`;
      if (clearedEffects.has(key7)) {
        continue;
      }
      clearedEffects.add(key7);
      clearEffectDragOverrides(keyframeTarget.nodePath, keyframeTarget.effectIndex);
    }
  }
};
var TimelineSequenceLeftEdgeDragHandleInner = ({
  nodePathInfo,
  windowWidth,
  timelineDurationInFrames,
  onDragEnd,
  onSelect,
  selected
}) => {
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext85(Internals80.VisualModeSettersContext);
  const propStatusesRef = useContext85(Internals80.VisualModePropStatusesRefContext);
  const sequencesRef = useContext85(Internals80.SequenceManagerRefContext);
  const { overrideIdToNodePathMappings } = useContext85(Internals80.OverrideIdsToNodePathsGettersContext);
  const { previewServerState } = useContext85(StudioServerConnectionCtx);
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  const stopPointerSessionRef = useRef43(null);
  const dragStateRef = useRef43(null);
  const latestRef = useRef43({
    nodePathInfo,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    onDragEnd,
    onSelect
  });
  latestRef.current = {
    nodePathInfo,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    onDragEnd,
    onSelect
  };
  const finishDrag = useCallback68((commit) => {
    const dragState = dragStateRef.current;
    if (!dragState) {
      return;
    }
    dragStateRef.current = null;
    latestRef.current.onDragEnd(dragState.didMove);
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    stopForcingSpecificCursor();
    if (commit && !dragState.didMove && dragState.selectionInteraction !== null) {
      latestRef.current.onSelect(dragState.selectionInteraction);
    }
    const {
      setPropStatuses: latestSetPropStatuses,
      clearDragOverrides: latestClear,
      previewServerState: latestServerState
    } = latestRef.current;
    const changes = getTimelineSequenceLeftEdgeDragChanges({
      targets: dragState.targets,
      deltaFrames: dragState.latestDeltaFrames
    });
    if (!commit || latestServerState.type !== "connected" || changes.length === 0) {
      clearLeftEdgeDragOverrides({
        clearDragOverrides: latestClear,
        targets: dragState.targets
      });
      return;
    }
    const savePromise = saveSequenceProps({
      addedKeyframes: null,
      movedKeyframes: null,
      changes,
      setPropStatuses: latestSetPropStatuses,
      clientId: latestServerState.clientId,
      undoLabel: dragState.targets.length > 1 ? "Resize selected sequences" : "Resize sequence",
      redoLabel: dragState.targets.length > 1 ? "Resize selected sequences back" : "Resize sequence back"
    });
    savePromise.catch((err) => {
      Internals80.Log.error({ logLevel: "error", tag: null }, "Could not save left edge drag", err);
    }).finally(() => {
      clearLeftEdgeDragOverrides({
        clearDragOverrides: latestClear,
        targets: dragState.targets
      });
    });
  }, []);
  const onPointerDown = useCallback68((e) => {
    if (e.button !== 0) {
      return;
    }
    e.stopPropagation();
    e.preventDefault();
    const selectionInteraction = getTimelineSequenceEdgeSelectionInteraction({
      button: e.button,
      selected,
      shiftKey: e.shiftKey,
      metaKey: e.metaKey,
      ctrlKey: e.ctrlKey
    });
    const pxPerFrame = timelineDurationInFrames > 0 ? (windowWidth - TIMELINE_PADDING * 2) / timelineDurationInFrames : 0;
    const canCalculateDelta = Number.isFinite(pxPerFrame) && pxPerFrame > 0;
    const {
      nodePathInfo: latestNodePathInfo,
      overrideIdToNodePathMappings: latestOverrideIdsToNodePaths
    } = latestRef.current;
    const { selectedItems: latestSelectedItems } = currentSelection.current;
    const targets = canCalculateDelta ? getTimelineSequenceLeftEdgeDragTargets({
      draggedNodePathInfo: latestNodePathInfo,
      selectedItems: latestSelectedItems,
      sequences: sequencesRef.current,
      overrideIdsToNodePaths: latestOverrideIdsToNodePaths,
      propStatuses: propStatusesRef.current
    }) ?? [] : [];
    stopPointerSessionRef.current?.();
    dragStateRef.current = {
      initialClientX: e.clientX,
      latestDeltaFrames: 0,
      didMove: false,
      pxPerFrame: canCalculateDelta ? pxPerFrame : 1,
      pointerId: e.pointerId,
      selectionInteraction,
      targets
    };
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    forceSpecificCursor("ew-resize");
    const onMove = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      const dx = pointerEvent.clientX - dragState.initialClientX;
      const deltaFrames = Math.round(dx / dragState.pxPerFrame);
      dragState.latestDeltaFrames = deltaFrames;
      if (Math.abs(dx) >= timelineSequenceEdgeDragThresholdPx || getTimelineSequenceLeftEdgeDragChanges({
        targets: dragState.targets,
        deltaFrames
      }).length > 0) {
        dragState.didMove = true;
      }
      for (const target of dragState.targets) {
        const nextValues = getTimelineSequenceLeftEdgeDragValues({
          initialDuration: target.initialDuration,
          initialFrom: target.initialFrom,
          initialTrimBefore: target.initialTrimBefore,
          deltaFrames,
          playbackRate: target.playbackRate,
          minimumDuration: target.minimumDuration
        });
        if (target.positionField !== null) {
          latestRef.current.setDragOverrides(target.nodePath, target.positionField, Internals80.makeStaticDragOverride(nextValues.from));
        }
        latestRef.current.setDragOverrides(target.nodePath, "durationInFrames", Internals80.makeStaticDragOverride(nextValues.durationInFrames));
        latestRef.current.setDragOverrides(target.nodePath, "trimBefore", Internals80.makeStaticDragOverride(nextValues.trimBefore));
      }
    };
    const onUp = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      onMove(pointerEvent);
      finishDrag(true);
    };
    const onCancel = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      finishDrag(false);
    };
    stopPointerSessionRef.current = startCapturedPointerSession({
      event: e.nativeEvent,
      captureTarget: e.currentTarget,
      onMove,
      onEnd: (reason, endEvent) => {
        stopPointerSessionRef.current = null;
        if (isPointerSessionRelease(reason, endEvent)) {
          onUp(endEvent);
        } else if (endEvent) {
          onCancel(endEvent);
        } else {
          finishDrag(false);
        }
      }
    });
  }, [
    currentSelection,
    finishDrag,
    propStatusesRef,
    selected,
    sequencesRef,
    timelineDurationInFrames,
    windowWidth
  ]);
  useEffect55(() => {
    return () => {
      stopPointerSessionRef.current?.();
      stopPointerSessionRef.current = null;
    };
  }, []);
  const style6 = {
    ...baseStyle3,
    left: -HANDLE_OUTSET,
    background: TRANSPARENT
  };
  return /* @__PURE__ */ jsx134("div", {
    role: "separator",
    "aria-orientation": "vertical",
    title: "Drag to trim start",
    style: style6,
    onPointerDown
  });
};
var TimelineSequenceLeftEdgeDragHandle = React96.memo(TimelineSequenceLeftEdgeDragHandleInner);
var useTimelineSequenceFromDrag = ({
  nodePathInfo,
  windowWidth,
  timelineDurationInFrames,
  onDragEnd
}) => {
  const {
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    setEffectDragOverrides,
    clearEffectDragOverrides
  } = useContext85(Internals80.VisualModeSettersContext);
  const propStatusesRef = useContext85(Internals80.VisualModePropStatusesRefContext);
  const sequencesRef = useContext85(Internals80.SequenceManagerRefContext);
  const { overrideIdToNodePathMappings } = useContext85(Internals80.OverrideIdsToNodePathsGettersContext);
  const { previewServerState } = useContext85(StudioServerConnectionCtx);
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  const { editorSnapping } = useContext85(EditorSnappingContext);
  const stopPointerSessionRef = useRef43(null);
  const dragStateRef = useRef43(null);
  const latestRef = useRef43({
    nodePathInfo,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    setEffectDragOverrides,
    clearEffectDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    editorSnapping,
    onDragEnd
  });
  latestRef.current = {
    nodePathInfo,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    setEffectDragOverrides,
    clearEffectDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    editorSnapping,
    onDragEnd
  };
  const finishDrag = useCallback68((commit) => {
    const dragState = dragStateRef.current;
    if (!dragState) {
      return;
    }
    dragStateRef.current = null;
    latestRef.current.onDragEnd(dragState.didMove);
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    const {
      setPropStatuses: latestSetPropStatuses,
      clearDragOverrides: latestClear,
      clearEffectDragOverrides: latestClearEffect,
      previewServerState: latestServerState
    } = latestRef.current;
    const changes = getTimelineSequenceFromDragChanges({
      targets: dragState.targets,
      deltaFrames: dragState.latestDeltaFrames
    });
    const keyframeMoves = getTimelineSequenceFromDragKeyframeMoves({
      targets: dragState.targets,
      deltaFrames: dragState.latestDeltaFrames
    });
    if (!commit || latestServerState.type !== "connected" || changes.length === 0 && keyframeMoves.sequenceKeyframes.length === 0 && keyframeMoves.effectKeyframes.length === 0) {
      clearFromDragOverrides({
        clearDragOverrides: latestClear,
        clearEffectDragOverrides: latestClearEffect,
        targets: dragState.targets
      });
      return;
    }
    const savePromise = saveSequenceProps({
      addedKeyframes: null,
      changes,
      movedKeyframes: {
        sequenceKeyframes: keyframeMoves.sequenceKeyframes,
        effectKeyframes: keyframeMoves.effectKeyframes
      },
      setPropStatuses: latestSetPropStatuses,
      clientId: latestServerState.clientId,
      undoLabel: dragState.targets.length > 1 ? "Move selected sequences" : "Move sequence",
      redoLabel: dragState.targets.length > 1 ? "Move selected sequences back" : "Move sequence back"
    });
    savePromise.catch((err) => {
      Internals80.Log.error({ logLevel: "error", tag: null }, "Could not save from", err);
    }).finally(() => {
      clearFromDragOverrides({
        clearDragOverrides: latestClear,
        clearEffectDragOverrides: latestClearEffect,
        targets: dragState.targets
      });
    });
  }, []);
  const onPointerDown = useCallback68((e) => {
    if (e.button !== 0) {
      return;
    }
    const pxPerFrame = timelineDurationInFrames > 0 ? (windowWidth - TIMELINE_PADDING * 2) / timelineDurationInFrames : 0;
    if (pxPerFrame <= 0) {
      return;
    }
    const {
      nodePathInfo: latestNodePathInfo,
      overrideIdToNodePathMappings: latestOverrideIdsToNodePaths
    } = latestRef.current;
    const { selectedItems: latestSelectedItems } = currentSelection.current;
    if (latestNodePathInfo === null) {
      return;
    }
    const targets = getTimelineSequenceFromDragTargets({
      draggedNodePathInfo: latestNodePathInfo,
      selectedItems: latestSelectedItems,
      sequences: sequencesRef.current,
      overrideIdsToNodePaths: latestOverrideIdsToNodePaths,
      propStatuses: propStatusesRef.current
    });
    if (targets === null || targets.length === 0) {
      return;
    }
    stopPointerSessionRef.current?.();
    e.preventDefault();
    dragStateRef.current = {
      initialClientX: e.clientX,
      latestDeltaFrames: 0,
      didMove: false,
      pxPerFrame,
      targets
    };
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    stopPointerSessionRef.current = startDeferredCapturedPointerSession({
      event: e.nativeEvent,
      captureTarget: e.currentTarget.ownerDocument.body,
      onMove: (moveEvent, capturePointer) => {
        const dragState = dragStateRef.current;
        if (!dragState) {
          return;
        }
        const dx = moveEvent.clientX - dragState.initialClientX;
        const deltaFrames = getTimelineSequenceFromDragDelta({
          timelineDurationInFrames,
          deltaFrames: Math.round(dx / dragState.pxPerFrame),
          pxPerFrame: dragState.pxPerFrame,
          snappingEnabled: latestRef.current.editorSnapping,
          targets: dragState.targets
        });
        dragState.latestDeltaFrames = deltaFrames;
        if (deltaFrames !== 0) {
          capturePointer();
          dragState.didMove = true;
        }
        for (const target of dragState.targets) {
          const nextFrom = getTimelineSequenceFromDragValue({
            initialFrom: target.initialFrom,
            deltaFrames
          });
          latestRef.current.setDragOverrides(target.nodePath, "from", Internals80.makeStaticDragOverride(nextFrom));
          for (const keyframeTarget of target.sequenceKeyframes) {
            latestRef.current.setDragOverrides(keyframeTarget.nodePath, keyframeTarget.fieldKey, {
              type: "keyframed",
              status: shiftKeyframedStatus({
                status: keyframeTarget.status,
                deltaFrames,
                isDescendant: keyframeTarget.isDescendant
              })
            });
          }
          for (const keyframeTarget of target.effectKeyframes) {
            latestRef.current.setEffectDragOverrides(keyframeTarget.nodePath, keyframeTarget.effectIndex, keyframeTarget.fieldKey, {
              type: "keyframed",
              status: shiftKeyframedStatus({
                status: keyframeTarget.status,
                deltaFrames,
                isDescendant: keyframeTarget.isDescendant
              })
            });
          }
        }
      },
      onEnd: (reason, endEvent) => {
        stopPointerSessionRef.current = null;
        finishDrag(isPointerSessionRelease(reason, endEvent));
      }
    });
  }, [
    currentSelection,
    finishDrag,
    propStatusesRef,
    sequencesRef,
    timelineDurationInFrames,
    windowWidth
  ]);
  useEffect55(() => {
    return () => {
      stopPointerSessionRef.current?.();
      stopPointerSessionRef.current = null;
    };
  }, []);
  return {
    onPointerDown
  };
};
var TimelineSequenceRightEdgeDragHandleInner = ({
  nodePathInfo,
  mediaDurationDragLimits,
  windowWidth,
  timelineDurationInFrames,
  onDragEnd,
  onSelect,
  selected
}) => {
  const { setPropStatuses, setDragOverrides, clearDragOverrides } = useContext85(Internals80.VisualModeSettersContext);
  const propStatusesRef = useContext85(Internals80.VisualModePropStatusesRefContext);
  const sequencesRef = useContext85(Internals80.SequenceManagerRefContext);
  const { overrideIdToNodePathMappings } = useContext85(Internals80.OverrideIdsToNodePathsGettersContext);
  const { previewServerState } = useContext85(StudioServerConnectionCtx);
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  const mediaDurationDragLimitsRegistry = useContext85(TimelineSequenceMediaDurationDragLimitsContext);
  const stopPointerSessionRef = useRef43(null);
  const dragStateRef = useRef43(null);
  const latestRef = useRef43({
    nodePathInfo,
    mediaDurationDragLimits,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    onDragEnd,
    onSelect
  });
  latestRef.current = {
    nodePathInfo,
    mediaDurationDragLimits,
    setPropStatuses,
    setDragOverrides,
    clearDragOverrides,
    previewServerState,
    overrideIdToNodePathMappings,
    onDragEnd,
    onSelect
  };
  const finishDrag = useCallback68((commit) => {
    const dragState = dragStateRef.current;
    if (!dragState) {
      return;
    }
    dragStateRef.current = null;
    latestRef.current.onDragEnd(dragState.didMove);
    document.body.style.userSelect = "";
    document.body.style.webkitUserSelect = "";
    stopForcingSpecificCursor();
    if (commit && !dragState.didMove && dragState.selectionInteraction !== null) {
      latestRef.current.onSelect(dragState.selectionInteraction);
    }
    const {
      setPropStatuses: latestSetPropStatuses,
      clearDragOverrides: latestClear,
      previewServerState: latestServerState
    } = latestRef.current;
    const changes = getTimelineSequenceDurationDragChanges({
      targets: dragState.targets,
      deltaFrames: dragState.latestDeltaFrames
    });
    if (!commit || latestServerState.type !== "connected" || changes.length === 0) {
      clearDurationDragOverrides({
        clearDragOverrides: latestClear,
        targets: dragState.targets
      });
      return;
    }
    const savePromise = saveSequenceProps({
      addedKeyframes: null,
      movedKeyframes: null,
      changes,
      setPropStatuses: latestSetPropStatuses,
      clientId: latestServerState.clientId,
      undoLabel: changes.length > 1 ? "Resize selected sequences" : "Resize sequence",
      redoLabel: changes.length > 1 ? "Resize selected sequences back" : "Resize sequence back"
    });
    savePromise.catch((err) => {
      Internals80.Log.error({ logLevel: "error", tag: null }, "Could not save durationInFrames", err);
    }).finally(() => {
      clearDurationDragOverrides({
        clearDragOverrides: latestClear,
        targets: dragState.targets
      });
    });
  }, []);
  const onPointerDown = useCallback68((e) => {
    if (e.button !== 0) {
      return;
    }
    e.stopPropagation();
    e.preventDefault();
    const selectionInteraction = getTimelineSequenceEdgeSelectionInteraction({
      button: e.button,
      selected,
      shiftKey: e.shiftKey,
      metaKey: e.metaKey,
      ctrlKey: e.ctrlKey
    });
    const pxPerFrame = timelineDurationInFrames > 0 ? (windowWidth - TIMELINE_PADDING * 2) / timelineDurationInFrames : 0;
    const canCalculateDelta = Number.isFinite(pxPerFrame) && pxPerFrame > 0;
    const {
      nodePathInfo: latestNodePathInfo,
      mediaDurationDragLimits: latestMediaDurationDragLimits,
      overrideIdToNodePathMappings: latestOverrideIdsToNodePaths
    } = latestRef.current;
    const { selectedItems: latestSelectedItems } = currentSelection.current;
    const targets = canCalculateDelta ? getTimelineSequenceDurationDragTargets({
      draggedNodePathInfo: latestNodePathInfo,
      draggedSequenceMediaDurationDragLimits: latestMediaDurationDragLimits,
      selectedSequenceMediaDurationDragLimits: mediaDurationDragLimitsRegistry?.current ?? null,
      selectedItems: latestSelectedItems,
      sequences: sequencesRef.current,
      overrideIdsToNodePaths: latestOverrideIdsToNodePaths,
      propStatuses: propStatusesRef.current,
      timelineDurationInFrames
    }) ?? [] : [];
    stopPointerSessionRef.current?.();
    dragStateRef.current = {
      initialClientX: e.clientX,
      latestDeltaFrames: 0,
      didMove: false,
      pxPerFrame: canCalculateDelta ? pxPerFrame : 1,
      pointerId: e.pointerId,
      selectionInteraction,
      targets
    };
    document.body.style.userSelect = "none";
    document.body.style.webkitUserSelect = "none";
    forceSpecificCursor("ew-resize");
    const onMove = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      const dx = pointerEvent.clientX - dragState.initialClientX;
      const deltaFrames = Math.round(dx / dragState.pxPerFrame);
      dragState.latestDeltaFrames = deltaFrames;
      if (Math.abs(dx) >= timelineSequenceEdgeDragThresholdPx || getTimelineSequenceDurationDragChanges({
        targets: dragState.targets,
        deltaFrames
      }).length > 0) {
        dragState.didMove = true;
      }
      for (const target of dragState.targets) {
        const previewValue = getTimelineSequenceDurationDragValue({
          initialDuration: target.initialDuration,
          deltaFrames,
          maximumDuration: target.maximumDuration,
          minimumDuration: target.minimumDuration
        });
        latestRef.current.setDragOverrides(target.nodePath, "durationInFrames", Internals80.makeStaticDragOverride(previewValue));
      }
    };
    const onUp = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      onMove(pointerEvent);
      finishDrag(true);
    };
    const onCancel = (pointerEvent) => {
      const dragState = dragStateRef.current;
      if (!dragState || pointerEvent.pointerId !== dragState.pointerId) {
        return;
      }
      finishDrag(false);
    };
    stopPointerSessionRef.current = startCapturedPointerSession({
      event: e.nativeEvent,
      captureTarget: e.currentTarget,
      onMove,
      onEnd: (reason, endEvent) => {
        stopPointerSessionRef.current = null;
        if (isPointerSessionRelease(reason, endEvent)) {
          onUp(endEvent);
        } else if (endEvent) {
          onCancel(endEvent);
        } else {
          finishDrag(false);
        }
      }
    });
  }, [
    currentSelection,
    finishDrag,
    mediaDurationDragLimitsRegistry,
    propStatusesRef,
    selected,
    sequencesRef,
    timelineDurationInFrames,
    windowWidth
  ]);
  useEffect55(() => {
    return () => {
      stopPointerSessionRef.current?.();
      stopPointerSessionRef.current = null;
    };
  }, []);
  const style6 = {
    ...baseStyle3,
    right: -HANDLE_OUTSET,
    background: TRANSPARENT
  };
  return /* @__PURE__ */ jsx134("div", {
    role: "separator",
    "aria-orientation": "vertical",
    title: "Drag to change duration",
    style: style6,
    onPointerDown
  });
};
var TimelineSequenceRightEdgeDragHandle = React96.memo(TimelineSequenceRightEdgeDragHandleInner);

// src/components/Timeline/TimelineTracks.tsx
import React105, { useMemo as useMemo84 } from "react";

// src/components/Timeline/TimelineTrack.tsx
import React104, { useContext as useContext90, useMemo as useMemo83 } from "react";

// src/components/Timeline/TimelineExpandedTrackKeyframes.tsx
import React99 from "react";

// src/components/Timeline/TimelineExpandedKeyframeRow.tsx
import React98, { useContext as useContext87 } from "react";

// src/components/Timeline/TimelineKeyframeDiamond.tsx
import React97, { useContext as useContext86, useMemo as useMemo77, useRef as useRef44 } from "react";
import { useVideoConfig as useVideoConfig11 } from "remotion";
import { jsx as jsx135 } from "react/jsx-runtime";
var diamondSize = 12;
var diamondBase = {
  alignItems: "center",
  background: "none",
  border: "none",
  display: "flex",
  height: diamondSize,
  justifyContent: "center",
  padding: 0,
  position: "absolute",
  width: diamondSize
};
var TimelineKeyframeDiamondUnmemoized = ({ frame: frame2, rowHeight, nodePathInfo }) => {
  const videoConfig = useVideoConfig11();
  const timelineWidth = useContext86(TimelineWidthContext);
  const ref = useRef44(null);
  const { selected, onSelect, selectable, selectionItem } = useTimelineKeyframeSelection(nodePathInfo, frame2);
  useTimelineMarqueeSelectableItem(selectionItem, ref);
  const { isKeyframeDragging } = useTimelineKeyframeDragState();
  const visuallySelected = selected || isKeyframeDragging({ nodePathInfo, frame: frame2 });
  const style6 = useMemo77(() => {
    if (timelineWidth === null) {
      return null;
    }
    return {
      ...diamondBase,
      left: getXPositionOfItemInTimelineImperatively(frame2, videoConfig.durationInFrames, timelineWidth) - TIMELINE_PADDING,
      pointerEvents: "auto",
      top: rowHeight / 2,
      transform: "translate(-50%, -50%)"
    };
  }, [frame2, rowHeight, timelineWidth, videoConfig.durationInFrames]);
  const onPointerDown = useTimelineKeyframeDrag({
    frame: frame2,
    nodePathInfo,
    onSelect,
    selectable,
    selected
  });
  if (style6 === null) {
    return null;
  }
  return /* @__PURE__ */ jsx135("button", {
    ref,
    ...{ [TIMELINE_MARQUEE_ITEM_ATTR]: true },
    type: "button",
    style: style6,
    title: `Keyframe at frame ${frame2}`,
    "aria-label": `Select keyframe at frame ${frame2}`,
    onPointerDown: selectable ? onPointerDown : undefined,
    children: /* @__PURE__ */ jsx135(TimelineKeyframeDiamondIcon, {
      color: LIGHT_TEXT,
      selected: visuallySelected,
      size: diamondSize
    })
  });
};
var TimelineKeyframeDiamond = React97.memo(TimelineKeyframeDiamondUnmemoized);

// src/components/Timeline/TimelineExpandedKeyframeRow.tsx
import { jsx as jsx136, jsxs as jsxs69, Fragment as Fragment33 } from "react/jsx-runtime";
var rowClipper = {
  boxSizing: "border-box",
  marginLeft: -TIMELINE_PADDING,
  marginRight: -TIMELINE_PADDING,
  minWidth: `calc(100% + ${TIMELINE_PADDING * 2}px)`,
  overflow: "hidden",
  paddingLeft: TIMELINE_PADDING,
  paddingRight: TIMELINE_PADDING
};
var row4 = {
  position: "relative"
};
var rowSeparator = {
  height: TIMELINE_ITEM_BORDER_BOTTOM
};
var TimelineExpandedKeyframeRowUnmemoized = ({ height, keyframes, canEditEasing, nodePathInfo, showSeparator }) => {
  const timelineWidth = useContext87(TimelineWidthContext);
  const rowHighlightBackground = useTimelineRowHighlightBackground(nodePathInfo, {
    hovered: false,
    selectedBackground: TIMELINE_EXPANDED_SELECTED_BACKGROUND
  });
  const easingSegments = canEditEasing ? getTimelineEasingSegments(keyframes) : [];
  return /* @__PURE__ */ jsxs69(Fragment33, {
    children: [
      showSeparator ? /* @__PURE__ */ jsx136("div", {
        style: rowSeparator
      }) : null,
      /* @__PURE__ */ jsx136("div", {
        style: { ...rowClipper, height, width: timelineWidth ?? undefined },
        children: /* @__PURE__ */ jsxs69("div", {
          style: { ...row4, height },
          children: [
            rowHighlightBackground && timelineWidth !== null ? /* @__PURE__ */ jsx136("div", {
              style: getTimelineSelectedTrackHighlightStyle(timelineWidth, rowHighlightBackground)
            }) : null,
            easingSegments.map((segment) => /* @__PURE__ */ jsx136(TimelineKeyframeEasingLine, {
              fromFrame: segment.fromFrame,
              toFrame: segment.toFrame,
              rowHeight: height,
              nodePathInfo,
              segmentIndex: segment.segmentIndex
            }, `${segment.segmentIndex}-${segment.fromFrame}-${segment.toFrame}`)),
            keyframes.map((keyframe) => /* @__PURE__ */ jsx136(TimelineKeyframeDiamond, {
              frame: keyframe.frame,
              rowHeight: height,
              nodePathInfo
            }, keyframe.frame))
          ]
        })
      })
    ]
  });
};
var areTimelineExpandedKeyframeRowPropsEqual = (prevProps, nextProps) => {
  if (prevProps.height !== nextProps.height || prevProps.canEditEasing !== nextProps.canEditEasing || prevProps.showSeparator !== nextProps.showSeparator || prevProps.keyframes.length !== nextProps.keyframes.length || !areSequenceNodePathInfosEqual(prevProps.nodePathInfo, nextProps.nodePathInfo)) {
    return false;
  }
  return prevProps.keyframes.every((keyframe, index) => keyframe.frame === nextProps.keyframes[index].frame);
};
var TimelineExpandedKeyframeRow = React98.memo(TimelineExpandedKeyframeRowUnmemoized, areTimelineExpandedKeyframeRowPropsEqual);

// src/components/Timeline/use-expanded-track-keyframe-rows.ts
import { canEditEasingForInterpolationFunction } from "@remotion/studio-shared";
import { useCallback as useCallback69, useContext as useContext88, useMemo as useMemo78 } from "react";
import { Internals as Internals81 } from "remotion";
var areTimelineTreeLayoutsEqual = (first, second) => {
  if (first.length !== second.length) {
    return false;
  }
  return first.every((firstNode, index) => {
    const secondNode = second[index];
    if (firstNode.kind !== secondNode.kind || timelineNodePathInfoToKey(firstNode.nodePathInfo) !== timelineNodePathInfoToKey(secondNode.nodePathInfo)) {
      return false;
    }
    if (firstNode.kind === "group" && secondNode.kind === "group") {
      return areTimelineTreeLayoutsEqual(firstNode.children, secondNode.children);
    }
    if (firstNode.kind !== "field" || secondNode.kind !== "field") {
      return false;
    }
    if (firstNode.field === null || secondNode.field === null) {
      return firstNode.field === secondNode.field;
    }
    return firstNode.field.kind === secondNode.field.kind && firstNode.field.key === secondNode.field.key && firstNode.field.typeName === secondNode.field.typeName && firstNode.field.rowHeight === secondNode.field.rowHeight && (firstNode.field.kind !== "effect-field" || secondNode.field.kind === "effect-field" && firstNode.field.effectIndex === secondNode.field.effectIndex);
  });
};
var getNodeCanEditEasing = ({
  node,
  nodePath,
  propStatuses
}) => {
  if (node.kind !== "field" || node.field === null) {
    return false;
  }
  if (node.field.kind === "sequence-field") {
    const sequencePropStatus = Internals81.getPropStatusesCtx(propStatuses, nodePath)?.[node.field.key];
    return sequencePropStatus?.status === "keyframed" && canEditEasingForInterpolationFunction(sequencePropStatus.interpolationFunction);
  }
  const effectStatus = Internals81.getEffectPropStatusesCtx({
    propStatuses,
    nodePath,
    effectIndex: node.field.effectIndex
  });
  const effectPropStatus = effectStatus.type === "can-update-effect" ? effectStatus.props[node.field.key] : null;
  return effectPropStatus?.status === "keyframed" && canEditEasingForInterpolationFunction(effectPropStatus.interpolationFunction);
};
var useExpandedTrackKeyframeRows = ({
  sequence,
  nodePathInfo,
  keyframeDisplayOffset
}) => {
  const { getIsExpanded } = useContext88(ExpandedTracksGetterContext);
  const { propStatuses } = useContext88(Internals81.VisualModePropStatusesContext);
  const { getDragOverrides, getEffectDragOverrides } = useContext88(Internals81.VisualModeDragOverridesContext);
  const { selectedItems } = useTimelineSelection();
  const selectTree = useCallback69((runtimeValues) => buildTimelineTree({
    sequence,
    nodePathInfo,
    getDragOverrides,
    getEffectDragOverrides,
    propStatuses,
    includeTextContent: false,
    includeSourceControls: false,
    runtimeValues
  }), [
    sequence,
    propStatuses,
    getDragOverrides,
    getEffectDragOverrides,
    nodePathInfo
  ]);
  const tree = useRuntimeValueSelector({
    controls: sequence.controls,
    selector: selectTree,
    isEqual: areTimelineTreeLayoutsEqual
  });
  const selectedRowKeys = useMemo78(() => getSelectedTimelineExpandedRowKeys(selectedItems), [selectedItems]);
  const filteredTree = useMemo78(() => filterTimelineExpandedTree({
    nodes: tree,
    shouldShowNode: (node) => isTimelineExpandedNodeSelected({
      nodePathInfo: node.nodePathInfo,
      selectedRowKeys
    }) || getNodeHasKeyframes({
      node,
      nodePath: nodePathInfo.sequenceSubscriptionKey,
      propStatuses,
      getDragOverrides,
      getEffectDragOverrides
    })
  }), [
    getDragOverrides,
    getEffectDragOverrides,
    nodePathInfo.sequenceSubscriptionKey,
    propStatuses,
    selectedRowKeys,
    tree
  ]);
  const flat = useMemo78(() => flattenVisibleTreeNodes({ nodes: filteredTree, getIsExpanded }), [filteredTree, getIsExpanded]);
  const expandedHeight = useMemo78(() => {
    const totalRowsHeight = flat.reduce((sum, { node }) => sum + getTreeRowHeight(node), 0);
    const separators = Math.max(0, flat.length - 1);
    return totalRowsHeight + separators;
  }, [flat]);
  const nodeKeyframes = useMemo78(() => new Map(flat.map(({ node }) => [
    timelineNodePathInfoToKey(node.nodePathInfo),
    getNodeKeyframes({
      node,
      nodePath: nodePathInfo.sequenceSubscriptionKey,
      propStatuses,
      keyframeDisplayOffset,
      getDragOverrides,
      getEffectDragOverrides,
      timelinePosition: getCurrentFrame()
    })
  ])), [
    flat,
    getDragOverrides,
    getEffectDragOverrides,
    keyframeDisplayOffset,
    nodePathInfo.sequenceSubscriptionKey,
    propStatuses
  ]);
  const rows = useMemo78(() => flat.map(({ node }) => {
    const rowKey = timelineNodePathInfoToKey(node.nodePathInfo);
    return {
      height: getTreeRowHeight(node),
      keyframes: nodeKeyframes.get(rowKey) ?? [],
      canEditEasing: getNodeCanEditEasing({
        node,
        nodePath: nodePathInfo.sequenceSubscriptionKey,
        propStatuses
      }),
      rowKey,
      nodePathInfo: node.nodePathInfo
    };
  }), [propStatuses, flat, nodePathInfo.sequenceSubscriptionKey, nodeKeyframes]);
  return { rows, expandedHeight };
};

// src/components/Timeline/TimelineExpandedTrackKeyframes.tsx
import { jsx as jsx137 } from "react/jsx-runtime";
var TimelineExpandedTrackKeyframesInner = ({ nodePathInfo, sequence, keyframeDisplayOffset }) => {
  const { rows, expandedHeight } = useExpandedTrackKeyframeRows({
    sequence,
    nodePathInfo,
    keyframeDisplayOffset
  });
  return /* @__PURE__ */ jsx137("div", {
    style: { height: expandedHeight },
    children: rows.map((row5, index) => /* @__PURE__ */ jsx137(TimelineExpandedKeyframeRow, {
      height: row5.height,
      keyframes: row5.keyframes,
      canEditEasing: row5.canEditEasing,
      nodePathInfo: row5.nodePathInfo,
      showSeparator: index > 0
    }, row5.rowKey))
  });
};
var areTimelineExpandedTrackKeyframesPropsEqual = (first, second) => {
  return first.keyframeDisplayOffset === second.keyframeDisplayOffset && first.sequence.controls?.schema === second.sequence.controls?.schema && first.sequence.controls?.runtimeValues === second.sequence.controls?.runtimeValues && first.sequence.effects === second.sequence.effects && areSequenceNodePathInfosEqual(first.nodePathInfo, second.nodePathInfo);
};
var TimelineExpandedTrackKeyframes = React99.memo(TimelineExpandedTrackKeyframesInner, areTimelineExpandedTrackKeyframesPropsEqual);

// src/components/Timeline/TimelineSequence.tsx
import {
  getTimelineVisibleDuration,
  getTimelineVisibleStart
} from "@remotion/canvas";
import React103, {
  useCallback as useCallback70,
  useContext as useContext89,
  useEffect as useEffect57,
  useMemo as useMemo82,
  useRef as useRef47
} from "react";
import { Internals as Internals88, useCurrentFrame as useCurrentFrame7 } from "remotion";

// src/helpers/get-timeline-sequence-layout.ts
var SEQUENCE_BORDER_WIDTH = 1;
var getWidthOfTrack = ({
  durationInFrames,
  timelineDuration,
  windowWidth,
  spatialDuration,
  nonNegativeMarginLeft
}) => {
  const fullWidth = windowWidth - TIMELINE_PADDING * 2;
  const base = durationInFrames === Infinity || timelineDuration <= 0 ? fullWidth : spatialDuration / timelineDuration * fullWidth;
  return Math.max(0, base - SEQUENCE_BORDER_WIDTH + nonNegativeMarginLeft);
};
var getTimelineSequenceLayout = ({
  durationInFrames,
  startFrom,
  cascadedStart,
  maxMediaDuration,
  startFromMedia,
  video,
  windowWidth,
  premountDisplay,
  postmountDisplay
}) => {
  const maxMediaSequenceDuration = (maxMediaDuration ?? Infinity) - startFromMedia;
  const timelineDuration = video.durationInFrames ?? 1;
  const fullWidth = windowWidth - TIMELINE_PADDING * 2;
  const spatialDuration = Math.max(0, Math.min(maxMediaSequenceDuration, durationInFrames, timelineDuration - startFrom));
  const naturalSpatialDuration = Math.max(0, Math.min(maxMediaSequenceDuration, durationInFrames));
  const marginLeft = timelineDuration <= 0 ? 0 : startFrom / timelineDuration * fullWidth;
  const naturalNegativeStartWidth = timelineDuration > 0 && startFrom === 0 && cascadedStart < 0 && durationInFrames > 0 ? Math.max(0, -cascadedStart / timelineDuration * fullWidth) : 0;
  const negativeStartWidth = Math.min(TIMELINE_PADDING, naturalNegativeStartWidth);
  const negativeStartClipped = naturalNegativeStartWidth > TIMELINE_PADDING;
  const nonNegativeMarginLeft = Math.min(marginLeft, 0);
  const width = getWidthOfTrack({
    durationInFrames,
    timelineDuration,
    nonNegativeMarginLeft,
    spatialDuration,
    windowWidth
  });
  const naturalWidth = getWidthOfTrack({
    durationInFrames,
    timelineDuration,
    nonNegativeMarginLeft,
    spatialDuration: naturalSpatialDuration,
    windowWidth
  });
  const premountWidth = premountDisplay ? getWidthOfTrack({
    durationInFrames: premountDisplay,
    timelineDuration,
    nonNegativeMarginLeft,
    spatialDuration: premountDisplay,
    windowWidth
  }) : null;
  const postmountWidth = postmountDisplay ? getWidthOfTrack({
    durationInFrames: postmountDisplay,
    timelineDuration,
    nonNegativeMarginLeft,
    spatialDuration: postmountDisplay,
    windowWidth
  }) : null;
  return {
    marginLeft: Math.max(marginLeft, 0) - negativeStartWidth - (premountWidth ?? 0),
    width: width + negativeStartWidth + (premountWidth ?? 0) + (postmountWidth ?? 0),
    naturalWidth: naturalWidth + (premountWidth ?? 0) + (postmountWidth ?? 0),
    negativeStartWidth,
    negativeStartClipped,
    premountWidth,
    postmountWidth
  };
};

// src/helpers/is-video-with-last-frame-hold.ts
var isVideoWithLastFrameHold = (sequence) => {
  return sequence.type === "video" && sequence.controls?.componentIdentity === "dev.remotion.media.Video";
};

// src/components/AudioWaveform.tsx
import {
  drawBars,
  getVisibleWaveformVolume,
  sliceVisibleWaveformPeaks,
  subscribeToWaveformPeaks
} from "@remotion/timeline-utils";
import React100, { useLayoutEffect as useLayoutEffect23, useMemo as useMemo79, useRef as useRef45, useState as useState49 } from "react";
import { Internals as Internals83 } from "remotion";
import { jsx as jsx138, jsxs as jsxs70 } from "react/jsx-runtime";
var EMPTY_PEAKS = new Float32Array(0);
var getContainerStyle2 = (height) => {
  return {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    position: "relative",
    width: "100%",
    height,
    overflow: "hidden"
  };
};
var waveformCanvasStyle = {
  pointerEvents: "none",
  flexShrink: 0
};
var volumeCanvasStyle = {
  position: "absolute"
};
var parseVolume = (volume) => {
  if (typeof volume === "number") {
    return volume;
  }
  return volume.split(",").map((v) => Number(v));
};
var AudioWaveformInner = ({
  src,
  height,
  startFrom,
  durationInFrames,
  displayOffsetInFrames,
  displayDurationInFrames,
  visualizationWidth,
  volume,
  doesVolumeChange,
  muted,
  playbackRate,
  loopDisplay
}) => {
  const [peaks, setPeaks] = useState49(null);
  const [error, setError] = useState49(null);
  const vidConf = Internals83.useUnsafeVideoConfig();
  if (vidConf === null) {
    throw new Error("Expected video config");
  }
  const waveformSampleRate = Math.ceil(vidConf.fps * TIMELINE_FRAME_WIDTH_AT_MAX_ZOOM);
  const waveformCanvas = useRef45(null);
  const volumeCanvas = useRef45(null);
  const shouldRenderVolumeOverlay = doesVolumeChange && typeof volume === "string";
  const parsedVolume = useMemo79(() => parseVolume(volume), [volume]);
  const visibleVolume = useMemo79(() => {
    if (muted) {
      return 0;
    }
    return getVisibleWaveformVolume({
      displayDurationInFrames,
      displayOffsetInFrames,
      loopDisplay,
      volume: parsedVolume
    });
  }, [
    displayDurationInFrames,
    displayOffsetInFrames,
    loopDisplay,
    muted,
    parsedVolume
  ]);
  useLayoutEffect23(() => {
    setPeaks(null);
    setError(null);
    return subscribeToWaveformPeaks({
      src,
      waveformSampleRate,
      onPeaks: (p) => setPeaks(p),
      onError: (err) => setError(err)
    });
  }, [src, waveformSampleRate]);
  const portionPeaks = useMemo79(() => {
    if (!peaks) {
      return null;
    }
    return sliceVisibleWaveformPeaks({
      displayDurationInFrames,
      displayOffsetInFrames,
      durationInFrames,
      fps: vidConf.fps,
      loopDisplay,
      peaks,
      playbackRate,
      startFrom,
      waveformSampleRate
    });
  }, [
    displayDurationInFrames,
    displayOffsetInFrames,
    durationInFrames,
    loopDisplay,
    peaks,
    playbackRate,
    startFrom,
    vidConf.fps,
    waveformSampleRate
  ]);
  useLayoutEffect23(() => {
    const { current: canvasElement } = waveformCanvas;
    if (!canvasElement) {
      return;
    }
    const pixelRatio = window.devicePixelRatio;
    const h = Math.ceil(height * pixelRatio);
    const w = Math.ceil(visualizationWidth * pixelRatio);
    const drawingWidth = visualizationWidth * pixelRatio;
    canvasElement.width = w;
    canvasElement.height = h;
    canvasElement.style.width = w / pixelRatio + "px";
    canvasElement.style.height = h / pixelRatio + "px";
    drawBars({
      canvas: canvasElement,
      peaks: portionPeaks ?? EMPTY_PEAKS,
      color: WHITE_ALPHA_60,
      volume: visibleVolume,
      width: drawingWidth
    });
  }, [height, portionPeaks, visibleVolume, visualizationWidth]);
  useLayoutEffect23(() => {
    if (!shouldRenderVolumeOverlay) {
      return;
    }
    const { current: volumeCanvasElement } = volumeCanvas;
    if (!volumeCanvasElement) {
      return;
    }
    const pixelRatio = window.devicePixelRatio;
    const h = Math.ceil(height * pixelRatio);
    const w = Math.ceil(visualizationWidth * pixelRatio);
    const drawingWidth = visualizationWidth * pixelRatio;
    const context = volumeCanvasElement.getContext("2d");
    if (!context) {
      return;
    }
    volumeCanvasElement.width = w;
    volumeCanvasElement.height = h;
    volumeCanvasElement.style.width = w / pixelRatio + "px";
    volumeCanvasElement.style.height = h / pixelRatio + "px";
    context.clearRect(0, 0, w, h);
    if (!Array.isArray(visibleVolume)) {
      return;
    }
    context.beginPath();
    context.moveTo(0, h);
    visibleVolume.forEach((v, index) => {
      const x = visibleVolume.length <= 1 ? 0 : index / (visibleVolume.length - 1) * drawingWidth;
      const y = (1 - v) * (h - TIMELINE_BORDER * 2 * pixelRatio) + pixelRatio;
      if (index === 0) {
        context.moveTo(x, y);
      } else {
        context.lineTo(x, y);
      }
    });
    context.strokeStyle = WHITE_ALPHA_70;
    context.lineWidth = pixelRatio;
    context.stroke();
  }, [height, shouldRenderVolumeOverlay, visibleVolume, visualizationWidth]);
  if (error) {
    return null;
  }
  if (!peaks) {
    return null;
  }
  return /* @__PURE__ */ jsxs70("div", {
    style: getContainerStyle2(height),
    children: [
      /* @__PURE__ */ jsx138("canvas", {
        ref: waveformCanvas,
        style: waveformCanvasStyle
      }),
      shouldRenderVolumeOverlay ? /* @__PURE__ */ jsx138("canvas", {
        ref: volumeCanvas,
        style: volumeCanvasStyle
      }) : null
    ]
  });
};
var AudioWaveform = React100.memo(AudioWaveformInner);

// src/components/Timeline/get-timeline-media-start-frame.ts
import { NoReactInternals as NoReactInternals13 } from "remotion/no-react";
var getTimelineMediaStartFrame = ({
  startMediaFrom,
  mediaFrameAtSequenceZero,
  sequenceFrameOffset,
  playbackRate
}) => {
  if (mediaFrameAtSequenceZero !== null) {
    return mediaFrameAtSequenceZero + sequenceFrameOffset * playbackRate;
  }
  return NoReactInternals13.getExpectedMediaFrameUncorrected({
    frame: sequenceFrameOffset,
    startFrom: startMediaFrom,
    playbackRate
  });
};

// src/components/Timeline/get-timeline-sequence-visible-layout.ts
var getVisibleSection = ({
  sectionStart,
  sectionEnd,
  cropStart,
  cropEnd
}) => {
  const start = Math.max(sectionStart, cropStart);
  const end = Math.min(sectionEnd, cropEnd);
  if (end <= start) {
    return null;
  }
  return {
    left: start - cropStart,
    width: end - start
  };
};
var getTimelineSequenceVisibleLayout = ({
  marginLeft,
  width,
  negativeStartWidth,
  premountWidth,
  postmountWidth,
  renderWindowLeft,
  renderWindowWidth
}) => {
  const itemStart = marginLeft;
  const itemEnd = marginLeft + width;
  const renderWindowEnd = renderWindowLeft + renderWindowWidth;
  const visibleStart = Math.max(itemStart, renderWindowLeft);
  const visibleEnd = Math.min(itemEnd, renderWindowEnd);
  if (visibleEnd <= visibleStart) {
    return null;
  }
  const cropStart = visibleStart - itemStart;
  const cropEnd = visibleEnd - itemStart;
  const premountStart = negativeStartWidth;
  const mediaStart = negativeStartWidth + premountWidth;
  const mediaEnd = Math.max(mediaStart, width - postmountWidth);
  const media = getVisibleSection({
    sectionStart: mediaStart,
    sectionEnd: mediaEnd,
    cropStart,
    cropEnd
  });
  return {
    marginLeft: visibleStart,
    width: visibleEnd - visibleStart,
    cropLeft: cropStart,
    leftEdgeVisible: visibleStart === itemStart,
    rightEdgeVisible: visibleEnd === itemEnd,
    negativeStart: getVisibleSection({
      sectionStart: 0,
      sectionEnd: negativeStartWidth,
      cropStart,
      cropEnd
    }),
    premount: getVisibleSection({
      sectionStart: premountStart,
      sectionEnd: premountStart + premountWidth,
      cropStart,
      cropEnd
    }),
    postmount: getVisibleSection({
      sectionStart: Math.max(0, width - postmountWidth),
      sectionEnd: width,
      cropStart,
      cropEnd
    }),
    media: media ? {
      ...media,
      offset: Math.max(0, cropStart - mediaStart),
      fullWidth: Math.max(0, mediaEnd - mediaStart)
    } : null
  };
};

// src/components/Timeline/LoopedTimelineIndicators.tsx
import { Internals as Internals86 } from "remotion";

// src/components/Timeline/LoopedIndicator.tsx
import { Internals as Internals85 } from "remotion";
import { jsx as jsx139, jsxs as jsxs71 } from "react/jsx-runtime";
var width = {
  width: 0,
  flexDirection: "row",
  display: "flex",
  position: "relative"
};
var icon2 = {
  height: 12,
  filter: LOOPED_INDICATOR_DROP_SHADOW
};
var Icon2 = () => /* @__PURE__ */ jsx139("svg", {
  viewBox: "0 0 512 512",
  style: icon2,
  children: /* @__PURE__ */ jsx139("path", {
    fill: LIGHT_COLOR,
    d: "M512 256c0 88.224-71.775 160-160 160H170.067l34.512 32.419c9.875 9.276 10.119 24.883.539 34.464l-10.775 10.775c-9.373 9.372-24.568 9.372-33.941 0l-92.686-92.686c-9.373-9.373-9.373-24.568 0-33.941l92.686-92.686c9.373-9.373 24.568-9.373 33.941 0l10.775 10.775c9.581 9.581 9.337 25.187-.539 34.464L170.067 352H352c52.935 0 96-43.065 96-96 0-13.958-2.996-27.228-8.376-39.204-4.061-9.039-2.284-19.626 4.723-26.633l12.183-12.183c11.499-11.499 30.965-8.526 38.312 5.982C505.814 205.624 512 230.103 512 256zM72.376 295.204C66.996 283.228 64 269.958 64 256c0-52.935 43.065-96 96-96h181.933l-34.512 32.419c-9.875 9.276-10.119 24.883-.539 34.464l10.775 10.775c9.373 9.372 24.568 9.372 33.941 0l92.686-92.686c9.373-9.373 9.373-24.568 0-33.941l-92.686-92.686c-9.373-9.373-24.568-9.373-33.941 0L306.882 29.12c-9.581 9.581-9.337 25.187.539 34.464L341.933 96H160C71.775 96 0 167.776 0 256c0 25.897 6.186 50.376 17.157 72.039 7.347 14.508 26.813 17.481 38.312 5.982l12.183-12.183c7.008-7.008 8.786-17.595 4.724-26.634z"
  })
});
var verticalLine = {
  height: "100%",
  width: 1,
  background: TRANSPARENT
};
var centerContainer = {
  justifyContent: "center",
  alignItems: "center"
};
var LoopedIndicator = () => {
  return /* @__PURE__ */ jsxs71("div", {
    style: width,
    children: [
      /* @__PURE__ */ jsx139(Internals85.AbsoluteFillElement, {
        style: centerContainer,
        children: /* @__PURE__ */ jsx139("div", {
          style: verticalLine
        })
      }),
      /* @__PURE__ */ jsx139(Internals85.AbsoluteFillElement, {
        style: centerContainer,
        children: /* @__PURE__ */ jsx139(Icon2, {})
      })
    ]
  });
};

// src/components/Timeline/LoopedTimelineIndicators.tsx
import { jsx as jsx140 } from "react/jsx-runtime";
var row5 = {
  overflow: "hidden"
};
var LoopedTimelineIndicator = ({ loops, fullWidth, visibleOffset, visibleWidth }) => {
  if (loops <= 1 || fullWidth <= 0 || visibleWidth <= 0) {
    return null;
  }
  const loopWidth = fullWidth / loops;
  const lastBoundaryIndex = Math.ceil(loops) - 1;
  const firstVisibleBoundaryIndex = Math.max(1, Math.floor(visibleOffset / loopWidth));
  const lastVisibleBoundaryIndex = Math.min(lastBoundaryIndex, Math.ceil((visibleOffset + visibleWidth) / loopWidth));
  const visibleBoundaryCount = Math.max(0, lastVisibleBoundaryIndex - firstVisibleBoundaryIndex + 1);
  const boundaries = new Array(visibleBoundaryCount).fill(true).map((_, index) => (firstVisibleBoundaryIndex + index) * loopWidth);
  return /* @__PURE__ */ jsx140(Internals86.AbsoluteFillElement, {
    style: row5,
    children: boundaries.map((boundary) => {
      return /* @__PURE__ */ jsx140("div", {
        style: {
          position: "absolute",
          display: "flex",
          left: boundary - visibleOffset,
          top: 0,
          bottom: 0
        },
        children: /* @__PURE__ */ jsx140(LoopedIndicator, {})
      }, boundary);
    })
  });
};

// src/components/Timeline/TimelineImageInfo.tsx
import { useMemo as useMemo80 } from "react";
import { jsx as jsx141 } from "react/jsx-runtime";
var HEIGHT = getTimelineLayerHeight("image") - 2;
var containerStyle2 = {
  height: HEIGHT,
  width: "100%",
  backgroundColor: BLACK_ALPHA_30,
  display: "flex",
  borderTopLeftRadius: 2,
  borderBottomLeftRadius: 2,
  backgroundRepeat: "repeat-x",
  backgroundSize: "auto 100%",
  backgroundPositionY: 0
};
var TimelineImageInfo = ({ src, offsetInPixels }) => {
  const style6 = useMemo80(() => {
    return {
      ...containerStyle2,
      backgroundImage: `url(${JSON.stringify(src)})`,
      backgroundPositionX: -offsetInPixels
    };
  }, [offsetInPixels, src]);
  return /* @__PURE__ */ jsx141("div", {
    style: style6
  });
};

// src/components/Timeline/TimelineSequenceFrame.tsx
import { jsx as jsx142, jsxs as jsxs72 } from "react/jsx-runtime";
var relativeFrameStyle = {
  alignItems: "center",
  display: "flex",
  fontSize: 11,
  fontFamily: "Arial, Helvetica, sans-serif",
  gap: 4,
  color: WHITE,
  opacity: 0.5,
  whiteSpace: "nowrap",
  pointerEvents: "none",
  userSelect: "none"
};
var snowflakeStyle = {
  flexShrink: 0,
  height: 12,
  width: 12
};
var TimelineSequenceFrame = ({ roundedFrame, frozenFrame, premounted, postmounted }) => {
  return /* @__PURE__ */ jsxs72("div", {
    style: relativeFrameStyle,
    children: [
      frozenFrame === null ? null : /* @__PURE__ */ jsx142(SnowflakeIcon, {
        style: snowflakeStyle,
        color: CURRENT_COLOR
      }),
      frozenFrame ?? (premounted ? "0 (Premounted)" : postmounted !== null ? `${postmounted} (Postmounted)` : roundedFrame)
    ]
  });
};

// src/components/Timeline/TimelineVideoInfo.tsx
import {
  addFrameToCache,
  aspectRatioCache,
  ensureSlots,
  extractFrames,
  fillFrameWhereItFits,
  fillWithCachedFrames,
  frameDatabase,
  getAspectRatioFromCache,
  getFrameDatabaseKeyPrefix,
  getLoopDisplaySegments,
  getTimestampFromFrameDatabaseKey,
  makeFrameDatabaseKey,
  resizeVideoFrame,
  WEBCODECS_TIMESCALE
} from "@remotion/timeline-utils";
import React102, { useEffect as useEffect56, useMemo as useMemo81, useRef as useRef46, useState as useState50 } from "react";
import { Internals as Internals87, useVideoConfig as useVideoConfig12 } from "remotion";

// src/components/Timeline/timeline-video-filmstrip-times.ts
import { shouldTileLoopDisplay } from "@remotion/timeline-utils";
var getTimelineVideoFilmstripTimes = ({
  trimBefore,
  durationInFrames,
  playbackRate,
  fps,
  loopDisplay,
  frozenMediaFrame
}) => {
  if (frozenMediaFrame !== null) {
    return {
      type: "frozen",
      timestampInSeconds: Math.max(0, frozenMediaFrame / fps)
    };
  }
  const fromSeconds = trimBefore / fps;
  const visibleDurationInFrames = shouldTileLoopDisplay(loopDisplay) && loopDisplay ? loopDisplay.durationInFrames : durationInFrames;
  return {
    type: "range",
    fromSeconds,
    toSeconds: fromSeconds + visibleDurationInFrames * playbackRate / fps
  };
};

// src/components/Timeline/TimelineVideoInfo.tsx
import { jsx as jsx143, jsxs as jsxs73 } from "react/jsx-runtime";
var outerStyle = {
  width: "100%",
  height: "100%",
  display: "flex",
  flexDirection: "column"
};
var filmstripContainerStyle = {
  height: TIMELINE_LAYER_FILMSTRIP_HEIGHT,
  backgroundColor: BLACK_ALPHA_30,
  display: "flex",
  overflow: "hidden",
  borderTopLeftRadius: 2,
  fontSize: 10,
  fontFamily: "Arial, Helvetica"
};
var MAX_FROZEN_FRAME_CACHE_DEVIATION = WEBCODECS_TIMESCALE * 0.05;
var TimelineVideoInfoSegment = ({
  src,
  visualizationWidth,
  startMediaFrom,
  mediaFrameAtSequenceZero,
  sequenceFrameOffset,
  durationInFrames,
  sourceOffsetInFrames,
  tiledLoop,
  playbackRate,
  volume,
  doesVolumeChange,
  muted,
  frozenMediaFrame,
  extendLastFrame
}) => {
  const { fps } = useVideoConfig12();
  const resolvedSrc = Internals87.usePreload(src);
  const ref = useRef46(null);
  const [error, setError] = useState50(null);
  const aspectRatio = useRef46(getAspectRatioFromCache(src));
  const mediaStartFrame = getTimelineMediaStartFrame({
    startMediaFrom,
    mediaFrameAtSequenceZero,
    sequenceFrameOffset,
    playbackRate
  }) + sourceOffsetInFrames * playbackRate;
  useEffect56(() => {
    if (error) {
      return;
    }
    const { current } = ref;
    if (!current) {
      return;
    }
    const controller = new AbortController;
    const pixelRatio = window.devicePixelRatio;
    const canvas = document.createElement("canvas");
    canvas.width = Math.ceil(visualizationWidth * pixelRatio);
    canvas.height = Math.ceil(TIMELINE_LAYER_FILMSTRIP_HEIGHT * pixelRatio);
    canvas.style.width = canvas.width / pixelRatio + "px";
    canvas.style.height = canvas.height / pixelRatio + "px";
    const ctx = canvas.getContext("2d");
    if (!ctx) {
      return;
    }
    current.appendChild(canvas);
    const drawRepeatedFrame = (frame2) => {
      const thumbnailWidth = Math.max(1, frame2.displayWidth);
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      for (let x = 0;x < canvas.width; x += thumbnailWidth) {
        ctx.drawImage(frame2, x, 0, thumbnailWidth, canvas.height);
      }
    };
    const getCachedFrozenFrame = (timestamp) => {
      const prefix = getFrameDatabaseKeyPrefix(src);
      const keys = Array.from(frameDatabase.keys()).filter((k) => k.startsWith(prefix));
      let bestDistance = Infinity;
      let bestFrame = null;
      for (const key7 of keys) {
        const frame2 = frameDatabase.get(key7);
        if (!frame2) {
          continue;
        }
        const distance2 = Math.abs(getTimestampFromFrameDatabaseKey(key7) - timestamp);
        if (distance2 < bestDistance) {
          bestDistance = distance2;
          bestFrame = frame2.frame;
        }
      }
      return bestDistance <= MAX_FROZEN_FRAME_CACHE_DEVIATION ? bestFrame : null;
    };
    const times = getTimelineVideoFilmstripTimes({
      trimBefore: mediaStartFrame,
      durationInFrames,
      playbackRate,
      fps,
      loopDisplay: undefined,
      frozenMediaFrame
    });
    if (times.type === "frozen") {
      const timestamp = times.timestampInSeconds * WEBCODECS_TIMESCALE;
      const cachedFrame = getCachedFrozenFrame(timestamp);
      if (cachedFrame) {
        drawRepeatedFrame(cachedFrame);
        return () => {
          current.removeChild(canvas);
        };
      }
      extractFrames({
        repeatLastFrame: extendLastFrame,
        timestampsInSeconds: ({
          track
        }) => {
          aspectRatio.current = track.width / track.height;
          aspectRatioCache.set(src, aspectRatio.current);
          return [times.timestampInSeconds];
        },
        src: resolvedSrc,
        onVideoSample: (sample) => {
          let frame2;
          try {
            frame2 = sample.toVideoFrame();
            const scale = canvas.height / frame2.displayHeight;
            const transformed = resizeVideoFrame({
              frame: frame2,
              scale
            });
            if (transformed !== frame2) {
              frame2.close();
            }
            frame2 = undefined;
            const databaseKey = makeFrameDatabaseKey(src, transformed.timestamp);
            addFrameToCache(databaseKey, transformed);
            drawRepeatedFrame(transformed);
          } catch (e) {
            if (frame2) {
              frame2.close();
            }
            throw e;
          } finally {
            sample.close();
          }
        },
        signal: controller.signal
      }).catch((e) => {
        setError(e);
      });
      return () => {
        controller.abort();
        current.removeChild(canvas);
      };
    }
    const targetCanvas = tiledLoop ? document.createElement("canvas") : canvas;
    targetCanvas.width = tiledLoop ? Math.max(1, Math.ceil(tiledLoop.loopWidth * pixelRatio)) : canvas.width;
    targetCanvas.height = canvas.height;
    const targetCtx = tiledLoop ? targetCanvas.getContext("2d") : ctx;
    if (!targetCtx) {
      current.removeChild(canvas);
      return;
    }
    const filledSlots = new Map;
    const { fromSeconds, toSeconds } = times;
    const targetWidth = tiledLoop ? tiledLoop.loopWidth * pixelRatio : visualizationWidth * pixelRatio;
    const repeatTarget = () => {
      if (!tiledLoop) {
        return;
      }
      const pattern = ctx.createPattern(targetCanvas, "repeat-x");
      if (!pattern) {
        return;
      }
      const phase = tiledLoop.displayOffsetInFrames % tiledLoop.loopDisplay.durationInFrames * (tiledLoop.loopWidth * pixelRatio / tiledLoop.loopDisplay.durationInFrames);
      pattern.setTransform(new DOMMatrix([
        tiledLoop.loopWidth * pixelRatio / targetCanvas.width,
        0,
        0,
        1,
        -phase,
        0
      ]));
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = pattern;
      ctx.fillRect(0, 0, canvas.width, canvas.height);
    };
    if (aspectRatio.current !== null) {
      ensureSlots({
        filledSlots,
        naturalWidth: targetWidth,
        fromSeconds,
        toSeconds,
        aspectRatio: aspectRatio.current,
        frameHeight: canvas.height
      });
      fillWithCachedFrames({
        ctx: targetCtx,
        naturalWidth: targetWidth,
        filledSlots,
        src,
        segmentDuration: toSeconds - fromSeconds,
        fromSeconds,
        devicePixelRatio: 1,
        frameHeight: canvas.height
      });
      repeatTarget();
      const unfilled = Array.from(filledSlots.keys()).filter((timestamp) => filledSlots.get(timestamp) === undefined);
      if (unfilled.length === 0) {
        return () => {
          current.removeChild(canvas);
        };
      }
    }
    extractFrames({
      repeatLastFrame: extendLastFrame,
      timestampsInSeconds: ({
        track
      }) => {
        aspectRatio.current = track.width / track.height;
        aspectRatioCache.set(src, aspectRatio.current);
        ensureSlots({
          filledSlots,
          fromSeconds,
          toSeconds,
          naturalWidth: targetWidth,
          aspectRatio: aspectRatio.current,
          frameHeight: canvas.height
        });
        fillWithCachedFrames({
          ctx: targetCtx,
          naturalWidth: targetWidth,
          filledSlots,
          src,
          segmentDuration: toSeconds - fromSeconds,
          fromSeconds,
          devicePixelRatio: 1,
          frameHeight: canvas.height
        });
        repeatTarget();
        return Array.from(filledSlots.keys()).filter((timestamp) => filledSlots.get(timestamp) === undefined).map((timestamp) => timestamp / WEBCODECS_TIMESCALE);
      },
      src: resolvedSrc,
      onVideoSample: (sample) => {
        let frame2;
        try {
          frame2 = sample.toVideoFrame();
          const scale = canvas.height / frame2.displayHeight;
          const transformed = resizeVideoFrame({
            frame: frame2,
            scale
          });
          if (transformed !== frame2) {
            frame2.close();
          }
          frame2 = undefined;
          const databaseKey = makeFrameDatabaseKey(src, transformed.timestamp);
          addFrameToCache(databaseKey, transformed);
          if (aspectRatio.current === null) {
            throw new Error("Aspect ratio is not set");
          }
          ensureSlots({
            filledSlots,
            fromSeconds,
            toSeconds,
            naturalWidth: targetWidth,
            aspectRatio: aspectRatio.current,
            frameHeight: canvas.height
          });
          fillFrameWhereItFits({
            ctx: targetCtx,
            filledSlots,
            visualizationWidth: targetWidth,
            frame: transformed,
            segmentDuration: toSeconds - fromSeconds,
            fromSeconds,
            devicePixelRatio: 1,
            frameHeight: canvas.height
          });
          repeatTarget();
        } catch (e) {
          if (frame2) {
            frame2.close();
          }
          throw e;
        } finally {
          sample.close();
        }
      },
      signal: controller.signal
    }).then(() => {
      if (controller.signal.aborted) {
        return;
      }
      fillWithCachedFrames({
        ctx: targetCtx,
        naturalWidth: targetWidth,
        filledSlots,
        src,
        segmentDuration: toSeconds - fromSeconds,
        fromSeconds,
        devicePixelRatio: 1,
        frameHeight: canvas.height
      });
      repeatTarget();
    }).catch((e) => {
      setError(e);
    });
    return () => {
      controller.abort();
      current.removeChild(canvas);
    };
  }, [
    durationInFrames,
    error,
    extendLastFrame,
    fps,
    frozenMediaFrame,
    mediaStartFrame,
    playbackRate,
    resolvedSrc,
    src,
    tiledLoop,
    visualizationWidth
  ]);
  const audioWidth = visualizationWidth;
  const filmstripStyle = useMemo81(() => {
    return {
      ...filmstripContainerStyle,
      width: visualizationWidth
    };
  }, [visualizationWidth]);
  const audioStyle = useMemo81(() => {
    return {
      width: audioWidth,
      position: "relative"
    };
  }, [audioWidth]);
  const segmentStyle = useMemo81(() => {
    return {
      ...outerStyle,
      width: visualizationWidth,
      flexShrink: 0
    };
  }, [visualizationWidth]);
  return /* @__PURE__ */ jsxs73("div", {
    style: segmentStyle,
    children: [
      /* @__PURE__ */ jsx143("div", {
        ref,
        style: filmstripStyle
      }),
      /* @__PURE__ */ jsx143("div", {
        style: audioStyle,
        children: /* @__PURE__ */ jsx143(AudioWaveform, {
          src,
          height: TIMELINE_VIDEO_INFO_WAVEFORM_HEIGHT,
          visualizationWidth: audioWidth,
          startFrom: mediaStartFrame,
          durationInFrames,
          displayOffsetInFrames: tiledLoop?.displayOffsetInFrames ?? 0,
          displayDurationInFrames: tiledLoop?.displayDurationInFrames ?? durationInFrames,
          volume,
          doesVolumeChange,
          muted,
          playbackRate,
          loopDisplay: tiledLoop?.loopDisplay
        })
      })
    ]
  });
};
var getVisibleSegments = ({
  displayDurationInFrames,
  displayOffsetInFrames,
  loopDisplay
}) => {
  if (!loopDisplay || loopDisplay.numberOfTimes <= 1 || loopDisplay.durationInFrames <= 0) {
    return [
      {
        key: "single",
        displayOffsetInFrames,
        durationInFrames: displayDurationInFrames,
        sourceOffsetInFrames: displayOffsetInFrames
      }
    ];
  }
  return getLoopDisplaySegments({
    displayDurationInFrames,
    displayOffsetInFrames,
    loopDurationInFrames: loopDisplay.durationInFrames
  }).map((segment) => {
    return {
      key: `loop-${segment.loopIndex}`,
      displayOffsetInFrames: segment.absoluteOffsetInFrames,
      durationInFrames: segment.durationInFrames,
      sourceOffsetInFrames: segment.loopOffsetInFrames
    };
  });
};
var TimelineVideoInfoInner = ({
  src,
  visualizationWidth,
  displayOffsetInFrames,
  displayDurationInFrames,
  startMediaFrom,
  mediaFrameAtSequenceZero,
  sequenceFrameOffset,
  playbackRate,
  volume,
  doesVolumeChange,
  muted,
  marginLeft,
  loopDisplay,
  frozenMediaFrame,
  extendLastFrame
}) => {
  const pixelsPerFrame = visualizationWidth / displayDurationInFrames;
  const loopWidth = loopDisplay ? loopDisplay.durationInFrames * pixelsPerFrame : null;
  const shouldTileLoop = loopDisplay !== undefined && loopWidth !== null && loopWidth <= visualizationWidth;
  const tiledLoop = useMemo81(() => {
    if (!shouldTileLoop || !loopDisplay || loopWidth === null) {
      return null;
    }
    return {
      displayDurationInFrames,
      displayOffsetInFrames,
      loopDisplay,
      loopWidth
    };
  }, [
    displayDurationInFrames,
    displayOffsetInFrames,
    loopDisplay,
    loopWidth,
    shouldTileLoop
  ]);
  const segments = useMemo81(() => {
    if (shouldTileLoop) {
      return [];
    }
    return getVisibleSegments({
      displayDurationInFrames,
      displayOffsetInFrames,
      loopDisplay
    });
  }, [
    displayDurationInFrames,
    displayOffsetInFrames,
    loopDisplay,
    shouldTileLoop
  ]);
  const getSegmentVolume = (segmentDisplayOffsetInFrames, segmentDurationInFrames) => {
    if (typeof volume === "number") {
      return volume;
    }
    const values = volume.split(",");
    return values.slice(Math.max(0, Math.floor(segmentDisplayOffsetInFrames)), Math.min(values.length, Math.ceil(segmentDisplayOffsetInFrames + segmentDurationInFrames))).join(",");
  };
  return /* @__PURE__ */ jsx143("div", {
    style: { display: "flex", marginLeft, height: "100%" },
    children: tiledLoop ? /* @__PURE__ */ jsx143(TimelineVideoInfoSegment, {
      src,
      visualizationWidth,
      startMediaFrom,
      mediaFrameAtSequenceZero,
      sequenceFrameOffset,
      durationInFrames: tiledLoop.loopDisplay.durationInFrames,
      sourceOffsetInFrames: 0,
      tiledLoop,
      playbackRate,
      volume,
      doesVolumeChange,
      muted,
      frozenMediaFrame,
      extendLastFrame
    }) : segments.map((segment) => /* @__PURE__ */ jsx143(TimelineVideoInfoSegment, {
      src,
      visualizationWidth: segment.durationInFrames * pixelsPerFrame,
      startMediaFrom,
      mediaFrameAtSequenceZero,
      sequenceFrameOffset,
      durationInFrames: segment.durationInFrames,
      sourceOffsetInFrames: segment.sourceOffsetInFrames,
      tiledLoop: null,
      playbackRate,
      volume: getSegmentVolume(segment.sourceOffsetInFrames, segment.durationInFrames),
      doesVolumeChange,
      muted,
      frozenMediaFrame,
      extendLastFrame
    }, segment.key))
  });
};
var TimelineVideoInfo = React102.memo(TimelineVideoInfoInner);

// src/components/Timeline/TimelineSequence.tsx
import { jsx as jsx144, jsxs as jsxs74, Fragment as Fragment34 } from "react/jsx-runtime";
var TimelineSequenceFn = ({
  s,
  connectedCompositions,
  nodePathInfo,
  sequenceFrameOffset,
  cascadedStart,
  localStart
}) => {
  const windowWidth = useContext89(TimelineWidthContext);
  if (windowWidth === null) {
    return null;
  }
  return /* @__PURE__ */ jsx144(TimelineSequenceInner, {
    windowWidth,
    s,
    connectedCompositions,
    nodePathInfo,
    sequenceFrameOffset,
    cascadedStart,
    localStart
  });
};
var TimelineSequenceNegativeStartInner = ({ left: left3, width: width2, leftEdgeVisible, clipped }) => {
  const outerStyle2 = useMemo82(() => {
    return {
      backgroundColor: clipped ? TIMELINE_BACKGROUND_COLOR : undefined,
      height: "100%",
      left: left3,
      minWidth: 2,
      pointerEvents: "none",
      position: "absolute",
      top: 0,
      width: width2
    };
  }, [clipped, left3, width2]);
  const innerStyle = useMemo82(() => {
    const showLeftEdge = leftEdgeVisible && !clipped;
    const maskImage2 = clipped ? "linear-gradient(to right, transparent, black 20%)" : undefined;
    return {
      backgroundColor: TIMELINE_NEGATIVE_START_BACKGROUND_COLOR,
      border: `${SEQUENCE_BORDER_WIDTH}px solid ${TIMELINE_NEGATIVE_START_BORDER_COLOR}`,
      borderBottomLeftRadius: showLeftEdge ? 2 : 0,
      borderLeft: showLeftEdge ? `${SEQUENCE_BORDER_WIDTH}px solid ${TIMELINE_NEGATIVE_START_BORDER_COLOR}` : "none",
      borderRight: "none",
      borderTopLeftRadius: showLeftEdge ? 2 : 0,
      boxSizing: "border-box",
      height: "100%",
      maskImage: maskImage2,
      position: "absolute",
      top: 0,
      WebkitMaskImage: maskImage2,
      width: "100%"
    };
  }, [clipped, leftEdgeVisible]);
  return /* @__PURE__ */ jsx144("div", {
    style: outerStyle2,
    children: /* @__PURE__ */ jsx144("div", {
      style: innerStyle
    })
  });
};
var TimelineSequenceNegativeStart = React103.memo(TimelineSequenceNegativeStartInner);
var TimelineSequenceCurrentFrame = ({
  s,
  displayDurationInFrames,
  premount,
  postmount,
  negativeStart,
  leftEdgeVisible,
  negativeStartClipped,
  style: style6,
  children,
  edgeDragHandles,
  nodePathInfo,
  sequenceFrameOffset,
  fromCanUpdate,
  frozenFrame,
  onMoveDragPointerDown,
  onPointerDownCapture,
  onClick
}) => {
  const { canvasContent } = useContext89(Internals88.CompositionManager);
  const isAsset = canvasContent?.type === "asset";
  const ref = useRef47(null);
  const { onSelect, selectable, selected, selectionItem } = useTimelineRowSelection(nodePathInfo);
  useTimelineMarqueeSelectableItem(selectionItem, ref);
  const onPointerDown = useCallback70((e) => {
    if (e.button === 0) {
      e.stopPropagation();
      if (shouldSelectTimelineRowOnPointerDown({
        selected,
        shiftKey: e.shiftKey,
        metaKey: e.metaKey,
        ctrlKey: e.ctrlKey
      })) {
        onSelect({
          shiftKey: e.shiftKey,
          toggleKey: e.metaKey || e.ctrlKey
        });
      }
      if (fromCanUpdate) {
        onMoveDragPointerDown(e);
      }
    }
  }, [fromCanUpdate, onMoveDragPointerDown, onSelect, selected]);
  const frame2 = useCurrentFrame7();
  const relativeFrame = frame2 - s.from;
  const sequenceFrame = relativeFrame + sequenceFrameOffset;
  const relativeFrameWithPremount = relativeFrame + (s.premountDisplay ?? 0);
  const relativeFrameWithPostmount = relativeFrame - displayDurationInFrames;
  const roundedFrame = Math.round(sequenceFrame * 100) / 100;
  const isInRange = relativeFrame >= 0 && relativeFrame < displayDurationInFrames;
  const isPremounting = relativeFrameWithPremount >= 0 && relativeFrameWithPremount < displayDurationInFrames && !isInRange;
  const isPostmounting = relativeFrameWithPostmount >= 0 && relativeFrameWithPostmount < (s.postmountDisplay ?? 0) && !isInRange;
  const negativeStartEnd = negativeStart ? negativeStart.left + negativeStart.width : 0;
  const actualStyle = useMemo82(() => {
    return {
      ...style6,
      background: negativeStart ? TRANSPARENT : style6.background,
      border: negativeStart ? "none" : style6.border,
      opacity: selected || isAsset ? 1 : 0.75
    };
  }, [isAsset, negativeStart, selected, style6]);
  const content2 = /* @__PURE__ */ jsxs74(Fragment34, {
    children: [
      premount ? /* @__PURE__ */ jsx144("div", {
        style: {
          left: premount.left,
          width: premount.width,
          height: "100%",
          background: `repeating-linear-gradient(
								-45deg,
								${TRANSPARENT},
								${TRANSPARENT} 2px,
								${isPremounting ? WHITE_ALPHA_50 : WHITE_ALPHA_20} 2px,
								${isPremounting ? WHITE_ALPHA_50 : WHITE_ALPHA_20} 4px
							)`,
          position: "absolute"
        }
      }) : null,
      postmount ? /* @__PURE__ */ jsx144("div", {
        style: {
          left: postmount.left,
          width: postmount.width,
          height: "100%",
          background: `repeating-linear-gradient(
								-45deg,
								${TRANSPARENT},
								${TRANSPARENT} 2px,
								${isPostmounting ? WHITE_ALPHA_50 : WHITE_ALPHA_20} 2px,
								${isPostmounting ? WHITE_ALPHA_50 : WHITE_ALPHA_20} 4px
							)`,
          position: "absolute"
        }
      }) : null,
      children,
      s.type !== "audio" && s.type !== "video" && s.type !== "image" && s.loopDisplay === undefined && (frozenFrame !== null || isInRange || isPremounting || isPostmounting) ? /* @__PURE__ */ jsx144("div", {
        style: {
          paddingLeft: 5 + negativeStartEnd + (premount?.width ?? 0),
          height: "100%",
          display: "flex",
          alignItems: "center"
        },
        children: /* @__PURE__ */ jsx144(TimelineSequenceFrame, {
          premounted: isPremounting,
          postmounted: isPostmounting ? s.duration - 1 : null,
          roundedFrame,
          frozenFrame
        })
      }) : null
    ]
  });
  return /* @__PURE__ */ jsxs74("div", {
    ref,
    ...{ [TIMELINE_MARQUEE_ITEM_ATTR]: true },
    style: actualStyle,
    title: s.displayName,
    onPointerDownCapture,
    onPointerDown: selectable ? onPointerDown : undefined,
    onClick: onClick ?? undefined,
    children: [
      negativeStart ? /* @__PURE__ */ jsxs74(Fragment34, {
        children: [
          /* @__PURE__ */ jsx144(TimelineSequenceNegativeStart, {
            left: negativeStart.left,
            width: negativeStart.width,
            leftEdgeVisible,
            clipped: negativeStartClipped
          }),
          /* @__PURE__ */ jsx144("div", {
            style: {
              background: style6.background,
              border: style6.border,
              borderBottomLeftRadius: 0,
              borderBottomRightRadius: style6.borderBottomRightRadius,
              borderLeft: "none",
              borderRightColor: style6.borderRightColor,
              borderTopLeftRadius: 0,
              borderTopRightRadius: style6.borderTopRightRadius,
              boxSizing: "border-box",
              height: "100%",
              left: negativeStartEnd,
              overflow: "hidden",
              position: "absolute",
              top: 0,
              width: `calc(100% - ${negativeStartEnd}px)`
            },
            children: /* @__PURE__ */ jsx144("div", {
              style: {
                height: "100%",
                left: -negativeStartEnd,
                position: "absolute",
                top: 0,
                width: style6.width
              },
              children: content2
            })
          })
        ]
      }) : /* @__PURE__ */ jsx144("div", {
        style: {
          position: "absolute",
          inset: 0,
          overflow: "hidden",
          borderRadius: "inherit"
        },
        children: content2
      }),
      edgeDragHandles
    ]
  });
};
var TimelineSequenceInner = ({
  s,
  connectedCompositions,
  windowWidth,
  nodePathInfo,
  sequenceFrameOffset,
  cascadedStart,
  localStart
}) => {
  const video = Internals88.useVideo();
  const { sequences } = useContext89(Internals88.SequenceManager);
  const renderWindow = useContext89(TimelineViewportContext);
  const mediaDurationDragLimitsRegistry = useContext89(TimelineSequenceMediaDurationDragLimitsContext);
  const dragAwareDoubleClick = useMemo82(() => createDragAwareDoubleClickTracker(), []);
  const mediaMetadata = useMediaMetadata(s.type === "audio" || s.type === "video" ? s.src : null);
  const extendVideoLastFrame = isVideoWithLastFrameHold(s);
  const naturalMediaDuration = (s.type === "audio" || s.type === "video") && mediaMetadata !== null && s.playbackRate > 0 ? Math.max(0, (mediaMetadata.duration * (video?.fps ?? 30) - getTimelineMediaStartFrame({
    startMediaFrom: s.startMediaFrom,
    mediaFrameAtSequenceZero: s.mediaFrameAtSequenceZero,
    sequenceFrameOffset,
    playbackRate: s.playbackRate
  })) / s.playbackRate) : null;
  const maxMediaDuration = s.type === "sequence" || s.type === "image" || extendVideoLastFrame ? Infinity : naturalMediaDuration;
  const effectiveMaxMediaDuration = s.loopDisplay ? null : maxMediaDuration;
  const {
    canConfigureApps,
    canOpenInEditor,
    canOpenSource,
    codingAgentInfo,
    editorInfo,
    openInCodingAgent: openInCodingAgent2,
    openInEditor: openInEditor3,
    openSource,
    originalLocation
  } = useOpenSequenceInApps(s);
  const validatedLocation = useMemo82(() => {
    if (!originalLocation || !originalLocation.source || !originalLocation.line) {
      return null;
    }
    return {
      source: originalLocation.source,
      line: originalLocation.line,
      column: originalLocation.column ?? 0
    };
  }, [originalLocation]);
  const { propStatuses } = useContext89(Internals88.VisualModePropStatusesContext);
  const nodePath = nodePathInfo?.sequenceSubscriptionKey ?? null;
  const propStatusesForOverride = useMemo82(() => {
    return nodePath ? Internals88.getPropStatusesCtx(propStatuses, nodePath) : undefined;
  }, [propStatuses, nodePath]);
  const durationCanUpdate = Boolean(isStudioInteractivityEnabled() && propStatusesForOverride?.durationInFrames?.status === "static");
  const durationCanResize = Boolean(isStudioInteractivityEnabled() && canResizeTimelineSequenceDuration({
    status: propStatusesForOverride?.durationInFrames
  }));
  const fromCanUpdate = Boolean(isStudioInteractivityEnabled() && propStatusesForOverride?.from?.status === "static");
  const trimBeforeCanUpdate = Boolean(isStudioInteractivityEnabled() && propStatusesForOverride?.trimBefore?.status === "static");
  const { previewServerState } = useContext89(StudioServerConnectionCtx);
  const previewConnected = previewServerState.type === "connected";
  const previewInteractive = previewConnected && isStudioInteractivityEnabled();
  const { setPropStatuses } = useContext89(Internals88.VisualModeSettersContext);
  const { setSelectedModal } = useContext89(SetSelectedModalContext);
  const selectAsset = useSelectAsset();
  const assetContextMenu = useAssetTimelineContextMenu();
  const selectComposition = useSelectComposition();
  const confirm = useConfirmationDialog();
  const deleteTimelineItems = useDeleteTimelineItems();
  const { onSelect, selectable, selected, selectedItems } = useTimelineRowSelection(nodePathInfo);
  const selectedSequenceNodePathInfos = useMemo82(() => {
    if (!selected || selectedItems.length < 2 || selectedItems.some((item) => item.type !== "sequence")) {
      return null;
    }
    return selectedItems.flatMap((item) => item.type === "sequence" ? [item.nodePathInfo] : []);
  }, [selected, selectedItems]);
  const performSequenceDoubleClick = useCallback70((e) => {
    if (isTimelineSelectionModifierEvent(e)) {
      e.stopPropagation();
      return;
    }
    const action = getSequenceDoubleClickAction({
      button: e.button,
      canOpenSource,
      numberOfConnectedCompositions: connectedCompositions.length,
      sequenceWasDragged: dragAwareDoubleClick.consumePointerGestureWasDragged()
    });
    if (action === null) {
      return;
    }
    e.stopPropagation();
    if (action === "open-connected-composition") {
      const timelinePosition = getCurrentFrame();
      selectComposition(connectedCompositions[0], true, getConnectedCompositionFrame({
        timelinePosition,
        sequence: s,
        sequenceFrameOffset
      }));
      return;
    }
    openSource();
  }, [
    canOpenSource,
    connectedCompositions,
    dragAwareDoubleClick,
    openSource,
    s,
    selectComposition,
    sequenceFrameOffset
  ]);
  const onSequenceClick = useCallback70((e) => {
    if (!dragAwareDoubleClick.acceptClickAsDoubleClick(e)) {
      return;
    }
    performSequenceDoubleClick(e);
  }, [dragAwareDoubleClick, performSequenceDoubleClick]);
  const canHandleSequenceDoubleClick = connectedCompositions.length === 1 || canOpenSource;
  const canDeleteFromSource = Boolean(nodePath && validatedLocation?.source);
  const deleteDisabled = !previewInteractive || !s.controls || !canDeleteFromSource;
  const isProgrammaticallyDuplicated = (nodePathInfo?.numberOfSequencesWithThisNodePath ?? 1) > 1;
  const duplicateDisabled = deleteDisabled || isProgrammaticallyDuplicated;
  const disableInteractivityDisabled = !previewInteractive || !s.showInTimeline || !nodePath || !validatedLocation?.source;
  const mediaSrc = s.type === "audio" || s.type === "video" || s.type === "image" ? s.src : null;
  const onDuplicateSequenceFromSource = useCallback70(() => {
    if (!validatedLocation?.source || !nodePathInfo || duplicateDisabled) {
      return;
    }
    duplicateSequencesFromSource([nodePathInfo], confirm).catch(() => {
      return;
    });
  }, [confirm, duplicateDisabled, nodePathInfo, validatedLocation?.source]);
  const onDuplicateSelectedSequences = useCallback70(() => {
    if (!previewInteractive || selectedSequenceNodePathInfos === null) {
      return;
    }
    duplicateSequencesFromSource(selectedSequenceNodePathInfos, confirm).catch(() => {
      return;
    });
  }, [confirm, previewInteractive, selectedSequenceNodePathInfos]);
  const onDeleteSequenceFromSource = useCallback70(() => {
    if (!validatedLocation?.source || !nodePath || !nodePathInfo || deleteDisabled) {
      return;
    }
    deleteTimelineItems([{ type: "sequence", nodePathInfo }]);
  }, [
    deleteDisabled,
    deleteTimelineItems,
    nodePath,
    nodePathInfo,
    validatedLocation?.source
  ]);
  const onDeleteSelectedSequences = useCallback70(() => {
    if (!previewInteractive || selectedSequenceNodePathInfos === null) {
      return;
    }
    deleteTimelineItems(selectedSequenceNodePathInfos.map((selectedNodePathInfo) => ({
      type: "sequence",
      nodePathInfo: selectedNodePathInfo
    })));
  }, [deleteTimelineItems, previewInteractive, selectedSequenceNodePathInfos]);
  const onDisableSequenceInteractivity = useCallback70(() => {
    if (disableInteractivityDisabled || !nodePath || !validatedLocation?.source || previewServerState.type !== "connected") {
      return;
    }
    disableSequenceInteractivity({
      fileName: validatedLocation.source,
      nodePath,
      setPropStatuses,
      clientId: previewServerState.clientId
    });
  }, [
    disableInteractivityDisabled,
    nodePath,
    previewServerState,
    setPropStatuses,
    validatedLocation?.source
  ]);
  const getContextMenuItems = useCallback70(() => {
    if (assetContextMenu !== null) {
      return assetContextMenu;
    }
    if (selectable && !selected) {
      onSelect({ shiftKey: false, toggleKey: false });
    }
    if (selectedSequenceNodePathInfos !== null) {
      return getMultiSequenceContextMenuItems({
        deleteDisabled: !previewInteractive,
        duplicateDisabled: !previewInteractive,
        onDeleteSelectedSequences,
        onDuplicateSelectedSequences
      });
    }
    const splitMenuItem = getSequenceSplitMenuItem({
      nodePathInfo,
      sequence: s,
      propStatuses: propStatusesForOverride,
      splitFrame: getCurrentFrame(),
      canEditSource: previewInteractive && Boolean(validatedLocation?.source),
      hasMultipleSelection: selected && selectedItems.length > 1
    });
    const freezeFrameMenuItem = getSequenceFreezeFrameMenuItem({
      clientId: previewInteractive && previewServerState.type === "connected" ? previewServerState.clientId : null,
      nodePath,
      propStatusesForOverride,
      sequence: s,
      sequenceFrameOffset,
      setPropStatuses,
      timelinePosition: getCurrentFrame(),
      validatedSource: validatedLocation?.source ?? null
    });
    return getSequenceContextMenuItems({
      assetLinkInfo: mediaSrc ? getTimelineAssetLinkInfo(mediaSrc) : null,
      canOpenInEditor,
      codingAgentInfo,
      copyImageElement: null,
      deleteDisabled,
      disableInteractivityDisabled,
      duplicateDisabled,
      editorInfo,
      includeSourceEditItems: isStudioInteractivityEnabled(),
      isProgrammaticallyDuplicated,
      onConfigureApps: canConfigureApps ? () => {
        setSelectedModal({
          type: "settings",
          initialTab: "apps",
          initialPublicLicenseKey: window.remotion_renderDefaults?.publicLicenseKey ?? null
        });
      } : null,
      onDeleteSequenceFromSource,
      onDisableSequenceInteractivity,
      onDuplicateSequenceFromSource,
      openInCodingAgent: openInCodingAgent2,
      openInEditor: openInEditor3,
      originalLocation,
      selectAsset,
      sequence: s,
      sourceActions: isStudioInteractivityEnabled() ? [
        ...splitMenuItem ? [splitMenuItem] : [],
        ...freezeFrameMenuItem ? [freezeFrameMenuItem] : []
      ] : []
    });
  }, [
    assetContextMenu,
    canOpenInEditor,
    canConfigureApps,
    codingAgentInfo,
    deleteDisabled,
    disableInteractivityDisabled,
    duplicateDisabled,
    editorInfo,
    isProgrammaticallyDuplicated,
    mediaSrc,
    nodePath,
    nodePathInfo,
    onSelect,
    onDeleteSequenceFromSource,
    onDeleteSelectedSequences,
    onDisableSequenceInteractivity,
    onDuplicateSequenceFromSource,
    onDuplicateSelectedSequences,
    openInCodingAgent2,
    openInEditor3,
    originalLocation,
    previewInteractive,
    previewServerState,
    propStatusesForOverride,
    s,
    selectAsset,
    selectable,
    selected,
    selectedItems.length,
    selectedSequenceNodePathInfos,
    sequenceFrameOffset,
    setPropStatuses,
    setSelectedModal,
    validatedLocation?.source
  ]);
  const { frozenFrame } = s;
  const { onPointerDown: onMoveDragPointerDown } = useTimelineSequenceFromDrag({
    nodePathInfo,
    windowWidth,
    timelineDurationInFrames: video?.durationInFrames ?? 1,
    onDragEnd: dragAwareDoubleClick.endPointerGesture
  });
  if (!video) {
    throw new TypeError("Expected video config");
  }
  const displayDurationInFrames = s.loopDisplay ? s.loopDisplay.durationInFrames * s.loopDisplay.numberOfTimes : s.duration;
  const {
    marginLeft,
    width: width2,
    negativeStartWidth,
    negativeStartClipped,
    premountWidth,
    postmountWidth
  } = useMemo82(() => {
    return getTimelineSequenceLayout({
      durationInFrames: displayDurationInFrames,
      startFrom: s.loopDisplay ? s.from + s.loopDisplay.startOffset : s.from,
      cascadedStart,
      startFromMedia: 0,
      maxMediaDuration: effectiveMaxMediaDuration,
      video,
      windowWidth,
      premountDisplay: s.premountDisplay,
      postmountDisplay: s.postmountDisplay
    });
  }, [
    cascadedStart,
    displayDurationInFrames,
    effectiveMaxMediaDuration,
    s,
    video,
    windowWidth
  ]);
  const visibleLayout = useMemo82(() => {
    if (renderWindow === null) {
      return null;
    }
    return getTimelineSequenceVisibleLayout({
      marginLeft,
      width: width2,
      negativeStartWidth,
      premountWidth: premountWidth ?? 0,
      postmountWidth: postmountWidth ?? 0,
      renderWindowLeft: renderWindow.left - TIMELINE_PADDING,
      renderWindowWidth: renderWindow.width
    });
  }, [
    marginLeft,
    negativeStartWidth,
    postmountWidth,
    premountWidth,
    renderWindow,
    width2
  ]);
  const mediaVisualizationStyle = useMemo82(() => {
    return {
      width: visibleLayout?.media?.width ?? 0,
      marginLeft: visibleLayout?.media?.left ?? 0,
      height: "100%"
    };
  }, [visibleLayout]);
  const showLeftBorderRadius = visibleLayout?.leftEdgeVisible === true && localStart >= 0 && (s.trimBefore ?? 0) === 0 && (s.type === "sequence" || s.type === "image" || getTimelineMediaStartFrame({
    startMediaFrom: s.startMediaFrom,
    mediaFrameAtSequenceZero: s.mediaFrameAtSequenceZero,
    sequenceFrameOffset,
    playbackRate: s.playbackRate
  }) === 0);
  const endsAtNaturalMediaDuration = !s.loopDisplay && s.frozenFrame === null && (s.type === "audio" || s.type === "video") && s.frozenMediaFrame === null && naturalMediaDuration !== null && Number.isFinite(naturalMediaDuration) && naturalMediaDuration > 0 && Math.ceil(Math.min(s.duration, video.durationInFrames - s.from, maxMediaDuration ?? Infinity)) === Math.ceil(naturalMediaDuration);
  const parentSequence = sequences.find((candidate) => candidate.id === s.parent);
  const parentEnd = parentSequence ? getTimelineVisibleStart(parentSequence, sequences) + getTimelineVisibleDuration(parentSequence, sequences) : video.durationInFrames;
  const endsAtContainerBoundary = Math.ceil(s.from + displayDurationInFrames) >= Math.ceil(Math.min(parentEnd, video.durationInFrames));
  const hasImplicitMediaDuration = (s.type === "audio" || s.type === "video") && propStatusesForOverride?.durationInFrames?.status === "static" && propStatusesForOverride.durationInFrames.codeValue === undefined;
  const durationInFramesCodeValue = propStatusesForOverride?.durationInFrames?.status === "static" ? propStatusesForOverride.durationInFrames.codeValue : null;
  const explicitDurationInFrames = typeof durationInFramesCodeValue === "number" && Number.isFinite(durationInFramesCodeValue) ? durationInFramesCodeValue : null;
  const isMedia = s.type === "audio" || s.type === "video";
  const mediaDurationDragLimits = isMedia ? getTimelineSequenceMediaDurationDragLimits({
    cascadedStart,
    displayDurationInFrames: s.duration,
    displayStart: s.from,
    effectiveMaxMediaDuration,
    explicitDurationInFrames,
    hasImplicitDuration: hasImplicitMediaDuration,
    naturalMediaDuration,
    timelineDurationInFrames: video.durationInFrames
  }) : null;
  const mediaDurationDragSelectionKey = nodePathInfo ? getTimelineSequenceSelectionKey(nodePathInfo) : null;
  useEffect57(() => {
    if (mediaDurationDragLimitsRegistry === null || mediaDurationDragSelectionKey === null || !isMedia) {
      return;
    }
    const registry = mediaDurationDragLimitsRegistry.current;
    registry.set(mediaDurationDragSelectionKey, mediaDurationDragLimits);
    return () => {
      registry.delete(mediaDurationDragSelectionKey);
    };
  }, [
    isMedia,
    mediaDurationDragLimits,
    mediaDurationDragLimitsRegistry,
    mediaDurationDragSelectionKey
  ]);
  const showRightBorderRadius = visibleLayout?.rightEdgeVisible === true && (endsAtContainerBoundary || endsAtNaturalMediaDuration);
  const style6 = useMemo82(() => {
    return {
      background: s.type === "audio" ? TIMELINE_AUDIO_GRADIENT : s.type === "video" ? TIMELINE_VIDEO_GRADIENT : BLUE,
      border: `${SEQUENCE_BORDER_WIDTH}px solid ${WHITE_ALPHA_20}`,
      borderLeftColor: visibleLayout?.leftEdgeVisible && !negativeStartClipped ? WHITE_ALPHA_20 : TRANSPARENT,
      borderRightColor: visibleLayout?.rightEdgeVisible ? WHITE_ALPHA_20 : TRANSPARENT,
      borderTopLeftRadius: showLeftBorderRadius ? 2 : 0,
      borderBottomLeftRadius: showLeftBorderRadius ? 2 : 0,
      borderTopRightRadius: showRightBorderRadius ? 2 : 0,
      borderBottomRightRadius: showRightBorderRadius ? 2 : 0,
      position: "absolute",
      height: getTimelineLayerHeight(s.type),
      marginLeft: visibleLayout?.marginLeft ?? 0,
      width: visibleLayout?.width ?? 0,
      color: WHITE,
      overflow: "visible"
    };
  }, [
    negativeStartClipped,
    s.type,
    showLeftBorderRadius,
    showRightBorderRadius,
    visibleLayout
  ]);
  const showRightEdgeDragHandle = isTimelineSequenceDurationDraggable(s) && nodePath !== null && validatedLocation !== null && durationCanResize && (!isMedia || mediaDurationDragLimits !== null);
  const showLeftEdgeDragHandle = isTimelineSequenceLeftEdgeDraggable(s) && nodePath !== null && validatedLocation !== null && (isCascadingSequence(s) || fromCanUpdate) && durationCanUpdate && trimBeforeCanUpdate;
  if (maxMediaDuration === null && !s.loopDisplay || visibleLayout === null) {
    return null;
  }
  const frameIncrement = (windowWidth - TIMELINE_PADDING * 2) / video.durationInFrames;
  const mediaDisplayOffsetInFrames = visibleLayout.media ? visibleLayout.media.offset / frameIncrement : 0;
  const mediaDisplayDurationInFrames = visibleLayout.media ? visibleLayout.media.width / frameIncrement : 0;
  const sequence = /* @__PURE__ */ jsxs74(TimelineSequenceCurrentFrame, {
    s,
    displayDurationInFrames,
    premount: visibleLayout.premount,
    postmount: visibleLayout.postmount,
    negativeStart: visibleLayout.negativeStart,
    leftEdgeVisible: visibleLayout.leftEdgeVisible,
    negativeStartClipped,
    style: style6,
    nodePathInfo,
    sequenceFrameOffset,
    fromCanUpdate,
    frozenFrame,
    onMoveDragPointerDown,
    onPointerDownCapture: dragAwareDoubleClick.beginPointerGesture,
    onClick: canHandleSequenceDoubleClick ? onSequenceClick : null,
    edgeDragHandles: /* @__PURE__ */ jsxs74(Fragment34, {
      children: [
        showLeftEdgeDragHandle && visibleLayout.leftEdgeVisible && negativeStartWidth === 0 && nodePathInfo && validatedLocation ? /* @__PURE__ */ jsx144(TimelineSequenceLeftEdgeDragHandle, {
          nodePathInfo,
          windowWidth,
          timelineDurationInFrames: video.durationInFrames ?? 1,
          onDragEnd: dragAwareDoubleClick.endPointerGesture,
          onSelect,
          selected
        }) : null,
        showRightEdgeDragHandle && visibleLayout.rightEdgeVisible && nodePathInfo && validatedLocation ? /* @__PURE__ */ jsx144(TimelineSequenceRightEdgeDragHandle, {
          nodePathInfo,
          mediaDurationDragLimits,
          windowWidth,
          timelineDurationInFrames: video.durationInFrames ?? 1,
          onDragEnd: dragAwareDoubleClick.endPointerGesture,
          onSelect,
          selected
        }) : null
      ]
    }),
    children: [
      s.type === "audio" && visibleLayout.media ? /* @__PURE__ */ jsx144("div", {
        style: mediaVisualizationStyle,
        children: /* @__PURE__ */ jsx144(AudioWaveform, {
          src: s.src,
          height: TIMELINE_LAYER_HEIGHT_AUDIO,
          doesVolumeChange: s.doesVolumeChange,
          muted: s.muted,
          visualizationWidth: visibleLayout.media.width,
          startFrom: s.startMediaFrom,
          durationInFrames: s.duration,
          displayOffsetInFrames: mediaDisplayOffsetInFrames,
          displayDurationInFrames: mediaDisplayDurationInFrames,
          volume: s.volume,
          playbackRate: s.playbackRate,
          loopDisplay: s.loopDisplay
        })
      }) : null,
      s.type === "video" && visibleLayout.media ? /* @__PURE__ */ jsx144(TimelineVideoInfo, {
        src: s.src,
        visualizationWidth: visibleLayout.media.width,
        displayOffsetInFrames: mediaDisplayOffsetInFrames,
        displayDurationInFrames: mediaDisplayDurationInFrames,
        startMediaFrom: s.startMediaFrom,
        mediaFrameAtSequenceZero: s.mediaFrameAtSequenceZero,
        sequenceFrameOffset,
        playbackRate: s.playbackRate,
        volume: s.volume,
        muted: s.muted,
        doesVolumeChange: s.doesVolumeChange,
        marginLeft: visibleLayout.media.left,
        loopDisplay: s.loopDisplay,
        frozenMediaFrame: s.frozenMediaFrame,
        extendLastFrame: extendVideoLastFrame
      }) : null,
      s.type === "image" && visibleLayout.media ? /* @__PURE__ */ jsx144("div", {
        style: mediaVisualizationStyle,
        children: /* @__PURE__ */ jsx144(TimelineImageInfo, {
          src: s.src,
          offsetInPixels: visibleLayout.media.offset
        })
      }) : null,
      s.loopDisplay === undefined ? null : /* @__PURE__ */ jsx144(LoopedTimelineIndicator, {
        loops: s.loopDisplay.numberOfTimes,
        fullWidth: width2,
        visibleOffset: visibleLayout.cropLeft,
        visibleWidth: visibleLayout.width
      })
    ]
  });
  return previewConnected || window.remotion_isReadOnlyStudio ? /* @__PURE__ */ jsx144(ContextMenu, {
    getItems: getContextMenuItems,
    children: sequence
  }) : sequence;
};
var TimelineSequence = React103.memo(TimelineSequenceFn);

// src/components/Timeline/TimelineTrack.tsx
import { jsx as jsx145, jsxs as jsxs75 } from "react/jsx-runtime";
var emptyConnectedCompositions = [];
var TimelineTrackUnmemoized = ({ track }) => {
  const { getIsExpanded } = useContext90(ExpandedTracksGetterContext);
  const { previewServerState } = useContext90(StudioServerConnectionCtx);
  const previewServerConnected = previewServerState.type === "connected";
  const timelineWidth = useContext90(TimelineWidthContext);
  const { hovered, onPointerEnter, onPointerLeave } = useTimelineSequenceHover(track.nodePathInfo);
  const rowHighlightBackground = useTimelineRowHighlightBackground(track.nodePathInfo, {
    hovered,
    selectedBackground: TIMELINE_SELECTED_BACKGROUND
  });
  const layerStyle = useMemo83(() => ({
    height: getTimelineLayerHeight(track.sequence.type),
    marginBottom: TIMELINE_ITEM_BORDER_BOTTOM,
    position: "relative"
  }), [track.sequence.type]);
  const showExpandedKeyframes = track.nodePathInfo !== null && previewServerConnected && getIsExpanded(track.nodePathInfo);
  return /* @__PURE__ */ jsxs75("div", {
    onPointerEnter,
    onPointerLeave,
    children: [
      /* @__PURE__ */ jsxs75("div", {
        style: layerStyle,
        children: [
          rowHighlightBackground && timelineWidth !== null ? /* @__PURE__ */ jsx145("div", {
            style: getTimelineSelectedTrackHighlightStyle(timelineWidth, rowHighlightBackground)
          }) : null,
          /* @__PURE__ */ jsx145(TimelineSequence, {
            s: track.sequence,
            cascadedStart: track.cascadedStart,
            localStart: track.localStart,
            connectedCompositions: track.connectedCompositions ?? emptyConnectedCompositions,
            nodePathInfo: track.nodePathInfo,
            sequenceFrameOffset: track.sequenceFrameOffset
          })
        ]
      }),
      showExpandedKeyframes && track.nodePathInfo ? /* @__PURE__ */ jsx145(TimelineExpandedTrackKeyframes, {
        sequence: track.sequence,
        nodePathInfo: track.nodePathInfo,
        keyframeDisplayOffset: track.keyframeDisplayOffset
      }) : null
    ]
  });
};
var TimelineTrack = React104.memo(TimelineTrackUnmemoized);

// src/components/Timeline/TimelineTracks.tsx
import { jsx as jsx146, jsxs as jsxs76 } from "react/jsx-runtime";
var content2 = {
  paddingLeft: TIMELINE_PADDING,
  paddingRight: TIMELINE_PADDING,
  position: "relative"
};
var timelineContent = {
  minHeight: "100%"
};
var TimelineTracksInner = ({ hasBeenCut }) => {
  const { rows, tracksEnd, virtualItems } = useTimelineVirtualization();
  const timelineStyle = useMemo84(() => {
    return {
      ...timelineContent,
      width: 100 + "%"
    };
  }, []);
  return /* @__PURE__ */ jsxs76("div", {
    style: timelineStyle,
    children: [
      /* @__PURE__ */ jsx146("div", {
        style: { ...content2, height: tracksEnd },
        children: virtualItems.map((virtualItem) => /* @__PURE__ */ jsx146("div", {
          style: {
            height: virtualItem.size,
            left: TIMELINE_PADDING,
            position: "absolute",
            right: TIMELINE_PADDING,
            top: virtualItem.start
          },
          children: /* @__PURE__ */ jsx146(TimelineTrack, {
            track: rows[virtualItem.index].track
          })
        }, virtualItem.key))
      }),
      hasBeenCut ? /* @__PURE__ */ jsx146(MaxTimelineTracksReached, {}) : null
    ]
  });
};
var TimelineTracks = React105.memo(TimelineTracksInner);

// src/components/Timeline/use-timeline-asset-drop.ts
import { useCallback as useCallback71, useContext as useContext91, useEffect as useEffect58, useMemo as useMemo85, useState as useState51 } from "react";
import { Internals as Internals89 } from "remotion";
var isEventTargetInsideElement = (event, element) => {
  return event.target instanceof Node && element.contains(event.target);
};
var useTimelineAssetDrop = () => {
  const { canvasContent, compositions } = useContext91(Internals89.CompositionManager);
  const videoConfig = Internals89.useUnsafeVideoConfig();
  const chooseSvgImportMode = useSvgImportDialog();
  const { setSelectedModal } = useContext91(SetSelectedModalContext);
  const { previewServerState } = useContext91(StudioServerConnectionCtx);
  const [isAddingAsset, setIsAddingAsset] = useState51(false);
  const [assetDropFrame, setAssetDropFrame] = useState51(null);
  const currentCompositionId = canvasContent?.type === "composition" ? canvasContent.compositionId : null;
  const currentComposition = useMemo85(() => {
    return compositions.find((composition) => composition.id === currentCompositionId) ?? null;
  }, [compositions, currentCompositionId]);
  const resolvedCompositionLocation = useResolvedStack(currentComposition?.stack ?? null);
  const compositionFile = resolvedCompositionLocation?.source ?? null;
  const compositionComponentInfo = useCachedCompositionComponentInfo({
    compositionFile,
    compositionId: currentCompositionId
  });
  const previewInteractive = previewServerState.type === "connected" && isStudioInteractivityEnabled();
  const canReceiveElementDrop = previewInteractive && !window.remotion_isReadOnlyStudio && currentCompositionId !== null && compositionFile !== null && videoConfig !== null && !isAddingAsset;
  const canInsertAsset = canReceiveElementDrop && compositionComponentInfo?.canAddSequence === true;
  const getDropFrame = useCallback71((event) => {
    if (videoConfig === null) {
      return null;
    }
    const scrollable = scrollableRef.current;
    return scrollable !== null && isEventTargetInsideElement(event, scrollable) ? getFrameFromTimelineDrop({
      clientX: event.clientX,
      durationInFrames: videoConfig.durationInFrames,
      scrollLeft: scrollable.scrollLeft,
      timelineLeft: scrollable.getBoundingClientRect().left,
      timelineWidth: getTimelineContentWidth()
    }) : getCurrentFrame();
  }, [videoConfig]);
  const onAssetDragOver = useCallback71((event) => {
    const timeline = timelineVerticalScroll.current;
    const { dataTransfer } = event;
    if (timeline === null || dataTransfer === null || !isSupportedDropEvent(event) || !isEventTargetInsideElement(event, timeline)) {
      setAssetDropFrame(null);
      return;
    }
    const canDropElementIntoNewComposition = compositionComponentInfo?.canAddSequence === false && canReceiveElementDrop && hasElementDragType(dataTransfer);
    event.preventDefault();
    event.stopPropagation();
    dataTransfer.dropEffect = canInsertAsset || canDropElementIntoNewComposition ? "copy" : "none";
    const scrollable = scrollableRef.current;
    const shouldShowDropFrame = canInsertAsset && scrollable !== null && isEventTargetInsideElement(event, scrollable);
    setAssetDropFrame(shouldShowDropFrame ? getDropFrame(event) : null);
  }, [
    canInsertAsset,
    canReceiveElementDrop,
    compositionComponentInfo?.canAddSequence,
    getDropFrame
  ]);
  const onAssetDrop = useCallback71(async (event) => {
    setAssetDropFrame(null);
    const timeline = timelineVerticalScroll.current;
    const { dataTransfer } = event;
    if (timeline === null || dataTransfer === null || !isSupportedDropEvent(event) || !isEventTargetInsideElement(event, timeline)) {
      return;
    }
    const localFiles = isFileDragEvent(event) ? Array.from(dataTransfer.files) : null;
    if (localFiles !== null && !window.remotion_isReadOnlyStudio) {
      event.preventDefault();
      event.stopPropagation();
      setIsAddingAsset(true);
      try {
        if (await handleCanvasCaptureDrop({
          files: localFiles,
          setSelectedModal
        })) {
          return;
        }
      } finally {
        setIsAddingAsset(false);
      }
    }
    if (getEffectDragData(dataTransfer) !== null) {
      event.preventDefault();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const canDropElementIntoNewComposition = compositionComponentInfo?.canAddSequence === false && canReceiveElementDrop && getElementDragData(dataTransfer) !== null;
    if (!canInsertAsset && !canDropElementIntoNewComposition || currentCompositionId === null || compositionFile === null || videoConfig === null) {
      if (compositionComponentInfo?.canAddSequence === false) {
        showNotification("Cannot insert this item: This composition component cannot accept a new <Sequence>.", 3000);
      }
      return;
    }
    const frame2 = getDropFrame(event);
    if (frame2 === null) {
      return;
    }
    setIsAddingAsset(true);
    try {
      await handleDrop({
        chooseSvgImportMode,
        compositionFile,
        compositionId: currentCompositionId,
        destinationDimensions: {
          height: videoConfig.height,
          width: videoConfig.width
        },
        dropPosition: {
          centerX: videoConfig.width / 2,
          centerY: videoConfig.height / 2
        },
        event,
        fps: videoConfig.fps,
        from: frame2,
        localFiles,
        preferCompositionStart: false
      });
    } finally {
      setIsAddingAsset(false);
    }
  }, [
    canInsertAsset,
    canReceiveElementDrop,
    chooseSvgImportMode,
    compositionComponentInfo?.canAddSequence,
    compositionFile,
    currentCompositionId,
    getDropFrame,
    setSelectedModal,
    videoConfig
  ]);
  const clearAssetDropFrame = useCallback71(() => {
    setAssetDropFrame(null);
  }, []);
  useEffect58(() => {
    document.addEventListener("dragover", onAssetDragOver, { capture: true });
    document.addEventListener("drop", onAssetDrop, { capture: true });
    document.addEventListener("dragend", clearAssetDropFrame, { capture: true });
    return () => {
      document.removeEventListener("dragover", onAssetDragOver, {
        capture: true
      });
      document.removeEventListener("drop", onAssetDrop, { capture: true });
      document.removeEventListener("dragend", clearAssetDropFrame, {
        capture: true
      });
    };
  }, [clearAssetDropFrame, onAssetDragOver, onAssetDrop]);
  return assetDropFrame;
};

// src/components/Timeline/Timeline.tsx
import { jsx as jsx147, jsxs as jsxs77, Fragment as Fragment35 } from "react/jsx-runtime";
var MIN_TIMELINE_LABELS_WIDTH = 240;
var container23 = {
  minHeight: "100%",
  flex: 1,
  display: "flex",
  height: 0,
  overflowY: "auto",
  backgroundColor: BACKGROUND
};
var noop2 = () => {
  return;
};
var TimelineContextMenuArea = ({ children }) => {
  const assetDropFrame = useTimelineAssetDrop();
  const { compositions, canvasContent } = useContext92(Internals90.CompositionManager);
  const videoConfig = Internals90.useUnsafeVideoConfig();
  const [isAddingSolid, setIsAddingSolid] = useState53(false);
  const [isAddingAsset, setIsAddingAsset] = useState53(false);
  const { previewServerState } = useContext92(StudioServerConnectionCtx);
  const previewConnected = previewServerState.type === "connected";
  const previewInteractive = previewConnected && isStudioInteractivityEnabled();
  const browserStudioOperations = getBrowserStudioOperations();
  const browserStudioCanInsertSolid = browserStudioOperations !== null;
  const currentCompositionId = canvasContent?.type === "composition" ? canvasContent.compositionId : null;
  const currentComposition = useMemo86(() => {
    if (currentCompositionId === null) {
      return null;
    }
    return compositions.find((composition) => composition.id === currentCompositionId) ?? null;
  }, [compositions, currentCompositionId]);
  const resolvedCompositionLocation = useResolvedStack(currentComposition?.stack ?? null);
  const compositionFile = resolvedCompositionLocation?.source ?? (currentCompositionId && browserStudioOperations ? browserStudioOperations.getCompositionFile(currentCompositionId) : null);
  const compositionComponentInfo = useCachedCompositionComponentInfo({
    compositionFile,
    compositionId: currentCompositionId
  });
  const canInsertSolid = (previewInteractive || browserStudioCanInsertSolid) && compositionComponentInfo?.canAddSequence === true && currentCompositionId !== null && compositionFile !== null && videoConfig !== null && !isAddingSolid;
  const canInsertAsset = previewInteractive && !window.remotion_isReadOnlyStudio && compositionComponentInfo?.canAddSequence === true && currentCompositionId !== null && compositionFile !== null && !isAddingAsset;
  const insertSolid = useCallback72(async () => {
    if (!canInsertSolid || currentCompositionId === null || compositionFile === null || videoConfig === null) {
      return;
    }
    setIsAddingSolid(true);
    try {
      const request = {
        compositionFile,
        compositionId: currentCompositionId,
        from: null,
        element: {
          type: "solid",
          width: videoConfig.width,
          height: videoConfig.height,
          position: null
        }
      };
      const result = browserStudioOperations ? await browserStudioOperations.insertSolid(request) : await callApi("/api/insert-jsx-element", request);
      if (result.success) {
        return;
      }
      showNotification(result.reason, 4000);
    } catch (err) {
      showNotification(err.message, 4000);
    } finally {
      setIsAddingSolid(false);
    }
  }, [
    browserStudioOperations,
    canInsertSolid,
    compositionFile,
    currentCompositionId,
    videoConfig
  ]);
  const insertAsset = useCallback72(async () => {
    if (!canInsertAsset || currentCompositionId === null || compositionFile === null || videoConfig === null) {
      return;
    }
    const files = await pickFilesToImport();
    if (files.length === 0) {
      return;
    }
    setIsAddingAsset(true);
    try {
      await importAssets({
        files,
        fps: videoConfig.fps,
        compositionFile,
        compositionId: currentCompositionId,
        destinationDimensions: null,
        dropPosition: null,
        from: null,
        preferCompositionStart: null,
        svgImportMode: "image"
      });
    } finally {
      setIsAddingAsset(false);
    }
  }, [canInsertAsset, compositionFile, currentCompositionId, videoConfig]);
  const getContextMenuItems = useCallback72(() => {
    return [
      {
        type: "item",
        id: "insert-solid",
        label: "Add <Solid>",
        value: "insert-solid",
        onClick: insertSolid,
        keyHint: null,
        leftItem: null,
        subMenu: null,
        quickSwitcherLabel: null,
        disabled: !canInsertSolid
      },
      {
        type: "item",
        id: "insert-asset",
        label: "Add asset",
        value: "insert-asset",
        onClick: insertAsset,
        keyHint: null,
        leftItem: null,
        subMenu: null,
        quickSwitcherLabel: null,
        disabled: !canInsertAsset
      }
    ];
  }, [insertSolid, canInsertSolid, insertAsset, canInsertAsset]);
  return /* @__PURE__ */ jsx147(ContextMenu, {
    ref: timelineVerticalScroll,
    getItems: getContextMenuItems,
    style: container23,
    className: "css-reset " + VERTICAL_SCROLLBAR_CLASSNAME,
    children: /* @__PURE__ */ jsx147(TimelineAssetDropFrameContext.Provider, {
      value: assetDropFrame,
      children
    })
  });
};
var TimelineInner = () => {
  const { sequences } = useContext92(Internals90.SequenceManager);
  const { canvasContent, compositions } = useContext92(Internals90.CompositionManager);
  const videoConfig = Internals90.useUnsafeVideoConfig();
  const isStill = useIsStill();
  const { overrideIdToNodePathMappings } = useContext92(Internals90.OverrideIdsToNodePathsGettersContext);
  const { previewServerState } = useContext92(StudioServerConnectionCtx);
  const previewConnected = previewServerState.type === "connected";
  const previewInteractive = previewConnected && isStudioInteractivityEnabled();
  const videoConfigIsNull = videoConfig === null;
  const timeline = useMemo86(() => {
    if (videoConfigIsNull) {
      return [];
    }
    return addTimelineDisplayGroups(calculateTimeline({
      sequences,
      overrideIdsToNodePaths: overrideIdToNodePathMappings,
      compositions
    }));
  }, [
    sequences,
    videoConfigIsNull,
    overrideIdToNodePathMappings,
    compositions
  ]);
  const durationInFrames = videoConfig?.durationInFrames ?? 0;
  const { getDragOverrides } = useContext92(Internals90.VisualModeDragOverridesContext);
  const filtered = useMemo86(() => {
    return timeline.filter((t) => {
      if (t.sequence.showInTimeline && t.nodePathInfo !== null && getDragOverrides(t.nodePathInfo.sequenceSubscriptionKey).from !== undefined) {
        return true;
      }
      return shouldShowTrackInTimeline(t, durationInFrames);
    });
  }, [durationInFrames, getDragOverrides, timeline]);
  const collapsed = useMemo86(() => {
    const seenDisplayGroups = new Set;
    return filtered.filter((track) => {
      if (track.displayGroup === null) {
        return true;
      }
      if (seenDisplayGroups.has(track.displayGroup.key)) {
        return false;
      }
      seenDisplayGroups.add(track.displayGroup.key);
      return true;
    });
  }, [filtered]);
  const { visibleTracks, value: layerChildrenValue } = useTimelineLayerChildren(collapsed, sequences, canvasContent?.type === "composition" ? canvasContent.compositionId : null);
  const pendingInsertedElementSelection = useSyncExternalStore(subscribeToInsertedElementSelection, getInsertedElementSelection, getInsertedElementSelection);
  const { fastRefreshes } = useContext92(FastRefreshContext);
  const pendingSelectionStart = useRef48(null);
  const currentSelection = useCurrentTimelineSelectionStateAsRef();
  useLayoutEffect25(() => {
    if (pendingInsertedElementSelection === null) {
      pendingSelectionStart.current = null;
      return;
    }
    const matchesInsertedNodePath = (track) => pendingInsertedElementSelection.nodePath === null || track.nodePathInfo !== null && track.nodePathInfo.sequenceSubscriptionKey.absolutePath === pendingInsertedElementSelection.nodePath.absolutePath && JSON.stringify(track.nodePathInfo.sequenceSubscriptionKey.nodePath) === JSON.stringify(pendingInsertedElementSelection.nodePath.nodePath);
    if (pendingSelectionStart.current?.selection !== pendingInsertedElementSelection) {
      pendingSelectionStart.current = {
        selection: pendingInsertedElementSelection,
        fastRefreshes,
        existingSequenceIds: new Set(timeline.filter(matchesInsertedNodePath).map((track) => track.sequence.id))
      };
      return;
    }
    if (pendingSelectionStart.current.fastRefreshes === fastRefreshes) {
      return;
    }
    if (canvasContent?.type === "composition" && canvasContent.compositionId !== pendingInsertedElementSelection.compositionId) {
      clearInsertedElementSelection(pendingInsertedElementSelection);
      return;
    }
    const insertedTrack = timeline.find((track) => matchesInsertedNodePath(track) && !pendingSelectionStart.current?.existingSequenceIds.has(track.sequence.id));
    if (!insertedTrack) {
      return;
    }
    const layerKey = layerChildrenValue.keys.get(insertedTrack.sequence.id);
    if (layerKey !== undefined && !layerChildrenValue.collapsed[layerKey]) {
      layerChildrenValue.toggle(layerKey);
    }
    if (insertedTrack.nodePathInfo === null) {
      return;
    }
    currentSelection.current.selectItems([{ type: "sequence", nodePathInfo: insertedTrack.nodePathInfo }], { reveal: true });
    clearInsertedElementSelection(pendingInsertedElementSelection);
    if (pendingInsertedElementSelection.notification !== null) {
      showNotification(pendingInsertedElementSelection.notification, 3000);
    }
  }, [
    canvasContent,
    currentSelection,
    fastRefreshes,
    layerChildrenValue,
    pendingInsertedElementSelection,
    timeline
  ]);
  const maxTimelineTracks = getStudioMaxTimelineTracks();
  const shown = useMemo86(() => {
    return maxTimelineTracks !== null && visibleTracks.length > maxTimelineTracks ? visibleTracks.slice(0, maxTimelineTracks) : visibleTracks;
  }, [visibleTracks, maxTimelineTracks]);
  const hasBeenCut = visibleTracks.length > shown.length;
  return /* @__PURE__ */ jsxs77(TimelineContextMenuArea, {
    children: [
      sequences.map((sequence) => {
        if (!shouldSubscribeToSequenceProps(sequence, previewInteractive)) {
          return null;
        }
        return /* @__PURE__ */ jsx147(SubscribeToNodePaths, {
          overrideId: sequence.controls.overrideId,
          componentIdentity: sequence.controls.componentIdentity,
          schema: sequence.controls.schema,
          getStack: sequence.getStack,
          effects: sequence.effects,
          videoConfigValues: sequence.controls.videoConfigValues
        }, sequence.id);
      }),
      isStudioInteractivityEnabled() ? /* @__PURE__ */ jsx147(SequencePropsObserver, {}) : null,
      /* @__PURE__ */ jsx147(TimelineLayerChildrenProvider, {
        value: layerChildrenValue,
        children: /* @__PURE__ */ jsx147(TimelineKeyframeTracksProvider, {
          tracks: filtered,
          children: /* @__PURE__ */ jsx147(TimelineSelectableItemsProvider, {
            timeline: shown,
            children: /* @__PURE__ */ jsxs77(TimelineVirtualizationProvider, {
              hasBeenCut,
              isStill,
              timeline: shown,
              children: [
                isStudioInteractivityEnabled() ? /* @__PURE__ */ jsx147(TimelineSelectAllKeybindings, {
                  timeline: shown
                }) : null,
                /* @__PURE__ */ jsx147(TimelineHeightContainer, {
                  children: isStill ? /* @__PURE__ */ jsx147(TimelineList, {}) : /* @__PURE__ */ jsxs77(TimelineWidthProvider, {
                    children: [
                      /* @__PURE__ */ jsx147(TimelinePinchZoom, {}),
                      /* @__PURE__ */ jsxs77(SplitterContainer, {
                        orientation: "vertical",
                        defaultFlex: 0.2,
                        id: "names-to-timeline",
                        maxFlex: 0.5,
                        minFlex: 0.15,
                        maxFlexerSize: null,
                        minFlexerSize: MIN_TIMELINE_LABELS_WIDTH,
                        maxAntiFlexerSize: null,
                        minAntiFlexerSize: null,
                        children: [
                          /* @__PURE__ */ jsx147(SplitterElement, {
                            type: "flexer",
                            sticky: /* @__PURE__ */ jsx147(TimelineTimePlaceholders, {}),
                            children: /* @__PURE__ */ jsx147(TimelineList, {})
                          }),
                          /* @__PURE__ */ jsx147(SplitterHandle, {
                            onCollapse: noop2,
                            onCollapseDuringDrag: null,
                            allowToCollapse: "none"
                          }),
                          /* @__PURE__ */ jsx147(SplitterElement, {
                            type: "anti-flexer",
                            sticky: /* @__PURE__ */ jsxs77(Fragment35, {
                              children: [
                                /* @__PURE__ */ jsx147(TimelineTimeIndicators, {}),
                                /* @__PURE__ */ jsx147(TimelineSlider, {})
                              ]
                            }),
                            children: /* @__PURE__ */ jsxs77(TimelineScrollable, {
                              children: [
                                /* @__PURE__ */ jsx147(TimelineTracks, {
                                  hasBeenCut
                                }),
                                /* @__PURE__ */ jsx147(TimelinePlayCursorSyncer, {}),
                                /* @__PURE__ */ jsx147(TimelineInOutPointer, {}),
                                /* @__PURE__ */ jsx147(TimelineDragHandler, {}),
                                isStudioInteractivityEnabled() ? /* @__PURE__ */ jsx147(TimelineInOutDragHandler, {}) : null
                              ]
                            })
                          })
                        ]
                      })
                    ]
                  })
                })
              ]
            })
          })
        })
      })
    ]
  });
};
var MemoizedTimelineInner = React106.memo(TimelineInner);
var Timeline = () => {
  return /* @__PURE__ */ jsx147(TimelineTickFormatProvider, {
    children: /* @__PURE__ */ jsx147(TimelineSequenceMediaDurationDragLimitsProvider, {
      children: /* @__PURE__ */ jsx147(MemoizedTimelineInner, {})
    })
  });
};

// src/components/Timeline/TimelineEmptyState.tsx
import { jsx as jsx148 } from "react/jsx-runtime";
var container24 = {
  display: "flex",
  flex: 1,
  height: 0,
  backgroundColor: BACKGROUND
};
var TimelineEmptyState = () => {
  return /* @__PURE__ */ jsx148("div", {
    style: container24
  });
};

// src/components/WebMcp.tsx
import { isValidPackageName } from "@remotion/studio-shared";
import {
  useContext as useContext94,
  useEffect as useEffect60,
  useMemo as useMemo87,
  useRef as useRef49
} from "react";
import { Internals as Internals91, staticFile as staticFile4 } from "remotion";

// src/components/OptionalPackageModal.tsx
import React107, { useCallback as useCallback73, useContext as useContext93, useEffect as useEffect59, useState as useState55 } from "react";
import { jsx as jsx149, jsxs as jsxs78, Fragment as Fragment36 } from "react/jsx-runtime";
var panelStyle = {
  width: "min(520px, calc(100vw - 40px))"
};
var contentStyle = {
  color: WHITE,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5,
  padding: 16
};
var codeStyle2 = {
  color: "inherit",
  fontFamily: "monospace",
  fontSize: "inherit"
};
var statusStyle = {
  alignItems: "center",
  color: LIGHT_TEXT,
  display: "flex",
  fontFamily: "sans-serif",
  fontSize: 14,
  gap: 10,
  lineHeight: 1.5
};
var errorStyle = {
  color: WARNING_COLOR,
  marginTop: 12,
  whiteSpace: "pre-wrap"
};
var cancelStyle = { minWidth: 90 };
var isOptionalPackageInstalled = (packageName) => window.remotion_installedPackages?.includes(packageName) ?? false;
var OPTIONAL_PACKAGE_INSTALLED_EVENT = "remotion-optional-package-installed";
var markOptionalPackageInstalled = (packageName) => {
  window.remotion_installedPackages = Array.from(new Set([...window.remotion_installedPackages ?? [], packageName]));
  window.dispatchEvent(new CustomEvent(OPTIONAL_PACKAGE_INSTALLED_EVENT, {
    detail: packageName
  }));
};
var installOptionalPackage = async (packageName) => {
  const dependencies = withRequiredAuxiliaryPackages([
    { name: packageName, version: null }
  ]);
  await installPackages(dependencies);
  for (const dependency of dependencies) {
    markOptionalPackageInstalled(dependency.name);
  }
};
var useOptionalPackageInstalled = (packageName) => {
  const [installed, setInstalled] = useState55(() => isOptionalPackageInstalled(packageName));
  useEffect59(() => {
    const onInstalled = (event) => {
      if (event.detail === packageName) {
        setInstalled(true);
      }
    };
    window.addEventListener(OPTIONAL_PACKAGE_INSTALLED_EVENT, onInstalled);
    return () => window.removeEventListener(OPTIONAL_PACKAGE_INSTALLED_EVENT, onInstalled);
  }, [packageName]);
  return installed;
};
var OptionalPackageModal = ({ ariaLabel, children, packageName, title: title2 }) => {
  const { setSelectedModal } = useContext93(SetSelectedModalContext);
  const installed = useOptionalPackageInstalled(packageName);
  const [installState, setInstallState] = useState55({
    type: "idle"
  });
  const busy = installState.type === "installing";
  const packageNames = withRequiredAuxiliaryPackages([
    { name: packageName, version: null }
  ]).map((dependency) => dependency.name);
  const packagesLabel = packageNames.join(" and ");
  const dismiss = useCallback73(() => {
    if (!busy) {
      setSelectedModal(null);
    }
  }, [busy, setSelectedModal]);
  const install = useCallback73(async () => {
    setInstallState({ type: "installing" });
    try {
      await installOptionalPackage(packageName);
    } catch (error) {
      setInstallState({ type: "error", error });
    }
  }, [packageName]);
  if (installed) {
    return children;
  }
  return /* @__PURE__ */ jsxs78(ModalContainer, {
    ariaLabel,
    onEscape: dismiss,
    onOutsideClick: dismiss,
    panelStyle,
    children: [
      /* @__PURE__ */ jsx149(ModalHeader, {
        title: title2,
        onClose: dismiss
      }),
      /* @__PURE__ */ jsx149("div", {
        style: contentStyle,
        children: busy ? /* @__PURE__ */ jsxs78("div", {
          style: statusStyle,
          children: [
            /* @__PURE__ */ jsx149(Spinner, {
              duration: 0.5,
              size: 18
            }),
            `Installing ${packagesLabel}…`
          ]
        }) : /* @__PURE__ */ jsxs78(Fragment36, {
          children: [
            "This requires installing",
            " ",
            packageNames.map((name, index) => /* @__PURE__ */ jsxs78(React107.Fragment, {
              children: [
                index > 0 ? " and " : null,
                /* @__PURE__ */ jsx149("code", {
                  style: codeStyle2,
                  children: name
                })
              ]
            }, name)),
            ". Continue?",
            installState.type === "error" ? /* @__PURE__ */ jsx149("div", {
              style: errorStyle,
              children: installState.error.message
            }) : null
          ]
        })
      }),
      /* @__PURE__ */ jsx149(ModalFooterContainer, {
        children: /* @__PURE__ */ jsxs78(Row, {
          align: "center",
          children: [
            /* @__PURE__ */ jsx149(Flex, {}),
            /* @__PURE__ */ jsx149(Button, {
              disabled: busy,
              onClick: dismiss,
              style: cancelStyle,
              children: "Cancel"
            }),
            /* @__PURE__ */ jsx149(Spacing, {
              x: 1
            }),
            /* @__PURE__ */ jsx149(ModalButton, {
              disabled: busy,
              onClick: install,
              autoFocus: true,
              children: installState.type === "error" ? "Retry" : "Continue"
            })
          ]
        })
      })
    ]
  });
};

// src/components/Transcription/whisper-webgpu-capability.ts
var WHISPER_WEBGPU_PACKAGE = "@remotion/whisper-webgpu";

// src/components/VideoMatting/video-matting-capability.ts
var VIDEO_MATTING_PACKAGE = "@remotion/video-matting";

// src/components/WebMcp.tsx
import { jsx as jsx150 } from "react/jsx-runtime";
var serializeSequence = (track) => {
  return {
    sequenceId: track.sequence.id,
    name: track.sequence.displayName || track.sequence.controls?.componentName || null,
    type: track.sequence.type,
    parentSequenceId: track.sequence.parent,
    depth: track.depth,
    startFrame: track.sequence.from,
    endFrame: track.sequence.from + track.sequence.duration - 1,
    durationInFrames: track.sequence.duration,
    stack: track.sequence.getStack(),
    selectable: track.nodePathInfo !== null
  };
};
var serializeCompositionTree = (items) => {
  return items.map((item) => {
    if (item.type === "composition") {
      return {
        type: "composition",
        compositionName: item.composition.id
      };
    }
    return {
      type: "folder",
      folderName: item.folderName,
      children: serializeCompositionTree(item.items)
    };
  });
};
var getNoStack = () => null;
var MAX_CANVAS_HTML_LENGTH = 1e5;
var missingOptionalPackageResult = (packageName) => ({
  error: `This requires ${packageName}. Call install_package with packageName set to ${packageName}, then retry.`,
  installPackage: {
    packageName,
    tool: "install_package"
  },
  success: false
});
var resolveAssetPath = ({
  assetPath,
  currentContent,
  staticFiles
}) => {
  const resolvedAssetPath = assetPath === undefined ? currentContent?.type === "asset" ? currentContent.asset ?? null : null : assetPath;
  if (typeof resolvedAssetPath !== "string" || resolvedAssetPath.length === 0) {
    throw new Error("assetPath must be a non-empty public-folder path, or an asset must be selected in Studio.");
  }
  if (!staticFiles.some((file) => file.name === resolvedAssetPath)) {
    throw new Error(`Asset ${resolvedAssetPath} was not found in public/.`);
  }
  return resolvedAssetPath;
};
var WebMcpSelectionSync = ({ currentSelectionRef, selectedSequenceRef }) => {
  const { selectedItems } = useTimelineSelection();
  const { sequences } = useContext94(Internals91.SequenceManager);
  const { overrideIdToNodePathMappings } = useContext94(Internals91.OverrideIdsToNodePathsGettersContext);
  const selectedItem = selectedItems.length === 1 ? selectedItems[0] : null;
  const track = useMemo87(() => {
    if (selectedItem === null || selectedItem.type === "guide") {
      return null;
    }
    return findTrackForNodePathInfo({
      sequences,
      overrideIdsToNodePaths: overrideIdToNodePathMappings,
      nodePathInfo: selectedItem.nodePathInfo
    }) ?? null;
  }, [overrideIdToNodePathMappings, selectedItem, sequences]);
  const { resolvedLocation, stack: stack2 } = useResolveStackAndReactToChange(track?.sequence.getStack ?? getNoStack, track?.sequence.controls?.overrideId ?? track?.sequence.id ?? "no-selection");
  const currentStack = track?.sequence.getStack() ?? null;
  const currentResolvedLocation = stack2 === currentStack ? resolvedLocation : null;
  const currentSelection = useMemo87(() => {
    if (selectedItems.length !== 1 || selectedItem === null || selectedItem.type === "guide" || track === null) {
      return null;
    }
    let name;
    if (selectedItem.type === "sequence") {
      name = track.sequence.displayName || track.sequence.controls?.componentName || null;
    } else if (selectedItem.type === "sequence-prop") {
      name = `Property "${selectedItem.key}"`;
    } else if (selectedItem.type === "sequence-all-effects") {
      name = "Effects";
    } else if (selectedItem.type === "sequence-effect") {
      const { effects } = track.sequence;
      const effect = effects[selectedItem.i];
      name = effect ? `Effect "${effect.label}"` : null;
    } else if (selectedItem.type === "sequence-effect-prop") {
      name = `Effect property "${selectedItem.key}"`;
    } else {
      const field = parseKeyframeFieldFromNodePath(selectedItem.nodePathInfo.auxiliaryKeys);
      name = field?.type === "sequence" ? `Property "${field.fieldKey}"` : field?.type === "effect" ? `Effect property "${field.fieldKey}"` : null;
    }
    return formatContextForAgents({
      location: currentResolvedLocation,
      name,
      root: window.remotion_cwd
    });
  }, [currentResolvedLocation, selectedItem, selectedItems.length, track]);
  currentSelectionRef.current = currentSelection;
  selectedSequenceRef.current = track === null ? null : serializeSequence(track);
  return null;
};
var WebMcp = () => {
  const { addCaptionJob, addVideoMattingJob } = useContext94(RenderQueueContext);
  const staticFiles = useStaticFiles();
  const timelineSelectionRef = useCurrentTimelineSelectionStateAsRef();
  const currentSelectionRef = useRef49(null);
  const selectedSequenceRef = useRef49(null);
  const { canvasContent, compositions, currentCompositionMetadata, folders } = useContext94(Internals91.CompositionManager);
  const { playbackRate: currentPlaybackRate, setPlaybackRate } = Internals91.usePlaybackRate();
  const { isPlaying } = Internals91.Timeline.useTimelineContext();
  const { mediaVolume, playerMuted } = useContext94(Internals91.MediaVolumeContext);
  const { setPlayerMuted } = useContext94(Internals91.SetMediaVolumeContext);
  const { setZoom: setTimelineZoom, zoom: timelineZoomMap } = useContext94(TimelineZoomCtx);
  const selectComposition = useSelectComposition();
  const { editorShowGuides, guidesList, setEditorShowGuides, setGuidesList } = useContext94(EditorShowGuidesContext);
  const { sequences } = useContext94(Internals91.SequenceManager);
  const { overrideIdToNodePathMappings } = useContext94(Internals91.OverrideIdsToNodePathsGettersContext);
  const currentCompositionDefinition = useMemo87(() => {
    if (canvasContent?.type !== "composition") {
      return null;
    }
    return compositions.find((composition) => composition.id === canvasContent.compositionId) ?? null;
  }, [canvasContent, compositions]);
  const currentContent = useMemo87(() => {
    if (canvasContent?.type === "output-blob") {
      return {
        type: canvasContent.type,
        displayName: canvasContent.displayName,
        width: canvasContent.width,
        height: canvasContent.height,
        sizeInBytes: canvasContent.sizeInBytes
      };
    }
    return canvasContent;
  }, [canvasContent]);
  const currentContentRef = useRef49(currentContent);
  currentContentRef.current = currentContent;
  const staticFilesRef = useRef49(staticFiles);
  staticFilesRef.current = staticFiles;
  const currentComposition = currentCompositionDefinition?.id ?? null;
  const currentCompositionRef = useRef49(currentComposition);
  currentCompositionRef.current = currentComposition;
  const currentCompositionDefinitionRef = useRef49(currentCompositionDefinition);
  currentCompositionDefinitionRef.current = currentCompositionDefinition;
  const compositionsRef = useRef49(compositions);
  compositionsRef.current = compositions;
  const sequencesRef = useRef49(sequences);
  sequencesRef.current = sequences;
  const overrideIdToNodePathMappingsRef = useRef49(overrideIdToNodePathMappings);
  overrideIdToNodePathMappingsRef.current = overrideIdToNodePathMappings;
  const foldersRef = useRef49(folders);
  foldersRef.current = folders;
  const currentCompositionMetadataRef = useRef49(currentCompositionMetadata);
  currentCompositionMetadataRef.current = currentCompositionMetadata;
  const guidesListRef = useRef49(guidesList);
  guidesListRef.current = guidesList;
  const editorShowGuidesRef = useRef49(editorShowGuides);
  editorShowGuidesRef.current = editorShowGuides;
  const mediaVolumeRef = useRef49(mediaVolume);
  mediaVolumeRef.current = mediaVolume;
  const playbackRateRef = useRef49(currentPlaybackRate);
  playbackRateRef.current = currentPlaybackRate;
  const playerMutedRef = useRef49(playerMuted);
  playerMutedRef.current = playerMuted;
  const timelineZoomRef = useRef49(timelineZoomMap);
  timelineZoomRef.current = timelineZoomMap;
  useEffect60(() => {
    const { modelContext } = document;
    if (typeof modelContext?.registerTool !== "function") {
      return;
    }
    const controller = new AbortController;
    const getCurrentTimeline = () => {
      const composition = currentCompositionDefinitionRef.current;
      if (composition === null) {
        return [];
      }
      const durationInFrames = currentCompositionMetadataRef.current?.durationInFrames ?? composition.durationInFrames ?? getCurrentDuration();
      return calculateTimeline({
        sequences: sequencesRef.current,
        overrideIdsToNodePaths: overrideIdToNodePathMappingsRef.current,
        compositions: compositionsRef.current
      }).filter((timelineTrack) => shouldShowTrackInTimeline(timelineTrack, durationInFrames));
    };
    Promise.all([
      modelContext.registerTool({
        name: "install_package",
        title: "Install Studio package",
        description: "Install an npm package into the current Remotion project. Remotion packages are pinned to the current Remotion version, and known auxiliary packages are pinned to their recommended version.",
        inputSchema: {
          type: "object",
          properties: {
            packageName: {
              type: "string",
              description: "The npm package name to install."
            },
            version: {
              type: "string",
              description: "An optional exact semantic version. Omit it to use the recommended version."
            }
          },
          required: ["packageName"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: async ({ packageName, version }) => {
          if (typeof packageName !== "string" || !isValidPackageName(packageName)) {
            throw new Error("packageName must be a valid npm package name.");
          }
          if (version !== undefined && (typeof version !== "string" || !/^[0-9]+\.[0-9]+\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?$/.test(version))) {
            throw new Error("version must be an exact semantic version.");
          }
          await installPackages([
            {
              name: packageName,
              version: typeof version === "string" ? version : null
            }
          ]);
          markOptionalPackageInstalled(packageName);
          return { installed: true, packageName };
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "transcribe_asset",
        title: "Transcribe Studio asset",
        description: "Transcribe an audio or video asset from the public folder into Remotion Caption[] JSON and add the work to the Jobs queue. If assetPath is omitted, the asset currently open in Studio is used.",
        inputSchema: {
          type: "object",
          properties: {
            assetPath: {
              type: "string",
              description: "Optional path relative to public/. Defaults to the asset currently open in Studio."
            },
            outputPath: {
              type: "string",
              description: "Optional Caption[] JSON path relative to public/. Defaults to <asset>-captions.json."
            },
            model: {
              type: "string",
              default: "small.en",
              description: "Whisper model. Defaults to small.en."
            },
            language: {
              type: "string",
              default: "en",
              description: "Spoken language code for multilingual models. Defaults to en."
            },
            task: {
              type: "string",
              enum: ["transcribe", "translate"],
              default: "transcribe"
            },
            chunkLengthInSeconds: {
              type: "number",
              minimum: 1,
              maximum: 30,
              default: 30
            },
            strideLengthInSeconds: {
              type: "number",
              minimum: 0,
              default: 5
            },
            forceFullSequences: { type: "boolean", default: false },
            doSample: { type: "boolean", default: false },
            temperature: { type: "number", exclusiveMinimum: 0, default: 1 },
            topK: { type: "integer", minimum: 0, default: 50 },
            repetitionPenalty: {
              type: "number",
              exclusiveMinimum: 0,
              default: 1
            },
            noRepeatNgramSize: { type: "integer", minimum: 0, default: 0 }
          },
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: async (input2) => {
          if (!isOptionalPackageInstalled(WHISPER_WEBGPU_PACKAGE)) {
            return missingOptionalPackageResult(WHISPER_WEBGPU_PACKAGE);
          }
          const assetPath = resolveAssetPath({
            assetPath: input2.assetPath,
            currentContent: currentContentRef.current,
            staticFiles: staticFilesRef.current
          });
          const fileType = getPreviewFileType(assetPath);
          if (fileType !== "audio" && fileType !== "video") {
            throw new Error("The transcription asset must be audio or video.");
          }
          const whisper = await import("@remotion/whisper-webgpu");
          const modelName = input2.model ?? "small.en";
          if (typeof modelName !== "string") {
            throw new Error("model must be a string.");
          }
          const model = whisper.getAvailableModels().find((candidate) => candidate.name === modelName);
          if (!model) {
            throw new Error(`Unknown Whisper model: ${modelName}.`);
          }
          const task = input2.task ?? "transcribe";
          if (task !== "transcribe" && task !== "translate") {
            throw new Error("task must be transcribe or translate.");
          }
          if (task === "translate" && !model.supportsTranslation) {
            throw new Error(`${model.name} does not support translation.`);
          }
          const language = input2.language ?? "en";
          if (typeof language !== "string" || language.length === 0) {
            throw new Error("language must be a non-empty string.");
          }
          const chunkLengthInSeconds = input2.chunkLengthInSeconds ?? 30;
          const strideLengthInSeconds = input2.strideLengthInSeconds ?? 5;
          const temperature = input2.temperature ?? 1;
          const topK = input2.topK ?? 50;
          const repetitionPenalty = input2.repetitionPenalty ?? 1;
          const noRepeatNgramSize = input2.noRepeatNgramSize ?? 0;
          if (typeof chunkLengthInSeconds !== "number" || !Number.isFinite(chunkLengthInSeconds) || chunkLengthInSeconds < 1 || chunkLengthInSeconds > 30) {
            throw new Error("chunkLengthInSeconds must be between 1 and 30.");
          }
          if (typeof strideLengthInSeconds !== "number" || !Number.isFinite(strideLengthInSeconds) || strideLengthInSeconds < 0 || strideLengthInSeconds * 2 >= chunkLengthInSeconds) {
            throw new Error("strideLengthInSeconds must be non-negative and less than half of chunkLengthInSeconds.");
          }
          if (typeof temperature !== "number" || !Number.isFinite(temperature) || temperature <= 0 || typeof repetitionPenalty !== "number" || !Number.isFinite(repetitionPenalty) || repetitionPenalty <= 0 || typeof topK !== "number" || !Number.isInteger(topK) || topK < 0 || typeof noRepeatNgramSize !== "number" || !Number.isInteger(noRepeatNgramSize) || noRepeatNgramSize < 0) {
            throw new Error("Invalid transcription decoding settings.");
          }
          const forceFullSequences = input2.forceFullSequences ?? false;
          const doSample = input2.doSample ?? false;
          if (typeof forceFullSequences !== "boolean" || typeof doSample !== "boolean") {
            throw new Error("forceFullSequences and doSample must be booleans.");
          }
          const src = staticFile4(assetPath);
          const displayName = assetPath.split("/").at(-1) ?? assetPath;
          const outputPath = input2.outputPath ?? getDefaultCaptionOutputName(src, displayName);
          if (typeof outputPath !== "string") {
            throw new Error("outputPath must be a string.");
          }
          const outputError = validateCaptionOutputName(outputPath);
          if (outputError !== null) {
            throw new Error(outputError);
          }
          const jobId = addCaptionJob({
            audioStreamIndex: null,
            chunkLengthInSeconds,
            displayName,
            doSample,
            forceFullSequences,
            language: model.multilingual ? language : null,
            model: model.name,
            noRepeatNgramSize,
            outName: outputPath,
            repetitionPenalty,
            requestInit: null,
            src,
            strideLengthInSeconds,
            task,
            temperature,
            topK
          });
          return { jobId, outputPath, success: true };
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "separate_video_layers",
        title: "Separate Studio video layers",
        description: "Separate a video asset from the public folder into background and foreground WebM files and add the work to the Jobs queue. If assetPath is omitted, the asset currently open in Studio is used.",
        inputSchema: {
          type: "object",
          properties: {
            assetPath: {
              type: "string",
              description: "Optional path relative to public/. Defaults to the asset currently open in Studio."
            },
            baseOutputPath: {
              type: "string",
              description: "Optional background output path relative to public/."
            },
            foregroundOutputPath: {
              type: "string",
              description: "Optional foreground output path relative to public/."
            },
            model: {
              type: "string",
              default: "ben2-base"
            },
            audio: {
              type: "string",
              enum: ["base", "foreground", "both", "none"],
              default: "base"
            },
            videoBitrate: {
              oneOf: [
                {
                  type: "string",
                  enum: ["very-low", "low", "medium", "high", "very-high"]
                },
                { type: "integer", minimum: 1 }
              ],
              default: "very-high"
            }
          },
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: async (input2) => {
          if (!isOptionalPackageInstalled(VIDEO_MATTING_PACKAGE)) {
            return missingOptionalPackageResult(VIDEO_MATTING_PACKAGE);
          }
          const assetPath = resolveAssetPath({
            assetPath: input2.assetPath,
            currentContent: currentContentRef.current,
            staticFiles: staticFilesRef.current
          });
          if (getPreviewFileType(assetPath) !== "video") {
            throw new Error("The separation asset must be a video.");
          }
          const videoMatting = await import("@remotion/video-matting");
          const modelName = input2.model ?? "ben2-base";
          if (typeof modelName !== "string") {
            throw new Error("model must be a string.");
          }
          const model = videoMatting.getAvailableModels().find((candidate) => candidate.name === modelName)?.name;
          if (!model) {
            throw new Error(`Unknown video matting model: ${modelName}.`);
          }
          const audio = input2.audio ?? "base";
          if (audio !== "base" && audio !== "foreground" && audio !== "both" && audio !== "none") {
            throw new Error("audio must be base, foreground, both, or none.");
          }
          const videoBitrate = input2.videoBitrate ?? "very-high";
          if ((typeof videoBitrate !== "number" || !Number.isInteger(videoBitrate) || videoBitrate <= 0) && videoBitrate !== "very-low" && videoBitrate !== "low" && videoBitrate !== "medium" && videoBitrate !== "high" && videoBitrate !== "very-high") {
            throw new Error("videoBitrate is invalid.");
          }
          const src = staticFile4(assetPath);
          const displayName = assetPath.split("/").at(-1) ?? assetPath;
          const baseName = getDefaultOutputBaseName(src, displayName, "video");
          const baseOutputPath = input2.baseOutputPath ?? `${baseName}-base.webm`;
          const foregroundOutputPath = input2.foregroundOutputPath ?? `${baseName}-foreground.webm`;
          if (typeof baseOutputPath !== "string" || typeof foregroundOutputPath !== "string") {
            throw new Error("Output paths must be strings.");
          }
          const baseError = validatePublicOutputName({
            extension: ".webm",
            outName: baseOutputPath
          });
          const foregroundError = validatePublicOutputName({
            extension: ".webm",
            outName: foregroundOutputPath
          });
          if (baseError !== null || foregroundError !== null) {
            throw new Error(baseError ?? foregroundError ?? "Invalid output path.");
          }
          if (baseOutputPath.normalize("NFC").toLowerCase() === foregroundOutputPath.normalize("NFC").toLowerCase()) {
            throw new Error("Background and foreground outputs must be different.");
          }
          const jobId = addVideoMattingJob({
            audio,
            baseOutName: baseOutputPath,
            displayName,
            foregroundOutName: foregroundOutputPath,
            model,
            src,
            videoBitrate
          });
          return {
            baseOutputPath,
            foregroundOutputPath,
            jobId,
            success: true
          };
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "restart_studio",
        title: "Restart Studio",
        description: "Restart the Studio server. The browser temporarily disconnects and reconnects when Studio is ready. Only available in a writable Studio with a server backend.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => restartStudio()
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "shut_down_studio",
        title: "Shut down Studio",
        description: "Gracefully shut down the Studio server. The browser disconnects. Start Studio again from the terminal to reconnect. Only available in a writable Studio with a server backend.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => shutDownStudio()
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_current_error",
        title: "Get current Studio error",
        description: "Read the error currently shown in the Remotion Studio error overlay. Returns null when the overlay is not visible and includes symbolicated stack frames when symbolication succeeds.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: async () => {
          const currentError = getCurrentError();
          if (currentError === null) {
            return null;
          }
          let symbolicatedStackFrames = null;
          try {
            const record = await currentError.symbolication;
            symbolicatedStackFrames = record?.stackFrames ?? null;
          } catch {}
          return {
            name: currentError.error.name,
            message: currentError.error.message,
            stack: currentError.error.stack ?? null,
            symbolicatedStackFrames
          };
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_compositions",
        title: "Get Studio compositions",
        description: "Read the mounted Remotion Studio compositions as the nested folder tree shown in the sidebar.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const tree = createFolderTree(compositionsRef.current, foldersRef.current, {});
          return Promise.resolve({
            compositions: serializeCompositionTree(tree)
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "select_composition",
        title: "Select Studio composition",
        description: "Open a registered composition in Remotion Studio by name.",
        inputSchema: {
          type: "object",
          properties: {
            compositionName: {
              type: "string",
              description: "The name of the composition to open."
            }
          },
          required: ["compositionName"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ compositionName }) => {
          if (typeof compositionName !== "string") {
            throw new Error("compositionName must be a string.");
          }
          const composition = compositionsRef.current.find((candidate) => candidate.id === compositionName);
          if (!composition) {
            throw new Error(`Composition ${compositionName} not found.`);
          }
          selectComposition(composition, true);
          return Promise.resolve({
            currentContent: {
              type: "composition",
              compositionId: composition.id
            }
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_sequences",
        title: "Get Studio sequences",
        description: "Read the mounted sequences in the current Remotion Studio timeline, including their IDs, hierarchy, timing, type, source stack, and whether they can be selected.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          return Promise.resolve({
            currentContent: currentContentRef.current,
            sequences: compositionId === null ? [] : getCurrentTimeline().map(serializeSequence)
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "select_sequence",
        title: "Select Studio sequence",
        description: "Select and reveal a sequence in the current Remotion Studio timeline by the sequence ID returned by get_sequences.",
        inputSchema: {
          type: "object",
          properties: {
            sequenceId: {
              type: "string",
              minLength: 1,
              description: "The sequence ID returned by get_sequences."
            }
          },
          required: ["sequenceId"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ sequenceId }) => {
          if (typeof sequenceId !== "string" || sequenceId.length === 0) {
            throw new Error("sequenceId must be a non-empty string.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          if (!timelineSelectionRef.current.canSelect) {
            throw new Error("Studio sequence selection is unavailable.");
          }
          const timelineTrack = getCurrentTimeline().find((candidate) => candidate.sequence.id === sequenceId);
          if (!timelineTrack) {
            throw new Error(`Sequence ${sequenceId} not found in the current composition.`);
          }
          const selection = getTimelineSelectionFromNodePathInfo(timelineTrack.nodePathInfo);
          if (selection === null) {
            throw new Error(`Sequence ${sequenceId} cannot be selected.`);
          }
          timelineSelectionRef.current.selectItems([selection], {
            reveal: true
          });
          return Promise.resolve({
            currentContent: currentContentRef.current,
            selectedSequence: serializeSequence(timelineTrack)
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_composition",
        title: "Get Studio composition",
        description: "Read the name, source stack, duration, dimensions, frame rate, and current frame of the composition open in Remotion Studio. All fields are null when the canvas is not showing a composition.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const composition = currentCompositionDefinitionRef.current;
          if (composition === null) {
            return Promise.resolve({
              compositionName: null,
              stack: null,
              durationInFrames: null,
              height: null,
              width: null,
              fps: null,
              currentFrame: null
            });
          }
          const metadata = currentCompositionMetadataRef.current;
          return Promise.resolve({
            compositionName: composition.id,
            stack: composition.stack,
            durationInFrames: metadata?.durationInFrames ?? composition.durationInFrames ?? null,
            height: metadata?.height ?? composition.height ?? null,
            width: metadata?.width ?? composition.width ?? null,
            fps: metadata?.fps ?? composition.fps ?? null,
            currentFrame: getCurrentFrame()
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_canvas_html",
        title: "Get Studio canvas HTML",
        description: "Read the HTML of the rendered composition at the current frame. The result is limited to the composition canvas and does not include the Studio interface. Canvas and WebGL pixels are not included.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            return Promise.resolve({
              currentContent: currentContentRef.current,
              currentFrame: null,
              html: null,
              htmlLength: null,
              truncated: false
            });
          }
          const outerHtml = Internals91.portalNode().outerHTML;
          return Promise.resolve({
            currentContent: currentContentRef.current,
            currentFrame: getCurrentFrame(),
            html: outerHtml.slice(0, MAX_CANVAS_HTML_LENGTH),
            htmlLength: outerHtml.length,
            truncated: outerHtml.length > MAX_CANVAS_HTML_LENGTH
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_outlines",
        title: "Get Studio canvas outlines",
        description: "Read the active selectable component outlines in the current Remotion Studio canvas, including sequence identity, source-code location, and geometry in composition pixels.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: async () => {
          const composition = currentCompositionDefinitionRef.current;
          if (composition === null) {
            return {
              currentContent: currentContentRef.current,
              currentFrame: null,
              outlines: []
            };
          }
          const currentFrame = getCurrentFrame();
          const selectableOutlines = getSequencesWithSelectableOutlines({
            sequences: sequencesRef.current,
            overrideIdsToNodePaths: overrideIdToNodePathMappingsRef.current,
            compositions: compositionsRef.current,
            timelinePosition: currentFrame
          });
          const portalNode = Internals91.portalNode();
          const portalRect = portalNode.getBoundingClientRect();
          const metadata = currentCompositionMetadataRef.current;
          const compositionWidth = metadata?.width ?? composition.width ?? portalNode.offsetWidth;
          const compositionHeight = metadata?.height ?? composition.height ?? portalNode.offsetHeight;
          const scaleX = portalRect.width / compositionWidth;
          const scaleY = portalRect.height / compositionHeight;
          if (!Number.isFinite(scaleX) || scaleX === 0 || !Number.isFinite(scaleY) || scaleY === 0) {
            throw new Error("The Studio canvas is not ready to be measured.");
          }
          const measuredOutlines = measureOutlineTargets(portalNode, selectableOutlines.map((outline) => {
            if (outline.sequence.refForOutline === null) {
              throw new Error("Expected an outline ref.");
            }
            return {
              key: outline.key,
              ref: outline.sequence.refForOutline,
              crop: { left: 0, right: 0, top: 0, bottom: 0 },
              includeOutsideContainer: true
            };
          }));
          const measurementsByKey = new Map(measuredOutlines.map((outline) => [outline.key, outline]));
          const contextByStack = new Map;
          const outlines = await Promise.all(selectableOutlines.map(async (outline) => {
            const measurement = measurementsByKey.get(outline.key);
            if (!measurement) {
              return null;
            }
            const outlineStack = outline.sequence.getStack();
            let location2 = null;
            if (outlineStack !== null) {
              let promise = contextByStack.get(outlineStack);
              if (!promise) {
                promise = getOriginalLocationFromStack(outlineStack, "sequence").catch(() => null);
                contextByStack.set(outlineStack, promise);
              }
              location2 = await promise;
            }
            const name = outline.sequence.displayName || outline.sequence.controls?.componentName || null;
            const points = measurement.points.map((point) => ({
              x: point.x / scaleX,
              y: point.y / scaleY
            }));
            const xValues = points.map((point) => point.x);
            const yValues = points.map((point) => point.y);
            const left3 = Math.min(...xValues);
            const top = Math.min(...yValues);
            const right = Math.max(...xValues);
            const bottom = Math.max(...yValues);
            return {
              sequenceId: outline.sequence.id,
              parentSequenceId: outline.sequence.parent,
              name,
              location: getRelativeFileLocation({
                location: location2,
                root: window.remotion_cwd
              }),
              geometry: {
                points,
                boundingBox: {
                  x: left3,
                  y: top,
                  width: right - left3,
                  height: bottom - top
                }
              }
            };
          }));
          return {
            currentContent: currentContentRef.current,
            currentFrame,
            outlines: outlines.filter((outline) => outline !== null)
          };
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_playback_state",
        title: "Get Studio playback state",
        description: "Read the current frame, playing state, audio state, playback rate, looping state, and normalized timeline zoom for the composition open in Remotion Studio. All playback fields are null when the canvas is not showing a composition.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            return Promise.resolve({
              currentContent: currentContentRef.current,
              currentFrame: null,
              playing: null,
              muted: null,
              volume: null,
              playbackRate: null,
              looping: null,
              timelineZoom: null
            });
          }
          const durationInFrames = getCurrentDuration();
          const timelineViewportWidth = scrollableRef.current?.clientWidth ?? 0;
          const minZoom = getTimelineMinZoom({
            durationInFrames,
            timelineViewportWidth
          });
          const timelineZoom = getTimelineZoom({
            durationInFrames,
            timelineViewportWidth,
            zoom: timelineZoomRef.current[compositionId] ?? null
          });
          return Promise.resolve({
            currentContent: currentContentRef.current,
            currentFrame: getCurrentFrame(),
            playing: isPlaying(),
            muted: playerMutedRef.current,
            volume: mediaVolumeRef.current,
            playbackRate: playbackRateRef.current,
            looping: loadLoopOption(),
            timelineZoom: timelineZoomToNormalized({
              zoom: timelineZoom,
              minZoom
            })
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_selection",
        title: "Get Studio selection",
        description: 'Read the current frame, canvas content (composition, asset, or output), and source-code context for the item currently selected in Remotion Studio. The selection matches "Copy context for agents".',
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => Promise.resolve({
          currentFrame: getCurrentFrame(),
          currentSelection: currentSelectionRef.current,
          currentContent: currentContentRef.current,
          selectionType: timelineSelectionRef.current.selectedItems.length === 1 ? timelineSelectionRef.current.selectedItems[0].type : null,
          selectedSequence: selectedSequenceRef.current
        })
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "get_guides",
        title: "Get Studio guides",
        description: "Read the guides for the current Remotion Studio composition. Vertical guide positions are x-coordinates from the left edge and horizontal guide positions are y-coordinates from the top edge, in composition pixels.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: true },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          const guidesAreVisible = editorShowGuidesRef.current;
          return Promise.resolve({
            currentContent: currentContentRef.current,
            guidesVisible: guidesAreVisible,
            guides: compositionId === null ? [] : guidesListRef.current.filter((guide) => guide.compositionId === compositionId).map((guide) => ({
              id: guide.id,
              orientation: guide.orientation,
              position: guide.position,
              visible: guidesAreVisible && guide.show
            }))
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "set_guides_visible",
        title: "Set Studio guides visibility",
        description: "Show or hide all guides for the current Remotion Studio composition.",
        inputSchema: {
          type: "object",
          properties: {
            visible: {
              type: "boolean",
              description: "Whether guides should be visible."
            }
          },
          required: ["visible"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ visible }) => {
          if (typeof visible !== "boolean") {
            throw new Error("visible must be a boolean.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          editorShowGuidesRef.current = visible;
          setEditorShowGuides(() => visible);
          return Promise.resolve({
            currentContent: currentContentRef.current,
            guidesVisible: visible
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "add_guide",
        title: "Add Studio guide",
        description: "Add a guide to the current Remotion Studio composition. A vertical guide position is an x-coordinate from the left edge and a horizontal guide position is a y-coordinate from the top edge, in composition pixels.",
        inputSchema: {
          type: "object",
          properties: {
            orientation: {
              type: "string",
              enum: ["horizontal", "vertical"],
              description: "The orientation of the guide."
            },
            position: {
              type: "number",
              description: "The guide position in composition pixels. Vertical guides use an x-coordinate and horizontal guides use a y-coordinate."
            }
          },
          required: ["orientation", "position"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ orientation, position }) => {
          if (orientation !== "horizontal" && orientation !== "vertical") {
            throw new Error('orientation must be either "horizontal" or "vertical".');
          }
          if (typeof position !== "number" || !Number.isFinite(position)) {
            throw new Error("position must be a finite number.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          const guide = {
            id: crypto.randomUUID(),
            orientation,
            position,
            show: true,
            compositionId
          };
          const nextGuides = [...guidesListRef.current, guide];
          guidesListRef.current = nextGuides;
          setGuidesList(() => nextGuides);
          persistGuidesList(nextGuides);
          editorShowGuidesRef.current = true;
          setEditorShowGuides(() => true);
          return Promise.resolve({
            currentContent: currentContentRef.current,
            guide: {
              id: guide.id,
              orientation: guide.orientation,
              position: guide.position,
              visible: true
            }
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "remove_guide",
        title: "Remove Studio guide",
        description: "Remove a guide from the current Remotion Studio composition by its guide ID.",
        inputSchema: {
          type: "object",
          properties: {
            guideId: {
              type: "string",
              minLength: 1,
              description: "The guide ID returned by get_guides or add_guide."
            }
          },
          required: ["guideId"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ guideId }) => {
          if (typeof guideId !== "string" || guideId.length === 0) {
            throw new Error("guideId must be a non-empty string.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          const guideExists = guidesListRef.current.some((guide) => guide.id === guideId && guide.compositionId === compositionId);
          if (!guideExists) {
            throw new Error(`Guide ${guideId} not found in the current composition.`);
          }
          const nextGuides = guidesListRef.current.filter((guide) => guide.id !== guideId || guide.compositionId !== compositionId);
          guidesListRef.current = nextGuides;
          setGuidesList(() => nextGuides);
          persistGuidesList(nextGuides);
          const removedGuideWasSelected = timelineSelectionRef.current.selectedItems.some((item) => item.type === "guide" && item.guideId === guideId);
          if (removedGuideWasSelected) {
            timelineSelectionRef.current.clearSelection();
          }
          return Promise.resolve({
            currentContent: currentContentRef.current,
            guideId,
            removed: true
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "play",
        title: "Play Studio composition",
        description: "Start playing the current Remotion Studio composition from the current frame.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          if (getCurrentDuration() <= 1) {
            throw new Error("The current composition is a still and cannot play.");
          }
          if (Internals91.timeValueRef.current === null) {
            throw new Error("Studio playback controls are not ready.");
          }
          play();
          return Promise.resolve({
            currentContent: currentContentRef.current,
            playing: true
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "pause",
        title: "Pause Studio composition",
        description: "Pause the current Remotion Studio composition at the current frame.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          if (Internals91.timeValueRef.current === null) {
            throw new Error("Studio playback controls are not ready.");
          }
          pause();
          return Promise.resolve({
            currentContent: currentContentRef.current,
            playing: false
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "mute",
        title: "Mute Studio composition",
        description: "Mute audio playback for the current Remotion Studio composition.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          setPlayerMuted(true);
          persistMuteOption(true);
          return Promise.resolve({
            currentContent: currentContentRef.current,
            muted: true
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "unmute",
        title: "Unmute Studio composition",
        description: "Unmute audio playback for the current Remotion Studio composition.",
        inputSchema: {
          type: "object",
          properties: {},
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: () => {
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          setPlayerMuted(false);
          persistMuteOption(false);
          return Promise.resolve({
            currentContent: currentContentRef.current,
            muted: false
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "set_timeline_zoom",
        title: "Set Studio timeline zoom",
        description: "Set the timeline zoom for the current Remotion Studio composition. Use 0 for fully zoomed out and 1 for the maximum zoom supported by the composition duration.",
        inputSchema: {
          type: "object",
          properties: {
            zoom: {
              type: "number",
              minimum: 0,
              maximum: 1,
              description: "The normalized timeline zoom, from 0 (fully zoomed out) to 1 (maximum zoom)."
            }
          },
          required: ["zoom"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ zoom }) => {
          if (typeof zoom !== "number" || !Number.isFinite(zoom) || zoom < 0 || zoom > 1) {
            throw new Error("zoom must be a finite number between 0 and 1.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          const durationInFrames = getCurrentDuration();
          if (durationInFrames <= 1) {
            throw new Error("The current composition is a still and has no timeline zoom.");
          }
          const timelineViewportWidth = scrollableRef.current?.clientWidth ?? 0;
          const minZoom = getTimelineMinZoom({
            durationInFrames,
            timelineViewportWidth
          });
          const timelineZoom = clampTimelineZoom({
            zoom: normalizedToTimelineZoom({
              normalized: zoom,
              minZoom
            }),
            durationInFrames,
            timelineViewportWidth
          });
          setTimelineZoom(compositionId, () => timelineZoom, {
            anchorFrame: null,
            anchorContentX: null
          });
          return Promise.resolve({
            currentContent: currentContentRef.current,
            timelineZoom: timelineZoomToNormalized({
              zoom: timelineZoom,
              minZoom
            })
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "set_playback_rate",
        title: "Set Studio playback rate",
        description: "Set the playback rate for the current Remotion Studio composition. Negative values play the composition backwards.",
        inputSchema: {
          type: "object",
          properties: {
            playbackRate: {
              type: "number",
              enum: commonPlaybackRates,
              description: "The playback multiplier. Negative values play backwards."
            }
          },
          required: ["playbackRate"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ playbackRate }) => {
          if (typeof playbackRate !== "number" || !commonPlaybackRates.includes(playbackRate)) {
            throw new Error(`playbackRate must be one of: ${commonPlaybackRates.join(", ")}.`);
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          setPlaybackRate(() => playbackRate);
          persistPlaybackRate(playbackRate);
          return Promise.resolve({
            currentContent: currentContentRef.current,
            playbackRate
          });
        }
      }, { signal: controller.signal }),
      modelContext.registerTool({
        name: "seek_to_frame",
        title: "Seek Studio timeline",
        description: "Seek the current Remotion Studio composition to a frame. Frames past the end of the composition are clamped to the final frame.",
        inputSchema: {
          type: "object",
          properties: {
            frame: {
              type: "integer",
              minimum: 0,
              description: "The zero-based frame to seek to."
            }
          },
          required: ["frame"],
          additionalProperties: false
        },
        annotations: { readOnlyHint: false },
        execute: ({ frame: frame2 }) => {
          if (typeof frame2 !== "number" || !Number.isInteger(frame2)) {
            throw new Error("frame must be an integer.");
          }
          const compositionId = currentCompositionRef.current;
          if (compositionId === null) {
            throw new Error("No composition is currently selected.");
          }
          const currentFrame = Math.min(Math.max(0, frame2), getCurrentDuration() - 1);
          seek(currentFrame);
          return Promise.resolve({
            currentFrame,
            currentContent: currentContentRef.current
          });
        }
      }, { signal: controller.signal })
    ]).catch(() => {
      return;
    });
    return () => {
      controller.abort();
    };
  }, [
    addCaptionJob,
    addVideoMattingJob,
    isPlaying,
    selectComposition,
    setEditorShowGuides,
    setGuidesList,
    setPlaybackRate,
    setPlayerMuted,
    setTimelineZoom,
    timelineSelectionRef
  ]);
  return /* @__PURE__ */ jsx150(WebMcpSelectionSync, {
    currentSelectionRef,
    selectedSequenceRef
  });
};

// src/components/EditorContent.tsx
import { jsx as jsx151, jsxs as jsxs79 } from "react/jsx-runtime";
var noop3 = () => {
  return;
};
var container25 = {
  display: "flex",
  flexDirection: "column",
  flex: 1,
  height: 0
};
var StudioClearSelectionArea = ({ children }) => {
  const { clearSelection } = useTimelineSelection();
  const onPointerDown = useCallback74((e) => {
    if (!shouldClearSelectionOnPointerDown(e)) {
      return;
    }
    clearSelection();
  }, [clearSelection]);
  return /* @__PURE__ */ jsx151("div", {
    style: container25,
    onPointerDown,
    children
  });
};
var EditorContent = ({ readOnlyStudio, children }) => {
  const { canvasContent, currentAssetMetadata } = useContext95(Internals93.CompositionManager);
  const showTimeline = canvasContent !== null && (canvasContent.type === "composition" || canvasContent.type === "asset" && currentAssetMetadata?.asset === canvasContent.asset);
  const content3 = /* @__PURE__ */ jsxs79(SplitterContainer, {
    orientation: "horizontal",
    id: "top-to-bottom",
    maxFlex: 0.9,
    minFlex: 0.2,
    defaultFlex: 0.75,
    maxFlexerSize: null,
    minFlexerSize: null,
    maxAntiFlexerSize: null,
    minAntiFlexerSize: null,
    children: [
      /* @__PURE__ */ jsx151(SplitterElement, {
        sticky: null,
        type: "flexer",
        children
      }),
      /* @__PURE__ */ jsx151(SplitterHandle, {
        allowToCollapse: "none",
        onCollapse: noop3,
        onCollapseDuringDrag: null
      }),
      /* @__PURE__ */ jsx151(SplitterElement, {
        sticky: null,
        type: "anti-flexer",
        children: showTimeline ? /* @__PURE__ */ jsx151(Timeline, {}) : /* @__PURE__ */ jsx151(TimelineEmptyState, {})
      })
    ]
  });
  return /* @__PURE__ */ jsxs79(TimelineSelectionProvider, {
    children: [
      /* @__PURE__ */ jsx151(WebMcp, {}),
      /* @__PURE__ */ jsx151(Transform3DModeStateProvider, {
        children: /* @__PURE__ */ jsxs79(StudioClearSelectionArea, {
          children: [
            /* @__PURE__ */ jsx151(InitialCompositionLoader, {}),
            /* @__PURE__ */ jsx151(MenuToolbar, {
              readOnlyStudio
            }),
            /* @__PURE__ */ jsx151(GlobalKeybindings, {}),
            /* @__PURE__ */ jsx151(TimelineKeyframeDragStateProvider, {
              children: content3
            })
          ]
        })
      })
    ]
  });
};

// src/components/Modals.tsx
import { useContext as useContext124, useEffect as useEffect88 } from "react";

// src/components/AssetSelectorModal.tsx
import {
  useCallback as useCallback77,
  useContext as useContext98,
  useEffect as useEffect64,
  useMemo as useMemo90,
  useState as useState58
} from "react";

// src/components/QuickSwitcher/QuickSwitcherContent.tsx
import {
  useCallback as useCallback76,
  useContext as useContext97,
  useEffect as useEffect63,
  useMemo as useMemo89,
  useRef as useRef51,
  useState as useState57
} from "react";
import { Internals as Internals95 } from "remotion";

// src/components/QuickSwitcher/algolia-search.ts
import { NoReactInternals as NoReactInternals14 } from "remotion/no-react";
var ALGOLIA_API_KEY = "3e42dbd4f895fe93ff5cf40d860c4a85";
var ALGOLIA_APPLICATION_ID = "PLSDUOL1CA";
var AGOLIA_SEARCH_URL = "https://plsduol1ca-dsn.algolia.net/1/indexes/*/queries";
var algoliaSearch = async (query) => {
  const url = new URL(AGOLIA_SEARCH_URL);
  url.searchParams.set("x-algolia-agen", encodeURIComponent("Remotion Studio DocSearch"));
  url.searchParams.set("x-algolia-api-key", ALGOLIA_API_KEY);
  url.searchParams.set("x-algolia-application-id", ALGOLIA_APPLICATION_ID);
  const { results } = await fetch(url.toString(), {
    headers: {
      "content-type": "application/x-www-form-urlencoded"
    },
    body: JSON.stringify({
      requests: [
        {
          query,
          indexName: "remotion",
          params: 'attributesToRetrieve=["hierarchy.lvl0","hierarchy.lvl1","hierarchy.lvl2","hierarchy.lvl3","hierarchy.lvl4","hierarchy.lvl5","hierarchy.lvl6","content","type","url"]&attributesToSnippet=["hierarchy.lvl1:10","hierarchy.lvl2:10","hierarchy.lvl3:10","hierarchy.lvl4:10","hierarchy.lvl5:10","hierarchy.lvl6:10","content:10"]&hitsPerPage=20'
        }
      ]
    }),
    method: "POST"
  }).then((res) => res.json());
  const { hits } = results[0];
  return hits.map((hit) => {
    const entries2 = Object.values(hit._highlightResult.hierarchy);
    const result = entries2.find((value) => value.matchLevel === "full") ?? entries2.find((value) => value.matchLevel === "partial");
    const { subtitle, title: title2 } = splitMatchIntoTitleAndSubtitle(hit);
    if (!result) {
      return null;
    }
    return {
      type: "search-result",
      id: hit.objectID,
      title: "Should not display",
      titleLine: title2,
      subtitleLine: subtitle,
      onSelected: () => {
        window.open(hit.url);
      }
    };
  }).filter(NoReactInternals14.truthy);
};
var splitMatchIntoTitleAndSubtitle = (match) => {
  const main = match.type === "content" ? match._highlightResult.content : match._highlightResult.hierarchy[match.type];
  const title2 = main.value;
  const subtitle = Object.entries(match._highlightResult.hierarchy).filter(([level]) => level !== match.type).map((value) => value[1].value).join(" • ");
  return { title: title2, subtitle };
};

// src/components/QuickSwitcher/asset-search.ts
var typeFilterRegex = /(^|\s)type:([^\s]+)/gi;
var filterAssetsByType = ({
  assets,
  query
}) => {
  const typeFilters = [];
  const queryWithoutTypeFilters = query.replace(typeFilterRegex, (_match, whitespace, type) => {
    typeFilters.push(type.toLowerCase());
    return whitespace;
  }).replace(/\s+/g, " ").trim();
  if (typeFilters.length === 0) {
    return {
      assets,
      query: queryWithoutTypeFilters
    };
  }
  return {
    assets: assets.filter((asset) => {
      return typeFilters.includes(getPreviewFileType(asset.name));
    }),
    query: queryWithoutTypeFilters
  };
};

// src/components/QuickSwitcher/composition-search.ts
var normalizeSearchText = (text2) => {
  return text2.trim().toLowerCase().replace(/[\s/-]/g, "");
};
var getMatchScore = (query, candidate) => {
  const normalizedCandidate = normalizeSearchText(candidate);
  if (normalizedCandidate === query) {
    return 400000 - normalizedCandidate.length;
  }
  if (normalizedCandidate.startsWith(query)) {
    return 300000 - (normalizedCandidate.length - query.length);
  }
  const substringIndex = normalizedCandidate.indexOf(query);
  if (substringIndex !== -1) {
    return 200000 - substringIndex * 100 - (normalizedCandidate.length - query.length);
  }
  let previousIndex = -1;
  let firstIndex = -1;
  let gapCount = 0;
  for (const character of query) {
    const nextIndex = normalizedCandidate.indexOf(character, previousIndex + 1);
    if (nextIndex === -1) {
      return null;
    }
    if (firstIndex === -1) {
      firstIndex = nextIndex;
    } else {
      gapCount += nextIndex - previousIndex - 1;
    }
    previousIndex = nextIndex;
  }
  return 1e5 - gapCount * 100 - firstIndex * 10 - (normalizedCandidate.length - query.length);
};
var searchCompositionTree = ({
  items,
  query
}) => {
  const normalizedQuery = normalizeSearchText(query);
  const filterAndRank = (treeItems, level, inheritedFolderScore) => {
    const rankedItems = treeItems.map((item, originalIndex) => {
      if (item.type === "composition") {
        const compositionPath = [
          item.composition.parentFolderName,
          item.composition.folderName,
          item.composition.id
        ].filter(Boolean).join("/");
        const idScore = normalizedQuery === "" ? 0 : getMatchScore(normalizedQuery, item.composition.id);
        const compositionPathScore = normalizedQuery === "" ? 0 : getMatchScore(normalizedQuery, compositionPath);
        const directScore = Math.max(idScore ?? -1, compositionPathScore ?? -1);
        const score = Math.max(directScore, inheritedFolderScore ?? -1);
        if (score === -1) {
          return null;
        }
        return {
          result: {
            type: "composition",
            composition: item.composition,
            level,
            score
          },
          originalIndex
        };
      }
      const fullPath = [item.parentName, item.folderName].filter(Boolean).join("/");
      const nameScore = normalizedQuery === "" ? 0 : getMatchScore(normalizedQuery, item.folderName);
      const pathScore = normalizedQuery === "" ? 0 : getMatchScore(normalizedQuery, fullPath);
      const directFolderScore = Math.max(nameScore ?? -1, pathScore ?? -1);
      const folderScore = Math.max(directFolderScore === -1 ? -1 : directFolderScore - 1, inheritedFolderScore ?? -1);
      const children = filterAndRank(item.items, level + 1, folderScore === -1 ? null : folderScore);
      if (children.length === 0) {
        return null;
      }
      return {
        result: {
          type: "folder",
          id: `folder-${fullPath}`,
          title: item.folderName,
          level,
          score: Math.max(folderScore, ...children.map((child) => child.score)),
          children
        },
        originalIndex
      };
    }).filter((item) => item !== null);
    if (normalizedQuery !== "") {
      rankedItems.sort((a, b) => {
        return b.result.score - a.result.score || a.originalIndex - b.originalIndex;
      });
    }
    return rankedItems.map((item) => item.result);
  };
  const flatten = (rankedItems) => {
    return rankedItems.flatMap((item) => {
      if (item.type === "composition") {
        return [
          {
            type: "composition",
            composition: item.composition,
            level: item.level
          }
        ];
      }
      return [
        {
          type: "folder",
          id: item.id,
          title: item.title,
          level: item.level
        },
        ...flatten(item.children)
      ];
    });
  };
  return flatten(filterAndRank(items, 0, null));
};

// src/components/QuickSwitcher/fuzzy-search.ts
function fuzzySearch(query, dataset) {
  const q = query.trim().toLowerCase().replace(/[\s-]/g, "");
  const matchingIndices = [];
  if (q.length === 0) {
    for (let i = 0;i < dataset.length; i++) {
      matchingIndices.push(i);
    }
    return dataset.filter((_, i) => matchingIndices.includes(i));
  }
  dataset.forEach((d, index) => {
    const s = d.title.trim().toLowerCase();
    let i = 0;
    let n = -1;
    let l;
    for (;l = q[i++]; )
      if (!~(n = s.indexOf(l, n + 1)))
        return;
    matchingIndices.push(index);
  });
  return dataset.filter((_, i) => matchingIndices.includes(i));
}

// src/components/QuickSwitcher/NoResults.tsx
import { jsxs as jsxs80 } from "react/jsx-runtime";
var container26 = {
  padding: 80,
  color: LIGHT_TEXT,
  textAlign: "center",
  fontSize: 14
};
var MODE_TO_STRING = {
  assets: "assets",
  commands: "commands",
  compositions: "compositions",
  docs: "documentation"
};
var QuickSwitcherNoResults = ({ query, mode }) => {
  return /* @__PURE__ */ jsxs80("div", {
    style: container26,
    children: [
      "No ",
      MODE_TO_STRING[mode],
      " matching ",
      '"',
      query,
      '"'
    ]
  });
};

// src/components/QuickSwitcher/QuickSwitcherResult.tsx
import {
  useCallback as useCallback75,
  useContext as useContext96,
  useEffect as useEffect62,
  useMemo as useMemo88,
  useRef as useRef50,
  useState as useState56
} from "react";

// src/components/QuickSwitcher/shared.ts
import { useEffect as useEffect61 } from "react";
var QUICK_SWITCHER_RESULT_LABEL_FONT_SIZE = 15;
var loopIndex = (index, length) => {
  if (index < 0) {
    index += length;
  }
  return index % length;
};
var useScrollIntoViewOnSelected = (ref, selected) => {
  useEffect61(() => {
    if (selected) {
      ref.current?.scrollIntoView({
        block: "nearest",
        inline: "center"
      });
    }
  }, [ref, selected]);
};

// src/components/QuickSwitcher/QuickSwitcherResult.tsx
import { jsx as jsx152, jsxs as jsxs81, Fragment as Fragment37 } from "react/jsx-runtime";
var isQuickSwitcherResultSelectable = (result) => {
  return result.type !== "folder";
};
var container27 = {
  paddingLeft: 16,
  paddingRight: 16,
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  cursor: "default",
  marginBottom: 1,
  marginLeft: 4,
  marginRight: 4,
  borderRadius: 4
};
var label7 = {
  whiteSpace: "nowrap",
  textOverflow: "ellipsis"
};
var searchLabel = {
  ...label7,
  lineHeight: 1.25
};
var iconStyle6 = {
  width: 18,
  height: 18,
  flexShrink: 0
};
var selectFileIconStyle = {
  ...iconStyle6,
  width: 20,
  height: 20
};
var labelContainer = {
  overflow: "hidden",
  flex: 1,
  paddingTop: 5,
  paddingBottom: 5
};
var QuickSwitcherResult = ({ result, selected }) => {
  const [hovered, setIsHovered] = useState56(false);
  const ref = useRef50(null);
  const keybindings = useKeybinding();
  const onSelected = result.type === "folder" ? null : result.onSelected;
  const composition = result.type === "composition" ? result.composition : null;
  const { setSelectedModal } = useContext96(SetSelectedModalContext);
  const { connectionStatus, openInApps } = useOpenInMenuApps();
  const resolvedLocation = useResolvedStack(composition?.stack ?? null);
  const getContextMenuItems = useCallback75(() => {
    if (composition === null) {
      return [];
    }
    return getCompositionContextMenuItems({
      closeMenu: noop,
      composition,
      connectionStatus,
      includeCompositionManagementItems: true,
      openInApps,
      resolvedLocation,
      setSelectedModal,
      readOnlyStudio: window.remotion_isReadOnlyStudio
    });
  }, [
    composition,
    connectionStatus,
    openInApps,
    resolvedLocation,
    setSelectedModal
  ]);
  useEffect62(() => {
    if (result.type === "folder") {
      return;
    }
    const { current } = ref;
    if (!current) {
      return;
    }
    const onMouseEnter = () => setIsHovered(true);
    const onMouseLeave = () => setIsHovered(false);
    current.addEventListener("mouseenter", onMouseEnter);
    current.addEventListener("mouseleave", onMouseLeave);
    return () => {
      current.removeEventListener("mouseenter", onMouseEnter);
      current.removeEventListener("mouseleave", onMouseLeave);
    };
  }, [result.type]);
  useEffect62(() => {
    if (!selected || onSelected === null) {
      return;
    }
    const binding = keybindings.registerKeybinding({
      key: "Enter",
      callback: onSelected,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      binding.unregister();
    };
  }, [keybindings, onSelected, selected]);
  useScrollIntoViewOnSelected(ref, selected);
  const style6 = useMemo88(() => {
    return {
      ...container27,
      paddingLeft: result.type === "folder" || result.type === "composition" ? 16 + result.level * 8 : container27.paddingLeft,
      backgroundColor: result.type !== "folder" && (hovered || selected) ? WHITE_ALPHA_06 : TRANSPARENT
    };
  }, [hovered, result, selected]);
  const labelStyle3 = useMemo88(() => {
    return {
      ...result.type === "search-result" ? searchLabel : label7,
      color: result.type === "search-result" || result.type === "folder" ? LIGHT_TEXT : selected || hovered ? WHITE : LIGHT_TEXT,
      fontSize: QUICK_SWITCHER_RESULT_LABEL_FONT_SIZE
    };
  }, [hovered, result.type, selected]);
  const row6 = /* @__PURE__ */ jsxs81("div", {
    ref,
    style: style6,
    onClick: onSelected ?? undefined,
    children: [
      result.type === "composition" ? result.compositionType === "still" ? /* @__PURE__ */ jsx152(StillIcon, {
        color: selected || hovered ? WHITE : LIGHT_TEXT,
        style: iconStyle6
      }) : /* @__PURE__ */ jsx152(FilmIcon, {
        color: selected || hovered ? WHITE : LIGHT_TEXT,
        style: iconStyle6
      }) : result.type === "folder" ? /* @__PURE__ */ jsx152(ExpandedFolderIcon, {
        color: LIGHT_TEXT,
        style: iconStyle6
      }) : result.type === "asset" ? /* @__PURE__ */ jsx152(AssetFileIcon, {
        fileType: result.fileType,
        color: selected || hovered ? WHITE : LIGHT_TEXT,
        style: iconStyle6
      }) : result.type === "select-file" ? /* @__PURE__ */ jsx152(UploadIcon, {
        color: selected || hovered ? WHITE : LIGHT_TEXT,
        style: selectFileIconStyle
      }) : null,
      /* @__PURE__ */ jsx152(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx152("div", {
        style: labelContainer,
        children: result.type === "search-result" ? /* @__PURE__ */ jsxs81(Fragment37, {
          children: [
            /* @__PURE__ */ jsx152("div", {
              dangerouslySetInnerHTML: {
                __html: result.titleLine
              },
              style: labelStyle3
            }),
            /* @__PURE__ */ jsx152("div", {
              dangerouslySetInnerHTML: {
                __html: result.subtitleLine
              },
              style: labelStyle3
            })
          ]
        }) : /* @__PURE__ */ jsx152("div", {
          style: labelStyle3,
          children: result.title
        })
      })
    ]
  }, result.id);
  if (composition === null) {
    return row6;
  }
  return /* @__PURE__ */ jsx152(ContextMenu, {
    getItems: getContextMenuItems,
    children: row6
  });
};

// src/components/QuickSwitcher/QuickSwitcherContent.tsx
import { jsx as jsx153, jsxs as jsxs82 } from "react/jsx-runtime";
var input2 = {
  width: "100%",
  borderRadius: 4
};
var touchscreenInput = {
  ...input2,
  fontSize: 16
};
var modeSelector = {
  paddingLeft: 16,
  paddingRight: 16,
  display: "flex",
  flexDirection: "row",
  paddingTop: 8,
  paddingBottom: 5
};
var modeItem = {
  appearance: "none",
  border: "none",
  fontFamily: "inherit",
  padding: 0,
  fontSize: 13,
  cursor: "pointer",
  userSelect: "none"
};
var modeInactive = {
  ...modeItem,
  color: LIGHT_TEXT
};
var modeActive = {
  ...modeItem,
  color: WHITE
};
var content3 = {
  paddingLeft: 16,
  paddingRight: 16,
  paddingTop: 4,
  paddingBottom: 10,
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var contentWithoutModeSelector = {
  ...content3,
  paddingTop: 16
};
var container28 = {
  width: 400,
  maxWidth: "calc(100vw - 40px)"
};
var results = {
  overflowY: "auto",
  height: 300
};
var stripQuery = (query) => {
  if (query.startsWith("$") || query.startsWith(">") || query.startsWith("?")) {
    return query.substring(1).trim();
  }
  return query.trim();
};
var mapQueryToMode = (query) => {
  return query.startsWith(">") ? "commands" : query.startsWith("?") ? "docs" : query.startsWith("$") ? "assets" : "compositions";
};
var mapModeToQuery = (mode) => {
  if (mode === "assets") {
    return "$ ";
  }
  if (mode === "commands") {
    return "> ";
  }
  if (mode === "compositions") {
    return "";
  }
  if (mode === "docs") {
    return "? ";
  }
  throw new Error("no mode" + mode);
};
var QuickSwitcherContent = ({
  initialMode,
  invocationTimestamp,
  readOnlyStudio,
  assetSelection,
  compositionSelection
}) => {
  const { compositions, folders } = useContext97(Internals95.CompositionManager);
  const staticFiles = useStaticFiles();
  const [state, setState] = useState57(() => {
    return {
      query: assetSelection === null ? compositionSelection === null ? mapModeToQuery(initialMode) : "" : assetSelection.initialQuery,
      selectedIndex: 0
    };
  });
  useEffect63(() => {
    setState({
      query: assetSelection === null ? compositionSelection === null ? mapModeToQuery(initialMode) : "" : assetSelection.initialQuery,
      selectedIndex: 0
    });
  }, [assetSelection, compositionSelection, initialMode, invocationTimestamp]);
  const inputRef = useRef51(null);
  const isTouchscreen = navigator.maxTouchPoints > 0;
  const selectComposition = useSelectComposition();
  const selectAsset = useSelectAsset();
  const closeMenu = useCallback76(() => {
    return;
  }, []);
  const actions = useMenuStructure(closeMenu, readOnlyStudio);
  const [docResults, setDocResults] = useState57({ type: "initial" });
  const { setSelectedModal } = useContext97(SetSelectedModalContext);
  const keybindings = useKeybinding();
  const mode = assetSelection !== null ? "assets" : compositionSelection !== null ? "compositions" : mapQueryToMode(state.query);
  const actualQuery = useMemo89(() => {
    return stripQuery(state.query);
  }, [state.query]);
  const menuActions = useMemo89(() => {
    if (mode !== "commands") {
      return [];
    }
    return makeSearchResults(actions, setSelectedModal);
  }, [actions, mode, setSelectedModal]);
  const assetSearch = useMemo89(() => {
    return filterAssetsByType({
      assets: staticFiles,
      query: actualQuery
    });
  }, [actualQuery, staticFiles]);
  const resultsArray = useMemo89(() => {
    if (mode === "commands") {
      return fuzzySearch(actualQuery, menuActions);
    }
    if (mode === "docs") {
      return docResults.type === "results" ? docResults.results : [];
    }
    if (mode === "assets") {
      const assetResults = fuzzySearch(assetSearch.query, assetSearch.assets.map((asset) => {
        return {
          id: "asset-" + asset.name,
          title: asset.name,
          type: "asset",
          fileType: getPreviewFileType(asset.name),
          onSelected: () => {
            if (assetSelection !== null) {
              assetSelection.onSelected(asset);
            } else {
              selectAsset(asset.name);
              pushUrl(`/assets/${asset.name}`);
            }
            setSelectedModal(null);
          }
        };
      }));
      if (assetSelection === null) {
        return assetResults;
      }
      return [
        {
          id: "select-file",
          title: "Import file...",
          type: "select-file",
          onSelected: () => {
            assetSelection.onSelectFile();
            setSelectedModal(null);
          }
        },
        ...assetResults
      ];
    }
    const tree = createFolderTree(compositions.filter((composition) => composition.id !== compositionSelection?.excludeCompositionId), folders, {});
    return searchCompositionTree({ items: tree, query: actualQuery }).map((result) => {
      if (result.type === "folder") {
        return result;
      }
      const { composition } = result;
      return {
        id: "composition-" + composition.id,
        title: composition.id,
        type: "composition",
        composition,
        level: result.level,
        onSelected: () => {
          if (compositionSelection !== null) {
            compositionSelection.onSelected(composition);
            setSelectedModal(null);
            return;
          }
          selectComposition(composition, true);
          setSelectedModal(null);
          const selector = `.__remotion-composition[data-compname="${composition.id}"]`;
          Internals95.compositionSelectorRef.current?.expandComposition(composition.id);
          waitForElm(selector).then(() => {
            document.querySelector(selector)?.scrollIntoView({ block: "center" });
          });
        },
        compositionType: isCompositionStill(composition) ? "still" : "composition"
      };
    });
  }, [
    mode,
    actualQuery,
    compositions,
    folders,
    menuActions,
    docResults,
    assetSearch,
    assetSelection,
    compositionSelection,
    selectAsset,
    selectComposition,
    setSelectedModal
  ]);
  const onArrowDown = useCallback76(() => {
    setState((s) => {
      return {
        ...s,
        selectedIndex: s.selectedIndex + 1
      };
    });
  }, []);
  const onArrowUp = useCallback76(() => {
    setState((s) => {
      return {
        ...s,
        selectedIndex: s.selectedIndex - 1
      };
    });
  }, []);
  useEffect63(() => {
    const binding = keybindings.registerKeybinding({
      key: "ArrowUp",
      callback: onArrowUp,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      binding.unregister();
    };
  }, [keybindings, onArrowDown, onArrowUp]);
  useEffect63(() => {
    if (mode !== "docs") {
      return;
    }
    if (actualQuery.trim() === "") {
      setDocResults({ type: "initial" });
      return;
    }
    let cancelled = false;
    setDocResults({ type: "loading" });
    algoliaSearch(actualQuery).then((agoliaResults) => {
      if (cancelled) {
        return;
      }
      setDocResults({ type: "results", results: agoliaResults });
    }).catch((err) => {
      if (cancelled) {
        return;
      }
      setDocResults({ type: "error", error: err });
    });
    return () => {
      cancelled = true;
    };
  }, [actualQuery, mode]);
  useEffect63(() => {
    const binding = keybindings.registerKeybinding({
      key: "ArrowDown",
      callback: onArrowDown,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      binding.unregister();
    };
  }, [keybindings, onArrowDown]);
  const onTextChange = useCallback76((e) => {
    setState({ query: e.target.value, selectedIndex: 0 });
  }, []);
  const selectableResults = resultsArray.filter(isQuickSwitcherResultSelectable);
  const selectedIndexRounded = loopIndex(state.selectedIndex, selectableResults.length);
  const selectedResultId = selectableResults[selectedIndexRounded]?.id ?? null;
  const focusInput = useCallback76(() => {
    if (!isTouchscreen) {
      inputRef.current?.focus();
    }
  }, [isTouchscreen]);
  const onActionsSelected = useCallback76(() => {
    setState((s) => ({
      query: `> ${stripQuery(s.query)}`,
      selectedIndex: 0
    }));
    focusInput();
  }, [focusInput]);
  const onCompositionsSelected = useCallback76(() => {
    setState((s) => ({
      query: stripQuery(s.query),
      selectedIndex: 0
    }));
    focusInput();
  }, [focusInput]);
  const onAssetsSelected = useCallback76(() => {
    setState((s) => ({
      query: `$ ${stripQuery(s.query)}`,
      selectedIndex: 0
    }));
    focusInput();
  }, [focusInput]);
  const onDocSearchSelected = useCallback76(() => {
    setState((s) => ({
      query: `? ${stripQuery(s.query)}`,
      selectedIndex: 0
    }));
    setDocResults({ type: "initial" });
    focusInput();
  }, [focusInput]);
  const showSearchLoadingState = mode === "docs" && docResults.type === "loading";
  const showEmptyDocSearch = mode === "docs" && actualQuery.trim() === "";
  const placeholder = mode === "assets" ? "Search assets..." : mode === "commands" ? "Search actions..." : mode === "docs" ? "Search documentation..." : "Search compositions...";
  return /* @__PURE__ */ jsxs82("div", {
    style: container28,
    children: [
      assetSelection === null && compositionSelection === null && /* @__PURE__ */ jsxs82("div", {
        style: modeSelector,
        children: [
          /* @__PURE__ */ jsx153("button", {
            onClick: onCompositionsSelected,
            style: mode === "compositions" ? modeActive : modeInactive,
            type: "button",
            children: "Compositions"
          }),
          /* @__PURE__ */ jsx153(Spacing, {
            x: 1
          }),
          /* @__PURE__ */ jsx153("button", {
            onClick: onAssetsSelected,
            style: mode === "assets" ? modeActive : modeInactive,
            type: "button",
            children: "Assets"
          }),
          /* @__PURE__ */ jsx153(Spacing, {
            x: 1
          }),
          /* @__PURE__ */ jsx153("button", {
            onClick: onActionsSelected,
            style: mode === "commands" ? modeActive : modeInactive,
            type: "button",
            children: "Actions"
          }),
          /* @__PURE__ */ jsx153(Spacing, {
            x: 1
          }),
          /* @__PURE__ */ jsx153("button", {
            onClick: onDocSearchSelected,
            style: mode === "docs" ? modeActive : modeInactive,
            type: "button",
            children: "Documentation"
          })
        ]
      }),
      /* @__PURE__ */ jsx153("div", {
        style: assetSelection === null && compositionSelection === null ? content3 : contentWithoutModeSelector,
        children: /* @__PURE__ */ jsx153(RemotionInput, {
          ref: inputRef,
          type: "text",
          style: isTouchscreen ? touchscreenInput : input2,
          autoFocus: !isTouchscreen,
          status: "ok",
          value: state.query,
          onChange: onTextChange,
          placeholder,
          rightAlign: false
        })
      }),
      /* @__PURE__ */ jsx153("div", {
        style: results,
        className: VERTICAL_SCROLLBAR_CLASSNAME,
        children: showEmptyDocSearch || showSearchLoadingState ? null : resultsArray.length === 0 ? /* @__PURE__ */ jsx153(QuickSwitcherNoResults, {
          mode,
          query: actualQuery
        }) : resultsArray.map((result) => {
          return /* @__PURE__ */ jsx153(QuickSwitcherResult, {
            selected: selectedResultId === result.id,
            result
          }, result.id);
        })
      })
    ]
  });
};
function waitForElm(selector) {
  return new Promise((resolve) => {
    if (document.querySelector(selector)) {
      resolve(document.querySelector(selector));
      return;
    }
    const observer = new MutationObserver(() => {
      if (document.querySelector(selector)) {
        resolve(document.querySelector(selector));
        observer.disconnect();
      }
    });
    observer.observe(document.body, {
      childList: true,
      subtree: true
    });
  });
}

// src/components/AssetSelectorModal.tsx
import { jsx as jsx154, jsxs as jsxs83 } from "react/jsx-runtime";
var panel = {
  maxWidth: "calc(100vw - 40px)",
  overflow: "hidden",
  width: 400
};
var sourceSelector = {
  display: "flex",
  padding: "12px 16px 0"
};
var urlContent = {
  boxSizing: "border-box",
  minHeight: 300,
  padding: 16
};
var compactUrlContent = {
  boxSizing: "border-box",
  padding: 16
};
var label8 = {
  color: LIGHT_TEXT,
  display: "block",
  fontFamily: "sans-serif",
  fontSize: 12,
  marginBottom: 8
};
var previewRow = {
  alignItems: "center",
  display: "flex",
  gap: 16,
  minHeight: 112,
  paddingTop: 16
};
var previewImage = {
  display: "block",
  height: 96,
  objectFit: "contain",
  width: 96
};
var previewStatus = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 12,
  lineHeight: 1.5
};
var footer = {
  display: "flex",
  minWidth: 0,
  width: "100%"
};
var isHttpUrl2 = (value) => {
  try {
    const { protocol } = new URL(value.startsWith("//") ? `https:${value}` : value);
    return protocol === "http:" || protocol === "https:";
  } catch {
    return false;
  }
};
var AssetSelectorModal = ({ state, readOnlyStudio }) => {
  const { setSelectedModal } = useContext98(SetSelectedModalContext);
  const [mode, setMode] = useState58(() => state.initialUrl === null ? "project" : "url");
  const [draft, setDraft] = useState58(state.initialUrl ?? "");
  const [previewState, setPreviewState] = useState58(() => state.assetType === "image" && state.initialUrl !== null ? { type: "loading" } : { type: "empty" });
  const close = useCallback77(() => {
    setSelectedModal(null);
  }, [setSelectedModal]);
  useEffect64(() => {
    if (state.assetType !== "image") {
      setPreviewState({ type: "empty" });
      return;
    }
    const value = draft.trim();
    if (value === "") {
      setPreviewState({ type: "empty" });
      return;
    }
    if (!isHttpUrl2(value)) {
      setPreviewState({ type: "invalid" });
      return;
    }
    let cancelled = false;
    const image = new Image;
    setPreviewState({ type: "loading" });
    image.onload = () => {
      if (!cancelled) {
        setPreviewState({
          type: "loaded",
          width: image.naturalWidth,
          height: image.naturalHeight
        });
      }
    };
    image.onerror = () => {
      if (!cancelled) {
        setPreviewState({ type: "error" });
      }
    };
    image.src = value.startsWith("//") ? `https:${value}` : value;
    return () => {
      cancelled = true;
      image.onload = null;
      image.onerror = null;
    };
  }, [draft, state.assetType]);
  const items = useMemo90(() => [
    {
      key: "project",
      label: "Project assets",
      onClick: () => setMode("project"),
      selected: mode === "project"
    },
    {
      key: "url",
      label: "URL",
      onClick: () => setMode("url"),
      selected: mode === "url"
    }
  ], [mode]);
  const trimmedDraft = draft.trim();
  const canApply = isHttpUrl2(trimmedDraft);
  const invalidDraft = trimmedDraft !== "" && !canApply;
  const apply = useCallback77(() => {
    if (!canApply) {
      return;
    }
    state.onSelectedUrl(trimmedDraft);
    close();
  }, [canApply, close, state, trimmedDraft]);
  const onSubmit = useCallback77((event) => {
    event.preventDefault();
    apply();
  }, [apply]);
  const assetTypeLabel = state.assetType.charAt(0).toUpperCase() + state.assetType.slice(1);
  return /* @__PURE__ */ jsxs83(ModalContainer, {
    onEscape: close,
    onOutsideClick: close,
    panelStyle: panel,
    children: [
      /* @__PURE__ */ jsx154(ModalHeader, {
        title: `Replace ${state.assetType}`,
        onClose: close
      }),
      /* @__PURE__ */ jsx154("div", {
        style: sourceSelector,
        children: /* @__PURE__ */ jsx154(SegmentedControl, {
          items,
          needsWrapping: false,
          size: "compact"
        })
      }),
      mode === "project" ? /* @__PURE__ */ jsx154(QuickSwitcherContent, {
        assetSelection: state.assetSelection,
        compositionSelection: null,
        initialMode: "assets",
        invocationTimestamp: state.invocationTimestamp,
        readOnlyStudio
      }) : /* @__PURE__ */ jsxs83("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs83("div", {
            style: state.assetType === "image" ? urlContent : compactUrlContent,
            children: [
              /* @__PURE__ */ jsxs83("label", {
                htmlFor: "remotion-asset-url",
                style: label8,
                children: [
                  assetTypeLabel,
                  " URL"
                ]
              }),
              /* @__PURE__ */ jsx154(RemotionInput, {
                id: "remotion-asset-url",
                autoFocus: true,
                onChange: (event) => setDraft(event.target.value),
                placeholder: `https://example.com/${state.assetType}`,
                rightAlign: false,
                status: invalidDraft ? "error" : "ok",
                type: "url",
                value: draft
              }),
              state.assetType === "image" ? /* @__PURE__ */ jsxs83("div", {
                style: previewRow,
                role: "status",
                children: [
                  previewState.type === "loaded" ? /* @__PURE__ */ jsx154("img", {
                    alt: "Image URL preview",
                    src: trimmedDraft.startsWith("//") ? `https:${trimmedDraft}` : trimmedDraft,
                    style: previewImage
                  }) : null,
                  /* @__PURE__ */ jsx154("div", {
                    style: previewState.type === "error" ? { ...previewStatus, color: FAIL_COLOR } : previewStatus,
                    children: previewState.type === "empty" ? "Enter an image URL." : previewState.type === "invalid" ? "Enter an HTTP(S) URL." : previewState.type === "loading" ? "Loading preview…" : previewState.type === "error" ? "Preview unavailable. You can still replace the source." : `${previewState.width} × ${previewState.height}`
                  })
                ]
              }) : null
            ]
          }),
          /* @__PURE__ */ jsx154(ModalFooterContainer, {
            style: footer,
            children: /* @__PURE__ */ jsxs83(Row, {
              align: "center",
              flex: 1,
              justify: "flex-end",
              children: [
                /* @__PURE__ */ jsx154(Button, {
                  onClick: close,
                  children: "Cancel"
                }),
                /* @__PURE__ */ jsx154(Spacing, {
                  x: 1
                }),
                /* @__PURE__ */ jsxs83(ModalButton, {
                  disabled: !canApply,
                  onClick: apply,
                  children: [
                    "Replace ",
                    state.assetType
                  ]
                })
              ]
            })
          })
        ]
      })
    ]
  });
};

// src/components/EffectPickerModal.tsx
import {
  EFFECT_CATALOG,
  getEffectDocumentationLink
} from "@remotion/studio-shared";
import {
  useCallback as useCallback78,
  useContext as useContext99,
  useEffect as useEffect65,
  useMemo as useMemo91,
  useRef as useRef52,
  useState as useState59
} from "react";

// src/icons/effects.tsx
import { jsx as jsx155 } from "react/jsx-runtime";
var EffectsIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx155("svg", {
    ...props,
    viewBox: "0 0 16 16",
    children: /* @__PURE__ */ jsx155("path", {
      fill: color,
      d: "M4.405 4.48C4.575 3.82 4.865 3.325 5.275 2.995C5.695 2.665 6.25 2.5 6.94 2.5H9.235V4.06H7.045C6.555 4.06 6.235 4.3 6.085 4.78L5.83 5.68H7.975V7.255H5.395L3.805 13H2.02L3.625 7.255H1.96V5.68H4.075L4.405 4.48ZM8.57102 9.085L6.87602 5.68H8.79602L9.86102 7.99L11.991 5.68H14.331L10.686 9.415L12.426 13H10.491L9.35102 10.585L7.02602 13H4.68602L8.57102 9.085Z"
    })
  });
};

// src/icons/external-link.tsx
import { jsx as jsx156 } from "react/jsx-runtime";
var ExternalLinkIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx156("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512",
    ...props,
    children: /* @__PURE__ */ jsx156("path", {
      fill: color,
      d: "M304 0c-8.8 0-16 7.2-16 16s7.2 16 16 16l153.4 0-260.7 260.7c-6.2 6.2-6.2 16.4 0 22.6s16.4 6.2 22.6 0L480 54.6 480 208c0 8.8 7.2 16 16 16s16-7.2 16-16l0-192c0-8.8-7.2-16-16-16L304 0zM80 96C35.8 96 0 131.8 0 176L0 432c0 44.2 35.8 80 80 80l256 0c44.2 0 80-35.8 80-80l0-96c0-8.8-7.2-16-16-16s-16 7.2-16 16l0 96c0 26.5-21.5 48-48 48L80 480c-26.5 0-48-21.5-48-48l0-256c0-26.5 21.5-48 48-48l96 0c8.8 0 16-7.2 16-16s-7.2-16-16-16L80 96z"
    })
  });
};

// src/components/effect-picker-search.ts
var normalize2 = (value) => value.trim().toLowerCase();
var compact = (value) => normalize2(value).replace(/[^a-z0-9]/g, "");
var fuzzyMatches = (query, value) => {
  let index = -1;
  for (const character of query) {
    index = value.indexOf(character, index + 1);
    if (index === -1) {
      return false;
    }
  }
  return true;
};
var getMatchScore2 = (item, query) => {
  const normalizedQuery = normalize2(query);
  const compactQuery = compact(query);
  if (normalizedQuery.length === 0) {
    return 0;
  }
  const label9 = normalize2(item.label);
  const name = normalize2(item.effect.name);
  const primary = [label9, name];
  const compactPrimary = primary.map(compact);
  if (primary.some((field) => field === normalizedQuery)) {
    return 0;
  }
  if (compactQuery.length > 0) {
    if (compactPrimary.some((field) => field === compactQuery)) {
      return 1;
    }
    if (compactPrimary.some((field) => field.startsWith(compactQuery))) {
      return 2;
    }
  }
  if (primary.some((field) => field.startsWith(normalizedQuery))) {
    return 2;
  }
  if (compactQuery.length > 0) {
    if (compactPrimary.some((field) => field.includes(compactQuery))) {
      return 3;
    }
  }
  if (primary.some((field) => field.includes(normalizedQuery))) {
    return 3;
  }
  const importPath = normalize2(item.effect.importPath);
  if (importPath.includes(normalizedQuery) || compactQuery.length > 0 && compact(importPath).includes(compactQuery)) {
    return 4;
  }
  const secondary = [normalize2(item.category), normalize2(item.description)];
  const compactSecondary = secondary.map(compact);
  if (secondary.some((field) => field.includes(normalizedQuery)) || compactQuery.length > 0 && compactSecondary.some((field) => field.includes(compactQuery))) {
    return 5;
  }
  if (compactQuery.length > 0 && compactPrimary.some((field) => fuzzyMatches(compactQuery, field))) {
    return 6;
  }
  return null;
};
var filterEffectCatalog = ({
  items,
  query
}) => {
  return items.map((item, index) => ({
    item,
    index,
    score: getMatchScore2(item, query)
  })).filter((candidate) => candidate.score !== null).sort((a, b) => a.score - b.score || a.index - b.index).map(({ item }) => item);
};

// src/components/EffectPickerModal.tsx
import { jsx as jsx157, jsxs as jsxs84 } from "react/jsx-runtime";
var container29 = {
  width: 400
};
var panelStyle2 = {
  borderRadius: 6,
  overflow: "hidden"
};
var content4 = {
  padding: "12px 16px 10px"
};
var inputStyle = {
  width: "100%",
  borderRadius: 4
};
var aboutEffectsRow = {
  display: "flex",
  justifyContent: "flex-end",
  marginBottom: 8
};
var aboutEffectsLink = {
  alignItems: "center",
  color: LIGHT_TEXT,
  cursor: "default",
  display: "inline-flex",
  fontFamily: "sans-serif",
  fontSize: 12,
  gap: 4,
  lineHeight: "14px",
  minWidth: 0,
  textDecoration: "none"
};
var aboutEffectsLinkHovered = {
  ...aboutEffectsLink,
  color: WHITE
};
var aboutEffectsLabel = {
  color: "inherit",
  fontFamily: "sans-serif",
  fontSize: 12,
  lineHeight: "14px",
  minWidth: 0,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var aboutEffectsIcon = {
  flexShrink: 0,
  height: 12,
  width: 12
};
var resultList = {
  height: 320,
  overflowY: "auto",
  paddingBottom: 10
};
var noResults = {
  color: LIGHT_TEXT,
  fontSize: 13,
  padding: "12px 16px"
};
var resultContainer = {
  cursor: "default",
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  paddingLeft: 16,
  paddingRight: 16,
  marginBottom: 1,
  marginLeft: 4,
  marginRight: 4,
  borderRadius: 4
};
var iconStyle7 = {
  width: 18,
  height: 18,
  flexShrink: 0
};
var labelContainer2 = {
  flex: 1,
  minWidth: 0,
  overflow: "hidden",
  paddingTop: 5,
  paddingBottom: 5
};
var label9 = {
  fontSize: QUICK_SWITCHER_RESULT_LABEL_FONT_SIZE,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var category = {
  color: LIGHT_TEXT,
  fontSize: 11,
  flexShrink: 0
};
var EffectPickerResult = ({ item, selected, onSelected }) => {
  const [hovered, setHovered] = useState59(false);
  const ref = useRef52(null);
  useScrollIntoViewOnSelected(ref, selected);
  const style6 = useMemo91(() => {
    return {
      ...resultContainer,
      backgroundColor: hovered || selected ? WHITE_ALPHA_06 : TRANSPARENT
    };
  }, [hovered, selected]);
  const labelStyle3 = useMemo91(() => {
    return {
      ...label9,
      color: selected || hovered ? WHITE : LIGHT_TEXT
    };
  }, [hovered, selected]);
  const onClick = useCallback78(() => {
    onSelected(item);
  }, [item, onSelected]);
  const getContextMenuItems = useCallback78(() => {
    const documentationLink = getEffectDocumentationLink(item);
    return [
      {
        type: "item",
        id: `open-${item.id}-docs`,
        keyHint: null,
        label: "Open effect docs",
        leftItem: null,
        disabled: false,
        onClick: () => {
          window.open(documentationLink, "_blank", "noopener,noreferrer");
        },
        quickSwitcherLabel: null,
        subMenu: null,
        value: `open-${item.id}-docs`
      }
    ];
  }, [item]);
  return /* @__PURE__ */ jsx157(ContextMenu, {
    getItems: getContextMenuItems,
    children: /* @__PURE__ */ jsxs84("div", {
      ref,
      style: style6,
      onClick,
      onMouseEnter: () => setHovered(true),
      onMouseLeave: () => setHovered(false),
      children: [
        /* @__PURE__ */ jsx157(EffectsIcon, {
          color: selected || hovered ? WHITE : LIGHT_TEXT,
          style: iconStyle7
        }),
        /* @__PURE__ */ jsx157(Spacing, {
          x: 1
        }),
        /* @__PURE__ */ jsx157("div", {
          style: labelContainer2,
          children: /* @__PURE__ */ jsx157("div", {
            style: labelStyle3,
            children: item.label
          })
        }),
        /* @__PURE__ */ jsx157(Spacing, {
          x: 1
        }),
        /* @__PURE__ */ jsx157("div", {
          style: category,
          children: item.category
        })
      ]
    })
  });
};
var EffectPickerContent = ({ state }) => {
  const { setSelectedModal } = useContext99(SetSelectedModalContext);
  const [aboutEffectsHovered, setAboutEffectsHovered] = useState59(false);
  const [query, setQuery] = useState59("");
  const [selectedIndex, setSelectedIndex] = useState59(0);
  const inputRef = useRef52(null);
  const keybindings = useKeybinding();
  const results2 = useMemo91(() => {
    return filterEffectCatalog({ items: EFFECT_CATALOG, query });
  }, [query]);
  const selectedIndexRounded = results2.length === 0 ? -1 : loopIndex(selectedIndex, results2.length);
  const selectItem = useCallback78((item) => {
    setSelectedModal(null);
    addEffectToSequence({
      clientId: state.clientId,
      effect: item.effect,
      fileName: state.fileName,
      nodePath: state.nodePath
    });
  }, [setSelectedModal, state.clientId, state.fileName, state.nodePath]);
  const onArrowDown = useCallback78(() => {
    setSelectedIndex((i) => i + 1);
  }, []);
  const onArrowUp = useCallback78(() => {
    setSelectedIndex((i) => i - 1);
  }, []);
  const onEnter = useCallback78(() => {
    if (selectedIndexRounded === -1) {
      return;
    }
    selectItem(results2[selectedIndexRounded]);
  }, [results2, selectItem, selectedIndexRounded]);
  useEffect65(() => {
    const downBinding = keybindings.registerKeybinding({
      key: "ArrowDown",
      callback: onArrowDown,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    const upBinding = keybindings.registerKeybinding({
      key: "ArrowUp",
      callback: onArrowUp,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    const enterBinding = keybindings.registerKeybinding({
      key: "Enter",
      callback: onEnter,
      commandCtrlKey: false,
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      downBinding.unregister();
      upBinding.unregister();
      enterBinding.unregister();
    };
  }, [keybindings, onArrowDown, onArrowUp, onEnter]);
  const onTextChange = useCallback78((e) => {
    setQuery(e.target.value);
    setSelectedIndex(0);
  }, []);
  return /* @__PURE__ */ jsxs84("div", {
    style: container29,
    children: [
      /* @__PURE__ */ jsxs84("div", {
        style: content4,
        children: [
          /* @__PURE__ */ jsx157("div", {
            style: aboutEffectsRow,
            children: /* @__PURE__ */ jsxs84("a", {
              href: "https://remotion.dev/effects",
              target: "_blank",
              rel: "noopener noreferrer",
              style: aboutEffectsHovered ? aboutEffectsLinkHovered : aboutEffectsLink,
              onMouseEnter: () => setAboutEffectsHovered(true),
              onMouseLeave: () => setAboutEffectsHovered(false),
              children: [
                /* @__PURE__ */ jsx157("span", {
                  style: aboutEffectsLabel,
                  children: "About effects"
                }),
                /* @__PURE__ */ jsx157(ExternalLinkIcon, {
                  "aria-hidden": "true",
                  color: aboutEffectsHovered ? WHITE : LIGHT_TEXT,
                  style: aboutEffectsIcon
                })
              ]
            })
          }),
          /* @__PURE__ */ jsx157(RemotionInput, {
            ref: inputRef,
            type: "text",
            style: inputStyle,
            autoFocus: true,
            status: "ok",
            value: query,
            onChange: onTextChange,
            placeholder: "Search effects...",
            rightAlign: false
          })
        ]
      }),
      /* @__PURE__ */ jsx157("div", {
        style: resultList,
        className: VERTICAL_SCROLLBAR_CLASSNAME,
        children: results2.length === 0 ? /* @__PURE__ */ jsx157("div", {
          style: noResults,
          children: "No effects found"
        }) : results2.map((item, i) => {
          return /* @__PURE__ */ jsx157(EffectPickerResult, {
            item,
            selected: selectedIndexRounded === i,
            onSelected: selectItem
          }, item.id);
        })
      })
    ]
  });
};
var EffectPickerModal = ({ state }) => {
  return /* @__PURE__ */ jsx157(DismissableModal, {
    panelStyle: panelStyle2,
    children: /* @__PURE__ */ jsx157(EffectPickerContent, {
      state
    })
  });
};

// src/components/ElementInstallConfirmation.tsx
import {
  useCallback as useCallback83,
  useContext as useContext102,
  useEffect as useEffect68,
  useLayoutEffect as useLayoutEffect26,
  useMemo as useMemo95,
  useRef as useRef54,
  useState as useState63
} from "react";
import { createPortal as createPortal2 } from "react-dom";
import { Internals as Internals97 } from "remotion";

// src/helpers/use-create-composition.ts
import { useCallback as useCallback79, useMemo as useMemo92 } from "react";
var toPascalCase = (value) => {
  const words = value.match(/[a-zA-Z0-9]+/g) ?? [];
  const candidate = words.map((word) => `${word.charAt(0).toUpperCase()}${word.slice(1)}`).join("");
  if (!candidate) {
    return "NewComposition";
  }
  if (/^[0-9]/.test(candidate)) {
    return `Composition${candidate}`;
  }
  return candidate;
};
var getUniqueCompositionName = (compositions) => {
  let counter = 1;
  while (true) {
    const name = counter === 1 ? "NewComposition" : `NewComposition${counter}`;
    const err = validateCompositionName(name, compositions);
    if (!err) {
      return name;
    }
    counter++;
  }
};
var useCreateComposition = ({
  compositions,
  durationInFrames,
  folderName,
  newId,
  parentName,
  selectedFrameRate,
  size: size3,
  canvasCapture
}) => {
  const selectComposition = useSelectComposition();
  const compositionId = slugifyName(newId);
  const componentName = useMemo92(() => toPascalCase(compositionId), [compositionId]);
  const nameValidationMessage = useMemo92(() => {
    return compositionId ? validateCompositionName(compositionId, compositions) : "Enter an ID containing letters or numbers.";
  }, [compositionId, compositions]);
  const widthValidationMessage = useMemo92(() => {
    return validateCompositionDimension("Width", size3.width);
  }, [size3.width]);
  const heightValidationMessage = useMemo92(() => {
    return validateCompositionDimension("Height", size3.height);
  }, [size3.height]);
  const codemod = useMemo92(() => {
    return {
      type: "new-composition",
      newDurationInFrames: Number(durationInFrames),
      newFps: Number(selectedFrameRate),
      newHeight: Number(size3.height),
      newWidth: Number(size3.width),
      newId: compositionId,
      componentName,
      componentImportPath: `./${componentName}`,
      folderName,
      parentName,
      canvasCapture: canvasCapture === null ? null : {
        data: canvasCapture.data,
        keyframeFps: Number(selectedFrameRate),
        videoFileName: canvasCapture.videoFileName,
        videoHeight: canvasCapture.videoHeight,
        videoWidth: canvasCapture.videoWidth
      }
    };
  }, [
    canvasCapture,
    componentName,
    compositionId,
    durationInFrames,
    folderName,
    parentName,
    selectedFrameRate,
    size3.height,
    size3.width
  ]);
  const valid = nameValidationMessage === null && widthValidationMessage === null && heightValidationMessage === null;
  const createComposition = useCallback79(async ({
    signal,
    symbolicatedStack
  }) => {
    const result = await applyCodemod({
      codemod,
      dryRun: false,
      signal,
      symbolicatedStack,
      undoRedoNavigation: {
        undoRoute: getRoute(),
        redoRoute: `/${compositionId}`
      }
    });
    if (result.success) {
      selectComposition({
        id: compositionId,
        folderName,
        parentFolderName: parentName
      }, true);
    }
    return result;
  }, [codemod, compositionId, folderName, parentName, selectComposition]);
  return {
    codemod,
    compositionId,
    createComposition,
    heightValidationMessage,
    nameValidationMessage,
    valid,
    widthValidationMessage
  };
};

// src/components/NewComposition/NewComposition.tsx
import { sortItemsByCommitOrder as sortItemsByCommitOrder2 } from "@remotion/canvas";
import {
  useCallback as useCallback82,
  useContext as useContext101,
  useEffect as useEffect67,
  useMemo as useMemo94,
  useRef as useRef53,
  useState as useState61
} from "react";
import { Internals as Internals96 } from "remotion";

// src/components/NewComposition/CodemodFooter.tsx
import {
  useCallback as useCallback80,
  useContext as useContext100,
  useEffect as useEffect66,
  useMemo as useMemo93,
  useState as useState60
} from "react";

// src/components/NewComposition/DiffPreview.tsx
import { jsx as jsx158, jsxs as jsxs85 } from "react/jsx-runtime";
var CodemodDiffPreview = ({ status }) => {
  if (status.type === "loading") {
    return null;
  }
  if (status.type === "fail") {
    return /* @__PURE__ */ jsx158("span", {
      style: { color: FAIL_COLOR, fontSize: 13, lineHeight: 1.2 },
      children: status.error
    });
  }
  return /* @__PURE__ */ jsxs85("div", {
    style: { lineHeight: 1.2 },
    children: [
      /* @__PURE__ */ jsx158("span", {
        style: { color: LIGHT_TEXT, fontSize: 13, lineHeight: 1.2 },
        children: "This will edit your codebase."
      }),
      /* @__PURE__ */ jsx158("br", {}),
      /* @__PURE__ */ jsxs85("span", {
        style: { color: BLUE, fontSize: 13, lineHeight: 1.2 },
        children: [
          status.diff.additions,
          " addition",
          status.diff.additions === 1 ? "" : "s",
          ","
        ]
      }),
      " ",
      /* @__PURE__ */ jsxs85("span", {
        style: {
          color: SELECTED_GUIDE,
          fontSize: 13,
          lineHeight: 1.2
        },
        children: [
          status.diff.deletions,
          " deletion",
          status.diff.deletions === 1 ? "" : "s"
        ]
      })
    ]
  });
};

// src/components/NewComposition/CodemodFooter.tsx
import { jsx as jsx159, jsxs as jsxs86 } from "react/jsx-runtime";
var CodemodFooterPresentation = ({
  codemodStatus,
  disabled,
  genericSubmitLabel,
  relativeFilePath,
  submitLabel,
  trigger
}) => {
  return /* @__PURE__ */ jsxs86(Row, {
    align: "center",
    children: [
      /* @__PURE__ */ jsx159(CodemodDiffPreview, {
        status: codemodStatus
      }),
      /* @__PURE__ */ jsx159(Flex, {}),
      /* @__PURE__ */ jsx159(Spacing, {
        block: true,
        x: 2
      }),
      /* @__PURE__ */ jsxs86(ModalButton, {
        onClick: trigger,
        disabled,
        children: [
          relativeFilePath ? submitLabel({ relativeRootPath: relativeFilePath }) : genericSubmitLabel,
          /* @__PURE__ */ jsx159(ShortcutHint, {
            keyToPress: "↵",
            cmdOrCtrl: false
          })
        ]
      })
    ]
  });
};
var CodemodFooter = ({
  codemod,
  stack: stack2,
  valid,
  loadingNotification,
  errorNotification,
  genericSubmitLabel,
  submitLabel,
  onSuccess,
  fallbackToRootFile = false,
  applyCodemod: applyCodemod2,
  applyCodemodForPreview
}) => {
  const [submitting, setSubmitting] = useState60(false);
  const { setSelectedModal } = useContext100(SetSelectedModalContext);
  const [codemodStatus, setCanApplyCodemod] = useState60({
    type: "loading"
  });
  const resolvedLocation = useResolvedStack(stack2);
  const symbolicatedStack = useMemo93(() => resolvedStackToSymbolicated(resolvedLocation), [resolvedLocation]);
  const relativeFilePath = symbolicatedStack?.originalFileName ?? null;
  const trigger = useCallback80(() => {
    setSubmitting(true);
    setSelectedModal(null);
    const notification = loadingNotification === null ? null : showNotification(loadingNotification, null);
    applyCodemod2({
      symbolicatedStack,
      signal: new AbortController().signal
    }).then((result) => {
      if (!result.success) {
        const message2 = `${errorNotification}: ${result.reason}`;
        if (notification) {
          notification.replaceContent(message2, 2000);
        } else {
          showNotification(message2, 2000);
        }
        return;
      }
      notification?.dismiss();
      onSuccess?.();
    }).catch((err) => {
      const message2 = `${errorNotification}: ${err.message}`;
      if (notification) {
        notification.replaceContent(message2, 2000);
      } else {
        showNotification(message2, 2000);
      }
    });
  }, [
    applyCodemod2,
    errorNotification,
    loadingNotification,
    onSuccess,
    setSelectedModal,
    symbolicatedStack
  ]);
  const getCanApplyCodemod = useCallback80(async (signal) => {
    const res = applyCodemodForPreview ? await applyCodemodForPreview({ signal, symbolicatedStack }) : await applyCodemod({
      codemod,
      dryRun: true,
      symbolicatedStack,
      signal,
      undoRedoNavigation: null
    });
    if (res.success) {
      setCanApplyCodemod({ type: "success", diff: res.diff });
    } else {
      setCanApplyCodemod({
        type: "fail",
        error: res.reason
      });
    }
  }, [applyCodemodForPreview, codemod, symbolicatedStack]);
  useEffect66(() => {
    if (!stack2) {
      if (fallbackToRootFile) {
        const rootFileAbortController = new AbortController;
        let rootFileAborted = false;
        getCanApplyCodemod(rootFileAbortController.signal).then(() => {
          return;
        }).catch((err) => {
          if (rootFileAborted) {
            return;
          }
          showNotification(`${errorNotification}: ${err.message}`, 3000);
        });
        return () => {
          rootFileAborted = true;
          rootFileAbortController.abort();
        };
      }
      setCanApplyCodemod({
        type: "fail",
        error: "Could not determine where this item is defined"
      });
      return;
    }
    if (!hasResolvedStack(stack2)) {
      return;
    }
    if (!symbolicatedStack) {
      setCanApplyCodemod({
        type: "fail",
        error: "Could not resolve the source location of this item"
      });
      return;
    }
    const abortController = new AbortController;
    let aborted = false;
    getCanApplyCodemod(abortController.signal).then(() => {
      return;
    }).catch((err) => {
      if (aborted) {
        return;
      }
      showNotification(`${errorNotification}: ${err.message}`, 3000);
    });
    return () => {
      aborted = true;
      abortController.abort();
    };
  }, [
    errorNotification,
    fallbackToRootFile,
    getCanApplyCodemod,
    stack2,
    symbolicatedStack
  ]);
  const disabled = !valid || submitting || symbolicatedStack === null && !fallbackToRootFile || codemodStatus.type !== "success";
  const { registerKeybinding } = useKeybinding();
  useEffect66(() => {
    if (disabled) {
      return;
    }
    const enter = registerKeybinding({
      callback() {
        trigger();
      },
      commandCtrlKey: false,
      key: "Enter",
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      enter.unregister();
    };
  }, [disabled, registerKeybinding, trigger, valid]);
  return /* @__PURE__ */ jsx159(CodemodFooterPresentation, {
    codemodStatus,
    disabled,
    genericSubmitLabel,
    relativeFilePath,
    submitLabel,
    trigger
  });
};

// src/components/NewComposition/get-new-composition-defaults.ts
var defaultCompositionDimensions = {
  width: 1920,
  height: 1080,
  fps: 30,
  durationInFrames: 150
};
var canvasCaptureFps = 60;
var getNewCompositionDefaults = (composition, canvasCaptureDurationInSeconds) => {
  if (canvasCaptureDurationInSeconds !== null) {
    return {
      ...defaultCompositionDimensions,
      fps: canvasCaptureFps,
      durationInFrames: Math.max(1, Math.ceil(canvasCaptureDurationInSeconds * canvasCaptureFps))
    };
  }
  return composition ?? defaultCompositionDimensions;
};

// src/components/NewComposition/InputAndValidationContainer.tsx
import { jsx as jsx160 } from "react/jsx-runtime";
var container30 = {
  width: 250,
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
  alignItems: "flex-end"
};
var InputAndValidationContainer = ({
  children
}) => {
  return /* @__PURE__ */ jsx160("div", {
    style: container30,
    children
  });
};

// src/components/NewComposition/NewCompDuration.tsx
import { useCallback as useCallback81 } from "react";
import { jsx as jsx161, jsxs as jsxs87, Fragment as Fragment38 } from "react/jsx-runtime";
var NewCompDuration = ({ durationInFrames, setDurationInFrames }) => {
  const onDurationInFramesChanged = useCallback81((newValue) => {
    setDurationInFrames(Number(newValue));
  }, [setDurationInFrames]);
  const onDurationChangedDirectly = useCallback81((newVal) => {
    setDurationInFrames(newVal);
  }, [setDurationInFrames]);
  const compDurationErrMessage = validateCompositionDuration(durationInFrames);
  return /* @__PURE__ */ jsxs87("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsx161("div", {
        style: label,
        children: "Duration in frames"
      }),
      /* @__PURE__ */ jsx161("div", {
        style: rightRow,
        children: /* @__PURE__ */ jsxs87(InputAndValidationContainer, {
          children: [
            /* @__PURE__ */ jsx161(InputDragger, {
              "aria-label": "Duration in frames",
              type: "number",
              value: durationInFrames,
              onTextChange: onDurationInFramesChanged,
              placeholder: "Duration (frames)",
              name: "durationInFrames",
              min: 1,
              step: 1,
              required: true,
              status: "ok",
              max: 300000,
              onValueChange: onDurationChangedDirectly,
              rightAlign: false
            }),
            compDurationErrMessage ? /* @__PURE__ */ jsxs87(Fragment38, {
              children: [
                /* @__PURE__ */ jsx161(Spacing, {
                  y: 1,
                  block: true
                }),
                /* @__PURE__ */ jsx161(ValidationMessage, {
                  align: "flex-start",
                  message: compDurationErrMessage,
                  type: "error"
                })
              ]
            }) : null
          ]
        })
      })
    ]
  });
};

// src/components/NewComposition/SlugPreview.tsx
import { jsx as jsx162, jsxs as jsxs88, Fragment as Fragment39 } from "react/jsx-runtime";
var SlugPreview = ({ action, currentName, input: input3, slug }) => {
  if (!slug || slug === input3 || slug === input3.trim() || slug === currentName) {
    return null;
  }
  return /* @__PURE__ */ jsxs88(Fragment39, {
    children: [
      /* @__PURE__ */ jsx162(Spacing, {
        y: 1,
        block: true
      }),
      /* @__PURE__ */ jsxs88("div", {
        "aria-live": "polite",
        style: { fontSize: 12, color: LIGHT_TEXT },
        children: [
          "Will be ",
          action === "create" ? "created as" : "renamed to",
          " ",
          slug
        ]
      })
    ]
  });
};

// src/components/NewComposition/NewComposition.tsx
import { jsx as jsx163, jsxs as jsxs89, Fragment as Fragment40 } from "react/jsx-runtime";
var content5 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var folderSelectStyle = {
  boxSizing: "border-box",
  width: 250
};
var folderIconStyle = {
  flexShrink: 0,
  height: 16,
  width: 16
};
var folderLabelStyle = {
  alignItems: "center",
  color: "inherit",
  display: "flex",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "normal",
  minWidth: 0
};
var folderLabelTextStyle = {
  color: "inherit",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "normal",
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var FolderDropdownLabel = ({ folderPath, indentation }) => {
  return /* @__PURE__ */ jsxs89("div", {
    style: folderLabelStyle,
    children: [
      /* @__PURE__ */ jsx163(Spacing, {
        x: indentation * 1.5
      }),
      folderPath === null ? /* @__PURE__ */ jsx163("div", {
        style: folderIconStyle
      }) : /* @__PURE__ */ jsx163(CollapsedFolderIcon, {
        color: LIGHT_TEXT,
        style: folderIconStyle
      }),
      /* @__PURE__ */ jsx163(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx163("span", {
        style: folderLabelTextStyle,
        children: folderPath ?? "None"
      })
    ]
  });
};
var rootFolderId = "new-composition-root-folder";
var folderSelectIdPrefix = "new-composition-folder-";
var NewCompositionFields = ({
  compositionId,
  heightValidationMessage,
  inputRef,
  nameValidationMessage,
  setValues,
  values,
  widthValidationMessage
}) => {
  const { folders } = useContext101(Internals96.CompositionManager);
  const { compositionSortOrder } = useContext101(FolderContext);
  const selectedFolderId = values.folder.folderName ? `${folderSelectIdPrefix}${getFolderId({
    folderName: values.folder.folderName,
    parentName: values.folder.parentName
  })}` : rootFolderId;
  const folderValues = useMemo94(() => {
    const foldersInTreeOrder = [];
    const sortedFolders = compositionSortOrder === "alphabetical" ? folders.slice().sort((a, b) => a.name.localeCompare(b.name, undefined, { numeric: true })) : sortItemsByCommitOrder2(folders, (folder) => folder.order);
    const appendFolders = (parent) => {
      sortedFolders.filter((folder) => folder.parent === parent).forEach((folder) => {
        foldersInTreeOrder.push(folder);
        appendFolders(getFolderId({ folderName: folder.name, parentName: folder.parent }));
      });
    };
    appendFolders(null);
    return [
      {
        id: rootFolderId,
        keyHint: null,
        label: /* @__PURE__ */ jsx163(FolderDropdownLabel, {
          folderPath: null,
          indentation: 0
        }),
        leftItem: values.folder.folderName === null ? /* @__PURE__ */ jsx163(Checkmark, {}) : null,
        onClick: () => {
          setValues((current) => ({
            ...current,
            folder: {
              folderName: null,
              parentName: null,
              stack: null
            }
          }));
        },
        quickSwitcherLabel: "None",
        subMenu: null,
        type: "item",
        value: rootFolderId
      },
      ...folders.length === 0 ? [] : [
        {
          id: "new-composition-root-folder-divider",
          type: "divider"
        }
      ],
      ...foldersInTreeOrder.map((folder) => {
        const folderPath = getFolderId({
          folderName: folder.name,
          parentName: folder.parent
        });
        const indentation = folder.parent?.split("/").length ?? 0;
        const id = `${folderSelectIdPrefix}${folderPath}`;
        return {
          id,
          keyHint: null,
          label: /* @__PURE__ */ jsx163(FolderDropdownLabel, {
            folderPath,
            indentation
          }),
          leftItem: selectedFolderId === id ? /* @__PURE__ */ jsx163(Checkmark, {}) : null,
          onClick: () => {
            setValues((current) => ({
              ...current,
              folder: {
                folderName: folder.name,
                parentName: folder.parent,
                stack: folder.stack
              }
            }));
          },
          quickSwitcherLabel: folderPath,
          subMenu: null,
          type: "item",
          value: id,
          disabled: folder.stack === null
        };
      })
    ];
  }, [
    compositionSortOrder,
    folders,
    selectedFolderId,
    setValues,
    values.folder.folderName
  ]);
  const onWidthChanged = useCallback82((newValue) => {
    setValues((current) => ({
      ...current,
      size: { height: current.size.height, width: Number(newValue) }
    }));
  }, [setValues]);
  const onWidthDirectlyChanged = useCallback82((newWidth) => {
    setValues((current) => ({
      ...current,
      size: { height: current.size.height, width: newWidth }
    }));
  }, [setValues]);
  const onHeightChanged = useCallback82((newValue) => {
    setValues((current) => ({
      ...current,
      size: { height: Number(newValue), width: current.size.width }
    }));
  }, [setValues]);
  const onHeightDirectlyChanged = useCallback82((newHeight) => {
    setValues((current) => ({
      ...current,
      size: { height: newHeight, width: current.size.width }
    }));
  }, [setValues]);
  const onNameChange = useCallback82((event) => {
    setValues((current) => ({ ...current, id: event.target.value }));
  }, [setValues]);
  const onTextFpsChange = useCallback82((newFps) => {
    setValues((current) => ({ ...current, fps: Number(newFps) }));
  }, [setValues]);
  const onFpsChange = useCallback82((newFps) => {
    setValues((current) => ({ ...current, fps: newFps }));
  }, [setValues]);
  const setDurationInFrames = useCallback82((update) => {
    setValues((current) => ({
      ...current,
      durationInFrames: typeof update === "function" ? update(current.durationInFrames) : update
    }));
  }, [setValues]);
  return /* @__PURE__ */ jsxs89(Fragment40, {
    children: [
      /* @__PURE__ */ jsxs89("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: label,
            children: "Folder"
          }),
          /* @__PURE__ */ jsx163("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx163(Combobox, {
              values: folderValues,
              selectedId: selectedFolderId,
              style: folderSelectStyle,
              title: "Folder"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs89("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: label,
            children: "ID"
          }),
          /* @__PURE__ */ jsx163("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsxs89(InputAndValidationContainer, {
              children: [
                /* @__PURE__ */ jsx163(RemotionInput, {
                  ref: inputRef,
                  value: values.id,
                  onChange: onNameChange,
                  type: "text",
                  autoFocus: true,
                  placeholder: "Composition ID",
                  status: "ok",
                  rightAlign: true
                }),
                /* @__PURE__ */ jsx163(SlugPreview, {
                  action: "create",
                  currentName: null,
                  input: values.id,
                  slug: compositionId
                }),
                nameValidationMessage ? /* @__PURE__ */ jsxs89(Fragment40, {
                  children: [
                    /* @__PURE__ */ jsx163(Spacing, {
                      y: 1,
                      block: true
                    }),
                    /* @__PURE__ */ jsx163(ValidationMessage, {
                      align: "flex-start",
                      message: nameValidationMessage,
                      type: "error"
                    })
                  ]
                }) : null
              ]
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs89("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: label,
            children: "Width"
          }),
          /* @__PURE__ */ jsx163("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsxs89(InputAndValidationContainer, {
              children: [
                /* @__PURE__ */ jsx163(InputDragger, {
                  "aria-label": "Width",
                  type: "number",
                  value: values.size.width,
                  placeholder: "Width",
                  onTextChange: onWidthChanged,
                  name: "width",
                  step: 2,
                  min: 2,
                  required: true,
                  status: "ok",
                  formatter: (width2) => `${width2}px`,
                  max: 1e8,
                  onValueChange: onWidthDirectlyChanged,
                  rightAlign: false
                }),
                widthValidationMessage ? /* @__PURE__ */ jsxs89(Fragment40, {
                  children: [
                    /* @__PURE__ */ jsx163(Spacing, {
                      y: 1,
                      block: true
                    }),
                    /* @__PURE__ */ jsx163(ValidationMessage, {
                      align: "flex-start",
                      message: widthValidationMessage,
                      type: "error"
                    })
                  ]
                }) : null
              ]
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs89("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: label,
            children: "Height"
          }),
          /* @__PURE__ */ jsx163("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsxs89(InputAndValidationContainer, {
              children: [
                /* @__PURE__ */ jsx163(InputDragger, {
                  "aria-label": "Height",
                  type: "number",
                  value: values.size.height,
                  onTextChange: onHeightChanged,
                  placeholder: "Height",
                  name: "height",
                  step: 2,
                  required: true,
                  formatter: (height) => `${height}px`,
                  min: 2,
                  status: "ok",
                  max: 1e8,
                  onValueChange: onHeightDirectlyChanged,
                  rightAlign: false
                }),
                heightValidationMessage ? /* @__PURE__ */ jsxs89(Fragment40, {
                  children: [
                    /* @__PURE__ */ jsx163(Spacing, {
                      y: 1,
                      block: true
                    }),
                    /* @__PURE__ */ jsx163(ValidationMessage, {
                      align: "flex-start",
                      message: heightValidationMessage,
                      type: "error"
                    })
                  ]
                }) : null
              ]
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx163(NewCompDuration, {
        durationInFrames: values.durationInFrames,
        setDurationInFrames
      }),
      /* @__PURE__ */ jsxs89("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: label,
            children: "FPS"
          }),
          /* @__PURE__ */ jsx163("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx163(InputDragger, {
              "aria-label": "FPS",
              type: "number",
              value: values.fps,
              onTextChange: onTextFpsChange,
              placeholder: "Frame rate (fps)",
              name: "fps",
              min: 1,
              required: true,
              status: "ok",
              max: 240,
              step: 0.01,
              onValueChange: onFpsChange,
              rightAlign: false
            })
          })
        ]
      })
    ]
  });
};
var NewCompositionLoaded = ({ canvasCapture, folderName, parentName, stack: stack2 }) => {
  const { compositions } = useContext101(Internals96.CompositionManager);
  const resolvedComposition = Internals96.useResolvedVideoConfig(null);
  const initialComposition = resolvedComposition?.type === "success" || resolvedComposition?.type === "success-and-refreshing" ? resolvedComposition.result : null;
  const initialDimensions = getNewCompositionDefaults(initialComposition, canvasCapture?.durationInSeconds ?? null);
  const [values, setValues] = useState61(() => ({
    durationInFrames: initialDimensions.durationInFrames,
    folder: { folderName, parentName, stack: stack2 },
    fps: initialDimensions.fps,
    id: getUniqueCompositionName(compositions),
    size: {
      height: initialDimensions.height,
      width: initialDimensions.width
    }
  }));
  const inputRef = useRef53(null);
  useEffect67(() => {
    const input3 = inputRef.current;
    if (!input3)
      return;
    input3.select();
  }, []);
  const {
    codemod,
    compositionId,
    createComposition,
    heightValidationMessage,
    nameValidationMessage,
    valid,
    widthValidationMessage
  } = useCreateComposition({
    compositions,
    durationInFrames: values.durationInFrames,
    folderName: values.folder.folderName,
    newId: values.id,
    parentName: values.folder.parentName,
    selectedFrameRate: values.fps,
    size: values.size,
    canvasCapture: canvasCapture === null ? null : {
      data: canvasCapture.data,
      videoFileName: canvasCapture.file.name,
      videoHeight: canvasCapture.height,
      videoWidth: canvasCapture.width
    }
  });
  const createCanvasCaptureComposition = useCallback82(async ({
    signal,
    symbolicatedStack
  }) => {
    if (canvasCapture !== null) {
      const existingFile = getStaticFiles().find((file) => file.name === canvasCapture.file.name);
      if (existingFile !== undefined && existingFile.sizeInBytes !== canvasCapture.file.size) {
        throw new Error(`File with name ${canvasCapture.file.name} already exists and is different`);
      }
      await installRequiredPackages([
        { name: "@remotion/media", version: null },
        { name: "@remotion/mac-cursors", version: null }
      ]);
      if (existingFile === undefined) {
        await writeStaticFile({
          contents: await canvasCapture.file.arrayBuffer(),
          filePath: canvasCapture.file.name
        });
      }
    }
    return createComposition({ signal, symbolicatedStack });
  }, [canvasCapture, createComposition]);
  const onSubmit = useCallback82((e) => {
    e.preventDefault();
  }, []);
  return /* @__PURE__ */ jsxs89(Fragment40, {
    children: [
      /* @__PURE__ */ jsx163(ModalHeader, {
        title: canvasCapture === null ? "New composition" : "Import Canvas Capture"
      }),
      /* @__PURE__ */ jsxs89("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsx163("div", {
            style: content5,
            children: /* @__PURE__ */ jsx163(NewCompositionFields, {
              compositionId,
              heightValidationMessage,
              inputRef,
              nameValidationMessage,
              setValues,
              values,
              widthValidationMessage
            })
          }),
          /* @__PURE__ */ jsx163(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx163(CodemodFooter, {
              loadingNotification: null,
              errorNotification: "Could not create composition",
              genericSubmitLabel: "Add to root file",
              submitLabel: ({ relativeRootPath }) => `Add to ${relativeRootPath}`,
              codemod,
              stack: values.folder.stack,
              valid,
              onSuccess: null,
              applyCodemod: ({ signal, symbolicatedStack }) => createCanvasCaptureComposition({
                signal,
                symbolicatedStack
              }),
              applyCodemodForPreview: null,
              fallbackToRootFile: true
            })
          })
        ]
      })
    ]
  });
};
var NewComposition = ({ canvasCapture, folderName, parentName, stack: stack2 }) => {
  return /* @__PURE__ */ jsx163(DismissableModal, {
    children: /* @__PURE__ */ jsx163(NewCompositionLoaded, {
      canvasCapture,
      folderName,
      parentName,
      stack: stack2
    })
  });
};

// src/components/RadioButton.tsx
import { jsx as jsx164, jsxs as jsxs90 } from "react/jsx-runtime";
var row6 = {
  ...hoverableStyle({
    idleBackground: TRANSPARENT,
    hoverBackground: getBackgroundFromHoverState({
      hovered: true,
      selected: false
    }),
    idleColor: LIGHT_TEXT,
    hoverColor: WHITE
  }),
  alignItems: "center",
  appearance: "none",
  border: "none",
  borderRadius: 4,
  boxSizing: "border-box",
  cursor: "default",
  display: "flex",
  fontFamily: "sans-serif",
  fontSize: 14,
  gap: 8,
  lineHeight: "20px",
  margin: "5px -8px",
  padding: "4px 8px",
  textAlign: "left",
  userSelect: "none",
  width: "calc(100% + 16px)"
};
var circle = {
  alignItems: "center",
  border: `1px solid ${CURRENT_COLOR}`,
  borderRadius: "50%",
  boxSizing: "border-box",
  display: "flex",
  flexShrink: 0,
  height: 20,
  justifyContent: "center",
  width: 20
};
var bullet = {
  backgroundColor: CURRENT_COLOR,
  borderRadius: "50%",
  height: 8,
  width: 8
};
var label10 = {
  color: "inherit",
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: "20px"
};
var RadioButton = ({ checked, disabled, children, onClick }) => {
  return /* @__PURE__ */ jsxs90("button", {
    "aria-checked": checked,
    disabled,
    className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
    onClick,
    role: "radio",
    style: row6,
    type: "button",
    children: [
      /* @__PURE__ */ jsx164("span", {
        style: circle,
        children: checked ? /* @__PURE__ */ jsx164("span", {
          style: bullet
        }) : null
      }),
      /* @__PURE__ */ jsx164("span", {
        style: label10,
        children
      })
    ]
  });
};

// src/components/ElementInstallConfirmation.tsx
import { jsx as jsx165, jsxs as jsxs91 } from "react/jsx-runtime";
var container31 = {
  display: "flex",
  flexDirection: "column",
  gap: 20,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: 1.5
};
var dialogContent = {
  ...container31,
  padding: 16,
  width: "min(520px, calc(100vw - 40px))",
  maxHeight: "min(720px, calc(100vh - 140px))",
  overflowY: "auto"
};
var sectionStyle = {
  display: "flex",
  flexDirection: "column",
  gap: 10
};
var sectionTitleStyle = {
  margin: 0,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 600,
  lineHeight: 1.5
};
var metadataStyle = {
  display: "flex",
  flexDirection: "column",
  gap: 8,
  margin: 0
};
var metadataRowStyle = {
  display: "grid",
  gridTemplateColumns: "120px minmax(0, 1fr)",
  alignItems: "baseline",
  gap: 12
};
var metadataTermStyle = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 500,
  lineHeight: 1.5
};
var metadataDescriptionStyle = {
  margin: 0,
  minWidth: 0,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 400,
  lineHeight: 1.5,
  overflowWrap: "anywhere"
};
var requestSourceStyle = {
  display: "flex",
  justifyContent: "space-between",
  gap: 16,
  margin: 0
};
var requestSourceDescriptionStyle = {
  ...metadataDescriptionStyle,
  color: LIGHT_TEXT,
  textAlign: "right"
};
var unverifiedRequestSourceStyle = {
  ...requestSourceDescriptionStyle,
  color: WARNING_COLOR
};
var codeStyle3 = {
  color: "inherit",
  fontFamily: "monospace",
  fontSize: 13,
  lineHeight: 1.5
};
var overwriteStyle = {
  margin: 0,
  color: WARNING_COLOR,
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 500,
  lineHeight: 1.5
};
var dependencyListStyle = {
  display: "flex",
  flexDirection: "column",
  gap: 8,
  margin: 0,
  padding: 0,
  listStyleType: "none",
  textAlign: "right",
  minWidth: 0
};
var dependencyNameStyle = {
  minWidth: 0,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: 1.5,
  overflowWrap: "anywhere"
};
var warningStyle = {
  display: "flex",
  alignItems: "flex-start",
  gap: 10,
  minWidth: 0
};
var warningIconStyle = {
  width: 14,
  height: 14,
  marginTop: 2,
  flexShrink: 0,
  fill: WARNING_COLOR
};
var warningDescriptionStyle = {
  margin: 0,
  minWidth: 0,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 400,
  lineHeight: 1.5
};
var browseElementsStyle = {
  color: "inherit",
  fontFamily: "inherit",
  fontSize: "inherit",
  fontWeight: 600,
  lineHeight: "inherit"
};
var sourceDetailsStyle = {
  paddingTop: 2,
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: 1.5
};
var sourceSummaryStyle = {
  cursor: "default",
  ...hoverableStyle({
    idleBackground: TRANSPARENT,
    hoverBackground: TRANSPARENT,
    idleColor: LIGHT_TEXT,
    hoverColor: WHITE
  }),
  fontFamily: "sans-serif",
  fontSize: 13,
  fontWeight: 500,
  lineHeight: 1.5
};
var sourceCodeContainerStyle = {
  marginTop: 10,
  overflow: "hidden",
  border: `1px solid ${WHITE_ALPHA_12}`,
  borderRadius: 6
};
var sourceCodeBlockStyle = {
  backgroundColor: INPUT_BACKGROUND,
  margin: 0,
  border: 0,
  borderRadius: 0,
  maxWidth: "none",
  maxHeight: 240,
  overflow: "auto",
  padding: 12,
  fontFamily: "monospace",
  fontSize: 12,
  lineHeight: 1.5,
  whiteSpace: "pre"
};
var sourceCodeStyle = {
  color: "inherit",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "inherit"
};
var destinationControlStyle = {
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  columnGap: 16,
  rowGap: 10,
  flexWrap: "wrap"
};
var footerStyle = {
  minWidth: 0
};
var cancelStyle2 = {
  minWidth: 90
};
var elementInstallTitleId = "remotion-element-install-title";
var makeSourceControlsVisible = (sourceCode) => {
  return sourceCode.replace(/[\u200b-\u200f\u202a-\u202e\u2066-\u2069\ufeff]/g, (character) => {
    return `\\u${character.codePointAt(0)?.toString(16).padStart(4, "0")}`;
  });
};
var ElementLibraryAddConfirmation = ({ displayName, origin, url }) => {
  return /* @__PURE__ */ jsxs91("div", {
    style: container31,
    children: [
      /* @__PURE__ */ jsxs91("dl", {
        style: metadataStyle,
        "aria-label": "Catalog details",
        children: [
          displayName === null ? null : /* @__PURE__ */ jsxs91("div", {
            style: metadataRowStyle,
            children: [
              /* @__PURE__ */ jsx165("dt", {
                style: metadataTermStyle,
                children: "Display name"
              }),
              /* @__PURE__ */ jsx165("dd", {
                style: metadataDescriptionStyle,
                children: displayName
              })
            ]
          }),
          /* @__PURE__ */ jsxs91("div", {
            style: metadataRowStyle,
            children: [
              /* @__PURE__ */ jsx165("dt", {
                style: metadataTermStyle,
                children: "Request source"
              }),
              /* @__PURE__ */ jsx165("dd", {
                style: metadataDescriptionStyle,
                children: origin
              })
            ]
          }),
          /* @__PURE__ */ jsxs91("div", {
            style: metadataRowStyle,
            children: [
              /* @__PURE__ */ jsx165("dt", {
                style: metadataTermStyle,
                children: "Catalog URL"
              }),
              /* @__PURE__ */ jsx165("dd", {
                style: metadataDescriptionStyle,
                children: /* @__PURE__ */ jsx165("code", {
                  style: codeStyle3,
                  children: url
                })
              })
            ]
          })
        ]
      }),
      /* @__PURE__ */ jsxs91("div", {
        style: warningStyle,
        children: [
          /* @__PURE__ */ jsx165(WarningTriangle, {
            style: warningIconStyle
          }),
          /* @__PURE__ */ jsxs91("p", {
            style: warningDescriptionStyle,
            children: [
              "This adds the catalog to",
              " ",
              /* @__PURE__ */ jsx165("strong", {
                style: browseElementsStyle,
                children: "Browse Elements"
              }),
              " when nothing is selected on the canvas. It is saved in",
              " ",
              /* @__PURE__ */ jsx165("code", {
                style: codeStyle3,
                children: "remotion.config.ts"
              }),
              "."
            ]
          })
        ]
      })
    ]
  });
};
var ElementInstallConfirmation = ({ state }) => {
  const {
    currentPlan,
    missingPackages,
    newPlan,
    onClose,
    request,
    sourceIsUnverified,
    sourceLabel
  } = state;
  const sourcePreview = useMemo95(() => makeSourceControlsVisible(request.element.sourceCode), [request.element.sourceCode]);
  const config = Internals97.useUnsafeVideoConfig();
  const { canvasContent, compositions } = useContext102(Internals97.CompositionManager);
  const selectComposition = useSelectComposition();
  const currentCompositionMetadata = config === null || canvasContent?.type !== "composition" || canvasContent.compositionId !== request.compositionId ? null : {
    durationInFrames: config.durationInFrames,
    fps: config.fps,
    height: config.height,
    width: config.width
  };
  const { currentZIndex } = useZIndex();
  const [mode, setMode] = useState63(currentPlan === null || request.source.type === "browser-studio-link" ? "new-composition" : "current-composition");
  const [submitting, setSubmitting] = useState63(false);
  const inputRef = useRef54(null);
  const [newCompositionValues, setNewCompositionValues] = useState63(() => {
    const elementComponentName = StudioProtocolInternals.getElementComponentNameFromSourceCode(request.element.sourceCode) ?? "Element";
    const baseName = `${elementComponentName}Composition`;
    let candidate = baseName;
    let suffix2 = 2;
    while (validateCompositionName(candidate, compositions) !== null) {
      candidate = `${baseName}${suffix2}`;
      suffix2++;
    }
    const size3 = request.element.dimensions ?? (currentCompositionMetadata === null ? { width: 1920, height: 1080 } : {
      width: currentCompositionMetadata.width,
      height: currentCompositionMetadata.height
    });
    return {
      durationInFrames: request.element.durationInFrames ?? currentCompositionMetadata?.durationInFrames ?? 150,
      folder: { folderName: null, parentName: null, stack: null },
      fps: currentCompositionMetadata?.fps ?? 30,
      id: candidate,
      size: size3
    };
  });
  const selectedFolderStack = newCompositionValues.folder.stack;
  const resolvedFolderLocation = useResolvedStack(selectedFolderStack);
  const folderSymbolicatedStack = useMemo95(() => resolvedStackToSymbolicated(resolvedFolderLocation), [resolvedFolderLocation]);
  const folderCompositionFile = folderSymbolicatedStack?.originalFileName ?? null;
  const [newCompositionPlanState, setNewCompositionPlanState] = useState63({
    folderStack: null,
    type: "ready",
    plan: newPlan
  });
  useEffect68(() => {
    if (selectedFolderStack === null) {
      setNewCompositionPlanState({
        folderStack: null,
        type: "ready",
        plan: newPlan
      });
      return;
    }
    if (!hasResolvedStack(selectedFolderStack)) {
      setNewCompositionPlanState({
        folderStack: selectedFolderStack,
        type: "loading"
      });
      return;
    }
    if (folderCompositionFile === null) {
      setNewCompositionPlanState({
        folderStack: selectedFolderStack,
        type: "error",
        reason: "Could not determine where the new composition should be created."
      });
      return;
    }
    let canceled = false;
    setNewCompositionPlanState({
      folderStack: selectedFolderStack,
      type: "loading"
    });
    prepareElementInstall({
      installationName: null,
      destination: {
        type: "new-composition",
        compositionFile: folderCompositionFile
      },
      element: request.element
    }).then((result) => {
      if (canceled) {
        return;
      }
      setNewCompositionPlanState(result.success ? {
        folderStack: selectedFolderStack,
        type: "ready",
        plan: result.plan
      } : {
        folderStack: selectedFolderStack,
        type: "error",
        reason: result.reason
      });
    }).catch((error) => {
      if (!canceled) {
        setNewCompositionPlanState({
          folderStack: selectedFolderStack,
          type: "error",
          reason: error instanceof Error ? error.message : String(error)
        });
      }
    });
    return () => {
      canceled = true;
    };
  }, [folderCompositionFile, newPlan, request.element, selectedFolderStack]);
  const {
    codemod: newCompositionCodemod,
    compositionId: newCompositionId,
    heightValidationMessage,
    nameValidationMessage,
    valid: newCompositionValuesAreValid,
    widthValidationMessage
  } = useCreateComposition({
    canvasCapture: null,
    compositions,
    durationInFrames: newCompositionValues.durationInFrames,
    folderName: newCompositionValues.folder.folderName,
    newId: newCompositionValues.id,
    parentName: newCompositionValues.folder.parentName,
    selectedFrameRate: newCompositionValues.fps,
    size: newCompositionValues.size
  });
  const activeNewCompositionPlanState = newCompositionPlanState.folderStack === selectedFolderStack ? newCompositionPlanState : null;
  const selectedNewCompositionPlan = activeNewCompositionPlanState?.type === "ready" ? activeNewCompositionPlanState.plan : null;
  const selectedPlan = mode === "current-composition" ? currentPlan : selectedNewCompositionPlan;
  const elementBaseName = request.element.slug.split("/").at(-1) ?? "";
  const [refreshPlan, setRefreshPlan] = useState63(0);
  const [input3, setInput] = useState63({
    basePlan: selectedPlan,
    baseName: elementBaseName,
    refresh: refreshPlan,
    name: null,
    overwritePlan: null
  });
  if (input3.basePlan !== selectedPlan || input3.baseName !== elementBaseName || input3.refresh !== refreshPlan) {
    setInput({
      basePlan: selectedPlan,
      baseName: elementBaseName,
      refresh: refreshPlan,
      name: null,
      overwritePlan: null
    });
  }
  const requestedName = input3.name;
  const [preparations, setPreparations] = useState63([]);
  const currentPreparation = preparations.find((preparation) => preparation.basePlan === selectedPlan && preparation.refresh === refreshPlan && preparation.requestedName === requestedName) ?? null;
  const preparedInstallation = currentPreparation?.installation ?? null;
  const planError = currentPreparation?.error ?? null;
  const installationName = requestedName ?? preparedInstallation?.name ?? elementBaseName;
  const [existingInstallations, setExistingInstallations] = useState63([]);
  const existingDestination = existingInstallations.find((installation) => installation.basePlan === selectedPlan && installation.refresh === refreshPlan) ?? null;
  const overwriteExisting = existingDestination !== null && input3.overwritePlan === existingDestination.plan;
  const activePlan = overwriteExisting ? existingDestination.plan : preparedInstallation?.plan ?? null;
  const [displayedPlans, setDisplayedPlans] = useState63([]);
  const rememberedDisplayedPlan = displayedPlans.find((displayed) => displayed.basePlan === selectedPlan && displayed.refresh === refreshPlan)?.plan ?? null;
  const displayedPlan = activePlan ?? rememberedDisplayedPlan ?? selectedPlan;
  useLayoutEffect26(() => {
    if (activePlan === null || selectedPlan === null) {
      return;
    }
    setDisplayedPlans((previous) => {
      const existing = previous.find((displayed) => displayed.basePlan === selectedPlan && displayed.refresh === refreshPlan);
      if (existing?.plan === activePlan) {
        return previous;
      }
      return [
        ...previous.filter((displayed) => displayed.basePlan !== selectedPlan || displayed.refresh !== refreshPlan),
        { basePlan: selectedPlan, refresh: refreshPlan, plan: activePlan }
      ];
    });
  }, [activePlan, refreshPlan, selectedPlan]);
  useEffect68(() => {
    if (selectedPlan === null) {
      return;
    }
    let canceled = false;
    (async () => {
      let candidate = requestedName ?? elementBaseName;
      let copyNumber = 1;
      while (true) {
        const result = await prepareElementInstall({
          installationName: candidate,
          destination: mode === "current-composition" ? {
            type: "current-composition",
            compositionFile: request.compositionFile,
            compositionId: request.compositionId
          } : {
            type: "new-composition",
            compositionFile: selectedPlan.compositionFile
          },
          element: request.element
        });
        if (canceled)
          return;
        if (!result.success)
          throw new Error(result.reason);
        const prepared = {
          basePlan: selectedPlan,
          name: candidate,
          refresh: refreshPlan,
          plan: result.plan
        };
        if (result.plan.expectedFileState.exists) {
          setExistingInstallations((previous) => previous.some((installation) => installation.basePlan === selectedPlan && installation.refresh === refreshPlan) ? previous : [...previous, prepared]);
          if (requestedName === null) {
            candidate = `${elementBaseName}-copy${copyNumber === 1 ? "" : `-${copyNumber}`}`;
            copyNumber++;
            continue;
          }
        }
        setPreparations((previous) => [
          ...previous.filter((preparation) => preparation.basePlan !== selectedPlan || preparation.refresh !== refreshPlan || preparation.requestedName !== requestedName),
          {
            basePlan: selectedPlan,
            refresh: refreshPlan,
            requestedName,
            installation: prepared,
            error: null
          }
        ]);
        return;
      }
    })().catch((error) => {
      if (!canceled) {
        setPreparations((previous) => [
          ...previous.filter((preparation) => preparation.basePlan !== selectedPlan || preparation.refresh !== refreshPlan || preparation.requestedName !== requestedName),
          {
            basePlan: selectedPlan,
            refresh: refreshPlan,
            requestedName,
            installation: null,
            error: error instanceof Error ? error.message : String(error)
          }
        ]);
      }
    });
    return () => {
      canceled = true;
    };
  }, [
    elementBaseName,
    requestedName,
    mode,
    refreshPlan,
    request,
    selectedPlan
  ]);
  const installationNameError = installationName.length > 0 ? planError ?? (requestedName !== null && activePlan?.expectedFileState.exists ? "Name already taken." : null) : null;
  const folderTargetIsReady = selectedFolderStack === null || hasResolvedStack(selectedFolderStack) && folderCompositionFile !== null;
  const title2 = `Install ${request.element.displayName}${request.element.displayName.endsWith(" Element") ? "" : " Element"}`;
  const canSubmit = !submitting && (overwriteExisting || planError === null && StudioProtocolInternals.makeElementFileNameFromSlug(installationName) === `${installationName}.element.tsx` && (activePlan !== null ? !activePlan.expectedFileState.exists : requestedName !== null)) && (mode === "current-composition" ? currentPlan !== null : newCompositionValuesAreValid && folderTargetIsReady && selectedNewCompositionPlan !== null);
  const submit = useCallback83(async () => {
    if (!canSubmit || selectedPlan === null) {
      return;
    }
    setSubmitting(true);
    const installed = await insertElement({
      installationName: overwriteExisting ? existingDestination?.name ?? installationName : installationName,
      compositionFile: mode === "new-composition" ? selectedPlan.compositionFile : request.compositionFile,
      compositionId: mode === "new-composition" ? newCompositionId : request.compositionId,
      element: request.element,
      expectedFileState: activePlan?.expectedFileState ?? { exists: false },
      from: mode === "new-composition" ? null : request.from,
      overwriteExisting,
      position: mode === "new-composition" ? null : request.position,
      undoRedoNavigation: mode === "new-composition" ? {
        undoRoute: getRoute(),
        redoRoute: `/${newCompositionId}`
      } : null,
      newComposition: mode === "new-composition" ? {
        codemod: newCompositionCodemod,
        symbolicatedStack: selectedFolderStack === null ? null : folderSymbolicatedStack
      } : null
    });
    if (installed) {
      if (mode === "new-composition") {
        selectComposition({
          id: newCompositionId,
          folderName: newCompositionValues.folder.folderName,
          parentFolderName: newCompositionValues.folder.parentName
        }, true);
      }
      onClose();
    } else {
      setSubmitting(false);
      setRefreshPlan((value) => value + 1);
    }
  }, [
    activePlan,
    existingDestination,
    installationName,
    overwriteExisting,
    canSubmit,
    folderSymbolicatedStack,
    mode,
    newCompositionCodemod,
    newCompositionId,
    onClose,
    request,
    selectedFolderStack,
    selectedPlan,
    selectComposition,
    newCompositionValues.folder.folderName,
    newCompositionValues.folder.parentName
  ]);
  const cancel = useCallback83(() => {
    if (!submitting) {
      onClose();
    }
  }, [onClose, submitting]);
  const destinationOptions = useMemo95(() => [
    {
      key: "current-composition",
      label: "Current composition",
      selected: mode === "current-composition",
      onClick: currentPlan === null ? null : () => setMode("current-composition")
    },
    {
      key: "new-composition",
      label: "New composition",
      selected: mode === "new-composition",
      onClick: () => {
        setMode("new-composition");
        requestAnimationFrame(() => inputRef.current?.select());
      }
    }
  ], [currentPlan, mode]);
  const onSubmit = useCallback83((event) => {
    event.preventDefault();
    submit();
  }, [submit]);
  return createPortal2(/* @__PURE__ */ jsxs91(ModalContainer, {
    ariaLabelledBy: elementInstallTitleId,
    onOutsideClick: cancel,
    onEscape: cancel,
    children: [
      /* @__PURE__ */ jsx165("div", {
        id: elementInstallTitleId,
        children: /* @__PURE__ */ jsx165(ModalHeader, {
          title: title2,
          onClose: cancel
        })
      }),
      /* @__PURE__ */ jsxs91("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs91("div", {
            style: dialogContent,
            className: VERTICAL_SCROLLBAR_CLASSNAME,
            children: [
              /* @__PURE__ */ jsxs91("dl", {
                style: requestSourceStyle,
                "aria-label": "Request source",
                children: [
                  /* @__PURE__ */ jsx165("dt", {
                    style: sectionTitleStyle,
                    children: "From"
                  }),
                  /* @__PURE__ */ jsx165("dd", {
                    style: sourceIsUnverified ? unverifiedRequestSourceStyle : requestSourceDescriptionStyle,
                    children: sourceLabel
                  })
                ]
              }),
              /* @__PURE__ */ jsxs91("dl", {
                style: {
                  ...requestSourceStyle,
                  visibility: displayedPlan === null ? "hidden" : "visible"
                },
                "aria-hidden": displayedPlan === null,
                children: [
                  /* @__PURE__ */ jsx165("dt", {
                    style: sectionTitleStyle,
                    children: "Destination"
                  }),
                  /* @__PURE__ */ jsx165("dd", {
                    style: requestSourceDescriptionStyle,
                    children: displayedPlan?.filePath ?? " "
                  })
                ]
              }),
              /* @__PURE__ */ jsxs91("div", {
                style: destinationControlStyle,
                children: [
                  /* @__PURE__ */ jsx165("div", {
                    style: sectionTitleStyle,
                    children: "Add to"
                  }),
                  /* @__PURE__ */ jsx165("div", {
                    "aria-label": "Add to",
                    role: "group",
                    style: { marginLeft: "auto" },
                    children: /* @__PURE__ */ jsx165(SegmentedControl, {
                      items: destinationOptions,
                      needsWrapping: false,
                      size: "medium"
                    })
                  })
                ]
              }),
              currentPlan === null ? /* @__PURE__ */ jsxs91("div", {
                style: warningStyle,
                role: "status",
                children: [
                  /* @__PURE__ */ jsx165(WarningTriangle, {
                    style: warningIconStyle
                  }),
                  /* @__PURE__ */ jsxs91("p", {
                    style: warningDescriptionStyle,
                    children: [
                      "Studio could not find a safe place in “",
                      request.compositionId,
                      "” to insert the Element. Install it into a new composition instead."
                    ]
                  })
                ]
              }) : null,
              mode === "new-composition" ? /* @__PURE__ */ jsxs91("section", {
                style: { ...sectionStyle, marginInline: -16 },
                "aria-label": "New composition settings",
                children: [
                  /* @__PURE__ */ jsx165(NewCompositionFields, {
                    compositionId: newCompositionId,
                    heightValidationMessage,
                    inputRef,
                    nameValidationMessage,
                    setValues: setNewCompositionValues,
                    values: newCompositionValues,
                    widthValidationMessage
                  }),
                  activeNewCompositionPlanState?.type === "error" ? /* @__PURE__ */ jsx165(ValidationMessage, {
                    align: "flex-start",
                    message: activeNewCompositionPlanState.reason,
                    type: "error"
                  }) : null
                ]
              }) : null,
              /* @__PURE__ */ jsx165("section", {
                style: sectionStyle,
                "aria-label": "Element implementation",
                children: existingDestination ? /* @__PURE__ */ jsxs91("div", {
                  role: "radiogroup",
                  "aria-label": "Existing Element file",
                  style: sectionStyle,
                  children: [
                    /* @__PURE__ */ jsx165(RadioButton, {
                      checked: !overwriteExisting,
                      disabled: submitting,
                      onClick: () => setInput((previous) => ({ ...previous, overwritePlan: null })),
                      children: "Create a copy"
                    }),
                    !overwriteExisting ? /* @__PURE__ */ jsxs91("div", {
                      style: {
                        ...sectionStyle,
                        paddingLeft: 28,
                        maxWidth: 320,
                        minWidth: 0
                      },
                      children: [
                        /* @__PURE__ */ jsx165(RemotionInput, {
                          rightAlign: false,
                          status: installationNameError ? "error" : "ok",
                          id: "element-install-name",
                          name: "installationName",
                          "aria-label": "Installation name",
                          placeholder: "New name",
                          value: installationName,
                          disabled: submitting,
                          onChange: (event) => {
                            const name = event.target.value;
                            setInput((previous) => ({
                              ...previous,
                              name,
                              overwritePlan: null
                            }));
                          }
                        }),
                        installationNameError ? /* @__PURE__ */ jsx165(ValidationMessage, {
                          align: "flex-start",
                          message: installationNameError,
                          type: "error"
                        }) : null
                      ]
                    }) : null,
                    /* @__PURE__ */ jsxs91("div", {
                      style: sectionStyle,
                      children: [
                        /* @__PURE__ */ jsx165(RadioButton, {
                          checked: overwriteExisting,
                          disabled: submitting,
                          onClick: () => setInput((previous) => ({
                            ...previous,
                            overwritePlan: existingDestination.plan
                          })),
                          children: "Replace existing"
                        }),
                        /* @__PURE__ */ jsx165("p", {
                          style: {
                            ...warningDescriptionStyle,
                            paddingLeft: 28,
                            overflowWrap: "anywhere"
                          },
                          children: existingDestination.plan.filePath
                        }),
                        overwriteExisting ? /* @__PURE__ */ jsx165("p", {
                          style: { ...overwriteStyle, paddingLeft: 28 },
                          children: "Overwrites customizations for all usages."
                        }) : null
                      ]
                    })
                  ]
                }) : !activePlan && planError ? /* @__PURE__ */ jsx165(ValidationMessage, {
                  align: "flex-start",
                  message: planError,
                  type: "error"
                }) : null
              }),
              missingPackages.length > 0 ? /* @__PURE__ */ jsxs91("section", {
                style: requestSourceStyle,
                "aria-labelledby": "element-install-dependencies",
                children: [
                  /* @__PURE__ */ jsx165("h3", {
                    id: "element-install-dependencies",
                    style: sectionTitleStyle,
                    children: "Packages to install"
                  }),
                  /* @__PURE__ */ jsx165("ul", {
                    style: dependencyListStyle,
                    role: "list",
                    children: missingPackages.map((packageName) => /* @__PURE__ */ jsx165("li", {
                      style: dependencyNameStyle,
                      children: packageName
                    }, packageName))
                  })
                ]
              }) : null,
              /* @__PURE__ */ jsxs91("div", {
                style: warningStyle,
                children: [
                  /* @__PURE__ */ jsx165(WarningTriangle, {
                    style: warningIconStyle
                  }),
                  /* @__PURE__ */ jsx165("p", {
                    style: warningDescriptionStyle,
                    children: "Installed code can access your files and network."
                  })
                ]
              }),
              /* @__PURE__ */ jsxs91("details", {
                style: sourceDetailsStyle,
                children: [
                  /* @__PURE__ */ jsx165("summary", {
                    className: `${HOVERABLE_CLASS_NAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
                    style: sourceSummaryStyle,
                    children: "Source code"
                  }),
                  /* @__PURE__ */ jsx165("div", {
                    style: sourceCodeContainerStyle,
                    children: /* @__PURE__ */ jsx165("pre", {
                      className: `language-tsx ${HORIZONTAL_SCROLLBAR_CLASSNAME} ${VERTICAL_SCROLLBAR_CLASSNAME} ${FOCUS_VISIBLE_ONLY_CLASS_NAME}`,
                      style: sourceCodeBlockStyle,
                      children: /* @__PURE__ */ jsx165("code", {
                        style: sourceCodeStyle,
                        children: /* @__PURE__ */ jsx165(LazySyntaxHighlightedSource, {
                          source: sourcePreview
                        })
                      })
                    })
                  })
                ]
              })
            ]
          }),
          /* @__PURE__ */ jsx165(ModalFooterContainer, {
            style: footerStyle,
            children: /* @__PURE__ */ jsxs91(Row, {
              align: "center",
              children: [
                /* @__PURE__ */ jsx165(Flex, {}),
                /* @__PURE__ */ jsx165(Button, {
                  disabled: submitting,
                  onClick: cancel,
                  style: cancelStyle2,
                  children: "Cancel"
                }),
                /* @__PURE__ */ jsx165(Spacing, {
                  x: 1
                }),
                /* @__PURE__ */ jsxs91(ModalButton, {
                  autoFocus: mode === "current-composition",
                  disabled: !canSubmit,
                  onClick: submit,
                  children: [
                    submitting ? "Installing…" : overwriteExisting ? "Replace and insert" : "Install",
                    /* @__PURE__ */ jsx165(ShortcutHint, {
                      keyToPress: "↵",
                      cmdOrCtrl: false
                    })
                  ]
                })
              ]
            })
          })
        ]
      })
    ]
  }), getPortal(currentZIndex));
};

// src/components/ElementLibraryModal.tsx
import { useLayoutEffect as useLayoutEffect27, useRef as useRef55 } from "react";
import { jsx as jsx166, jsxs as jsxs92 } from "react/jsx-runtime";
var panelStyle3 = {
  display: "flex",
  flexDirection: "column",
  height: getMaxModalHeight(1000),
  overflow: "hidden",
  width: getMaxModalWidth(1400)
};
var iframeStyle = {
  border: 0,
  colorScheme: "dark",
  flex: 1,
  minHeight: 0,
  width: "100%"
};
var ElementLibraryModal = ({ name, url }) => {
  const iframeRef = useRef55(null);
  useLayoutEffect27(() => {
    const iframe = iframeRef.current;
    if (iframe === null) {
      return;
    }
    iframe.setAttribute("credentialless", "");
    const iframeUrl = new URL(url);
    iframeUrl.searchParams.set("remotion-studio", "true");
    iframeUrl.searchParams.set("docusaurus-theme", "dark");
    iframe.src = iframeUrl.toString();
  }, [url]);
  return /* @__PURE__ */ jsxs92(DismissableModal, {
    panelStyle: panelStyle3,
    children: [
      /* @__PURE__ */ jsx166(ModalHeader, {
        title: name
      }),
      /* @__PURE__ */ jsx166("iframe", {
        ref: iframeRef,
        allow: "local-network-access; loopback-network",
        "data-remotion-element-library": "",
        style: iframeStyle,
        title: `${name} library`
      })
    ]
  });
};

// src/components/FixComputedValueModal.tsx
import { useContext as useContext105, useEffect as useEffect69, useState as useState65 } from "react";

// src/components/AgentPrompt.tsx
import { useCallback as useCallback85, useContext as useContext104 } from "react";

// src/icons/skills.tsx
import { jsx as jsx167, jsxs as jsxs93 } from "react/jsx-runtime";
var SkillsIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsxs93("svg", {
    ...props,
    fill: "none",
    viewBox: "0 0 16 16",
    children: [
      /* @__PURE__ */ jsx167("path", {
        d: "M8 0.9 14.05 4.35v7.3L8 15.1 1.95 11.65v-7.3L8 0.9Z",
        stroke: color,
        strokeLinejoin: "round",
        strokeWidth: "0.85"
      }),
      /* @__PURE__ */ jsx167("path", {
        d: "m1.95 4.35 6.05 3.55 6.05-3.55M8 7.9v7.2M1.95 8 8 11.55 14.05 8",
        stroke: color,
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "0.85"
      })
    ]
  });
};

// src/components/SkillsSettings.tsx
import { useCallback as useCallback84, useContext as useContext103, useMemo as useMemo96 } from "react";
import { jsx as jsx168, jsxs as jsxs94 } from "react/jsx-runtime";
var INSTALL_COMMAND = "npx remotion skills add";
var container32 = {
  alignSelf: "flex-start",
  boxSizing: "border-box",
  flex: 1,
  fontFamily: "sans-serif",
  minWidth: 0,
  padding: "16px 16px 0"
};
var description = {
  color: LIGHT_TEXT,
  fontSize: 13,
  lineHeight: 1.5,
  margin: 0
};
var commandField = {
  alignItems: "center",
  backgroundColor: "rgba(0, 0, 0, 0.22)",
  borderRadius: 4,
  display: "flex",
  marginTop: 8,
  padding: "5px 6px 5px 9px"
};
var command = {
  color: WHITE,
  flex: 1,
  fontFamily: "monospace",
  fontSize: 13,
  lineHeight: "24px",
  margin: 0,
  minWidth: 0,
  overflowX: "auto",
  whiteSpace: "pre"
};
var copyIcon = {
  height: 12,
  width: 12
};
var list2 = {
  marginTop: 14
};
var skillRow = {
  alignItems: "center",
  borderBottom: BORDER_WHITE_ALPHA_12,
  display: "flex",
  gap: 10,
  minHeight: 38,
  padding: "0 10px"
};
var lastSkillRow = {
  ...skillRow,
  borderBottom: "none"
};
var statusIcon = {
  flexShrink: 0,
  height: 14,
  width: 14
};
var skillName = {
  color: WHITE,
  flex: 1,
  fontFamily: "monospace",
  fontSize: 13,
  lineHeight: 1.4,
  minWidth: 0,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var status = {
  color: LIGHT_TEXT,
  fontSize: 12,
  lineHeight: 1.4,
  whiteSpace: "nowrap"
};
var actionIcon = {
  height: 14,
  width: 14
};
var actionSlot = {
  alignItems: "center",
  display: "inline-flex",
  flexShrink: 0,
  height: 24,
  justifyContent: "center",
  width: 24
};
var loading = {
  ...description,
  marginTop: 14
};
var SkillSettingsRow = ({ isLast, skill }) => {
  const { installSkill, removeSkill, skillAction } = useSettings();
  const { previewServerState } = useContext103(StudioServerConnectionCtx);
  const canInstall = !window.remotion_isReadOnlyStudio && previewServerState.type === "connected";
  const installed = skill.installedInProject || skill.installedGlobally;
  const processingThisSkill = skillAction?.skill === skill.name;
  const installedLocation = skill.installedInProject && skill.installedGlobally ? "Project and global" : skill.installedInProject ? "Project" : skill.installedGlobally ? "Global" : null;
  const renderInstallAction = useCallback84((color) => {
    return /* @__PURE__ */ jsx168(CloudDownloadIcon, {
      color,
      style: actionIcon
    });
  }, []);
  const renderRemoveAction = useCallback84((color) => {
    return /* @__PURE__ */ jsx168(TrashIcon, {
      color,
      style: actionIcon
    });
  }, []);
  return /* @__PURE__ */ jsxs94("div", {
    role: "listitem",
    style: isLast ? lastSkillRow : skillRow,
    children: [
      /* @__PURE__ */ jsxs94("span", {
        style: skillName,
        children: [
          "/",
          skill.name
        ]
      }),
      processingThisSkill ? /* @__PURE__ */ jsx168("span", {
        style: status,
        children: skillAction.type === "installing" ? "Installing…" : "Removing…"
      }) : installedLocation ? /* @__PURE__ */ jsx168("span", {
        style: status,
        children: installedLocation
      }) : null,
      installed ? /* @__PURE__ */ jsx168(CheckCircleFilled, {
        "aria-hidden": true,
        style: { ...statusIcon, fill: BLUE }
      }) : null,
      processingThisSkill ? /* @__PURE__ */ jsx168("span", {
        style: actionSlot,
        children: /* @__PURE__ */ jsx168(Spinner, {
          duration: 0.5,
          size: 14
        })
      }) : installed && canInstall ? /* @__PURE__ */ jsx168(InlineAction, {
        title: skill.installedInProject ? `Remove ${skill.name} from this project` : `Remove ${skill.name} globally`,
        disabled: skillAction !== null,
        onClick: () => removeSkill(skill.name),
        renderAction: renderRemoveAction,
        variant: null
      }) : canInstall ? /* @__PURE__ */ jsx168(InlineAction, {
        title: `Install ${skill.name} in this project`,
        disabled: skillAction !== null,
        onClick: () => installSkill(skill.name),
        renderAction: renderInstallAction,
        variant: null
      }) : null
    ]
  });
};
var SkillsSettings = () => {
  const { error, remotionSkillsInfo, skillActionError } = useSettings();
  const { previewServerState } = useContext103(StudioServerConnectionCtx);
  const canInstall = !window.remotion_isReadOnlyStudio && previewServerState.type === "connected";
  const { copied, markCopied } = useCopyFeedback();
  const installedSkills = useMemo96(() => {
    return remotionSkillsInfo?.skills.filter(({ installedGlobally, installedInProject }) => installedGlobally || installedInProject).length ?? 0;
  }, [remotionSkillsInfo]);
  const missingSkills = (remotionSkillsInfo?.skills.length ?? 0) - installedSkills;
  const onCopy = useCallback84(() => {
    copyText(INSTALL_COMMAND).then(markCopied).catch((err) => {
      showNotification(`Could not copy: ${err.message}`, 2000);
    });
  }, [markCopied]);
  const renderCopyAction = useCallback84((color) => {
    return /* @__PURE__ */ jsx168(CopyIcon, {
      copied,
      color,
      style: copyIcon
    });
  }, [copied]);
  return /* @__PURE__ */ jsxs94("div", {
    style: container32,
    children: [
      remotionSkillsInfo === null && error === null ? /* @__PURE__ */ jsx168("p", {
        style: loading,
        children: "Checking installed skills..."
      }) : null,
      remotionSkillsInfo === null && error ? /* @__PURE__ */ jsx168("div", {
        style: { marginTop: 14 },
        children: /* @__PURE__ */ jsx168(ValidationMessage, {
          message: error,
          align: "flex-start",
          type: "error"
        })
      }) : null,
      missingSkills > 0 && canInstall ? /* @__PURE__ */ jsx168("p", {
        style: description,
        children: "Install skills in this project to use them with your coding agent."
      }) : null,
      skillActionError ? /* @__PURE__ */ jsx168("div", {
        style: { marginTop: 14 },
        children: /* @__PURE__ */ jsx168(ValidationMessage, {
          message: skillActionError,
          align: "flex-start",
          type: "error"
        })
      }) : null,
      missingSkills > 0 && !canInstall ? /* @__PURE__ */ jsxs94("div", {
        children: [
          /* @__PURE__ */ jsx168("p", {
            style: description,
            children: "Not all skills are installed. Run this command in the project directory, then reload Studio and restart your coding agent."
          }),
          /* @__PURE__ */ jsxs94("div", {
            style: commandField,
            children: [
              /* @__PURE__ */ jsx168("pre", {
                style: command,
                children: INSTALL_COMMAND
              }),
              /* @__PURE__ */ jsx168(InlineAction, {
                variant: null,
                onClick: onCopy,
                renderAction: renderCopyAction,
                title: "Copy install command"
              })
            ]
          })
        ]
      }) : null,
      remotionSkillsInfo ? /* @__PURE__ */ jsx168("div", {
        style: list2,
        role: "list",
        "aria-label": "Remotion Agent Skills",
        children: remotionSkillsInfo.skills.map((skill, index) => {
          return /* @__PURE__ */ jsx168(SkillSettingsRow, {
            isLast: index === remotionSkillsInfo.skills.length - 1,
            skill
          }, skill.name);
        })
      }) : null
    ]
  });
};

// src/components/AgentPrompt.tsx
import { jsx as jsx169, jsxs as jsxs95, Fragment as Fragment41 } from "react/jsx-runtime";
var text2 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5
};
var commandField2 = {
  alignItems: "flex-start",
  background: BLACK_ALPHA_30,
  borderRadius: 6,
  boxSizing: "border-box",
  display: "flex",
  gap: 8,
  marginTop: 10,
  padding: "8px 8px 8px 10px",
  width: "100%"
};
var code = {
  color: WHITE,
  flex: 1,
  fontFamily: "monospace",
  fontSize: 13,
  fontWeight: "normal",
  lineHeight: 1.5,
  margin: 0,
  minWidth: 0,
  overflowWrap: "anywhere",
  whiteSpace: "pre-wrap"
};
var copyAction = { flexShrink: 0 };
var copyIcon2 = { flexShrink: 0, height: 12, width: 12 };
var skillsIcon = {
  display: "inline-block",
  height: 18,
  marginRight: 6,
  verticalAlign: "middle",
  width: 18
};
var promptHeader = {
  alignItems: "center",
  display: "flex",
  gap: 12,
  justifyContent: "space-between"
};
var skillRowContainer = {
  borderTop: BORDER_WHITE_ALPHA_12,
  marginTop: 10
};
var skillError = { marginTop: 10 };
var AgentPrompt = ({ availableText, promptDetails, skillId }) => {
  const { codingAgentInfo, remotionSkillsInfo, skillActionError } = useSettings();
  const { previewServerState } = useContext104(StudioServerConnectionCtx);
  const skill = remotionSkillsInfo?.skills.find(({ name }) => name === skillId);
  const skillAvailable = Boolean(skill?.installedInProject || skill?.installedGlobally);
  const canInstallSkill = !window.remotion_isReadOnlyStudio && previewServerState.type === "connected" && remotionSkillsInfo !== null;
  const skillName2 = `/${skillId}`;
  const prompt = `${skillName2}${promptDetails}`;
  const installCommand = "npx remotion skills add";
  const hasCodingAgent = (codingAgentInfo?.installedCodingAgents.length ?? 0) > 0;
  const { copied: promptCopied, markCopied: markPromptCopied } = useCopyFeedback();
  const { copied: installCopied, markCopied: markInstallCopied } = useCopyFeedback();
  const copy = useCallback85((value, markCopied) => {
    copyText(value).then(markCopied).catch((err) => showNotification(`Could not copy: ${err.message}`, 2000));
  }, []);
  const renderPromptCopy = useCallback85((color) => /* @__PURE__ */ jsx169(CopyIcon, {
    copied: promptCopied,
    color,
    style: copyIcon2
  }), [promptCopied]);
  const renderInstallCopy = useCallback85((color) => /* @__PURE__ */ jsx169(CopyIcon, {
    copied: installCopied,
    color,
    style: copyIcon2
  }), [installCopied]);
  return /* @__PURE__ */ jsxs95(Fragment41, {
    children: [
      skillAvailable ? null : /* @__PURE__ */ jsxs95(Fragment41, {
        children: [
          /* @__PURE__ */ jsx169("div", {
            style: text2,
            children: "Install the Agent Skill first:"
          }),
          canInstallSkill && skill ? /* @__PURE__ */ jsx169("div", {
            "aria-label": "Remotion Agent Skills",
            role: "list",
            style: skillRowContainer,
            children: /* @__PURE__ */ jsx169(SkillSettingsRow, {
              isLast: false,
              skill
            })
          }) : /* @__PURE__ */ jsxs95("div", {
            style: commandField2,
            children: [
              /* @__PURE__ */ jsx169("pre", {
                style: code,
                children: installCommand
              }),
              /* @__PURE__ */ jsx169(InlineAction, {
                onClick: () => copy(installCommand, markInstallCopied),
                renderAction: renderInstallCopy,
                style: copyAction,
                title: "Copy command",
                variant: null
              })
            ]
          }),
          skillActionError ? /* @__PURE__ */ jsx169("div", {
            style: skillError,
            children: /* @__PURE__ */ jsx169(ValidationMessage, {
              align: "flex-start",
              message: skillActionError,
              type: "error"
            })
          }) : null
        ]
      }),
      /* @__PURE__ */ jsxs95("div", {
        style: { ...promptHeader, marginTop: skillAvailable ? 0 : 16 },
        children: [
          /* @__PURE__ */ jsx169("div", {
            style: text2,
            children: skillAvailable ? availableText : `Then ${availableText.toLowerCase()}`
          }),
          hasCodingAgent ? /* @__PURE__ */ jsx169(CodingAgentButton, {
            label: "Open in",
            prompt,
            size: "compact",
            style: null
          }) : null
        ]
      }),
      /* @__PURE__ */ jsxs95("div", {
        style: commandField2,
        children: [
          /* @__PURE__ */ jsxs95("pre", {
            style: code,
            children: [
              /* @__PURE__ */ jsx169(SkillsIcon, {
                color: BLUE,
                style: skillsIcon,
                "aria-hidden": true
              }),
              /* @__PURE__ */ jsx169("span", {
                style: {
                  color: BLUE,
                  fontFamily: "inherit",
                  fontSize: "inherit",
                  fontWeight: "inherit",
                  lineHeight: "inherit"
                },
                children: skillName2
              }),
              promptDetails
            ]
          }),
          /* @__PURE__ */ jsx169(InlineAction, {
            onClick: () => copy(prompt, markPromptCopied),
            renderAction: renderPromptCopy,
            style: copyAction,
            title: "Copy prompt",
            variant: null
          })
        ]
      })
    ]
  });
};

// src/components/FixComputedValueModal.tsx
import { jsx as jsx170, jsxs as jsxs96 } from "react/jsx-runtime";
var panelStyle4 = {
  borderRadius: 6,
  display: "flex",
  flexDirection: "column",
  width: getMaxModalWidth(560),
  maxHeight: getMaxModalHeight(800),
  minWidth: 0,
  overflow: "hidden"
};
var container33 = {
  padding: 16,
  minHeight: 0,
  overflowY: "auto"
};
var text3 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5
};
var sourcePreviewContainer = {
  marginBottom: 16,
  marginTop: 4
};
var sourcePreviewStatus = {
  ...text3,
  marginBottom: 16,
  marginTop: 10
};
var FixComputedValueModal = ({ state }) => {
  const { previewServerState } = useContext105(StudioServerConnectionCtx);
  const { canOpenInEditor, defaultEditorId, defaultEditorName } = useEditorOpening(previewServerState.type === "connected");
  const [sourcePreview, setSourcePreview] = useState65({
    type: "loading"
  });
  const promptLocation = sourcePreview.type === "loaded" ? `${sourcePreview.stack.originalFileName}:${sourcePreview.stack.originalLineNumber}:${sourcePreview.stack.originalColumnNumber}` : `${state.location.source}:${state.location.line}:${state.location.column}`;
  const promptDetails = ` ${promptLocation} make "${state.prop}" interactive`;
  const canOpenSourceInEditor = sourcePreview.type === "loaded" && canOpenInEditor && defaultEditorId !== null && defaultEditorName !== null;
  useEffect69(() => {
    let cancelled = false;
    setSourcePreview({ type: "loading" });
    findOriginalPositionInFileAtProperty({
      originalPosition: state.location,
      property: state.prop.split(".").at(-1) ?? state.prop
    }).then((position) => {
      return resolveFileSource({
        columnNumber: position.column,
        fileName: position.source,
        lineNumber: position.line,
        message: `Computed value "${state.prop}"`
      }, 1);
    }).then((stack2) => {
      if (!cancelled) {
        setSourcePreview({ type: "loaded", stack: stack2 });
      }
    }).catch((err) => {
      if (!cancelled) {
        setSourcePreview({
          type: "error",
          message: err instanceof Error ? err.message : String(err)
        });
      }
    });
    return () => {
      cancelled = true;
    };
  }, [state.location, state.prop]);
  return /* @__PURE__ */ jsxs96(DismissableModal, {
    panelStyle: panelStyle4,
    children: [
      /* @__PURE__ */ jsx170(ModalHeader, {
        title: "Fix computed value"
      }),
      /* @__PURE__ */ jsxs96("div", {
        style: container33,
        children: [
          /* @__PURE__ */ jsx170("div", {
            style: text3,
            children: "This value cannot be interactively edited because it is computed:"
          }),
          sourcePreview.type === "loaded" ? /* @__PURE__ */ jsx170("div", {
            style: sourcePreviewContainer,
            children: /* @__PURE__ */ jsx170(StackElement, {
              collapsible: false,
              defaultFunctionName: null,
              editorId: canOpenInEditor ? defaultEditorId : null,
              fontSize: 13,
              headerAction: canOpenSourceInEditor ? /* @__PURE__ */ jsx170(OpenInEditor, {
                canHaveKeyboardShortcuts: false,
                editorId: defaultEditorId,
                editorName: defaultEditorName,
                size: "compact",
                stack: sourcePreview.stack
              }) : null,
              horizontalSpacing: 0,
              isFirst: true,
              lineNumberWidth: String(Math.max(0, ...(sourcePreview.stack.originalScriptCode ?? []).map((line2) => line2.lineNumber))).length,
              s: sourcePreview.stack
            })
          }) : /* @__PURE__ */ jsx170("div", {
            style: sourcePreviewStatus,
            children: sourcePreview.type === "loading" ? "Loading code preview..." : `Could not load code preview: ${sourcePreview.message}`
          }),
          /* @__PURE__ */ jsx170(AgentPrompt, {
            availableText: "Use this prompt to make this value editable:",
            promptDetails,
            skillId: "remotion-interactivity"
          })
        ]
      })
    ]
  });
};

// src/components/GenerateWithAgentModal.tsx
import { jsx as jsx171, jsxs as jsxs97 } from "react/jsx-runtime";
var panelStyle5 = {
  borderRadius: 6,
  display: "flex",
  flexDirection: "column",
  maxHeight: getMaxModalHeight(800),
  minWidth: 0,
  overflow: "hidden",
  width: getMaxModalWidth(560)
};
var container34 = { padding: 16 };
var text4 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5,
  marginBottom: 16
};
var GenerateWithAgentModal = ({
  state
}) => {
  const location2 = formatFileLocation({
    location: state.location,
    root: window.remotion_cwd
  });
  return /* @__PURE__ */ jsxs97(DismissableModal, {
    panelStyle: panelStyle5,
    children: [
      /* @__PURE__ */ jsx171(ModalHeader, {
        title: "Generate with agent"
      }),
      /* @__PURE__ */ jsxs97("div", {
        style: container34,
        children: [
          /* @__PURE__ */ jsx171("div", {
            style: text4,
            children: "Ask a coding agent to generate content in this composition."
          }),
          /* @__PURE__ */ jsx171(AgentPrompt, {
            availableText: "Start your prompt with:",
            promptDetails: location2 ? ` ${location2}` : "",
            skillId: "remotion-markup"
          })
        ]
      })
    ]
  });
};

// src/components/NewComposition/DeleteComposition.tsx
import { useCallback as useCallback86, useContext as useContext107, useMemo as useMemo98, useRef as useRef56 } from "react";
import { Internals as Internals99 } from "remotion";

// src/components/RenderModal/ResolveCompositionBeforeModal.tsx
import React121, { useContext as useContext106, useEffect as useEffect70, useMemo as useMemo97 } from "react";
import { Internals as Internals98 } from "remotion";
import { jsx as jsx172, jsxs as jsxs98 } from "react/jsx-runtime";
var loaderContainer2 = {
  paddingTop: 40,
  paddingBottom: 40,
  paddingLeft: 100,
  paddingRight: 100,
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  flexDirection: "column"
};
var loaderLabel2 = {
  fontSize: 14,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  lineHeight: 1.5
};
var ResolvedCompositionContext = React121.createContext(null);
var ResolveCompositionBeforeModal = ({ compositionId, children }) => {
  const resolved = Internals98.useResolvedVideoConfig(compositionId);
  const unresolvedContext = useContext106(Internals98.CompositionManager);
  const unresolved = unresolvedContext.compositions.find((c) => compositionId === c.id);
  useEffect70(() => {
    const { current } = Internals98.resolveCompositionsRef;
    if (!current) {
      throw new Error("No ref to trigger composition calc");
    }
    current.setCurrentRenderModalComposition(compositionId);
    return () => {
      current.setCurrentRenderModalComposition(null);
    };
  }, [compositionId]);
  if (!unresolved) {
    throw new Error("Composition not found: " + compositionId);
  }
  const value = useMemo97(() => {
    return {
      resolved,
      unresolved
    };
  }, [resolved, unresolved]);
  if (!resolved || resolved.type === "loading") {
    return /* @__PURE__ */ jsx172(RunningCalculateMetadata, {});
  }
  if (resolved.type === "error") {
    return /* @__PURE__ */ jsxs98("div", {
      style: loaderContainer2,
      children: [
        /* @__PURE__ */ jsx172(Spacing, {
          y: 2
        }),
        /* @__PURE__ */ jsxs98("div", {
          style: loaderLabel2,
          children: [
            "Running ",
            /* @__PURE__ */ jsx172("code", {
              style: inlineCodeSnippet,
              children: "calculateMetadata()"
            }),
            " ",
            "yielded an error:"
          ]
        }),
        /* @__PURE__ */ jsx172(Spacing, {
          y: 1
        }),
        /* @__PURE__ */ jsx172("div", {
          style: loaderLabel2,
          children: resolved.error.message || "Unknown error"
        })
      ]
    });
  }
  return /* @__PURE__ */ jsx172(ResolvedCompositionContext.Provider, {
    value,
    children
  });
};

// src/components/NewComposition/DeleteComposition.tsx
import { jsx as jsx173, jsxs as jsxs99, Fragment as Fragment42 } from "react/jsx-runtime";
var content6 = {
  padding: 16,
  fontSize: 14,
  flex: 1,
  minWidth: 500
};
var DeleteCompositionLoaded = ({ compositionId }) => {
  const context = useContext107(ResolvedCompositionContext);
  if (!context) {
    throw new Error("Resolved composition context");
  }
  const { unresolved } = context;
  const compositionStack = unresolved.stack ?? null;
  const { compositions } = useContext107(Internals99.CompositionManager);
  const { setCanvasContent } = useContext107(Internals99.CompositionSetters);
  const selectComposition = useSelectComposition();
  const navigationAfterDelete = useRef56(undefined);
  const codemod = useMemo98(() => {
    return {
      type: "delete-composition",
      idToDelete: compositionId
    };
  }, [compositionId]);
  const onSubmit = useCallback86((e) => {
    e.preventDefault();
  }, []);
  const onSuccess = useCallback86(() => {
    if (navigationAfterDelete.current === undefined) {
      return;
    }
    if (navigationAfterDelete.current === null) {
      pushUrl("/");
      setCanvasContent(null);
      return;
    }
    selectComposition(navigationAfterDelete.current, true);
  }, [selectComposition, setCanvasContent]);
  return /* @__PURE__ */ jsxs99(Fragment42, {
    children: [
      /* @__PURE__ */ jsx173(ModalHeader, {
        title: "Delete composition"
      }),
      /* @__PURE__ */ jsxs99("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs99("div", {
            style: content6,
            children: [
              "Do you want to delete the",
              " ",
              /* @__PURE__ */ jsx173("code", {
                style: inlineCodeSnippet,
                children: unresolved.durationInFrames === 1 ? `<Still>` : "<Composition>"
              }),
              " ",
              "with ID ",
              '"',
              unresolved.id,
              '"',
              "?",
              /* @__PURE__ */ jsx173("br", {}),
              "The associated ",
              /* @__PURE__ */ jsx173("code", {
                style: inlineCodeSnippet,
                children: "component"
              }),
              " will remain in your code."
            ]
          }),
          /* @__PURE__ */ jsx173(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx173(CodemodFooter, {
              errorNotification: `Could not delete composition`,
              loadingNotification: "Deleting",
              genericSubmitLabel: `Delete`,
              submitLabel: ({ relativeRootPath }) => `Delete from ${relativeRootPath}`,
              codemod,
              stack: compositionStack,
              valid: true,
              onSuccess,
              applyCodemod: ({ signal, symbolicatedStack }) => {
                const currentRoute = getRoute();
                const currentCanvasContent = deriveCanvasContentFromUrl();
                const isSelected = currentCanvasContent?.type === "composition" && currentCanvasContent.compositionId === compositionId;
                const fallback = isSelected ? compositions.find(({ id }) => id !== compositionId) ?? null : undefined;
                navigationAfterDelete.current = fallback;
                return applyCodemod({
                  codemod,
                  dryRun: false,
                  signal,
                  symbolicatedStack,
                  undoRedoNavigation: fallback === undefined ? null : {
                    undoRoute: currentRoute,
                    redoRoute: fallback === null ? "/" : `/${fallback.id}`
                  }
                });
              },
              applyCodemodForPreview: null
            })
          })
        ]
      })
    ]
  });
};
var DeleteComposition = ({ compositionId }) => {
  return /* @__PURE__ */ jsx173(DismissableModal, {
    children: /* @__PURE__ */ jsx173(ResolveCompositionBeforeModal, {
      compositionId,
      children: /* @__PURE__ */ jsx173(DeleteCompositionLoaded, {
        compositionId
      })
    })
  });
};

// src/components/NewComposition/DeleteFolder.tsx
import { useCallback as useCallback87, useMemo as useMemo99 } from "react";
import { jsx as jsx174, jsxs as jsxs100 } from "react/jsx-runtime";
var content7 = {
  padding: 16,
  fontSize: 14,
  flex: 1,
  minWidth: 500
};
var DeleteFolder = ({ folderName, parentName, stack: stack2 }) => {
  const codemod = useMemo99(() => {
    return {
      type: "delete-folder",
      folderName,
      parentName
    };
  }, [folderName, parentName]);
  const onSubmit = useCallback87((e) => {
    e.preventDefault();
  }, []);
  const folderId = getFolderId({ folderName, parentName });
  return /* @__PURE__ */ jsxs100(DismissableModal, {
    children: [
      /* @__PURE__ */ jsx174(ModalHeader, {
        title: "Delete folder"
      }),
      /* @__PURE__ */ jsxs100("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs100("div", {
            style: content7,
            children: [
              "Do you want to delete the",
              " ",
              /* @__PURE__ */ jsx174("code", {
                style: inlineCodeSnippet,
                children: "Folder"
              }),
              " with ID ",
              '"',
              folderId,
              '"',
              "?",
              /* @__PURE__ */ jsx174("br", {}),
              "The compositions and nested folders inside it will stay in your code."
            ]
          }),
          /* @__PURE__ */ jsx174(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx174(CodemodFooter, {
              errorNotification: `Could not delete folder`,
              loadingNotification: "Deleting folder",
              genericSubmitLabel: `Delete`,
              submitLabel: ({ relativeRootPath }) => `Delete from ${relativeRootPath}`,
              codemod,
              stack: stack2,
              valid: true,
              onSuccess: null,
              applyCodemod: ({ signal, symbolicatedStack }) => applyCodemod({
                codemod,
                dryRun: false,
                signal,
                symbolicatedStack,
                undoRedoNavigation: null
              }),
              applyCodemodForPreview: null
            })
          })
        ]
      })
    ]
  });
};

// src/components/NewComposition/DuplicateComposition.tsx
import { useCallback as useCallback88, useContext as useContext108, useMemo as useMemo100, useState as useState66 } from "react";
import { Internals as Internals100 } from "remotion";

// src/components/NewComposition/duplicate-composition-api.ts
var duplicateComposition = ({
  codemod,
  dryRun,
  signal,
  symbolicatedStack,
  undoRedoNavigation
}) => {
  const browserStudioOperations = getBrowserStudioOperations();
  if (browserStudioOperations === null) {
    return applyCodemod({
      codemod,
      dryRun,
      signal,
      symbolicatedStack,
      undoRedoNavigation
    }).then((result) => result.success ? result : { ...result, stack: "" });
  }
  return browserStudioOperations.duplicateComposition({
    codemod,
    dryRun,
    undoRedoNavigation
  });
};

// src/components/NewComposition/DuplicateComposition.tsx
import { jsx as jsx175, jsxs as jsxs101, Fragment as Fragment43 } from "react/jsx-runtime";
var content8 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var comboBoxStyle = {
  width: 190
};
var DuplicateCompositionLoaded = ({ initialType }) => {
  const context = useContext108(ResolvedCompositionContext);
  if (!context) {
    throw new Error("Resolved composition context");
  }
  const { resolved, unresolved } = context;
  const compositionStack = unresolved.stack ?? null;
  const [initialCompType] = useState66(initialType);
  const hadDimensionsDefined = unresolved.width && unresolved.height;
  const hadFpsDefined = unresolved.fps !== undefined;
  const hadDurationDefined = unresolved.durationInFrames !== undefined;
  const [selectedFrameRate, setFrameRate] = useState66(resolved.result.fps);
  const { compositions } = useContext108(Internals100.CompositionManager);
  const [type, setType] = useState66(initialCompType);
  const [newId, setName] = useState66(() => {
    const numberAtEnd = resolved.result.id.match(/([0-9]+)$/)?.[0];
    let prefix = numberAtEnd ? Number(numberAtEnd) : 1;
    const initialName = resolved.result.id.replace(/([0-9]+)$/, "");
    let currentName = initialName;
    while (true) {
      currentName = initialName + prefix;
      const err = validateCompositionName(currentName, compositions);
      if (!err) {
        break;
      }
      prefix++;
    }
    return currentName;
  });
  const [size3, setSize] = useState66(() => ({
    width: resolved.result.width,
    height: resolved.result.height
  }));
  const [durationInFrames, setDurationInFrames] = useState66(resolved.result.durationInFrames);
  const onTypeChanged = useCallback88((newType) => {
    setType(newType);
  }, []);
  const onWidthChanged = useCallback88((newValue) => {
    setSize((s) => {
      const { height } = s;
      const newWidth = Number(newValue);
      return {
        height,
        width: newWidth
      };
    });
  }, []);
  const onWidthDirectlyChanged = useCallback88((newWidth) => {
    setSize((s) => {
      const { height } = s;
      return {
        height,
        width: newWidth
      };
    });
  }, []);
  const onHeightDirectlyChanged = useCallback88((newHeight) => {
    setSize((s) => {
      const { width: width2 } = s;
      return {
        width: width2,
        height: newHeight
      };
    });
  }, []);
  const onHeightChanged = useCallback88((newValue) => {
    setSize((s) => {
      const { width: width2 } = s;
      const newHeight = Number(newValue);
      return {
        width: width2,
        height: newHeight
      };
    });
  }, []);
  const onNameChange = useCallback88((e) => {
    setName(e.target.value);
  }, []);
  const onTextFpsChange = useCallback88((newFps) => {
    setFrameRate(Number(newFps));
  }, []);
  const onFpsChange = useCallback88((newFps) => {
    setFrameRate(newFps);
  }, []);
  const compNameErrMessage = validateCompositionName(newId, compositions);
  const compWidthErrMessage = validateCompositionDimension("Width", size3.width);
  const compHeightErrMessage = validateCompositionDimension("Height", size3.height);
  const typeValues = useMemo100(() => {
    return [
      {
        id: "composition",
        keyHint: null,
        label: "<Composition />",
        leftItem: null,
        onClick: () => onTypeChanged("composition"),
        subMenu: null,
        type: "item",
        value: "composition",
        quickSwitcherLabel: null
      },
      {
        id: "still",
        keyHint: null,
        label: "<Still />",
        leftItem: null,
        onClick: () => onTypeChanged("still"),
        subMenu: null,
        type: "item",
        value: "still",
        quickSwitcherLabel: null
      }
    ];
  }, [onTypeChanged]);
  const valid = compNameErrMessage === null && compWidthErrMessage === null && compHeightErrMessage === null;
  const codemod = useMemo100(() => {
    return {
      type: "duplicate-composition",
      idToDuplicate: resolved.result.id,
      newDurationInFrames: hadDurationDefined ? Number(durationInFrames) : null,
      newFps: hadFpsDefined ? Number(selectedFrameRate) : null,
      newHeight: hadDimensionsDefined ? Number(size3.height) : null,
      newWidth: hadDimensionsDefined ? Number(size3.width) : null,
      newId,
      tag: type === "still" ? "Still" : "Composition"
    };
  }, [
    durationInFrames,
    hadDimensionsDefined,
    hadDurationDefined,
    hadFpsDefined,
    newId,
    resolved.result.id,
    selectedFrameRate,
    size3.height,
    size3.width,
    type
  ]);
  const onDuplicateSuccess = useCallback88(() => {
    pushUrl(`/${newId}`);
  }, [newId]);
  const onSubmit = useCallback88((e) => {
    e.preventDefault();
  }, []);
  return /* @__PURE__ */ jsxs101(Fragment43, {
    children: [
      /* @__PURE__ */ jsx175(ModalHeader, {
        title: `Duplicate ${resolved.result.id}`
      }),
      /* @__PURE__ */ jsxs101("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs101("div", {
            style: content8,
            children: [
              initialCompType === "composition" ? /* @__PURE__ */ jsxs101("div", {
                style: optionRow,
                children: [
                  /* @__PURE__ */ jsx175("div", {
                    style: label,
                    children: "Type"
                  }),
                  /* @__PURE__ */ jsx175("div", {
                    style: rightRow,
                    children: /* @__PURE__ */ jsx175(Combobox, {
                      title: "Type of composition",
                      style: comboBoxStyle,
                      values: typeValues,
                      selectedId: type
                    })
                  })
                ]
              }) : null,
              /* @__PURE__ */ jsxs101("div", {
                style: optionRow,
                children: [
                  /* @__PURE__ */ jsx175("div", {
                    style: label,
                    children: "ID"
                  }),
                  /* @__PURE__ */ jsx175("div", {
                    style: rightRow,
                    children: /* @__PURE__ */ jsxs101(InputAndValidationContainer, {
                      children: [
                        /* @__PURE__ */ jsx175(RemotionInput, {
                          value: newId,
                          onChange: onNameChange,
                          type: "text",
                          autoFocus: true,
                          placeholder: "Composition ID",
                          status: "ok",
                          rightAlign: true
                        }),
                        compNameErrMessage ? /* @__PURE__ */ jsxs101(Fragment43, {
                          children: [
                            /* @__PURE__ */ jsx175(Spacing, {
                              y: 1,
                              block: true
                            }),
                            /* @__PURE__ */ jsx175(ValidationMessage, {
                              align: "flex-start",
                              message: compNameErrMessage,
                              type: "error"
                            })
                          ]
                        }) : null
                      ]
                    })
                  })
                ]
              }),
              hadDimensionsDefined ? /* @__PURE__ */ jsxs101(Fragment43, {
                children: [
                  /* @__PURE__ */ jsxs101("div", {
                    style: optionRow,
                    children: [
                      /* @__PURE__ */ jsx175("div", {
                        style: label,
                        children: "Width"
                      }),
                      /* @__PURE__ */ jsx175("div", {
                        style: rightRow,
                        children: /* @__PURE__ */ jsxs101(InputAndValidationContainer, {
                          children: [
                            /* @__PURE__ */ jsx175(InputDragger, {
                              type: "number",
                              value: size3.width,
                              placeholder: "Width",
                              onTextChange: onWidthChanged,
                              name: "width",
                              step: 2,
                              min: 2,
                              required: true,
                              status: "ok",
                              formatter: (w) => `${w}px`,
                              max: 1e8,
                              onValueChange: onWidthDirectlyChanged,
                              rightAlign: false
                            }),
                            compWidthErrMessage ? /* @__PURE__ */ jsxs101(Fragment43, {
                              children: [
                                /* @__PURE__ */ jsx175(Spacing, {
                                  y: 1,
                                  block: true
                                }),
                                /* @__PURE__ */ jsx175(ValidationMessage, {
                                  align: "flex-start",
                                  message: compWidthErrMessage,
                                  type: "error"
                                })
                              ]
                            }) : null
                          ]
                        })
                      })
                    ]
                  }),
                  /* @__PURE__ */ jsxs101("div", {
                    style: optionRow,
                    children: [
                      /* @__PURE__ */ jsx175("div", {
                        style: label,
                        children: "Height"
                      }),
                      /* @__PURE__ */ jsx175("div", {
                        style: rightRow,
                        children: /* @__PURE__ */ jsxs101(InputAndValidationContainer, {
                          children: [
                            /* @__PURE__ */ jsx175(InputDragger, {
                              type: "number",
                              value: size3.height,
                              onTextChange: onHeightChanged,
                              placeholder: "Height",
                              name: "height",
                              step: 2,
                              required: true,
                              formatter: (h) => `${h}px`,
                              min: 2,
                              status: "ok",
                              max: 1e8,
                              onValueChange: onHeightDirectlyChanged,
                              rightAlign: false
                            }),
                            compHeightErrMessage ? /* @__PURE__ */ jsxs101(Fragment43, {
                              children: [
                                /* @__PURE__ */ jsx175(Spacing, {
                                  y: 1,
                                  block: true
                                }),
                                /* @__PURE__ */ jsx175(ValidationMessage, {
                                  align: "flex-start",
                                  message: compHeightErrMessage,
                                  type: "error"
                                })
                              ]
                            }) : null
                          ]
                        })
                      })
                    ]
                  })
                ]
              }) : null,
              type === "composition" && hadDurationDefined ? /* @__PURE__ */ jsx175(NewCompDuration, {
                durationInFrames,
                setDurationInFrames
              }) : null,
              type === "composition" && hadFpsDefined ? /* @__PURE__ */ jsxs101("div", {
                style: optionRow,
                children: [
                  /* @__PURE__ */ jsx175("div", {
                    style: label,
                    children: "FPS"
                  }),
                  /* @__PURE__ */ jsx175("div", {
                    style: rightRow,
                    children: /* @__PURE__ */ jsx175(InputDragger, {
                      type: "number",
                      value: selectedFrameRate,
                      onTextChange: onTextFpsChange,
                      placeholder: "Frame rate (fps)",
                      name: "fps",
                      min: 1,
                      required: true,
                      status: "ok",
                      max: 240,
                      step: 0.01,
                      onValueChange: onFpsChange,
                      rightAlign: false
                    })
                  })
                ]
              }) : null
            ]
          }),
          /* @__PURE__ */ jsx175(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx175(CodemodFooter, {
              loadingNotification: "Duplicating...",
              errorNotification: "Could not duplicate composition",
              genericSubmitLabel: "Duplicate",
              submitLabel: ({ relativeRootPath }) => `Add to ${relativeRootPath}`,
              codemod,
              stack: compositionStack,
              valid,
              onSuccess: onDuplicateSuccess,
              applyCodemod: ({ signal, symbolicatedStack }) => duplicateComposition({
                codemod,
                dryRun: false,
                signal,
                symbolicatedStack,
                undoRedoNavigation: {
                  undoRoute: getRoute(),
                  redoRoute: `/${newId}`
                }
              }),
              applyCodemodForPreview: ({ signal, symbolicatedStack }) => duplicateComposition({
                codemod,
                dryRun: true,
                signal,
                symbolicatedStack,
                undoRedoNavigation: null
              })
            })
          })
        ]
      })
    ]
  });
};
var DuplicateComposition = ({ compositionId, compositionType }) => {
  return /* @__PURE__ */ jsx175(DismissableModal, {
    children: /* @__PURE__ */ jsx175(ResolveCompositionBeforeModal, {
      compositionId,
      children: /* @__PURE__ */ jsx175(DuplicateCompositionLoaded, {
        initialType: compositionType
      })
    })
  });
};

// src/components/NewComposition/NewFolder.tsx
import {
  useCallback as useCallback89,
  useContext as useContext109,
  useEffect as useEffect71,
  useMemo as useMemo101,
  useRef as useRef57,
  useState as useState67
} from "react";
import { Internals as Internals103 } from "remotion";

// src/helpers/validate-new-folder-name.ts
import { Internals as Internals101 } from "remotion";
var validateNewFolderName = ({
  folders,
  newName,
  parentName
}) => {
  if (!Internals101.isFolderNameValid(newName)) {
    return Internals101.invalidFolderNameErrorMessage;
  }
  if (folders.find((folder) => {
    return folder.name === newName && folder.parent === parentName;
  })) {
    return `A folder with that name already exists.`;
  }
  return null;
};

// src/components/NewComposition/NewFolder.tsx
import { jsx as jsx176, jsxs as jsxs102, Fragment as Fragment44 } from "react/jsx-runtime";
var content9 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var parentNameStyle = {
  ...rightRow,
  fontSize: 13
};
var getUniqueFolderName = ({
  folders,
  parentName
}) => {
  let counter = 1;
  while (true) {
    const name = counter === 1 ? "NewFolder" : `NewFolder${counter}`;
    const err = validateNewFolderName({ folders, newName: name, parentName });
    if (!err) {
      return name;
    }
    counter++;
  }
};
var NewFolder = ({ parentName, stack: stack2 }) => {
  const { folders } = useContext109(Internals103.CompositionManager);
  const { setCompositionFoldersExpanded } = useContext109(FolderContext);
  const [newName, setName] = useState67(() => getUniqueFolderName({ folders, parentName }));
  const inputRef = useRef57(null);
  useEffect71(() => {
    const input3 = inputRef.current;
    if (!input3)
      return;
    input3.select();
  }, []);
  const onNameChange = useCallback89((e) => {
    setName(e.target.value);
  }, []);
  const folderName = slugifyName(newName);
  const folderNameErrMessage = folderName ? validateNewFolderName({ folders, newName: folderName, parentName }) : "Enter a name containing letters or numbers.";
  const valid = folderNameErrMessage === null;
  const codemod = useMemo101(() => {
    return {
      type: "new-folder",
      folderName,
      parentName
    };
  }, [folderName, parentName]);
  const onSubmit = useCallback89((e) => {
    e.preventDefault();
  }, []);
  const onSuccess = useCallback89(() => {
    if (parentName === null) {
      return;
    }
    const parentFolder = splitParentIntoNameAndParent(parentName);
    const parentFolderName = parentFolder.name;
    if (parentFolderName === null) {
      return;
    }
    setCompositionFoldersExpanded((previousState) => {
      const foldersExpanded = { ...previousState };
      for (const key7 of getKeysToExpand(parentFolderName, parentFolder.parent)) {
        foldersExpanded[key7] = true;
      }
      persistExpandedFolders("compositions", foldersExpanded);
      return foldersExpanded;
    });
  }, [parentName, setCompositionFoldersExpanded]);
  return /* @__PURE__ */ jsxs102(DismissableModal, {
    children: [
      /* @__PURE__ */ jsx176(ModalHeader, {
        title: "New folder"
      }),
      /* @__PURE__ */ jsxs102("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsxs102("div", {
            style: content9,
            children: [
              parentName ? /* @__PURE__ */ jsxs102("div", {
                style: optionRow,
                children: [
                  /* @__PURE__ */ jsx176("div", {
                    style: label,
                    children: "Parent"
                  }),
                  /* @__PURE__ */ jsx176("div", {
                    style: parentNameStyle,
                    children: parentName
                  })
                ]
              }) : null,
              /* @__PURE__ */ jsxs102("div", {
                style: optionRow,
                children: [
                  /* @__PURE__ */ jsx176("div", {
                    style: label,
                    children: "Name"
                  }),
                  /* @__PURE__ */ jsx176("div", {
                    style: rightRow,
                    children: /* @__PURE__ */ jsxs102(InputAndValidationContainer, {
                      children: [
                        /* @__PURE__ */ jsx176(RemotionInput, {
                          ref: inputRef,
                          value: newName,
                          onChange: onNameChange,
                          type: "text",
                          autoFocus: true,
                          placeholder: "Folder name",
                          status: "ok",
                          rightAlign: true
                        }),
                        /* @__PURE__ */ jsx176(SlugPreview, {
                          action: "create",
                          currentName: null,
                          input: newName,
                          slug: folderName
                        }),
                        folderNameErrMessage ? /* @__PURE__ */ jsxs102(Fragment44, {
                          children: [
                            /* @__PURE__ */ jsx176(Spacing, {
                              y: 1,
                              block: true
                            }),
                            /* @__PURE__ */ jsx176(ValidationMessage, {
                              align: "flex-start",
                              message: folderNameErrMessage,
                              type: "error"
                            })
                          ]
                        }) : null
                      ]
                    })
                  })
                ]
              })
            ]
          }),
          /* @__PURE__ */ jsx176(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx176(CodemodFooter, {
              loadingNotification: "Creating folder...",
              errorNotification: "Could not create folder",
              genericSubmitLabel: "Add to root file",
              submitLabel: ({ relativeRootPath }) => `Add to ${relativeRootPath}`,
              codemod,
              stack: stack2,
              valid,
              onSuccess,
              fallbackToRootFile: true,
              applyCodemod: ({ signal, symbolicatedStack }) => applyCodemod({
                codemod,
                dryRun: false,
                signal,
                symbolicatedStack,
                undoRedoNavigation: null
              }),
              applyCodemodForPreview: null
            })
          })
        ]
      })
    ]
  });
};

// src/components/NewComposition/RenameComposition.tsx
import {
  useCallback as useCallback90,
  useContext as useContext110,
  useEffect as useEffect72,
  useRef as useRef58,
  useState as useState68
} from "react";
import { Internals as Internals105 } from "remotion";
import { jsx as jsx177, jsxs as jsxs103, Fragment as Fragment45 } from "react/jsx-runtime";
var content10 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var RenameCompositionLoaded = () => {
  const context = useContext110(ResolvedCompositionContext);
  if (!context) {
    throw new Error("Resolved composition context");
  }
  const { resolved, unresolved } = context;
  const compositionStack = unresolved.stack ?? null;
  const { compositions } = useContext110(Internals105.CompositionManager);
  const [newId, setName] = useState68(() => {
    return resolved.result.id;
  });
  const inputRef = useRef58(null);
  useEffect72(() => {
    const input3 = inputRef.current;
    if (!input3)
      return;
    input3.select();
  }, []);
  const onNameChange = useCallback90((e) => {
    setName(e.target.value);
  }, []);
  const {
    codemod,
    compositionId,
    renameComposition,
    valid,
    validationMessage: compNameErrMessage
  } = useRenameComposition({
    compositions,
    currentId: resolved.result.id,
    newId
  });
  const onSubmit = useCallback90((e) => {
    e.preventDefault();
  }, []);
  return /* @__PURE__ */ jsxs103(Fragment45, {
    children: [
      /* @__PURE__ */ jsx177(ModalHeader, {
        title: `Rename ${resolved.result.id}`
      }),
      /* @__PURE__ */ jsxs103("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsx177("div", {
            style: content10,
            children: /* @__PURE__ */ jsxs103("div", {
              style: optionRow,
              children: [
                /* @__PURE__ */ jsx177("div", {
                  style: label,
                  children: "ID"
                }),
                /* @__PURE__ */ jsx177("div", {
                  style: rightRow,
                  children: /* @__PURE__ */ jsxs103(InputAndValidationContainer, {
                    children: [
                      /* @__PURE__ */ jsx177(RemotionInput, {
                        ref: inputRef,
                        value: newId,
                        onChange: onNameChange,
                        type: "text",
                        autoFocus: true,
                        placeholder: "Composition ID",
                        status: "ok",
                        rightAlign: true
                      }),
                      /* @__PURE__ */ jsx177(SlugPreview, {
                        action: "rename",
                        currentName: resolved.result.id,
                        input: newId,
                        slug: compositionId
                      }),
                      compNameErrMessage ? /* @__PURE__ */ jsxs103(Fragment45, {
                        children: [
                          /* @__PURE__ */ jsx177(Spacing, {
                            y: 1,
                            block: true
                          }),
                          /* @__PURE__ */ jsx177(ValidationMessage, {
                            align: "flex-start",
                            message: compNameErrMessage,
                            type: "error"
                          })
                        ]
                      }) : null
                    ]
                  })
                })
              ]
            })
          }),
          /* @__PURE__ */ jsx177(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx177(CodemodFooter, {
              loadingNotification: "Renaming...",
              errorNotification: "Could not rename composition",
              genericSubmitLabel: "Rename",
              submitLabel: ({ relativeRootPath }) => `Modify ${relativeRootPath}`,
              codemod,
              stack: compositionStack,
              valid,
              onSuccess: null,
              applyCodemod: ({ signal, symbolicatedStack }) => renameComposition({
                newCompositionId: newId,
                signal,
                symbolicatedStack
              }),
              applyCodemodForPreview: null
            })
          })
        ]
      })
    ]
  });
};
var RenameComposition = ({ compositionId }) => {
  return /* @__PURE__ */ jsx177(DismissableModal, {
    children: /* @__PURE__ */ jsx177(ResolveCompositionBeforeModal, {
      compositionId,
      children: /* @__PURE__ */ jsx177(RenameCompositionLoaded, {})
    })
  });
};

// src/components/NewComposition/RenameFolder.tsx
import {
  useCallback as useCallback91,
  useContext as useContext111,
  useEffect as useEffect73,
  useMemo as useMemo102,
  useRef as useRef59,
  useState as useState69
} from "react";
import { Internals as Internals106 } from "remotion";

// src/helpers/validate-folder-rename.ts
var validateFolderRename = ({
  folders,
  newName,
  originalName,
  parentName
}) => {
  if (newName === originalName) {
    return null;
  }
  return validateNewFolderName({ folders, newName, parentName });
};

// src/components/NewComposition/RenameFolder.tsx
import { jsx as jsx178, jsxs as jsxs104, Fragment as Fragment46 } from "react/jsx-runtime";
var content11 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var RenameFolder = ({ folderName, parentName, stack: stack2 }) => {
  const { folders } = useContext111(Internals106.CompositionManager);
  const [newName, setName] = useState69(folderName);
  const inputRef = useRef59(null);
  useEffect73(() => {
    const input3 = inputRef.current;
    if (!input3)
      return;
    input3.select();
  }, []);
  const onNameChange = useCallback91((e) => {
    setName(e.target.value);
  }, []);
  const slug = slugifyName(newName);
  const folderNameErrMessage = slug ? validateFolderRename({
    folders,
    newName: slug,
    originalName: folderName,
    parentName
  }) : "Enter a name containing letters or numbers.";
  const valid = folderNameErrMessage === null && folderName !== slug;
  const codemod = useMemo102(() => {
    return {
      type: "rename-folder",
      folderName,
      parentName,
      newName: slug
    };
  }, [folderName, slug, parentName]);
  const onSubmit = useCallback91((e) => {
    e.preventDefault();
  }, []);
  const folderId = getFolderId({ folderName, parentName });
  return /* @__PURE__ */ jsxs104(DismissableModal, {
    children: [
      /* @__PURE__ */ jsx178(ModalHeader, {
        title: `Rename ${folderId}`
      }),
      /* @__PURE__ */ jsxs104("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsx178("div", {
            style: content11,
            children: /* @__PURE__ */ jsxs104("div", {
              style: optionRow,
              children: [
                /* @__PURE__ */ jsx178("div", {
                  style: label,
                  children: "Name"
                }),
                /* @__PURE__ */ jsx178("div", {
                  style: rightRow,
                  children: /* @__PURE__ */ jsxs104(InputAndValidationContainer, {
                    children: [
                      /* @__PURE__ */ jsx178(RemotionInput, {
                        ref: inputRef,
                        value: newName,
                        onChange: onNameChange,
                        type: "text",
                        autoFocus: true,
                        placeholder: "Folder name",
                        status: "ok",
                        rightAlign: true
                      }),
                      /* @__PURE__ */ jsx178(SlugPreview, {
                        action: "rename",
                        currentName: folderName,
                        input: newName,
                        slug
                      }),
                      folderNameErrMessage ? /* @__PURE__ */ jsxs104(Fragment46, {
                        children: [
                          /* @__PURE__ */ jsx178(Spacing, {
                            y: 1,
                            block: true
                          }),
                          /* @__PURE__ */ jsx178(ValidationMessage, {
                            align: "flex-start",
                            message: folderNameErrMessage,
                            type: "error"
                          })
                        ]
                      }) : null
                    ]
                  })
                })
              ]
            })
          }),
          /* @__PURE__ */ jsx178(ModalFooterContainer, {
            children: /* @__PURE__ */ jsx178(CodemodFooter, {
              loadingNotification: "Renaming folder...",
              errorNotification: "Could not rename folder",
              genericSubmitLabel: "Rename",
              submitLabel: ({ relativeRootPath }) => `Modify ${relativeRootPath}`,
              codemod,
              stack: stack2,
              valid,
              onSuccess: null,
              applyCodemod: ({ signal, symbolicatedStack }) => applyCodemod({
                codemod,
                dryRun: false,
                signal,
                symbolicatedStack,
                undoRedoNavigation: null
              }),
              applyCodemodForPreview: null
            })
          })
        ]
      })
    ]
  });
};

// src/components/NewComposition/RenameStaticFile.tsx
import {
  useCallback as useCallback92,
  useContext as useContext112,
  useEffect as useEffect74,
  useMemo as useMemo103,
  useRef as useRef60,
  useState as useState70
} from "react";
import { jsx as jsx179, jsxs as jsxs105, Fragment as Fragment47 } from "react/jsx-runtime";
var content12 = {
  padding: 12,
  paddingRight: 12,
  flex: 1,
  fontSize: 13,
  minWidth: 500
};
var RenameStaticFileModal = ({ relativePath }) => {
  const { setSelectedModal } = useContext112(SetSelectedModalContext);
  const staticFiles = useStaticFiles();
  const [newName, setNewName] = useState70(() => getStaticFileBaseName(relativePath));
  const [submitting, setSubmitting] = useState70(false);
  const inputRef = useRef60(null);
  const renameFile = useRenameStaticFile({ relativePath, staticFiles });
  useEffect74(() => {
    const input3 = inputRef.current;
    if (!input3)
      return;
    const [start, end] = getStaticFileRenameSelection(newName);
    input3.setSelectionRange(start, end);
  }, []);
  const newRelativePath = useMemo103(() => {
    return getRenamedStaticFilePath({ newName, relativePath });
  }, [newName, relativePath]);
  const changed = newRelativePath !== relativePath;
  const validationMessage = useMemo103(() => {
    return validateStaticFileRename({
      newName,
      newRelativePath,
      relativePath,
      staticFiles
    });
  }, [newName, newRelativePath, relativePath, staticFiles]);
  const valid = changed && validationMessage === null;
  const onNameChange = useCallback92((e) => {
    setNewName(e.target.value);
  }, []);
  const onRename = useCallback92(() => {
    if (!valid) {
      return;
    }
    setSubmitting(true);
    renameFile(newName).then((renamed) => {
      if (!renamed) {
        setSubmitting(false);
        return;
      }
      setSelectedModal(null);
    }).catch(() => {
      setSubmitting(false);
    });
  }, [newName, renameFile, setSelectedModal, valid]);
  const onSubmit = useCallback92((e) => {
    e.preventDefault();
    onRename();
  }, [onRename]);
  return /* @__PURE__ */ jsxs105(DismissableModal, {
    children: [
      /* @__PURE__ */ jsx179(ModalHeader, {
        title: `Rename ${relativePath}`
      }),
      /* @__PURE__ */ jsxs105("form", {
        onSubmit,
        children: [
          /* @__PURE__ */ jsx179("div", {
            style: content12,
            children: /* @__PURE__ */ jsxs105("div", {
              style: optionRow,
              children: [
                /* @__PURE__ */ jsx179("div", {
                  style: label,
                  children: "Name"
                }),
                /* @__PURE__ */ jsx179("div", {
                  style: rightRow,
                  children: /* @__PURE__ */ jsxs105(InputAndValidationContainer, {
                    children: [
                      /* @__PURE__ */ jsx179(RemotionInput, {
                        ref: inputRef,
                        value: newName,
                        onChange: onNameChange,
                        type: "text",
                        autoFocus: true,
                        autoComplete: "off",
                        "data-1p-ignore": true,
                        placeholder: "Asset name",
                        status: validationMessage ? "error" : "ok",
                        rightAlign: true
                      }),
                      validationMessage ? /* @__PURE__ */ jsxs105(Fragment47, {
                        children: [
                          /* @__PURE__ */ jsx179(Spacing, {
                            y: 1,
                            block: true
                          }),
                          /* @__PURE__ */ jsx179(ValidationMessage, {
                            align: "flex-start",
                            message: validationMessage,
                            type: "error"
                          })
                        ]
                      }) : null
                    ]
                  })
                })
              ]
            })
          }),
          /* @__PURE__ */ jsx179(ModalFooterContainer, {
            children: /* @__PURE__ */ jsxs105(Row, {
              align: "center",
              justify: "flex-end",
              children: [
                /* @__PURE__ */ jsx179(Button, {
                  onClick: () => setSelectedModal(null),
                  children: "Cancel"
                }),
                /* @__PURE__ */ jsx179(Spacing, {
                  x: 1
                }),
                /* @__PURE__ */ jsx179(Button, {
                  onClick: onRename,
                  disabled: !valid || submitting,
                  children: "Rename"
                })
              ]
            })
          })
        ]
      })
    ]
  });
};

// src/components/OverrideInputProps.tsx
import { useCallback as useCallback93, useContext as useContext113, useMemo as useMemo104, useState as useState71 } from "react";
import { Internals as Internals107 } from "remotion";
import { jsx as jsx180, jsxs as jsxs106 } from "react/jsx-runtime";
var style6 = {
  padding: 12,
  width: 500
};
var label11 = {
  color: LIGHT_TEXT,
  fontSize: 14,
  marginBottom: 10
};
var textAreaStyle = {
  fontFamily: "monospace",
  minHeight: 200
};
var codeSnippet = {
  fontSize: 14,
  color: BLUE,
  fontFamily: "monospace"
};
var row7 = {
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  justifyContent: "space-between"
};
var isValidJSON = (value) => {
  try {
    JSON.parse(value);
    return true;
  } catch {
    return false;
  }
};
var Inner = () => {
  const [value, setValue] = useState71(() => {
    const override = Internals107.getInputPropsOverride();
    if (override) {
      return JSON.stringify(override, null, 2);
    }
    return null;
  });
  const { setSelectedModal } = useContext113(SetSelectedModalContext);
  const valid = useMemo104(() => {
    if (!value)
      return true;
    return isValidJSON(value);
  }, [value]);
  const onChange = useCallback93((newValue) => {
    if (newValue.trim() === "") {
      setValue(null);
      Internals107.setInputPropsOverride(null);
      return;
    }
    setValue(newValue);
    if (!isValidJSON(newValue)) {
      return;
    }
    Internals107.setInputPropsOverride(JSON.parse(newValue));
  }, [setValue]);
  const onReset = useCallback93(() => {
    onChange("");
  }, [onChange]);
  const onReloadPage = useCallback93(() => {
    window.location.reload();
  }, []);
  const onDone = useCallback93(() => {
    setSelectedModal(null);
  }, [setSelectedModal]);
  return /* @__PURE__ */ jsxs106("div", {
    style: style6,
    children: [
      /* @__PURE__ */ jsxs106("div", {
        style: label11,
        children: [
          "Enter a valid JSON to override the value that",
          " ",
          /* @__PURE__ */ jsx180("code", {
            style: codeSnippet,
            children: "getInputProps()"
          }),
          " returns to preview a composition with different props. The Studio must be reloaded to see the changes."
        ]
      }),
      /* @__PURE__ */ jsx180(RemTextarea, {
        onChange: (e) => onChange(e.target.value),
        value: value ?? "",
        status: valid ? "ok" : "error",
        style: textAreaStyle
      }),
      /* @__PURE__ */ jsxs106("div", {
        style: row7,
        children: [
          /* @__PURE__ */ jsx180(Button, {
            onClick: onReset,
            children: "Reset"
          }),
          /* @__PURE__ */ jsx180(Spacing, {
            x: 0.5
          }),
          /* @__PURE__ */ jsx180(Button, {
            onClick: onReloadPage,
            children: "Reload page"
          }),
          /* @__PURE__ */ jsx180(Flex, {}),
          /* @__PURE__ */ jsx180(Button, {
            onClick: onDone,
            children: "Done"
          })
        ]
      })
    ]
  });
};
var OverrideInputPropsModal = () => {
  return /* @__PURE__ */ jsx180(DismissableModal, {
    children: /* @__PURE__ */ jsx180(Inner, {})
  });
};

// src/components/QuickSwitcher/QuickSwitcher.tsx
import { jsx as jsx181 } from "react/jsx-runtime";
var panelStyle6 = {
  borderRadius: 6,
  overflow: "hidden"
};
var QuickSwitcher = ({
  initialMode,
  invocationTimestamp,
  readOnlyStudio,
  assetSelection,
  compositionSelection
}) => {
  return /* @__PURE__ */ jsx181(DismissableModal, {
    panelStyle: panelStyle6,
    children: /* @__PURE__ */ jsx181(QuickSwitcherContent, {
      readOnlyStudio,
      invocationTimestamp,
      initialMode,
      assetSelection,
      compositionSelection
    })
  });
};
var QuickSwitcher_default = QuickSwitcher;

// src/components/RenderModal/RenderStatusModal.tsx
import { useCallback as useCallback95, useContext as useContext114 } from "react";

// src/components/RenderModal/ClientRenderProgress.tsx
import { formatBytes as formatBytes2 } from "@remotion/studio-shared";
import { jsx as jsx182, jsxs as jsxs107 } from "react/jsx-runtime";
var progressItem = {
  padding: 10,
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var label12 = {
  fontSize: 14,
  width: 400,
  color: WHITE
};
var right = {
  fontSize: 14,
  color: LIGHT_TEXT,
  textAlign: "right",
  flex: 1
};
var ProgressStatus = ({ encodedFrames, totalFrames, doneIn, renderEstimatedTime, progress }) => {
  const done = doneIn !== null;
  const message2 = getClientRenderProgressMessage({
    encodedFrames,
    totalFrames,
    doneIn,
    renderEstimatedTime,
    progress
  });
  return /* @__PURE__ */ jsxs107("div", {
    style: progressItem,
    children: [
      done ? /* @__PURE__ */ jsx182(SuccessIcon, {}) : /* @__PURE__ */ jsx182(CircularProgress, {
        progress
      }),
      /* @__PURE__ */ jsx182(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx182("div", {
        style: label12,
        children: message2
      }),
      doneIn !== null ? /* @__PURE__ */ jsxs107("div", {
        style: right,
        children: [
          doneIn,
          "ms"
        ]
      }) : null
    ]
  });
};
var DoneStatus = ({ job }) => {
  return /* @__PURE__ */ jsxs107("div", {
    style: progressItem,
    children: [
      /* @__PURE__ */ jsx182(SuccessIcon, {}),
      /* @__PURE__ */ jsx182(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx182("div", {
        style: label12,
        children: job.outName
      }),
      /* @__PURE__ */ jsx182("div", {
        style: right,
        children: formatBytes2(job.metadata.sizeInBytes)
      })
    ]
  });
};
var ClientRenderProgress = ({ job }) => {
  if (job.status === "idle" || job.status === "failed" || job.status === "cancelled") {
    throw new Error("This component should not be rendered when the job is idle, failed, or cancelled");
  }
  if (job.status === "done") {
    return /* @__PURE__ */ jsxs107("div", {
      children: [
        /* @__PURE__ */ jsx182(Spacing, {
          y: 0.5
        }),
        /* @__PURE__ */ jsx182(DoneStatus, {
          job
        }),
        /* @__PURE__ */ jsx182(Spacing, {
          y: 1
        })
      ]
    });
  }
  if (job.status === "saving") {
    return /* @__PURE__ */ jsxs107("div", {
      children: [
        /* @__PURE__ */ jsx182(Spacing, {
          y: 0.5
        }),
        /* @__PURE__ */ jsx182("div", {
          style: label12,
          children: "Saving to out/..."
        }),
        /* @__PURE__ */ jsx182(Spacing, {
          y: 1
        })
      ]
    });
  }
  const { encodedFrames, totalFrames, doneIn, renderEstimatedTime, progress } = job.progress;
  return /* @__PURE__ */ jsxs107("div", {
    children: [
      /* @__PURE__ */ jsx182(Spacing, {
        y: 0.5
      }),
      job.type === "client-video" && /* @__PURE__ */ jsx182(ProgressStatus, {
        encodedFrames,
        totalFrames,
        doneIn,
        renderEstimatedTime,
        progress
      }),
      /* @__PURE__ */ jsx182(Spacing, {
        y: 1
      })
    ]
  });
};

// src/components/RenderModal/GuiRenderStatus.tsx
import { useCallback as useCallback94, useMemo as useMemo105 } from "react";
import { jsx as jsx183, jsxs as jsxs108 } from "react/jsx-runtime";
var progressItem2 = {
  padding: 10,
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var label13 = {
  fontSize: 14,
  width: 400,
  color: WHITE
};
var right2 = {
  fontSize: 14,
  color: LIGHT_TEXT,
  textAlign: "right",
  flex: 1
};
var BundlingProgress = ({ progress, doneIn }) => {
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      progress === 1 ? /* @__PURE__ */ jsx183(SuccessIcon, {}) : /* @__PURE__ */ jsx183(CircularProgress, {
        progress
      }),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx183("div", {
        style: label13,
        children: progress === 1 ? "Bundled" : `Bundling ${progress * 100}%`
      }),
      doneIn ? /* @__PURE__ */ jsxs108("div", {
        style: right2,
        children: [
          doneIn,
          "ms"
        ]
      }) : null
    ]
  });
};
var failIconStyle = {
  width: 16,
  height: 16
};
var FailIcon = () => {
  return /* @__PURE__ */ jsx183("svg", {
    style: failIconStyle,
    viewBox: "0 0 512 512",
    children: /* @__PURE__ */ jsx183("path", {
      fill: FAIL_COLOR,
      d: "M0 160V352L160 512H352L512 352V160L352 0H160L0 160zm353.9 32l-17 17-47 47 47 47 17 17L320 353.9l-17-17-47-47-47 47-17 17L158.1 320l17-17 47-47-47-47-17-17L192 158.1l17 17 47 47 47-47 17-17L353.9 192z"
    })
  });
};
var BrowserSetupProgress = ({ progress, doneIn, startedBundling, alreadyAvailable, error }) => {
  if (error) {
    return /* @__PURE__ */ jsxs108("div", {
      style: progressItem2,
      children: [
        /* @__PURE__ */ jsx183(FailIcon, {}),
        /* @__PURE__ */ jsx183(Spacing, {
          x: 1
        }),
        /* @__PURE__ */ jsx183("div", {
          style: label13,
          children: "Failed to download Headless Shell"
        })
      ]
    });
  }
  const stillChecking = alreadyAvailable && progress === 0 && !startedBundling;
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      stillChecking ? /* @__PURE__ */ jsx183(CircularProgress, {
        progress: 0
      }) : progress === 1 || alreadyAvailable ? /* @__PURE__ */ jsx183(SuccessIcon, {}) : /* @__PURE__ */ jsx183(CircularProgress, {
        progress
      }),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx183("div", {
        style: label13,
        children: stillChecking ? "Checking for Headless Shell" : alreadyAvailable && startedBundling ? "Headless browser already available" : progress === 1 ? "Downloaded Headless Shell" : `Downloading Headless Shell ${Math.round(progress * 100)}%`
      }),
      doneIn ? /* @__PURE__ */ jsxs108("div", {
        style: right2,
        children: [
          doneIn,
          "ms"
        ]
      }) : null
    ]
  });
};
var RenderingProgress = ({ progress }) => {
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      progress.frames === progress.totalFrames ? /* @__PURE__ */ jsx183(SuccessIcon, {}) : /* @__PURE__ */ jsx183(CircularProgress, {
        progress: progress.frames / progress.totalFrames
      }),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx183("div", {
        style: label13,
        children: progress.doneIn ? `Rendered ${progress.totalFrames} frames` : `Rendering ${progress.frames} / ${progress.totalFrames} frames`
      }),
      progress.doneIn ? /* @__PURE__ */ jsxs108("div", {
        style: right2,
        children: [
          progress.doneIn,
          "ms"
        ]
      }) : null
    ]
  });
};
var StitchingProgress = ({ progress }) => {
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      progress.frames === progress.totalFrames ? /* @__PURE__ */ jsx183(SuccessIcon, {}) : /* @__PURE__ */ jsx183(CircularProgress, {
        progress: progress.frames / progress.totalFrames
      }),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsx183("div", {
        style: label13,
        children: progress.doneIn ? `Encoded ${progress.totalFrames} frames` : `Encoding ${progress.frames} / ${progress.totalFrames} frames`
      }),
      progress.doneIn ? /* @__PURE__ */ jsxs108("div", {
        style: right2,
        children: [
          progress.doneIn,
          "ms"
        ]
      }) : null
    ]
  });
};
var DownloadsProgress = ({ downloads }) => {
  const allHaveProgress = downloads.every((a) => a.totalBytes);
  const totalBytes = allHaveProgress ? downloads.reduce((a, b) => a + b.totalBytes, 0) : null;
  const downloaded = allHaveProgress ? downloads.reduce((a, b) => a + b.downloaded, 0) : null;
  const progress = allHaveProgress ? downloaded / totalBytes : 0.1;
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      progress === 1 ? /* @__PURE__ */ jsx183(SuccessIcon, {}) : /* @__PURE__ */ jsx183(CircularProgress, {
        progress
      }),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      /* @__PURE__ */ jsxs108("div", {
        style: label13,
        children: [
          "Downloading ",
          downloads.length,
          " file",
          downloads.length === 1 ? "" : "s"
        ]
      })
    ]
  });
};
var OpenFile = ({ job }) => {
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const labelStyle3 = useMemo105(() => {
    return {
      ...label13,
      textAlign: "left",
      appearance: "none",
      border: 0,
      paddingLeft: 0,
      cursor: job.deletedOutputLocation ? "inherit" : "pointer",
      textDecoration: job.deletedOutputLocation ? "line-through" : "none"
    };
  }, [job.deletedOutputLocation]);
  const onClick = useCallback94(() => {
    openInFileExplorer({ directory: job.outName });
  }, [job.outName]);
  return /* @__PURE__ */ jsxs108("div", {
    style: progressItem2,
    children: [
      /* @__PURE__ */ jsx183(SuccessIcon, {}),
      /* @__PURE__ */ jsx183(Spacing, {
        x: 1
      }),
      isBrowserStudio ? /* @__PURE__ */ jsx183("div", {
        style: labelStyle3,
        children: job.outName
      }) : /* @__PURE__ */ jsx183("button", {
        style: labelStyle3,
        type: "button",
        onClick,
        children: job.outName
      }),
      /* @__PURE__ */ jsx183("div", {
        style: right2,
        children: /* @__PURE__ */ jsx183(RenderQueueOpenInFinderItem, {
          job
        })
      })
    ]
  });
};
var GuiRenderStatus = ({ job }) => {
  if (job.status === "idle" || job.status === "failed") {
    throw new Error("This component should not be rendered when the job is idle");
  }
  return /* @__PURE__ */ jsxs108("div", {
    children: [
      /* @__PURE__ */ jsx183(Spacing, {
        y: 0.5
      }),
      /* @__PURE__ */ jsx183(BrowserSetupProgress, {
        ...job.progress.browser,
        startedBundling: Boolean(job.progress.bundling)
      }),
      job.progress.bundling && /* @__PURE__ */ jsx183(BundlingProgress, {
        progress: job.progress.bundling.progress,
        doneIn: job.progress.bundling.doneIn
      }),
      job.progress.rendering ? /* @__PURE__ */ jsx183(RenderingProgress, {
        progress: job.progress.rendering
      }) : null,
      job.progress.stitching ? /* @__PURE__ */ jsx183(StitchingProgress, {
        progress: job.progress.stitching
      }) : null,
      job.progress.downloads.length > 0 ? /* @__PURE__ */ jsx183(DownloadsProgress, {
        downloads: job.progress.downloads
      }) : null,
      job.status === "done" ? /* @__PURE__ */ jsx183(OpenFile, {
        job
      }) : null,
      /* @__PURE__ */ jsx183(Spacing, {
        y: 1
      })
    ]
  });
};

// src/components/RenderModal/RenderStatusModal.tsx
import { jsx as jsx184, jsxs as jsxs109, Fragment as Fragment48 } from "react/jsx-runtime";
var container35 = {
  padding: 20,
  maxWidth: 900,
  paddingTop: 0
};
var codeBlock = {
  backgroundColor: RENDER_STATUS_BACKGROUND,
  whiteSpace: "pre",
  padding: 12,
  borderRadius: 4,
  fontFamily: "monospace",
  overflow: "auto",
  maxHeight: 300
};
var spacer3 = {
  height: SPACING_UNIT,
  width: SPACING_UNIT
};
var buttonRow = {
  display: "flex",
  flexDirection: "row",
  justifyContent: "flex-end"
};
var RenderStatusModal = ({
  jobId
}) => {
  const { setSelectedModal } = useContext114(SetSelectedModalContext);
  const { jobs, removeClientJob, cancelClientJob } = useContext114(RenderQueueContext);
  const job = jobs.find((j) => j.id === jobId);
  if (!job) {
    throw new Error("job not found");
  }
  const isClientJob = isClientRenderJob(job);
  const onQuit = useCallback95(() => {
    setSelectedModal(null);
  }, [setSelectedModal]);
  const onRetry = useCallback95(() => {
    if (isClientJob) {
      if (isRestoredClientJob(job)) {
        return;
      }
      const retryPayload = makeClientRetryPayload(job);
      setSelectedModal(retryPayload);
    } else {
      const retryPayload = makeRetryPayload(job);
      setSelectedModal(retryPayload);
    }
  }, [job, isClientJob, setSelectedModal]);
  const onClickOnRemove = useCallback95(() => {
    setSelectedModal(null);
    if (isClientJob) {
      removeClientJob(job.id);
    } else {
      removeRenderJob(job).catch((err) => {
        showNotification(`Could not remove job: ${err.message}`, 2000);
      });
    }
  }, [job, isClientJob, removeClientJob, setSelectedModal]);
  const onClickOnCancel = useCallback95(() => {
    if (isClientJob) {
      cancelClientJob(job.id);
    } else {
      cancelRenderJob(job).catch((err) => {
        showNotification(`Could not cancel job: ${err.message}`, 2000);
      });
    }
  }, [job, isClientJob, cancelClientJob]);
  if (job.status === "idle") {
    throw new Error("should not have rendered this modal");
  }
  const errorDetails = job.status === "failed" ? job.error.stack?.split(`
`)[0].includes(job.error.message) ? job.error.stack : [job.error.message, job.error.stack].filter(Boolean).join(`
`) : null;
  return /* @__PURE__ */ jsxs109(ModalContainer, {
    onOutsideClick: onQuit,
    onEscape: onQuit,
    children: [
      /* @__PURE__ */ jsx184(ModalHeader, {
        title: `Render ${job.compositionId}`
      }),
      /* @__PURE__ */ jsxs109("div", {
        style: container35,
        children: [
          job.status === "failed" ? /* @__PURE__ */ jsxs109(Fragment48, {
            children: [
              /* @__PURE__ */ jsx184("p", {
                children: "The render failed because of the following error:"
              }),
              /* @__PURE__ */ jsx184("div", {
                className: HORIZONTAL_SCROLLBAR_CLASSNAME,
                style: codeBlock,
                children: errorDetails
              })
            ]
          }) : null,
          (job.status === "done" || job.status === "running") && (isClientJob ? /* @__PURE__ */ jsx184(ClientRenderProgress, {
            job
          }) : /* @__PURE__ */ jsx184(GuiRenderStatus, {
            job
          })),
          /* @__PURE__ */ jsx184("div", {
            style: spacer3
          }),
          /* @__PURE__ */ jsxs109("div", {
            style: buttonRow,
            children: [
              job.status === "running" ? /* @__PURE__ */ jsx184(Button, {
                onClick: onClickOnCancel,
                children: "Cancel render"
              }) : /* @__PURE__ */ jsx184(Button, {
                onClick: onClickOnRemove,
                children: "Remove render"
              }),
              /* @__PURE__ */ jsx184(Flex, {}),
              job.status === "failed" ? /* @__PURE__ */ jsx184(Button, {
                onClick: onRetry,
                children: "Retry"
              }) : null,
              /* @__PURE__ */ jsx184("div", {
                style: spacer3
              }),
              /* @__PURE__ */ jsx184(Button, {
                onClick: onQuit,
                children: "Close"
              })
            ]
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/ServerRenderModal.tsx
import { BrowserSafeApis as BrowserSafeApis10 } from "@remotion/renderer/client";
import { getDefaultOutLocation } from "@remotion/studio-shared";
import {
  useCallback as useCallback110,
  useContext as useContext116,
  useEffect as useEffect76,
  useMemo as useMemo114,
  useReducer as useReducer2,
  useRef as useRef62,
  useState as useState75
} from "react";

// src/helpers/convert-env-variables.ts
var envVariablesObjectToArray = (envVariables) => {
  return Object.entries(envVariables).map(([key7, one]) => [
    key7.trim().toUpperCase(),
    one.trim()
  ]);
};
var envVariablesArrayToObject = (envVariables) => {
  return envVariables.map(([key7, val]) => [key7.trim(), val.trim()]).filter(([key7, val]) => key7 && val).reduce((acc, [key7, value]) => {
    acc[key7.toUpperCase()] = value;
    return acc;
  }, {});
};

// src/helpers/make-render-command.ts
import { BrowserSafeApis } from "@remotion/renderer/client";
var shellQuote = (value) => {
  return `'${value.replace(/'/g, "'\\''")}'`;
};
var addFlagWithValue = (flags, flag, value) => {
  if (value === null || value === undefined) {
    return;
  }
  flags.push(`--${flag}=${shellQuote(String(value))}`);
};
var addBooleanFlag = (flags, flag, value) => {
  if (value) {
    flags.push(`--${flag}`);
  }
};
var valueFlag = (flag, value, defaultValue) => {
  return { flag, value, defaultValue };
};
var booleanFlag = (flag, value, defaultValue) => {
  return { flag, value, defaultValue };
};
var addValueFlagsIfChanged = (flags, mappings) => {
  for (const mapping of mappings) {
    if (mapping.value !== mapping.defaultValue) {
      addFlagWithValue(flags, mapping.flag, mapping.value);
    }
  }
};
var addTrueBooleanFlagsIfChanged = (flags, mappings) => {
  for (const mapping of mappings) {
    if (mapping.value && mapping.value !== mapping.defaultValue) {
      addBooleanFlag(flags, mapping.flag, true);
    }
  }
};
var getNpmRemotionCommandPrefix = (version) => {
  return version.trim() === "" ? "bunx --yes --location=global -p @remotion/cli remotion" : `bunx --yes --location=global -p @remotion/cli@${version} remotion`;
};
var normalizeServeUrlForRenderCommand = ({
  locationHref,
  compositionId
}) => {
  const parsed = new URL(locationHref);
  parsed.hash = "";
  parsed.search = "";
  const suffix2 = `/${compositionId}`;
  if (parsed.pathname === suffix2) {
    parsed.pathname = "/";
  } else if (parsed.pathname.endsWith(suffix2)) {
    const basePath = parsed.pathname.slice(0, -suffix2.length);
    parsed.pathname = basePath === "" ? "/" : basePath;
  }
  if (parsed.pathname !== "/" && parsed.pathname.endsWith("/")) {
    parsed.pathname = parsed.pathname.slice(0, -1);
  }
  return `${parsed.origin}${parsed.pathname === "/" ? "" : parsed.pathname}`;
};
var trimDefaultOutPrefix = (outName) => {
  if (outName.startsWith("out/")) {
    const trimmed = outName.slice("out/".length);
    return trimmed.length === 0 ? outName : trimmed;
  }
  if (outName.startsWith("./out/")) {
    const trimmed = outName.slice("./out/".length);
    return trimmed.length === 0 ? outName : trimmed;
  }
  return outName;
};
var getRenderMediaFlag = (option) => {
  return BrowserSafeApis.optionsMap.renderMedia[option].cliFlag;
};
var renderMediaValueFlag = (option, value, defaultValue) => {
  return valueFlag(getRenderMediaFlag(option), value, defaultValue);
};
var renderMediaBooleanFlag = (option, value, defaultValue) => {
  return booleanFlag(getRenderMediaFlag(option), value, defaultValue);
};
var makeReadOnlyStudioRenderCommand = ({
  remotionVersion,
  locationHref,
  compositionId,
  outName,
  renderMode,
  renderDefaults,
  durationInFrames,
  concurrency,
  frame: frame2,
  startFrame,
  endFrame,
  stillImageFormat,
  sequenceImageFormat,
  videoImageFormat,
  jpegQuality,
  codec,
  muted,
  enforceAudioTrack,
  proResProfile,
  x264Preset,
  gopSize,
  pixelFormat,
  crf,
  videoBitrate,
  audioBitrate,
  audioCodec,
  everyNthFrame,
  numberOfGifLoops,
  disallowParallelEncoding,
  encodingBufferSize,
  encodingMaxRate,
  forSeamlessAacConcatenation,
  separateAudioTo,
  colorSpace,
  scale,
  logLevel,
  delayRenderTimeout,
  hardwareAcceleration,
  chromeMode,
  headless,
  disableWebSecurity,
  ignoreCertificateErrors,
  gl,
  userAgent,
  multiProcessOnLinux,
  darkMode,
  offthreadVideoCacheSizeInBytes,
  offthreadVideoThreads,
  mediaCacheSizeInBytes,
  beepOnFinish,
  repro,
  metadata,
  sampleRate,
  envVariables,
  inputProps
}) => {
  const serveUrl = normalizeServeUrlForRenderCommand({
    locationHref,
    compositionId
  });
  const isStillRender = renderMode === "still";
  const isSequenceRender = renderMode === "sequence";
  const hasCodecSpecificOptions = !isSequenceRender;
  const commandType = isStillRender ? "still" : "render";
  const command2 = getNpmRemotionCommandPrefix(remotionVersion);
  const { options } = BrowserSafeApis;
  const flags = [];
  addValueFlagsIfChanged(flags, [
    valueFlag(options.scaleOption.cliFlag, scale, renderDefaults.scale),
    renderMediaValueFlag("logLevel", logLevel, renderDefaults.logLevel),
    renderMediaValueFlag("timeoutInMilliseconds", delayRenderTimeout, renderDefaults.delayRenderTimeout),
    renderMediaValueFlag("chromeMode", chromeMode, renderDefaults.chromeMode),
    valueFlag(options.glOption.cliFlag, gl, renderDefaults.openGlRenderer),
    valueFlag(options.userAgentOption.cliFlag, userAgent, renderDefaults.userAgent),
    renderMediaValueFlag("offthreadVideoCacheSizeInBytes", offthreadVideoCacheSizeInBytes, renderDefaults.offthreadVideoCacheSizeInBytes),
    renderMediaValueFlag("offthreadVideoThreads", offthreadVideoThreads, renderDefaults.offthreadVideoThreads),
    renderMediaValueFlag("mediaCacheSizeInBytes", mediaCacheSizeInBytes, renderDefaults.mediaCacheSizeInBytes)
  ]);
  if (headless !== renderDefaults.headless) {
    addFlagWithValue(flags, options.headlessOption.cliFlag, !headless);
  }
  addTrueBooleanFlagsIfChanged(flags, [
    booleanFlag(options.disableWebSecurityOption.cliFlag, disableWebSecurity, renderDefaults.disableWebSecurity),
    booleanFlag(options.ignoreCertificateErrorsOption.cliFlag, ignoreCertificateErrors, renderDefaults.ignoreCertificateErrors),
    booleanFlag(options.enableMultiprocessOnLinuxOption.cliFlag, multiProcessOnLinux, renderDefaults.multiProcessOnLinux),
    booleanFlag(options.darkModeOption.cliFlag, darkMode, renderDefaults.darkMode),
    booleanFlag(options.beepOnFinishOption.cliFlag, beepOnFinish, renderDefaults.beepOnFinish)
  ]);
  if (isStillRender) {
    addValueFlagsIfChanged(flags, [
      valueFlag(options.stillFrameOption.cliFlag, frame2, 0),
      valueFlag(options.stillImageFormatOption.cliFlag, stillImageFormat, renderDefaults.stillImageFormat),
      valueFlag(options.jpegQualityOption.cliFlag, jpegQuality, renderDefaults.jpegQuality)
    ]);
  } else {
    addValueFlagsIfChanged(flags, [
      valueFlag(options.concurrencyOption.cliFlag, concurrency, renderDefaults.concurrency)
    ]);
    if (isSequenceRender) {
      addBooleanFlag(flags, options.imageSequenceOption.cliFlag, true);
      if (sequenceImageFormat !== "jpeg") {
        addFlagWithValue(flags, options.videoImageFormatOption.cliFlag, sequenceImageFormat);
      }
    } else {
      addValueFlagsIfChanged(flags, [
        valueFlag(options.videoImageFormatOption.cliFlag, videoImageFormat, renderDefaults.videoImageFormat),
        renderMediaValueFlag("hardwareAcceleration", hardwareAcceleration, renderDefaults.hardwareAcceleration)
      ]);
    }
    if (hasCodecSpecificOptions && codec !== renderDefaults.codec) {
      addFlagWithValue(flags, getRenderMediaFlag("codec"), codec);
    }
    if (startFrame !== 0 || endFrame !== durationInFrames - 1) {
      addFlagWithValue(flags, options.framesOption.cliFlag, `${startFrame}-${endFrame}`);
    }
    if (hasCodecSpecificOptions) {
      addTrueBooleanFlagsIfChanged(flags, [
        renderMediaBooleanFlag("muted", muted, renderDefaults.muted),
        booleanFlag(options.enforceAudioOption.cliFlag, enforceAudioTrack, renderDefaults.enforceAudioTrack),
        renderMediaBooleanFlag("forSeamlessAacConcatenation", forSeamlessAacConcatenation, renderDefaults.forSeamlessAacConcatenation)
      ]);
      addValueFlagsIfChanged(flags, [
        valueFlag(options.pixelFormatOption.cliFlag, pixelFormat, renderDefaults.pixelFormat),
        renderMediaValueFlag("colorSpace", colorSpace, renderDefaults.colorSpace),
        valueFlag(options.proResProfileOption.cliFlag, proResProfile, renderDefaults.proResProfile),
        renderMediaValueFlag("x264Preset", x264Preset, renderDefaults.x264Preset),
        renderMediaValueFlag("gopSize", gopSize, renderDefaults.gopSize),
        valueFlag(options.crfOption.cliFlag, crf, null),
        valueFlag(options.jpegQualityOption.cliFlag, jpegQuality, renderDefaults.jpegQuality),
        renderMediaValueFlag("videoBitrate", videoBitrate, renderDefaults.videoBitrate),
        renderMediaValueFlag("audioBitrate", audioBitrate, renderDefaults.audioBitrate),
        valueFlag(options.everyNthFrameOption.cliFlag, everyNthFrame, renderDefaults.everyNthFrame),
        renderMediaValueFlag("numberOfGifLoops", numberOfGifLoops, renderDefaults.numberOfGifLoops),
        renderMediaValueFlag("encodingBufferSize", encodingBufferSize, renderDefaults.encodingBufferSize),
        renderMediaValueFlag("encodingMaxRate", encodingMaxRate, renderDefaults.encodingMaxRate),
        renderMediaValueFlag("separateAudioTo", separateAudioTo, null)
      ]);
      const defaultAudioCodec = BrowserSafeApis.defaultAudioCodecs[codec]?.compressed;
      if (audioCodec !== defaultAudioCodec) {
        addFlagWithValue(flags, getRenderMediaFlag("audioCodec"), audioCodec);
      }
      addValueFlagsIfChanged(flags, [
        renderMediaValueFlag("sampleRate", sampleRate, renderDefaults.sampleRate)
      ]);
    }
    addTrueBooleanFlagsIfChanged(flags, [
      renderMediaBooleanFlag("disallowParallelEncoding", disallowParallelEncoding, false),
      renderMediaBooleanFlag("repro", repro, renderDefaults.repro)
    ]);
  }
  if (metadata) {
    for (const [key7, value] of Object.entries(metadata)) {
      addFlagWithValue(flags, options.metadataOption.cliFlag, `${key7}=${value}`);
    }
  }
  if (Object.keys(inputProps).length > 0) {
    addFlagWithValue(flags, options.propsOption.cliFlag, JSON.stringify(inputProps));
  }
  const envArgs = Object.entries(envVariables).sort(([a], [b]) => a.localeCompare(b)).map(([key7, value]) => shellQuote(`${key7}=${value}`));
  const renderCommand = [
    command2,
    commandType,
    shellQuote(serveUrl),
    shellQuote(compositionId),
    shellQuote(trimDefaultOutPrefix(outName)),
    ...flags
  ].join(" ");
  if (envArgs.length === 0) {
    return renderCommand;
  }
  return ["env", ...envArgs, renderCommand].join(" ");
};

// src/helpers/render-modal-sections.ts
import { useMemo as useMemo106, useState as useState72 } from "react";
var useRenderModalSections = (renderMode, codec) => {
  const [selectedTab, setTab] = useState72("general");
  const shownTabs = useMemo106(() => {
    if (renderMode === "audio") {
      return [
        "general",
        "data",
        "audio",
        "encoding",
        "environment",
        "advanced"
      ];
    }
    if (renderMode === "still") {
      return ["general", "data", "picture", "environment", "advanced"];
    }
    if (renderMode === "sequence") {
      return [
        "general",
        "data",
        "picture",
        "encoding",
        "environment",
        "advanced"
      ];
    }
    if (renderMode === "video") {
      if (codec === "gif") {
        return [
          "general",
          "data",
          "picture",
          "gif",
          "encoding",
          "environment",
          "advanced"
        ];
      }
      return [
        "general",
        "data",
        "picture",
        "audio",
        "encoding",
        "environment",
        "advanced"
      ];
    }
    throw new TypeError("Unknown render mode");
  }, [codec, renderMode]);
  const tab = useMemo106(() => {
    if (!shownTabs.includes(selectedTab)) {
      return shownTabs[0];
    }
    return selectedTab;
  }, [selectedTab, shownTabs]);
  return useMemo106(() => {
    return { tab, setTab, shownTabs };
  }, [tab, shownTabs]);
};

// src/icons/data.tsx
import { jsx as jsx185 } from "react/jsx-runtime";
var DataIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx185("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 448 512",
    ...props,
    children: /* @__PURE__ */ jsx185("path", {
      fill: color ?? CURRENT_COLOR_LOWERCASE,
      d: "M224 512C100.3 512 0 476.2 0 432V80C0 35.82 100.3 0 224 0C347.7 0 448 35.82 448 80V432C448 476.2 347.7 512 224 512zM416 80.45C415.7 79.69 414.4 77.27 409.8 73.31C402.4 67.11 389.9 60.09 371.6 53.57C335.4 40.62 283.2 32 224 32C164.8 32 112.6 40.62 76.37 53.57C58.1 60.09 45.59 67.11 38.25 73.31C33.55 77.27 32.29 79.69 32 80.45V182.1C46.47 192.7 69.9 202.8 100.9 210.4C135.5 218.9 177.1 224 224 224C270 224 312.5 218.9 347.1 210.4C378.1 202.8 401.5 192.7 416 182.1V80.45zM416 219.5C398.8 228.4 377.9 235.8 354.8 241.5C317.3 250.7 272.2 256 224 256C175.8 256 130.7 250.7 93.22 241.5C70.11 235.8 49.18 228.4 32 219.5V310.1C46.47 320.7 69.9 330.8 100.9 338.4C135.5 346.9 177.1 352 224 352C270 352 312.5 346.9 347.1 338.4C378.1 330.8 401.5 320.7 416 310.1V219.5zM38.25 438.7C45.59 444.9 58.1 451.9 76.37 458.4C112.6 471.4 164.8 480 224 480C283.2 480 335.4 471.4 371.6 458.4C389.9 451.9 402.4 444.9 409.8 438.7C414.4 434.7 415.7 432.3 416 431.6V347.5C398.8 356.4 377.9 363.8 354.8 369.5C317.3 378.7 272.2 384 224 384C175.8 384 130.7 378.7 93.22 369.5C70.11 363.8 49.18 356.4 32 347.5V431.6C32.29 432.3 33.55 434.7 38.25 438.7zM416 431.4C416.1 431.3 416.1 431.3 416.1 431.3L416 431.4zM31.96 431.4C31.94 431.3 31.93 431.3 31.92 431.3L31.96 431.4zM31.96 80.56C31.93 80.65 31.92 80.7 31.92 80.7L31.96 80.56zM416.1 80.7C416.1 80.7 416.1 80.65 416 80.56z"
    })
  });
};

// src/icons/gif.tsx
import { jsx as jsx186 } from "react/jsx-runtime";
var GifIcon = ({ color, ...props }) => /* @__PURE__ */ jsx186("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 576 512",
  ...props,
  children: /* @__PURE__ */ jsx186("path", {
    fill: color ?? CURRENT_COLOR_LOWERCASE,
    d: "M512 32H64C28.65 32 0 60.65 0 96v320c0 35.35 28.65 64 64 64h448c35.35 0 64-28.65 64-64V96c0-35.35-28.7-64-64-64zm32 384c0 17.64-14.36 32-32 32H64c-17.64 0-32-14.36-32-32V96c0-17.64 14.36-32 32-32h448c17.64 0 32 14.36 32 32v320zm-80-256h-96c-8.8 0-16 7.2-16 16v160c0 8.844 7.156 16 16 16s16-7.156 16-16v-64h56c8.844 0 16-7.156 16-16s-7.156-16-16-16h-56v-48h80c8.8 0 16-7.2 16-16s-7.2-16-16-16zm-160 0c-8.8 0-16 7.2-16 16v160c0 8.844 7.156 16 16 16s16-7.156 16-16V176c0-8.8-7.2-16-16-16zm-64 80h-64c-8.8 0-16 7.2-16 16s7.156 16 16 16h48v33.45c-24.83 19.91-69.13 18.16-91.48-4.203-24.95-24.95-24.95-65.55 0-90.5 25.03-25 64.19-25 89.22 0 6.25 6.25 16.38 6.25 22.62 0s6.25-16.38 0-22.62c-37.69-37.72-96.78-37.72-134.5 0-37.42 37.42-37.42 98.33 0 135.8C127.8 341.8 153.5 352 180.6 352c27.06 0 52.84-10.25 70.7-28.12 3-2.98 4.7-7.08 4.7-11.28V256c0-8.8-7.2-16-16-16z"
  })
});

// src/icons/input-props.tsx
import { jsx as jsx187 } from "react/jsx-runtime";
var InputPropsIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx187("svg", {
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 576 512",
    ...props,
    children: /* @__PURE__ */ jsx187("path", {
      fill: color ?? CURRENT_COLOR_LOWERCASE,
      d: "M144 32c-44.2 0-80 35.8-80 80v53.5c0 12.7-5.1 24.9-14.1 33.9L4.7 244.7C1.7 247.7 0 251.8 0 256s1.7 8.3 4.7 11.3L50 312.6c9 9 14.1 21.2 14.1 33.9V400c0 44.2 35.8 80 80 80h64c8.8 0 16-7.2 16-16s-7.2-16-16-16h-64c-26.5 0-48-21.5-48-48v-53.5c0-21.2-8.4-41.6-23.4-56.6L38.6 256l34-33.9c15-15 23.4-35.4 23.4-56.6V112c0-26.5 21.5-48 48-48h64c8.8 0 16-7.2 16-16s-7.2-16-16-16h-64zm288 0h-64c-8.8 0-16 7.2-16 16s7.2 16 16 16h64c26.5 0 48 21.5 48 48v53.5c0 21.2 8.4 41.6 23.4 56.6l33.9 33.9-33.9 33.9c-15 15-23.4 35.4-23.4 56.6V400c0 26.5-21.5 48-48 48h-64c-8.8 0-16 7.2-16 16s7.2 16 16 16h64c44.2 0 80-35.8 80-80v-53.5c0-12.7 5.1-24.9 14.1-33.9l45.3-45.3c3-3 4.7-7.1 4.7-11.3s-1.7-8.3-4.7-11.3l-45.3-45.3c-9-9-14.1-21.2-14.1-33.9V112c0-44.2-35.8-80-80-80z"
    })
  });
};

// src/components/RenderModal/CrfSetting.tsx
import { BrowserSafeApis as BrowserSafeApis2 } from "@remotion/renderer/client";
import { useState as useState73 } from "react";
import { jsx as jsx188 } from "react/jsx-runtime";
var getDefaultCrfState = () => {
  return BrowserSafeApis2.validCodecs.map((c) => {
    return [c, BrowserSafeApis2.getDefaultCrfForCodec(c)];
  }).reduce((acc, [codec, crf]) => {
    return {
      ...acc,
      [codec]: crf
    };
  }, {});
};
var useCrfState = (codec) => {
  const [state, setState] = useState73(() => getDefaultCrfState());
  const range = BrowserSafeApis2.getValidCrfRanges(codec);
  const setCrf = (updater) => {
    setState((q) => {
      const val = q[codec];
      if (val === null) {
        throw new TypeError(`Got unexpected codec "${codec}"`);
      }
      return {
        ...q,
        [codec]: typeof updater === "number" ? updater : updater(val)
      };
    });
  };
  return {
    crf: state[codec],
    setCrf,
    minCrf: range[0],
    maxCrf: range[1]
  };
};
var CrfSetting = ({ crf, setCrf, min, max, option }) => {
  return /* @__PURE__ */ jsx188(NumberSetting, {
    min,
    max,
    name: "CRF",
    onValueChanged: setCrf,
    value: crf,
    step: 1,
    hint: option
  });
};

// src/components/RenderModal/get-default-codecs.ts
import { BrowserSafeApis as BrowserSafeApis3 } from "@remotion/renderer/client";
import { NoReactAPIs } from "@remotion/renderer/pure";
var getDefaultCodecs = ({
  defaultConfigurationVideoCodec,
  compositionDefaultVideoCodec,
  renderType,
  defaultConfigurationAudioCodec
}) => {
  const userPreferredVideoCodec = compositionDefaultVideoCodec ?? defaultConfigurationVideoCodec ?? "h264";
  const userPreferredVideoCodecForAudioTab = userPreferredVideoCodec === "aac" ? "aac" : userPreferredVideoCodec === "mp3" ? "mp3" : userPreferredVideoCodec === "wav" ? "wav" : defaultConfigurationAudioCodec === "pcm-16" ? "wav" : defaultConfigurationAudioCodec === "mp3" ? "mp3" : "aac";
  const isVideoCodecAnAudioCodec = NoReactAPIs.isAudioCodec(userPreferredVideoCodec);
  if (isVideoCodecAnAudioCodec) {
    return {
      initialAudioCodec: null,
      initialRenderType: "audio",
      initialVideoCodec: userPreferredVideoCodec,
      initialVideoCodecForAudioTab: userPreferredVideoCodecForAudioTab,
      initialVideoCodecForVideoTab: NoReactAPIs.isAudioCodec(defaultConfigurationVideoCodec) ? "h264" : defaultConfigurationVideoCodec
    };
  }
  const suitableAudioCodecForVideoCodec = BrowserSafeApis3.defaultAudioCodecs[userPreferredVideoCodec].compressed;
  return {
    initialAudioCodec: defaultConfigurationAudioCodec ?? suitableAudioCodecForVideoCodec,
    initialVideoCodec: userPreferredVideoCodec,
    initialRenderType: renderType,
    initialVideoCodecForAudioTab: userPreferredVideoCodecForAudioTab,
    initialVideoCodecForVideoTab: userPreferredVideoCodec
  };
};

// src/components/RenderModal/get-string-before-suffix.ts
var getStringBeforeSuffix = (fileName) => {
  const dotPos = fileName.lastIndexOf(".");
  if (dotPos === -1) {
    return fileName;
  }
  return fileName.substring(0, dotPos);
};

// src/components/RenderModal/out-name-checker.ts
import { BrowserSafeApis as BrowserSafeApis4 } from "@remotion/renderer/client";
var invalidCharacters = ["?", "*", "+", ":", "%"];
var isValidStillExtension = (extension, stillImageFormat) => {
  if (stillImageFormat === "jpeg" && extension === "jpg") {
    return true;
  }
  return extension === stillImageFormat;
};
var validateOutnameGui = ({
  outName,
  codec,
  audioCodec,
  renderMode,
  stillImageFormat,
  separateAudioTo
}) => {
  try {
    isValidOutName({
      audioCodec,
      codec,
      outName,
      renderMode,
      stillImageFormat,
      separateAudioTo
    });
    return { valid: true };
  } catch (err) {
    return { valid: false, error: err };
  }
};
var isValidOutName = ({
  outName,
  codec,
  audioCodec,
  renderMode,
  stillImageFormat,
  separateAudioTo
}) => {
  const extension = outName.substring(outName.lastIndexOf(".") + 1);
  const prefix = outName.substring(0, outName.lastIndexOf("."));
  const map = BrowserSafeApis4.defaultFileExtensionMap[codec];
  if (BrowserSafeApis4.supportedAudioCodecs[codec].length > 0 && !(audioCodec in map.forAudioCodec)) {
    throw new Error(`Audio codec ${audioCodec} is not supported for codec ${codec}`);
  }
  const hasDotAfterSlash = () => {
    const substrings = prefix.split("/");
    for (const str of substrings) {
      if (str[0] === ".") {
        return true;
      }
    }
    return false;
  };
  const hasInvalidChar = () => {
    return prefix.split("").some((char) => invalidCharacters.includes(char));
  };
  if (renderMode === "video" || renderMode === "audio") {
    BrowserSafeApis4.validateOutputFilename({
      codec,
      audioCodecSetting: audioCodec ?? null,
      extension,
      preferLossless: false,
      separateAudioTo
    });
  }
  if (prefix.length < 1 && renderMode !== "sequence") {
    throw new Error("The prefix must be at least 1 character long");
  }
  if (prefix[0] === "." || hasDotAfterSlash()) {
    throw new Error("The output name must not start with a dot");
  }
  if (hasInvalidChar()) {
    throw new Error("Filename can't contain the following characters:  ?, *, +, %, :");
  }
  if (renderMode === "still" && stillImageFormat && !isValidStillExtension(extension, stillImageFormat)) {
    throw new Error(`The extension ${extension} is not supported for still image format ${stillImageFormat}`);
  }
  if (renderMode === "sequence") {
    if (outName.includes(".")) {
      throw new Error("Folder names must not contain a dot");
    }
  }
};

// src/components/RenderModal/RenderModalAdvanced.tsx
import { BrowserSafeApis as BrowserSafeApis5 } from "@remotion/renderer/client";
import { useCallback as useCallback96, useMemo as useMemo107 } from "react";
import { jsx as jsx189, jsxs as jsxs110 } from "react/jsx-runtime";
var container36 = {
  flex: 1,
  overflowY: "auto"
};
var RenderModalAdvanced = ({
  renderMode,
  maxConcurrency,
  minConcurrency,
  setConcurrency,
  concurrency,
  delayRenderTimeout,
  setDelayRenderTimeout,
  setDisableWebSecurity,
  setIgnoreCertificateErrors,
  setHeadless,
  headless,
  ignoreCertificateErrors,
  disableWebSecurity,
  openGlOption,
  setOpenGlOption,
  setMediaCacheSizeInBytes,
  mediaCacheSizeInBytes,
  offthreadVideoCacheSizeInBytes,
  setOffthreadVideoCacheSizeInBytes,
  offthreadVideoThreads,
  setOffthreadVideoThreads,
  enableMultiProcessOnLinux,
  setChromiumMultiProcessOnLinux,
  setUserAgent,
  userAgent,
  beep,
  setBeep,
  repro,
  setRepro,
  chromeModeOption,
  setChromeModeOption,
  darkMode,
  setDarkMode
}) => {
  const extendedOpenGlOptions = useMemo107(() => {
    return [
      "angle",
      "egl",
      "swangle",
      "swiftshader",
      "vulkan",
      "angle-egl",
      "default"
    ];
  }, []);
  const toggleCustomMediaCacheSizeInBytes = useCallback96(() => {
    setMediaCacheSizeInBytes((previous) => {
      if (previous === null) {
        return 1000 * 1000 * 1000;
      }
      return null;
    });
  }, [setMediaCacheSizeInBytes]);
  const toggleCustomOffthreadVideoCacheSizeInBytes = useCallback96(() => {
    setOffthreadVideoCacheSizeInBytes((previous) => {
      if (previous === null) {
        return 512 * 1024 * 1024;
      }
      return null;
    });
  }, [setOffthreadVideoCacheSizeInBytes]);
  const toggleCustomOffthreadVideoThreads = useCallback96(() => {
    setOffthreadVideoThreads((previous) => {
      if (previous === null) {
        return 2;
      }
      return null;
    });
  }, [setOffthreadVideoThreads]);
  const toggleCustomUserAgent = useCallback96(() => {
    setUserAgent((previous) => {
      if (previous === null) {
        return "Mozilla/5.0 (Remotion)";
      }
      return null;
    });
  }, [setUserAgent]);
  const onDisableWebSecurityChanged = useCallback96((e) => {
    setDisableWebSecurity(e.target.checked);
  }, [setDisableWebSecurity]);
  const onEnableMultiProcessOnLinux = useCallback96((e) => {
    setChromiumMultiProcessOnLinux(e.target.checked);
  }, [setChromiumMultiProcessOnLinux]);
  const onIgnoreCertificatErrors = useCallback96((e) => {
    setIgnoreCertificateErrors(e.target.checked);
  }, [setIgnoreCertificateErrors]);
  const onHeadless = useCallback96((e) => {
    setHeadless(e.target.checked);
  }, [setHeadless]);
  const onUserAgentChanged = useCallback96((e) => {
    setUserAgent(e.target.value);
  }, [setUserAgent]);
  const onDarkMode = useCallback96((e) => {
    setDarkMode(e.target.checked);
  }, [setDarkMode]);
  const onPlayBeepSound = useCallback96((e) => {
    setBeep(e.target.checked);
  }, [setBeep]);
  const onReproToggle = useCallback96((e) => {
    setRepro(e.target.checked);
  }, [setRepro]);
  const openGlOptions = useMemo107(() => {
    return extendedOpenGlOptions.map((option) => {
      return {
        label: option === "default" ? "Default" : option,
        onClick: () => setOpenGlOption(option),
        key: option,
        leftItem: openGlOption === option ? /* @__PURE__ */ jsx189(Checkmark, {}) : null,
        id: option,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: option
      };
    });
  }, [extendedOpenGlOptions, openGlOption, setOpenGlOption]);
  const chromeModeOptions = useMemo107(() => {
    return BrowserSafeApis5.validChromeModeOptions.map((option) => {
      return {
        label: option,
        onClick: () => setChromeModeOption(option),
        key: option,
        leftItem: chromeModeOption === option ? /* @__PURE__ */ jsx189(Checkmark, {}) : null,
        id: option,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: option
      };
    });
  }, [chromeModeOption, setChromeModeOption]);
  const changeMediaCacheSizeInBytes = useCallback96((cb) => {
    setMediaCacheSizeInBytes((prev) => {
      if (prev === null) {
        throw new TypeError("Expected previous value");
      }
      if (typeof cb === "function") {
        return cb(prev);
      }
      return cb;
    });
  }, [setMediaCacheSizeInBytes]);
  const changeOffthreadVideoCacheSizeInBytes = useCallback96((cb) => {
    setOffthreadVideoCacheSizeInBytes((prev) => {
      if (prev === null) {
        throw new TypeError("Expected previous value");
      }
      if (typeof cb === "function") {
        return cb(prev);
      }
      return cb;
    });
  }, [setOffthreadVideoCacheSizeInBytes]);
  const changeOffthreadVideoThreads = useCallback96((cb) => {
    setOffthreadVideoThreads((prev) => {
      if (prev === null) {
        throw new TypeError("Expected previous value");
      }
      if (typeof cb === "function") {
        return cb(prev);
      }
      return cb;
    });
  }, [setOffthreadVideoThreads]);
  return /* @__PURE__ */ jsxs110("div", {
    style: container36,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: [
      renderMode === "still" ? null : /* @__PURE__ */ jsx189(NumberSetting, {
        min: minConcurrency,
        max: maxConcurrency,
        step: 1,
        name: "Concurrency",
        formatter: (w) => `${w}x`,
        onValueChanged: setConcurrency,
        value: concurrency
      }),
      /* @__PURE__ */ jsx189(NumberSetting, {
        min: 7000,
        max: 900000,
        name: "delayRender() timeout",
        onValueChanged: setDelayRenderTimeout,
        formatter: (w) => `${w}ms`,
        step: 1000,
        hint: "delayRenderTimeoutInMillisecondsOption",
        value: delayRenderTimeout
      }),
      renderMode === "audio" ? null : /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Custom @remotion/media cache size",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "mediaCacheSizeInBytesOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: mediaCacheSizeInBytes !== null,
              onChange: toggleCustomMediaCacheSizeInBytes,
              name: "media-cache-size"
            })
          })
        ]
      }),
      renderMode === "audio" || mediaCacheSizeInBytes === null ? null : /* @__PURE__ */ jsx189(NumberSetting, {
        min: 0,
        max: 2000 * 1024 * 1024,
        step: 1024,
        name: "@remotion/media cache size",
        formatter: (w) => `${w} bytes`,
        onValueChanged: changeMediaCacheSizeInBytes,
        value: mediaCacheSizeInBytes
      }),
      renderMode === "audio" ? null : /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Custom OffthreadVideo cache",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "offthreadVideoCacheSizeInBytesOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: offthreadVideoCacheSizeInBytes !== null,
              onChange: toggleCustomOffthreadVideoCacheSizeInBytes,
              name: "custom-audio-bitrate"
            })
          })
        ]
      }),
      renderMode === "audio" || offthreadVideoCacheSizeInBytes === null ? null : /* @__PURE__ */ jsx189(NumberSetting, {
        min: 0,
        max: 2000 * 1024 * 1024,
        step: 1024,
        name: "OffthreadVideo cache size",
        formatter: (w) => `${w} bytes`,
        onValueChanged: changeOffthreadVideoCacheSizeInBytes,
        value: offthreadVideoCacheSizeInBytes
      }),
      renderMode === "audio" ? null : /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "OffthreadVideo threads ",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "offthreadVideoThreadsOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: offthreadVideoThreads !== null,
              onChange: toggleCustomOffthreadVideoThreads,
              name: "offthread-video-threads"
            })
          })
        ]
      }),
      renderMode === "audio" || offthreadVideoThreads === null ? null : /* @__PURE__ */ jsx189(NumberSetting, {
        min: 0,
        max: 16,
        step: 1,
        name: "OffthreadVideo threads",
        formatter: (w) => `${w}x`,
        onValueChanged: changeOffthreadVideoThreads,
        value: offthreadVideoThreads
      }),
      /* @__PURE__ */ jsx189(RenderModalHr, {}),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx189("div", {
            style: label,
            children: "Disable web security"
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: disableWebSecurity,
              onChange: onDisableWebSecurityChanged,
              name: "disable-web-security"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx189("div", {
            style: label,
            children: "Ignore certificate errors"
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: ignoreCertificateErrors,
              onChange: onIgnoreCertificatErrors,
              name: "ignore-certificate-errors"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Headless mode",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "headlessOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: headless,
              onChange: onHeadless,
              name: "headless"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Chrome Mode ",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "chromeModeOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Combobox, {
              values: chromeModeOptions,
              selectedId: chromeModeOption,
              title: "Chrome mode"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "OpenGL render backend ",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "glOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Combobox, {
              values: openGlOptions,
              selectedId: openGlOption,
              title: "OpenGl option"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Multi-process Chrome on Linux",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "enableMultiprocessOnLinuxOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: enableMultiProcessOnLinux,
              onChange: onEnableMultiProcessOnLinux,
              name: "enable-multi-process-on-linux"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Dark Mode",
              /* @__PURE__ */ jsx189(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "darkModeOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: darkMode,
              onChange: onDarkMode,
              name: "dark-mode"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx189("div", {
            style: label,
            children: "Custom User Agent"
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: userAgent !== null,
              onChange: toggleCustomUserAgent,
              name: "custom-user-agent"
            })
          })
        ]
      }),
      userAgent === null ? null : /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx189("div", {
            style: label,
            children: "User Agent"
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189("div", {
              children: /* @__PURE__ */ jsx189(RemotionInput, {
                style: input,
                value: userAgent,
                onChange: onUserAgentChanged,
                status: "ok",
                rightAlign: true
              })
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Create a reproduction ",
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "reproOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: repro,
              onChange: onReproToggle,
              name: "repro"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs110("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs110("div", {
            style: label,
            children: [
              "Beep when finished ",
              /* @__PURE__ */ jsx189(OptionExplainerBubble, {
                id: "beepOnFinishOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx189("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx189(Checkbox, {
              checked: beep,
              onChange: onPlayBeepSound,
              name: "beep-when-finished"
            })
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/RenderModalAudio.tsx
import { BrowserSafeApis as BrowserSafeApis7 } from "@remotion/renderer/client";
import { useCallback as useCallback100, useMemo as useMemo109 } from "react";

// src/components/RenderModal/EnforceAudioTrackSetting.tsx
import { useCallback as useCallback97 } from "react";
import { jsx as jsx190, jsxs as jsxs111 } from "react/jsx-runtime";
var EnforceAudioTrackSetting = ({ enforceAudioTrack, muted, setEnforceAudioTrack }) => {
  const onEnforceAudioTrackChanged = useCallback97((e) => {
    setEnforceAudioTrack(e.target.checked);
  }, [setEnforceAudioTrack]);
  return /* @__PURE__ */ jsxs111("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsxs111("div", {
        style: label,
        children: [
          "Enforce Audio Track",
          /* @__PURE__ */ jsx190(Spacing, {
            x: 0.5
          }),
          /* @__PURE__ */ jsx190(OptionExplainerBubble, {
            id: "enforceAudioOption"
          })
        ]
      }),
      /* @__PURE__ */ jsx190("div", {
        style: rightRow,
        children: /* @__PURE__ */ jsx190(Checkbox, {
          disabled: muted && !enforceAudioTrack,
          checked: enforceAudioTrack,
          onChange: onEnforceAudioTrackChanged,
          name: "enforce-audio-track"
        })
      })
    ]
  });
};

// src/components/RenderModal/human-readable-audio-codecs.ts
var humanReadableAudioCodec = (audioCodec) => {
  if (audioCodec === "aac") {
    return "AAC";
  }
  if (audioCodec === "mp3") {
    return "MP3";
  }
  if (audioCodec === "pcm-16") {
    return "Lossless";
  }
  if (audioCodec === "opus") {
    return "Opus";
  }
};

// src/components/RenderModal/MutedSetting.tsx
import { useCallback as useCallback98 } from "react";
import { jsx as jsx191, jsxs as jsxs112 } from "react/jsx-runtime";
var MutedSetting = ({ muted, setMuted, enforceAudioTrack }) => {
  const onMutedChanged = useCallback98((e) => {
    setMuted(e.target.checked);
  }, [setMuted]);
  return /* @__PURE__ */ jsxs112("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsxs112("div", {
        style: label,
        children: [
          "Muted",
          /* @__PURE__ */ jsx191(Spacing, {
            x: 0.5
          }),
          /* @__PURE__ */ jsx191(OptionExplainerBubble, {
            id: "mutedOption"
          })
        ]
      }),
      /* @__PURE__ */ jsx191(Spacing, {
        x: 0.25
      }),
      /* @__PURE__ */ jsx191("div", {
        style: rightRow,
        children: /* @__PURE__ */ jsx191(Checkbox, {
          checked: muted,
          disabled: enforceAudioTrack && !muted,
          onChange: onMutedChanged,
          name: "muted"
        })
      })
    ]
  });
};

// src/components/RenderModal/SeparateAudioOption.tsx
import { BrowserSafeApis as BrowserSafeApis6 } from "@remotion/renderer/client";
import { useCallback as useCallback99, useMemo as useMemo108 } from "react";

// src/helpers/use-file-existence.ts
import { useContext as useContext115, useEffect as useEffect75, useRef as useRef61, useState as useState74 } from "react";
var useFileExistence = (outName) => {
  const [exists, setExists] = useState74(false);
  const { previewServerState: state, subscribeToEvent } = useContext115(StudioServerConnectionCtx);
  const clientId = state.type === "connected" ? state.clientId : undefined;
  const currentOutName = useRef61("");
  currentOutName.current = outName;
  useEffect75(() => {
    if (!clientId) {
      return;
    }
    subscribeToFileExistenceWatcher({
      file: outName,
      clientId
    }).then((_exists) => {
      if (currentOutName.current === outName) {
        setExists(_exists);
      }
    });
    return () => {
      unsubscribeFromFileExistenceWatcher({ file: outName, clientId });
    };
  }, [clientId, outName]);
  useEffect75(() => {
    const listener = (event) => {
      if (event.type !== "watched-file-deleted") {
        return;
      }
      if (event.file !== currentOutName.current) {
        return;
      }
      if (currentOutName.current === outName) {
        setExists(false);
      }
    };
    const unsub = subscribeToEvent("watched-file-deleted", listener);
    return () => {
      unsub();
    };
  }, [outName, subscribeToEvent]);
  useEffect75(() => {
    const listener = (event) => {
      if (event.type !== "watched-file-undeleted") {
        return;
      }
      if (event.file !== outName) {
        return;
      }
      if (currentOutName.current === outName) {
        setExists(true);
      }
    };
    const unsub = subscribeToEvent("watched-file-undeleted", listener);
    return () => {
      unsub();
    };
  }, [outName, subscribeToEvent]);
  return exists;
};

// src/components/RenderModal/SeparateAudioOption.tsx
import { jsx as jsx192, jsxs as jsxs113, Fragment as Fragment49 } from "react/jsx-runtime";
var SeparateAudioOptionInput = ({ separateAudioTo, setSeparateAudioTo, audioCodec }) => {
  const existence = useFileExistence(separateAudioTo);
  const onValueChange = useCallback99((e) => {
    setSeparateAudioTo(e.target.value);
  }, [setSeparateAudioTo]);
  const validationMessage = useMemo108(() => {
    const expectedExtension = BrowserSafeApis6.getExtensionFromAudioCodec(audioCodec);
    const actualExtension = separateAudioTo.split(".").pop();
    if (actualExtension !== expectedExtension) {
      return `Expected extension: .${expectedExtension}`;
    }
    return null;
  }, [audioCodec, separateAudioTo]);
  return /* @__PURE__ */ jsx192(RenderModalOutputName, {
    ariaLabel: null,
    existingOutputPath: null,
    existence,
    inputStyle: input,
    onValueChange,
    outName: separateAudioTo,
    label: "Separate audio to",
    validationMessage
  });
};
var SeparateAudioOption = ({ separateAudioTo, setSeparateAudioTo, audioCodec, outName }) => {
  const onSeparateAudioChange = useCallback99((e) => {
    if (e.target.checked) {
      const extension = BrowserSafeApis6.getExtensionFromAudioCodec(audioCodec);
      setSeparateAudioTo(`${getStringBeforeSuffix(outName)}.${extension}`);
    } else {
      setSeparateAudioTo(null);
    }
  }, [audioCodec, outName, setSeparateAudioTo]);
  return /* @__PURE__ */ jsxs113(Fragment49, {
    children: [
      /* @__PURE__ */ jsxs113("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs113("div", {
            style: label,
            children: [
              "Separate audio",
              /* @__PURE__ */ jsx192(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx192(OptionExplainerBubble, {
                id: "separateAudioOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx192("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx192(Checkbox, {
              disabled: false,
              checked: Boolean(separateAudioTo),
              onChange: onSeparateAudioChange,
              name: "separate-audio-to"
            })
          })
        ]
      }),
      separateAudioTo === null ? null : /* @__PURE__ */ jsx192(SeparateAudioOptionInput, {
        separateAudioTo,
        setSeparateAudioTo,
        audioCodec
      })
    ]
  });
};

// src/components/RenderModal/RenderModalAudio.tsx
import { jsx as jsx193, jsxs as jsxs114, Fragment as Fragment50 } from "react/jsx-runtime";
var container37 = {
  flex: 1,
  overflowY: "auto"
};
var commonSampleRates = [8000, 16000, 22050, 44100, 48000, 96000];
var RenderModalAudio = ({
  muted,
  setMuted,
  renderMode,
  enforceAudioTrack,
  setEnforceAudioTrackState,
  setShouldHaveCustomTargetAudioBitrate,
  shouldHaveCustomTargetAudioBitrate,
  setCustomTargetAudioBitrateValue,
  customTargetAudioBitrate,
  audioCodec,
  codec,
  setAudioCodec,
  forSeamlessAacConcatenation,
  setForSeamlessAacConcatenation,
  separateAudioTo,
  setSeparateAudioTo,
  outName,
  sampleRate,
  setSampleRate
}) => {
  const onShouldHaveTargetAudioBitrateChanged = useCallback100((e) => {
    setShouldHaveCustomTargetAudioBitrate(e.target.checked);
  }, [setShouldHaveCustomTargetAudioBitrate]);
  const onTargetAudioBitrateChanged = useCallback100((e) => {
    setCustomTargetAudioBitrateValue(e.target.value);
  }, [setCustomTargetAudioBitrateValue]);
  const onSeamlessAacConcatenationChanges = useCallback100((e) => {
    setForSeamlessAacConcatenation(e.target.checked);
  }, [setForSeamlessAacConcatenation]);
  const sampleRateOptions = useMemo109(() => {
    return commonSampleRates.map((rate) => ({
      label: rate === 48000 ? `${rate} Hz (default)` : `${rate} Hz`,
      onClick: () => setSampleRate(rate),
      key: String(rate),
      leftItem: sampleRate === rate ? /* @__PURE__ */ jsx193(Checkmark, {}) : null,
      id: String(rate),
      keyHint: null,
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: String(rate)
    }));
  }, [sampleRate, setSampleRate]);
  const audioCodecOptions = useCallback100((currentCodec) => {
    return BrowserSafeApis7.supportedAudioCodecs[currentCodec].map((audioCodecOption) => {
      return {
        label: humanReadableAudioCodec(audioCodecOption),
        onClick: () => setAudioCodec(audioCodecOption),
        key: audioCodecOption,
        leftItem: codec === audioCodecOption ? /* @__PURE__ */ jsx193(Checkmark, {}) : null,
        id: audioCodecOption,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: audioCodecOption
      };
    });
  }, [codec, setAudioCodec]);
  return /* @__PURE__ */ jsxs114("div", {
    style: container37,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: [
      renderMode === "video" ? /* @__PURE__ */ jsxs114(Fragment50, {
        children: [
          /* @__PURE__ */ jsx193(MutedSetting, {
            enforceAudioTrack,
            muted,
            setMuted
          }),
          /* @__PURE__ */ jsx193(RenderModalHr, {})
        ]
      }) : null,
      renderMode === "video" && audioCodecOptions(codec).length >= 2 && !muted ? /* @__PURE__ */ jsxs114("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs114("div", {
            style: label,
            children: [
              "Audio Codec ",
              /* @__PURE__ */ jsx193(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx193(OptionExplainerBubble, {
                id: "audioCodecOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx193("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx193(Combobox, {
              values: audioCodecOptions(codec),
              selectedId: audioCodec,
              title: "AudioCodec"
            })
          })
        ]
      }) : null,
      (renderMode === "video" || renderMode === "audio") && !muted && /* @__PURE__ */ jsxs114(Fragment50, {
        children: [
          /* @__PURE__ */ jsx193(EnforceAudioTrackSetting, {
            muted,
            enforceAudioTrack,
            setEnforceAudioTrack: setEnforceAudioTrackState
          }),
          /* @__PURE__ */ jsx193(RenderModalHr, {})
        ]
      }),
      renderMode === "video" && !muted ? /* @__PURE__ */ jsx193(SeparateAudioOption, {
        separateAudioTo,
        setSeparateAudioTo,
        audioCodec,
        outName
      }) : null,
      audioCodec === "aac" && !muted ? /* @__PURE__ */ jsxs114("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs114("div", {
            style: label,
            children: [
              "For seamless AAC concatenation",
              /* @__PURE__ */ jsx193(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx193(OptionExplainerBubble, {
                id: "forSeamlessAacConcatenationOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx193("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx193(Checkbox, {
              disabled: false,
              checked: forSeamlessAacConcatenation,
              onChange: onSeamlessAacConcatenationChanges,
              name: "enforce-audio-track"
            })
          })
        ]
      }) : null,
      renderMode === "still" || muted ? null : /* @__PURE__ */ jsxs114("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs114("div", {
            style: label,
            children: [
              "Custom audio bitrate",
              " ",
              /* @__PURE__ */ jsx193(OptionExplainerBubble, {
                id: "audioBitrateOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx193("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx193(Checkbox, {
              checked: shouldHaveCustomTargetAudioBitrate,
              onChange: onShouldHaveTargetAudioBitrateChanged,
              name: "custom-audio-bitrate"
            })
          })
        ]
      }),
      shouldHaveCustomTargetAudioBitrate && renderMode !== "still" && !muted ? /* @__PURE__ */ jsxs114("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx193("div", {
            style: label,
            children: "Target audio bitrate"
          }),
          /* @__PURE__ */ jsx193("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx193("div", {
              children: /* @__PURE__ */ jsx193(RemotionInput, {
                style: input,
                value: customTargetAudioBitrate,
                onChange: onTargetAudioBitrateChanged,
                status: "ok",
                rightAlign: true
              })
            })
          })
        ]
      }) : null,
      renderMode !== "still" && !muted ? /* @__PURE__ */ jsxs114("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs114("div", {
            style: label,
            children: [
              "Sample Rate ",
              /* @__PURE__ */ jsx193(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx193(OptionExplainerBubble, {
                id: "sampleRateOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx193("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx193(Combobox, {
              values: sampleRateOptions,
              selectedId: String(sampleRate),
              title: "Sample Rate"
            })
          })
        ]
      }) : null
    ]
  });
};

// src/components/RenderModal/RenderModalBasic.tsx
import { BrowserSafeApis as BrowserSafeApis8 } from "@remotion/renderer/client";
import { NoReactAPIs as NoReactAPIs2 } from "@remotion/renderer/pure";
import { useCallback as useCallback103, useMemo as useMemo111 } from "react";

// src/helpers/prores-labels.ts
var labelProResProfile = (profile) => {
  if (profile === "4444") {
    return "4444";
  }
  if (profile === "4444-xq") {
    return "4444 XQ (Best)";
  }
  if (profile === "hq") {
    return "HQ";
  }
  if (profile === "proxy") {
    return "Proxy (Worst)";
  }
  if (profile === "light") {
    return "Light";
  }
  if (profile === "standard") {
    return "Standard";
  }
  throw new TypeError(`Unknown ProRes profile: ${profile}`);
};

// src/components/RenderModal/FrameRangeSetting.tsx
import { useCallback as useCallback102 } from "react";

// src/components/RenderModal/MultiRangeSlider.tsx
import { useCallback as useCallback101, useMemo as useMemo110 } from "react";
import { jsx as jsx194, jsxs as jsxs115 } from "react/jsx-runtime";
var container38 = {
  height: 6,
  flex: 1,
  minWidth: 80,
  maxWidth: 220,
  position: "relative",
  backgroundColor: WHITE_ALPHA_10,
  marginLeft: 8,
  marginRight: 8,
  borderRadius: 3
};
var sliderRange = {
  position: "absolute",
  top: 0,
  backgroundColor: BLUE,
  height: 6,
  borderRadius: 3
};
var MultiRangeSlider = ({
  min,
  max,
  start,
  end,
  step,
  onLeftThumbDrag,
  onRightThumbDrag
}) => {
  const getPercent = useCallback101((value) => Math.round((value - min) / (max - min) * 100), [min, max]);
  const rangeStyle = useMemo110(() => {
    const minPercent = getPercent(start);
    const maxPercent = getPercent(end);
    return {
      ...sliderRange,
      left: `${minPercent}%`,
      width: `${maxPercent - minPercent}%`
    };
  }, [end, getPercent, start]);
  const onChangeLeft = useCallback101((event) => {
    const value = Math.min(Number(event.target.value), end - 1);
    onLeftThumbDrag(value);
  }, [end, onLeftThumbDrag]);
  const onChangeRight = useCallback101((event) => {
    const value = Math.max(Number(event.target.value), start + 1);
    onRightThumbDrag(value);
  }, [onRightThumbDrag, start]);
  return /* @__PURE__ */ jsxs115("div", {
    style: container38,
    children: [
      /* @__PURE__ */ jsx194("input", {
        type: "range",
        min,
        max,
        value: start,
        step,
        onChange: onChangeLeft,
        className: "__remotion_thumb"
      }),
      /* @__PURE__ */ jsx194("input", {
        type: "range",
        min,
        max,
        value: end,
        step,
        onChange: onChangeRight,
        className: "__remotion_thumb"
      }),
      /* @__PURE__ */ jsx194("div", {
        style: rangeStyle
      })
    ]
  });
};

// src/components/RenderModal/FrameRangeSetting.tsx
import { jsx as jsx195, jsxs as jsxs116 } from "react/jsx-runtime";
var INPUT_WIDTH = 40;
var startFrameInputContainer = {
  display: "flex",
  justifyContent: "flex-end",
  width: INPUT_WIDTH
};
var FrameRangeSetting = ({ startFrame, endFrame, setEndFrame, durationInFrames, setStartFrame }) => {
  const minStartFrame = 0;
  const maxEndFrame = durationInFrames - 1;
  const onStartFrameChangedDirectly = useCallback102((newStartFrame) => {
    setStartFrame(newStartFrame);
  }, [setStartFrame]);
  const onEndFrameChangedDirectly = useCallback102((newEndFrame) => {
    setEndFrame(newEndFrame);
  }, [setEndFrame]);
  const onStartFrameChanged = useCallback102((newVal) => {
    onStartFrameChangedDirectly(parseInt(newVal, 10));
  }, [onStartFrameChangedDirectly]);
  const onEndFrameChanged = useCallback102((newVal) => {
    onEndFrameChangedDirectly(parseInt(newVal, 10));
  }, [onEndFrameChangedDirectly]);
  return /* @__PURE__ */ jsxs116("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsx195("div", {
        style: label,
        children: "Frame range"
      }),
      /* @__PURE__ */ jsxs116("div", {
        style: rightRow,
        children: [
          /* @__PURE__ */ jsx195("div", {
            style: startFrameInputContainer,
            children: /* @__PURE__ */ jsx195(InputDragger, {
              min: minStartFrame,
              max: endFrame - 1,
              name: "Start frame",
              value: startFrame,
              step: 1,
              onTextChange: onStartFrameChanged,
              onValueChange: onStartFrameChangedDirectly,
              status: "ok",
              rightAlign: true,
              style: { width: INPUT_WIDTH }
            })
          }),
          /* @__PURE__ */ jsx195(MultiRangeSlider, {
            min: minStartFrame,
            max: maxEndFrame,
            start: startFrame,
            end: endFrame,
            step: 1,
            onLeftThumbDrag: onStartFrameChangedDirectly,
            onRightThumbDrag: onEndFrameChangedDirectly
          }),
          " ",
          /* @__PURE__ */ jsx195("div", {
            style: { width: INPUT_WIDTH },
            children: /* @__PURE__ */ jsx195(InputDragger, {
              min: startFrame + 1,
              max: maxEndFrame,
              name: "End frame",
              value: endFrame,
              step: 1,
              onTextChange: onEndFrameChanged,
              onValueChange: onEndFrameChangedDirectly,
              status: "ok",
              rightAlign: true,
              style: { width: INPUT_WIDTH }
            })
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/human-readable-codec.ts
var humanReadableCodec = (codec) => {
  if (codec === "aac") {
    return "AAC";
  }
  if (codec === "mp3") {
    return "MP3";
  }
  if (codec === "gif") {
    return "GIF";
  }
  if (codec === "h264") {
    return "H.264";
  }
  if (codec === "h264-mkv") {
    return "H.264 (Matroska)";
  }
  if (codec === "h264-ts") {
    return "H.264 (Transport Stream)";
  }
  if (codec === "h265") {
    return "H.265";
  }
  if (codec === "av1") {
    return "AV1";
  }
  if (codec === "prores") {
    return "ProRes";
  }
  if (codec === "vp8") {
    return "VP8";
  }
  if (codec === "vp9") {
    return "VP9";
  }
  if (codec === "wav") {
    return "Waveform";
  }
  throw new TypeError(`Got unexpected codec "${codec}"`);
};

// src/components/RenderModal/human-readable-loglevel.ts
var humanReadableLogLevel = (logLevel) => {
  if (logLevel === "trace") {
    return "Trace";
  }
  if (logLevel === "verbose") {
    return "Verbose";
  }
  if (logLevel === "info") {
    return "Info";
  }
  if (logLevel === "warn") {
    return "Warn";
  }
  if (logLevel === "error") {
    return "Error";
  }
  throw new TypeError(`Got unexpected log level "${logLevel}"`);
};

// src/components/RenderModal/RenderModalBasic.tsx
import { jsx as jsx196, jsxs as jsxs117 } from "react/jsx-runtime";
var container39 = {
  flex: 1
};
var RenderModalBasic = ({
  renderMode,
  imageFormatOptions,
  outName,
  codec,
  setVideoCodec: setCodec,
  proResProfile,
  setProResProfile,
  frame: frame2,
  setFrame,
  resolvedComposition,
  setOutName,
  setEndFrame,
  endFrame,
  setStartFrame,
  startFrame,
  validationMessage,
  setVerboseLogging,
  logLevel,
  showOutputName
}) => {
  const existence = useFileExistence(outName);
  const videoCodecOptions = useMemo111(() => {
    return BrowserSafeApis8.validCodecs.filter((c) => {
      return NoReactAPIs2.isAudioCodec(c) === (renderMode === "audio");
    }).map((codecOption) => {
      return {
        label: humanReadableCodec(codecOption),
        onClick: () => setCodec(codecOption),
        key: codecOption,
        leftItem: codec === codecOption ? /* @__PURE__ */ jsx196(Checkmark, {}) : null,
        id: codecOption,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: codecOption
      };
    });
  }, [renderMode, setCodec, codec]);
  const proResProfileOptions = useMemo111(() => {
    return BrowserSafeApis8.proResProfileOptions.map((option) => {
      return {
        label: labelProResProfile(option),
        onClick: () => setProResProfile(option),
        key: option,
        selected: proResProfile === option,
        type: "item",
        id: option,
        keyHint: null,
        leftItem: null,
        quickSwitcherLabel: null,
        subMenu: null,
        value: option
      };
    });
  }, [proResProfile, setProResProfile]);
  const onFrameSetDirectly = useCallback103((newFrame) => {
    setFrame(newFrame);
  }, [setFrame]);
  const onFrameChanged = useCallback103((e) => {
    setFrame((q) => {
      const newFrame = parseFloat(e);
      if (Number.isNaN(newFrame)) {
        return q;
      }
      return newFrame;
    });
  }, [setFrame]);
  const onValueChange = useCallback103((e) => {
    setOutName(e.target.value);
  }, [setOutName]);
  const logLevelOptions = useMemo111(() => {
    return ["trace", "verbose", "info", "warn", "error"].map((level) => {
      return {
        label: humanReadableLogLevel(level),
        onClick: () => setVerboseLogging(level),
        leftItem: logLevel === level ? /* @__PURE__ */ jsx196(Checkmark, {}) : null,
        id: level,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: level
      };
    });
  }, [logLevel, setVerboseLogging]);
  return /* @__PURE__ */ jsxs117("div", {
    style: container39,
    children: [
      renderMode === "still" || renderMode === "sequence" ? /* @__PURE__ */ jsxs117("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx196("div", {
            style: label,
            children: "Format"
          }),
          /* @__PURE__ */ jsx196("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx196(SegmentedControl, {
              items: imageFormatOptions,
              needsWrapping: true
            })
          })
        ]
      }) : /* @__PURE__ */ jsxs117("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs117("div", {
            style: label,
            children: [
              "Codec",
              /* @__PURE__ */ jsx196(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx196(OptionExplainerBubble, {
                id: "videoCodecOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx196("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx196(Combobox, {
              values: videoCodecOptions,
              selectedId: codec,
              title: "Codec"
            })
          })
        ]
      }),
      renderMode === "still" && resolvedComposition.durationInFrames > 1 ? /* @__PURE__ */ jsxs117("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx196("div", {
            style: label,
            children: "Frame"
          }),
          /* @__PURE__ */ jsx196("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx196(RightAlignInput, {
              children: /* @__PURE__ */ jsx196(InputDragger, {
                value: frame2,
                onTextChange: onFrameChanged,
                placeholder: `0-${resolvedComposition.durationInFrames - 1}`,
                onValueChange: onFrameSetDirectly,
                name: "frame",
                step: 1,
                min: 0,
                status: "ok",
                max: resolvedComposition.durationInFrames - 1,
                rightAlign: true
              })
            })
          })
        ]
      }) : null,
      renderMode === "video" && codec === "prores" ? /* @__PURE__ */ jsxs117("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx196("div", {
            style: label,
            children: "ProRes profile"
          }),
          /* @__PURE__ */ jsx196("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx196(Combobox, {
              title: "proResProfile",
              selectedId: proResProfile,
              values: proResProfileOptions
            })
          })
        ]
      }) : null,
      renderMode === "still" ? null : /* @__PURE__ */ jsx196(FrameRangeSetting, {
        durationInFrames: resolvedComposition.durationInFrames,
        endFrame,
        setEndFrame,
        setStartFrame,
        startFrame
      }),
      showOutputName ? /* @__PURE__ */ jsx196(RenderModalOutputName, {
        ariaLabel: null,
        existingOutputPath: null,
        existence,
        inputStyle: input,
        outName,
        onValueChange,
        validationMessage,
        label: renderMode === "sequence" ? "Folder name" : "Output name"
      }) : null,
      /* @__PURE__ */ jsxs117("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs117("div", {
            style: label,
            children: [
              "Log Level ",
              /* @__PURE__ */ jsx196(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx196(OptionExplainerBubble, {
                id: "logLevelOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx196("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx196(Combobox, {
              values: logLevelOptions,
              selectedId: logLevel,
              title: "Log Level"
            })
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/RenderModalEncoding.tsx
import { BrowserSafeApis as BrowserSafeApis9 } from "@remotion/renderer/client";
import { useCallback as useCallback104, useMemo as useMemo112 } from "react";

// src/helpers/presets-labels.ts
var labelx264Preset = (profile) => {
  if (profile === "ultrafast") {
    return "ultrafast";
  }
  if (profile === "superfast") {
    return "superfast";
  }
  if (profile === "veryfast") {
    return "veryfast";
  }
  if (profile === "faster") {
    return "faster";
  }
  if (profile === "fast") {
    return "fast";
  }
  if (profile === "medium") {
    return "medium";
  }
  if (profile === "slow") {
    return "slow";
  }
  if (profile === "slower") {
    return "slower";
  }
  if (profile === "veryslow") {
    return "veryslow";
  }
  if (profile === "placebo") {
    return "placebo";
  }
  throw new TypeError(`Unknown x264 preset: ${profile}`);
};

// src/components/RenderModal/RenderModalEncoding.tsx
import { jsx as jsx197, jsxs as jsxs118, Fragment as Fragment51 } from "react/jsx-runtime";
var qualityControlModes = ["crf", "bitrate"];
var container40 = {
  flex: 1,
  overflowY: "auto"
};
var RenderModalEncoding = ({
  renderMode,
  codec,
  qualityControlType,
  setQualityControl,
  shouldDisplayQualityControlPicker,
  crf,
  minCrf,
  maxCrf,
  setCrf,
  customTargetVideoBitrate,
  setCustomTargetVideoBitrateValue,
  encodingBufferSize,
  setEncodingBufferSize,
  encodingMaxRate,
  setEncodingMaxRate,
  pixelFormat,
  pixelFormatOptions,
  colorSpace,
  setColorSpace,
  x264Preset,
  setx264Preset,
  gopSize,
  setGopSize,
  hardwareAcceleration,
  setHardwareAcceleration,
  disallowParallelEncoding,
  setDisallowParallelEncoding
}) => {
  const colorSpaceOptions = useMemo112(() => {
    return BrowserSafeApis9.validColorSpaces.map((option) => {
      return {
        label: option,
        onClick: () => setColorSpace(option),
        key: option,
        id: option,
        keyHint: null,
        leftItem: colorSpace === option ? /* @__PURE__ */ jsx197(Checkmark, {}) : null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: option
      };
    });
  }, [colorSpace, setColorSpace]);
  const qualityControlOptions = useMemo112(() => {
    return qualityControlModes.map((option) => {
      return {
        label: option === "crf" ? "CRF" : "Bitrate",
        onClick: () => setQualityControl(option),
        key: option,
        selected: qualityControlType === option
      };
    });
  }, [qualityControlType, setQualityControl]);
  const x264PresetOptions = useMemo112(() => {
    return BrowserSafeApis9.x264PresetOptions.map((option) => {
      return {
        label: labelx264Preset(option),
        onClick: () => setx264Preset(option),
        key: option,
        type: "item",
        id: option,
        keyHint: null,
        leftItem: x264Preset === option ? /* @__PURE__ */ jsx197(Checkmark, {}) : null,
        quickSwitcherLabel: null,
        subMenu: null,
        value: option
      };
    });
  }, [setx264Preset, x264Preset]);
  const hardwareAccelerationValues = useMemo112(() => {
    return BrowserSafeApis9.hardwareAccelerationOptions.map((option) => {
      return {
        label: option,
        onClick: () => setHardwareAcceleration(option),
        leftItem: hardwareAcceleration === option ? /* @__PURE__ */ jsx197(Checkmark, {}) : null,
        subMenu: null,
        quickSwitcherLabel: null,
        type: "item",
        id: option,
        keyHint: null,
        value: option
      };
    });
  }, [hardwareAcceleration, setHardwareAcceleration]);
  const toggleCustomGopSize = useCallback104(() => {
    setGopSize((previous) => {
      if (previous === null) {
        return 120;
      }
      return null;
    });
  }, [setGopSize]);
  const changeGopSize = useCallback104((cb) => {
    setGopSize((prev) => {
      if (prev === null) {
        throw new TypeError("Expected previous value");
      }
      if (typeof cb === "function") {
        return cb(prev);
      }
      return cb;
    });
  }, [setGopSize]);
  const onTargetVideoBitrateChanged = useCallback104((e) => {
    setCustomTargetVideoBitrateValue(e.target.value);
  }, [setCustomTargetVideoBitrateValue]);
  const onEncodingBufferSizeToggled = useCallback104((e) => {
    setEncodingBufferSize(e.target.checked ? "10000k" : null);
  }, [setEncodingBufferSize]);
  const onEncodingMaxRateToggled = useCallback104((e) => {
    setEncodingMaxRate(e.target.checked ? "5000k" : null);
  }, [setEncodingMaxRate]);
  const onEncodingBufferSizeChanged = useCallback104((e) => {
    setEncodingBufferSize(e.target.value);
  }, [setEncodingBufferSize]);
  const onEncodingMaxRateChanged = useCallback104((e) => {
    setEncodingMaxRate(e.target.value);
  }, [setEncodingMaxRate]);
  const onDisallowParallelEncodingChanged = useCallback104((e) => {
    setDisallowParallelEncoding(e.target.checked);
  }, [setDisallowParallelEncoding]);
  return /* @__PURE__ */ jsxs118("div", {
    style: container40,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: [
      shouldDisplayQualityControlPicker && renderMode === "video" ? /* @__PURE__ */ jsxs118("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx197("div", {
            style: label,
            children: "Quality control"
          }),
          /* @__PURE__ */ jsx197("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx197(SegmentedControl, {
              items: qualityControlOptions,
              needsWrapping: true
            })
          })
        ]
      }) : null,
      qualityControlType === "crf" && renderMode === "video" && crf !== null ? /* @__PURE__ */ jsx197(CrfSetting, {
        crf,
        min: minCrf,
        max: maxCrf,
        setCrf,
        option: "crfOption"
      }) : null,
      qualityControlType === "bitrate" && renderMode === "video" ? /* @__PURE__ */ jsxs118("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs118("div", {
            style: label,
            children: [
              "Target video bitrate",
              /* @__PURE__ */ jsx197(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                id: "videoBitrateOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx197("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx197("div", {
              children: /* @__PURE__ */ jsx197(RemotionInput, {
                style: input,
                value: customTargetVideoBitrate,
                onChange: onTargetVideoBitrateChanged,
                status: "ok",
                rightAlign: true
              })
            })
          })
        ]
      }) : null,
      renderMode === "video" ? /* @__PURE__ */ jsxs118(Fragment51, {
        children: [
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "Custom FFmpeg -bufsize",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "encodingBufferSizeOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Checkbox, {
                  checked: encodingBufferSize !== null,
                  onChange: onEncodingBufferSizeToggled,
                  name: "encoding-buffer-size"
                })
              })
            ]
          }),
          encodingBufferSize === null ? null : /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx197("div", {
                style: label,
                children: "-bufsize value"
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197("div", {
                  children: /* @__PURE__ */ jsx197(RemotionInput, {
                    style: input,
                    value: encodingBufferSize,
                    onChange: onEncodingBufferSizeChanged,
                    status: "ok",
                    rightAlign: true
                  })
                })
              })
            ]
          }),
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "Custom FFmpeg -maxrate",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "encodingMaxRateOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Checkbox, {
                  checked: encodingMaxRate !== null,
                  onChange: onEncodingMaxRateToggled,
                  name: "encoding-max-rate"
                })
              })
            ]
          }),
          encodingMaxRate === null ? null : /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx197("div", {
                style: label,
                children: "-maxrate value"
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197("div", {
                  children: /* @__PURE__ */ jsx197(RemotionInput, {
                    style: input,
                    value: encodingMaxRate,
                    onChange: onEncodingMaxRateChanged,
                    status: "ok",
                    rightAlign: true
                  })
                })
              })
            ]
          }),
          /* @__PURE__ */ jsx197(RenderModalHr, {}),
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx197("div", {
                style: label,
                children: "Pixel format"
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Combobox, {
                  values: pixelFormatOptions,
                  selectedId: pixelFormat,
                  title: "Pixel Format"
                })
              })
            ]
          }),
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "Color space",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "colorSpaceOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Combobox, {
                  values: colorSpaceOptions,
                  selectedId: colorSpace,
                  title: "Color Space"
                })
              })
            ]
          }),
          /* @__PURE__ */ jsx197(RenderModalHr, {}),
          codec === "h264" ? /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "x264 Preset",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "x264Option"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Combobox, {
                  title: x264Preset,
                  selectedId: x264Preset,
                  values: x264PresetOptions
                })
              })
            ]
          }) : null,
          codec !== "gif" ? /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "Custom GOP size",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "gopSizeOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Checkbox, {
                  checked: gopSize !== null,
                  onChange: toggleCustomGopSize,
                  name: "custom-gop-size"
                })
              })
            ]
          }) : null,
          codec !== "gif" && gopSize !== null ? /* @__PURE__ */ jsx197(NumberSetting, {
            min: 1,
            max: 1e4,
            step: 1,
            name: "GOP size",
            formatter: (value) => `${value} frames`,
            onValueChanged: changeGopSize,
            value: gopSize
          }) : null,
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs118("div", {
                style: label,
                children: [
                  "Hardware acceleration",
                  /* @__PURE__ */ jsx197(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx197(OptionExplainerBubble, {
                    id: "hardwareAccelerationOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Combobox, {
                  title: hardwareAcceleration,
                  selectedId: hardwareAcceleration,
                  values: hardwareAccelerationValues
                })
              })
            ]
          })
        ]
      }) : null,
      renderMode === "still" ? null : /* @__PURE__ */ jsxs118(Fragment51, {
        children: [
          renderMode === "video" ? /* @__PURE__ */ jsx197(RenderModalHr, {}) : null,
          /* @__PURE__ */ jsxs118("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx197("div", {
                style: label,
                children: "No parallel encoding"
              }),
              /* @__PURE__ */ jsx197("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx197(Checkbox, {
                  checked: disallowParallelEncoding,
                  onChange: onDisallowParallelEncodingChanged,
                  name: "disallow-parallel-encoding"
                })
              })
            ]
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/RenderModalEnvironmentVariables.tsx
import { useCallback as useCallback107 } from "react";

// src/components/RenderModal/EnvInput.tsx
import React140, { useCallback as useCallback106 } from "react";

// src/components/RenderModal/InlineEyeIcon.tsx
import { useCallback as useCallback105 } from "react";
import { jsx as jsx198 } from "react/jsx-runtime";
var clearIcon = {
  height: 14,
  color: CURRENT_COLOR
};
var InlineEyeButton = ({ onClick, enabled }) => {
  const renderAction = useCallback105((color) => {
    return enabled ? /* @__PURE__ */ jsx198("svg", {
      style: clearIcon,
      viewBox: "0 0 640 512",
      children: /* @__PURE__ */ jsx198("path", {
        fill: color,
        d: "M25.9 3.4C19-2 8.9-.8 3.4 6.1S-.8 23.1 6.1 28.6l608 480c6.9 5.5 17 4.3 22.5-2.6s4.3-17-2.6-22.5L25.9 3.4zM605.5 268.3c3.3-7.9 3.3-16.7 0-24.6c-14.9-35.7-46.2-87.7-93-131.1C465.5 68.8 400.8 32 320 32c-51.2 0-96 14.8-133.9 36.8l27.3 21.5C244.6 74.2 280.2 64 320 64c70.4 0 127.7 32 170.8 72c43.1 40 71.9 88 85.2 120c-9.2 22.1-25.9 52-49.5 81.5l25.1 19.8c25.6-32 43.7-64.4 53.9-89zM88.4 154.7c-25.6 32-43.7 64.4-53.9 89c-3.3 7.9-3.3 16.7 0 24.6c14.9 35.7 46.2 87.7 93 131.1C174.5 443.2 239.2 480 320 480c51.2 0 96-14.8 133.9-36.8l-27.3-21.5C395.4 437.8 359.8 448 320 448c-70.4 0-127.7-32-170.8-72C106.1 336 77.3 288 64 256c9.2-22.1 25.9-52 49.5-81.5L88.4 154.7zM320 384c16.7 0 32.7-3.2 47.4-9.1l-30.9-24.4c-5.4 .9-10.9 1.4-16.5 1.4c-51 0-92.8-39.8-95.8-90.1l-30.9-24.4c-.9 6-1.3 12.2-1.3 18.5c0 70.7 57.3 128 128 128zM448 256c0-70.7-57.3-128-128-128c-16.7 0-32.7 3.2-47.4 9.1l30.9 24.4c5.4-.9 10.9-1.4 16.5-1.4c51 0 92.8 39.8 95.8 90.1l30.9 24.4c.9-6 1.3-12.2 1.3-18.5z"
      })
    }) : /* @__PURE__ */ jsx198("svg", {
      style: clearIcon,
      viewBox: "0 0 576 512",
      children: /* @__PURE__ */ jsx198("path", {
        fill: color,
        d: "M117.2 136C160.3 96 217.6 64 288 64s127.7 32 170.8 72c43.1 40 71.9 88 85.2 120c-13.3 32-42.1 80-85.2 120c-43.1 40-100.4 72-170.8 72s-127.7-32-170.8-72C74.1 336 45.3 288 32 256c13.3-32 42.1-80 85.2-120zM288 32c-80.8 0-145.5 36.8-192.6 80.6C48.6 156 17.3 208 2.5 243.7c-3.3 7.9-3.3 16.7 0 24.6C17.3 304 48.6 356 95.4 399.4C142.5 443.2 207.2 480 288 480s145.5-36.8 192.6-80.6c46.8-43.5 78.1-95.4 93-131.1c3.3-7.9 3.3-16.7 0-24.6c-14.9-35.7-46.2-87.7-93-131.1C433.5 68.8 368.8 32 288 32zM192 256a96 96 0 1 1 192 0 96 96 0 1 1 -192 0zm224 0a128 128 0 1 0 -256 0 128 128 0 1 0 256 0z"
      })
    });
  }, [enabled]);
  return /* @__PURE__ */ jsx198(InlineAction, {
    renderAction,
    onClick,
    variant: null
  });
};

// src/components/RenderModal/EnvInput.tsx
import { jsx as jsx199, jsxs as jsxs119, Fragment as Fragment52 } from "react/jsx-runtime";
var input3 = {
  flex: 1,
  width: "100%"
};
var validationStyle = {
  paddingLeft: optionRow.paddingLeft,
  paddingRight: optionRow.paddingRight
};
var EnvInput = ({
  onEnvKeyChange,
  onEnvValChange,
  envKey,
  envVal,
  onDelete,
  index,
  autoFocus,
  isDuplicate
}) => {
  const [showInPlainText, setShowInPlainText] = React140.useState(false);
  const [initialWarningKeyMissing, setKeyWarningEligible] = React140.useState(() => {
    return envKey.trim() === "" && envVal.trim() !== "";
  });
  const [initialWarningValMissing, setValueWarningEligible] = React140.useState(() => {
    return envKey.trim() !== "" && envVal.trim() === "";
  });
  const isKeyMissing = envKey.trim() === "" && initialWarningKeyMissing && envVal.trim() !== "";
  const isValMissing = envVal.trim() === "" && initialWarningValMissing && envKey.trim() !== "";
  const handleDelete = useCallback106(() => {
    onDelete(index);
  }, [index, onDelete]);
  const togglePlainText = useCallback106(() => {
    setShowInPlainText((prev) => !prev);
  }, []);
  const handleKeyChange = useCallback106((e) => {
    onEnvKeyChange(index, e.target.value);
  }, [index, onEnvKeyChange]);
  const handleValueChange = useCallback106((e) => {
    onEnvValChange(index, e.target.value);
  }, [index, onEnvValChange]);
  const makeKeyWarningEligible = useCallback106(() => {
    setKeyWarningEligible(true);
  }, []);
  const makeValueWarningEligible = useCallback106(() => {
    setValueWarningEligible(true);
  }, []);
  const isNodeEnvKey = envKey.trim() === "NODE_ENV";
  return /* @__PURE__ */ jsxs119(Fragment52, {
    children: [
      /* @__PURE__ */ jsxs119(Row, {
        align: "center",
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx199(RemotionInput, {
            status: isNodeEnvKey || isDuplicate || isKeyMissing ? "warning" : "ok",
            type: "text",
            placeholder: "Key",
            style: input3,
            value: envKey,
            onBlur: makeKeyWarningEligible,
            autoFocus,
            onChange: handleKeyChange,
            rightAlign: false
          }),
          /* @__PURE__ */ jsx199(Spacing, {
            x: 1
          }),
          /* @__PURE__ */ jsx199(RemotionInput, {
            status: isValMissing ? "warning" : "ok",
            placeholder: "Value",
            type: showInPlainText ? "text" : "password",
            style: input3,
            value: envVal,
            onBlur: makeValueWarningEligible,
            onChange: handleValueChange,
            rightAlign: false
          }),
          /* @__PURE__ */ jsx199(Spacing, {
            x: 1.5
          }),
          /* @__PURE__ */ jsx199(InlineEyeButton, {
            enabled: !showInPlainText,
            onClick: togglePlainText
          }),
          /* @__PURE__ */ jsx199(InlineRemoveButton, {
            onClick: handleDelete
          })
        ]
      }),
      isNodeEnvKey ? /* @__PURE__ */ jsx199("div", {
        style: validationStyle,
        children: /* @__PURE__ */ jsx199(ValidationMessage, {
          align: "flex-start",
          type: "warning",
          message: "NODE_ENV will be overwritten by Remotion during the render process."
        })
      }) : null,
      isDuplicate ? /* @__PURE__ */ jsx199("div", {
        style: validationStyle,
        children: /* @__PURE__ */ jsx199(ValidationMessage, {
          align: "flex-start",
          type: "warning",
          message: `${envKey.toUpperCase()} is already defined.`
        })
      }) : null,
      isKeyMissing ? /* @__PURE__ */ jsx199("div", {
        style: validationStyle,
        children: /* @__PURE__ */ jsx199(ValidationMessage, {
          align: "flex-start",
          type: "warning",
          message: "Key is missing."
        })
      }) : null,
      isValMissing ? /* @__PURE__ */ jsx199("div", {
        style: validationStyle,
        children: /* @__PURE__ */ jsx199(ValidationMessage, {
          align: "flex-start",
          type: "warning",
          message: "Value is missing."
        })
      }) : null
    ]
  });
};

// src/components/RenderModal/RenderModalEnvironmentVariables.tsx
import { jsx as jsx200, jsxs as jsxs120 } from "react/jsx-runtime";
var container41 = {
  marginTop: 20
};
var button2 = {
  marginLeft: 16
};
var RenderModalEnvironmentVariables = ({ envVariables, setEnvVariables }) => {
  const onEnvValChange = useCallback107((index, value) => {
    setEnvVariables((oldEnv) => {
      const newEnv = [...oldEnv];
      newEnv[index][1] = value;
      return newEnv;
    });
  }, [setEnvVariables]);
  const onEnvKeyChange = useCallback107((index, value) => {
    setEnvVariables((oldEnv) => {
      const newEnv = [...oldEnv];
      newEnv[index][0] = value;
      return newEnv;
    });
  }, [setEnvVariables]);
  const onDelete = useCallback107((index) => {
    setEnvVariables((oldEnv) => oldEnv.filter((_, idx) => idx !== index));
  }, [setEnvVariables]);
  const addField = useCallback107(() => {
    setEnvVariables((oldEnv) => [...oldEnv, ["", ""]]);
  }, [setEnvVariables]);
  const usedKeys = [];
  return /* @__PURE__ */ jsxs120("div", {
    style: container41,
    children: [
      envVariables.map((env, i) => {
        let isDuplicate = false;
        if (usedKeys.includes(env[0].toUpperCase())) {
          isDuplicate = true;
        }
        usedKeys.push(env[0].toUpperCase());
        return /* @__PURE__ */ jsx200(EnvInput, {
          onEnvKeyChange,
          onEnvValChange,
          envKey: env[0],
          envVal: env[1],
          onDelete,
          index: i,
          isDuplicate,
          autoFocus: i === envVariables.length - 1 && env[0] === ""
        }, i);
      }),
      /* @__PURE__ */ jsx200(Spacing, {
        y: 1,
        block: true
      }),
      /* @__PURE__ */ jsx200(Button, {
        style: button2,
        onClick: addField,
        children: "+ Add env variable"
      }),
      /* @__PURE__ */ jsx200(Spacing, {
        y: 1,
        block: true
      })
    ]
  });
};

// src/components/RenderModal/RenderModalGif.tsx
import { useCallback as useCallback109 } from "react";

// src/components/RenderModal/NumberOfLoopsSetting.tsx
import { useCallback as useCallback108 } from "react";
import { jsx as jsx201, jsxs as jsxs121 } from "react/jsx-runtime";
var min = 0;
var NumberOfLoopsSetting = ({ numberOfGifLoops, setNumberOfGifLoops }) => {
  const onNumberOfGifLoopsChangedDirectly = useCallback108((newConcurrency) => {
    setNumberOfGifLoops(newConcurrency);
  }, [setNumberOfGifLoops]);
  const onNumberOfGifLoopsChanged = useCallback108((e) => {
    setNumberOfGifLoops((q) => {
      const newConcurrency = parseInt(e, 10);
      if (Number.isNaN(newConcurrency)) {
        return q;
      }
      const newConcurrencyClamped = Math.max(newConcurrency, min);
      return newConcurrencyClamped;
    });
  }, [setNumberOfGifLoops]);
  return /* @__PURE__ */ jsxs121("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsx201("div", {
        style: label,
        children: "Number of loops"
      }),
      /* @__PURE__ */ jsx201("div", {
        style: rightRow,
        children: /* @__PURE__ */ jsx201(RightAlignInput, {
          children: /* @__PURE__ */ jsx201(InputDragger, {
            value: numberOfGifLoops,
            onTextChange: onNumberOfGifLoopsChanged,
            placeholder: `${min}-`,
            onValueChange: onNumberOfGifLoopsChangedDirectly,
            name: "number-of-gif-loops",
            step: 1,
            min,
            status: "ok",
            rightAlign: true
          })
        })
      })
    ]
  });
};

// src/components/RenderModal/RenderModalGif.tsx
import { jsx as jsx202, jsxs as jsxs122 } from "react/jsx-runtime";
var container42 = {
  flex: 1
};
var RenderModalGif = ({
  everyNthFrame,
  limitNumberOfGifLoops,
  numberOfGifLoopsSetting,
  setEveryNthFrameSetting,
  setLimitNumberOfGifLoops,
  setNumberOfGifLoopsSetting
}) => {
  const onShouldLimitNumberOfGifLoops = useCallback109((e) => {
    setLimitNumberOfGifLoops(e.target.checked);
  }, [setLimitNumberOfGifLoops]);
  return /* @__PURE__ */ jsxs122("div", {
    style: container42,
    children: [
      /* @__PURE__ */ jsx202(NumberSetting, {
        name: "Every nth frame",
        min: 1,
        onValueChanged: setEveryNthFrameSetting,
        value: everyNthFrame,
        step: 1
      }),
      /* @__PURE__ */ jsxs122("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs122("div", {
            style: label,
            children: [
              "Limit GIF loops ",
              /* @__PURE__ */ jsx202(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx202(OptionExplainerBubble, {
                id: "numberOfGifLoopsOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx202("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx202(Checkbox, {
              checked: limitNumberOfGifLoops,
              onChange: onShouldLimitNumberOfGifLoops,
              name: "limitNumberOfGifLoops"
            })
          })
        ]
      }),
      limitNumberOfGifLoops ? /* @__PURE__ */ jsx202(NumberOfLoopsSetting, {
        numberOfGifLoops: numberOfGifLoopsSetting,
        setNumberOfGifLoops: setNumberOfGifLoopsSetting
      }) : null
    ]
  });
};

// src/components/RenderModal/JpegQualitySetting.tsx
import { jsx as jsx203 } from "react/jsx-runtime";
var MIN_JPEG_QUALITY = 1;
var MAX_JPEG_QUALITY = 100;
var JpegQualitySetting = ({ jpegQuality, setJpegQuality }) => {
  return /* @__PURE__ */ jsx203(NumberSetting, {
    min: MIN_JPEG_QUALITY,
    max: MAX_JPEG_QUALITY,
    step: 1,
    name: "JPEG Quality",
    onValueChanged: setJpegQuality,
    value: jpegQuality,
    hint: "jpegQualityOption"
  });
};

// src/components/RenderModal/ScaleSetting.tsx
import { useMemo as useMemo113 } from "react";
import { jsx as jsx204, jsxs as jsxs123, Fragment as Fragment53 } from "react/jsx-runtime";
var MIN_SCALE = 0.1;
var MAX_SCALE = 10;
var outputDimensionsStyle = {
  fontSize: 13,
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  paddingRight: 16,
  textAlign: "right",
  marginBottom: 14,
  marginTop: -10
};
var ScaleSetting = ({ scale, setScale, compositionWidth, compositionHeight }) => {
  const outputDimensions = useMemo113(() => {
    const outputWidth = Math.round(compositionWidth * scale);
    const outputHeight = Math.round(compositionHeight * scale);
    return `${outputWidth}×${outputHeight}`;
  }, [compositionWidth, compositionHeight, scale]);
  return /* @__PURE__ */ jsxs123(Fragment53, {
    children: [
      /* @__PURE__ */ jsx204(NumberSetting, {
        min: MIN_SCALE,
        max: MAX_SCALE,
        step: 0.1,
        name: "Scale",
        formatter: (w) => {
          if (typeof w === "number") {
            return `${w.toFixed(1)}x`;
          }
          return `${w}x`;
        },
        onValueChanged: setScale,
        value: scale,
        hint: "scaleOption"
      }),
      scale !== 1 && /* @__PURE__ */ jsxs123("div", {
        style: outputDimensionsStyle,
        children: [
          "Output resolution: ",
          outputDimensions
        ]
      })
    ]
  });
};

// src/components/RenderModal/RenderModalPicture.tsx
import { jsx as jsx205, jsxs as jsxs124 } from "react/jsx-runtime";
var container43 = {
  flex: 1,
  overflowY: "auto"
};
var RenderModalPicture = ({
  renderMode,
  scale,
  setScale,
  imageFormatOptions,
  videoImageFormat,
  setJpegQuality,
  jpegQuality,
  stillImageFormat,
  compositionWidth,
  compositionHeight
}) => {
  return /* @__PURE__ */ jsxs124("div", {
    style: container43,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: [
      renderMode === "video" ? /* @__PURE__ */ jsxs124("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx205("div", {
            style: label,
            children: "Image Format"
          }),
          /* @__PURE__ */ jsx205("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx205(SegmentedControl, {
              items: imageFormatOptions,
              needsWrapping: false
            })
          })
        ]
      }) : null,
      renderMode === "video" && videoImageFormat === "jpeg" ? /* @__PURE__ */ jsx205(JpegQualitySetting, {
        jpegQuality,
        setJpegQuality
      }) : null,
      renderMode === "still" && stillImageFormat === "jpeg" ? /* @__PURE__ */ jsx205(JpegQualitySetting, {
        jpegQuality,
        setJpegQuality
      }) : null,
      renderMode === "video" ? /* @__PURE__ */ jsx205(RenderModalHr, {}) : null,
      /* @__PURE__ */ jsx205(ScaleSetting, {
        scale,
        setScale,
        compositionWidth,
        compositionHeight
      })
    ]
  });
};

// src/components/RenderModal/ServerRenderModal.tsx
import { jsx as jsx206, jsxs as jsxs125 } from "react/jsx-runtime";
var initialState = { type: "idle" };
var reducer = (state, action) => {
  if (action.type === "start") {
    return {
      type: "load"
    };
  }
  if (action.type === "fail") {
    return {
      type: "error"
    };
  }
  if (action.type === "succeed") {
    return {
      type: "success"
    };
  }
  return state;
};
var RenderModal = ({
  readOnlyStudio,
  initialFrame,
  initialVideoImageFormat,
  initialStillImageFormat,
  initialJpegQuality,
  initialScale,
  initialLogLevel,
  initialConcurrency,
  maxConcurrency,
  minConcurrency,
  initialMuted,
  initialEnforceAudioTrack,
  initialProResProfile,
  initialx264Preset,
  initialGopSize,
  initialPixelFormat,
  initialVideoBitrate,
  initialAudioBitrate,
  initialEveryNthFrame,
  initialNumberOfGifLoops,
  initialDelayRenderTimeout,
  initialOffthreadVideoCacheSizeInBytes,
  initialEnvVariables,
  initialDisableWebSecurity,
  initialGl,
  initialHeadless,
  initialIgnoreCertificateErrors,
  initialEncodingBufferSize,
  initialEncodingMaxRate,
  initialOffthreadVideoThreads,
  initialMediaCacheSizeInBytes,
  initialDarkMode,
  initialUserAgent,
  defaultProps,
  inFrameMark,
  outFrameMark,
  initialColorSpace,
  initialMultiProcessOnLinux,
  defaultConfigurationAudioCodec,
  defaultConfigurationVideoCodec,
  initialBeep,
  initialRepro,
  initialForSeamlessAacConcatenation,
  renderTypeOfLastRender,
  initialHardwareAcceleration,
  initialSampleRate,
  defaultMetadata,
  initialChromeMode,
  renderDefaults
}) => {
  const { setSelectedModal } = useContext116(SetSelectedModalContext);
  const context = useContext116(ResolvedCompositionContext);
  if (!context) {
    throw new Error("Should not be able to render without resolving comp first");
  }
  const {
    resolved: { result: resolvedComposition },
    unresolved: unresolvedComposition
  } = context;
  const isMounted = useRef62(true);
  const [isVideo] = useState75(() => {
    return typeof resolvedComposition.durationInFrames === "undefined" ? true : resolvedComposition.durationInFrames > 1;
  });
  const [
    {
      initialAudioCodec,
      initialRenderType,
      initialVideoCodec,
      initialVideoCodecForAudioTab,
      initialVideoCodecForVideoTab
    }
  ] = useState75(() => {
    return getDefaultCodecs({
      defaultConfigurationVideoCodec,
      compositionDefaultVideoCodec: resolvedComposition.defaultCodec,
      defaultConfigurationAudioCodec,
      renderType: renderTypeOfLastRender ?? (isVideo ? "video" : "still")
    });
  });
  const [state, dispatch] = useReducer2(reducer, initialState);
  const [unclampedFrame, setFrame] = useState75(() => initialFrame);
  const [stillImageFormat, setStillImageFormat] = useState75(() => initialStillImageFormat);
  const [videoImageFormat, setVideoImageFormat] = useState75(() => initialVideoImageFormat ?? resolvedComposition.defaultVideoImageFormat ?? renderDefaults.videoImageFormat);
  const [sequenceImageFormat, setSequenceImageFormat] = useState75(() => initialStillImageFormat === "jpeg" ? "jpeg" : "png");
  const [concurrency, setConcurrency] = useState75(() => initialConcurrency);
  const [videoCodecForVideoTab, setVideoCodecForVideoTab] = useState75(() => initialVideoCodecForVideoTab);
  const [userSelectedAudioCodec, setUserSelectedAudioCodec] = useState75(() => initialAudioCodec);
  const [separateAudioTo, setSeparateAudioTo] = useState75(null);
  const [envVariables, setEnvVariables] = useState75(() => envVariablesObjectToArray(initialEnvVariables).filter(([key7]) => key7 !== "NODE_ENV"));
  const [initialOutName] = useState75(() => {
    return getDefaultOutLocation({
      compositionName: resolvedComposition.id,
      defaultExtension: initialRenderType === "still" ? initialStillImageFormat : isVideo ? BrowserSafeApis10.getFileExtensionFromCodec(initialVideoCodec, initialAudioCodec) : initialStillImageFormat,
      type: "asset",
      compositionDefaultOutName: resolvedComposition.defaultOutName,
      outputLocation: renderDefaults.outputLocation
    });
  });
  const [videoCodecForAudioTab, setVideoCodecForAudioTab] = useState75(() => initialVideoCodecForAudioTab);
  const [mutedState, setMuted] = useState75(() => initialMuted);
  const [repro, setRepro] = useState75(() => initialRepro);
  const [enforceAudioTrackState, setEnforceAudioTrackState] = useState75(() => initialEnforceAudioTrack);
  const [forSeamlessAacConcatenation, setForSeamlessAacConcatenation] = useState75(() => initialForSeamlessAacConcatenation);
  const [sampleRate, setSampleRate] = useState75(() => initialSampleRate);
  const [renderMode, setRenderModeState] = useState75(initialRenderType);
  const [jpegQuality, setJpegQuality] = useState75(() => initialJpegQuality);
  const [scale, setScale] = useState75(() => initialScale);
  const [logLevel, setLogLevel] = useState75(() => initialLogLevel);
  const [disallowParallelEncoding, setDisallowParallelEncoding] = useState75(false);
  const [disableWebSecurity, setDisableWebSecurity] = useState75(() => initialDisableWebSecurity);
  const [headless, setHeadless] = useState75(() => initialHeadless);
  const [beepOnFinish, setBeepOnFinish] = useState75(() => initialBeep);
  const [ignoreCertificateErrors, setIgnoreCertificateErrors] = useState75(() => initialIgnoreCertificateErrors);
  const [multiProcessOnLinux, setChromiumMultiProcessOnLinux] = useState75(() => initialMultiProcessOnLinux);
  const [darkMode, setDarkMode] = useState75(() => initialDarkMode);
  const [openGlOption, setOpenGlOption] = useState75(() => initialGl ?? "default");
  const [colorSpace, setColorSpace] = useState75(() => initialColorSpace);
  const [userAgent, setUserAgent] = useState75(() => initialUserAgent === null ? null : initialUserAgent.trim() === "" ? null : initialUserAgent);
  const chromiumOptions = useMemo114(() => {
    return {
      headless,
      disableWebSecurity,
      ignoreCertificateErrors,
      gl: openGlOption === "default" ? null : openGlOption,
      userAgent: userAgent === null ? null : userAgent.trim() === "" ? null : userAgent,
      enableMultiProcessOnLinux: multiProcessOnLinux,
      darkMode
    };
  }, [
    headless,
    disableWebSecurity,
    ignoreCertificateErrors,
    openGlOption,
    userAgent,
    multiProcessOnLinux,
    darkMode
  ]);
  const [outName, setOutName] = useState75(() => initialOutName);
  const [endFrameOrNull, setEndFrame] = useState75(() => outFrameMark ?? null);
  const [startFrameOrNull, setStartFrame] = useState75(() => inFrameMark ?? null);
  const [proResProfileSetting, setProResProfile] = useState75(() => initialProResProfile ?? resolvedComposition.defaultProResProfile ?? "hq");
  const [x264PresetSetting, setx264Preset] = useState75(() => initialx264Preset);
  const [gopSize, setGopSize] = useState75(() => initialGopSize);
  const [hardwareAcceleration, setHardwareAcceleration] = useState75(() => initialHardwareAcceleration);
  const [userPreferredPixelFormat, setPixelFormat] = useState75(() => initialPixelFormat ?? resolvedComposition.defaultPixelFormat ?? renderDefaults.pixelFormat);
  const [preferredQualityControlType, setQualityControl] = useState75(() => initialVideoBitrate === null ? "crf" : "bitrate");
  const [
    shouldHaveCustomTargetAudioBitrate,
    setShouldHaveCustomTargetAudioBitrate
  ] = useState75(() => initialAudioBitrate !== null);
  const [customTargetAudioBitrate, setCustomTargetAudioBitrateValue] = useState75(() => initialAudioBitrate ?? "320K");
  const [customTargetVideoBitrate, setCustomTargetVideoBitrateValue] = useState75(() => initialVideoBitrate ?? "1M");
  const [encodingMaxRate, setEncodingMaxRate] = useState75(() => initialEncodingMaxRate ?? null);
  const [encodingBufferSize, setEncodingBufferSize] = useState75(() => initialEncodingBufferSize ?? null);
  const [limitNumberOfGifLoops, setLimitNumberOfGifLoops] = useState75(() => initialNumberOfGifLoops !== null);
  const [numberOfGifLoopsSetting, setNumberOfGifLoopsSetting] = useState75(() => initialNumberOfGifLoops ?? 0);
  const [delayRenderTimeout, setDelayRenderTimeout] = useState75(() => initialDelayRenderTimeout);
  const [chromeMode, setChromeMode] = useState75(() => initialChromeMode);
  const [offthreadVideoCacheSizeInBytes, setOffthreadVideoCacheSizeInBytes] = useState75(initialOffthreadVideoCacheSizeInBytes);
  const [mediaCacheSizeInBytes, setMediaCacheSizeInBytes] = useState75(initialMediaCacheSizeInBytes);
  const [offthreadVideoThreads, setOffthreadVideoThreads] = useState75(() => initialOffthreadVideoThreads);
  const codec = useMemo114(() => {
    if (renderMode === "audio") {
      return videoCodecForAudioTab;
    }
    return videoCodecForVideoTab;
  }, [videoCodecForAudioTab, renderMode, videoCodecForVideoTab]);
  const numberOfGifLoops = useMemo114(() => {
    if (codec === "gif" && limitNumberOfGifLoops) {
      return numberOfGifLoopsSetting;
    }
    return null;
  }, [codec, limitNumberOfGifLoops, numberOfGifLoopsSetting]);
  const audioBitrate = useMemo114(() => {
    if (shouldHaveCustomTargetAudioBitrate) {
      return customTargetAudioBitrate;
    }
    return null;
  }, [customTargetAudioBitrate, shouldHaveCustomTargetAudioBitrate]);
  const supportsCrf = BrowserSafeApis10.codecSupportsCrf(codec);
  const supportsVideoBitrate = BrowserSafeApis10.codecSupportsVideoBitrate(codec);
  const supportsBothQualityControls = useMemo114(() => {
    return supportsCrf && supportsVideoBitrate && hardwareAcceleration !== "if-possible" && hardwareAcceleration !== "required";
  }, [hardwareAcceleration, supportsCrf, supportsVideoBitrate]);
  const qualityControlType = useMemo114(() => {
    if (hardwareAcceleration === "if-possible" || hardwareAcceleration === "required") {
      if (supportsVideoBitrate) {
        return "bitrate";
      }
      return null;
    }
    if (supportsBothQualityControls) {
      return preferredQualityControlType;
    }
    if (supportsCrf) {
      return "crf";
    }
    if (supportsVideoBitrate) {
      return "bitrate";
    }
    return null;
  }, [
    hardwareAcceleration,
    preferredQualityControlType,
    supportsBothQualityControls,
    supportsCrf,
    supportsVideoBitrate
  ]);
  const videoBitrate = useMemo114(() => {
    if (qualityControlType === "bitrate") {
      return customTargetVideoBitrate;
    }
    return null;
  }, [customTargetVideoBitrate, qualityControlType]);
  const { crf, maxCrf, minCrf, setCrf } = useCrfState(codec);
  const dispatchIfMounted = useCallback110((payload) => {
    if (isMounted.current === false)
      return;
    dispatch(payload);
  }, []);
  const muted = useMemo114(() => {
    if (renderMode === "video") {
      return mutedState;
    }
    return false;
  }, [mutedState, renderMode]);
  const enforceAudioTrack = useMemo114(() => {
    if (renderMode === "video") {
      return enforceAudioTrackState;
    }
    if (renderMode === "audio") {
      return enforceAudioTrackState;
    }
    return false;
  }, [enforceAudioTrackState, renderMode]);
  const proResProfile = useMemo114(() => {
    if (renderMode === "video" && codec === "prores") {
      return proResProfileSetting;
    }
    return null;
  }, [codec, proResProfileSetting, renderMode]);
  const x264Preset = useMemo114(() => {
    if (renderMode === "video" && codec === "h264") {
      return x264PresetSetting;
    }
    return null;
  }, [codec, x264PresetSetting, renderMode]);
  const [inputProps, _setInputProps] = useState75(() => defaultProps);
  const setInputProps = useCallback110((updater) => {
    _setInputProps(updater);
  }, []);
  const [metadata] = useState75(() => defaultMetadata);
  const endFrame = useMemo114(() => {
    if (endFrameOrNull === null) {
      return resolvedComposition.durationInFrames - 1;
    }
    return Math.max(0, Math.min(resolvedComposition.durationInFrames - 1, endFrameOrNull));
  }, [resolvedComposition.durationInFrames, endFrameOrNull]);
  const startFrame = useMemo114(() => {
    if (startFrameOrNull === null) {
      return 0;
    }
    return Math.max(0, Math.min(endFrame - 1, startFrameOrNull));
  }, [endFrame, startFrameOrNull]);
  const frame2 = useMemo114(() => {
    const parsed = Math.floor(unclampedFrame);
    return Math.max(0, Math.min(resolvedComposition.durationInFrames - 1, parsed));
  }, [resolvedComposition.durationInFrames, unclampedFrame]);
  const deriveFinalAudioCodec = useCallback110((passedVideoCodec, passedAudioCodec) => {
    if (passedAudioCodec !== null && BrowserSafeApis10.supportedAudioCodecs[passedVideoCodec].includes(passedAudioCodec)) {
      return passedAudioCodec;
    }
    return BrowserSafeApis10.defaultAudioCodecs[passedVideoCodec].compressed;
  }, []);
  const setDefaultOutName = useCallback110((options) => {
    if (options.type === "still") {
      setOutName((prev) => {
        const newFileName = getStringBeforeSuffix(prev) + "." + options.imageFormat;
        return newFileName;
      });
    } else if (options.type === "sequence") {
      setOutName((prev) => {
        const folderName = getStringBeforeSuffix(prev);
        return folderName;
      });
    } else {
      setOutName((prev) => {
        const codecSuffix = BrowserSafeApis10.getFileExtensionFromCodec(options.codec, deriveFinalAudioCodec(options.codec, options.audioCodec));
        const newFileName = getStringBeforeSuffix(prev) + "." + codecSuffix;
        return newFileName;
      });
    }
  }, [deriveFinalAudioCodec]);
  const setAudioCodec = useCallback110((newAudioCodec) => {
    setUserSelectedAudioCodec(newAudioCodec);
    setDefaultOutName({
      type: "render",
      codec: videoCodecForVideoTab,
      audioCodec: newAudioCodec
    });
    setSeparateAudioTo((prev) => {
      if (prev === null) {
        return null;
      }
      const newExtension = BrowserSafeApis10.getExtensionFromAudioCodec(newAudioCodec);
      const newFileName = getStringBeforeSuffix(prev) + "." + newExtension;
      return newFileName;
    });
  }, [setDefaultOutName, videoCodecForVideoTab]);
  const setCodec = useCallback110((newCodec) => {
    if (renderMode === "audio") {
      setVideoCodecForAudioTab(newCodec);
    } else {
      setVideoCodecForVideoTab(newCodec);
    }
    setDefaultOutName({
      type: "render",
      codec: newCodec,
      audioCodec: deriveFinalAudioCodec(newCodec, userSelectedAudioCodec)
    });
  }, [
    userSelectedAudioCodec,
    deriveFinalAudioCodec,
    renderMode,
    setDefaultOutName
  ]);
  const setStillFormat = useCallback110((format) => {
    setStillImageFormat(format);
    setDefaultOutName({ type: "still", imageFormat: format });
  }, [setDefaultOutName]);
  const { setSidebarCollapsedState } = useContext116(SidebarContext);
  const onClickStill = useCallback110(() => {
    setSidebarCollapsedState({ left: null, right: "expanded" });
    persistSelectedOptionsSidebarPanel("renders");
    optionsSidebarTabs.current?.selectRendersPanel();
    dispatchIfMounted({ type: "start" });
    addStillRenderJob({
      compositionId: resolvedComposition.id,
      outName,
      imageFormat: stillImageFormat,
      jpegQuality,
      frame: frame2,
      scale,
      logLevel,
      chromiumOptions,
      delayRenderTimeout,
      envVariables: envVariablesArrayToObject(envVariables),
      inputProps,
      offthreadVideoCacheSizeInBytes,
      multiProcessOnLinux,
      beepOnFinish,
      metadata,
      chromeMode,
      offthreadVideoThreads,
      mediaCacheSizeInBytes,
      licenseKey: renderDefaults.publicLicenseKey
    }).then(() => {
      dispatchIfMounted({ type: "succeed" });
      setSelectedModal(null);
    }).catch(() => {
      dispatchIfMounted({ type: "fail" });
    });
  }, [
    setSidebarCollapsedState,
    dispatchIfMounted,
    resolvedComposition.id,
    outName,
    stillImageFormat,
    jpegQuality,
    frame2,
    scale,
    logLevel,
    chromiumOptions,
    delayRenderTimeout,
    envVariables,
    inputProps,
    offthreadVideoCacheSizeInBytes,
    multiProcessOnLinux,
    beepOnFinish,
    setSelectedModal,
    metadata,
    chromeMode,
    offthreadVideoThreads,
    mediaCacheSizeInBytes,
    renderDefaults.publicLicenseKey
  ]);
  const [everyNthFrameSetting, setEveryNthFrameSetting] = useState75(() => initialEveryNthFrame);
  const everyNthFrame = useMemo114(() => {
    if (codec === "gif") {
      return everyNthFrameSetting;
    }
    return 1;
  }, [codec, everyNthFrameSetting]);
  const audioCodec = deriveFinalAudioCodec(codec, userSelectedAudioCodec);
  const availablePixelFormats = useMemo114(() => {
    return BrowserSafeApis10.validPixelFormatsForCodec(codec);
  }, [codec]);
  const pixelFormat = useMemo114(() => {
    if (availablePixelFormats.includes(userPreferredPixelFormat)) {
      return userPreferredPixelFormat;
    }
    return availablePixelFormats[0];
  }, [availablePixelFormats, userPreferredPixelFormat]);
  const onClickVideo = useCallback110(() => {
    setSidebarCollapsedState({ left: null, right: "expanded" });
    persistSelectedOptionsSidebarPanel("renders");
    optionsSidebarTabs.current?.selectRendersPanel();
    dispatchIfMounted({ type: "start" });
    addVideoRenderJob({
      compositionId: resolvedComposition.id,
      outName,
      imageFormat: videoImageFormat,
      jpegQuality: stillImageFormat === "jpeg" ? jpegQuality : null,
      scale,
      logLevel,
      codec,
      concurrency,
      crf: qualityControlType === "crf" && hardwareAcceleration !== "if-possible" && hardwareAcceleration !== "required" ? crf : null,
      gopSize,
      endFrame,
      startFrame,
      muted,
      enforceAudioTrack,
      proResProfile,
      x264Preset,
      pixelFormat,
      audioBitrate,
      videoBitrate,
      everyNthFrame,
      numberOfGifLoops,
      delayRenderTimeout,
      audioCodec,
      disallowParallelEncoding,
      chromiumOptions,
      envVariables: envVariablesArrayToObject(envVariables),
      inputProps,
      offthreadVideoCacheSizeInBytes,
      colorSpace,
      multiProcessOnLinux,
      encodingBufferSize,
      encodingMaxRate,
      beepOnFinish,
      repro,
      forSeamlessAacConcatenation,
      separateAudioTo,
      metadata,
      hardwareAcceleration,
      chromeMode,
      offthreadVideoThreads,
      mediaCacheSizeInBytes,
      sampleRate,
      licenseKey: renderDefaults.publicLicenseKey
    }).then(() => {
      dispatchIfMounted({ type: "succeed" });
      setSelectedModal(null);
    }).catch(() => {
      dispatchIfMounted({ type: "fail" });
    });
  }, [
    setSidebarCollapsedState,
    dispatchIfMounted,
    resolvedComposition.id,
    outName,
    videoImageFormat,
    stillImageFormat,
    jpegQuality,
    scale,
    logLevel,
    codec,
    concurrency,
    qualityControlType,
    crf,
    endFrame,
    startFrame,
    muted,
    enforceAudioTrack,
    proResProfile,
    x264Preset,
    pixelFormat,
    audioBitrate,
    videoBitrate,
    everyNthFrame,
    numberOfGifLoops,
    delayRenderTimeout,
    audioCodec,
    disallowParallelEncoding,
    chromiumOptions,
    envVariables,
    inputProps,
    offthreadVideoCacheSizeInBytes,
    colorSpace,
    multiProcessOnLinux,
    encodingBufferSize,
    encodingMaxRate,
    beepOnFinish,
    repro,
    forSeamlessAacConcatenation,
    gopSize,
    separateAudioTo,
    setSelectedModal,
    metadata,
    hardwareAcceleration,
    chromeMode,
    offthreadVideoThreads,
    mediaCacheSizeInBytes,
    sampleRate,
    renderDefaults.publicLicenseKey
  ]);
  const onClickSequence = useCallback110(() => {
    setSidebarCollapsedState({ left: null, right: "expanded" });
    persistSelectedOptionsSidebarPanel("renders");
    optionsSidebarTabs.current?.selectRendersPanel();
    dispatchIfMounted({ type: "start" });
    addSequenceRenderJob({
      compositionId: resolvedComposition.id,
      outName,
      imageFormat: sequenceImageFormat,
      scale,
      logLevel,
      concurrency,
      endFrame,
      jpegQuality,
      startFrame,
      delayRenderTimeout,
      chromiumOptions,
      envVariables: envVariablesArrayToObject(envVariables),
      inputProps,
      offthreadVideoCacheSizeInBytes,
      disallowParallelEncoding,
      multiProcessOnLinux,
      beepOnFinish,
      repro,
      metadata,
      chromeMode,
      offthreadVideoThreads,
      mediaCacheSizeInBytes
    }).then(() => {
      dispatchIfMounted({ type: "succeed" });
      setSelectedModal(null);
    }).catch(() => {
      dispatchIfMounted({ type: "fail" });
    });
  }, [
    setSidebarCollapsedState,
    dispatchIfMounted,
    resolvedComposition.id,
    outName,
    sequenceImageFormat,
    scale,
    logLevel,
    concurrency,
    endFrame,
    jpegQuality,
    startFrame,
    delayRenderTimeout,
    chromiumOptions,
    envVariables,
    inputProps,
    offthreadVideoCacheSizeInBytes,
    disallowParallelEncoding,
    multiProcessOnLinux,
    beepOnFinish,
    repro,
    setSelectedModal,
    metadata,
    chromeMode,
    offthreadVideoThreads,
    mediaCacheSizeInBytes
  ]);
  useEffect76(() => {
    return () => {
      isMounted.current = false;
    };
  }, []);
  const imageFormatOptions = useMemo114(() => {
    if (renderMode === "still") {
      return [
        {
          label: "PNG",
          onClick: () => setStillFormat("png"),
          key: "png",
          selected: stillImageFormat === "png"
        },
        {
          label: "JPEG",
          onClick: () => setStillFormat("jpeg"),
          key: "jpeg",
          selected: stillImageFormat === "jpeg"
        },
        {
          label: "PDF",
          onClick: () => setStillFormat("pdf"),
          key: "pdf",
          selected: stillImageFormat === "pdf"
        },
        {
          label: "WebP",
          onClick: () => setStillFormat("webp"),
          key: "webp",
          selected: stillImageFormat === "webp"
        }
      ];
    }
    if (renderMode === "sequence") {
      return [
        {
          label: "PNG",
          onClick: () => setSequenceImageFormat("png"),
          key: "png",
          selected: sequenceImageFormat === "png"
        },
        {
          label: "JPEG",
          onClick: () => setSequenceImageFormat("jpeg"),
          key: "jpeg",
          selected: sequenceImageFormat === "jpeg"
        }
      ];
    }
    return [
      {
        label: "PNG",
        onClick: () => setVideoImageFormat("png"),
        key: "png",
        selected: videoImageFormat === "png"
      },
      {
        label: "JPEG",
        onClick: () => setVideoImageFormat("jpeg"),
        key: "jpeg",
        selected: videoImageFormat === "jpeg"
      }
    ];
  }, [
    renderMode,
    videoImageFormat,
    stillImageFormat,
    setStillFormat,
    sequenceImageFormat
  ]);
  const setRenderMode = useCallback110((newRenderMode) => {
    setRenderModeState(newRenderMode);
    if (newRenderMode === "audio") {
      setDefaultOutName({
        type: "render",
        codec: videoCodecForAudioTab,
        audioCodec: deriveFinalAudioCodec(videoCodecForAudioTab, userSelectedAudioCodec)
      });
    }
    if (newRenderMode === "video") {
      setDefaultOutName({
        type: "render",
        codec: videoCodecForVideoTab,
        audioCodec: deriveFinalAudioCodec(videoCodecForVideoTab, userSelectedAudioCodec)
      });
    }
    if (newRenderMode === "still") {
      setDefaultOutName({ type: "still", imageFormat: stillImageFormat });
    }
    if (newRenderMode === "sequence") {
      setDefaultOutName({ type: "sequence" });
    }
  }, [
    videoCodecForAudioTab,
    userSelectedAudioCodec,
    deriveFinalAudioCodec,
    setDefaultOutName,
    stillImageFormat,
    videoCodecForVideoTab
  ]);
  const renderTabOptions = useMemo114(() => {
    if (resolvedComposition?.durationInFrames < 2) {
      return [
        {
          label: "Still",
          onClick: () => {
            setRenderMode("still");
          },
          key: "still",
          selected: renderMode === "still"
        }
      ];
    }
    return [
      {
        label: "Still",
        onClick: () => {
          setRenderMode("still");
        },
        key: "still",
        selected: renderMode === "still"
      },
      {
        label: "Video",
        onClick: () => {
          setRenderMode("video");
        },
        key: "video",
        selected: renderMode === "video"
      },
      {
        label: "Audio",
        onClick: () => {
          setRenderMode("audio");
        },
        key: "audio",
        selected: renderMode === "audio"
      },
      {
        label: "Image sequence",
        onClick: () => {
          setRenderMode("sequence");
        },
        key: "sequence",
        selected: renderMode === "sequence"
      }
    ];
  }, [resolvedComposition?.durationInFrames, renderMode, setRenderMode]);
  const outnameValidation = validateOutnameGui({
    outName,
    codec,
    audioCodec,
    renderMode,
    stillImageFormat,
    separateAudioTo
  });
  const { tab, setTab, shownTabs } = useRenderModalSections(renderMode, codec);
  const { registerKeybinding } = useKeybinding();
  const readOnlyRenderCommand = useMemo114(() => {
    if (!readOnlyStudio) {
      return null;
    }
    return makeReadOnlyStudioRenderCommand({
      remotionVersion: window.remotion_version,
      locationHref: window.location.href,
      compositionId: resolvedComposition.id,
      outName,
      renderMode,
      renderDefaults,
      durationInFrames: resolvedComposition.durationInFrames,
      concurrency,
      frame: frame2,
      startFrame,
      endFrame,
      stillImageFormat,
      sequenceImageFormat,
      videoImageFormat,
      jpegQuality: renderMode === "video" ? stillImageFormat === "jpeg" ? jpegQuality : null : renderMode === "audio" ? null : jpegQuality,
      codec,
      muted,
      enforceAudioTrack,
      proResProfile,
      x264Preset,
      pixelFormat,
      crf: qualityControlType === "crf" && hardwareAcceleration !== "if-possible" && hardwareAcceleration !== "required" ? crf : null,
      gopSize,
      videoBitrate,
      audioBitrate,
      audioCodec,
      everyNthFrame,
      numberOfGifLoops,
      disallowParallelEncoding,
      encodingBufferSize,
      encodingMaxRate,
      forSeamlessAacConcatenation,
      separateAudioTo,
      colorSpace,
      scale,
      logLevel,
      delayRenderTimeout,
      hardwareAcceleration,
      chromeMode,
      headless,
      disableWebSecurity,
      ignoreCertificateErrors,
      gl: openGlOption === "default" ? null : openGlOption,
      userAgent,
      multiProcessOnLinux,
      darkMode,
      offthreadVideoCacheSizeInBytes,
      offthreadVideoThreads,
      mediaCacheSizeInBytes,
      beepOnFinish,
      repro,
      metadata,
      sampleRate,
      envVariables: envVariablesArrayToObject(envVariables),
      inputProps
    });
  }, [
    audioBitrate,
    audioCodec,
    beepOnFinish,
    chromeMode,
    codec,
    colorSpace,
    concurrency,
    crf,
    darkMode,
    delayRenderTimeout,
    disableWebSecurity,
    disallowParallelEncoding,
    endFrame,
    encodingBufferSize,
    encodingMaxRate,
    enforceAudioTrack,
    envVariables,
    everyNthFrame,
    frame2,
    forSeamlessAacConcatenation,
    gopSize,
    hardwareAcceleration,
    headless,
    ignoreCertificateErrors,
    inputProps,
    jpegQuality,
    logLevel,
    mediaCacheSizeInBytes,
    metadata,
    multiProcessOnLinux,
    muted,
    numberOfGifLoops,
    offthreadVideoCacheSizeInBytes,
    offthreadVideoThreads,
    openGlOption,
    outName,
    pixelFormat,
    proResProfile,
    qualityControlType,
    readOnlyStudio,
    renderDefaults,
    renderMode,
    repro,
    resolvedComposition.durationInFrames,
    resolvedComposition.id,
    scale,
    sampleRate,
    separateAudioTo,
    sequenceImageFormat,
    startFrame,
    stillImageFormat,
    userAgent,
    videoImageFormat,
    videoBitrate,
    x264Preset
  ]);
  const [commandCopiedAt, setCommandCopiedAt] = useState75(null);
  const renderDisabled = readOnlyStudio ? false : !outnameValidation.valid || state.type === "load";
  const trigger = useCallback110(() => {
    if (readOnlyStudio) {
      if (!readOnlyRenderCommand) {
        return;
      }
      copyText(readOnlyRenderCommand).then(() => {
        setCommandCopiedAt(Date.now());
      }).catch((err) => {
        showNotification(`Could not copy: ${err.message}`, 2000);
      });
      return;
    }
    if (renderMode === "still") {
      onClickStill();
    } else if (renderMode === "sequence") {
      onClickSequence();
    } else {
      onClickVideo();
    }
  }, [
    onClickSequence,
    onClickStill,
    onClickVideo,
    readOnlyRenderCommand,
    readOnlyStudio,
    renderMode
  ]);
  useEffect76(() => {
    if (commandCopiedAt === null) {
      return;
    }
    const timeout = setTimeout(() => {
      setCommandCopiedAt(null);
    }, 2000);
    return () => clearTimeout(timeout);
  }, [commandCopiedAt]);
  useEffect76(() => {
    if (renderDisabled) {
      return;
    }
    const enter = registerKeybinding({
      callback() {
        trigger();
      },
      commandCtrlKey: true,
      key: "Enter",
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: false
    });
    return () => {
      enter.unregister();
    };
  }, [registerKeybinding, renderDisabled, trigger]);
  const pixelFormatOptions = useMemo114(() => {
    return availablePixelFormats.map((option) => {
      return {
        label: option,
        onClick: () => setPixelFormat(option),
        key: option,
        id: option,
        keyHint: null,
        leftItem: pixelFormat === option ? /* @__PURE__ */ jsx206(Checkmark, {}) : null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: option
      };
    });
  }, [availablePixelFormats, pixelFormat]);
  return /* @__PURE__ */ jsxs125("div", {
    style: outerModalStyle,
    children: [
      /* @__PURE__ */ jsx206(ModalHeader, {
        title: `Render ${resolvedComposition.id} ${readOnlyStudio ? "via CLI" : "on the server"}`
      }),
      /* @__PURE__ */ jsxs125("div", {
        style: container,
        children: [
          /* @__PURE__ */ jsx206(SegmentedControl, {
            items: renderTabOptions,
            needsWrapping: false
          }),
          /* @__PURE__ */ jsx206("div", {
            style: flexer
          }),
          /* @__PURE__ */ jsxs125(Button, {
            autoFocus: true,
            onClick: trigger,
            disabled: renderDisabled,
            style: {
              ...buttonStyle,
              backgroundColor: outnameValidation.valid ? BLUE : BLUE_DISABLED
            },
            children: [
              readOnlyStudio ? commandCopiedAt ? "Copied command!" : "Copy command" : state.type === "idle" ? `Render ${renderMode}` : "Rendering...",
              /* @__PURE__ */ jsx206(ShortcutHint, {
                keyToPress: "↵",
                cmdOrCtrl: true
              })
            ]
          })
        ]
      }),
      /* @__PURE__ */ jsxs125("div", {
        style: horizontalLayout,
        children: [
          /* @__PURE__ */ jsxs125("div", {
            style: leftSidebar,
            children: [
              shownTabs.includes("general") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "general",
                onClick: () => setTab("general"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(FileIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "General"
              }) : null,
              shownTabs.includes("data") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "data",
                onClick: () => setTab("data"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(InputPropsIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Input Props"
              }) : null,
              shownTabs.includes("picture") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "picture",
                onClick: () => setTab("picture"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(PicIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Picture"
              }) : null,
              shownTabs.includes("audio") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "audio",
                onClick: () => setTab("audio"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(AudioIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Audio"
              }) : null,
              shownTabs.includes("gif") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "gif",
                onClick: () => setTab("gif"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(GifIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "GIF"
              }) : null,
              shownTabs.includes("encoding") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "encoding",
                onClick: () => setTab("encoding"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(FilmIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Encoding"
              }) : null,
              shownTabs.includes("environment") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "environment",
                onClick: () => setTab("environment"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(DataIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Environment"
              }) : null,
              shownTabs.includes("advanced") ? /* @__PURE__ */ jsx206(VerticalTab, {
                style: horizontalTab,
                selected: tab === "advanced",
                onClick: () => setTab("advanced"),
                renderIcon: (color) => /* @__PURE__ */ jsx206("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx206(GearIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Other"
              }) : null
            ]
          }),
          /* @__PURE__ */ jsx206("div", {
            style: optionsPanel,
            className: VERTICAL_SCROLLBAR_CLASSNAME,
            children: tab === "general" ? /* @__PURE__ */ jsx206(RenderModalBasic, {
              codec,
              resolvedComposition,
              frame: frame2,
              imageFormatOptions,
              outName,
              proResProfile,
              renderMode,
              setVideoCodec: setCodec,
              setFrame,
              setOutName,
              setProResProfile,
              endFrame,
              setEndFrame,
              setStartFrame,
              setVerboseLogging: setLogLevel,
              logLevel,
              showOutputName: !readOnlyStudio,
              startFrame,
              validationMessage: outnameValidation.valid ? null : outnameValidation.error.message
            }) : tab === "picture" ? /* @__PURE__ */ jsx206(RenderModalPicture, {
              renderMode,
              scale,
              setScale,
              imageFormatOptions,
              jpegQuality,
              setJpegQuality,
              videoImageFormat,
              stillImageFormat,
              compositionWidth: resolvedComposition.width,
              compositionHeight: resolvedComposition.height
            }) : tab === "audio" ? /* @__PURE__ */ jsx206(RenderModalAudio, {
              muted,
              renderMode,
              setMuted,
              codec,
              audioCodec,
              setAudioCodec,
              enforceAudioTrack,
              setEnforceAudioTrackState,
              customTargetAudioBitrate,
              setCustomTargetAudioBitrateValue,
              setShouldHaveCustomTargetAudioBitrate,
              shouldHaveCustomTargetAudioBitrate,
              forSeamlessAacConcatenation,
              setForSeamlessAacConcatenation,
              separateAudioTo,
              setSeparateAudioTo,
              outName,
              sampleRate,
              setSampleRate
            }) : tab === "gif" ? /* @__PURE__ */ jsx206(RenderModalGif, {
              everyNthFrame,
              limitNumberOfGifLoops,
              numberOfGifLoopsSetting,
              setEveryNthFrameSetting,
              setLimitNumberOfGifLoops,
              setNumberOfGifLoopsSetting
            }) : tab === "encoding" ? /* @__PURE__ */ jsx206(RenderModalEncoding, {
              renderMode,
              codec,
              qualityControlType,
              setQualityControl,
              shouldDisplayQualityControlPicker: supportsBothQualityControls,
              crf,
              setCrf,
              maxCrf,
              minCrf,
              customTargetVideoBitrate,
              setCustomTargetVideoBitrateValue,
              encodingBufferSize,
              setEncodingBufferSize,
              encodingMaxRate,
              setEncodingMaxRate,
              pixelFormat,
              pixelFormatOptions,
              colorSpace,
              setColorSpace,
              x264Preset,
              setx264Preset,
              gopSize,
              setGopSize,
              hardwareAcceleration,
              setHardwareAcceleration,
              disallowParallelEncoding,
              setDisallowParallelEncoding
            }) : tab === "environment" ? /* @__PURE__ */ jsx206(RenderModalEnvironmentVariables, {
              envVariables,
              setEnvVariables
            }) : tab === "data" ? /* @__PURE__ */ jsx206(DataEditor, {
              defaultProps: inputProps,
              setDefaultProps: setInputProps,
              unresolvedComposition,
              propsEditType: "input-props",
              canSaveDefaultProps: {
                canUpdate: false,
                reason: "render dialogue",
                determined: false
              }
            }) : /* @__PURE__ */ jsx206(RenderModalAdvanced, {
              concurrency,
              maxConcurrency,
              minConcurrency,
              renderMode,
              setConcurrency,
              delayRenderTimeout,
              setDelayRenderTimeout,
              setDisableWebSecurity,
              setIgnoreCertificateErrors,
              setHeadless,
              headless,
              ignoreCertificateErrors,
              disableWebSecurity,
              openGlOption,
              setOpenGlOption,
              offthreadVideoCacheSizeInBytes,
              setMediaCacheSizeInBytes,
              mediaCacheSizeInBytes,
              setOffthreadVideoCacheSizeInBytes,
              offthreadVideoThreads,
              setOffthreadVideoThreads,
              enableMultiProcessOnLinux: multiProcessOnLinux,
              setChromiumMultiProcessOnLinux,
              userAgent,
              setUserAgent,
              setBeep: setBeepOnFinish,
              beep: beepOnFinish,
              repro,
              setRepro,
              chromeModeOption: chromeMode,
              setChromeModeOption: setChromeMode,
              darkMode,
              setDarkMode
            })
          })
        ]
      })
    ]
  });
};
var RenderModalWithLoader = (props) => {
  return /* @__PURE__ */ jsx206(DismissableModal, {
    children: /* @__PURE__ */ jsx206(ResolveCompositionBeforeModal, {
      compositionId: props.compositionId,
      children: /* @__PURE__ */ jsx206(RenderModal, {
        ...props
      })
    })
  });
};

// src/components/RenderModal/WebRenderModal.tsx
import { getDefaultOutLocation as getDefaultOutLocation2 } from "@remotion/studio-shared";
import {
  getDefaultAudioCodecForContainer,
  getDefaultContainerForCodec,
  getDefaultVideoCodecForContainer,
  isAudioOnlyContainer
} from "@remotion/web-renderer";
import { useCallback as useCallback113, useContext as useContext117, useMemo as useMemo119, useState as useState78 } from "react";

// src/icons/certificate.tsx
import { jsx as jsx207 } from "react/jsx-runtime";
var CertificateIcon = ({
  color,
  ...props
}) => /* @__PURE__ */ jsx207("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 576 512",
  ...props,
  children: /* @__PURE__ */ jsx207("path", {
    fill: color ?? CURRENT_COLOR_LOWERCASE,
    d: "M192 32l128 0 0 96c0 35.3 28.7 64 64 64l96 0 0 256c0 17.7-14.3 32-32 32l-192 0 0 32 192 0c35.3 0 64-28.7 64-64l0-261.5c0-17-6.7-33.3-18.7-45.3L370.7 18.7C358.7 6.7 342.5 0 325.5 0L192 0c-35.3 0-64 28.7-64 64l0 80c10.9 0 21.6 1 32 2.9L160 64c0-17.7 14.3-32 32-32zM352 45.3L466.7 160 384 160c-17.7 0-32-14.3-32-32l0-82.7zM32 320a96 96 0 1 1 192 0 96 96 0 1 1 -192 0zM176 438.7l0 66.3-40.1-22.9c-4.9-2.8-11-2.8-15.9 0L80 505 80 438.7c14.8 6 31 9.3 48 9.3s33.2-3.3 48-9.3zm32-18.8c29.3-23.5 48-59.5 48-99.9 0-70.7-57.3-128-128-128S0 249.3 0 320c0 40.4 18.7 76.5 48 99.9l0 101.8c0 12.3 10 22.3 22.3 22.3 3.9 0 7.7-1 11.1-2.9l46.6-26.6 46.6 26.6c3.4 1.9 7.2 2.9 11.1 2.9 12.3 0 22.3-10 22.3-22.3l0-101.8zM128 344a24 24 0 1 1 0-48 24 24 0 1 1 0 48zm0-80a56 56 0 1 0 0 112 56 56 0 1 0 0-112z"
  })
});

// src/components/RenderModal/use-encodable-audio-codecs.ts
import {
  getEncodableAudioCodecs,
  getSupportedAudioCodecsForContainer
} from "@remotion/web-renderer";
import { useEffect as useEffect77, useRef as useRef63, useState as useState76 } from "react";
var useEncodableAudioCodecs = (container44) => {
  const cacheRef = useRef63({});
  const [codecsByContainer, setCodecsByContainer] = useState76(() => {
    return {
      [container44]: getSupportedAudioCodecsForContainer(container44)
    };
  });
  useEffect77(() => {
    const cached = cacheRef.current[container44];
    if (cached) {
      return;
    }
    const supported = getSupportedAudioCodecsForContainer(container44);
    cacheRef.current[container44] = {
      codecs: supported,
      status: "fetching"
    };
    getEncodableAudioCodecs(container44).then((encodable) => {
      cacheRef.current[container44] = {
        codecs: encodable,
        status: "done"
      };
      setCodecsByContainer((prev) => ({
        ...prev,
        [container44]: encodable
      }));
    }).catch(() => {
      cacheRef.current[container44] = {
        codecs: supported,
        status: "done"
      };
    });
  }, [container44]);
  return codecsByContainer[container44] ?? getSupportedAudioCodecsForContainer(container44);
};

// src/components/RenderModal/use-encodable-video-codecs.ts
import {
  getEncodableVideoCodecs,
  getSupportedVideoCodecsForContainer
} from "@remotion/web-renderer";
import { useEffect as useEffect78, useRef as useRef64, useState as useState77 } from "react";
var useEncodableVideoCodecs = (container44) => {
  const cacheRef = useRef64({});
  const [codecsByContainer, setCodecsByContainer] = useState77(() => {
    return {
      [container44]: getSupportedVideoCodecsForContainer(container44)
    };
  });
  useEffect78(() => {
    const cached = cacheRef.current[container44];
    if (cached) {
      return;
    }
    const supported = getSupportedVideoCodecsForContainer(container44);
    cacheRef.current[container44] = {
      codecs: supported,
      status: "fetching"
    };
    getEncodableVideoCodecs(container44).then((encodable) => {
      cacheRef.current[container44] = {
        codecs: encodable,
        status: "done"
      };
      setCodecsByContainer((prev) => ({
        ...prev,
        [container44]: encodable
      }));
    }).catch(() => {
      cacheRef.current[container44] = {
        codecs: supported,
        status: "done"
      };
    });
  }, [container44]);
  return codecsByContainer[container44] ?? getSupportedVideoCodecsForContainer(container44);
};

// src/components/RenderModal/WebRenderModalAdvanced.tsx
import { useCallback as useCallback111, useMemo as useMemo115 } from "react";
import { jsx as jsx208, jsxs as jsxs126, Fragment as Fragment54 } from "react/jsx-runtime";
var tabContainer = {
  flex: 1
};
var paragraph = {
  margin: 0,
  marginBottom: 8
};
var lastParagraph = {
  margin: 0
};
var PageResponsivenessDescription = () => {
  return /* @__PURE__ */ jsxs126(Fragment54, {
    children: [
      /* @__PURE__ */ jsx208("p", {
        style: paragraph,
        children: "The Web Renderer runs in the same browser tab as the Studio. Rendering can block the tab while Remotion captures frames."
      }),
      /* @__PURE__ */ jsxs126("p", {
        style: paragraph,
        children: [
          "The default is ",
          /* @__PURE__ */ jsx208("code", {
            children: "Medium"
          }),
          ", which tries to free the event loop every 33ms of rendering work so progress updates and UI interactions can stay responsive."
        ]
      }),
      /* @__PURE__ */ jsxs126("p", {
        style: lastParagraph,
        children: [
          "Choose ",
          /* @__PURE__ */ jsx208("code", {
            children: "Disabled"
          }),
          " to prioritize render speed, or",
          " ",
          /* @__PURE__ */ jsx208("code", {
            children: "High"
          }),
          " to give the browser more chances to update."
        ]
      })
    ]
  });
};
var WebRenderModalAdvanced = ({
  renderMode,
  delayRenderTimeout,
  setDelayRenderTimeout,
  mediaCacheSizeInBytes,
  setMediaCacheSizeInBytes,
  hardwareAcceleration,
  setHardwareAcceleration,
  allowHtmlInCanvas,
  setAllowHtmlInCanvas,
  pageResponsiveness,
  setPageResponsiveness
}) => {
  const toggleCustomMediaCacheSizeInBytes = useCallback111(() => {
    setMediaCacheSizeInBytes((previous) => {
      if (previous === null) {
        return 1000 * 1000 * 1000;
      }
      return null;
    });
  }, [setMediaCacheSizeInBytes]);
  const toggleAllowHtmlInCanvas = useCallback111(() => {
    setAllowHtmlInCanvas((previous) => !previous);
  }, [setAllowHtmlInCanvas]);
  const changeMediaCacheSizeInBytes = useCallback111((cb) => {
    setMediaCacheSizeInBytes((prev) => {
      if (prev === null) {
        throw new TypeError("Expected previous value");
      }
      if (typeof cb === "function") {
        return cb(prev);
      }
      return cb;
    });
  }, [setMediaCacheSizeInBytes]);
  const hardwareAccelerationOptions = useMemo115(() => {
    return [
      {
        label: "No Preference",
        onClick: () => setHardwareAcceleration("no-preference"),
        leftItem: hardwareAcceleration === "no-preference" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "no-preference",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "no-preference"
      },
      {
        label: "Prefer Hardware",
        onClick: () => setHardwareAcceleration("prefer-hardware"),
        leftItem: hardwareAcceleration === "prefer-hardware" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "prefer-hardware",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "prefer-hardware"
      },
      {
        label: "Prefer Software",
        onClick: () => setHardwareAcceleration("prefer-software"),
        leftItem: hardwareAcceleration === "prefer-software" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "prefer-software",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "prefer-software"
      }
    ];
  }, [hardwareAcceleration, setHardwareAcceleration]);
  const selectedPageResponsiveness = typeof pageResponsiveness === "number" ? "custom" : pageResponsiveness;
  const pageResponsivenessOptions = useMemo115(() => {
    return [
      {
        label: "Disabled",
        onClick: () => setPageResponsiveness("disabled"),
        leftItem: selectedPageResponsiveness === "disabled" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "disabled",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "disabled"
      },
      {
        label: "Low (100ms)",
        onClick: () => setPageResponsiveness("low"),
        leftItem: selectedPageResponsiveness === "low" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "low",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "low"
      },
      {
        label: "Medium (33ms)",
        onClick: () => setPageResponsiveness("medium"),
        leftItem: selectedPageResponsiveness === "medium" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "medium",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "medium"
      },
      {
        label: "High (16ms)",
        onClick: () => setPageResponsiveness("high"),
        leftItem: selectedPageResponsiveness === "high" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "high",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "high"
      },
      {
        label: "Custom",
        onClick: () => setPageResponsiveness((previous) => typeof previous === "number" ? previous : 33),
        leftItem: selectedPageResponsiveness === "custom" ? /* @__PURE__ */ jsx208(Checkmark, {}) : null,
        id: "custom",
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: "custom"
      }
    ];
  }, [selectedPageResponsiveness, setPageResponsiveness]);
  const setCustomPageResponsiveness = useCallback111((value) => {
    setPageResponsiveness((previous) => {
      const currentValue = typeof previous === "number" ? previous : 33;
      return typeof value === "function" ? value(currentValue) : value;
    });
  }, [setPageResponsiveness]);
  return /* @__PURE__ */ jsxs126("div", {
    style: tabContainer,
    children: [
      /* @__PURE__ */ jsx208(NumberSetting, {
        name: "Delay Render Timeout",
        formatter: (v) => `${v}ms`,
        min: 0,
        max: 1e9,
        step: 1000,
        value: delayRenderTimeout,
        onValueChanged: setDelayRenderTimeout,
        hint: "delayRenderTimeoutInMillisecondsOption"
      }),
      /* @__PURE__ */ jsxs126("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs126("div", {
            style: label,
            children: [
              "Custom @remotion/media cache size ",
              /* @__PURE__ */ jsx208(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx208(WebRendererOptionExplainerBubble, {
                apiName: "mediaCacheSizeInBytes",
                id: "mediaCacheSizeInBytesOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx208("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx208(Checkbox, {
              checked: mediaCacheSizeInBytes !== null,
              onChange: toggleCustomMediaCacheSizeInBytes,
              name: "media-cache-size"
            })
          })
        ]
      }),
      mediaCacheSizeInBytes === null ? null : /* @__PURE__ */ jsx208(NumberSetting, {
        name: "@remotion/media cache size",
        formatter: (w) => `${w} bytes`,
        min: 0,
        max: 10000000000,
        step: 10 * 1024 * 1024,
        value: mediaCacheSizeInBytes,
        onValueChanged: changeMediaCacheSizeInBytes
      }),
      renderMode === "video" ? /* @__PURE__ */ jsxs126("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx208("div", {
            style: label,
            children: "Hardware Acceleration"
          }),
          /* @__PURE__ */ jsx208("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx208(Combobox, {
              values: hardwareAccelerationOptions,
              selectedId: hardwareAcceleration,
              title: "Hardware Acceleration"
            })
          })
        ]
      }) : null,
      /* @__PURE__ */ jsxs126("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs126("div", {
            style: label,
            children: [
              "Allow HTML-in-canvas ",
              /* @__PURE__ */ jsx208(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx208(WebRendererOptionExplainerBubble, {
                apiName: "allowHtmlInCanvas",
                id: "allowHtmlInCanvasOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx208("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx208(Checkbox, {
              checked: allowHtmlInCanvas,
              onChange: toggleAllowHtmlInCanvas,
              name: "allow-html-in-canvas"
            })
          })
        ]
      }),
      renderMode === "still" ? null : /* @__PURE__ */ jsxs126(Fragment54, {
        children: [
          /* @__PURE__ */ jsxs126("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs126("div", {
                style: label,
                children: [
                  "Page Responsiveness ",
                  /* @__PURE__ */ jsx208(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx208(WebRendererCustomOptionExplainerBubble, {
                    apiName: "pageResponsiveness",
                    description: /* @__PURE__ */ jsx208(PageResponsivenessDescription, {}),
                    docLink: "https://www.remotion.dev/docs/client-side-rendering/page-responsiveness",
                    name: "Page responsiveness"
                  })
                ]
              }),
              /* @__PURE__ */ jsx208("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx208(Combobox, {
                  values: pageResponsivenessOptions,
                  selectedId: selectedPageResponsiveness,
                  title: "Page Responsiveness"
                })
              })
            ]
          }),
          typeof pageResponsiveness === "number" ? /* @__PURE__ */ jsx208(NumberSetting, {
            name: "Responsiveness Interval",
            formatter: (v) => `${v}ms`,
            min: 1,
            max: 1e9,
            step: 1,
            value: pageResponsiveness,
            onValueChanged: setCustomPageResponsiveness
          }) : null
        ]
      })
    ]
  });
};

// src/components/RenderModal/WebRenderModalAudio.tsx
import { getSupportedAudioCodecsForContainer as getSupportedAudioCodecsForContainer2 } from "@remotion/web-renderer";
import { useMemo as useMemo116 } from "react";

// src/components/RenderModal/quality-options.tsx
import { jsx as jsx209 } from "react/jsx-runtime";
var QUALITY_OPTIONS = [
  { value: "very-low", label: "Very Low" },
  { value: "low", label: "Low" },
  { value: "medium", label: "Medium" },
  { value: "high", label: "High" },
  { value: "very-high", label: "Very High" }
];
var getQualityOptions = (selectedQuality, setQuality) => {
  return QUALITY_OPTIONS.map(({ value, label: label14 }) => ({
    label: label14,
    onClick: () => setQuality(value),
    leftItem: selectedQuality === value ? /* @__PURE__ */ jsx209(Checkmark, {}) : null,
    id: value,
    keyHint: null,
    quickSwitcherLabel: null,
    subMenu: null,
    type: "item",
    value
  }));
};

// src/components/RenderModal/WebRenderModalAudio.tsx
import { jsx as jsx211, jsxs as jsxs127, Fragment as Fragment55 } from "react/jsx-runtime";
var container44 = {
  flex: 1,
  overflowY: "auto"
};
var fallbackNoticeStyle = {
  backgroundColor: INFO_BLUE_BACKGROUND,
  border: BORDER_INFO_BLUE,
  borderRadius: 4,
  padding: "8px 12px",
  marginLeft: 16,
  marginRight: 16,
  marginTop: 8,
  fontSize: 13,
  lineHeight: 1.4,
  color: INFO_BLUE
};
var humanReadableWebAudioCodec = (audioCodec) => {
  switch (audioCodec) {
    case "aac":
      return "AAC";
    case "opus":
      return "Opus";
    case "mp3":
      return "MP3";
    case "vorbis":
      return "Vorbis";
    case "pcm-s16":
      return "Lossless (PCM)";
    case "flac":
      return "FLAC";
    default:
      throw new Error(`Unsupported audio codec: ${audioCodec}`);
  }
};
var WebRenderModalAudio = ({
  renderMode,
  muted,
  setMuted,
  audioCodec,
  setAudioCodec,
  audioBitrate,
  setAudioBitrate,
  container: videoContainer,
  encodableCodecs,
  effectiveAudioCodec
}) => {
  const containerSupported = useMemo116(() => getSupportedAudioCodecsForContainer2(videoContainer), [videoContainer]);
  const audioCodecOptions = useMemo116(() => {
    return containerSupported.map((codec) => {
      const isEncodable = encodableCodecs.includes(codec);
      return {
        label: humanReadableWebAudioCodec(codec),
        onClick: () => setAudioCodec(codec),
        leftItem: audioCodec === codec ? /* @__PURE__ */ jsx211(Checkmark, {}) : null,
        id: codec,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: codec,
        disabled: !isEncodable
      };
    });
  }, [containerSupported, encodableCodecs, audioCodec, setAudioCodec]);
  const audioBitrateOptions = useMemo116(() => getQualityOptions(audioBitrate, setAudioBitrate), [audioBitrate, setAudioBitrate]);
  const isAudioOnly = renderMode === "audio";
  const showAudioSettings = isAudioOnly || !muted;
  const showAudioCodecSetting = !isAudioOnly || containerSupported.length > 1;
  return /* @__PURE__ */ jsxs127("div", {
    style: container44,
    className: VERTICAL_SCROLLBAR_CLASSNAME,
    children: [
      isAudioOnly ? null : /* @__PURE__ */ jsx211(MutedSetting, {
        enforceAudioTrack: false,
        muted,
        setMuted
      }),
      showAudioSettings ? /* @__PURE__ */ jsxs127(Fragment55, {
        children: [
          isAudioOnly ? null : /* @__PURE__ */ jsx211(RenderModalHr, {}),
          /* @__PURE__ */ jsxs127("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs127("div", {
                style: label,
                children: [
                  "Audio Quality",
                  /* @__PURE__ */ jsx211(Spacing, {
                    x: 0.5
                  })
                ]
              }),
              /* @__PURE__ */ jsx211("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx211(Combobox, {
                  values: audioBitrateOptions,
                  selectedId: audioBitrate,
                  title: "Audio Quality"
                })
              })
            ]
          }),
          showAudioCodecSetting ? /* @__PURE__ */ jsxs127("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs127("div", {
                style: label,
                children: [
                  "Audio Codec",
                  /* @__PURE__ */ jsx211(Spacing, {
                    x: 0.5
                  })
                ]
              }),
              /* @__PURE__ */ jsx211("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx211(Combobox, {
                  values: audioCodecOptions,
                  selectedId: audioCodec,
                  title: "Audio Codec"
                })
              })
            ]
          }) : null,
          showAudioCodecSetting && effectiveAudioCodec !== audioCodec ? /* @__PURE__ */ jsxs127("div", {
            style: fallbackNoticeStyle,
            children: [
              humanReadableWebAudioCodec(audioCodec),
              " is not available in this browser. Using ",
              humanReadableWebAudioCodec(effectiveAudioCodec),
              " ",
              "instead."
            ]
          }) : null
        ]
      }) : null
    ]
  });
};

// src/components/RenderModal/WebRenderModalBasic.tsx
import { useMemo as useMemo117 } from "react";
import { jsx as jsx213, jsxs as jsxs128, Fragment as Fragment56 } from "react/jsx-runtime";
var tabContainer2 = {
  flex: 1
};
var containerLabels = {
  mp4: "MP4",
  webm: "WebM",
  mkv: "MKV",
  mov: "MOV",
  wav: "WAV",
  mp3: "MP3",
  aac: "AAC",
  ogg: "OGG",
  flac: "FLAC"
};
var videoContainers = ["mp4", "webm", "mkv", "mov"];
var audioContainers = [
  "wav",
  "mp3",
  "aac",
  "ogg",
  "flac"
];
var codecLabels = {
  h264: "H.264",
  h265: "H.265",
  vp8: "VP8",
  vp9: "VP9",
  av1: "AV1"
};
var WebRenderModalBasic = ({
  renderMode,
  resolvedComposition,
  imageFormat,
  setStillFormat,
  frame: frame2,
  onFrameChanged,
  onFrameSetDirectly,
  container: container45,
  setContainerFormat,
  setCodec,
  encodableVideoCodecs,
  effectiveVideoCodec,
  startFrame,
  setStartFrame,
  endFrame,
  setEndFrame,
  outName,
  onOutNameChange,
  validationMessage,
  logLevel,
  setLogLevel
}) => {
  const imageFormatOptions = useMemo117(() => {
    return [
      {
        label: "PNG",
        onClick: () => setStillFormat("png"),
        key: "png",
        selected: imageFormat === "png"
      },
      {
        label: "JPEG",
        onClick: () => setStillFormat("jpeg"),
        key: "jpeg",
        selected: imageFormat === "jpeg"
      },
      {
        label: "WebP",
        onClick: () => setStillFormat("webp"),
        key: "webp",
        selected: imageFormat === "webp"
      }
    ];
  }, [imageFormat, setStillFormat]);
  const logLevelOptions = useMemo117(() => {
    return ["trace", "verbose", "info", "warn", "error"].map((level) => {
      return {
        label: humanReadableLogLevel(level),
        onClick: () => setLogLevel(level),
        leftItem: logLevel === level ? /* @__PURE__ */ jsx213(Checkmark, {}) : null,
        id: level,
        keyHint: null,
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: level
      };
    });
  }, [logLevel, setLogLevel]);
  const containerOptions = useMemo117(() => {
    const containers = renderMode === "audio" ? audioContainers : videoContainers;
    return containers.map((c) => ({
      label: containerLabels[c],
      onClick: () => setContainerFormat(c),
      leftItem: container45 === c ? /* @__PURE__ */ jsx213(Checkmark, {}) : null,
      id: c,
      keyHint: null,
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: c
    }));
  }, [container45, setContainerFormat, renderMode]);
  const codecOptions = useMemo117(() => {
    return encodableVideoCodecs.map((c) => ({
      label: codecLabels[c],
      onClick: () => setCodec(c),
      leftItem: effectiveVideoCodec === c ? /* @__PURE__ */ jsx213(Checkmark, {}) : null,
      id: c,
      keyHint: null,
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: c
    }));
  }, [encodableVideoCodecs, effectiveVideoCodec, setCodec]);
  return /* @__PURE__ */ jsxs128("div", {
    style: tabContainer2,
    children: [
      renderMode === "still" ? /* @__PURE__ */ jsxs128(Fragment56, {
        children: [
          /* @__PURE__ */ jsxs128("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx213("div", {
                style: label,
                children: "Format"
              }),
              /* @__PURE__ */ jsx213("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx213(SegmentedControl, {
                  items: imageFormatOptions,
                  needsWrapping: true
                })
              })
            ]
          }),
          resolvedComposition.durationInFrames > 1 ? /* @__PURE__ */ jsxs128("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx213("div", {
                style: label,
                children: "Frame"
              }),
              /* @__PURE__ */ jsx213("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx213(RightAlignInput, {
                  children: /* @__PURE__ */ jsx213(InputDragger, {
                    value: frame2,
                    onTextChange: onFrameChanged,
                    placeholder: `0-${resolvedComposition.durationInFrames - 1}`,
                    onValueChange: onFrameSetDirectly,
                    name: "frame",
                    step: 1,
                    min: 0,
                    status: "ok",
                    max: resolvedComposition.durationInFrames - 1,
                    rightAlign: true
                  })
                })
              })
            ]
          }) : null
        ]
      }) : /* @__PURE__ */ jsxs128(Fragment56, {
        children: [
          /* @__PURE__ */ jsxs128("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx213("div", {
                style: label,
                children: "Container"
              }),
              /* @__PURE__ */ jsx213("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx213(Combobox, {
                  values: containerOptions,
                  selectedId: container45,
                  title: "Container"
                })
              })
            ]
          }),
          renderMode === "video" ? /* @__PURE__ */ jsxs128("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsxs128("div", {
                style: label,
                children: [
                  "Codec",
                  /* @__PURE__ */ jsx213(Spacing, {
                    x: 0.5
                  }),
                  /* @__PURE__ */ jsx213(WebRendererOptionExplainerBubble, {
                    apiName: "codec",
                    id: "videoCodecOption"
                  })
                ]
              }),
              /* @__PURE__ */ jsx213("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx213(Combobox, {
                  values: codecOptions,
                  selectedId: effectiveVideoCodec,
                  title: "Codec"
                })
              })
            ]
          }) : null,
          /* @__PURE__ */ jsx213(FrameRangeSetting, {
            durationInFrames: resolvedComposition.durationInFrames,
            startFrame: startFrame ?? 0,
            endFrame: endFrame ?? resolvedComposition.durationInFrames - 1,
            setStartFrame,
            setEndFrame
          })
        ]
      }),
      /* @__PURE__ */ jsx213(RenderModalOutputName, {
        ariaLabel: window.remotion_isReadOnlyStudio ? "Download name" : "Output name",
        existingOutputPath: null,
        existence: false,
        inputStyle: input,
        outName,
        onValueChange: onOutNameChange,
        validationMessage,
        label: window.remotion_isReadOnlyStudio ? "Download name" : "Output name"
      }),
      /* @__PURE__ */ jsxs128("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs128("div", {
            style: label,
            children: [
              "Log Level ",
              /* @__PURE__ */ jsx213(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx213(WebRendererOptionExplainerBubble, {
                apiName: "logLevel",
                id: "logLevelOption"
              })
            ]
          }),
          /* @__PURE__ */ jsx213("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx213(Combobox, {
              values: logLevelOptions,
              selectedId: logLevel,
              title: "Log Level"
            })
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/WebRenderModalPicture.tsx
import { useCallback as useCallback112, useMemo as useMemo118 } from "react";
import { jsx as jsx214, jsxs as jsxs129, Fragment as Fragment57 } from "react/jsx-runtime";
var tabContainer3 = {
  flex: 1
};
var WebRenderModalPicture = ({
  renderMode,
  videoBitrate,
  setVideoBitrate,
  keyframeIntervalInSeconds,
  setKeyframeIntervalInSeconds,
  transparent,
  setTransparent,
  scale,
  setScale,
  compositionWidth,
  compositionHeight
}) => {
  const qualityOptions = useMemo118(() => getQualityOptions(videoBitrate, setVideoBitrate), [videoBitrate, setVideoBitrate]);
  const onTransparentChanged = useCallback112((e) => {
    setTransparent(e.target.checked);
  }, [setTransparent]);
  return /* @__PURE__ */ jsxs129("div", {
    style: tabContainer3,
    children: [
      /* @__PURE__ */ jsx214(ScaleSetting, {
        scale,
        setScale,
        compositionWidth,
        compositionHeight
      }),
      renderMode !== "video" ? null : /* @__PURE__ */ jsxs129(Fragment57, {
        children: [
          /* @__PURE__ */ jsxs129("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx214("div", {
                style: label,
                children: "Quality"
              }),
              /* @__PURE__ */ jsx214("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx214(Combobox, {
                  values: qualityOptions,
                  selectedId: videoBitrate,
                  title: "Quality"
                })
              })
            ]
          }),
          /* @__PURE__ */ jsx214(NumberSetting, {
            name: "Keyframe Interval",
            formatter: (v) => `${v}s`,
            min: 1,
            max: 300,
            step: 1,
            value: keyframeIntervalInSeconds,
            onValueChanged: setKeyframeIntervalInSeconds
          }),
          /* @__PURE__ */ jsxs129("div", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx214("div", {
                style: label,
                children: "Transparent"
              }),
              /* @__PURE__ */ jsx214("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx214(Checkbox, {
                  checked: transparent,
                  onChange: onTransparentChanged,
                  name: TRANSPARENT
                })
              })
            ]
          })
        ]
      })
    ]
  });
};

// src/components/RenderModal/WebRenderModal.tsx
import { jsx as jsx215, jsxs as jsxs130 } from "react/jsx-runtime";
var invalidCharacters2 = ["?", "*", "+", ":", "%"];
var isValidStillExtension2 = (extension, stillImageFormat) => {
  if (stillImageFormat === "jpeg" && extension === "jpg") {
    return true;
  }
  return extension === stillImageFormat;
};
var validateOutnameForStill = ({
  outName,
  stillImageFormat
}) => {
  try {
    const extension = outName.substring(outName.lastIndexOf(".") + 1);
    const prefix = outName.substring(0, outName.lastIndexOf("."));
    const hasDotAfterSlash = () => {
      const substrings = prefix.split("/");
      for (const str of substrings) {
        if (str[0] === ".") {
          return true;
        }
      }
      return false;
    };
    const hasInvalidChar = () => {
      return prefix.split("").some((char) => invalidCharacters2.includes(char));
    };
    if (prefix.length < 1) {
      throw new Error("The prefix must be at least 1 character long");
    }
    if (prefix[0] === "." || hasDotAfterSlash()) {
      throw new Error("The output name must not start with a dot");
    }
    if (hasInvalidChar()) {
      throw new Error("Filename can't contain the following characters:  ?, *, +, %, :");
    }
    if (!isValidStillExtension2(extension, stillImageFormat)) {
      throw new Error(`The extension ${extension} is not supported for still image format ${stillImageFormat}`);
    }
    return { valid: true };
  } catch (err) {
    return { valid: false, error: err };
  }
};
var WebRenderModal = ({
  initialFrame,
  defaultProps,
  inFrameMark,
  outFrameMark,
  initialLogLevel,
  initialStillImageFormat,
  initialDefaultOutName,
  initialScale,
  initialDelayRenderTimeout,
  initialMediaCacheSizeInBytes,
  initialContainer,
  initialVideoCodec,
  initialAudioCodec,
  initialAudioBitrate,
  initialVideoBitrate,
  initialHardwareAcceleration,
  initialKeyframeIntervalInSeconds,
  initialTransparent,
  initialMuted,
  initialAllowHtmlInCanvas,
  initialPageResponsiveness
}) => {
  const context = useContext117(ResolvedCompositionContext);
  const { setSelectedModal } = useContext117(SetSelectedModalContext);
  const { setSidebarCollapsedState } = useContext117(SidebarContext);
  const { addClientStillJob, addClientVideoJob } = useContext117(RenderQueueContext);
  const { previewServerState } = useContext117(StudioServerConnectionCtx);
  if (!context) {
    throw new Error("Should not be able to render without resolving comp first");
  }
  const renderDefaults = window.remotion_renderDefaults;
  if (!renderDefaults) {
    throw new Error("Render defaults are not available");
  }
  const { publicLicenseKey } = renderDefaults;
  const {
    resolved: { result: resolvedComposition },
    unresolved: unresolvedComposition
  } = context;
  const [isVideo] = useState78(() => {
    return typeof resolvedComposition.durationInFrames === "undefined" ? true : resolvedComposition.durationInFrames > 1;
  });
  const [renderMode, setRenderMode] = useState78(initialContainer && isAudioOnlyContainer(initialContainer) ? "audio" : isVideo ? "video" : "still");
  const [tab, setTab] = useState78("general");
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const canEditConfig = canEditStudioConfig({
    isBrowserStudio,
    previewServerConnected: previewServerState.type === "connected",
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  const [imageFormat, setImageFormat] = useState78(() => initialStillImageFormat ?? "png");
  const [frame2, setFrame] = useState78(() => initialFrame);
  const [logLevel, setLogLevel] = useState78(() => initialLogLevel);
  const [inputProps, _setInputProps] = useState78(() => defaultProps);
  const setInputProps = useCallback113((updater) => {
    _setInputProps(updater);
  }, []);
  const [delayRenderTimeout, setDelayRenderTimeout] = useState78(initialDelayRenderTimeout ?? 30000);
  const [mediaCacheSizeInBytes, setMediaCacheSizeInBytes] = useState78(initialMediaCacheSizeInBytes);
  const [codec, setCodec] = useState78(initialVideoCodec ?? "h264");
  const [container45, setContainer] = useState78(initialContainer ?? "mp4");
  const [audioCodec, setAudioCodec] = useState78(initialAudioCodec ?? "aac");
  const [audioBitrate, setAudioBitrate] = useState78(initialAudioBitrate ?? "medium");
  const [videoBitrate, setVideoBitrate] = useState78(initialVideoBitrate ?? "high");
  const [hardwareAcceleration, setHardwareAcceleration] = useState78(initialHardwareAcceleration ?? "no-preference");
  const [keyframeIntervalInSeconds, setKeyframeIntervalInSeconds] = useState78(initialKeyframeIntervalInSeconds ?? 5);
  const [startFrame, setStartFrame] = useState78(() => inFrameMark);
  const [endFrame, setEndFrame] = useState78(() => outFrameMark);
  const [transparent, setTransparent] = useState78(initialTransparent ?? false);
  const [muted, setMuted] = useState78(initialMuted ?? false);
  const [scale, setScale] = useState78(initialScale ?? 1);
  const [allowHtmlInCanvas, setAllowHtmlInCanvas] = useState78(initialAllowHtmlInCanvas ?? false);
  const [pageResponsiveness, setPageResponsiveness] = useState78(initialPageResponsiveness ?? "medium");
  const encodableAudioCodecs = useEncodableAudioCodecs(container45);
  const encodableVideoCodecs = useEncodableVideoCodecs(container45);
  const effectiveAudioCodec = useMemo119(() => {
    if (encodableAudioCodecs.includes(audioCodec)) {
      return audioCodec;
    }
    return encodableAudioCodecs[0] ?? audioCodec;
  }, [audioCodec, encodableAudioCodecs]);
  const effectiveVideoCodec = useMemo119(() => {
    if (encodableVideoCodecs.includes(codec)) {
      return codec;
    }
    return encodableVideoCodecs[0] ?? codec;
  }, [codec, encodableVideoCodecs]);
  const finalEndFrame = useMemo119(() => {
    if (endFrame === null) {
      return resolvedComposition.durationInFrames - 1;
    }
    return Math.max(0, Math.min(resolvedComposition.durationInFrames - 1, endFrame));
  }, [endFrame, resolvedComposition.durationInFrames]);
  const finalStartFrame = useMemo119(() => {
    if (startFrame === null) {
      return 0;
    }
    return Math.max(0, Math.min(finalEndFrame, startFrame));
  }, [finalEndFrame, startFrame]);
  const [initialOutNameState] = useState78(() => {
    if (initialDefaultOutName) {
      return initialDefaultOutName;
    }
    const defaultOut = getDefaultOutLocation2({
      compositionName: resolvedComposition.id,
      defaultExtension: renderMode === "still" ? imageFormat : isVideo ? container45 : imageFormat,
      type: "asset",
      compositionDefaultOutName: resolvedComposition.defaultOutName,
      outputLocation: window.remotion_renderDefaults?.outputLocation ?? null
    });
    if (window.remotion_isReadOnlyStudio) {
      return defaultOut.replace(/^out\//, "");
    }
    return defaultOut;
  });
  const [outName, setOutName] = useState78(() => initialOutNameState);
  const updateOutNameExtension = useCallback113((extension) => {
    setOutName((prev) => getStringBeforeSuffix(prev) + "." + extension);
  }, []);
  const setStillFormat = useCallback113((format) => {
    setImageFormat(format);
    updateOutNameExtension(format);
  }, [updateOutNameExtension]);
  const setContainerFormat = useCallback113((newContainer) => {
    setContainer(newContainer);
    const defaultVideoCodec = getDefaultVideoCodecForContainer(newContainer);
    if (defaultVideoCodec) {
      setCodec(defaultVideoCodec);
    }
    setAudioCodec(getDefaultAudioCodecForContainer(newContainer));
    updateOutNameExtension(newContainer);
  }, [updateOutNameExtension]);
  const setCodecWithContainer = useCallback113((newCodec) => {
    setCodec(newCodec);
    const newContainer = getDefaultContainerForCodec(newCodec);
    setContainer(newContainer);
    setAudioCodec(getDefaultAudioCodecForContainer(newContainer));
    updateOutNameExtension(newContainer);
  }, [updateOutNameExtension]);
  const onRenderModeChange = useCallback113((newMode) => {
    setRenderMode(newMode);
    if (newMode === "video") {
      const newContainer = isAudioOnlyContainer(container45) ? "mp4" : container45;
      setContainer(newContainer);
      setAudioCodec(getDefaultAudioCodecForContainer(newContainer));
      updateOutNameExtension(newContainer);
    } else if (newMode === "audio") {
      const newContainer = "wav";
      setContainer(newContainer);
      setMuted(false);
      setAudioCodec(getDefaultAudioCodecForContainer(newContainer));
      updateOutNameExtension(newContainer);
      setTab((prev) => prev === "picture" ? "general" : prev);
    } else if (newMode === "still") {
      updateOutNameExtension(imageFormat);
      setTab((prev) => prev === "audio" ? "general" : prev);
    }
  }, [container45, imageFormat, updateOutNameExtension]);
  const renderTabOptions = useMemo119(() => {
    const options = [
      {
        label: "Still",
        onClick: () => {
          onRenderModeChange("still");
        },
        key: "still",
        selected: renderMode === "still"
      }
    ];
    if (resolvedComposition.durationInFrames > 1) {
      options.push({
        label: "Video",
        onClick: () => {
          onRenderModeChange("video");
        },
        key: "video",
        selected: renderMode === "video"
      });
      options.push({
        label: "Audio",
        onClick: () => {
          onRenderModeChange("audio");
        },
        key: "audio",
        selected: renderMode === "audio"
      });
    }
    return options;
  }, [renderMode, resolvedComposition.durationInFrames, onRenderModeChange]);
  const onFrameSetDirectly = useCallback113((newFrame) => {
    setFrame(newFrame);
  }, [setFrame]);
  const onFrameChanged = useCallback113((e) => {
    setFrame((q) => {
      const newFrame = parseFloat(e);
      if (Number.isNaN(newFrame)) {
        return q;
      }
      return newFrame;
    });
  }, [setFrame]);
  const onOutNameChange = useCallback113((e) => {
    setOutName(e.target.value);
  }, []);
  const outnameValidation = useMemo119(() => {
    if (renderMode === "still") {
      return validateOutnameForStill({
        outName,
        stillImageFormat: imageFormat
      });
    }
    try {
      const extension = outName.substring(outName.lastIndexOf(".") + 1);
      const prefix = outName.substring(0, outName.lastIndexOf("."));
      const hasDotAfterSlash = () => {
        const substrings = prefix.split("/");
        for (const str of substrings) {
          if (str[0] === ".") {
            return true;
          }
        }
        return false;
      };
      const hasInvalidChar = () => {
        return prefix.split("").some((char) => invalidCharacters2.includes(char));
      };
      if (prefix.length < 1) {
        throw new Error("The prefix must be at least 1 character long");
      }
      if (prefix[0] === "." || hasDotAfterSlash()) {
        throw new Error("The output name must not start with a dot");
      }
      if (hasInvalidChar()) {
        throw new Error("Filename can't contain the following characters:  ?, *, +, %, :");
      }
      if (extension !== container45) {
        throw new Error(`The extension ${extension} is not supported for container format ${container45}`);
      }
      return { valid: true };
    } catch (err) {
      return { valid: false, error: err };
    }
  }, [outName, imageFormat, renderMode, container45]);
  const onAddToQueue = useCallback113(() => {
    const compositionRef = {
      component: unresolvedComposition.component,
      calculateMetadata: unresolvedComposition.calculateMetadata ?? null,
      width: resolvedComposition.width,
      height: resolvedComposition.height,
      fps: resolvedComposition.fps,
      durationInFrames: resolvedComposition.durationInFrames,
      defaultProps: resolvedComposition.defaultProps
    };
    if (renderMode === "still") {
      addClientStillJob({
        type: "client-still",
        compositionId: resolvedComposition.id,
        outName,
        imageFormat,
        frame: frame2,
        inputProps,
        delayRenderTimeout,
        mediaCacheSizeInBytes,
        logLevel,
        licenseKey: publicLicenseKey,
        scale,
        allowHtmlInCanvas
      }, compositionRef);
    } else {
      addClientVideoJob({
        type: "client-video",
        compositionId: resolvedComposition.id,
        outName,
        container: container45,
        videoCodec: isAudioOnlyContainer(container45) ? null : effectiveVideoCodec,
        audioCodec: effectiveAudioCodec,
        startFrame: finalStartFrame,
        endFrame: finalEndFrame,
        audioBitrate,
        videoBitrate,
        hardwareAcceleration,
        keyframeIntervalInSeconds,
        transparent,
        muted,
        inputProps,
        delayRenderTimeout,
        mediaCacheSizeInBytes,
        logLevel,
        licenseKey: publicLicenseKey,
        scale,
        allowHtmlInCanvas,
        pageResponsiveness
      }, compositionRef);
    }
    setSidebarCollapsedState({ left: null, right: "expanded" });
    persistSelectedOptionsSidebarPanel("renders");
    optionsSidebarTabs.current?.selectRendersPanel();
    setSelectedModal(null);
  }, [
    renderMode,
    unresolvedComposition.component,
    unresolvedComposition.calculateMetadata,
    resolvedComposition.width,
    resolvedComposition.height,
    resolvedComposition.fps,
    resolvedComposition.durationInFrames,
    resolvedComposition.defaultProps,
    resolvedComposition.id,
    setSidebarCollapsedState,
    outName,
    imageFormat,
    frame2,
    inputProps,
    delayRenderTimeout,
    mediaCacheSizeInBytes,
    logLevel,
    publicLicenseKey,
    container45,
    effectiveVideoCodec,
    effectiveAudioCodec,
    finalStartFrame,
    finalEndFrame,
    audioBitrate,
    videoBitrate,
    hardwareAcceleration,
    keyframeIntervalInSeconds,
    transparent,
    muted,
    setSelectedModal,
    addClientStillJob,
    addClientVideoJob,
    scale,
    allowHtmlInCanvas,
    pageResponsiveness
  ]);
  return /* @__PURE__ */ jsxs130("div", {
    style: outerModalStyle,
    children: [
      /* @__PURE__ */ jsx215(ModalHeader, {
        title: `Render ${resolvedComposition.id} in the browser`
      }),
      /* @__PURE__ */ jsxs130("div", {
        style: container,
        children: [
          /* @__PURE__ */ jsx215(SegmentedControl, {
            items: renderTabOptions,
            needsWrapping: false
          }),
          /* @__PURE__ */ jsx215("div", {
            style: flexer
          }),
          /* @__PURE__ */ jsxs130(Button, {
            autoFocus: true,
            onClick: onAddToQueue,
            style: buttonStyle,
            disabled: !outnameValidation.valid,
            children: [
              "Render ",
              renderMode,
              /* @__PURE__ */ jsx215(ShortcutHint, {
                keyToPress: "↵",
                cmdOrCtrl: true
              })
            ]
          })
        ]
      }),
      /* @__PURE__ */ jsxs130("div", {
        style: horizontalLayout,
        children: [
          /* @__PURE__ */ jsxs130("div", {
            style: leftSidebar,
            children: [
              /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: tab === "general",
                onClick: () => setTab("general"),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(FileIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "General"
              }),
              /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: tab === "data",
                onClick: () => setTab("data"),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(InputPropsIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Input Props"
              }),
              renderMode !== "audio" ? /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: tab === "picture",
                onClick: () => setTab("picture"),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(PicIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Picture"
              }) : null,
              renderMode === "video" || renderMode === "audio" ? /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: tab === "audio",
                onClick: () => setTab("audio"),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(AudioIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Audio"
              }) : null,
              /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: tab === "advanced",
                onClick: () => setTab("advanced"),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(GearIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "Other"
              }),
              canEditConfig ? /* @__PURE__ */ jsx215(VerticalTab, {
                style: horizontalTab,
                selected: false,
                onClick: () => setSelectedModal({
                  type: "settings",
                  initialTab: "license",
                  initialPublicLicenseKey: publicLicenseKey
                }),
                renderIcon: (color) => /* @__PURE__ */ jsx215("div", {
                  style: iconContainer,
                  children: /* @__PURE__ */ jsx215(CertificateIcon, {
                    color,
                    style: icon
                  })
                }),
                children: "License"
              }) : null
            ]
          }),
          /* @__PURE__ */ jsx215("div", {
            style: optionsPanel,
            className: VERTICAL_SCROLLBAR_CLASSNAME,
            children: tab === "general" ? /* @__PURE__ */ jsx215(WebRenderModalBasic, {
              renderMode,
              resolvedComposition,
              imageFormat,
              setStillFormat,
              frame: frame2,
              onFrameChanged,
              onFrameSetDirectly,
              container: container45,
              setContainerFormat,
              setCodec: setCodecWithContainer,
              encodableVideoCodecs,
              effectiveVideoCodec,
              startFrame: finalStartFrame,
              setStartFrame,
              endFrame: finalEndFrame,
              setEndFrame,
              outName,
              onOutNameChange,
              validationMessage: outnameValidation.valid ? null : outnameValidation.error.message,
              logLevel,
              setLogLevel
            }) : tab === "data" ? /* @__PURE__ */ jsx215(DataEditor, {
              defaultProps: inputProps,
              setDefaultProps: setInputProps,
              unresolvedComposition,
              propsEditType: "input-props",
              canSaveDefaultProps: {
                canUpdate: false,
                reason: "render dialogue",
                determined: false
              }
            }) : tab === "picture" ? /* @__PURE__ */ jsx215(WebRenderModalPicture, {
              renderMode,
              videoBitrate,
              setVideoBitrate,
              keyframeIntervalInSeconds,
              setKeyframeIntervalInSeconds,
              transparent,
              setTransparent,
              scale,
              setScale,
              compositionWidth: resolvedComposition.width,
              compositionHeight: resolvedComposition.height
            }) : tab === "audio" ? /* @__PURE__ */ jsx215(WebRenderModalAudio, {
              renderMode,
              muted,
              setMuted,
              audioCodec,
              setAudioCodec,
              audioBitrate,
              setAudioBitrate,
              container: container45,
              encodableCodecs: encodableAudioCodecs,
              effectiveAudioCodec
            }) : tab === "advanced" ? /* @__PURE__ */ jsx215(WebRenderModalAdvanced, {
              renderMode,
              delayRenderTimeout,
              setDelayRenderTimeout,
              mediaCacheSizeInBytes,
              setMediaCacheSizeInBytes,
              hardwareAcceleration,
              setHardwareAcceleration,
              allowHtmlInCanvas,
              setAllowHtmlInCanvas,
              pageResponsiveness,
              setPageResponsiveness
            }) : null
          })
        ]
      })
    ]
  });
};
var WebRenderModalWithLoader = (props) => {
  return /* @__PURE__ */ jsx215(DismissableModal, {
    children: /* @__PURE__ */ jsx215(ResolveCompositionBeforeModal, {
      compositionId: props.compositionId,
      children: /* @__PURE__ */ jsx215(WebRenderModal, {
        ...props
      })
    })
  });
};

// src/components/SettingsModal.tsx
import {
  useCallback as useCallback123,
  useContext as useContext123,
  useEffect as useEffect87,
  useLayoutEffect as useLayoutEffect28,
  useMemo as useMemo129,
  useState as useState88
} from "react";

// src/icons/keyboard.tsx
import { jsx as jsx216 } from "react/jsx-runtime";
var KeyboardIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx216("svg", {
    ...props,
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512",
    children: /* @__PURE__ */ jsx216("path", {
      fill: color,
      d: "M96 192l64 0 0 128-64 0c-53 0-96 43-96 96s43 96 96 96 96-43 96-96l0-64 128 0 0 64c0 53 43 96 96 96s96-43 96-96-43-96-96-96l-64 0 0-128 64 0c53 0 96-43 96-96s-43-96-96-96-96 43-96 96l0 64-128 0 0-64c0-53-43-96-96-96S0 43 0 96 43 192 96 192zM256 320l-64 0 0-128 128 0 0 128-64 0zM96 160c-35.3 0-64-28.7-64-64s28.7-64 64-64 64 28.7 64 64l0 64-64 0zm64 256c0 35.3-28.7 64-64 64s-64-28.7-64-64 28.7-64 64-64l64 0 0 64zM352 160l0-64c0-35.3 28.7-64 64-64s64 28.7 64 64-28.7 64-64 64l-64 0zm64 192c35.3 0 64 28.7 64 64s-28.7 64-64 64-64-28.7-64-64l0-64 64 0z"
    })
  });
};

// src/icons/license.tsx
import { jsx as jsx217 } from "react/jsx-runtime";
var LicenseIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx217("svg", {
    ...props,
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 -40 512 593",
    children: /* @__PURE__ */ jsx217("path", {
      fill: color,
      d: "M309.4-7.4L256-40 202.6-7.4 140-8.9 110 46 55.1 76 56.6 138.6 24 192 56.6 245.4 55.1 308 110 338 128 371 128 552.3c1-.4 43.6-18.7 128-54.9 84.4 36.2 127 54.4 128 54.9l0-181.3 18-33 54.9-30-1.5-62.6 32.6-53.4-32.6-53.4 1.5-62.6-54.9-30-30-54.9-62.6 1.5zM160 503.7l0-111.3 42.6-1 53.4 32.6 53.4-32.6 42.6 1 0 111.3-89.7-38.4-6.3-2.7-6.3 2.7-89.7 38.4zM353.3 23.5l20.7 37.8 4.5 8.2c.6 .3 15.9 8.7 46 25.2-.8 34.3-1.2 51.8-1.2 52.5 .3 .5 9.4 15.5 27.3 44.8-17.9 29.3-27 44.3-27.3 44.8 0 .6 .4 18.1 1.2 52.5l-37.8 20.7-8.2 4.5-4.5 8.2-20.7 37.8c-34.3-.8-51.8-1.2-52.5-1.2-.5 .3-15.5 9.4-44.8 27.3-29.3-17.9-44.3-27-44.8-27.3-.6 0-18.1 .4-52.5 1.2-16.5-30.1-24.9-45.5-25.2-46l-8.2-4.5-37.8-20.7c.8-34.3 1.2-51.8 1.2-52.5-.3-.5-9.4-15.5-27.3-44.8 17.9-29.3 27-44.3 27.3-44.8 0-.6-.4-18.1-1.2-52.5 30.1-16.5 45.5-24.9 46-25.2 .3-.6 8.7-15.9 25.2-46 34.3 .8 51.8 1.2 52.5 1.2 .5-.3 15.5-9.4 44.8-27.3 29.3 17.9 44.3 27 44.8 27.3 .6 0 18.1-.4 52.5-1.2zM336 192a80 80 0 1 1 -160 0 80 80 0 1 1 160 0zM256 80a112 112 0 1 0 0 224 112 112 0 1 0 0-224z"
    })
  });
};

// src/icons/package.tsx
import { jsx as jsx218 } from "react/jsx-runtime";
var PackageIcon = ({ color, ...props }) => {
  return /* @__PURE__ */ jsx218("svg", {
    ...props,
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 -16 512 544",
    children: /* @__PURE__ */ jsx218("path", {
      fill: color,
      d: "M240.3 25.2L64.5 126.8 256.3 237.5 448.2 126.8 272.3 25.2c-9.9-5.7-22.1-5.7-32 0zM48.5 154.5l0 203c0 11.4 6.1 22 16 27.7l175.8 101.5 0-221.5-191.8-110.8zM272.3 486.8L448.2 385.2c9.9-5.7 16-16.3 16-27.7l0-203-191.8 110.8 0 221.5zM224.3-2.5c19.8-11.4 44.2-11.4 64 0L464.2 99c19.8 11.4 32 32.6 32 55.4l0 203c0 22.9-12.2 44-32 55.4L288.3 514.5c-19.8 11.4-44.2 11.4-64 0L48.5 413c-19.8-11.4-32-32.6-32-55.4l0-203c0-22.9 12.2-44 32-55.4L224.3-2.5z"
    })
  });
};

// src/icons/remotion-triangle.tsx
import { jsx as jsx219 } from "react/jsx-runtime";
var RemotionTriangleIcon = ({
  color,
  ...props
}) => /* @__PURE__ */ jsx219("svg", {
  viewBox: "-14 -14 438 453",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  ...props,
  children: /* @__PURE__ */ jsx219("path", {
    d: "M92.3905 0.603638C83.5124 1.08554 76.358 2.49417 69.1481 5.2373C65.5523 6.59033 59.6583 9.53735 56.4148 11.5762C42.8474 20.0836 32.5051 32.8724 27.1115 47.7558C26.0365 50.7028 23.1266 60.0998 21.2546 66.5684C8.78078 109.81 1.70055 157.092 0.180713 207.135C-0.0602375 215.105 -0.0602375 234.084 0.180713 241.925C1.20012 275.102 4.36954 305.053 10.1153 335.599C12.4506 347.961 16.1946 364.568 18.3632 372.112C22.793 387.44 31.8379 400.488 44.7009 410.052C53.3195 416.465 63.1058 420.765 73.9485 422.878C79.1753 423.897 86.0702 424.361 91.1487 424.027C98.1733 423.564 112.111 421.692 123.436 419.672C174.48 410.571 221.632 394.279 264.392 370.963C291.471 356.19 314.621 340.195 336.955 320.789C359.215 301.476 378.194 280.699 394.894 257.382C398.768 251.989 400.714 248.838 402.66 244.872C407.665 234.64 410.018 224.465 410 213.085C410 202.483 407.998 193.067 403.68 183.429C401.604 178.777 399.62 175.441 395.172 169.102C378.788 145.767 360.605 125.434 338.438 105.676C304.074 75.0573 263.261 49.9428 217.685 31.3526C207.806 27.3306 198.075 23.7905 186.361 19.9538C161.562 11.8542 130.868 4.82956 104.901 1.32651C100.824 0.770477 95.2819 0.455353 92.3905 0.603638Z",
    stroke: color ?? CURRENT_COLOR_LOWERCASE,
    strokeLinejoin: "round",
    strokeWidth: 24
  })
});

// src/components/ConfigureDefaultEditorModal.tsx
import { useEffect as useEffect80, useMemo as useMemo121, useState as useState79 } from "react";

// src/components/use-auto-save-config.ts
import { useContext as useContext118, useEffect as useEffect79, useMemo as useMemo120, useRef as useRef65 } from "react";
var useAutoSaveConfig = ({
  enabled,
  onError,
  ready,
  syncRevision,
  updates
}) => {
  const { previewServerState } = useContext118(StudioServerConnectionCtx);
  const serializedUpdates = useMemo120(() => JSON.stringify(updates), [updates]);
  const initialUpdates = useRef65(null);
  const lastQueuedUpdates = useRef65(null);
  const lastSyncRevision = useRef65(null);
  const syncGeneration = useRef65(0);
  const saveQueue = useRef65(Promise.resolve());
  const mounted = useRef65(true);
  useEffect79(() => {
    mounted.current = true;
    return () => {
      mounted.current = false;
    };
  }, []);
  useEffect79(() => {
    if (!ready) {
      return;
    }
    if (initialUpdates.current === null || lastSyncRevision.current !== syncRevision) {
      initialUpdates.current = serializedUpdates;
      lastQueuedUpdates.current = serializedUpdates;
      lastSyncRevision.current = syncRevision;
      syncGeneration.current++;
      return;
    }
    if (!enabled || previewServerState.type !== "connected" || lastQueuedUpdates.current === serializedUpdates) {
      return;
    }
    lastQueuedUpdates.current = serializedUpdates;
    onError(null);
    const generation = syncGeneration.current;
    saveQueue.current = saveQueue.current.then(async () => {
      if (generation !== syncGeneration.current) {
        return;
      }
      try {
        const response = await callApi("/api/update-config", {
          clientId: previewServerState.clientId,
          updates
        });
        if (!response.success && mounted.current && lastQueuedUpdates.current === serializedUpdates) {
          onError(response.reason);
        }
      } catch (err) {
        if (mounted.current && lastQueuedUpdates.current === serializedUpdates) {
          onError(err.message);
        }
      }
    });
  }, [
    enabled,
    onError,
    previewServerState,
    ready,
    serializedUpdates,
    syncRevision,
    updates
  ]);
};

// src/components/ConfigureDefaultEditorModal.tsx
import { jsx as jsx220, jsxs as jsxs131, Fragment as Fragment58 } from "react/jsx-runtime";
var container45 = {
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minWidth: 0
};
var content13 = {
  flex: 1
};
var description2 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5,
  margin: 0
};
var comboBoxStyle2 = {
  boxSizing: "border-box",
  width: 180
};
var rightDescription = {
  ...description2,
  textAlign: "right"
};
var rightColumn = {
  ...rightRow,
  alignItems: "flex-end",
  flexDirection: "column"
};
var appLabel = {
  display: "flex",
  alignItems: "center",
  gap: 8,
  maxWidth: "100%",
  minWidth: 0,
  color: "inherit",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "inherit"
};
var appName = {
  minWidth: 0,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap",
  color: "inherit",
  fontFamily: "inherit",
  fontSize: "inherit",
  lineHeight: "inherit"
};
var NO_PREFERENCE_ID = "no-preference";
var DefaultEditorSettings = () => {
  const {
    codingAgentInfo,
    editorInfo,
    error: settingsError,
    revision
  } = useSettings();
  const [selectedEditor, setSelectedEditor] = useState79(null);
  const [selectedCodingAgent, setSelectedCodingAgent] = useState79(null);
  const [syncedRevision, setSyncedRevision] = useState79(-1);
  const [error, setError] = useState79(null);
  const editorValues = useMemo121(() => {
    const noPreference = {
      id: NO_PREFERENCE_ID,
      keyHint: null,
      label: "No preference",
      leftItem: selectedEditor === null ? /* @__PURE__ */ jsx220(Checkmark, {}) : null,
      onClick: () => {
        setSelectedEditor(null);
        setError(null);
      },
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: NO_PREFERENCE_ID
    };
    const installedEditors = (editorInfo?.installedEditors ?? []).map((editor) => {
      return {
        id: editor.id,
        keyHint: null,
        label: /* @__PURE__ */ jsxs131("span", {
          style: appLabel,
          children: [
            /* @__PURE__ */ jsx220(EditorIcon, {
              editorId: editor.id,
              size: 18
            }),
            /* @__PURE__ */ jsx220("span", {
              style: appName,
              children: editor.name
            })
          ]
        }),
        leftItem: selectedEditor === editor.id ? /* @__PURE__ */ jsx220(Checkmark, {}) : null,
        onClick: () => {
          setSelectedEditor(editor.id);
          setError(null);
        },
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: editor.id
      };
    });
    return [noPreference, ...installedEditors];
  }, [editorInfo?.installedEditors, selectedEditor]);
  const codingAgentValues = useMemo121(() => {
    const noPreference = {
      id: NO_PREFERENCE_ID,
      keyHint: null,
      label: "No preference",
      leftItem: selectedCodingAgent === null ? /* @__PURE__ */ jsx220(Checkmark, {}) : null,
      onClick: () => {
        setSelectedCodingAgent(null);
        setError(null);
      },
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: NO_PREFERENCE_ID
    };
    const installedCodingAgents = (codingAgentInfo?.installedCodingAgents ?? []).map((codingAgent) => {
      return {
        id: codingAgent.id,
        keyHint: null,
        label: /* @__PURE__ */ jsxs131("span", {
          style: appLabel,
          children: [
            /* @__PURE__ */ jsx220(CodingAgentIcon, {
              codingAgentId: codingAgent.id,
              size: 18
            }),
            /* @__PURE__ */ jsx220("span", {
              style: appName,
              children: codingAgent.name
            })
          ]
        }),
        leftItem: selectedCodingAgent === codingAgent.id ? /* @__PURE__ */ jsx220(Checkmark, {}) : null,
        onClick: () => {
          setSelectedCodingAgent(codingAgent.id);
          setError(null);
        },
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: codingAgent.id
      };
    });
    return [noPreference, ...installedCodingAgents];
  }, [codingAgentInfo?.installedCodingAgents, selectedCodingAgent]);
  useEffect80(() => {
    if (editorInfo === null || codingAgentInfo === null) {
      return;
    }
    setSelectedEditor(editorInfo.defaultEditor !== null && editorInfo.installedEditors.some(({ id }) => id === editorInfo.defaultEditor) ? editorInfo.defaultEditor : null);
    setSelectedCodingAgent(codingAgentInfo.defaultCodingAgent !== null && codingAgentInfo.installedCodingAgents.some(({ id }) => id === codingAgentInfo.defaultCodingAgent) ? codingAgentInfo.defaultCodingAgent : null);
    setSyncedRevision(revision);
    setError(null);
  }, [codingAgentInfo, editorInfo, revision]);
  const updates = useMemo121(() => {
    const editorUpdate = selectedEditor === "custom" ? [] : [
      selectedEditor === null ? { setter: "setDefaultEditor", type: "delete" } : {
        setter: "setDefaultEditor",
        type: "set",
        value: selectedEditor
      }
    ];
    return [
      ...editorUpdate,
      selectedCodingAgent === null ? { setter: "setDefaultCodingAgent", type: "delete" } : {
        setter: "setDefaultCodingAgent",
        type: "set",
        value: selectedCodingAgent
      }
    ];
  }, [selectedCodingAgent, selectedEditor]);
  const ready = editorInfo !== null && codingAgentInfo !== null && syncedRevision === revision;
  useAutoSaveConfig({
    enabled: ready,
    onError: setError,
    ready,
    syncRevision: syncedRevision,
    updates
  });
  const displayedError = error ?? settingsError;
  return /* @__PURE__ */ jsx220("div", {
    style: container45,
    children: /* @__PURE__ */ jsxs131("div", {
      style: content13,
      children: [
        /* @__PURE__ */ jsxs131("div", {
          style: optionRow,
          children: [
            /* @__PURE__ */ jsx220("div", {
              style: label,
              children: "Default editor"
            }),
            /* @__PURE__ */ jsxs131("div", {
              style: rightColumn,
              children: [
                editorInfo === null && displayedError === null ? /* @__PURE__ */ jsx220("p", {
                  style: rightDescription,
                  children: "Loading installed editors..."
                }) : null,
                editorInfo?.installedEditors.length === 0 ? /* @__PURE__ */ jsx220("p", {
                  style: rightDescription,
                  children: "No supported editors were found on this computer."
                }) : null,
                editorInfo === null ? null : /* @__PURE__ */ jsx220(Combobox, {
                  values: editorValues,
                  selectedId: selectedEditor ?? NO_PREFERENCE_ID,
                  style: comboBoxStyle2,
                  title: "Default editor"
                })
              ]
            })
          ]
        }),
        /* @__PURE__ */ jsxs131("div", {
          style: optionRow,
          children: [
            /* @__PURE__ */ jsx220("div", {
              style: label,
              children: "Default coding agent"
            }),
            /* @__PURE__ */ jsxs131("div", {
              style: rightColumn,
              children: [
                codingAgentInfo === null && displayedError === null ? /* @__PURE__ */ jsx220("p", {
                  style: rightDescription,
                  children: "Loading installed coding agents..."
                }) : null,
                codingAgentInfo?.installedCodingAgents.length === 0 ? /* @__PURE__ */ jsx220("p", {
                  style: rightDescription,
                  children: "No supported coding agents were found on this computer."
                }) : null,
                codingAgentInfo === null ? null : /* @__PURE__ */ jsx220(Combobox, {
                  values: codingAgentValues,
                  selectedId: selectedCodingAgent ?? NO_PREFERENCE_ID,
                  style: comboBoxStyle2,
                  title: "Default coding agent"
                })
              ]
            })
          ]
        }),
        displayedError ? /* @__PURE__ */ jsxs131(Fragment58, {
          children: [
            /* @__PURE__ */ jsx220(Spacing, {
              y: 1.5
            }),
            /* @__PURE__ */ jsx220("div", {
              style: { paddingLeft: 16, paddingRight: 16 },
              children: /* @__PURE__ */ jsx220(ValidationMessage, {
                message: displayedError,
                align: "flex-start",
                type: "error"
              })
            })
          ]
        }) : null
      ]
    })
  });
};

// src/components/ConfigureLicenseModal.tsx
import { useCallback as useCallback114, useEffect as useEffect81, useMemo as useMemo122, useState as useState80 } from "react";

// src/components/LicenseKeyValidation.tsx
import { jsx as jsx221, jsxs as jsxs132 } from "react/jsx-runtime";
var LICENSE_KEY_LENGTH = 55;
var LICENSE_KEY_PREFIX = "rm_pub_";
var PRO_HOST = "https://www.remotion.pro";
var hasActiveCompanyLicense = (details) => {
  return details.hasActiveSubscription === true;
};
var validateLicenseKey = (key7) => {
  if (key7.length === 0) {
    return { valid: false, message: null };
  }
  if (!key7.startsWith(LICENSE_KEY_PREFIX)) {
    return {
      valid: false,
      message: `License key must start with "${LICENSE_KEY_PREFIX}"`
    };
  }
  const afterPrefix = key7.slice(LICENSE_KEY_PREFIX.length);
  if (!/^[a-zA-Z0-9]*$/.test(afterPrefix)) {
    return {
      valid: false,
      message: "License key must contain only alphanumeric characters after the prefix"
    };
  }
  if (key7.length !== LICENSE_KEY_LENGTH) {
    return {
      valid: false,
      message: `License key must be ${LICENSE_KEY_LENGTH} characters long`
    };
  }
  return { valid: true, message: null };
};
var fetchLicenseKeyDetails = async (licenseKey) => {
  const response = await fetch(`${PRO_HOST}/api/validate-license-key`, {
    method: "POST",
    body: JSON.stringify({
      licenseKey
    }),
    headers: {
      "Content-Type": "application/json"
    }
  });
  return response.json();
};
var actionStyle = {
  cursor: "pointer",
  marginLeft: 0,
  marginRight: 0,
  width: "100%"
};
var actionContent = {
  alignItems: "center",
  color: "inherit",
  display: "flex",
  fontFamily: "sans-serif",
  fontSize: 13,
  gap: 8,
  lineHeight: "18px",
  minWidth: 0,
  width: "100%"
};
var actionText = {
  color: "inherit",
  flex: 1,
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "18px",
  minWidth: 0,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var actionLabel = {
  color: "inherit",
  flexShrink: 0,
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "18px"
};
var inactiveLicenseText = {
  ...actionText,
  color: FAIL_COLOR
};
var iconStyle8 = {
  width: 14,
  height: 14,
  flexShrink: 0
};
var ErrorIcon = () => {
  return /* @__PURE__ */ jsx221("svg", {
    style: iconStyle8,
    viewBox: "0 0 512 512",
    children: /* @__PURE__ */ jsx221("path", {
      fill: FAIL_COLOR,
      d: "M0 160V352L160 512H352L512 352V160L352 0H160L0 160zm353.9 32l-17 17-47 47 47 47 17 17L320 353.9l-17-17-47-47-47 47-17 17L158.1 320l17-17 47-47-47-47-17-17L192 158.1l17 17 47 47 47-47 17-17L353.9 192z"
    })
  });
};
var LicenseKeyDetailsDisplay = ({ details }) => {
  const projectUrl = `${PRO_HOST}/projects/${details.projectSlug}`;
  const usageUrl = `${projectUrl}/usage#client-renders-usage`;
  const hasActiveLicense = hasActiveCompanyLicense(details);
  return /* @__PURE__ */ jsxs132("div", {
    children: [
      /* @__PURE__ */ jsx221(InspectorQuickAction, {
        disabled: false,
        onClick: () => window.open(usageUrl, "_blank", "noopener,noreferrer"),
        renderIcon: (color) => /* @__PURE__ */ jsx221(CheckCircleFilled, {
          style: { ...iconStyle8, fill: color }
        }),
        style: actionStyle,
        title: "View license usage",
        children: /* @__PURE__ */ jsxs132("span", {
          style: actionContent,
          children: [
            /* @__PURE__ */ jsxs132("span", {
              style: actionText,
              children: [
                "Belongs to ",
                details.projectName
              ]
            }),
            /* @__PURE__ */ jsx221("span", {
              style: actionLabel,
              children: "View usage"
            })
          ]
        })
      }),
      /* @__PURE__ */ jsx221(InspectorQuickAction, {
        disabled: false,
        onClick: () => window.open(projectUrl, "_blank", "noopener,noreferrer"),
        renderIcon: (color) => hasActiveLicense ? /* @__PURE__ */ jsx221(CheckCircleFilled, {
          style: { ...iconStyle8, fill: color }
        }) : /* @__PURE__ */ jsx221(ErrorIcon, {}),
        style: actionStyle,
        title: "Manage Company License",
        children: /* @__PURE__ */ jsxs132("span", {
          style: actionContent,
          children: [
            /* @__PURE__ */ jsx221("span", {
              style: hasActiveLicense ? actionText : inactiveLicenseText,
              children: hasActiveLicense ? "Active Company License" : "No active Company License"
            }),
            /* @__PURE__ */ jsx221("span", {
              style: actionLabel,
              children: "Manage"
            })
          ]
        })
      })
    ]
  });
};

// src/components/ConfigureLicenseModal.tsx
import { jsx as jsx222, jsxs as jsxs133, Fragment as Fragment59 } from "react/jsx-runtime";
var container46 = {
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minWidth: 0
};
var content14 = {
  flex: 1,
  padding: 16
};
var description3 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5,
  margin: 0
};
var externalDescriptionLink = {
  color: BLUE,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: "21px"
};
var externalLinkIndicator = {
  display: "inline-block",
  height: 12,
  marginLeft: 4,
  verticalAlign: -2,
  width: 12
};
var freeLicenseMessage = {
  ...description3,
  marginBottom: 5,
  marginLeft: 28
};
var companyLicenseContent = {
  marginLeft: 28
};
var inputLabel = {
  color: LIGHT_TEXT,
  display: "block",
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "20px",
  marginBottom: 6,
  marginTop: 10
};
var LicenseSettings = () => {
  const { error: settingsError, publicLicenseKey, revision } = useSettings();
  const initialLicenseType = publicLicenseKey === "free-license" ? "free" : publicLicenseKey === null ? null : "company";
  const [licenseType, setLicenseType] = useState80(initialLicenseType);
  const [companyLicenseKey, setCompanyLicenseKey] = useState80(initialLicenseType === "company" ? publicLicenseKey ?? "" : "");
  const [companyLicenseKeyToSave, setCompanyLicenseKeyToSave] = useState80(companyLicenseKey);
  const [error, setError] = useState80(null);
  const [syncedRevision, setSyncedRevision] = useState80(revision);
  const [isValidatingLicenseKey, setIsValidatingLicenseKey] = useState80(false);
  const [remoteValidationMessage, setRemoteValidationMessage] = useState80(null);
  const [licenseKeyDetails, setLicenseKeyDetails] = useState80(null);
  useEffect81(() => {
    const nextLicenseType = publicLicenseKey === "free-license" ? "free" : publicLicenseKey === null ? null : "company";
    const nextCompanyLicenseKey = nextLicenseType === "company" ? publicLicenseKey ?? "" : "";
    setLicenseType(nextLicenseType);
    setCompanyLicenseKey(nextCompanyLicenseKey);
    setCompanyLicenseKeyToSave(nextCompanyLicenseKey);
    setSyncedRevision(revision);
    setError(null);
  }, [publicLicenseKey, revision]);
  const toggleLicenseType = useCallback114((newLicenseType) => {
    setLicenseType((currentLicenseType) => currentLicenseType === newLicenseType ? null : newLicenseType);
    setError(null);
  }, []);
  const toggleFreeLicense = useCallback114(() => {
    toggleLicenseType("free");
  }, [toggleLicenseType]);
  const toggleCompanyLicense = useCallback114(() => {
    toggleLicenseType("company");
  }, [toggleLicenseType]);
  const localLicenseKeyValidation = useMemo122(() => validateLicenseKey(companyLicenseKey.trim()), [companyLicenseKey]);
  useEffect81(() => {
    let cancelled = false;
    setLicenseKeyDetails(null);
    setRemoteValidationMessage(null);
    if (licenseType !== "company" || !localLicenseKeyValidation.valid) {
      setIsValidatingLicenseKey(false);
      return () => {
        cancelled = true;
      };
    }
    setIsValidatingLicenseKey(true);
    fetchLicenseKeyDetails(companyLicenseKey.trim()).then((details) => {
      if (cancelled) {
        return;
      }
      setIsValidatingLicenseKey(false);
      if (details.isValid) {
        setLicenseKeyDetails(details);
      } else {
        setRemoteValidationMessage("License key is invalid or has been reset");
      }
    }).catch(() => {
      if (cancelled) {
        return;
      }
      setIsValidatingLicenseKey(false);
      setRemoteValidationMessage("Failed to fetch license key details");
    });
    return () => {
      cancelled = true;
    };
  }, [companyLicenseKey, licenseType, localLicenseKeyValidation.valid]);
  const companyLicenseKeyIsValid = localLicenseKeyValidation.valid && licenseKeyDetails?.isValid === true && !isValidatingLicenseKey;
  const publicLicenseKeyToSave = useMemo122(() => {
    if (licenseType === "free") {
      return "free-license";
    }
    if (licenseType === "company") {
      return companyLicenseKeyToSave;
    }
    return "";
  }, [companyLicenseKeyToSave, licenseType]);
  const updates = useMemo122(() => {
    if (licenseType === null) {
      return [{ setter: "setPublicLicenseKey", type: "delete" }];
    }
    return [
      {
        setter: "setPublicLicenseKey",
        type: "set",
        value: publicLicenseKeyToSave
      }
    ];
  }, [licenseType, publicLicenseKeyToSave]);
  useAutoSaveConfig({
    enabled: licenseType === null || publicLicenseKeyToSave.length > 0 && (licenseType !== "company" || companyLicenseKeyToSave === companyLicenseKey.trim() && companyLicenseKeyIsValid),
    onError: setError,
    ready: syncedRevision === revision,
    syncRevision: syncedRevision,
    updates
  });
  const displayedError = error ?? settingsError;
  return /* @__PURE__ */ jsx222("div", {
    style: container46,
    children: /* @__PURE__ */ jsxs133("div", {
      style: content14,
      children: [
        /* @__PURE__ */ jsx222(LicenseExplanation, {}),
        /* @__PURE__ */ jsx222(Spacing, {
          y: 2
        }),
        /* @__PURE__ */ jsxs133("div", {
          "aria-label": "License type",
          role: "radiogroup",
          children: [
            /* @__PURE__ */ jsx222(RadioButton, {
              checked: licenseType === "free",
              disabled: false,
              onClick: toggleFreeLicense,
              children: "I am eligible for the Free License"
            }),
            licenseType === "free" ? /* @__PURE__ */ jsx222("p", {
              style: freeLicenseMessage,
              children: "That's it! Enjoy Remotion."
            }) : null,
            /* @__PURE__ */ jsx222(RadioButton, {
              checked: licenseType === "company",
              disabled: false,
              onClick: toggleCompanyLicense,
              children: "I need a Company License"
            }),
            licenseType === "company" ? /* @__PURE__ */ jsxs133("div", {
              style: companyLicenseContent,
              children: [
                /* @__PURE__ */ jsxs133("p", {
                  style: description3,
                  children: [
                    "Visit",
                    " ",
                    /* @__PURE__ */ jsxs133("a", {
                      style: externalDescriptionLink,
                      href: "https://remotion.pro/license",
                      target: "_blank",
                      rel: "noopener noreferrer",
                      children: [
                        "remotion.pro/license",
                        /* @__PURE__ */ jsx222("svg", {
                          "aria-hidden": "true",
                          viewBox: "0 0 16 16",
                          style: externalLinkIndicator,
                          children: /* @__PURE__ */ jsx222("path", {
                            d: "M4 12 12 4M6 4h6v6",
                            fill: "none",
                            stroke: BLUE,
                            strokeLinecap: "round",
                            strokeLinejoin: "round",
                            strokeWidth: "1.5"
                          })
                        })
                      ]
                    }),
                    " ",
                    "to obtain a license key. Then enter it here."
                  ]
                }),
                /* @__PURE__ */ jsxs133("label", {
                  style: inputLabel,
                  children: [
                    "Public license key",
                    /* @__PURE__ */ jsx222(RemotionInput, {
                      status: localLicenseKeyValidation.message || remoteValidationMessage || licenseKeyDetails !== null && !hasActiveCompanyLicense(licenseKeyDetails) ? "error" : "ok",
                      rightAlign: false,
                      value: companyLicenseKey,
                      onChange: (event) => {
                        setCompanyLicenseKey(event.target.value);
                        setError(null);
                      },
                      onBlur: () => {
                        setCompanyLicenseKeyToSave(companyLicenseKey.trim());
                      },
                      placeholder: "rm_pub_...",
                      autoFocus: true
                    })
                  ]
                }),
                localLicenseKeyValidation.message ? /* @__PURE__ */ jsxs133(Fragment59, {
                  children: [
                    /* @__PURE__ */ jsx222(Spacing, {
                      y: 1
                    }),
                    /* @__PURE__ */ jsx222(ValidationMessage, {
                      message: localLicenseKeyValidation.message,
                      align: "flex-start",
                      type: "error"
                    })
                  ]
                }) : null,
                remoteValidationMessage ? /* @__PURE__ */ jsxs133(Fragment59, {
                  children: [
                    /* @__PURE__ */ jsx222(Spacing, {
                      y: 1
                    }),
                    /* @__PURE__ */ jsx222(ValidationMessage, {
                      message: remoteValidationMessage,
                      align: "flex-start",
                      type: "error"
                    })
                  ]
                }) : null,
                isValidatingLicenseKey ? /* @__PURE__ */ jsxs133(Fragment59, {
                  children: [
                    /* @__PURE__ */ jsx222(Spacing, {
                      y: 1
                    }),
                    /* @__PURE__ */ jsx222("p", {
                      style: description3,
                      children: "Loading license key details..."
                    })
                  ]
                }) : null,
                licenseKeyDetails ? /* @__PURE__ */ jsx222(LicenseKeyDetailsDisplay, {
                  details: licenseKeyDetails
                }) : null
              ]
            }) : null
          ]
        }),
        displayedError ? /* @__PURE__ */ jsxs133(Fragment59, {
          children: [
            /* @__PURE__ */ jsx222(Spacing, {
              y: 1.5
            }),
            /* @__PURE__ */ jsx222(ValidationMessage, {
              message: displayedError,
              align: "flex-start",
              type: "error"
            })
          ]
        }) : null
      ]
    })
  });
};

// src/components/InstallPackage.tsx
import {
  apiDocs,
  descriptions,
  extraPackages,
  installableMap
} from "@remotion/studio-shared";
import React149, { useCallback as useCallback115, useContext as useContext119, useEffect as useEffect82 } from "react";
import ReactDOM2 from "react-dom";
import { VERSION } from "remotion";

// src/components/InstallablePackage.tsx
import { jsx as jsx223, jsxs as jsxs134 } from "react/jsx-runtime";
var FONT_SIZE = 13;
var container47 = {
  alignItems: "center",
  display: "flex",
  flex: 1,
  gap: 10,
  minWidth: 0
};
var packageDetails = {
  flex: 1,
  minWidth: 0
};
var packageName = {
  color: WHITE,
  display: "block",
  fontFamily: "monospace",
  fontSize: FONT_SIZE,
  lineHeight: 1.4,
  overflow: "hidden",
  textDecoration: "none",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
};
var descriptionStyle = {
  color: LIGHT_TEXT,
  fontSize: FONT_SIZE,
  lineHeight: 1.4
};
var status2 = {
  color: LIGHT_TEXT,
  fontSize: 12,
  lineHeight: 1.4,
  whiteSpace: "nowrap"
};
var InstallablePackageComp = ({ isInstalled, pkg, link, description: description4 }) => {
  return /* @__PURE__ */ jsxs134("div", {
    style: container47,
    children: [
      /* @__PURE__ */ jsxs134("div", {
        style: packageDetails,
        children: [
          /* @__PURE__ */ jsx223("a", {
            href: link,
            style: packageName,
            target: "_blank",
            children: pkg
          }),
          /* @__PURE__ */ jsx223("div", {
            style: descriptionStyle,
            children: description4
          })
        ]
      }),
      /* @__PURE__ */ jsx223("span", {
        style: status2,
        children: isInstalled ? "Installed" : "Not installed"
      })
    ]
  });
};

// src/components/InstallPackage.tsx
import { jsx as jsx224, jsxs as jsxs135 } from "react/jsx-runtime";
var container48 = {
  boxSizing: "border-box",
  flex: 1,
  fontFamily: "sans-serif",
  minHeight: 0,
  overflowY: "auto",
  padding: "16px 16px 0"
};
var settingsContainer = {
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minHeight: 0,
  minWidth: 0
};
var text5 = {
  fontSize: 14
};
var footer2 = {
  flex: "none"
};
var packageRow = {
  alignItems: "center",
  borderBottom: BORDER_WHITE_ALPHA_12,
  display: "flex",
  gap: 10,
  minHeight: 52,
  padding: "4px 0"
};
var lastPackageRow = {
  ...packageRow,
  borderBottom: "none"
};
var InstallPackageSettings = ({ footerContainer, packageManager }) => {
  const [state, setState] = React149.useState({ type: "idle" });
  const [map, setMap] = React149.useState({});
  const { previewServerState: ctx } = useContext119(StudioServerConnectionCtx);
  const { setSelectedModal } = useContext119(SetSelectedModalContext);
  const selectedPackages = Object.keys(map).filter((pkg) => map[pkg]);
  const selectedPackageSpecs = selectedPackages.map((name) => ({
    name,
    version: extraPackages.find((pkg) => pkg.name === name)?.version ?? null
  }));
  const onClick = useCallback115(async () => {
    if (state.type === "done") {
      if (packageManager === null) {
        setSelectedModal(null);
        return;
      }
      setState({ type: "restarting" });
      restartStudio();
      return;
    }
    setState({ type: "installing" });
    try {
      await installPackages(selectedPackageSpecs);
      for (const packageName2 of selectedPackages) {
        markOptionalPackageInstalled(packageName2);
      }
      setState({ type: "done" });
    } catch (err) {
      setState({ type: "error", error: err });
    }
  }, [
    packageManager,
    selectedPackageSpecs,
    selectedPackages,
    setSelectedModal,
    state.type
  ]);
  const canSelectPackages = state.type === "idle" && ctx.type === "connected";
  const disabled = !(canSelectPackages || state.type === "done") || selectedPackages.length === 0;
  const { registerKeybinding } = useKeybinding();
  useEffect82(() => {
    if (disabled) {
      return;
    }
    const enter = registerKeybinding({
      callback() {
        onClick();
      },
      commandCtrlKey: true,
      key: "Enter",
      event: "keydown",
      preventDefault: true,
      triggerIfInputFieldFocused: true,
      keepRegisteredWhenNotHighestContext: true
    });
    return () => {
      enter.unregister();
    };
  }, [disabled, onClick, registerKeybinding]);
  const installablePackages = Object.entries(installableMap).filter(([, install]) => install);
  const packageCount = installablePackages.length + extraPackages.length;
  return /* @__PURE__ */ jsxs135("div", {
    style: settingsContainer,
    children: [
      /* @__PURE__ */ jsx224("div", {
        style: container48,
        className: VERTICAL_SCROLLBAR_CLASSNAME,
        children: state.type === "done" ? /* @__PURE__ */ jsxs135("div", {
          style: text5,
          children: [
            "Installed package",
            selectedPackages.length === 1 ? "" : "s",
            " ",
            "successfully.",
            packageManager === null ? null : " Restart the server to complete."
          ]
        }) : state.type === "restarting" ? /* @__PURE__ */ jsx224("div", {
          style: text5,
          children: "Restarting the Studio server..."
        }) : state.type === "installing" ? /* @__PURE__ */ jsxs135("div", {
          style: text5,
          children: [
            "Installing package",
            selectedPackages.length === 1 ? "" : "s",
            packageManager === null ? "." : ". Check your terminal for progress."
          ]
        }) : /* @__PURE__ */ jsxs135("div", {
          style: text5,
          role: "list",
          "aria-label": "Remotion packages",
          children: [
            installablePackages.map(([pkgShort], index) => {
              const pkg = pkgShort === "core" ? "remotion" : `@remotion/${pkgShort}`;
              const isInstalled = window.remotion_installedPackages?.includes(pkg) ?? false;
              const link = apiDocs[pkgShort];
              const description4 = descriptions[pkgShort];
              if (!link) {
                throw new Error("No link for " + pkg);
              }
              if (!description4) {
                throw new Error("No description for " + pkg);
              }
              return /* @__PURE__ */ jsxs135(Row, {
                align: "center",
                role: "listitem",
                style: index === packageCount - 1 ? lastPackageRow : packageRow,
                children: [
                  /* @__PURE__ */ jsx224(Checkbox, {
                    checked: map[pkg],
                    name: pkg,
                    onChange: () => {
                      setMap((prev) => ({ ...prev, [pkg]: !prev[pkg] }));
                    },
                    disabled: !canSelectPackages || isInstalled
                  }),
                  /* @__PURE__ */ jsx224(InstallablePackageComp, {
                    description: description4,
                    isInstalled,
                    link,
                    pkg
                  })
                ]
              }, pkg);
            }),
            extraPackages.map((extraPkg, index) => {
              const isInstalled = window.remotion_installedPackages?.includes(extraPkg.name) ?? false;
              return /* @__PURE__ */ jsxs135(Row, {
                align: "center",
                role: "listitem",
                style: installablePackages.length + index === packageCount - 1 ? lastPackageRow : packageRow,
                children: [
                  /* @__PURE__ */ jsx224(Checkbox, {
                    checked: map[extraPkg.name],
                    name: extraPkg.name,
                    onChange: () => {
                      setMap((prev) => ({
                        ...prev,
                        [extraPkg.name]: !prev[extraPkg.name]
                      }));
                    },
                    disabled: !canSelectPackages || isInstalled
                  }),
                  /* @__PURE__ */ jsx224(InstallablePackageComp, {
                    description: extraPkg.description,
                    isInstalled,
                    link: extraPkg.docsUrl,
                    pkg: `${extraPkg.name}@${extraPkg.version}`
                  })
                ]
              }, extraPkg.name);
            })
          ]
        })
      }),
      footerContainer ? ReactDOM2.createPortal(/* @__PURE__ */ jsx224(ModalFooterContainer, {
        style: footer2,
        children: /* @__PURE__ */ jsxs135(Row, {
          align: "center",
          children: [
            state.type === "idle" ? /* @__PURE__ */ jsxs135("span", {
              style: { color: LIGHT_TEXT, fontSize: 13, lineHeight: 1.2 },
              children: [
                "This will install ",
                selectedPackages.length,
                " package",
                selectedPackages.length === 1 ? "" : "s",
                /* @__PURE__ */ jsx224("br", {}),
                packageManager === null ? `in this project, Remotion v${VERSION}` : `using ${packageManager}, Remotion v${VERSION}`
              ]
            }) : null,
            /* @__PURE__ */ jsx224(Flex, {}),
            /* @__PURE__ */ jsxs135(ModalButton, {
              onClick,
              disabled,
              children: [
                state.type === "restarting" ? "Restarting..." : state.type === "installing" ? "Installing..." : state.type === "done" ? packageManager === null ? "Done" : "Restart Server" : "Install",
                disabled ? null : /* @__PURE__ */ jsx224(ShortcutHint, {
                  keyToPress: "↵",
                  cmdOrCtrl: true
                })
              ]
            })
          ]
        })
      }), footerContainer) : null
    ]
  });
};

// src/components/KeyboardShortcutsSettings.tsx
import React150, {
  useCallback as useCallback116,
  useContext as useContext120,
  useEffect as useEffect83,
  useMemo as useMemo123,
  useState as useState81
} from "react";
import { jsx as jsx225, jsxs as jsxs136, Fragment as Fragment60 } from "react/jsx-runtime";
var container49 = {
  alignSelf: "flex-start",
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minWidth: 0
};
var dividerLabel = {
  ...sectionHeader,
  margin: 0,
  padding: "4px 16px"
};
var shortcutSectionTitle = {
  ...dividerLabel,
  marginTop: 8
};
var shortcutRow = {
  alignItems: "center",
  borderBottom: BORDER_WHITE_ALPHA_12,
  columnGap: 4,
  display: "grid",
  gridTemplateColumns: "minmax(0, 1fr) minmax(76px, max-content) 14px",
  margin: "0 16px",
  minHeight: 42
};
var lastShortcutRow = {
  ...shortcutRow,
  borderBottom: "none"
};
var actionName = {
  color: LIGHT_TEXT,
  fontSize: 14,
  lineHeight: 1.4,
  minWidth: 0
};
var shortcutCell = {
  alignItems: "center",
  display: "flex",
  justifyContent: "flex-end",
  minWidth: 0
};
var actionCell = {
  alignItems: "center",
  display: "flex",
  gap: 6,
  justifyContent: "flex-start",
  whiteSpace: "nowrap"
};
var chords = {
  alignItems: "center",
  display: "flex",
  gap: 6,
  whiteSpace: "nowrap"
};
var chord = {
  alignItems: "center",
  display: "flex",
  gap: 4
};
var key7 = {
  background: INPUT_BACKGROUND,
  border: `1px solid ${BLACK_ALPHA_60}`,
  borderBottomWidth: 2,
  borderRadius: 3,
  color: WHITE,
  fontFamily: "monospace",
  fontSize: 12,
  lineHeight: "18px",
  minWidth: 18,
  padding: "1px 5px",
  textAlign: "center"
};
var chordButton = {
  alignItems: "center",
  background: "transparent",
  border: 0,
  cursor: "pointer",
  display: "flex",
  font: "inherit",
  margin: 0,
  padding: "4px 0"
};
var alternative = {
  color: LIGHT_TEXT,
  fontSize: 11
};
var emptyShortcut = {
  color: LIGHT_TEXT,
  fontSize: 12,
  fontStyle: "italic"
};
var isSameShortcut = (first, second) => first.key.toLowerCase() === second.key.toLowerCase() && (first.commandOrControl ?? false) === (second.commandOrControl ?? false) && (first.shift ?? false) === (second.shift ?? false) && (first.alt ?? false) === (second.alt ?? false);
var ShortcutChords = ({ values }) => {
  if (values.length === 0) {
    return /* @__PURE__ */ jsx225("span", {
      style: emptyShortcut,
      children: "Unassigned"
    });
  }
  return /* @__PURE__ */ jsx225("span", {
    style: chords,
    children: values.map((keys, chordIndex) => /* @__PURE__ */ jsxs136(React150.Fragment, {
      children: [
        chordIndex > 0 ? /* @__PURE__ */ jsx225("span", {
          style: alternative,
          children: "or"
        }) : null,
        /* @__PURE__ */ jsx225("span", {
          style: chord,
          children: keys.map((keyboardKey) => /* @__PURE__ */ jsx225("kbd", {
            style: key7,
            children: keyboardKey
          }, keyboardKey))
        })
      ]
    }, keys.join("-")))
  });
};
var KeyboardShortcutsSettings = () => {
  const { error: settingsError, revision, studioRuntimeConfig } = useSettings();
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const { previewServerState } = useContext120(StudioServerConnectionCtx);
  const canEdit = canEditStudioConfig({
    isBrowserStudio,
    previewServerConnected: previewServerState.type === "connected",
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  const [enabled, setEnabled] = useState81(true);
  const [configuredShortcuts, setConfiguredShortcuts] = useState81({});
  const [enabledEdited, setEnabledEdited] = useState81(false);
  const [shortcutsEdited, setShortcutsEdited] = useState81(false);
  const [recording, setRecording] = useState81(null);
  const [syncedRevision, setSyncedRevision] = useState81(-1);
  const [error, setError] = useState81(null);
  const displayedShortcutGroups = getStudioAskAIEnabled() ? [...keyboardShortcutGroups, askAIKeyboardShortcutGroup] : keyboardShortcutGroups;
  const visibleShortcutGroups = enabled ? displayedShortcutGroups : [];
  useEffect83(() => {
    if (studioRuntimeConfig === null) {
      return;
    }
    setEnabled(studioRuntimeConfig.configFileStudioSettings?.keyboardShortcutsEnabled !== false);
    setConfiguredShortcuts(studioRuntimeConfig.keyboardShortcuts ?? {});
    setEnabledEdited(false);
    setShortcutsEdited(false);
    setRecording(null);
    setSyncedRevision(revision);
    setError(null);
  }, [revision, studioRuntimeConfig]);
  const onEnabledChange = useCallback116((event) => {
    setEnabled(event.target.checked);
    setEnabledEdited(true);
  }, []);
  const updates = useMemo123(() => {
    const nextUpdates = [];
    if (enabledEdited) {
      nextUpdates.push(enabled ? { setter: "setKeyboardShortcutsEnabled", type: "delete" } : {
        setter: "setKeyboardShortcutsEnabled",
        type: "set",
        value: false
      });
    }
    if (shortcutsEdited) {
      nextUpdates.push(Object.keys(configuredShortcuts).length === 0 ? { setter: "setKeyboardShortcuts", type: "delete" } : {
        setter: "setKeyboardShortcuts",
        type: "set",
        value: configuredShortcuts
      });
    }
    return nextUpdates;
  }, [configuredShortcuts, enabled, enabledEdited, shortcutsEdited]);
  const ready = studioRuntimeConfig !== null && syncedRevision === revision;
  useAutoSaveConfig({
    enabled: ready && canEdit,
    onError: setError,
    ready,
    syncRevision: syncedRevision,
    updates
  });
  if (studioRuntimeConfig === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs136("div", {
    style: container49,
    children: [
      canEdit ? /* @__PURE__ */ jsxs136(Fragment60, {
        children: [
          /* @__PURE__ */ jsx225("p", {
            style: dividerLabel,
            children: "General"
          }),
          /* @__PURE__ */ jsxs136("label", {
            style: optionRow,
            children: [
              /* @__PURE__ */ jsx225("div", {
                style: label,
                children: "Keyboard shortcuts"
              }),
              /* @__PURE__ */ jsx225("div", {
                style: rightRow,
                children: /* @__PURE__ */ jsx225(Checkbox, {
                  checked: enabled,
                  name: "keyboard-shortcuts",
                  onChange: onEnabledChange
                })
              })
            ]
          })
        ]
      }) : null,
      visibleShortcutGroups.map((group, groupIndex) => /* @__PURE__ */ jsxs136("div", {
        children: [
          /* @__PURE__ */ jsx225("p", {
            style: shortcutSectionTitle,
            children: group.name
          }),
          /* @__PURE__ */ jsx225("div", {
            role: "list",
            "aria-label": group.name,
            children: group.shortcuts.map((shortcut, shortcutIndex) => {
              const configured = shortcut.actionId === null ? false : Object.prototype.hasOwnProperty.call(configuredShortcuts, shortcut.actionId);
              const shortcutValues = shortcut.actionId === null ? shortcut.fixedChords ?? [] : getKeyboardShortcutsForAction(shortcut.actionId, configuredShortcuts).map(formatKeyboardShortcut);
              const shortcutButtonId = shortcut.actionId === null ? null : `keyboard-shortcut-${shortcut.actionId}`;
              const shortcutMenuItems = shortcut.actionId === null ? [] : [
                {
                  id: "remap-shortcut",
                  keyHint: null,
                  label: "Remap shortcut",
                  leftItem: null,
                  onClick: () => {
                    setError(null);
                    setRecording(shortcut.actionId);
                    requestAnimationFrame(() => {
                      document.getElementById(shortcutButtonId)?.focus();
                    });
                  },
                  quickSwitcherLabel: null,
                  subMenu: null,
                  type: "item",
                  value: "remap-shortcut"
                },
                ...configured ? [
                  {
                    id: "reset-shortcut",
                    keyHint: null,
                    label: "Reset to default",
                    leftItem: null,
                    onClick: () => {
                      setConfiguredShortcuts((current) => {
                        const next = { ...current };
                        delete next[shortcut.actionId];
                        return next;
                      });
                      setShortcutsEdited(true);
                    },
                    quickSwitcherLabel: null,
                    subMenu: null,
                    type: "item",
                    value: "reset-shortcut"
                  }
                ] : [],
                ...configuredShortcuts[shortcut.actionId] !== null ? [
                  {
                    id: "disable-shortcut",
                    keyHint: null,
                    label: "Disable shortcut",
                    leftItem: null,
                    onClick: () => {
                      setConfiguredShortcuts((current) => ({
                        ...current,
                        [shortcut.actionId]: null
                      }));
                      setShortcutsEdited(true);
                    },
                    quickSwitcherLabel: null,
                    subMenu: null,
                    type: "item",
                    value: "disable-shortcut"
                  }
                ] : []
              ];
              return /* @__PURE__ */ jsxs136("div", {
                role: "listitem",
                style: groupIndex === displayedShortcutGroups.length - 1 && shortcutIndex === group.shortcuts.length - 1 ? lastShortcutRow : shortcutRow,
                children: [
                  /* @__PURE__ */ jsx225("span", {
                    style: actionName,
                    children: shortcut.action
                  }),
                  shortcut.actionId === null || !canEdit ? /* @__PURE__ */ jsxs136(Fragment60, {
                    children: [
                      /* @__PURE__ */ jsx225("span", {
                        style: shortcutCell,
                        children: /* @__PURE__ */ jsx225(ShortcutChords, {
                          values: shortcutValues
                        })
                      }),
                      /* @__PURE__ */ jsx225("span", {})
                    ]
                  }) : /* @__PURE__ */ jsxs136(Fragment60, {
                    children: [
                      /* @__PURE__ */ jsx225("span", {
                        style: shortcutCell,
                        children: /* @__PURE__ */ jsx225("button", {
                          id: shortcutButtonId,
                          type: "button",
                          style: chordButton,
                          "aria-label": `Change shortcut for ${shortcut.action}`,
                          onClick: () => {
                            setError(null);
                            setRecording(shortcut.actionId);
                          },
                          onKeyDown: (event) => {
                            if (recording !== shortcut.actionId)
                              return;
                            event.preventDefault();
                            event.stopPropagation();
                            if (event.key === "Escape") {
                              setRecording(null);
                              return;
                            }
                            const value = shortcutFromKeyboardEvent(event.nativeEvent);
                            if (value === null)
                              return;
                            const conflict = displayedShortcutGroups.flatMap((item) => item.shortcuts).find((item) => item.actionId !== null && item.actionId !== shortcut.actionId && getKeyboardShortcutsForAction(item.actionId, configuredShortcuts).some((candidate) => keyboardShortcutsOverlap(candidate, value)));
                            if (conflict) {
                              setError(`Shortcut is already used by “${conflict.action}”.`);
                              return;
                            }
                            setConfiguredShortcuts((current) => {
                              if (defaultKeyboardShortcuts[shortcut.actionId].some((defaultShortcut) => isSameShortcut(defaultShortcut, value))) {
                                const next = { ...current };
                                delete next[shortcut.actionId];
                                return next;
                              }
                              return {
                                ...current,
                                [shortcut.actionId]: value
                              };
                            });
                            setShortcutsEdited(true);
                            setRecording(null);
                          },
                          children: recording === shortcut.actionId ? /* @__PURE__ */ jsx225("span", {
                            style: emptyShortcut,
                            children: "Press shortcut…"
                          }) : /* @__PURE__ */ jsx225(ShortcutChords, {
                            values: shortcutValues
                          })
                        })
                      }),
                      /* @__PURE__ */ jsx225("span", {
                        style: actionCell,
                        children: /* @__PURE__ */ jsx225(InlineDropdown, {
                          renderAction: (color) => /* @__PURE__ */ jsx225(CaretDown, {
                            color
                          }),
                          title: `Actions for ${shortcut.action}`,
                          values: shortcutMenuItems,
                          variant: "compact"
                        })
                      })
                    ]
                  })
                ]
              }, shortcut.action);
            })
          })
        ]
      }, group.name)),
      error ?? settingsError ? /* @__PURE__ */ jsxs136(Fragment60, {
        children: [
          /* @__PURE__ */ jsx225(Spacing, {
            y: 1,
            block: true
          }),
          /* @__PURE__ */ jsx225("div", {
            style: { paddingLeft: 16, paddingRight: 16 },
            children: /* @__PURE__ */ jsx225(ValidationMessage, {
              align: "flex-start",
              message: error ?? settingsError ?? "",
              type: "error"
            })
          })
        ]
      }) : null
    ]
  });
};

// src/components/ModelsSettings.tsx
import React151, { Suspense as Suspense2, useCallback as useCallback117, useContext as useContext121, useState as useState82 } from "react";
import { jsx as jsx226, jsxs as jsxs137, Fragment as Fragment61 } from "react/jsx-runtime";
var LazyWhisperModels = React151.lazy(() => import("./LazyModels-mtggyjye.mjs"));
var LazyVideoMattingModels = React151.lazy(() => import("./LazyModels-nk7py0zt.mjs"));
var container50 = {
  alignSelf: "flex-start",
  boxSizing: "border-box",
  fontFamily: "sans-serif",
  minWidth: 0,
  padding: 16,
  width: "100%"
};
var overview = {
  color: LIGHT_TEXT,
  fontSize: 13,
  lineHeight: 1.5,
  margin: "0 0 16px"
};
var section = {
  paddingBottom: 16
};
var lastSection = {
  paddingTop: 16
};
var title2 = {
  ...sectionHeader,
  margin: 0,
  padding: "4px 0 0"
};
var packageNameStyle = {
  color: LIGHT_TEXT,
  fontFamily: "monospace",
  fontSize: 12,
  lineHeight: 1.4,
  margin: 0
};
var headingRow = {
  alignItems: "center",
  display: "flex",
  gap: 12,
  justifyContent: "space-between"
};
var missingText = {
  color: LIGHT_TEXT,
  fontSize: 13,
  lineHeight: 1.5,
  margin: 0
};
var missingDescription = {
  ...missingText,
  marginTop: 14
};
var installButton = {
  backgroundColor: BLUE,
  color: WHITE,
  flexShrink: 0
};
var errorStyle2 = {
  color: WARNING_COLOR,
  fontSize: 12,
  lineHeight: 1.4,
  margin: "8px 0 0"
};
var OptionalModelPackage = ({ canInstall, children, label: label14, packageName: packageName2, style: style7 }) => {
  const installed = useOptionalPackageInstalled(packageName2);
  const [installState, setInstallState] = useState82({
    type: "idle"
  });
  const install = useCallback117(() => {
    setInstallState({ type: "installing" });
    installOptionalPackage(packageName2).catch((error) => {
      setInstallState({
        type: "error",
        message: error instanceof Error ? error.message : String(error)
      });
    });
  }, [packageName2]);
  return /* @__PURE__ */ jsxs137("section", {
    style: style7,
    children: [
      /* @__PURE__ */ jsxs137("div", {
        style: headingRow,
        children: [
          /* @__PURE__ */ jsxs137("div", {
            children: [
              /* @__PURE__ */ jsx226("h3", {
                style: title2,
                children: label14
              }),
              /* @__PURE__ */ jsx226("p", {
                style: packageNameStyle,
                children: packageName2
              })
            ]
          }),
          installed || !canInstall ? null : /* @__PURE__ */ jsx226(Button, {
            disabled: installState.type === "installing",
            onClick: install,
            size: "compact",
            style: installButton,
            children: installState.type === "installing" ? "Installing…" : "Install"
          })
        ]
      }),
      installed ? /* @__PURE__ */ jsx226(Suspense2, {
        fallback: /* @__PURE__ */ jsx226("p", {
          style: missingText,
          children: "Loading models…"
        }),
        children
      }) : /* @__PURE__ */ jsxs137(Fragment61, {
        children: [
          /* @__PURE__ */ jsx226("p", {
            style: missingDescription,
            children: canInstall ? `Install the package and ${TRANSFORMERS_PACKAGE} to download and manage its models.` : "Reconnect the Studio server to install this package and manage its models."
          }),
          installState.type === "error" ? /* @__PURE__ */ jsx226("p", {
            style: errorStyle2,
            children: installState.message
          }) : null
        ]
      })
    ]
  });
};
var ModelsSettings = () => {
  const { previewServerState } = useContext121(StudioServerConnectionCtx);
  const canInstall = canInstallPackagesInStudio({
    isBrowserStudio: getBrowserStudioOperations() !== null,
    previewServerConnected: previewServerState.type === "connected",
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  return /* @__PURE__ */ jsxs137("div", {
    style: container50,
    children: [
      /* @__PURE__ */ jsx226("p", {
        style: overview,
        children: "Models are downloaded automatically when needed. You can also manage the browser cache here."
      }),
      /* @__PURE__ */ jsx226(OptionalModelPackage, {
        canInstall,
        label: "Transcription",
        packageName: WHISPER_WEBGPU_PACKAGE,
        style: section,
        children: /* @__PURE__ */ jsx226(LazyWhisperModels, {
          description: null,
          indent: false,
          visible: true
        })
      }),
      /* @__PURE__ */ jsx226(OptionalModelPackage, {
        canInstall,
        label: "Video matting",
        packageName: VIDEO_MATTING_PACKAGE,
        style: lastSection,
        children: /* @__PURE__ */ jsx226(LazyVideoMattingModels, {
          description: null,
          indent: false,
          visible: true
        })
      })
    ]
  });
};

// src/components/RenderingSettings.tsx
import { BrowserSafeApis as BrowserSafeApis11 } from "@remotion/renderer/client";
import { useCallback as useCallback118, useEffect as useEffect84, useMemo as useMemo124, useState as useState83 } from "react";
import { jsx as jsx227, jsxs as jsxs138, Fragment as Fragment62 } from "react/jsx-runtime";
var container51 = {
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minWidth: 0
};
var dividerLabel2 = {
  ...sectionHeader,
  margin: 0,
  padding: "4px 16px"
};
var sectionTitle = {
  ...dividerLabel2,
  marginTop: 8
};
var fullWidth = {
  boxSizing: "border-box",
  width: 180
};
var REMOTION_DEFAULT = "remotion-default";
var DEFAULT_CODEC = "h264";
var DEFAULT_STILL_IMAGE_FORMAT = "png";
var DEFAULT_VIDEO_IMAGE_FORMAT = "jpeg";
var DEFAULT_PRO_RES_PROFILE = "hq";
var DEFAULT_X264_PRESET = "medium";
var RenderingSettings = () => {
  const { error: settingsError, renderDefaults, revision } = useSettings();
  const [codec, setCodec] = useState83(null);
  const [scale, setScale] = useState83(1);
  const [stillImageFormat, setStillImageFormat] = useState83(null);
  const [videoImageFormat, setVideoImageFormat] = useState83(null);
  const [proResProfile, setProResProfile] = useState83(null);
  const [x264Preset, setX264Preset] = useState83(null);
  const [qualityMode, setQualityMode] = useState83("crf");
  const [crf, setCrf] = useState83(18);
  const [videoBitrate, setVideoBitrate] = useState83("1M");
  const [audioCodec, setAudioCodec] = useState83(null);
  const [concurrency, setConcurrency] = useState83(1);
  const [editedSetters, setEditedSetters] = useState83(() => new Set);
  const [syncedRevision, setSyncedRevision] = useState83(-1);
  const [error, setError] = useState83(null);
  useEffect84(() => {
    if (renderDefaults === null) {
      return;
    }
    setCodec(renderDefaults.configFileRenderDefaults?.codec ?? null);
    setScale(renderDefaults.scale);
    setStillImageFormat(renderDefaults.configFileRenderDefaults?.stillImageFormat ?? null);
    setVideoImageFormat(renderDefaults.configFileRenderDefaults?.videoImageFormat ?? null);
    setProResProfile(renderDefaults.configFileRenderDefaults?.proResProfile ?? null);
    setX264Preset(renderDefaults.configFileRenderDefaults?.x264Preset ?? null);
    setQualityMode(renderDefaults.videoBitrate === null ? "crf" : "bitrate");
    setCrf(renderDefaults.crf ?? BrowserSafeApis11.getDefaultCrfForCodec(renderDefaults.codec) ?? 18);
    setVideoBitrate(renderDefaults.videoBitrate ?? "1M");
    setAudioCodec(renderDefaults.audioCodec);
    setConcurrency(renderDefaults.concurrency);
    setEditedSetters(new Set);
    setSyncedRevision(revision);
    setError(null);
  }, [renderDefaults, revision]);
  const markEdited = useCallback118((setter) => {
    setEditedSetters((current) => new Set(current).add(setter));
  }, []);
  const selectCodec = useCallback118((value) => {
    setCodec(value);
    markEdited("setCodec");
  }, [markEdited]);
  const selectStillImageFormat = useCallback118((value) => {
    setStillImageFormat(value);
    markEdited("setStillImageFormat");
  }, [markEdited]);
  const selectVideoImageFormat = useCallback118((value) => {
    setVideoImageFormat(value);
    markEdited("setVideoImageFormat");
  }, [markEdited]);
  const selectProResProfile = useCallback118((value) => {
    setProResProfile(value);
    markEdited("setProResProfile");
  }, [markEdited]);
  const selectX264Preset = useCallback118((value) => {
    setX264Preset(value);
    markEdited("setX264Preset");
  }, [markEdited]);
  const selectQualityMode = useCallback118((value) => {
    setQualityMode(value);
    markEdited("setCrf");
    markEdited("setVideoBitrate");
  }, [markEdited]);
  const changeCrf = useCallback118((value) => {
    setCrf(value);
    markEdited("setCrf");
    markEdited("setVideoBitrate");
  }, [markEdited]);
  const selectAudioCodec = useCallback118((value) => {
    setAudioCodec(value);
    markEdited("setAudioCodec");
  }, [markEdited]);
  const changeScale = useCallback118((value) => {
    setScale(value);
    markEdited("setScale");
  }, [markEdited]);
  const changeConcurrency = useCallback118((value) => {
    setConcurrency(value);
    markEdited("setConcurrency");
  }, [markEdited]);
  const makeValues = ({
    options,
    selected,
    setSelected,
    labelValue = (value) => value
  }) => options.map((value) => ({
    id: value,
    key: value,
    keyHint: null,
    label: labelValue(value),
    leftItem: selected === value ? /* @__PURE__ */ jsx227(Checkmark, {}) : null,
    onClick: () => setSelected(value),
    quickSwitcherLabel: null,
    subMenu: null,
    type: "item",
    value
  }));
  const makeRemotionDefaultValue = ({
    labelValue,
    onClick,
    selected
  }) => [
    {
      id: REMOTION_DEFAULT,
      keyHint: null,
      label: `Default (${labelValue})`,
      leftItem: selected ? /* @__PURE__ */ jsx227(Checkmark, {}) : null,
      onClick,
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: REMOTION_DEFAULT
    },
    { id: `${REMOTION_DEFAULT}-divider`, type: "divider" }
  ];
  const codecValues = useMemo124(() => [
    ...makeRemotionDefaultValue({
      labelValue: humanReadableCodec(DEFAULT_CODEC),
      onClick: () => selectCodec(null),
      selected: codec === null
    }),
    ...makeValues({
      options: BrowserSafeApis11.validCodecs.filter((value) => value !== "mp3" && value !== "wav" && value !== "aac"),
      selected: codec,
      setSelected: selectCodec,
      labelValue: humanReadableCodec
    })
  ], [codec, selectCodec]);
  const stillFormatValues = useMemo124(() => [
    ...makeRemotionDefaultValue({
      labelValue: DEFAULT_STILL_IMAGE_FORMAT.toUpperCase(),
      onClick: () => selectStillImageFormat(null),
      selected: stillImageFormat === null
    }),
    ...makeValues({
      options: BrowserSafeApis11.validStillImageFormats,
      selected: stillImageFormat,
      setSelected: selectStillImageFormat,
      labelValue: (value) => value.toUpperCase()
    })
  ], [selectStillImageFormat, stillImageFormat]);
  const videoFormatValues = useMemo124(() => [
    ...makeRemotionDefaultValue({
      labelValue: DEFAULT_VIDEO_IMAGE_FORMAT.toUpperCase(),
      onClick: () => selectVideoImageFormat(null),
      selected: videoImageFormat === null
    }),
    ...makeValues({
      options: BrowserSafeApis11.validVideoImageFormats,
      selected: videoImageFormat,
      setSelected: selectVideoImageFormat,
      labelValue: (value) => value.toUpperCase()
    })
  ], [selectVideoImageFormat, videoImageFormat]);
  const profileValues = useMemo124(() => [
    ...makeRemotionDefaultValue({
      labelValue: labelProResProfile(DEFAULT_PRO_RES_PROFILE),
      onClick: () => selectProResProfile(null),
      selected: proResProfile === null
    }),
    ...makeValues({
      options: BrowserSafeApis11.proResProfileOptions,
      selected: proResProfile,
      setSelected: selectProResProfile,
      labelValue: labelProResProfile
    })
  ], [proResProfile, selectProResProfile]);
  const presetValues = useMemo124(() => [
    ...makeRemotionDefaultValue({
      labelValue: labelx264Preset(DEFAULT_X264_PRESET),
      onClick: () => selectX264Preset(null),
      selected: x264Preset === null
    }),
    ...makeValues({
      options: BrowserSafeApis11.x264PresetOptions,
      selected: x264Preset,
      setSelected: selectX264Preset,
      labelValue: labelx264Preset
    })
  ], [selectX264Preset, x264Preset]);
  const audioCodecValues = useMemo124(() => {
    return [
      ...makeRemotionDefaultValue({
        labelValue: "Automatic",
        onClick: () => selectAudioCodec(null),
        selected: audioCodec === null
      }),
      ...makeValues({
        options: BrowserSafeApis11.validAudioCodecs,
        selected: audioCodec,
        setSelected: selectAudioCodec,
        labelValue: (value) => value.toUpperCase()
      })
    ];
  }, [audioCodec, selectAudioCodec]);
  const qualityModeValues = useMemo124(() => {
    return ["crf", "bitrate"].map((value) => ({
      key: value,
      label: value === "crf" ? "CRF" : "Bitrate",
      onClick: () => selectQualityMode(value),
      selected: qualityMode === value
    }));
  }, [qualityMode, selectQualityMode]);
  const resolvedCodec = codec ?? DEFAULT_CODEC;
  const updates = useMemo124(() => {
    const allUpdates = [
      codec === null ? { setter: "setCodec", type: "delete" } : { setter: "setCodec", type: "set", value: codec },
      { setter: "setScale", type: "set", value: scale },
      stillImageFormat === null ? { setter: "setStillImageFormat", type: "delete" } : {
        setter: "setStillImageFormat",
        type: "set",
        value: stillImageFormat
      },
      videoImageFormat === null ? { setter: "setVideoImageFormat", type: "delete" } : {
        setter: "setVideoImageFormat",
        type: "set",
        value: videoImageFormat
      },
      qualityMode === "crf" ? { setter: "setCrf", type: "set", value: crf } : { setter: "setCrf", type: "delete" },
      qualityMode === "bitrate" ? { setter: "setVideoBitrate", type: "set", value: videoBitrate.trim() } : { setter: "setVideoBitrate", type: "delete" },
      ...resolvedCodec === "prores" ? [
        proResProfile === null ? { setter: "setProResProfile", type: "delete" } : {
          setter: "setProResProfile",
          type: "set",
          value: proResProfile
        }
      ] : [],
      ...resolvedCodec === "h264" ? [
        x264Preset === null ? { setter: "setX264Preset", type: "delete" } : {
          setter: "setX264Preset",
          type: "set",
          value: x264Preset
        }
      ] : [],
      audioCodec === null ? { setter: "setAudioCodec", type: "delete" } : { setter: "setAudioCodec", type: "set", value: audioCodec },
      { setter: "setConcurrency", type: "set", value: concurrency }
    ];
    return allUpdates.filter((update) => editedSetters.has(update.setter));
  }, [
    audioCodec,
    codec,
    concurrency,
    crf,
    editedSetters,
    proResProfile,
    qualityMode,
    resolvedCodec,
    scale,
    stillImageFormat,
    videoImageFormat,
    videoBitrate,
    x264Preset
  ]);
  const ready = renderDefaults !== null && syncedRevision === revision;
  useAutoSaveConfig({
    enabled: ready,
    onError: setError,
    ready,
    syncRevision: syncedRevision,
    updates
  });
  const onVideoBitrateChange = (event) => {
    setVideoBitrate(event.target.value);
  };
  const onVideoBitrateBlur = () => {
    markEdited("setCrf");
    markEdited("setVideoBitrate");
  };
  if (renderDefaults === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs138("div", {
    style: container51,
    children: [
      /* @__PURE__ */ jsx227("p", {
        style: dividerLabel2,
        children: "Output"
      }),
      /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Default codec"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: codecValues,
              selectedId: codec ?? REMOTION_DEFAULT,
              title: "Codec"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx227(NumberSetting, {
        name: "Scale",
        value: scale,
        onValueChanged: changeScale,
        min: 0.1,
        step: 0.1
      }),
      /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Still image format"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: stillFormatValues,
              selectedId: stillImageFormat ?? REMOTION_DEFAULT,
              title: "Still image format"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Video frame format"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: videoFormatValues,
              selectedId: videoImageFormat ?? REMOTION_DEFAULT,
              title: "Video frame format"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx227("p", {
        style: sectionTitle,
        children: "Encoding"
      }),
      BrowserSafeApis11.codecSupportsCrf(resolvedCodec) && BrowserSafeApis11.codecSupportsVideoBitrate(resolvedCodec) ? /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Quality control"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(SegmentedControl, {
              items: qualityModeValues,
              needsWrapping: true
            })
          })
        ]
      }) : null,
      qualityMode === "crf" && BrowserSafeApis11.codecSupportsCrf(resolvedCodec) && BrowserSafeApis11.getValidCrfRanges(resolvedCodec) ? /* @__PURE__ */ jsx227(CrfSetting, {
        crf,
        setCrf: changeCrf,
        min: BrowserSafeApis11.getValidCrfRanges(resolvedCodec)[0],
        max: BrowserSafeApis11.getValidCrfRanges(resolvedCodec)[1],
        option: "crfOption"
      }) : null,
      qualityMode === "bitrate" && BrowserSafeApis11.codecSupportsVideoBitrate(resolvedCodec) ? /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Video bitrate"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(RemotionInput, {
              style: { ...input, ...fullWidth },
              value: videoBitrate,
              onChange: onVideoBitrateChange,
              onBlur: onVideoBitrateBlur,
              status: "ok",
              rightAlign: true
            })
          })
        ]
      }) : null,
      resolvedCodec === "prores" ? /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "ProRes profile"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: profileValues,
              selectedId: proResProfile ?? REMOTION_DEFAULT,
              title: "ProRes profile"
            })
          })
        ]
      }) : null,
      resolvedCodec === "h264" ? /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "x264 preset"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: presetValues,
              selectedId: x264Preset ?? REMOTION_DEFAULT,
              title: "x264 preset"
            })
          })
        ]
      }) : null,
      /* @__PURE__ */ jsx227("p", {
        style: sectionTitle,
        children: "Audio"
      }),
      /* @__PURE__ */ jsxs138("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx227("div", {
            style: label,
            children: "Audio codec"
          }),
          /* @__PURE__ */ jsx227("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx227(Combobox, {
              values: audioCodecValues,
              selectedId: audioCodec ?? REMOTION_DEFAULT,
              title: "Audio codec"
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx227("p", {
        style: sectionTitle,
        children: "Performance"
      }),
      /* @__PURE__ */ jsx227(NumberSetting, {
        name: "Concurrency",
        value: concurrency,
        onValueChanged: changeConcurrency,
        min: renderDefaults.minConcurrency,
        max: renderDefaults.maxConcurrency,
        step: 1
      }),
      error ?? settingsError ? /* @__PURE__ */ jsxs138(Fragment62, {
        children: [
          /* @__PURE__ */ jsx227(Spacing, {
            y: 1,
            block: true
          }),
          /* @__PURE__ */ jsx227("div", {
            style: { paddingLeft: 16, paddingRight: 16 },
            children: /* @__PURE__ */ jsx227(ValidationMessage, {
              message: error ?? settingsError ?? "",
              align: "flex-start",
              type: "error"
            })
          })
        ]
      }) : null,
      /* @__PURE__ */ jsx227(Spacing, {
        y: 2,
        block: true
      })
    ]
  });
};

// src/components/SettingsModalFooter.tsx
import { useCallback as useCallback119, useContext as useContext122, useMemo as useMemo125, useState as useState84 } from "react";
import { jsx as jsx228, jsxs as jsxs139, Fragment as Fragment63 } from "react/jsx-runtime";
var footer3 = {
  flex: "none"
};
var footerRow = {
  alignItems: "center",
  display: "flex",
  height: 28,
  justifyContent: "space-between"
};
var configFileHint = {
  alignItems: "center",
  color: LIGHT_TEXT,
  display: "flex",
  fontFamily: "sans-serif",
  fontSize: 13,
  lineHeight: "20px",
  whiteSpace: "nowrap"
};
var configFileName = {
  color: BLUE,
  fontFamily: "inherit",
  fontSize: 13,
  fontWeight: "bold",
  lineHeight: "20px"
};
var externalLinkIndicator2 = {
  display: "inline-block",
  height: 12,
  marginLeft: 4,
  verticalAlign: -2,
  width: 12
};
var SettingsModalFooter = ({ showLicenseFaq }) => {
  const { restartRequired } = useContext122(StudioServerConnectionCtx);
  const [restarting, setRestarting] = useState84(false);
  const configFileLocation = useMemo125(() => {
    return {
      source: "remotion.config.ts",
      line: 1,
      column: 1
    };
  }, []);
  const openLicenseFaq = useCallback119(() => {
    window.open("https://www.remotion.dev/docs/license/faq", "_blank", "noopener,noreferrer");
  }, []);
  const restart = useCallback119(() => {
    setRestarting(true);
    restartStudio().catch((error) => {
      setRestarting(false);
      showNotification(`Could not restart Studio: ${error.message}`, 4000);
    });
  }, []);
  return /* @__PURE__ */ jsx228(ModalFooterContainer, {
    style: footer3,
    children: /* @__PURE__ */ jsxs139("div", {
      style: footerRow,
      children: [
        /* @__PURE__ */ jsx228("div", {
          style: configFileHint,
          children: restartRequired ? "Restart the server to apply changes" : /* @__PURE__ */ jsxs139(Fragment63, {
            children: [
              "Changes save to",
              /* @__PURE__ */ jsx228(Spacing, {
                x: 0.5
              }),
              /* @__PURE__ */ jsx228(InspectorOpenInEditor, {
                locationType: null,
                location: configFileLocation,
                label: /* @__PURE__ */ jsx228("strong", {
                  style: configFileName,
                  children: "remotion.config.ts"
                }),
                showTooltips: false
              })
            ]
          })
        }),
        restartRequired ? /* @__PURE__ */ jsx228(ModalButton, {
          disabled: restarting,
          onClick: restart,
          size: "compact",
          title: "Restart Studio to apply config file changes",
          children: restarting ? "Restarting..." : "Restart Studio"
        }) : showLicenseFaq ? /* @__PURE__ */ jsxs139(InspectorQuickAction, {
          disabled: false,
          onClick: openLicenseFaq,
          style: { flex: "none", width: "fit-content" },
          title: "Open the Remotion License FAQ in a new tab",
          children: [
            "License FAQ",
            /* @__PURE__ */ jsx228("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 16 16",
              style: externalLinkIndicator2,
              children: /* @__PURE__ */ jsx228("path", {
                d: "M4 12 12 4M6 4h6v6",
                fill: "none",
                stroke: CURRENT_COLOR,
                strokeLinecap: "round",
                strokeLinejoin: "round",
                strokeWidth: "1.5"
              })
            })
          ]
        }) : null
      ]
    })
  });
};

// src/components/StudioSettings.tsx
import { BrowserSafeApis as BrowserSafeApis12 } from "@remotion/renderer/client";
import { useCallback as useCallback120, useEffect as useEffect85, useMemo as useMemo126, useState as useState85 } from "react";

// src/components/ConfigSelect.tsx
import { jsx as jsx229, jsxs as jsxs140 } from "react/jsx-runtime";
var DEFAULT_VALUE = "studio-default";
var controlWidth = {
  boxSizing: "border-box",
  width: 180
};
var ConfigSelect = ({
  defaultLabel,
  name,
  onChange,
  options,
  value
}) => {
  const values = [
    {
      id: DEFAULT_VALUE,
      keyHint: null,
      label: `Default (${defaultLabel})`,
      leftItem: value === null ? /* @__PURE__ */ jsx229(Checkmark, {}) : null,
      onClick: () => onChange(null),
      quickSwitcherLabel: null,
      subMenu: null,
      type: "item",
      value: DEFAULT_VALUE
    },
    { id: `${DEFAULT_VALUE}-divider`, type: "divider" },
    ...options.map((option) => {
      const id = `${name}-${String(option.value)}`;
      return {
        id,
        keyHint: null,
        label: option.label,
        leftItem: value === option.value ? /* @__PURE__ */ jsx229(Checkmark, {}) : null,
        onClick: () => onChange(option.value),
        quickSwitcherLabel: null,
        subMenu: null,
        type: "item",
        value: id
      };
    })
  ];
  return /* @__PURE__ */ jsxs140("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsx229("div", {
        style: label,
        children: name
      }),
      /* @__PURE__ */ jsx229("div", {
        style: rightRow,
        children: /* @__PURE__ */ jsx229(Combobox, {
          values,
          selectedId: value === null ? DEFAULT_VALUE : `${name}-${String(value)}`,
          style: controlWidth,
          title: name
        })
      })
    ]
  });
};

// src/components/StudioSettings.tsx
import { jsx as jsx230, jsxs as jsxs141, Fragment as Fragment64 } from "react/jsx-runtime";
var container52 = {
  alignSelf: "flex-start",
  display: "flex",
  flex: 1,
  flexDirection: "column",
  minWidth: 0
};
var dividerLabel3 = {
  ...sectionHeader,
  margin: 0,
  padding: "4px 16px"
};
var sectionTitle2 = {
  ...dividerLabel3,
  marginTop: 8
};
var resetIcon = {
  height: 12,
  width: 12
};
var initialSettings = {
  askAIEnabled: null,
  audioLatencyHint: null,
  beepOnFinish: null,
  enableCrossSiteIsolation: null,
  interactivityEnabled: null,
  keyboardShortcutsEnabled: null,
  logLevel: null,
  maxTimelineTracks: null,
  numberOfSharedAudioTags: null,
  rspack: null
};
var ConfigNumber = ({
  defaultValue,
  name,
  onChange,
  onChangeEnd,
  value
}) => {
  return /* @__PURE__ */ jsxs141("div", {
    style: optionRow,
    children: [
      /* @__PURE__ */ jsx230("div", {
        style: label,
        children: name
      }),
      /* @__PURE__ */ jsxs141("div", {
        style: { ...rightRow, gap: 6 },
        children: [
          /* @__PURE__ */ jsx230(InputDragger, {
            "aria-label": name,
            buttonStyle: { textAlign: "right", width: 140 },
            formatter: () => value === null ? `Default (${defaultValue})` : String(value),
            min: 0,
            onTextChange: () => {
              return;
            },
            onValueChange: onChange,
            onValueChangeEnd: onChangeEnd,
            placeholder: `Default (${defaultValue})`,
            rightAlign: true,
            status: "ok",
            step: 1,
            value: value ?? defaultValue
          }),
          /* @__PURE__ */ jsx230(Button, {
            disabled: value === null,
            onClick: () => onChangeEnd(null),
            size: "compact",
            style: { color: LIGHT_TEXT },
            title: `Use default (${defaultValue})`,
            children: /* @__PURE__ */ jsx230(UndoIcon, {
              style: resetIcon
            })
          })
        ]
      })
    ]
  });
};
var StudioSettings = () => {
  const { error: settingsError, revision, studioRuntimeConfig } = useSettings();
  const [settings, setSettings] = useState85(initialSettings);
  const [committedNumberSettings, setCommittedNumberSettings] = useState85({ numberOfSharedAudioTags: null });
  const [editedSetters, setEditedSetters] = useState85(() => new Set);
  const [syncedRevision, setSyncedRevision] = useState85(-1);
  const [error, setError] = useState85(null);
  useEffect85(() => {
    if (studioRuntimeConfig === null) {
      return;
    }
    setSettings(studioRuntimeConfig.configFileStudioSettings ?? initialSettings);
    setCommittedNumberSettings({
      numberOfSharedAudioTags: studioRuntimeConfig.configFileStudioSettings?.numberOfSharedAudioTags ?? null
    });
    setEditedSetters(new Set);
    setSyncedRevision(revision);
    setError(null);
  }, [revision, studioRuntimeConfig]);
  const changeSetting = useCallback120((key8, setter, value) => {
    setSettings((current) => ({ ...current, [key8]: value }));
    setEditedSetters((current) => new Set(current).add(setter));
  }, []);
  const previewNumberSetting = useCallback120((key8, value) => {
    setSettings((current) => ({ ...current, [key8]: value }));
  }, []);
  const commitNumberSetting = useCallback120((key8, setter, value) => {
    setSettings((current) => ({ ...current, [key8]: value }));
    setCommittedNumberSettings((current) => ({ ...current, [key8]: value }));
    setEditedSetters((current) => new Set(current).add(setter));
  }, []);
  const updates = useMemo126(() => {
    const update = (setter, value) => value === null ? { setter, type: "delete" } : { setter, type: "set", value };
    const updatesForEditedSetters = [
      update("setAskAIEnabled", settings.askAIEnabled),
      update("setEnableCrossSiteIsolation", settings.enableCrossSiteIsolation),
      update("setBeepOnFinish", settings.beepOnFinish),
      update("setAudioLatencyHint", settings.audioLatencyHint),
      update("setNumberOfSharedAudioTags", committedNumberSettings.numberOfSharedAudioTags),
      update("setRspack", settings.rspack),
      update("setInteractivityEnabled", settings.interactivityEnabled),
      update("setLogLevel", settings.logLevel)
    ].filter((item) => editedSetters.has(item.setter));
    if (editedSetters.has("setRspack")) {
      updatesForEditedSetters.push({
        setter: "setExperimentalRspackEnabled",
        type: "delete"
      });
    }
    if (editedSetters.has("setLogLevel")) {
      updatesForEditedSetters.push({ setter: "setLevel", type: "delete" });
    }
    return updatesForEditedSetters;
  }, [committedNumberSettings, editedSetters, settings]);
  const ready = studioRuntimeConfig !== null && syncedRevision === revision;
  useAutoSaveConfig({
    enabled: ready,
    onError: setError,
    ready,
    syncRevision: syncedRevision,
    updates
  });
  if (studioRuntimeConfig === null) {
    return null;
  }
  return /* @__PURE__ */ jsxs141("div", {
    style: container52,
    children: [
      /* @__PURE__ */ jsx230("p", {
        style: dividerLabel3,
        children: "Development"
      }),
      /* @__PURE__ */ jsx230(ConfigSelect, {
        defaultLabel: "Webpack",
        name: "Bundler",
        onChange: (value) => changeSetting("rspack", "setRspack", value),
        options: [
          { label: "Rspack", value: true },
          { label: "Webpack", value: false }
        ],
        value: settings.rspack
      }),
      /* @__PURE__ */ jsxs141("div", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsxs141("div", {
            style: label,
            children: [
              "Cross-site isolation",
              /* @__PURE__ */ jsx230(InfoBubble, {
                title: "About cross-site isolation",
                horizontalAlignment: "right",
                children: /* @__PURE__ */ jsx230("div", {
                  style: {
                    padding: 12,
                    maxWidth: 280,
                    fontSize: 14,
                    lineHeight: 1.5
                  },
                  children: "Isolates Studio from other websites to enable shared memory and multithreading. Required for @remotion/whisper-web and can speed up ProRes decoding. Media from other websites that requires cookies or authentication may not load. Disabled by default."
                })
              })
            ]
          }),
          /* @__PURE__ */ jsx230("label", {
            style: rightRow,
            "aria-label": "Cross-site isolation",
            children: /* @__PURE__ */ jsx230(Checkbox, {
              checked: settings.enableCrossSiteIsolation === true,
              name: "Cross-site isolation",
              onChange: (event) => changeSetting("enableCrossSiteIsolation", "setEnableCrossSiteIsolation", event.target.checked ? true : null)
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx230(ConfigSelect, {
        defaultLabel: "Info",
        name: "Log level",
        onChange: (value) => changeSetting("logLevel", "setLogLevel", value),
        options: BrowserSafeApis12.logLevels.map((value) => ({
          label: value[0].toUpperCase() + value.slice(1),
          value
        })),
        value: settings.logLevel
      }),
      /* @__PURE__ */ jsx230("p", {
        style: sectionTitle2,
        children: "Interface"
      }),
      /* @__PURE__ */ jsxs141("label", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx230("div", {
            style: label,
            children: "Ask AI enabled"
          }),
          /* @__PURE__ */ jsx230("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx230(Checkbox, {
              checked: settings.askAIEnabled !== false,
              name: "Ask AI enabled",
              onChange: (event) => changeSetting("askAIEnabled", "setAskAIEnabled", event.target.checked ? null : false)
            })
          })
        ]
      }),
      /* @__PURE__ */ jsxs141("label", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx230("div", {
            style: label,
            children: "Interactivity enabled"
          }),
          /* @__PURE__ */ jsx230("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx230(Checkbox, {
              checked: settings.interactivityEnabled !== false,
              name: "Interactivity enabled",
              onChange: (event) => changeSetting("interactivityEnabled", "setInteractivityEnabled", event.target.checked ? null : false)
            })
          })
        ]
      }),
      /* @__PURE__ */ jsx230("p", {
        style: sectionTitle2,
        children: "Audio"
      }),
      /* @__PURE__ */ jsx230(ConfigSelect, {
        defaultLabel: "Playback",
        name: "Audio latency hint",
        onChange: (value) => changeSetting("audioLatencyHint", "setAudioLatencyHint", value),
        options: [
          { label: "Interactive", value: "interactive" },
          { label: "Balanced", value: "balanced" },
          { label: "Playback", value: "playback" }
        ],
        value: settings.audioLatencyHint
      }),
      /* @__PURE__ */ jsx230(ConfigNumber, {
        defaultValue: 0,
        name: "Number of shared audio tags",
        onChange: (value) => previewNumberSetting("numberOfSharedAudioTags", value),
        onChangeEnd: (value) => commitNumberSetting("numberOfSharedAudioTags", "setNumberOfSharedAudioTags", value),
        value: settings.numberOfSharedAudioTags
      }),
      /* @__PURE__ */ jsxs141("label", {
        style: optionRow,
        children: [
          /* @__PURE__ */ jsx230("div", {
            style: label,
            children: "Beep on finish"
          }),
          /* @__PURE__ */ jsx230("div", {
            style: rightRow,
            children: /* @__PURE__ */ jsx230(Checkbox, {
              checked: settings.beepOnFinish === true,
              name: "Beep on finish",
              onChange: (event) => changeSetting("beepOnFinish", "setBeepOnFinish", event.target.checked ? true : null)
            })
          })
        ]
      }),
      error ?? settingsError ? /* @__PURE__ */ jsxs141(Fragment64, {
        children: [
          /* @__PURE__ */ jsx230(Spacing, {
            y: 1,
            block: true
          }),
          /* @__PURE__ */ jsx230("div", {
            style: { paddingLeft: 16, paddingRight: 16 },
            children: /* @__PURE__ */ jsx230(ValidationMessage, {
              align: "flex-start",
              message: error ?? settingsError ?? "",
              type: "error"
            })
          })
        ]
      }) : null,
      /* @__PURE__ */ jsx230(Spacing, {
        y: 2,
        block: true
      })
    ]
  });
};

// src/components/UpdatesSettings.tsx
import { useCallback as useCallback122, useEffect as useEffect86, useMemo as useMemo128, useRef as useRef66, useState as useState87 } from "react";

// src/components/OpenIssueButton.tsx
import { useCallback as useCallback121, useMemo as useMemo127, useState as useState86 } from "react";
import { jsx as jsx231 } from "react/jsx-runtime";
var svgStyle = {
  width: "11px",
  height: "11px"
};
var buttonStyle11 = {
  border: "none",
  width: "24px",
  height: "24px",
  display: "flex",
  justifyContent: "center",
  alignItems: "center"
};
var OpenIssueButton = ({ link }) => {
  const [hovered, setHovered] = useState86(false);
  const buttonTooltip = `Open GitHub issue in new Tab`;
  const handleClick = useCallback121(() => {
    window.open(link, "_blank");
  }, [link]);
  const svgFillColor = useMemo127(() => {
    return hovered ? WHITE : LIGHT_TEXT;
  }, [hovered]);
  const openInEditorSvg = /* @__PURE__ */ jsx231("svg", {
    viewBox: "0 0 512 512",
    style: svgStyle,
    children: /* @__PURE__ */ jsx231("path", {
      fill: svgFillColor,
      d: "M320 0c-17.7 0-32 14.3-32 32s14.3 32 32 32h82.7L201.4 265.4c-12.5 12.5-12.5 32.8 0 45.3s32.8 12.5 45.3 0L448 109.3V192c0 17.7 14.3 32 32 32s32-14.3 32-32V32c0-17.7-14.3-32-32-32H320zM80 32C35.8 32 0 67.8 0 112V432c0 44.2 35.8 80 80 80H400c44.2 0 80-35.8 80-80V320c0-17.7-14.3-32-32-32s-32 14.3-32 32V432c0 8.8-7.2 16-16 16H80c-8.8 0-16-7.2-16-16V112c0-8.8 7.2-16 16-16H192c17.7 0 32-14.3 32-32s-14.3-32-32-32H80z"
    })
  });
  const onPointerEnter = useCallback121(() => {
    setHovered(true);
  }, []);
  const onPointerLeave = useCallback121(() => {
    setHovered(false);
  }, []);
  return /* @__PURE__ */ jsx231("button", {
    title: buttonTooltip,
    type: "button",
    onPointerEnter,
    onPointerLeave,
    style: buttonStyle11,
    onClick: handleClick,
    children: openInEditorSvg
  });
};

// src/components/KnownBugs.tsx
import { jsx as jsx232, jsxs as jsxs142 } from "react/jsx-runtime";
var container53 = {
  display: "flex",
  flexDirection: "row",
  alignItems: "center"
};
var text6 = {
  fontSize: 14,
  flex: 1
};
var KnownBugs = ({ bugs }) => {
  const bugElements = bugs.map((bug) => {
    return /* @__PURE__ */ jsxs142("div", {
      style: container53,
      children: [
        /* @__PURE__ */ jsxs142("div", {
          style: text6,
          children: [
            "\uD83E\uDEB2 ",
            bug.title
          ]
        }),
        /* @__PURE__ */ jsx232(OpenIssueButton, {
          link: bug.link
        })
      ]
    }, bug.description + bug.link);
  });
  return /* @__PURE__ */ jsx232("div", {
    children: bugElements
  });
};

// src/components/UpdatesSettings.tsx
import { jsx as jsx233, jsxs as jsxs143, Fragment as Fragment65 } from "react/jsx-runtime";
var container54 = {
  alignSelf: "flex-start",
  boxSizing: "border-box",
  flex: 1,
  minWidth: 0,
  padding: "4px 16px 0"
};
var text7 = {
  color: LIGHT_TEXT,
  fontFamily: "sans-serif",
  fontSize: 14,
  lineHeight: 1.5
};
var title3 = {
  paddingTop: 12,
  paddingBottom: 8,
  ...text7
};
var titleBeforeCommand = {
  ...title3,
  paddingBottom: 0
};
var commandField3 = {
  alignItems: "center",
  background: SELECTED_BACKGROUND,
  borderRadius: 6,
  boxSizing: "border-box",
  display: "flex",
  marginBottom: 10,
  marginTop: 10,
  padding: "8px 8px 8px 10px",
  width: "100%"
};
var code2 = {
  color: WHITE,
  flex: 1,
  fontFamily: "monospace",
  fontSize: 14,
  lineHeight: 1.5,
  margin: 0,
  minWidth: 0,
  overflowX: "auto",
  whiteSpace: "pre"
};
var copyIcon3 = {
  flexShrink: 0,
  height: 12,
  width: 12
};
var link = {
  ...text7,
  fontWeight: "bold",
  color: BLUE,
  textDecoration: "none"
};
var releaseNotesFrame = {
  backgroundColor: BACKGROUND,
  border: 0,
  display: "block",
  marginTop: 8,
  overflow: "hidden",
  width: "100%"
};
var releaseNotesTitle = {
  ...title3,
  borderBottom: BORDER_WHITE_ALPHA_12,
  width: "100%"
};
var commands = {
  npm: "npx remotion upgrade",
  yarn: "yarn remotion upgrade",
  pnpm: "pnpm exec remotion upgrade",
  bun: "bun remotion upgrade",
  nub: "nubx remotion upgrade",
  unknown: "npx remotion upgrade"
};
var skillsUpdateCommand = "npx remotion skills update";
var remotionUpgradeSkill = "/remotion-upgrade";
var formatReleaseDate = (publishedAt) => {
  if (publishedAt === null) {
    return null;
  }
  const date = new Date(publishedAt);
  if (Number.isNaN(date.getTime())) {
    return null;
  }
  const day = date.getDate();
  const lastTwoDigits = day % 100;
  const suffix2 = lastTwoDigits >= 11 && lastTwoDigits <= 13 ? "th" : day % 10 === 1 ? "st" : day % 10 === 2 ? "nd" : day % 10 === 3 ? "rd" : "th";
  const month = new Intl.DateTimeFormat("en-US", {
    month: "long"
  }).format(date);
  return `${month} ${day}${suffix2}, ${date.getFullYear()}`;
};
var RenderedReleaseNotes = ({ release }) => {
  const [height, setHeight] = useState87(220);
  const frameRef = useRef66(null);
  const resizeObserver = useRef66(null);
  const formattedReleaseDate = formatReleaseDate(release.publishedAt);
  const document2 = useMemo128(() => {
    if (release.releaseNotesHtml === null) {
      return null;
    }
    return `<!doctype html><html><head><base href="https://github.com/remotion-dev/remotion/" target="_blank"><style>
			:root { color-scheme: dark; }
			html, body { overflow: hidden; }
			body { background: ${BACKGROUND}; color: ${LIGHT_TEXT}; font-family: sans-serif; font-size: 13px; line-height: 1.5; margin: 0; overflow-wrap: anywhere; }
			h1, h2, h3, h4, h5, h6 { color: ${WHITE}; line-height: 1.25; margin: 16px 0 8px; }
			h1:first-child, h2:first-child, h3:first-child { margin-top: 0; }
			h1 { font-size: 18px; } h2 { font-size: 16px; } h3, h4, h5, h6 { font-size: 14px; }
			p, ul, ol, pre, blockquote { margin: 8px 0; }
			ul, ol { padding-left: 22px; }
			a { color: ${BLUE}; text-decoration: none; }
			code { background: transparent; color: ${LIGHT_TEXT}; font-family: monospace; padding: 0; }
			pre { background: ${SELECTED_BACKGROUND}; border-radius: 6px; overflow-x: auto; padding: 10px; }
			pre code { background: transparent; padding: 0; }
			img { max-width: 100%; }
			blockquote { border-left: 3px solid ${SELECTED_BACKGROUND}; margin-left: 0; padding-left: 10px; }
			hr { border: 0; border-top: 1px solid ${SELECTED_BACKGROUND}; }
		</style></head><body>${release.releaseNotesHtml}</body></html>`;
  }, [release.releaseNotesHtml]);
  useEffect86(() => {
    return () => resizeObserver.current?.disconnect();
  }, []);
  const onLoad = useCallback122(() => {
    resizeObserver.current?.disconnect();
    const iframeDocument = frameRef.current?.contentDocument;
    if (!iframeDocument?.body) {
      return;
    }
    const updateHeight = () => {
      setHeight(iframeDocument.documentElement.scrollHeight + 2);
    };
    updateHeight();
    const observer = new ResizeObserver(updateHeight);
    observer.observe(iframeDocument.body);
    resizeObserver.current = observer;
  }, []);
  return /* @__PURE__ */ jsxs143(Fragment65, {
    children: [
      /* @__PURE__ */ jsxs143("div", {
        style: releaseNotesTitle,
        children: [
          "v",
          release.version,
          formattedReleaseDate === null ? null : ` – ${formattedReleaseDate}`
        ]
      }),
      document2 === null ? /* @__PURE__ */ jsxs143("div", {
        style: text7,
        children: [
          "Release notes could not be loaded.",
          " ",
          /* @__PURE__ */ jsx233("a", {
            style: link,
            target: "_blank",
            href: `https://github.com/remotion-dev/remotion/releases/tag/v${release.version}`,
            children: "View them on GitHub"
          }),
          "."
        ]
      }) : /* @__PURE__ */ jsx233("iframe", {
        ref: frameRef,
        onLoad,
        sandbox: "allow-popups allow-popups-to-escape-sandbox allow-same-origin",
        scrolling: "no",
        srcDoc: document2,
        style: { ...releaseNotesFrame, height },
        title: `Release notes for Remotion v${release.version}`
      })
    ]
  });
};
var UpdatesSettings = () => {
  const { remotionSkillsInfo } = useSettings();
  const { error, info, knownBugs, upgradeState, upgradeError, upgrade, restart } = useUpdateStatus();
  const [releaseNotes, setReleaseNotes] = useState87(null);
  useEffect86(() => {
    if (!info?.updateAvailable) {
      return;
    }
    const controller = new AbortController;
    getReleaseNotes(info.currentVersion, info.latestVersion, controller.signal).then((response) => {
      setReleaseNotes({
        ...response,
        currentVersion: info.currentVersion,
        latestVersion: info.latestVersion
      });
    }).catch(() => {
      if (!controller.signal.aborted) {
        setReleaseNotes({
          currentVersion: info.currentVersion,
          hasMore: false,
          latestVersion: info.latestVersion,
          releases: []
        });
      }
    });
    return () => controller.abort();
  }, [info]);
  const hasKnownBugs = useMemo128(() => {
    return (knownBugs?.length ?? 0) > 0;
  }, [knownBugs]);
  const updateAction = info ? remotionSkillsInfo?.remotionUpgradeSkillAvailable ? remotionUpgradeSkill : info.updateAvailable ? commands[info.packageManager] : skillsUpdateCommand : null;
  const updateActionType = remotionSkillsInfo?.remotionUpgradeSkillAvailable ? "skill" : "command";
  const { copied, markCopied } = useCopyFeedback();
  const onClick = useCallback122(() => {
    if (updateAction === null) {
      return;
    }
    copyText(updateAction).then(markCopied).catch((err) => {
      showNotification("Could not copy: " + err.message, 2000);
    });
  }, [markCopied, updateAction]);
  const renderCopyAction = useCallback122((color) => {
    return /* @__PURE__ */ jsx233(CopyIcon, {
      copied,
      color,
      style: copyIcon3
    });
  }, [copied]);
  const onUpgrade = useCallback122(() => {
    if (info) {
      upgrade(info.latestVersion);
    }
  }, [info, upgrade]);
  if (upgradeState !== "idle") {
    return /* @__PURE__ */ jsxs143("div", {
      style: container54,
      children: [
        /* @__PURE__ */ jsx233("div", {
          style: title3,
          children: upgradeState === "upgrading" ? "Upgrading Remotion..." : "Remotion has been upgraded."
        }),
        /* @__PURE__ */ jsx233("div", {
          style: text7,
          children: upgradeState === "upgrading" ? "This may take a few minutes. You can follow the progress in your terminal." : upgradeState === "restarting" ? "Studio is restarting with the new version." : "Restart Studio to use the new version."
        }),
        upgradeState === "upgraded" || upgradeState === "restarting" ? /* @__PURE__ */ jsx233("div", {
          style: { marginTop: 12 },
          children: /* @__PURE__ */ jsx233(ModalButton, {
            onClick: restart,
            disabled: upgradeState === "restarting",
            children: upgradeState === "restarting" ? "Restarting..." : "Restart Studio"
          })
        }) : null,
        upgradeError ? /* @__PURE__ */ jsx233(ValidationMessage, {
          message: upgradeError,
          type: "error",
          align: "flex-start"
        }) : null
      ]
    });
  }
  if (info === null) {
    return /* @__PURE__ */ jsx233("div", {
      style: container54,
      children: error ? /* @__PURE__ */ jsx233("div", {
        style: { paddingTop: 12 },
        children: /* @__PURE__ */ jsx233(ValidationMessage, {
          message: "Could not check for updates: " + error,
          align: "flex-start",
          type: "error"
        })
      }) : /* @__PURE__ */ jsx233("div", {
        style: title3,
        children: "Checking for updates..."
      })
    });
  }
  if (info.timedOut && !info.skillsUpdateAvailable) {
    return /* @__PURE__ */ jsxs143("div", {
      style: container54,
      children: [
        /* @__PURE__ */ jsx233("div", {
          style: title3,
          children: "Could not check for updates."
        }),
        /* @__PURE__ */ jsx233("div", {
          style: text7,
          children: "The update check timed out. Try again later."
        })
      ]
    });
  }
  if (!info.updateAvailable && !info.skillsUpdateAvailable) {
    return /* @__PURE__ */ jsxs143("div", {
      style: container54,
      children: [
        /* @__PURE__ */ jsxs143("div", {
          style: title3,
          children: [
            "You",
            "'re",
            " up to date."
          ]
        }),
        /* @__PURE__ */ jsxs143("div", {
          style: text7,
          children: [
            "Remotion v",
            info.currentVersion,
            " and your Remotion Agent Skills are up to date."
          ]
        })
      ]
    });
  }
  return /* @__PURE__ */ jsxs143("div", {
    style: container54,
    children: [
      info.updateAvailable ? /* @__PURE__ */ jsx233("div", {
        style: title3,
        children: "A new Remotion update is available."
      }) : null,
      info.updateAvailable && info.packageManager !== "unknown" ? /* @__PURE__ */ jsx233("div", {
        children: /* @__PURE__ */ jsxs143(ModalButton, {
          onClick: onUpgrade,
          children: [
            "Upgrade to v",
            info.latestVersion
          ]
        })
      }) : null,
      upgradeError ? /* @__PURE__ */ jsx233(ValidationMessage, {
        message: upgradeError,
        type: "error",
        align: "flex-start"
      }) : null,
      hasKnownBugs && info.updateAvailable ? /* @__PURE__ */ jsxs143(Fragment65, {
        children: [
          /* @__PURE__ */ jsxs143("div", {
            style: title3,
            children: [
              "The currently installed version v",
              info.currentVersion,
              " has the following known bugs:"
            ]
          }),
          /* @__PURE__ */ jsx233(KnownBugs, {
            bugs: knownBugs ?? []
          }),
          /* @__PURE__ */ jsx233("div", {
            style: { height: "20px" }
          }),
          /* @__PURE__ */ jsxs143("div", {
            style: text7,
            children: [
              "Or run the following ",
              updateActionType,
              ":"
            ]
          })
        ]
      }) : info.updateAvailable ? /* @__PURE__ */ jsxs143("div", {
        style: titleBeforeCommand,
        children: [
          "You can also run the following ",
          updateActionType,
          ":"
        ]
      }) : /* @__PURE__ */ jsxs143("div", {
        style: titleBeforeCommand,
        children: [
          "Your Remotion Agent Skills are out of date. Run the following",
          " ",
          updateActionType,
          ":"
        ]
      }),
      /* @__PURE__ */ jsxs143("div", {
        style: commandField3,
        children: [
          /* @__PURE__ */ jsx233("pre", {
            style: code2,
            children: updateAction
          }),
          /* @__PURE__ */ jsx233(InlineAction, {
            variant: null,
            onClick,
            renderAction: renderCopyAction,
            title: "Copy command"
          })
        ]
      }),
      info.updateAvailable ? /* @__PURE__ */ jsxs143("div", {
        style: text7,
        children: [
          "You",
          "'re",
          " currently running Remotion v",
          info.currentVersion,
          "."
        ]
      }) : null,
      info.updateAvailable ? releaseNotes?.currentVersion === info.currentVersion && releaseNotes.latestVersion === info.latestVersion ? releaseNotes.releases.length === 0 ? /* @__PURE__ */ jsxs143(Fragment65, {
        children: [
          /* @__PURE__ */ jsxs143("div", {
            style: releaseNotesTitle,
            children: [
              "v",
              info.latestVersion
            ]
          }),
          /* @__PURE__ */ jsxs143("div", {
            style: text7,
            children: [
              "Release notes could not be loaded.",
              " ",
              /* @__PURE__ */ jsx233("a", {
                style: link,
                target: "_blank",
                href: "https://github.com/remotion-dev/remotion/releases",
                children: "View them on GitHub"
              }),
              "."
            ]
          })
        ]
      }) : /* @__PURE__ */ jsxs143(Fragment65, {
        children: [
          releaseNotes.releases.map((release) => /* @__PURE__ */ jsx233(RenderedReleaseNotes, {
            release
          }, release.version)),
          releaseNotes.hasMore ? /* @__PURE__ */ jsxs143("div", {
            style: { ...text7, paddingTop: 12 },
            children: [
              "Showing the 5 most recent releases.",
              " ",
              /* @__PURE__ */ jsx233("a", {
                style: link,
                target: "_blank",
                href: "https://github.com/remotion-dev/remotion/releases",
                children: "View all releases on GitHub"
              }),
              "."
            ]
          }) : null
        ]
      }) : /* @__PURE__ */ jsx233("div", {
        style: text7,
        children: "Loading release notes..."
      }) : null
    ]
  });
};

// src/components/SettingsModal.tsx
import { jsx as jsx234, jsxs as jsxs144, Fragment as Fragment66 } from "react/jsx-runtime";
var hiddenPanel = {
  display: "none"
};
var settingsOptionsPanel = {
  ...optionsPanel,
  boxSizing: "border-box",
  paddingBottom: 16
};
var settingsLeftSidebar = {
  ...leftSidebar,
  paddingLeft: 8
};
var appsIcon = {
  ...icon,
  flexShrink: 0,
  height: 24,
  width: 24
};
var skillsIcon2 = {
  ...icon,
  flexShrink: 0,
  height: 20,
  width: 20
};
var keyboardIcon = {
  ...icon,
  height: 16,
  width: 16
};
var SettingsModal = ({ initialPublicLicenseKey, initialTab }) => {
  const { setSelectedModal } = useContext123(SetSelectedModalContext);
  const { setPublicLicenseKey } = useSettings();
  const { previewServerState } = useContext123(StudioServerConnectionCtx);
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const { upgradeState } = useUpdateStatus();
  const showUpdates = upgradeState !== "idle" || canShowUpdates({
    connectionStatus: previewServerState.type,
    isBrowserStudio,
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  const packageManager = window.remotion_packageManager === "unknown" ? null : window.remotion_packageManager;
  const previewServerConnected = previewServerState.type === "connected";
  const canEditConfig = canEditStudioConfig({
    isBrowserStudio,
    previewServerConnected,
    readOnlyStudio: window.remotion_isReadOnlyStudio
  });
  const availableTabs = useMemo129(() => getAvailableSettingsTabs({
    isBrowserStudio,
    packageManagerAvailable: packageManager !== null,
    previewServerConnected,
    readOnlyStudio: window.remotion_isReadOnlyStudio,
    showUpdates
  }), [isBrowserStudio, packageManager, previewServerConnected, showUpdates]);
  const safeInitialTab = getSafeSettingsTab(initialTab, availableTabs);
  const [tab, setTab] = useState88(safeInitialTab);
  const [openedTabs, setOpenedTabs] = useState88([safeInitialTab]);
  const [packagesFooterContainer, setPackagesFooterContainer] = useState88(null);
  const dismiss = useCallback123(() => {
    setSelectedModal(null);
  }, [setSelectedModal]);
  const selectTab = useCallback123((newTab) => {
    setTab(newTab);
    setOpenedTabs((currentOpenedTabs) => {
      if (currentOpenedTabs.includes(newTab)) {
        return currentOpenedTabs;
      }
      return [...currentOpenedTabs, newTab];
    });
  }, []);
  useLayoutEffect28(() => {
    const safeTab = getSafeSettingsTab(tab, availableTabs);
    if (safeTab !== tab) {
      selectTab(safeTab);
    }
  }, [availableTabs, selectTab, tab]);
  useEffect87(() => {
    setPublicLicenseKey(initialPublicLicenseKey);
  }, [initialPublicLicenseKey, setPublicLicenseKey]);
  return /* @__PURE__ */ jsx234(DismissableModal, {
    panelStyle: outerModalStyle,
    children: /* @__PURE__ */ jsxs144(Fragment66, {
      children: [
        /* @__PURE__ */ jsx234(ModalHeader, {
          title: "Settings",
          onClose: dismiss
        }),
        /* @__PURE__ */ jsxs144("div", {
          style: horizontalLayout,
          children: [
            /* @__PURE__ */ jsxs144("div", {
              style: settingsLeftSidebar,
              children: [
                availableTabs.includes("studio") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "studio",
                  onClick: () => selectTab("studio"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(RemotionTriangleIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "Studio"
                }) : null,
                availableTabs.includes("rendering") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "rendering",
                  onClick: () => selectTab("rendering"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(FilmIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "Defaults"
                }) : null,
                /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "shortcuts",
                  onClick: () => selectTab("shortcuts"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(KeyboardIcon, {
                      color,
                      style: keyboardIcon
                    })
                  }),
                  children: "Shortcuts"
                }),
                availableTabs.includes("packages") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "packages",
                  onClick: () => selectTab("packages"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(PackageIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "Packages"
                }) : null,
                availableTabs.includes("skills") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "skills",
                  onClick: () => selectTab("skills"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(SkillsIcon, {
                      color,
                      style: skillsIcon2
                    })
                  }),
                  children: "Skills"
                }) : null,
                availableTabs.includes("models") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "models",
                  onClick: () => selectTab("models"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(ModelsIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "Models"
                }) : null,
                availableTabs.includes("apps") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "apps",
                  onClick: () => selectTab("apps"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(AppsIcon, {
                      color,
                      style: appsIcon
                    })
                  }),
                  children: "Apps"
                }) : null,
                availableTabs.includes("license") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "license",
                  onClick: () => selectTab("license"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(LicenseIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "License"
                }) : null,
                availableTabs.includes("updates") ? /* @__PURE__ */ jsx234(VerticalTab, {
                  style: horizontalTab,
                  selected: tab === "updates",
                  onClick: () => selectTab("updates"),
                  renderIcon: (color) => /* @__PURE__ */ jsx234("div", {
                    style: iconContainer,
                    children: /* @__PURE__ */ jsx234(CloudDownloadIcon, {
                      color,
                      style: icon
                    })
                  }),
                  children: "Updates"
                }) : null
              ]
            }),
            availableTabs.includes("packages") && openedTabs.includes("packages") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "packages" ? optionsPanel : hiddenPanel,
              children: /* @__PURE__ */ jsx234(InstallPackageSettings, {
                footerContainer: packagesFooterContainer,
                packageManager
              })
            }) : null,
            availableTabs.includes("apps") && openedTabs.includes("apps") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "apps" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(DefaultEditorSettings, {})
            }) : null,
            availableTabs.includes("models") && openedTabs.includes("models") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "models" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(ModelsSettings, {})
            }) : null,
            availableTabs.includes("license") && openedTabs.includes("license") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "license" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(LicenseSettings, {})
            }) : null,
            availableTabs.includes("skills") && openedTabs.includes("skills") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "skills" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(SkillsSettings, {})
            }) : null,
            openedTabs.includes("shortcuts") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "shortcuts" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(KeyboardShortcutsSettings, {})
            }) : null,
            availableTabs.includes("rendering") && openedTabs.includes("rendering") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "rendering" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(RenderingSettings, {})
            }) : null,
            availableTabs.includes("studio") && openedTabs.includes("studio") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "studio" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(StudioSettings, {})
            }) : null,
            availableTabs.includes("updates") && openedTabs.includes("updates") ? /* @__PURE__ */ jsx234("div", {
              style: tab === "updates" ? settingsOptionsPanel : hiddenPanel,
              className: VERTICAL_SCROLLBAR_CLASSNAME,
              children: /* @__PURE__ */ jsx234(UpdatesSettings, {})
            }) : null
          ]
        }),
        tab === "packages" ? /* @__PURE__ */ jsx234("div", {
          ref: setPackagesFooterContainer
        }) : !canEditConfig || tab === "models" || tab === "updates" || tab === "skills" ? null : /* @__PURE__ */ jsx234(SettingsModalFooter, {
          showLicenseFaq: tab === "license"
        })
      ]
    })
  });
};

// src/components/Transcription/TranscriptionModalWithOptionalWhisper.tsx
import React157, { Suspense as Suspense3 } from "react";
import { jsx as jsx235 } from "react/jsx-runtime";
var LazyTranscriptionModal = React157.lazy(async () => {
  const { TranscriptionModal } = await import("./TranscriptionModal-ymnx1c7m.mjs");
  return { default: TranscriptionModal };
});
var TranscriptionModalWithOptionalWhisper = ({ state }) => {
  return /* @__PURE__ */ jsx235(OptionalPackageModal, {
    ariaLabel: "Install transcription package",
    packageName: WHISPER_WEBGPU_PACKAGE,
    title: "Transcribe",
    children: /* @__PURE__ */ jsx235(Suspense3, {
      fallback: null,
      children: /* @__PURE__ */ jsx235(LazyTranscriptionModal, {
        ...state
      })
    })
  });
};

// src/components/VideoMatting/VideoMattingModalWithOptionalPackage.tsx
import React158, { Suspense as Suspense4 } from "react";
import { jsx as jsx236 } from "react/jsx-runtime";
var LazyVideoMattingModal = React158.lazy(async () => {
  const { VideoMattingModal } = await import("./VideoMattingModal-05vpczks.mjs");
  return { default: VideoMattingModal };
});
var VideoMattingModalWithOptionalPackage = ({ state }) => /* @__PURE__ */ jsx236(OptionalPackageModal, {
  ariaLabel: "Install video matting package",
  packageName: VIDEO_MATTING_PACKAGE,
  title: "Track matting",
  children: /* @__PURE__ */ jsx236(Suspense4, {
    fallback: null,
    children: /* @__PURE__ */ jsx236(LazyVideoMattingModal, {
      ...state
    })
  })
});

// src/components/Modals.tsx
import { jsx as jsx237, jsxs as jsxs145, Fragment as Fragment67 } from "react/jsx-runtime";
var Modals = ({ readOnlyStudio }) => {
  const modalContextType = useContext124(SelectedModalContext);
  const { setSelectedModal } = useContext124(SetSelectedModalContext);
  const { previewServerState, subscribeToEvent } = useContext124(StudioServerConnectionCtx);
  const canRender = previewServerState.type === "connected";
  const isBrowserStudio = getBrowserStudioOperations() !== null;
  const confirm = useConfirmationDialog();
  useEffect88(() => {
    if (isBrowserStudio) {
      return;
    }
    return subscribeToEvent("license-key-install-request", (event) => {
      if (event.type !== "license-key-install-request") {
        return;
      }
      setSelectedModal({
        type: "settings",
        initialTab: "license",
        initialPublicLicenseKey: event.licenseKey
      });
    });
  }, [isBrowserStudio, setSelectedModal, subscribeToEvent]);
  useEffect88(() => {
    if (isBrowserStudio) {
      return;
    }
    return subscribeToEvent("element-library-add-request", (event) => {
      if (event.type !== "element-library-add-request") {
        return;
      }
      (async () => {
        const confirmed = await confirm({
          title: "Add Element catalog",
          message: /* @__PURE__ */ jsx237(ElementLibraryAddConfirmation, {
            displayName: event.displayName,
            origin: event.origin,
            url: event.url
          }),
          confirmLabel: "Add catalog",
          cancelLabel: "Cancel"
        });
        if (!confirmed) {
          return;
        }
        if (previewServerState.type !== "connected") {
          showNotification("Could not add catalog: Studio disconnected", 4000);
          return;
        }
        try {
          const result = await callApi("/api/update-config", {
            clientId: previewServerState.clientId,
            updates: [
              {
                setter: "addElementLibrary",
                type: "set",
                value: event.displayName === null ? { url: event.url } : { url: event.url, displayName: event.displayName }
              }
            ]
          });
          if (!result.success) {
            showNotification(`Could not add catalog: ${result.reason}`, 4000);
          }
        } catch (error) {
          showNotification(`Could not add catalog: ${error.message}`, 4000);
        }
      })();
    });
  }, [confirm, isBrowserStudio, previewServerState, subscribeToEvent]);
  return /* @__PURE__ */ jsxs145(Fragment67, {
    children: [
      modalContextType && modalContextType.type === "new-comp" && /* @__PURE__ */ jsx237(NewComposition, {
        folderName: modalContextType.folderName,
        parentName: modalContextType.parentName,
        stack: modalContextType.stack,
        canvasCapture: modalContextType.canvasCapture
      }),
      modalContextType && modalContextType.type === "new-folder" && /* @__PURE__ */ jsx237(NewFolder, {
        parentName: modalContextType.parentName,
        stack: modalContextType.stack
      }),
      modalContextType && modalContextType.type === "duplicate-comp" && /* @__PURE__ */ jsx237(DuplicateComposition, {
        compositionType: modalContextType.compositionType,
        compositionId: modalContextType.compositionId
      }),
      modalContextType && modalContextType.type === "delete-comp" && /* @__PURE__ */ jsx237(DeleteComposition, {
        compositionId: modalContextType.compositionId
      }),
      modalContextType && modalContextType.type === "rename-comp" && /* @__PURE__ */ jsx237(RenameComposition, {
        compositionId: modalContextType.compositionId
      }),
      modalContextType && modalContextType.type === "delete-folder" && /* @__PURE__ */ jsx237(DeleteFolder, {
        folderName: modalContextType.folderName,
        parentName: modalContextType.parentName,
        stack: modalContextType.stack
      }),
      modalContextType && modalContextType.type === "rename-folder" && /* @__PURE__ */ jsx237(RenameFolder, {
        folderName: modalContextType.folderName,
        parentName: modalContextType.parentName,
        stack: modalContextType.stack
      }),
      modalContextType && modalContextType.type === "rename-static-file" && /* @__PURE__ */ jsx237(RenameStaticFileModal, {
        relativePath: modalContextType.relativePath
      }),
      modalContextType && modalContextType.type === "input-props-override" && /* @__PURE__ */ jsx237(OverrideInputPropsModal, {}),
      modalContextType && modalContextType.type === "settings" ? /* @__PURE__ */ jsx237(SettingsModal, {
        initialTab: modalContextType.initialTab,
        initialPublicLicenseKey: modalContextType.initialPublicLicenseKey
      }, `${modalContextType.initialTab}-${modalContextType.initialPublicLicenseKey}`) : null,
      modalContextType && modalContextType.type === "web-render" && /* @__PURE__ */ jsx237(WebRenderModalWithLoader, {
        ...modalContextType
      }),
      modalContextType && modalContextType.type === "server-render" && (canRender || modalContextType.readOnlyStudio) ? /* @__PURE__ */ jsx237(RenderModalWithLoader, {
        readOnlyStudio: modalContextType.readOnlyStudio ?? false,
        initialFrame: modalContextType.initialFrame,
        initialDarkMode: modalContextType.initialDarkMode,
        compositionId: modalContextType.compositionId,
        initialVideoImageFormat: modalContextType.initialVideoImageFormat,
        initialJpegQuality: modalContextType.initialJpegQuality,
        initialScale: modalContextType.initialScale,
        initialLogLevel: modalContextType.initialLogLevel,
        initialOffthreadVideoCacheSizeInBytes: modalContextType.initialOffthreadVideoCacheSizeInBytes,
        initialOffthreadVideoThreads: modalContextType.initialOffthreadVideoThreads,
        initialMediaCacheSizeInBytes: modalContextType.initialMediaCacheSizeInBytes,
        initialConcurrency: modalContextType.initialConcurrency,
        maxConcurrency: modalContextType.maxConcurrency,
        minConcurrency: modalContextType.minConcurrency,
        initialStillImageFormat: modalContextType.initialStillImageFormat,
        initialMuted: modalContextType.initialMuted,
        initialEnforceAudioTrack: modalContextType.initialEnforceAudioTrack,
        initialProResProfile: modalContextType.initialProResProfile,
        initialx264Preset: modalContextType.initialx264Preset,
        initialGopSize: modalContextType.initialGopSize,
        initialPixelFormat: modalContextType.initialPixelFormat,
        initialAudioBitrate: modalContextType.initialAudioBitrate,
        initialVideoBitrate: modalContextType.initialVideoBitrate,
        initialEveryNthFrame: modalContextType.initialEveryNthFrame,
        initialNumberOfGifLoops: modalContextType.initialNumberOfGifLoops,
        initialDelayRenderTimeout: modalContextType.initialDelayRenderTimeout,
        initialEnvVariables: modalContextType.initialEnvVariables,
        initialDisableWebSecurity: modalContextType.initialDisableWebSecurity,
        initialGl: modalContextType.initialOpenGlRenderer,
        initialHeadless: modalContextType.initialHeadless,
        initialIgnoreCertificateErrors: modalContextType.initialIgnoreCertificateErrors,
        initialEncodingBufferSize: modalContextType.initialEncodingBufferSize,
        initialEncodingMaxRate: modalContextType.initialEncodingMaxRate,
        initialUserAgent: modalContextType.initialUserAgent,
        initialColorSpace: modalContextType.initialColorSpace,
        initialMultiProcessOnLinux: modalContextType.initialMultiProcessOnLinux,
        initialRepro: modalContextType.initialRepro,
        initialBeep: modalContextType.initialBeep,
        initialForSeamlessAacConcatenation: modalContextType.initialForSeamlessAacConcatenation,
        defaultProps: modalContextType.defaultProps,
        inFrameMark: modalContextType.inFrameMark,
        outFrameMark: modalContextType.outFrameMark,
        defaultConfigurationAudioCodec: modalContextType.defaultConfigurationAudioCodec,
        defaultConfigurationVideoCodec: modalContextType.defaultConfigurationVideoCodec,
        renderTypeOfLastRender: modalContextType.renderTypeOfLastRender,
        defaultMetadata: modalContextType.defaulMetadata,
        initialHardwareAcceleration: modalContextType.initialHardwareAcceleration,
        initialSampleRate: modalContextType.initialSampleRate,
        initialChromeMode: modalContextType.initialChromeMode,
        renderDefaults: modalContextType.renderDefaults
      }) : null,
      modalContextType && modalContextType.type === "render-progress" && /* @__PURE__ */ jsx237(RenderStatusModal, {
        jobId: modalContextType.jobId
      }),
      modalContextType && modalContextType.type === "transcribe" ? /* @__PURE__ */ jsx237(TranscriptionModalWithOptionalWhisper, {
        state: modalContextType
      }) : null,
      modalContextType && modalContextType.type === "video-matting" ? /* @__PURE__ */ jsx237(VideoMattingModalWithOptionalPackage, {
        state: modalContextType
      }) : null,
      modalContextType && modalContextType.type === "fix-computed-value" && /* @__PURE__ */ jsx237(FixComputedValueModal, {
        state: modalContextType
      }),
      modalContextType && modalContextType.type === "generate-with-agent" ? /* @__PURE__ */ jsx237(GenerateWithAgentModal, {
        state: modalContextType
      }) : null,
      modalContextType && modalContextType.type === "quick-switcher" && /* @__PURE__ */ jsx237(QuickSwitcher_default, {
        readOnlyStudio,
        invocationTimestamp: modalContextType.invocationTimestamp,
        initialMode: modalContextType.mode,
        assetSelection: modalContextType.assetSelection,
        compositionSelection: modalContextType.compositionSelection
      }),
      modalContextType && modalContextType.type === "asset-selection" ? /* @__PURE__ */ jsx237(AssetSelectorModal, {
        readOnlyStudio,
        state: modalContextType
      }) : null,
      modalContextType && modalContextType.type === "element-library" && /* @__PURE__ */ jsx237(ElementLibraryModal, {
        name: modalContextType.name,
        url: modalContextType.url
      }),
      modalContextType && modalContextType.type === "element-install" && /* @__PURE__ */ jsx237(ElementInstallConfirmation, {
        state: modalContextType
      }),
      modalContextType && modalContextType.type === "add-effect" && /* @__PURE__ */ jsx237(EffectPickerModal, {
        state: modalContextType
      }),
      modalContextType && modalContextType.type === "confirmation-dialog" && /* @__PURE__ */ jsx237(ConfirmationDialog, {
        state: modalContextType
      }),
      modalContextType && modalContextType.type === "svg-import-dialog" && /* @__PURE__ */ jsx237(SvgImportDialog, {
        state: modalContextType
      }),
      getStudioAskAIEnabled() && /* @__PURE__ */ jsx237(AskAiModal, {})
    ]
  });
};

// src/components/SequencePropsSubscriptionProvider.tsx
import { useState as useState89, useCallback as useCallback124, useContext as useContext125, useMemo as useMemo130 } from "react";
import { Internals as Internals108 } from "remotion";
import { jsx as jsx238 } from "react/jsx-runtime";
var getReadOnlyOverrideIdToNodePathMappings = (sequences) => {
  return Object.fromEntries(sequences.flatMap((sequence) => {
    const overrideId = sequence.controls?.overrideId;
    if (!overrideId) {
      return [];
    }
    return [
      [
        overrideId,
        {
          absolutePath: "",
          effectKeys: [],
          nodePath: ["readonly-sequence", overrideId],
          sequenceKeys: [],
          videoConfigValues: null
        }
      ]
    ];
  }));
};
var SequencePropsSubscriptionProvider = ({ children }) => {
  const { sequences } = useContext125(Internals108.SequenceManager);
  const [overrideToNodePathMap, setOverrideIdToNodePathMap] = useState89({});
  const readOnlyOverrideToNodePathMap = useMemo130(() => window.remotion_isReadOnlyStudio ? getReadOnlyOverrideIdToNodePathMappings(sequences) : null, [sequences]);
  const setOverrideIdToNodePath = useCallback124((overrideId, state) => {
    setOverrideIdToNodePathMap((prev) => {
      const existing = prev[overrideId];
      if (state === null) {
        if (!existing) {
          return prev;
        }
        const next = { ...prev };
        delete next[overrideId];
        return next;
      }
      if (existing && existing === state) {
        return prev;
      }
      return { ...prev, [overrideId]: state };
    });
  }, []);
  const getters = useMemo130(() => {
    return {
      overrideIdToNodePathMappings: readOnlyOverrideToNodePathMap ?? overrideToNodePathMap
    };
  }, [overrideToNodePathMap, readOnlyOverrideToNodePathMap]);
  const setters = useMemo130(() => {
    return { setOverrideIdToNodePath };
  }, [setOverrideIdToNodePath]);
  return /* @__PURE__ */ jsx238(Internals108.OverrideIdsToNodePathsGettersContext.Provider, {
    value: getters,
    children: /* @__PURE__ */ jsx238(Internals108.OverrideIdsToNodePathsSettersContext.Provider, {
      value: setters,
      children
    })
  });
};

// src/components/Editor.tsx
import { jsx as jsx239, jsxs as jsxs146, Fragment as Fragment68 } from "react/jsx-runtime";
var background = {
  backgroundColor: BACKGROUND,
  display: "flex",
  width: "100%",
  height: "100%",
  flexDirection: "column",
  position: "absolute"
};
var RootCompositionLoader = ({ Root }) => {
  return /* @__PURE__ */ jsxs146(Fragment68, {
    children: [
      /* @__PURE__ */ jsx239(Root, {}),
      /* @__PURE__ */ jsx239(CompositionListRenderMarker, {})
    ]
  });
};
var CompositionListRenderMarker = () => {
  useLayoutEffect29(() => {
    compositionListRenderedRef.current = true;
    return () => {
      compositionListRenderedRef.current = false;
    };
  }, []);
  return null;
};
var Editor = ({ Root, readOnlyStudio }) => {
  useStudioConfigRevision();
  const [drawElement, setDrawElement] = useState90(null);
  const size3 = PlayerInternals17.useElementSize(drawElement, {
    triggerOnWindowResize: false,
    shouldApplyCssTransforms: true
  });
  const [canvasMounted, setCanvasMounted] = React160.useState(false);
  const onMounted = useCallback125(() => {
    setCanvasMounted(true);
  }, []);
  const setDrawRef = useCallback125((node) => {
    drawRef.current = node;
    setDrawElement(node);
  }, []);
  const value = useMemo131(() => {
    if (!size3) {
      return null;
    }
    return getStudioCurrentScaleContext(size3);
  }, [size3]);
  const MemoRoot = useMemo131(() => {
    return React160.memo(Root);
  }, [Root]);
  const [renderError, setRenderError] = useState90(null);
  const clearError = useCallback125(() => {
    setRenderError(null);
  }, []);
  const compositionRenderErrorContextValue = useMemo131(() => ({ setError: setRenderError, clearError }), [clearError]);
  const renderErrorContextValue = useMemo131(() => ({ error: renderError }), [renderError]);
  return /* @__PURE__ */ jsx239(HigherZIndex, {
    onEscape: noop,
    onOutsideClick: noop,
    children: /* @__PURE__ */ jsx239(TimelineZoomContext, {
      children: /* @__PURE__ */ jsxs146(SequencePropsSubscriptionProvider, {
        children: [
          /* @__PURE__ */ jsx239(RefreshCanvasSizeContext.Provider, {
            value: size3?.refresh ?? null,
            children: /* @__PURE__ */ jsxs146(Internals109.CurrentScaleContext.Provider, {
              value,
              children: [
                /* @__PURE__ */ jsx239(ForceSpecificCursor, {}),
                /* @__PURE__ */ jsx239(CanvasCaptureDropHandler, {
                  readOnlyStudio
                }),
                /* @__PURE__ */ jsx239(ScaleLockProvider, {
                  children: /* @__PURE__ */ jsxs146("div", {
                    style: background,
                    children: [
                      /* @__PURE__ */ jsx239(Internals109.CompositionRenderErrorContext.Provider, {
                        value: compositionRenderErrorContextValue,
                        children: canvasMounted ? /* @__PURE__ */ jsx239(RootCompositionLoader, {
                          Root: MemoRoot
                        }) : null
                      }),
                      /* @__PURE__ */ jsx239(Internals109.CanUseRemotionHooksProvider, {
                        children: /* @__PURE__ */ jsx239(RenderErrorContext.Provider, {
                          value: renderErrorContextValue,
                          children: /* @__PURE__ */ jsx239(EditorContent, {
                            readOnlyStudio,
                            children: /* @__PURE__ */ jsx239(TopPanel, {
                              drawRef: setDrawRef,
                              bufferStateDelayInMilliseconds: getStudioBufferStateDelayInMilliseconds(),
                              onMounted,
                              readOnlyStudio
                            })
                          })
                        })
                      })
                    ]
                  })
                })
              ]
            })
          }),
          /* @__PURE__ */ jsx239(Modals, {
            readOnlyStudio
          }),
          /* @__PURE__ */ jsx239(NotificationCenter, {})
        ]
      })
    })
  });
};

// src/components/EditorContexts.tsx
import { PlayerInternals as PlayerInternals18 } from "@remotion/player";

// src/state/preview-size.tsx
import { useCallback as useCallback126, useContext as useContext126, useMemo as useMemo132, useState as useState91 } from "react";
import { Internals as Internals110 } from "remotion";
import { jsx as jsx240 } from "react/jsx-runtime";
var key8 = "remotion.previewSize";
var persistPreviewSizeOption = (option) => {
  localStorage.setItem(key8, JSON.stringify(option));
};
var loadPreviewSizeOption = () => {
  const item = localStorage.getItem(key8);
  if (item === null) {
    return {
      size: "auto",
      translation: {
        x: 0,
        y: 0
      }
    };
  }
  return JSON.parse(item);
};
var PreviewSizeProvider = ({ children }) => {
  const [size3, setSizeState] = useState91(() => loadPreviewSizeOption());
  const [translation, setTranslation] = useState91(() => {
    return {
      x: 0,
      y: 0
    };
  });
  const { editorZoomGestures } = useContext126(EditorZoomGesturesContext);
  const setSize = useCallback126((newValue) => {
    setSizeState((prevState) => {
      const newVal = newValue(prevState);
      persistPreviewSizeOption(newVal);
      return newVal;
    });
  }, []);
  const previewSizeCtx = useMemo132(() => {
    return {
      size: editorZoomGestures ? size3 : {
        size: size3.size,
        translation: {
          x: 0,
          y: 0
        }
      },
      setSize,
      translation,
      setTranslation
    };
  }, [editorZoomGestures, size3, setSize, translation]);
  return /* @__PURE__ */ jsx240(Internals110.PreviewSizeContext.Provider, {
    value: previewSizeCtx,
    children
  });
};

// src/components/CheckerboardProvider.tsx
import { useCallback as useCallback127, useMemo as useMemo133, useState as useState92 } from "react";
import { jsx as jsx241 } from "react/jsx-runtime";
var CheckerboardProvider = ({ children }) => {
  const [checkerboard, setCheckerboardState] = useState92(() => loadCheckerboardOption());
  const setCheckerboard = useCallback127((newValue) => {
    setCheckerboardState((prevState) => {
      const newVal = newValue(prevState);
      persistCheckerboardOption(newVal);
      return newVal;
    });
  }, []);
  const checkerboardCtx = useMemo133(() => {
    return {
      checkerboard,
      setCheckerboard
    };
  }, [checkerboard, setCheckerboard]);
  return /* @__PURE__ */ jsx241(CheckerboardContext.Provider, {
    value: checkerboardCtx,
    children
  });
};

// src/components/MediaVolumeProvider.tsx
import { useMemo as useMemo134, useState as useState93 } from "react";
import { Internals as Internals111 } from "remotion";
import { jsx as jsx242 } from "react/jsx-runtime";
var MediaVolumeProvider = ({ children }) => {
  const [playerMuted, setPlayerMuted] = useState93(() => loadMuteOption());
  const [mediaVolume, setMediaVolume] = useState93(1);
  const mediaVolumeContextValue = useMemo134(() => {
    return {
      playerMuted,
      mediaVolume
    };
  }, [playerMuted, mediaVolume]);
  const setMediaVolumeContextValue = useMemo134(() => {
    return {
      setPlayerMuted,
      setMediaVolume
    };
  }, []);
  return /* @__PURE__ */ jsx242(Internals111.MediaVolumeContext.Provider, {
    value: mediaVolumeContextValue,
    children: /* @__PURE__ */ jsx242(Internals111.SetMediaVolumeContext.Provider, {
      value: setMediaVolumeContextValue,
      children
    })
  });
};

// src/components/ModalsProvider.tsx
import { useMemo as useMemo135, useState as useState94 } from "react";
import { jsx as jsx243 } from "react/jsx-runtime";
var ModalsProvider = ({ children }) => {
  const [selectedModal, setSelectedModal] = useState94(null);
  const setSelectedModalContext = useMemo135(() => {
    return {
      setSelectedModal
    };
  }, []);
  return /* @__PURE__ */ jsx243(SetSelectedModalContext.Provider, {
    value: setSelectedModalContext,
    children: /* @__PURE__ */ jsx243(SelectedModalContext.Provider, {
      value: selectedModal,
      children
    })
  });
};

// src/components/RenderQueue/CaptionQueueProcessorLoader.tsx
import React165, { Suspense as Suspense5, useContext as useContext127 } from "react";
import { jsx as jsx244 } from "react/jsx-runtime";
var LazyCaptionQueueProcessor = React165.lazy(async () => {
  const { CaptionQueueProcessor } = await import("./CaptionQueueProcessor-k0yrm3dj.mjs");
  return { default: CaptionQueueProcessor };
});
var CaptionQueueProcessorLoader = () => {
  const { captionJobs } = useContext127(RenderQueueContext);
  const installed = useOptionalPackageInstalled(WHISPER_WEBGPU_PACKAGE);
  if (!installed || captionJobs.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx244(Suspense5, {
    fallback: null,
    children: /* @__PURE__ */ jsx244(LazyCaptionQueueProcessor, {})
  });
};

// src/components/RenderQueue/ClientRenderQueueProcessor.tsx
import { renderMediaOnWeb, renderStillOnWeb } from "@remotion/web-renderer";
import { useCallback as useCallback128, useContext as useContext128, useEffect as useEffect89 } from "react";
var ClientRenderQueueProcessor = () => {
  const {
    getAbortController,
    getCompositionForJob,
    updateClientJobProgress,
    markClientJobSaving,
    markClientJobDone,
    markClientJobFailed,
    markClientJobCancelled,
    setProcessJobCallback
  } = useContext128(RenderQueueContext);
  const processStillJob = useCallback128(async (job, signal) => {
    const compositionRef = getCompositionForJob(job.id);
    if (!compositionRef) {
      throw new Error(`Composition not found for job ${job.id}`);
    }
    const blob = await (await renderStillOnWeb({
      composition: {
        component: compositionRef.component,
        width: compositionRef.width,
        height: compositionRef.height,
        fps: compositionRef.fps,
        durationInFrames: compositionRef.durationInFrames,
        defaultProps: compositionRef.defaultProps,
        calculateMetadata: compositionRef.calculateMetadata ?? undefined,
        id: job.compositionId
      },
      frame: job.frame,
      inputProps: job.inputProps,
      delayRenderTimeoutInMilliseconds: job.delayRenderTimeout,
      mediaCacheSizeInBytes: job.mediaCacheSizeInBytes,
      logLevel: job.logLevel,
      licenseKey: job.licenseKey ?? undefined,
      scale: job.scale,
      signal,
      allowHtmlInCanvas: job.allowHtmlInCanvas
    })).blob({ format: job.imageFormat });
    return {
      getBlob: () => Promise.resolve(blob),
      width: compositionRef.width,
      height: compositionRef.height
    };
  }, [getCompositionForJob]);
  const processVideoJob = useCallback128(async (job, signal, onProgress) => {
    const compositionRef = getCompositionForJob(job.id);
    if (!compositionRef) {
      throw new Error(`Composition not found for job ${job.id}`);
    }
    const totalFrames = job.endFrame - job.startFrame + 1;
    const { getBlob } = await renderMediaOnWeb({
      composition: {
        component: compositionRef.component,
        width: compositionRef.width,
        height: compositionRef.height,
        fps: compositionRef.fps,
        durationInFrames: compositionRef.durationInFrames,
        defaultProps: compositionRef.defaultProps,
        calculateMetadata: compositionRef.calculateMetadata ?? undefined,
        id: job.compositionId
      },
      inputProps: job.inputProps,
      delayRenderTimeoutInMilliseconds: job.delayRenderTimeout,
      mediaCacheSizeInBytes: job.mediaCacheSizeInBytes,
      logLevel: job.logLevel,
      videoCodec: job.videoCodec ?? undefined,
      audioCodec: job.audioCodec,
      audioBitrate: job.audioBitrate,
      container: job.container,
      videoBitrate: job.videoBitrate,
      hardwareAcceleration: job.hardwareAcceleration,
      keyframeIntervalInSeconds: job.keyframeIntervalInSeconds,
      frameRange: [job.startFrame, job.endFrame],
      transparent: job.transparent,
      muted: job.muted,
      scale: job.scale,
      signal,
      onProgress: (progress) => {
        onProgress(job.id, {
          encodedFrames: progress.encodedFrames,
          totalFrames,
          doneIn: progress.doneIn,
          renderEstimatedTime: progress.renderEstimatedTime,
          progress: progress.progress
        });
      },
      outputTarget: null,
      licenseKey: job.licenseKey ?? undefined,
      pageResponsiveness: job.pageResponsiveness,
      allowHtmlInCanvas: job.allowHtmlInCanvas
    });
    return {
      getBlob,
      width: compositionRef.width,
      height: compositionRef.height
    };
  }, [getCompositionForJob]);
  const processJob = useCallback128(async (job) => {
    const abortController = getAbortController(job.id);
    try {
      let result;
      if (job.type === "client-still") {
        result = await processStillJob(job, abortController.signal);
      } else if (job.type === "client-video") {
        result = await processVideoJob(job, abortController.signal, updateClientJobProgress);
      } else {
        throw new Error(`Unknown job type`);
      }
      const blob = await result.getBlob();
      const metadata = {
        width: result.width,
        height: result.height,
        sizeInBytes: blob.size
      };
      markClientJobSaving(job.id);
      const getBlob = () => Promise.resolve(blob);
      const downloadAndFinish = () => {
        downloadBlob(blob, job.outName);
        markClientJobDone(job.id, metadata, getBlob);
      };
      if (window.remotion_isReadOnlyStudio || getBrowserStudioOperations() !== null) {
        downloadAndFinish();
      } else {
        try {
          await saveOutputFile({ blob, filePath: job.outName });
          await registerClientRender({
            id: job.id,
            type: job.type,
            compositionId: job.compositionId,
            outName: job.outName,
            startedAt: job.startedAt,
            deletedOutputLocation: false,
            metadata
          });
          markClientJobDone(job.id, metadata);
        } catch (err) {
          console.error("Failed to save render output, falling back to browser download.", err);
          downloadAndFinish();
        }
      }
    } catch (err) {
      if (abortController.signal.aborted) {
        markClientJobCancelled(job.id);
      } else {
        markClientJobFailed(job.id, err);
      }
    }
  }, [
    getAbortController,
    processStillJob,
    processVideoJob,
    updateClientJobProgress,
    markClientJobSaving,
    markClientJobDone,
    markClientJobFailed,
    markClientJobCancelled
  ]);
  useEffect89(() => {
    setProcessJobCallback(processJob);
    return () => setProcessJobCallback(null);
  }, [processJob, setProcessJobCallback]);
  return null;
};

// src/components/RenderQueue/VideoMattingQueueProcessorLoader.tsx
import React166, { Suspense as Suspense6, useContext as useContext129 } from "react";
import { jsx as jsx245 } from "react/jsx-runtime";
var LazyProcessor = React166.lazy(async () => {
  const { VideoMattingQueueProcessor } = await import("./VideoMattingQueueProcessor-t1hgm2rm.mjs");
  return { default: VideoMattingQueueProcessor };
});
var VideoMattingQueueProcessorLoader = () => {
  const installed = useOptionalPackageInstalled(VIDEO_MATTING_PACKAGE);
  const { videoMattingJobs } = useContext129(RenderQueueContext);
  if (!installed || videoMattingJobs.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx245(Suspense6, {
    fallback: null,
    children: /* @__PURE__ */ jsx245(LazyProcessor, {})
  });
};

// src/components/SetTimelineInOutProvider.tsx
import { useEffect as useEffect90, useMemo as useMemo136, useState as useState95 } from "react";

// src/state/marks.ts
var localStorageKey3 = () => `remotion.editor.marksv2`;
var persistMarks = (marks) => {
  localStorage.setItem(localStorageKey3(), JSON.stringify(marks));
};
var loadMarks = () => {
  const item = localStorage.getItem(localStorageKey3());
  if (item === null) {
    return {};
  }
  return JSON.parse(item);
};

// src/components/SetTimelineInOutProvider.tsx
import { jsx as jsx246 } from "react/jsx-runtime";
var SetTimelineInOutProvider = ({ children }) => {
  const [inAndOutFrames, setInAndOutFrames] = useState95(() => loadMarks());
  const setTimelineInOutContextValue = useMemo136(() => {
    return {
      setInAndOutFrames
    };
  }, []);
  useEffect90(() => {
    persistMarks(inAndOutFrames);
  }, [inAndOutFrames]);
  return /* @__PURE__ */ jsx246(TimelineInOutContext.Provider, {
    value: inAndOutFrames,
    children: /* @__PURE__ */ jsx246(SetTimelineInOutContext.Provider, {
      value: setTimelineInOutContextValue,
      children
    })
  });
};

// src/components/ShowGuidesProvider.tsx
import { useCallback as useCallback129, useMemo as useMemo137, useRef as useRef67, useState as useState96 } from "react";
import { jsx as jsx247 } from "react/jsx-runtime";
var ShowGuidesProvider = ({ children }) => {
  const [guidesList, setGuidesList] = useState96(() => loadGuidesList());
  const [draggingGuideId, setDraggingGuideId] = useState96(null);
  const moveGuidePointerRef = useRef67(null);
  const [hoveredGuideId, setHoveredGuideId] = useState96(null);
  const [editorShowGuides, setEditorShowGuidesState] = useState96(() => loadEditorShowGuidesOption());
  const shouldCreateGuideRef = useRef67(false);
  const shouldDeleteGuideRef = useRef67(false);
  const setEditorShowGuides = useCallback129((newValue) => {
    setEditorShowGuidesState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorShowGuidesOption(newVal);
      return newVal;
    });
  }, []);
  const editorShowGuidesCtx = useMemo137(() => {
    return {
      editorShowGuides,
      setEditorShowGuides,
      guidesList,
      setGuidesList,
      draggingGuideId,
      moveGuidePointerRef,
      setDraggingGuideId,
      shouldCreateGuideRef,
      shouldDeleteGuideRef,
      hoveredGuideId,
      setHoveredGuideId
    };
  }, [
    editorShowGuides,
    setEditorShowGuides,
    guidesList,
    draggingGuideId,
    hoveredGuideId
  ]);
  return /* @__PURE__ */ jsx247(EditorShowGuidesContext.Provider, {
    value: editorShowGuidesCtx,
    children
  });
};

// src/components/ShowOutlinesProvider.tsx
import { useCallback as useCallback130, useEffect as useEffect91, useMemo as useMemo138, useState as useState97 } from "react";
import { jsx as jsx248 } from "react/jsx-runtime";
var browserStudioPointerLeaveEvent = "remotion-browser-studio-pointerleave";
var ShowOutlinesProvider = ({ children }) => {
  const [editorShowOutlines, setEditorShowOutlinesState] = useState97(() => loadEditorShowOutlinesOption());
  const timelineSequenceHoverStore = useMemo138(() => createTimelineSequenceHoverStore(), []);
  useEffect91(() => {
    const onPointerLeave = () => {
      timelineSequenceHoverStore.setHoveredSequence((hover) => hover?.source === "timeline" ? null : hover);
    };
    window.addEventListener("pointerleave", onPointerLeave);
    window.addEventListener(browserStudioPointerLeaveEvent, onPointerLeave);
    return () => {
      window.removeEventListener("pointerleave", onPointerLeave);
      window.removeEventListener(browserStudioPointerLeaveEvent, onPointerLeave);
    };
  }, [timelineSequenceHoverStore]);
  const setEditorShowOutlines = useCallback130((newValue) => {
    setEditorShowOutlinesState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorShowOutlinesOption(newVal);
      return newVal;
    });
  }, []);
  const editorShowOutlinesCtx = useMemo138(() => {
    return {
      editorShowOutlines,
      setEditorShowOutlines
    };
  }, [editorShowOutlines, setEditorShowOutlines]);
  return /* @__PURE__ */ jsx248(EditorShowOutlinesContext.Provider, {
    value: editorShowOutlinesCtx,
    children: /* @__PURE__ */ jsx248(TimelineSequenceHoverContext.Provider, {
      value: timelineSequenceHoverStore,
      children
    })
  });
};

// src/components/ShowPixelGridProvider.tsx
import { useCallback as useCallback131, useMemo as useMemo139, useState as useState98 } from "react";
import { jsx as jsx249 } from "react/jsx-runtime";
var ShowPixelGridProvider = ({ children }) => {
  const [editorShowPixelGrid, setEditorShowPixelGridState] = useState98(() => loadEditorShowPixelGridOption());
  const setEditorShowPixelGrid = useCallback131((newValue) => {
    setEditorShowPixelGridState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorShowPixelGridOption(newVal);
      return newVal;
    });
  }, []);
  const editorShowPixelGridCtx = useMemo139(() => {
    return {
      editorShowPixelGrid,
      setEditorShowPixelGrid
    };
  }, [editorShowPixelGrid, setEditorShowPixelGrid]);
  return /* @__PURE__ */ jsx249(EditorShowPixelGridContext.Provider, {
    value: editorShowPixelGridCtx,
    children
  });
};

// src/components/ShowRulersProvider.tsx
import { useCallback as useCallback132, useMemo as useMemo140, useState as useState99 } from "react";
import { jsx as jsx250 } from "react/jsx-runtime";
var ShowRulersProvider = ({ children }) => {
  const [editorShowRulers, setEditorShowRulersState] = useState99(() => loadEditorShowRulersOption());
  const setEditorShowRulers = useCallback132((newValue) => {
    setEditorShowRulersState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorShowRulersOption(newVal);
      return newVal;
    });
  }, []);
  const editorShowRulersCtx = useMemo140(() => {
    return {
      editorShowRulers,
      setEditorShowRulers
    };
  }, [editorShowRulers, setEditorShowRulers]);
  return /* @__PURE__ */ jsx250(EditorShowRulersContext.Provider, {
    value: editorShowRulersCtx,
    children
  });
};

// src/components/SnappingProvider.tsx
import { useCallback as useCallback133, useMemo as useMemo141, useState as useState100 } from "react";
import { jsx as jsx251 } from "react/jsx-runtime";
var SnappingProvider = ({ children }) => {
  const [editorSnapping, setEditorSnappingState] = useState100(() => loadEditorSnappingOption());
  const setEditorSnapping = useCallback133((newValue) => {
    setEditorSnappingState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorSnappingOption(newVal);
      return newVal;
    });
  }, []);
  const editorSnappingCtx = useMemo141(() => {
    return {
      editorSnapping,
      setEditorSnapping
    };
  }, [editorSnapping, setEditorSnapping]);
  return /* @__PURE__ */ jsx251(EditorSnappingContext.Provider, {
    value: editorSnappingCtx,
    children
  });
};

// src/components/VisualControls/VisualControlsUndoSync.tsx
import { useContext as useContext130, useEffect as useEffect92 } from "react";
var VisualControlsUndoSync = () => {
  const { subscribeToEvent } = useContext130(StudioServerConnectionCtx);
  const { updateValue } = useContext130(SetVisualControlsContext);
  useEffect92(() => {
    const unsub = subscribeToEvent("visual-control-values-changed", (event) => {
      if (event.type !== "visual-control-values-changed") {
        return;
      }
      for (const entry of event.values) {
        updateValue(entry.id, entry.isUndefined ? undefined : entry.value);
      }
    });
    return () => unsub();
  }, [subscribeToEvent, updateValue]);
  return null;
};

// src/components/ZoomGesturesProvider.tsx
import { useCallback as useCallback134, useMemo as useMemo142, useState as useState101 } from "react";
import { jsx as jsx252 } from "react/jsx-runtime";
var ZoomGesturesProvider = ({ children }) => {
  const [editorZoomGestures, setEditorZoomGesturesState] = useState101(() => loadEditorZoomGesturesOption());
  const setEditorZoomGestures = useCallback134((newValue) => {
    setEditorZoomGesturesState((prevState) => {
      const newVal = newValue(prevState);
      persistEditorZoomGesturesOption(newVal);
      return newVal;
    });
  }, []);
  const editorZoomGesturesCtx = useMemo142(() => {
    return {
      editorZoomGestures,
      setEditorZoomGestures
    };
  }, [editorZoomGestures, setEditorZoomGestures]);
  return /* @__PURE__ */ jsx252(EditorZoomGesturesContext.Provider, {
    value: editorZoomGesturesCtx,
    children
  });
};

// src/components/EditorContexts.tsx
import { jsx as jsx253, jsxs as jsxs147 } from "react/jsx-runtime";
var EditorContexts = ({ children }) => {
  return /* @__PURE__ */ jsx253(ZodProvider, {
    children: /* @__PURE__ */ jsx253(VisualControlsProvider, {
      children: /* @__PURE__ */ jsx253(PreviewServerConnection, {
        children: /* @__PURE__ */ jsxs147(SettingsProvider, {
          children: [
            /* @__PURE__ */ jsx253(VisualControlsUndoSync, {}),
            /* @__PURE__ */ jsxs147(RenderQueueContextProvider, {
              children: [
                /* @__PURE__ */ jsx253(ClientRenderQueueProcessor, {}),
                /* @__PURE__ */ jsx253(CaptionQueueProcessorLoader, {}),
                /* @__PURE__ */ jsx253(VideoMattingQueueProcessorLoader, {}),
                /* @__PURE__ */ jsx253(KeybindingContextProvider, {
                  children: /* @__PURE__ */ jsx253(CheckerboardProvider, {
                    children: /* @__PURE__ */ jsx253(ZoomGesturesProvider, {
                      children: /* @__PURE__ */ jsx253(ShowPixelGridProvider, {
                        children: /* @__PURE__ */ jsx253(ShowRulersProvider, {
                          children: /* @__PURE__ */ jsx253(ShowGuidesProvider, {
                            children: /* @__PURE__ */ jsx253(ShowOutlinesProvider, {
                              children: /* @__PURE__ */ jsx253(SnappingProvider, {
                                children: /* @__PURE__ */ jsx253(PreviewSizeProvider, {
                                  children: /* @__PURE__ */ jsx253(ModalsProvider, {
                                    children: /* @__PURE__ */ jsx253(MediaVolumeProvider, {
                                      children: /* @__PURE__ */ jsx253(PlayerInternals18.PlayerEmitterProvider, {
                                        currentPlaybackRate: null,
                                        children: /* @__PURE__ */ jsx253(SidebarContextProvider, {
                                          children: /* @__PURE__ */ jsx253(FolderContextProvider, {
                                            children: /* @__PURE__ */ jsx253(HighestZIndexProvider, {
                                              children: /* @__PURE__ */ jsx253(SetTimelineInOutProvider, {
                                                children: /* @__PURE__ */ jsx253(ExpandedTracksProvider, {
                                                  children
                                                })
                                              })
                                            })
                                          })
                                        })
                                      })
                                    })
                                  })
                                })
                              })
                            })
                          })
                        })
                      })
                    })
                  })
                })
              ]
            })
          ]
        })
      })
    })
  });
};

// src/components/Notifications/ServerDisconnected.tsx
import { useContext as useContext131 } from "react";
import { jsx as jsx254, jsxs as jsxs148 } from "react/jsx-runtime";
var container55 = {
  position: "fixed",
  justifyContent: "flex-end",
  alignItems: "flex-start",
  display: "flex",
  width: "100%",
  height: "100%",
  flexDirection: "column",
  padding: 30,
  pointerEvents: "none",
  backgroundColor: TRANSPARENT,
  fontFamily: "SF Pro, Arial, Helvetica, sans-serif"
};
var message2 = {
  backgroundColor: SERVER_DISCONNECTED_BACKGROUND,
  color: WHITE,
  paddingLeft: 20,
  paddingRight: 20,
  paddingTop: 12,
  paddingBottom: 12,
  borderRadius: 4,
  boxShadow: SERVER_DISCONNECTED_SHADOW,
  lineHeight: 1.5
};
var inlineCode2 = {
  fontSize: 16,
  fontFamily: "monospace"
};
var pageIsGoingToReload = false;
window.addEventListener("beforeunload", () => {
  pageIsGoingToReload = true;
});
var ServerDisconnected = () => {
  const { previewServerState: ctx } = useContext131(StudioServerConnectionCtx);
  const fav = document.getElementById("__remotion_favicon");
  if (ctx.type !== "disconnected") {
    fav.setAttribute("href", "/favicon.ico");
    return null;
  }
  if (pageIsGoingToReload) {
    return null;
  }
  const base64Favicon = "data:image/png;base64,";
  fav.setAttribute("href", base64Favicon);
  return /* @__PURE__ */ jsx254("div", {
    style: container55,
    className: "css-reset",
    children: /* @__PURE__ */ jsxs148("div", {
      style: message2,
      children: [
        "The studio server has disconnected. ",
        /* @__PURE__ */ jsx254("br", {}),
        window.remotion_studioServerCommand ? /* @__PURE__ */ jsxs148("span", {
          children: [
            "Run",
            " ",
            /* @__PURE__ */ jsx254("code", {
              style: inlineCode2,
              children: window.remotion_studioServerCommand
            }),
            " ",
            "to run it again."
          ]
        }) : /* @__PURE__ */ jsx254("span", {
          children: "Fast refresh will not work."
        })
      ]
    })
  });
};

// src/FastRefreshProvider.tsx
import { REACT_REFRESH_FINISHED_EVENT as REACT_REFRESH_FINISHED_EVENT2 } from "@remotion/studio-shared";
import { useCallback as useCallback135, useEffect as useEffect93, useMemo as useMemo143, useState as useState102 } from "react";
import { flushSync as flushSync2 } from "react-dom";
import { jsx as jsx255 } from "react/jsx-runtime";
var FastRefreshProvider = ({ children }) => {
  const [fastRefreshes, setFastRefreshes] = useState102(0);
  const [manualRefreshes, setManualRefreshes] = useState102(0);
  const increaseManualRefreshes = useCallback135(() => {
    setManualRefreshes((i) => i + 1);
  }, []);
  useEffect93(() => {
    const onReactRefreshFinished = () => {
      flushSync2(() => {
        setFastRefreshes((i) => i + 1);
      });
    };
    window.addEventListener(REACT_REFRESH_FINISHED_EVENT2, onReactRefreshFinished);
    return () => {
      window.removeEventListener(REACT_REFRESH_FINISHED_EVENT2, onReactRefreshFinished);
    };
  }, []);
  const value = useMemo143(() => ({ fastRefreshes, manualRefreshes, increaseManualRefreshes }), [fastRefreshes, manualRefreshes, increaseManualRefreshes]);
  return /* @__PURE__ */ jsx255(FastRefreshContext.Provider, {
    value,
    children
  });
};

// src/helpers/inject-css.ts
import { Internals as Internals113 } from "remotion";

// src/helpers/prism-vsc-dark-plus.ts
var prismVscDarkPlus = `pre[class*="language-"],
code[class*="language-"] {
	color: #d4d4d4;
	font-size: 13px;
	text-shadow: none;
	font-family: Menlo, Monaco, Consolas, "Andale Mono", "Ubuntu Mono", "Courier New", monospace;
	direction: ltr;
	text-align: left;
	white-space: pre;
	word-spacing: normal;
	word-break: normal;
	line-height: 1.5;
	-moz-tab-size: 4;
	-o-tab-size: 4;
	tab-size: 4;
	-webkit-hyphens: none;
	-moz-hyphens: none;
	-ms-hyphens: none;
	hyphens: none;
}

pre[class*="language-"]::selection,
code[class*="language-"]::selection,
pre[class*="language-"] *::selection,
code[class*="language-"] *::selection {
	text-shadow: none;
	background: #264F78;
}

@media print {
	pre[class*="language-"],
	code[class*="language-"] {
		text-shadow: none;
	}
}

pre[class*="language-"] {
	padding: 1em;
	margin: .5em 0;
	overflow: auto;
	background: #1e1e1e;
}

:not(pre) > code[class*="language-"] {
	padding: .1em .3em;
	border-radius: .3em;
	color: #db4c69;
	background: #1e1e1e;
}
/*********************************************************
* Tokens
*/
.namespace {
	opacity: .7;
}

.token.doctype .token.doctype-tag {
	color: #569CD6;
}

.token.doctype .token.name {
	color: #9cdcfe;
}

.token.comment,
.token.prolog {
	color: #6a9955;
}

.token.punctuation,
.language-html .language-css .token.punctuation,
.language-html .language-javascript .token.punctuation {
	color: #d4d4d4;
}

.token.property,
.token.tag,
.token.boolean,
.token.number,
.token.constant,
.token.symbol,
.token.inserted,
.token.unit {
	color: #b5cea8;
}

.token.selector,
.token.attr-name,
.token.string,
.token.char,
.token.builtin,
.token.deleted {
	color: #ce9178;
}

.language-css .token.string.url {
	text-decoration: underline;
}

.token.operator,
.token.entity {
	color: #d4d4d4;
}

.token.operator.arrow {
	color: #569CD6;
}

.token.atrule {
	color: #ce9178;
}

.token.atrule .token.rule {
	color: #c586c0;
}

.token.atrule .token.url {
	color: #9cdcfe;
}

.token.atrule .token.url .token.function {
	color: #dcdcaa;
}

.token.atrule .token.url .token.punctuation {
	color: #d4d4d4;
}

.token.keyword {
	color: #569CD6;
}

.token.keyword.module,
.token.keyword.control-flow {
	color: #c586c0;
}

.token.function,
.token.function .token.maybe-class-name {
	color: #dcdcaa;
}

.token.regex {
	color: #d16969;
}

.token.important {
	color: #569cd6;
}

.token.italic {
	font-style: italic;
}

.token.constant {
	color: #9cdcfe;
}

.token.class-name,
.token.maybe-class-name {
	color: #4ec9b0;
}

.token.console {
	color: #9cdcfe;
}

.token.parameter {
	color: #9cdcfe;
}

.token.interpolation {
	color: #9cdcfe;
}

.token.punctuation.interpolation-punctuation {
	color: #569cd6;
}

.token.boolean {
	color: #569cd6;
}

.token.property,
.token.variable,
.token.imports .token.maybe-class-name,
.token.exports .token.maybe-class-name {
	color: #9cdcfe;
}

.token.selector {
	color: #d7ba7d;
}

.token.escape {
	color: #d7ba7d;
}

.token.tag {
	color: #569cd6;
}

.token.tag .token.punctuation {
	color: #808080;
}

.token.cdata {
	color: #808080;
}

.token.attr-name {
	color: #9cdcfe;
}

.token.attr-value,
.token.attr-value .token.punctuation {
	color: #ce9178;
}

.token.attr-value .token.punctuation.attr-equals {
	color: #d4d4d4;
}

.token.entity {
	color: #569cd6;
}

.token.namespace {
	color: #4ec9b0;
}
/*********************************************************
* Language Specific
*/

pre[class*="language-javascript"],
code[class*="language-javascript"],
pre[class*="language-jsx"],
code[class*="language-jsx"],
pre[class*="language-typescript"],
code[class*="language-typescript"],
pre[class*="language-tsx"],
code[class*="language-tsx"] {
	color: #9cdcfe;
}

pre[class*="language-css"],
code[class*="language-css"] {
	color: #ce9178;
}

pre[class*="language-html"],
code[class*="language-html"] {
	color: #d4d4d4;
}

.language-regex .token.anchor {
	color: #dcdcaa;
}

.language-html .token.punctuation {
	color: #808080;
}
/*********************************************************
* Line highlighting
*/
pre[class*="language-"] > code[class*="language-"] {
	position: relative;
	z-index: 1;
}

.line-highlight.line-highlight {
	background: #f7ebc6;
	box-shadow: inset 5px 0 0 #f7d87c;
	z-index: 0;
}
`;

// src/helpers/inject-css.ts
var makeDefaultGlobalCSS = () => {
  const dragAreaFactor = 2;
  const fromMiddle = 50 / dragAreaFactor;
  return `
	  html {
	    --remotion-cli-internals-blue: ${BLUE};
	    --remotion-cli-internals-blue-hovered: ${BLUE_HOVERED};
	    overscroll-behavior-y: none;
	  }

  body {
    overscroll-behavior-y: none;
    /* Override Chakra UI position: relative on body */
    position: static !important;
  }

  .remotion-splitter {
    user-select: none;
    -webkit-user-select: none;
  }

  .remotion-splitter-horizontal {
    cursor: row-resize;
    transform: scaleY(${dragAreaFactor});
    background: linear-gradient(
      to bottom,
      ${TRANSPARENT} ${50 - fromMiddle}%,
      ${BLACK} ${50 - fromMiddle}%,
      ${BLACK} ${50 + fromMiddle}%,
      ${TRANSPARENT} ${50 + fromMiddle}%
    );
  }

  .remotion-splitter-vertical {
    cursor: col-resize;
    transform: scaleX(${dragAreaFactor});
    background: linear-gradient(
      to right,
      ${TRANSPARENT} ${50 - fromMiddle}%,
      ${BLACK} ${50 - fromMiddle}%,
      ${BLACK} ${50 + fromMiddle}%,
      ${TRANSPARENT} ${50 + fromMiddle}%
    );
  }

  input::-webkit-outer-spin-button,
  input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  input:focus,
  textarea:focus,
  button:focus:not(.__remotion_input_dragger):not(.__remotion_color_swatch):not(.__remotion-inspector-section-title):not(.${FOCUS_VISIBLE_ONLY_CLASS_NAME}):not(.__remotion-timeline-expand-arrow-button),
  a:focus {
	    outline: none;
	    box-shadow: ${FOCUS_BOX_SHADOW};
	  }

  .__remotion-composition-selector-item:focus,
  .__remotion-inspector-quick-action:focus,
  .__remotion-inspector-section-title:focus,
  .${FOCUS_VISIBLE_ONLY_CLASS_NAME}:focus,
  .__remotion-timeline-expand-arrow-button:focus {
    outline: none;
    box-shadow: none;
  }

  .__remotion-composition-selector-item:focus-visible,
  .__remotion-inspector-quick-action:focus-visible,
  .__remotion-inspector-section-title:focus-visible,
  .${FOCUS_VISIBLE_ONLY_CLASS_NAME}:focus-visible,
  .__remotion-timeline-expand-arrow-button:focus-visible {
    box-shadow: ${FOCUS_BOX_SHADOW};
  }

  .__remotion_color_swatch:focus {
    outline: none;
  }

	  .__remotion_input_dragger:focus-visible {
	    outline: none;
	    box-shadow: ${FOCUS_BOX_SHADOW};
	  }

	  .__remotion_thumb,
	  .__remotion_thumb::-webkit-slider-thumb {
	    -webkit-appearance: none;
	    -webkit-tap-highlight-color: ${TRANSPARENT};
	  }

	  .__remotion_thumb {
	    appearance: none;
	    background: transparent;
	    border: 0;
	    height: 100%;
	    left: 0;
	    margin: 0;
	    outline: none;
	    padding: 0;
	    pointer-events: none;
	    position: absolute;
	    top: 0;
	    width: 100%;
	    z-index: 2;
	  }

	  .__remotion_thumb::-moz-range-track {
	    background: transparent;
	    border: 0;
	    height: 6px;
	  }

	  .__remotion_thumb::-webkit-slider-runnable-track {
	    background: transparent;
	    border: 0;
	    height: 6px;
	  }

	  /* For Firefox browsers */
	  .__remotion_thumb::-moz-range-thumb {
	    appearance: none;
	    border: 0;
	    border-radius: 50%;
	    cursor: pointer;
	    height: 14px;
	    width: 14px;
	    pointer-events: all;
	    background-color: ${WHITE};
	    position: relative;
	  }

	  /* For Chrome browsers */
	  .__remotion_thumb::-webkit-slider-thumb {
	    border: 0;
	    border-radius: 50%;
	    cursor: pointer;
	    height: 14px;
	    margin-top: -4px;
	    width: 14px;
	    pointer-events: all;
	    background-color: ${WHITE};
	    position: relative;
	  }

	.__remotion_input_dragger:hover > span:first-child {
    color: var(--remotion-cli-internals-blue-hovered) !important;
  }

  .${DEFAULT_PROPS_PATH_ACTIVE_CLASSNAME} span {
    color: var(--remotion-cli-internals-blue) !important;
    transition: color 0.2s ease-in-out;
  }

  ${makeHoverableCSS()}
  `.trim();
};
var injected = false;
var injectCSS = () => {
  if (injected) {
    return;
  }
  Internals113.CSSUtils.injectCSS(prismVscDarkPlus);
  Internals113.CSSUtils.injectCSS(makeDefaultGlobalCSS());
  injected = true;
};

// src/ResolveCompositionConfigInStudio.tsx
import {
  useCallback as useCallback136,
  useContext as useContext132,
  useEffect as useEffect94,
  useImperativeHandle as useImperativeHandle7,
  useMemo as useMemo144,
  useState as useState103
} from "react";
import { getInputProps, Internals as Internals115 } from "remotion";
import { jsx as jsx256 } from "react/jsx-runtime";
var ResolveCompositionConfigInStudio = ({ children }) => {
  const [currentRenderModalComposition, setCurrentRenderModalComposition] = useState103(null);
  const { compositions, canvasContent, currentCompositionMetadata } = useContext132(Internals115.CompositionManager);
  const { fastRefreshes, manualRefreshes } = useContext132(FastRefreshContext);
  if (manualRefreshes) {}
  const selectedComposition = useMemo144(() => {
    return compositions.find((c) => canvasContent && canvasContent.type === "composition" && canvasContent.compositionId === c.id);
  }, [canvasContent, compositions]);
  const renderModalComposition = compositions.find((c) => c.id === currentRenderModalComposition);
  const { props: allEditorProps } = useContext132(Internals115.EditorPropsContext);
  const env = Internals115.getRemotionEnvironment();
  const inputProps = useMemo144(() => {
    return typeof window === "undefined" || env.isPlayer ? {} : getInputProps() ?? {};
  }, [env.isPlayer]);
  const [resolvedConfigs, setResolvedConfigs] = useState103({});
  const selectedEditorProps = useMemo144(() => {
    return selectedComposition ? allEditorProps[selectedComposition.id] ?? {} : {};
  }, [allEditorProps, selectedComposition]);
  const renderModalProps = useMemo144(() => {
    return renderModalComposition ? allEditorProps[renderModalComposition.id] ?? {} : {};
  }, [allEditorProps, renderModalComposition]);
  const hasResolution = Boolean(currentCompositionMetadata);
  const doResolution = useCallback136(({
    calculateMetadata,
    combinedProps,
    compositionDurationInFrames,
    compositionFps,
    compositionHeight,
    compositionId,
    compositionWidth,
    defaultProps
  }) => {
    const controller = new AbortController;
    if (hasResolution) {
      return controller;
    }
    const { signal } = controller;
    const result = Internals115.resolveVideoConfigWithMetadataOrCatch({
      compositionId,
      calculateMetadata,
      inputProps: combinedProps,
      signal,
      defaultProps,
      compositionDurationInFrames,
      compositionFps,
      compositionHeight,
      compositionWidth
    });
    if (result.type === "error") {
      setResolvedConfigs((r) => ({
        ...r,
        [compositionId]: {
          type: "error",
          error: result.error
        }
      }));
      return controller;
    }
    const promOrNot = result.result;
    if (typeof promOrNot === "object" && "then" in promOrNot) {
      setResolvedConfigs((r) => {
        const prev = r[compositionId];
        if (prev?.type === "success" || prev?.type === "success-and-refreshing") {
          return {
            ...r,
            [compositionId]: {
              type: "success-and-refreshing",
              result: prev.result,
              metadataSource: prev.metadataSource
            }
          };
        }
        return {
          ...r,
          [compositionId]: {
            type: "loading"
          }
        };
      });
      promOrNot.then((c) => {
        if (controller.signal.aborted) {
          return;
        }
        setResolvedConfigs((r) => ({
          ...r,
          [compositionId]: {
            type: "success",
            result: c.videoConfig,
            metadataSource: c.metadataSource
          }
        }));
      }).catch((err) => {
        if (controller.signal.aborted) {
          return;
        }
        setResolvedConfigs((r) => ({
          ...r,
          [compositionId]: {
            type: "error",
            error: err
          }
        }));
      });
    } else {
      setResolvedConfigs((r) => ({
        ...r,
        [compositionId]: {
          type: "success",
          result: promOrNot.videoConfig,
          metadataSource: promOrNot.metadataSource
        }
      }));
    }
    return controller;
  }, [hasResolution]);
  const currentComposition = canvasContent?.type === "composition" ? canvasContent.compositionId : null;
  const resolveComposition = useCallback136(async (compositionId) => {
    const composition = compositions.find((c) => c.id === compositionId);
    if (!composition) {
      throw new Error(`Could not find composition with id ${compositionId}`);
    }
    const editorProps = allEditorProps[composition.id] ?? {};
    const defaultProps = {
      ...composition.defaultProps ?? {},
      ...editorProps ?? {}
    };
    const combinedProps = {
      ...defaultProps,
      ...inputProps ?? {}
    };
    const controller = new AbortController;
    const result = Internals115.resolveVideoConfigWithMetadataOrCatch({
      compositionId: composition.id,
      calculateMetadata: composition.calculateMetadata,
      inputProps: combinedProps,
      signal: controller.signal,
      defaultProps,
      compositionDurationInFrames: composition.durationInFrames ?? null,
      compositionFps: composition.fps ?? null,
      compositionHeight: composition.height ?? null,
      compositionWidth: composition.width ?? null
    });
    if (result.type === "error") {
      throw result.error;
    }
    const resolved = await Promise.resolve(result.result);
    setResolvedConfigs((configs) => ({
      ...configs,
      [composition.id]: {
        type: "success",
        result: resolved.videoConfig,
        metadataSource: resolved.metadataSource
      }
    }));
    return resolved.videoConfig;
  }, [allEditorProps, compositions, inputProps]);
  useImperativeHandle7(Internals115.resolveCompositionsRef, () => {
    return {
      setCurrentRenderModalComposition: (id) => {
        setCurrentRenderModalComposition(id);
      },
      resolveComposition,
      reloadCurrentlySelectedComposition: () => {
        if (!currentComposition) {
          return;
        }
        const composition = compositions.find((c) => c.id === currentComposition);
        if (!composition) {
          throw new Error(`Could not find composition with id ${currentComposition}`);
        }
        const editorProps = allEditorProps[currentComposition] ?? {};
        const defaultProps = {
          ...composition.defaultProps ?? {},
          ...editorProps ?? {}
        };
        const props = {
          ...defaultProps,
          ...inputProps ?? {}
        };
        doResolution({
          defaultProps,
          calculateMetadata: composition.calculateMetadata,
          combinedProps: props,
          compositionDurationInFrames: composition.durationInFrames ?? null,
          compositionFps: composition.fps ?? null,
          compositionHeight: composition.height ?? null,
          compositionWidth: composition.width ?? null,
          compositionId: composition.id
        });
      }
    };
  }, [
    allEditorProps,
    compositions,
    currentComposition,
    doResolution,
    inputProps,
    resolveComposition
  ]);
  const isTheSame = selectedComposition?.id === renderModalComposition?.id;
  const currentDefaultProps = useMemo144(() => {
    return {
      ...selectedComposition?.defaultProps ?? {},
      ...selectedEditorProps ?? {}
    };
  }, [selectedComposition?.defaultProps, selectedEditorProps]);
  const originalProps = useMemo144(() => {
    return {
      ...currentDefaultProps,
      ...inputProps ?? {}
    };
  }, [currentDefaultProps, inputProps]);
  const canResolve = selectedComposition && Boolean(selectedComposition.calculateMetadata);
  const shouldIgnoreUpdate = typeof window !== "undefined" && window.remotion_ignoreFastRefreshUpdate && fastRefreshes <= window.remotion_ignoreFastRefreshUpdate;
  useEffect94(() => {
    if (shouldIgnoreUpdate) {
      return;
    }
    if (canResolve) {
      const controller = doResolution({
        calculateMetadata: selectedComposition.calculateMetadata,
        combinedProps: originalProps,
        compositionDurationInFrames: selectedComposition.durationInFrames ?? null,
        compositionFps: selectedComposition.fps ?? null,
        compositionHeight: selectedComposition.height ?? null,
        compositionWidth: selectedComposition.width ?? null,
        defaultProps: currentDefaultProps,
        compositionId: selectedComposition.id
      });
      return () => {
        controller.abort();
      };
    }
  }, [
    canResolve,
    currentDefaultProps,
    doResolution,
    originalProps,
    selectedComposition?.calculateMetadata,
    selectedComposition?.durationInFrames,
    selectedComposition?.fps,
    selectedComposition?.height,
    selectedComposition?.id,
    selectedComposition?.width,
    shouldIgnoreUpdate
  ]);
  useEffect94(() => {
    if (renderModalComposition && !isTheSame) {
      const combinedProps = {
        ...renderModalComposition.defaultProps ?? {},
        ...renderModalProps ?? {},
        ...inputProps ?? {}
      };
      const controller = doResolution({
        calculateMetadata: renderModalComposition.calculateMetadata,
        compositionDurationInFrames: renderModalComposition.durationInFrames ?? null,
        compositionFps: renderModalComposition.fps ?? null,
        compositionHeight: renderModalComposition.height ?? null,
        compositionId: renderModalComposition.id,
        compositionWidth: renderModalComposition.width ?? null,
        defaultProps: currentDefaultProps,
        combinedProps
      });
      return () => {
        controller.abort();
      };
    }
  }, [
    currentDefaultProps,
    doResolution,
    inputProps,
    isTheSame,
    renderModalComposition,
    renderModalProps
  ]);
  const resolvedConfigsIncludingStaticOnes = useMemo144(() => {
    const staticComps = compositions.filter((c) => {
      return c.calculateMetadata === null;
    });
    return {
      ...resolvedConfigs,
      ...staticComps.reduce((acc, curr) => {
        return {
          ...acc,
          [curr.id]: {
            type: "success",
            result: { ...curr, defaultProps: curr.defaultProps ?? {} },
            metadataSource: {
              durationInFrames: "composition",
              fps: "composition",
              height: "composition",
              width: "composition"
            }
          }
        };
      }, {})
    };
  }, [compositions, resolvedConfigs]);
  return /* @__PURE__ */ jsx256(Internals115.ResolveCompositionContext.Provider, {
    value: resolvedConfigsIncludingStaticOnes,
    children
  });
};

// src/Studio.tsx
import { jsx as jsx257, jsxs as jsxs149 } from "react/jsx-runtime";
var getServerDisconnectedDomElement = () => {
  return document.getElementById("server-disconnected-overlay");
};
var StudioInner = ({ rootComponent, readOnly }) => {
  return /* @__PURE__ */ jsx257(Internals116.CompositionManagerProvider, {
    onlyRenderComposition: null,
    currentCompositionMetadata: null,
    initialCompositions: [],
    initialCanvasContent: null,
    children: /* @__PURE__ */ jsx257(Internals116.RemotionRootContexts, {
      frameState: null,
      audioEnabled: window.remotion_audioEnabled,
      videoEnabled: window.remotion_videoEnabled,
      logLevel: window.remotion_logLevel ?? "info",
      numberOfAudioTags: window.remotion_numberOfAudioTags,
      audioLatencyHint: window.remotion_audioLatencyHint ?? "playback",
      previewSampleRate: window.remotion_previewSampleRate,
      _experimentalKeepAudioContextAlive: window.remotion_experimentalKeepAudioContextAlive ?? false,
      children: /* @__PURE__ */ jsx257(StaticFilesProvider, {
        children: /* @__PURE__ */ jsx257(ResolveCompositionConfigInStudio, {
          children: /* @__PURE__ */ jsxs149(EditorContexts, {
            children: [
              /* @__PURE__ */ jsx257(CompositionListProvider, {
                children: /* @__PURE__ */ jsx257(Editor, {
                  readOnlyStudio: readOnly,
                  Root: rootComponent
                })
              }),
              readOnly ? null : createPortal3(/* @__PURE__ */ jsx257(ServerDisconnected, {}), getServerDisconnectedDomElement())
            ]
          })
        })
      })
    })
  });
};
var Studio = ({ rootComponent, readOnly }) => {
  useLayoutEffect30(() => {
    injectCSS();
  }, []);
  return /* @__PURE__ */ jsx257(FastRefreshProvider, {
    children: /* @__PURE__ */ jsx257(StudioInner, {
      rootComponent,
      readOnly
    })
  });
};

export { markErrorMessageAsLoggedByServer, setErrorsRef, Overlay, didUnmountReactApp, startReportingRuntimeErrors, Studio };
